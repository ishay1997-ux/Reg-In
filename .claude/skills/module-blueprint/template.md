Hi Claude,

We are opening **Module [MODULE_NUMBER]: [MODULE_NAME]** (feature branch `[BRANCH_NAME]`).
Your task in this turn: act as Lead Software Architect and produce the module's **micro-guide** — the machine-actionable blueprint that a *future Claude session* will execute step by step.

### 🎯 Role & Audience (read this first — it shapes everything)
The micro-guide's **primary reader is a future Claude Code session with zero memory of this conversation**. Ishay will only: paste prompts, answer decision questions, and approve at phase boundaries. Therefore:
- Every step must be executable by Claude **without asking anything** that the guide could have answered.
- Write the guide in **full English** (machine-first precision). Hebrew appears ONLY as data — DB values (module/role names like 'לקוחות', 'מנכ"ל'), UI strings, and user-facing message texts. All chat reports to Ishay are always in **Hebrew**.
- Do not write for a human following a tutorial. Write for an agent: exact paths, exact commands, expected outputs, unambiguous stop conditions.

### ⚠️ Workflow & Plan Rules
1. **Plan-then-Approve:** present everything in chat for Ishay's review, in this exact order: (a) **Plain-Hebrew capabilities summary** (🎁 below) → (b) Sequence & Dependency Assessment incl. the Integration Impact statement → (c) Questions & Assumptions triage → (d) **🎤 PM Interview** (section below — its answers are baked into the Decisions Ledger before (f)) → (e) Claude's Optimization Proposals (chat-only — NOT saved into the micro-guide unless explicitly approved into a step) → (f) the full 9-section blueprint. Write `docs/micro_guides/module-[MODULE_NUMBER].md` to disk **only after explicit approval**, containing only the approved 9 sections.
🔴 **And the approval gate is Plan Mode, not this sentence.** ‏**The prose version of this rule already existed and was still breached — Ishay caught it on 29/07/2026** — so the enforcement moved out of Claude's judgement and into the tool layer. **The hardened wording is the SSOT in `.claude/skills/module-build/SKILL.md` (the 🔒 Plan-Mode gate); read it there and obey it, and do not weaken it into a self-check: any marker whose state Claude itself controls is a reminder, not a wall.** *(Corrected 05/08/2026 — this template still carried the pre-29/07 version, i.e. the one that was actually breached, and the next blueprint would have written the broken form into module 4's guide.)*
🔴 **Interruption-safety, and it costs Ishay directly:** the PM interview (🎤 below) happens BEFORE anything may be written to disk. **A blueprint session that dies after the interview loses every answer, and Ishay gets interviewed again from scratch.** ⇒ **As each interview answer is confirmed, append it immediately to a scratch file — `docs/micro_guides/module-[MODULE_NUMBER]-interview.md`** — this is an explicit exception to the approval gate above, because it records what Ishay already said rather than proposing anything. Fold it into the Decisions Ledger at approval and delete it. **Fresh-context blueprint review (iron rule 2ב extended to blueprints):** before presenting the 9-section blueprint for approval, dispatch an internal fresh-context reviewer subagent (no memory of this turn) that audits it against the iron rules, `STATUS.md`, `db_roadmap.md`, the §6/§7 registries, and "every 🚧 capability carries its `🚧 מN` token with a matching §6 line" — the same adversarial pre-submission check plans get under rule 2ב. **Plus two lenses beyond compliance-checking (ported from a sister project — field experience: these catch what rule/spec-coverage checking alone misses):** the **Contrarian** lens ("what's the strongest case this breaks something that already works?") and the **Outsider** lens (a REG-IN staff user with zero context — does this screen/flow actually make sense to *them*, without knowing how it was built?). Report findings from both explicitly, not folded silently into the compliance findings. **Spec-coverage cross-check (MANDATORY — the enforcement that catches silent omissions; the screen-to-column audit alone only checks numbers already IN the blueprint, so a spec element the blueprint never mentions slips through):** the reviewer FIRST extracts the module's *required-list* directly from its C5 screen(s)+process, C6 table(s), and mockups — every field · action · displayed number/metric · validation rule · process rule — reading the spec blind to the draft; THEN cross-checks that each required item maps to a build step OR a logged deferral (a §7 item / a `🚧 מN`). A spec/mockup item with neither a step nor a deferral is a **silent-omission finding**; an item the blueprint builds but the spec does NOT require is an **invented-beyond-spec finding** (must carry a "deviation from 5.x" note). Mockup-only details are limited-liability — flag them for confirm-intent, do not treat as required. Fix its findings before presenting. Immediately after saving, in the same session: update `STATUS.md`'s row for this module (📘 blueprint ready + link to the guide) and run the end-of-session protocol — a saved blueprint that STATUS doesn't point to is invisible to the next session.
1ב. **Render the human-facing presentation as HTML, ordered by volatility (Ishay's own working method):** the parts he actually reads and rules on — (a) the capabilities summary, (c) the triage, (d) the PM-interview outcomes — render as one HTML page via the `Artifact` tool, not chat text alone: a page he can actually read, not a wall of Hebrew paragraphs. **Lead with the decisions most likely to change** (the ones carrying the most product judgment / least certainty), not the settled ones — so if Ishay flips one, it flips before the full blueprint (f) gets written around it. The 9-section machine blueprint (f) itself stays exactly as defined by the Role & Audience section above (English, chat/file) — its reader is a future zero-memory Claude session, not Ishay, so it does not move into the HTML render.

2. **Grounding & Citations:** no chat memory, no assumptions. Every factual claim about existing code, DB, or config MUST cite a source you read in this turn — file + line for code, file + section (e.g. `PROJECT_MASTER.md §3`) for docs; filename alone is acceptable only when the whole file is the subject (e.g. a migration).
3. **Hierarchy of Truth:** `docs/schema.sql` (highest) ➔ approved spec `docs/reference_spec/C5_*`+`C6_*` via `PROJECT_MASTER.md` ➔ mockups (`docs/mockups/`) ➔ previous micro-guides (lowest). Frozen spec files are never edited; intentional deviations are recorded in living docs with a "deviation from 5.x" note.
4. Read before writing: `CLAUDE.md`, `STATUS.md`, `docs/PROJECT_MASTER.md` (sections [RELEVANT_SECTIONS], the full §7 registry, **and §6 — the cross-module debt registry: find every item targeted at THIS module and schedule it as explicit steps in the blueprint**), `docs/schema.sql`, **`docs/db_roadmap.md` — the consolidated DB map: this module's Lane-A rows are mandatory blueprint steps, its Lane-B rows targeting this module are mandatory triage inputs, and its §6 per-table index is the checklist for every table you touch**, `docs/architecture_and_qa_roadmap.md` (the canonical DoD that section 7 instantiates), the current codebase, and `docs/micro_guides/module-1.md` as the format exemplar.

### 🎁 Plain-Hebrew Capabilities Summary (present FIRST)
Before any technical material, give Ishay a short Hebrew table answering "מה המשתמשת תקבל בסוף המודול": every user-facing action, marked ✅ built now / 🚧 partially (frame now, completed by module N — carries a `🚧 מN` token registered in §6 per iron rule 15, authored in the §2 table below; also say why the data/logic only exists there) / ❌ not now (target module). Include which roles can use the screen. This is the non-technical contract the human approves; the blueprint's section-2 capabilities table is its machine mirror.

### 🔄 Sequence & Dependency Validation
Before blueprinting, challenge the workflow itself: is building *this module now* sound given data-model cross-references? Missing prerequisites? Will this architecture strain upcoming modules? State your conclusion first.
**Integration Impact statement (mandatory — "does this break anything?"):** list every EXISTING file the module will modify (with what changes), prove the rest is additive (new files/tables/policies only), name what could plausibly break, and point to the regression step that guards it (existing unit+E2E suites staying green is the minimum safety net). **Cross-module collision check:** if another micro-guide is still open (per STATUS.md — e.g. work paused mid-module, or a parallel worktree), read its "Files to create" / "Existing files to touch/reuse" lists and cross-check against this module's — any overlap on an EXISTING file is a 🛑 item raised with Ishay BEFORE the blueprint is approved, not discovered at merge time.

### 🗡️ DB Design Challenge (mandatory for any module that touches the DB)
Before Phase 1 is blueprinted, run an ADVERSARIAL pass on this module's tables *as designed* — the schema, the C6 ERD, and prior decisions are challengeable inputs, not gospel. The Hierarchy of Truth (rule 3 above) settles *build-time conflicts*; it is not immunity from challenge — a design finding goes to the triage below (🛑/⏳, or a proposed new §7 item per its 📌 bullet), never silently absorbed. Work through, for every table this module reads or writes:
- **Keys & mutability:** PK choice, ON UPDATE behavior, PII in keys (§7.64 policy once ruled).
- **Relationships & lineage:** can every derived/duplicated row be traced back to its source row? (the `logistics`⟷`quote_services` lesson, §7.72).
- **Lifecycle:** every status the processes need exists; state transitions have defined writers (§7.44) and time anchors; locked states are DB-enforced, not UI-hoped (§7.50).
- **Screen-to-column audit — "every number a screen displays has a named source column or computation":** walk this module's C5 screens; a displayed figure with NO data home is a 🛑 finding (the C5:833 "חיוב על שינויים" lesson — a spec'd screen showed a number no table could produce).
- **Derived-vs-stored coherence:** what freezes, when, and what stays live (§7.78 "snapshot as a unit").
- **Permissions↔RLS mapping:** does the module write columns on a table another module owns? (§7.63; see the rollout matrix in `db_roadmap.md` §7).
- **Files/Storage, temporal columns (timestamptz, §7.56), and the full Migration Design Checklist (`db_roadmap.md` §1) for every migration you blueprint.**
Also read `db_roadmap.md` §9 (known reference-spec defects) so you don't re-derive schema from a known-bad spec passage.

🔴 **Required output — one line per sub-check above, INCLUDING the ones that found nothing.** Write them into the micro-guide under a `### 🗡️` heading: *sub-check · what you examined · finding, or the words "אין ממצאים"*. **Without this the pass has no trace: a session that ran all seven and found nothing produces output identical to a session that skipped every one** — and Ishay cannot read code to tell them apart. *(Measured 05/08/2026: `🗡️` appears **zero** times in `micro_guides/module-2.md` and `module-3.md`. Both modules touched the DB. Nobody can now say whether the pass ever ran.)* **How we'll know it's fixed:** `grep '🗡️' docs/micro_guides/module-4.md` returns ≥1.

### 🎯 Triage of Gaps & Questions
While reading, collect every contradiction/omission/ambiguity between spec, schema, and mockups. Then, BEFORE writing the blueprint, act on each by category — never decide an open question yourself (iron rule 1):
- 🛑 **Must decide NOW** (blocks Phase 1 or a later phase) — **ASK Ishay directly, in-session, via the AskUserQuestion tool** — do not just list-and-hope. Batch them into as few rounds as possible; give concrete options where the choice is bounded (an "Other" free-text path is always available). Bake each answer into the Decisions Ledger (section 3) before presenting the blueprint. **Write-back rule (iron rule 13(א)):** an answer that rules an existing PROJECT_MASTER §7 item is ALSO marked ruled in §7 itself (+ STATUS.md's open-items line) in this same session — the ledger cites; §7 stays the SSOT. A ledger that says "ruled" while §7 still says "open" is exactly the drift this project fights. **§7-first is only the FIRST leg of rule 13's full ripple — run the rest too, in the same session:** the code/DB where the ruling is encoded if it already exists (13ב) · every other place that cites or mirrors it beyond this blueprint (13ג) · `architecture_and_qa_roadmap.md` if the ruling is QA/CI/DoD-related (13ד) · `CLAUDE_CODE_LOG.md` alongside STATUS (13ה). **This blueprint's own 🛑 triage round IS a "§7-ruling gate" by rule 13's own definition ⇒ 13ז applies: RECOMMEND to Ishay that he run `regin-docs-sync`, say why now, and wait. 🔴 Claude never runs routines — Ishay clicks "Run now" himself (his ruling 29/07/2026; `docs/CLAUDE.md` rule 13ז is the SSOT for this).** *(Corrected 05/08/2026: this line read "run `regin-docs-sync` before ending the session" — `module-close`, `post-merge` and `section7-rulings` were all fixed on 29/07 and **this one survived the dedicated contradiction sweep because it sits inside a 2,500-character line.**)* **Question style — recommendation FIRST** (the standing default in `~/.claude/CLAUDE.md`, which loads every session): each question is presented in Hebrew as **(1) your reasoned recommendation** · (2) one line of background — why it comes up now and what it blocks · (3) the alternatives in one line each, with what actually happens if chosen · (4) full detail only if Ishay says "פרט לי". Frame it as a concrete story — a named person, a clock time, real numbers from the actual data, and the honest trap. Fewer, well-explained questions beat many terse ones; Ishay's default action should be one tap on the recommendation, not an analysis exercise. *(Aligned 29/07/2026 — this template used to put the recommendation LAST, contradicting the global file and `section7-rulings`.)*
- ⏳ **Can decide LATER** — do NOT ask about these now (that defeats the point of deferring). Record each in the Decisions Ledger marked "OPEN — deferred", stating the future module/stage where it becomes critical.
- 🧭 **Deviations from frozen spec** — record in living docs only, never edit `reference_spec/*`.
- 📌 **§7 cross-references** — cite item numbers that block (🛑) vs. relevant-but-deferred (⏳). Never restate their text. A genuinely NEW open question (not yet in §7) is surfaced to Ishay and proposed as a new PROJECT_MASTER §7 item per CLAUDE.md rule 13 — never self-answered.
- **Persistence rule:** every triage item must survive into the saved guide — decided ones in the Decisions Ledger (§3), deferred/new ones in the Decisions Ledger and Deviations & Tech-Debt Log (§9). Nothing you surfaced may live only in chat.

### 🎤 PM Interview (ראיון מנהל-מוצר) — mandatory 👤 product-decision gate BEFORE blueprint approval (Ishay 11/07/2026)
After the 🛑 triage round and BEFORE the blueprint is presented for approval, walk Ishay through the module's **full user journey in simple Hebrew** — who enters the screen(s), what they see on each screen, what happens on every action, and the edge cases — phrased as **understanding-declarations invited for correction** (the same stance as the 🗣️ experience-brief's הצהרת-הבנה: only the product manager knows how the process really works; Claude states what it understood and asks to be corrected). Along the walkthrough: ask **targeted questions** on every unclear or spec-silent point (batch with the 🛑 triage round where natural — fewer, well-explained rounds). **Close with an explicit blindspot pass (Ishay's own working method — make it its own deliverable, not folded silently into the last question):** before this interview ends, actively name your own unknown-unknowns — not only answer what Ishay asked, but state plainly what a senior engineer would think to check about this module that he didn't think to ask, before a single line of code is written. Present it as its own short block ("הנה מה שאני לא בטוח לגביו / מה שיכול להפתיע אותנו במודול הזה"), then ask the mandatory open question: **"על מה לא שאלתי ושווה שתספר לי?"**. Every answer is recorded into the blueprint BEFORE the approval gate — rulings into the Decisions Ledger (§3), still-open points anchored to their steps (the question-anchoring rule). **Why this exists:** extract the not-thought-through-to-the-end cases upfront — knowledge that lives only with the product manager — instead of discovering them mid-build (mid-build surprises are the biggest stall risk; Ishay's ruling).

### 🧠 Optimization Proposals (separate!)
You may suggest patterns/optimizations/edge-cases — but list them under a separate `"Claude's Optimization Proposals"` section for explicit approval. Do NOT bake unapproved suggestions into the numbered steps.

### 📜 Required Micro-Guide Structure (exactly these 9 sections)
Output file: `docs/micro_guides/module-[MODULE_NUMBER].md` (English filename — iron rule 11; English content per Role & Audience above).

1. **🟢 Live Status Header** — module, branch, owner, overall status, "Last updated" (**format `DD/MM/YYYY HH:MM`, taken from the system clock — never guessed**; multiple sessions per day are the norm), "**Active step: N**", and a step→status table using: ⬜ pending · 🔨 in progress · ✅ done · ⏸️ deferred (with target module) · ❌ blocked (with reason). This header is the single line of truth a fresh session reads first.
   *(Historical note: modules 6/12 used to be "shared" between two developers, and this template once specified a dual-owner header with per-developer Active-step lines and an Owner column. Retired 22/07/2026 — single developer, so every module uses the ordinary single Active-step header. Modules 6 and 12 are still the integration points **between modules**; that meaning is unchanged.)*
2. **📦 Context Packet for Claude** — module purpose in ≤3 lines; **"Capabilities delivered vs deferred" table** (capability · what this module delivers · which module completes it · tracked where) — **rule (ENFORCED — iron rule 15 + Stop hook): each partial/deferred capability is marked `🚧 מN` (N = the module that finishes it) and MUST get its byte-matching `🚧 מN` line in `PROJECT_MASTER.md` §6 in the SAME session. The 🚧/§6 mechanism itself — why it exists, the `grep '🚧 מ<their-number>' §6` router future modules read on open, the silent-debt Stop-hook enforcement (`check-docs-updated.sh` 0ג), and 🚧 ≠ ⏳ ≠ 🕓 — is defined once in iron rule 15 (the SSOT); do not restate it here, execute it. This is a 🔻🤖 stop-point and a rule-13 ripple: the moment you write a 🚧 in this table, open §6 and add its line — format `🚧 מN ← מ<this-module> · what · why it lives only there · מקור: micro_guides/module-<this>.md` — then verify `grep '🚧 מN' docs/PROJECT_MASTER.md`.**; map of existing files to touch/reuse (paths + line refs); **Files to create** — the new paths this module authors (components under `src/modules/NN_name/`, `api.js`, `src/lib/*.js` + unit tests, `e2e/*.spec.js`, migration files), so a zero-memory build session knows exactly what to author vs reuse; DB tables + relevant migrations; dependencies on other modules; **🔑 Test Identities (MANDATORY when the module touches RLS or role-based UI — this is the recurring load-bearing gap; do NOT omit):** the 5 seeded test users (resolve `role→email→user_id` LIVE from the seed, never hard-code), the impersonation method for RLS scenarios (`set_config('request.jwt.claims', …)` carrying BOTH `sub` and `email` — one missing key makes every query return 0 rows and *look* like perfect RLS), a **positive control** (a known-`edit` role MUST return ≥1 row — a 0 there means broken impersonation, NOT working RLS), and where UI-login creds live (`.env.local` `E2E_<ROLE>_*`). 🔴 **Verify which `E2E_` pairs ACTUALLY exist before naming any of them — count them, do not assume the set is complete.** *(Harvested from module 3, 05/08/2026: this exact failure was caught **three separate times** — 10/07, 11/07 and again at step 4.1 — because a guide named test identities that were never provisioned. Module 3 fixed it locally and **the fix was never collected back into this template**, which is why it kept recurring.)* **A guide that names a non-existent identity does not fail loudly — every query returns 0 rows and it looks like working RLS.** Pattern to copy: `module-2.md` §2. *(If a future consumer needs the identical block, extract it to a single shared reference and have both Context Packets cite it — SSOT; until the second consumer exists, this template line + the module-2 pattern is enough.)*; spec sections (5.x) + mockup folders; environment facts (supabase client import path `@/supabaseClient`, dev server, RTL, etc.). Everything a zero-memory session needs to start working without exploration.
3. **🧭 Decisions Ledger** — table: §7 item / local decision · the ruling · who · date · which step it unblocks. Cite §7 numbers only; where a ruling's *content* must be restated for execution (e.g. load-bearing SQL, an exact enum mapping), tag the restatement as a managed mirror per iron rule 13: `🔗 מראת §7.N — SSOT: PROJECT_MASTER §7` (regin-docs-sync auto-syncs tagged mirrors; untagged restatements are flagged as drift).
4. **🛡️ Security & Auth Model Statement** (iron rule 9) — how this module leans on Module-1 auth: RLS policies per the §7.21 standard template (state the exact `module_name` string used in the policy), role/permission gates in UI (`SYSTEM_MODULES`/`isAllowed` patterns), session/OAuth handling, and explicitly accepted limitations.
5. **🏗️ Phase & Step Plan** — **opens with a "Model & effort per phase" table (English): for each phase — which model (Opus/Sonnet), how much effort (High/Medium/Low), and why — plus a dedicated row for the Phase-5 closing-audit step (Opus/High: it re-verifies independently)** (instantiates the CLAUDE.md model rule so every build session knows exactly what to run with). Canonical phase order: **Phase 1 DB/RLS → Phase 2 Business Logic (`src/lib/` + `src/modules/NN_name/api.js` + unit tests; iron rule 14: UI never duplicates a formula) → Phase 3 UI → Phase 4 Control & Integration → Phase 5 QA & Handoff — whose LAST step is always a 🔻👤 closing-audit step that runs the closing-audit template (`.claude/skills/module-close/template.md`, invoked by the `module-close` skill) in a FRESH session (independent re-verification → DoD typed-echo sign-off → PR instructions); a blueprint without this step is incomplete** (merge/adapt phases only with a stated reason). **Every Phase-1 migration step embeds the Migration Design Checklist (`docs/db_roadmap.md` §1) in its What-to-do, and its Verification includes a Supabase-advisors run (MCP, read-only) after apply — zero new findings or a written triage note.** Every step specifies: **Goal · Files · What to do · Verification command + expected output · 🔻 stop-point tagged 🤖 or 👤**.
   - **🤖 (default):** Claude verifies by itself (SQL check, unit/E2E test, preview evidence) — and for UI build-units the self-verification is **functional AND visual** (drive the flow in the live preview) with **screenshot(s) attached as the evidence** — reports the evidence and continues. **Mid-phase visual *evidence* checks are 🤖-with-screenshot, never a human wait** (Ishay 11/07/2026): human eyes come at the end-of-phase gates and on design decisions. *(Scope note: the no-wait rule covers verification EVIDENCE only — build-INTENT still gates mid-phase at each unit's 🗣️ experience-brief below; the two are complementary, not contradictory.)*
   - **👤 (human gate):** required ONLY at: end of every phase (incl. the end-of-Phase-3 🎨 review) · §7/product decisions — **including the 🎤 PM-Interview (Workflow rule above) and each build-unit's 🗣️ experience-brief approval** · before applying any migration (shared Supabase project! — the apply gate is a **typed-echo of the migration name**, DB protocol; a plain approval is NOT sufficient to apply, and the gate is NOT "pre-granted" at an earlier step) · anything touching secrets/accounts/OAuth config · final DoD sign-off.
   - **🗣️ Experience-brief (תקציר-חוויה) + PM approval (Hebrew — a 👤 product-decision gate on build-INTENT; NOT a 🤖 evidence gate):** before code is written for each *significant build-unit* (a screen, a component, a table/migration, a feature — coarser than a single step; per-step would be noise), the build session posts a **mandatory simple-Hebrew brief**: **(א) הצהרת-הבנה של הזרימה העסקית** — the business flow as Claude understood it, phrased as an understanding-declaration **explicitly invited for correction** (only Ishay know how the process really works); **(ב)** the **validations** planned for the unit; **(ג)** a **screen description or mockup** of what will be built (+ which files it touches or creates); **(ד)** every **mockup-sourced or spec-silent** detail flagged *"מהמוקאפ / אפיון-שותק — לאישורך"* — **mockups are limited-liability reference (CLAUDE.md hierarchy of truth); a drawn detail may not be what was intended, so a detail whose ONLY source is a mockup (or spec silence) is confirmed, never reproduced blindly. Validation rules fall under (ד) too:** a validation the spec is silent on (a format rule, a required-field choice, a cross-field constraint) is exactly such a detail — where feasible **show the behaviour visually** (mockup / preview screenshot of the validation state); never invent or silently omit a validation. Then the session **WAITS for product-manager (Ishay) approval before writing the unit's code** (ruled by Ishay 11/07/2026 — replaces the earlier narrate-and-continue mechanism). Verification is untouched: 🤖 evidence stays exactly as defined above — this gate is about *what to build*, not *whether it was built right*.
   - **🎨 UX & functional review (end of Phase 3 — a 👤 gate, NOT optional):** the end-of-Phase-3 human gate is a **design + functional review**, not just "does it match the mockup". The build session presents, and Ishay rule on: **(a)** §4 design conformance (palette/layout/RTL); **(b)** functional states present on every screen — loading · empty · no-results · error (with a retry path) · success feedback; **(c)** basic keyboard operability of every primary action + a visible focus ring; **(d)** **validation-completeness** — every field/rule the spec requires is validated, and every spec-silent validation choice was confirmed with Ishay per the 🗣️ rule above; **(e)** the real design question — *"should anything be redesigned / added / removed?"* — the session proposes concrete UX improvements, the human decides. Findings become build steps (now) or `🚧`/§7 deferrals (logged in §9). This is the module's OWN UX pass — it does NOT replace the M12 system-wide usability/cross-browser sweep, and it feeds the §6 QA-matrix "Usability" cell + the §7 DoD UX-&-validation checkbox.
   - **Question-anchoring rule:** every Decisions-Ledger row that is ⏳/OPEN/nod-pending AND shapes a specific step MUST also appear as an inline note on that step, with the nearest 👤 gate instructed to "ask if not yet ruled" (canonical pattern: module-2.md step 1.1's nod-pending extension note). A deferred ledger item with no anchoring step is a closing-audit finding — deferral is safe only when the question is physically attached to the step where it explodes.
   - **⚠️ shared-surface marker:** any step that modifies a file OUTSIDE `src/modules/[MODULE_NUMBER]*_*/` (e.g. `src/lib/**`, `src/App.jsx`, `docs/schema.sql`, `package.json`) is tagged `⚠️ shared-surface` in its title, and its What/Verification must: (a) grep any other OPEN micro-guide for that file — declared there too ⇒ stop and reconcile before editing; (b) shape the change additively (a clearly-bounded per-module block, not a rewrite of an existing region) so even a future git conflict is trivially auto-mergeable. **Handoff rule (CLAUDE.md iron rule 17):** when a 👤 step is a browser action Claude cannot perform (GitHub PR, Supabase Studio), the build session prints plain-Hebrew instructions AND a self-contained "🧩 prompt for Claude-in-Chrome" (full context, no secrets). OAuth/secrets are the exception — always manual, no handoff prompt.
6. **📊 QA Matrix** — planned coverage per test type (Unit · Integration · E2E · Regression · UAT · Security/Pen · Performance · Usability · Compatibility) with an empty "as-run" column that the closing audit fills in. The **Usability** cell is not a generic tick — it is filled from the end-of-Phase-3 🎨 UX & functional review (§5) plus the closing template's UX & Validation Audit. Align to the real infra: Vitest (`npm run test:run`), Playwright specs in `e2e/` (`npm run test:e2e`), CI (`.github/workflows/ci.yml`).
7. **✅ Definition of Done** — checkboxes: the canonical DoD from `docs/architecture_and_qa_roadmap.md` instantiated for this module + module-specific items (row counts, policies, flows) **and a UX-&-validation checkbox** (the end-of-Phase-3 🎨 UX & functional review passed — §4 design · states · RTL · keyboard-basics — AND validation-completeness holds: every spec'd validation implemented, spec-silent ones confirmed with Ishay). Includes: migration applied + `docs/schema.sql` snapshot updated + committed together (DB protocol in `CLAUDE.md`). **Split audit-time-verifiable items (checked at closing) from post-merge items** — "PR opened / CI green / merged" are NOT audit checkboxes (the closing audit must not merge) and NOT Section-6 blockers; list them under a short "post-merge" note, not as checkboxes a truthful audit would be forced to mark ❌. The closing verdict confirms the module is *mergeable* (green verify + no blocker), not *merged*.
8. **🔄 Self-Update Protocol** — the micro-guide's §8 restates **(a)–(d) and (h) verbatim** (the self-update + phase-entry mechanics unique to the build flow); **(e)/(f)/(g) are automatic ripples of CLAUDE.md iron rules (13/15/16 + end-of-session) that already bind every session — the generated §8 CITES them ("(e)–(g): per CLAUDE.md iron rules 13/15/16 + end-of-session protocol") instead of duplicating the text (F1 subtraction: do not restate what the SSOT already enforces; the earlier practice of demanding all eight verbatim produced silent partial copies).** The full rules: (a) at every step transition, update the status header + step table **in the same session, before moving on**; (b) any deviation from the plan gets an inline "↳ as-built" note on the step + a line in section 9; (c) the repo's Stop hook (`.claude/hooks/check-docs-updated.sh`) blocks session end if module code under `src/modules/[MODULE_NUMBER]*_*/` changed but this guide didn't — keep it current, not as an afterthought; (d) end-of-session protocol in `CLAUDE.md` applies (CLAUDE_CODE_LOG → STATUS; `CHANGELOG` retired 23/07/2026); (e) a NEW open question discovered mid-build (business/architectural — not answerable from this guide or the docs) ⇒ stop at the nearest gate, present it in Ishay's question style (background · what the ruling means going forward · options with per-option consequences · a reasoned recommendation), record it in the Decisions Ledger as OPEN + an inline note on the affected step, and register it in PROJECT_MASTER §7 if business-level (prompt P10) — NEVER silently pick an answer and keep building; (f) **applied a migration or discovered a DB gap mid-build ⇒ update the matching row in `docs/db_roadmap.md` in the same session** (parallel of iron rule 15; the Stop hook blocks a session that changed `supabase/migrations/**` without touching the roadmap); (g) **a change that lands on a FUTURE module** (schema, a §7 ruling, or a shared-surface file touching a module not yet built) ⇒ the CLAUDE_CODE_LOG session entry + a `PROJECT_MASTER §6` line (and `db_roadmap §10` for DB) name those module numbers, so the session that opens that module finds the change instead of rediscovering it — the push-side complement of the opening collision check; (h) **on ENTERING a phase:** sweep the Decisions Ledger for OPEN/nod-pending items anchored to this phase's steps and present them to Ishay for a consolidated ruling (P13 style) BEFORE the phase's first step — deferred questions get settled at the phase door, not mid-step where they ambush the build.
(i) **Compaction:** when a phase closes, replace its step-by-step instructions with a done-table (what landed + the evidence that proved it) plus a short carry-forward note for anything later phases must not re-derive. NEVER compact the active phase, §9, or the Decisions Ledger — those are the memory. Archive the pre-compaction copy under `docs/archive/` first. At module close the whole guide compacts to an as-built summary. **Why:** this guide is read in full on every "תמשיך לבנות" turn, so an uncompacted guide taxes every remaining turn of the module. (iron rule 15, full text in `docs/CLAUDE.md`)
9. **📝 Deviations & Tech-Debt Log** — append-only dated lines; summarizes all "↳ as-built" notes and deferred items with their target module/milestone.

> **Display-glyph note (F7):** 🗣️ (experience-brief + build-intent approval), 🎤 (PM interview), 📘 (blueprint ready), 🔒 (module closed) are **presentation markers** used by this template/the guides/STATUS — they are NOT part of the frozen F7 operational set (🔻🤖👤🚧⏳🕓🔗🧩🧊🔮🗡️ — 📣 was retired 22/07/2026 with the move to a single developer) and adding them to F7 is not required; a zero-memory session reads their meaning from where they are defined, not from F7.

### ✍️ Machine-First Writing Rules
- Each step is self-contained: an agent landing on it cold can execute it from the step text + context packet alone.
- Prefer instructions + load-bearing skeletons (SQL policy templates, function signatures) over full code dumps — code is written live at build time, guided by the step. Include full SQL only where exactness is load-bearing (RLS policies, constraints).
- Every 🤖 verification is a *concrete mechanism with an expected result*, never "make sure it works". Acceptable forms: SQL (`select count(*) from customers;` → `0`) · test run (`npm run test:run` → `N passed`) · E2E (`npm run test:e2e` → `N passed`) · preview evidence (snapshot/screenshot of a named screen state). Pick the cheapest form that actually proves the step.
- Hebrew comments in code follow iron rule 3 (why-first) — the guide states this once, not per step.

Now output: your Plain-Hebrew capabilities summary, Sequence Assessment (incl. Integration Impact), Questions & Assumptions (if any), Optimization Proposals, and the complete blueprint per the 9-section structure — in chat, for approval.
