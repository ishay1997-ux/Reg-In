// 🔤 **שני נוסחי-הריקות של 📐10, בהגדרה אחת.**
//
// 🔴 **למה הם עברו לכאן מ-`Envelope.jsx` (16/09/2026):** עד עכשיו הם היו קבועים מקומיים שם,
// עם הערה מפורשת *"לא מיוצאים — אין להם צרכן מחוץ לקובץ"*. **עכשיו יש**: ‏`ChartCard` צריך
// את אותו משפט בדיוק לגרף שחזר בלי דאטה (גרף ריק מצייר מסגרת ⁦260⁩px ריקה בלי מילה — נמדד
// על מ14 בלחיצה אחת על "החודש"). ⇒ העברה, ולא עותק שני: שני משפטים שיסטו זה מזה הם בדיוק
// מה ש-📐10 קיים כדי למנוע.
//
// 🚫 **וקובץ נפרד ולא ייצוא מ-`Envelope.jsx`:** ‏`react-refresh/only-export-components` חוסם
// קובץ שמייצא גם רכיב וגם קבוע — נמדד בריפו הזה פעמיים (`reportsPeriod.js` · `reportsShellContext.js`).
//
// ⚠️ **ההפרדה שאסור לאבד** (מועתקת מהערת-`Envelope` המקורית): `empty` *(ריק-אחרי-סינון)*
// מציע פעולה, `blank` *(ריק-לגמרי)* אומר שאין עדיין דאטה בכלל. מיזוגם היה מציע למשתמשת
// לנקות מסננים שלא יעזרו.

/** §⑥ גלריית-המצבים, מצב 2 — ריק אחרי סינון, כשהמסנן שרוקן הוא **התקופה**. */
export const EMPTY_AFTER_FILTER = 'אין נתונים בתקופה שנבחרה'

/**
 * §⑥ מצב 2 — ריק אחרי סינון, כשהמסנן שרוקן הוא **הלקוח** (17/09/2026, פריט [5]).
 *
 * 🔴 **למה שני נוסחים ולא אחד:** הכרעה 15(ו) נעלה משפט אחד, ‏*"אין נתונים בתקופה
 * שנבחרה"*, כי כשהיא נכתבה מצב 2 נולד מהתקופה בלבד. סבב-התיקונים של 17/09 פתח אותו גם
 * לבחירת-לקוח (`isEmptyAfterLoad` — ‏`rows=0` ולקוח מסונן), **ואז המשפט מצביע על המסנן
 * הלא-נכון**: נמדד על מ21, בחירת לקוח ⁦411⁩ הציגה *"אין נתונים בתקופה שנבחרה"* בעוד
 * התקופה לא נגעה בדבר — ולחיצה על "נקי מסננים" הייתה נראית כמו פעולה על התקופה.
 * 🔑 **וזה בדיוק כלל כ17** (`m11-copy-rules §כ17`): *"נוסח-הריק חייב לתאר את המסנן
 * ש**קיים באמת** בדף הזה"*, עם אותה דוגמה ואותה החלפה — *"בתקופה שנבחרה"* ⇒
 * *"ללקוח שנבחר"*. ⚠️ **ושני המצבים שומרים על "נקי מסננים"**, כי הפעולה זהה.
 * **הנחתי** את הנוסח המדויק (הכרעת-ניסוח היא של ישי) — ר' הדיווח.
 */
export const EMPTY_AFTER_CUSTOMER_FILTER = 'אין נתונים ללקוח שנבחר'

/** ‏`נקי מסננים` — תקדים-קוד חי **בנקבה** (4/4 אתרי-קריאה, `spec.md §1.5`), לא מנוסח מחדש. */
export const CLEAR_FILTERS_LABEL = 'נקי מסננים'

/** §⑥ מצב 3 — ריק לגמרי. ‏`StatTile` נושא את אותה מחרוזת כברירת-מחדל. */
export const EMPTY_ENTIRELY = 'אין נתונים עדיין'
