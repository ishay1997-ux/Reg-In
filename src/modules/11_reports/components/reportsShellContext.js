// 🚪 **מה שהמעטפת יודעת והמשטח אינו — ההרשאות.** קונטקסט אחד, ערך אחד: `canOpenTarget`.
//
// 🔴 **הפגם שהקובץ הזה סוגר, נמדד 16/09/2026 ע"י בונה-הלקוחות:** אריח שיש לו `target`
// ללשונית **ממוסכת** נשאר `<button>` עם `aria-label` *"… — פתחי את הדוח"*, ולחיצה עליו
// **אינה עושה דבר, בלי הודעה** *(נמדד על אריח ⁦4⁩ של מ19 עבור מנהלת-פרויקטים: הרשאת-צפייה
// על 'לקוחות', חסומה על 'כספים')*. כרטיס ⑧19.2 ממליץ על אפשרות **(א) — הערך נשאר, הדלת
// נעלמת**; ‏ת8 כבר קובע שמיסוך אינו נעקף דרך אריח, ו-`ReportsPage.openDoor` אכן בולם —
// אבל הבלימה קורית **אחרי** שהמשתמשת לחצה על משהו שנראה כמו קישור.
//
// 🔑 **ולמה קונטקסט ולא פרופ שיורד שרשרת:** בין `ReportsPage` ל-`KpiTile` יושבים רכיב-הלשונית
// (ארבעה שונים, של ארבעה בעלים) ו-`ReportSurface`. פרופ היה מחייב את ארבעת קובצי-הלשונית
// להעביר אותו — כלומר ארבעה מקומות שבהם אפשר לשכוח, בקבצים שאינם של המעטפת.
//
// 🔴 **ולמה קובץ נפרד עם ייצוא אחד, ולא `Provider` עטוף:** ‏`react-refresh/only-export-
// components` חוסם קובץ שמייצא גם רכיב וגם לא-רכיב (נמדד כאן, אותו מוקש בדיוק שבגללו
// `reportsPeriod.js` נפרד מ-`FiltersBar.jsx`). ⇒ הקונטקסט לבדו כאן, ה-`Provider` נכתב
// ב-`ReportsPage`, והצרכן קורא ל-`useContext` אצלו.
//
// ⚠️ **ובלי ספק — ברירת-המחדל היא "כן":** ‏`null` הוא הערך ההתחלתי, וכל צרכן נופל ל"פתוח"
// (ר' `KpiTile`). רכיב שמרונדר בלי מעטפת (בדיקת-יחידה של לשונית) ממשיך לצייר דלתות בדיוק
// כפי שצייר. הקונטקסט **מצמצם** דלתות ולעולם אינו מוסיף אחת.

import { createContext } from 'react'

/** ‏`{ canOpenTarget(target) => boolean }` · `null` = אין מעטפת ⇒ כל יעד נחשב פתיח. */
export const ReportsShellContext = createContext(null)
