// בלוק-רמזים של לשונית "דיילות" — **רשימת `<Hint>` אחת, ולא ארבעה עותקים של אותו JSX.**
//
// 🔑 **למה רכיב ולא `ids.map` בתוך כל משטח:** ארבעת המשטחים שותלים יחד **38 רמזים** בארבע
// נקודות-הרחבה, וכל אחד מהם היה אותו `map` בדיוק — שכפול ש-jscpd (סף 3%) תופס, ובצדק.
//
// 🔴 **והמוקש שהרכיב הזה מנטרל פעם אחת:** ‏`<Hint id>` עם מפתח שאינו קיים ב-`onboardingCopy`
// מרנדר **`null` בשקט בייצור** (`src/components/Hint.jsx:41` · `spec.md §🚫.5`). ⇒ הקבועים
// למטה הם **רשימת-המפתחות היחידה** של הלשונית, והבדיקה מייבאת אותם ואת קובץ-הקופי ומוודאת
// שכל מפתח נפתר. מפתח שנכתב אינליין במשטח היה חומק מהבדיקה הזו.
//
// 📐 **מיקום:** ‏`onboarding-layer-contract §4ב` — הרמז הוא **שורה עצמאית מתחת לבלוק שהוא
// מסביר**, ולעולם לא ילד של אריח. הדפוס מועתק מ-`FinancePage.jsx:648-649`.

import Hint from '@/components/Hint'

export default function HintBlock({ ids, testId = 'report-hints' }) {
  if (!ids?.length) return null
  return (
    <div data-testid={testId}>
      {ids.map((id) => (
        <Hint key={id} id={id} />
      ))}
    </div>
  )
}
