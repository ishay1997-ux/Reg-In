<div dir="rtl">

# מודול 08 — כספים ודוחות שכר (Finance & Payroll Runbook)

> **ספר הפעלה תפעולי לעבודה על מודול 08 (מעקב כספי פרויקטים, חישוב רווחיות, דמי ביטול והפקת דוחות שכר)**  
> נטען אוטומטית בעבודה על `src/modules/08_finance/`. מפה מלאה: [docs/CODE_MAP.md](../../../docs/CODE_MAP.md).

---

## 1. שערי איכות ופקודות הרצה מקומיות (Quality Gates & Commands)

| פקודה | תפקיד ופירוט | סף מעבר מחייב |
|---|---|:---:|
| `npx vitest run src/modules/08_finance/` | בדיקות יחידה ורכיבי מודול 08 | 100% ירוק |
| `npx vitest run src/lib/projectFinance.test.js` | בדיקות לוגיקת כספי פרויקטים וסולמות ביטול | 100% ירוק |
| `npx vitest run src/lib/salaryReport.test.js` | בדיקות מנוע הפקת דוח שכר ואימות שורות | 100% ירוק |
| `npm run lint` | בדיקת תקני קוד (רף מורכבות קוגניטיבית 20) | 0 אזהרות/שגיאות |

---

## 2. חוקי ברזל ארכיטקטוניים ואינווריאנטים (Architectural Invariants)

1. **בסיס הנתונים הוא מחשבון הכסף הבלעדי:**  
   הפונקציות השרתיות (`finance_project_money`, `finance_cancellation_fee_proposal`, `generate_salary_report`) הן הסמכות הבלעדית לחישובי סכומים, הוצאות ורווח. קוד ה-JavaScript ב-`src/lib/projectFinance.js` מבצע **גזירות תצוגה בלבד** (מועדי פירעון, ימי איחור, תגיות סטטוס).  
   **איסור מוחלט:** לעולם אין לחשב מחדש בצד הלקוח תעריפים, שעות, אחוזים או עלויות סחורה.
2. **חוסר נתון אינו אפס לעולם:**  
   שדה חסום בהרשאה, פיצוי שטרם חושב, או דמי ביטול שלא נקבעו מוצגים כ-`—` או בניסוח מפורש, ולעולם לא כ-`0 ₪`. אפס שקט על מסך כספי מהווה הטעיה חמורה.
3. **שערי אבטחה בתוך גוף פונקציות DEFINER:**  
   כל פונקציית מסד כספית הנגישה לדפדפן מחייבת שער הרשאה מפורש בגוף הפונקציה (`assert_module_permission`) ולא רק ברמת ה-ACL של תפקיד `authenticated`.
4. **חיוב סחורה ומודל ביטולים:**  
   - סחורה מחויבת במחיר ללקוח (`goods_at_price`), בעוד עלות הרכש נשמרת בנפרד (`goods_at_cost`). החיוב מבוסס על הכמות שהוזמנה בפועל בצ'קליסט הלוגיסטי (`planned_qty`), ולא על כמות האורחים.
   - סולם דמי ביטול: מעל 72 שעות = 0%, בין 24 ל-72 שעות = 50%, פחות מ-24 שעות = 100% (הגבולות 24 ו-72 שעות כלולים ב-50%).
   - בביטול שאינו כוח עליון ("אחר"), דיילות מקבלות תשלום בדוח השכר, בעוד הצעת החיוב ללקוח מושהית להכרעת מנהלת.
5. **היסטוריית דוחות שכר מתוך הדיאלוג בלבד:**  
   היסטוריית הדוחות מרוכזת בתוך דיאלוג הפקת דוח השכר (`SalaryReportDialog.jsx`). כתובת הנמען בהיסטוריה נקראת מתוך `email_log` (`sent_to`) שנרשמה בעת ההפקה, ולעולם לא מתוך הגדרת הדוא"ל הנוכחית ב-`params`.
6. **שלמות שורות שכר ב-PostgreSQL:**  
   השדות `hours`, `rate`, `line_total`, `hostess_name` ו-`id_number` בטבלת `salary_report_lines` מוגדרים כ-`NOT NULL`. תעריף חסר מפיל את ההפקה בקול ומסרב ליצור דוח חלקי.

---

## 3. פנקס מלכודות שקטות במודול (Silent Traps Register)

| מלכודת שקטה | מדוע הבאג שורד קומפילציה ובדיקות | התוצאה והפתרון ההנדסי המחייב |
|---|---|---|
| **סדר בדיקת ענפים בתג תוצאת שליחה (`SendResultTag`)** | תהליך ההפקה כולל שמירת קובץ ושליחת דוא"ל בשני שלבים נפרדים | בדיקת `fileError` לפני `sendResult` מציגה שגוי "נשלח — קובץ לא נשמר" על מייל שנכשל לחלוטין. `sendResult` נבדק ראשון תמיד |
| **מצב חודש קודם כברירת מחדל בדוח שכר** | הדיאלוג נפתח כברירת מחדל על החודש הקודם (`setMonth(month - 1)`) | ב-1 לחודש, ברירת המחדל היא החודש שכבר הופק ומציגה באנר נעילה במקום טופס ריק. הבדיקות מוודאות קיום אחד משני המצבים |
| **מצב תלת-ערכי ב-`existingForSelected`** | כיווץ המצב לבוליאני (`true/false`) | בחלון הטעינה שלפני הגעת הנתונים מוצג טופס הפקה שגוי על חודש שכבר נחתם. המצב מוגדר כ-`undefined` (בטעינה), `null` (אין דוח), אובייקט (קיים) |
| **דריפט עמודות בשער `assertFinanceShape`** | הוספת או הסרת עמודה בפונקציית ה-RPC של הכספים ללא עדכון רשימות השדות ב-`api.js` | הפונקציה זורקת שגיאת שער-צורה. כל שינוי ב-SQL מחייב עדכון רשימות השדות ב-`api.js` באותו קומיט |

---

## 4. מוסכמות קוד ודפוסי מודול (Module Patterns)

1. **ניקוי קודים פנימיים מממשק משתמש:**  
   טקסטים בממשק המשתמש אינם חושפים קודים טכניים או מזהי מפרט (כגון `P1`, `§7.52`). כל סטטוס מוסבר בעברית פשוטה.
2. **אימות דוחות שכר:**  
   בדיקות תקינות הדוח מאמתות התאמה מדויקת בין שורות הדוח לבין הסיכום הכולל ב-`src/lib/salaryReport.js`.

---

## 5. עוגני קבצים קריטיים (Critical File Anchors)

- **מסך כספים ראשי:** `src/modules/08_finance/FinancePage.jsx`
- **דיאלוג הפקת דוח שכר:** `src/modules/08_finance/SalaryReportDialog.jsx`
- **דיאלוג חלון סגירה ודמי ביטול:** `src/modules/08_finance/ClosingWindowDialog.jsx`
- **שכבת נתוני כספים:** `src/modules/08_finance/api.js`
- **לוגיקת כספים ופרויקטים:** `src/lib/projectFinance.js`
- **לוגיקת דוח שכר:** `src/lib/salaryReport.js`

</div>
