<div dir="rtl">

# מודול 3 — הצעות מחיר · מוקשים

> 🔨 **מודול בבנייה — Phase 3 (ממשק), צעדים 3.1–3.4 הושלמו.** המוסכמות הכלליות: `src/CLAUDE.md`. כאן רק
> מה שייחודי למודול הזה. היסטוריית הבנייה: `docs/micro_guides/module-3.md`.
> ⚠️ הקובץ הזה עדיין חלקי במכוון — הוא גדל עם כל צעד (כלל ברזל 15). היום מתועד: `src/lib/pricing.js` ·
> ‏`src/lib/catalog.js` · **`src/lib/quotes.js`** · `validators.js` (תוספת) · `api.js` ·
> ‏`01_auth/pricesApi.js` · `quotePdf.jsx` · **מסך-הבנייה (3.2)** · **מסך-הניהול (3.3)** ·
> **שליחת המייל (3.4)**.

## 📧 שליחת ההצעה במייל (3.4) — מוקשים שלא רואים בקוד

**הזרימה:** החלון ⇒ `send-email` (Edge Function) ⇒ webhook של Make ⇒ Gmail עם ה-PDF מצורף.
המנוע עצמו **גנרי** ומשותף לכל המודולים — `src/lib/email.js`; ר' `src/CLAUDE.md` §"שליחת מייל".

- **⚠️ `toBinary()` ב-Make אינו מפענח base64 בלי פרמטר שני.** ברירת-המחדל היא UTF-8, ולכן
  ‏`toBinary(x)` מייצר קובץ שהוא **המחרוזת עצמה** — נראה כמו PDF (הסיומת קובעת את ה-MIME) ולא
  נפתח. הצורה הנכונה: `toBinary(2.pdf_base64; "base64")`. **עלה שני סבבים מול ישי.**
- **⚠️ שם-קובץ ו-MIME הם מטא-דאטה, לא תוכן.** ‏Gmail מדווח `application/pdf` לפי הסיומת גם
  כשהבייטים זבל. **אימות קובץ מצורף = גודל או בייטים** (המסמך של מודול 3 הוא ~33KB/34,026 בתים).
- **⚠️ Make מחזיר 200 מיד כשהבקשה נכנסת**, לפני שמודול-הדואר רץ. לכן בתרחיש יש מודול
  ‏`Webhook response` שמחזיר 200 **רק אחרי** הצלחת השליחה, ו-502 בענף-השגיאה — ואחריו "Skip",
  כדי שנמען פגום לא ישבית את התרחיש (התקרית של פרויקט 710).
- **שם-המודול הנכון ב-Make הוא `google-email:sendAnEmail` גרסה 4** (‏`ActionSendEmail` נדחה
  ע"י ה-API עם כל חיבור). יצירת תרחיש דרך ה-API **כן** אפשרית — לשימוש מ4/מ8/מ11.
- **חמשת שדות-המשלוח הם חוזה** מול מבנה-הנתונים `regin-quote`: שדה שיתווסף בקוד בלי שינוי
  מקביל בתרחיש פשוט **לא יגיע למייל, בלי שגיאה**.
- **⚠️ שלושת שדות-המטא (`entity_type`/`entity_id`/`template_name`) נשלחים בנפרד מהחוזה** —
  ל-`email_log.entity_id` יש NOT NULL, וללא השליחה שלהם **המייל יוצא והיומן נשאר ריק**. זה
  קרה בפועל, וזה הכשל-השקט שהטבלה נועדה למנוע.
- **"האם כבר נשלח" נענה רק מ-`email_log`** (‏`getLastSuccessfulSend`) — ההגנות שבחלון חיות
  ב-state ומתות ברענון-דף. הכפתור נשאר **פעיל** אחרי שליחה (שליחה חוזרת היא מקרה אמיתי)
  ומשנה תווית ל"שליחה חוזרת" + `confirm`.
- **חלון-המסמך במסך-הבנייה מקבל "הצעה" שנבנתה מהטופס** (`formToPreviewQuote`) ולא מהשורה
  השמורה — ולכן אין בו כפתור-שליחה: `isQuoteSendable` מחזיר false בהיעדר סטטוס, וזה מכוון
  (שליחת גרסה שלא נשמרה הייתה מצרפת מסמך שה-DB אינו מחזיק).

## 🗂️ מסך הניהול (3.3) — מה שלא רואים בקוד

- **הכול צד-לקוח.** ‏`listQuotes()` מביאה את כל מה שה-RLS מתיר; הלשוניות, המונים, המסננים,
  המיון והמדדים מחושבים בזיכרון (`src/lib/quotes.js`). זה נכון כל עוד מספר ההצעות קטן —
  **הצעות נצברות** (בשונה מלקוחות), וזה המקום הראשון שיצטרך סינון-בשאילתה.
- **מונֵי-הלשוניות מכבדים את החיפוש/הלקוח/התאריך, אך לא את הצ'יפים.** בכוונה: מספר בלשונית
  חייב לתאום למה שיוצג בלחיצה עליה, אבל צ'יפ הוא זיקוק **בתוך** לשונית ולכן אינו משנה אותה.
- **שני הצ'יפים מוסתרים בלשוניות "מאושרות"/"נדחו" — וכשהם מוסתרים הם גם לא מסננים.**
  הצעה שנדחתה אינה יכולה לפוג, ולכן "פג בקרוב" שם הוא 0 לנצח. הסתרה בלי ביטול-הסינון הייתה
  משאירה מסנן דלוק שמעלים שורות בלי פקד נראה שמסביר למה.
- **‏`showChips` חייב להיות מוכרז לפני `chipFilters`.** ‏`const` בגוף-קומפוננטה חי ב-TDZ:
  שימוש לפני ההכרזה מפיל את הרינדור כולו ל**מסך לבן**, והבדיקה תדווח רק "element not found".
- **שורת-הסינון מכוילת לרוחב.** חמישה פקדים = 893px בכרטיס של 912px (חלון 1280). ⚠️ שני
  ‏`input[type=date]` תופסים **258px** ואינם מצטמצמים — לכן טווח-התאריכים הוא **כפתור שפותח
  פאנל** ולא שדות פתוחים בשורה. הרחבת פקד כלשהו כאן שוברת את השורה לשתיים בשקט.
- **מיון "הקרוב לפוג ראשון" = `updated_at` עולה.** תפוגה היא `updated_at + ימי_תוקף_הצעה`
  (קבוע לכולם), ולכן זה בדיוק אותו סדר — כלל אחד לכל הסטטוסים, בלי מקרה-קצה להצעות סגורות.
- **"פג בקרוב" = 7 ימים (LOCAL-4) · "אירועים קרובים" = הפרמטר `ימי_אזהרה_קדם_אירוע` (14).**
  שני מדדים שונים לחלוטין: הראשון מודד הזדקנות של **המסמך שלנו**, השני קרבת **המועד של הלקוח**.
- **‏`daysUntil` פנימית ואינה מיוצאת** — המסך שואל שאלה עסקית ("פג בקרוב?"), לא סופר ימים בעצמו.
- **חלון "צפייה במסמך" הוא `<iframe src={blobURL}>` ומשחרר את ה-blob ב-cleanup.** ⚠️ בדפדפן
  ללא מציג-PDF מובנה (כמו Chromium של Playwright) ה-iframe **ריק** — זו תכונת-הסביבה ולא באג.
  אימות המסמך נעשה על הבייטים עצמם, לא על צילום-מסך.
- **‏`QuoteActionDialog` מחזיק מצב-שמירה ושגיאה; הקורא מחזיק ולידציה ופעולה.** ולידציה
  **זורקת** ומוצגת באותו מקום שבו תוצג שגיאת-שרת — משתמש לא לומד שני דפוסי-שגיאה באותו חלון.
- **‏`NON_LOSS_REJECTION_REASONS` ריקה בכוונה** — המנגנון להחרגת "נפתחה בטעות" משיעור-האישור
  קיים ובדוק, והסיבה עצמה ממתינה למיגרציה עם שער-הקלדה שטרם בוצע.

## 🖥️ מסך הבנייה (3.2) — מה שלא רואים בקוד

- **‏`/quotes` הוא עדיין "בבנייה"; המסך חי ב-`/quotes/new` וב-`/quotes/:quoteId/edit`.** צעד 3.3
  יחליף את `/quotes` ב-`QuotesPage`. כפתורי "ביטול"/שמירה מנווטים ל-`/quotes` — כלומר **כרגע
  למסך "בבנייה"**. זו לא תקלה, זה סדר-הבנייה.
- **‏`minmax(0,1fr)` בגריד הראשי הוא load-bearing.** עמודת-גריד רגילה (`1fr`) היא
  ‏`minmax(auto,1fr)` ו**מסרבת להצטמצם** מתחת לרוחב-התוכן; טבלת-המפרט נושאת `min-width`, ולכן עם
  ‏`1fr` **כל העמוד** גולש אופקית (נמדד: 1456px בחלון 1280) והגלילה-הפנימית של הטבלה לא נכנסת
  לפעולה. ב-RTL זה מסתיר תוכן בצד שאיש לא גולל אליו.
- **‏`min-w` של הטבלה (40rem) ורוחב פאנל-הסיכום (17rem) מכוילים יחד.** הגדלת אחד מהם דוחפת את
  עמודת **"סה"כ שורה"** אל מחוץ לתצוגה — העמודה שבשבילה פותחים את המסך.
- **‏`input[type=number]` — חצי-ההגדלה מוסתרים בתאים הצרים** (`[appearance:textfield]`): הם גוזלים
  ‏~16px ו"300" הוצג כ-"30".
- **המחיר ליחידה אינו נערך** (הכרעת-ישי 29/07) — הנחה ניתנת רק דרך שדה ההנחה הידנית, ששמור ומדווח.
- **‏`recommended_hostess_count` הוא שדה נערך-ידנית, למרות שמו** (הכרעת-ישי 29/07): המשתמש מקליד
  ‏7 ישירות במקום לחשב לאחור איזה יחס ייתן 7. שינוי אורחים/יחס **דורס** אותו בהמלצה החדשה, ולכן
  יש קיצור "החזרה להמלצה" שמופיע רק כשהם נפרדו. **דריסה בטוחה** כי השדה אינו משתתף בתמחור
  ואינו זה שקובע את הפרויקט — ה-RPC של האישור סוכם את כמויות **שורות-הדיילות בפועל** (F22).
- **המק"ט אינו מוצג במסך** (הכרעת-ישי 29/07) — קוד-מלאי פנימי חסר-משמעות לבונה ההצעה. הוא כן
  מודפס ב-PDF ללקוח, שם הוא חלק ממסמך רשמי.
- **תגית-המדרגה מוצגת רק כשהמדרגה הוזילה בפועל** (`unitPrice < base_price`). "מדרגה 1–50" חוזרת
  על מחיר-המחירון ולכן היא רעש; "מדרגה 201–400" מסבירה למה 5 ₪ ולא 6 ₪ — בלעדיה זה נראה כתקלה.
- **‏`onFocus → select()` בכל שדה מספרי** (‏`LtrFieldGroup` + שדה-הכמות): השדות נטענים עם ערך
  התחלתי (‏1 / 0 / המלצה), ובלעדיו כל הקלדה מחייבת מחיקה ידנית קודם. ⚠️ לחיצה על שדה **שכבר
  ממוקד** אינה בוחרת מחדש — זו התנהגות נכונה (מיקום-סמן), ובדיקה שתלחץ פעמיים תשרשר ערכים.
- **בחירת-הצבע מוצגת רק ל-`category='product'`**; בחירת מוצר חסר-צבע **מנקה** צבע שנבחר קודם,
  אחרת הוא היה נשמר בשקט על שורת-דיילות.
- **חסימת תאריך-עבר כבר ביצירה** (הכרעת-ישי) — ה-RPC חוסם רק באישור; המסך מקדים אותו בכוונה.
- **חסימת הצעה בלי שורת-דיילות** (§7.53) — אחרת נשמרת הצעה שלעולם לא תוכל להפוך לפרויקט.
- **⚠️ מפת-השגיאות של הטופס אינה `state` — היא נגזרת בכל רינדור** (`submitAttempted ? validateQuoteForm(...) : {}`).
  עד 29/07/2026 היא נשמרה ב-`state` בלחיצת "שמור", ולכן הודעה אדומה **נשארה תלויה מעל שדה שכבר
  תוקן** עד הלחיצה הבאה (ישי תפס). ⚠️ **לא לנקות "רק את השגיאה של השדה שנגעו בו"** — שלושה
  כללים כאן חוצי-שדות (סכום ההנחות ≤100% · חובת שורת-דיילות · תאריך-עבר), וניקוי-פר-שדה היה
  משאיר אותם תקועים על מסך תקין. `todayIso` מרוענן בתוך `handleSave` ולא רק בטעינה, כדי שטופס
  שנשאר פתוח לילה שלם לא ייבדק מול תאריך של אתמול.
- **ספירת "אלמנטים אדומים" אינה מדד לשגיאות** — במסך הזה אדום משמש גם לאייקוני-המחיקה של
  השורות וגם לסכומי-ההנחה (`-315 ₪`). בטעינת מסך-עריכה תקין יש 5 אלמנטים אדומים ואפס שגיאות.

## 🛡️ שומר-המע"מ — מה שאסור להחזיר לאחור (31/07/2026)

**‏`quote?.vatRate ?? 0` היה ב-`buildQuoteDocument` והפיק ללקוח משלם מסמך עם `מע"מ (0%)` וסכום
נמוך ב-~15%, בלי שגיאה.** הטריגר אמיתי: §7.84 קובע ששורות `params` נערכות ידנית ב-Table Editor.

- **המנוע מסרב, ולא המסך.** ‏`buildQuoteDocument` מאמת דרך `parseVatPercent` וזורק
  `MISSING_VAT_MESSAGE` עם `code:'MISSING_VAT'`. **למה במנוע:** ל-`buildQuoteDocument` **אפס
  קוראים בייצור** — כולם עוברים דרך `renderQuotePdfBlob` → `QuoteDocumentDialog` (אתר-קריאה
  יחיד) ← שלושה מסכים; ומודול 10 ירים את המנוע לשרת, שם אין מסך שיגן. ⛔ **החזרת ברירת-מחדל
  כלשהי כאן מחזירה את הבאג במלואו.**
- **‏`0%` הוא ערך חוקי ולא "חסר"** — פטור-ממע"מ הוא מצב אמיתי, ויש בדיקה ששומרת על ההבחנה.
- **‏`MISSING_VAT_CODE` הוא חוזה** בין המנוע לחלון: הוא מה שמבדיל בין ההודעה שאומרת מה לתקן
  לבין "הפקת המסמך נכשלה" הכללית. שינוי-שם שובר את החלון **בלי שגיאת-בנייה**.
- **‏`emailSendDisabledReason` (‏`src/lib/email.js`) לא נגעו בו במכוון** — הוא המנוע הגנרי של
  מ4/מ8/מ11. סיבת-המע"מ מורכבת מקומית בחלון.
- **שלוש שכבות, ולכל אחת תפקיד אחר:** המנוע (חוסם) · החלון (מסביר) · באנר במסך-ההצעות
  (‏`missingPricingParamsMessage`, מתריע לפני שבכלל לחצו). הבאנר מכסה **גם** את
  ‏`ימי_תוקף_הצעה`, כי עבודת-התפוגה נכשלת ל-`cron.job_run_details` — מקום שאיש לא פותח
  (הכרעת-ישי 31/07).
- **‏`deriveQuoteMetrics.openValue` מחזיר `null` ולא 0** כשהמע"מ לא ידוע — אחרת האריח הציג
  "שווי הצעות פתוחות: 0 ₪" בדיוק מתחת לאזהרה שאומרת שאי-אפשר לתמחר.
- **בצד ה-DB** (מיגרציה `20260731085335`): ה-RPC של האישור נכשל בעברית **לפני כל כתיבה**
  (אין פרויקט יתום), שני CHECK על `quotes`, ועבודת-ה-cron זורקת במקום לדווח `UPDATE 0`.

## 📄 `quotePdf.jsx` — שני כשלים שקטים

שניהם **לא זורקים שגיאה**. שניהם מייצרים מסמך שנראה סביר ושגוי. אל תגלה אותם מחדש.

- **הגופן חייב להיות TTF.** ‏`fontkit` של `@react-pdf/renderer` קורא TTF/OTF בלבד; חבילות-גופנים
  מודרניות (‏`@fontsource/*`) מספקות **woff/woff2 בלבד**. ‏`Font.register` על woff **מצליח** ואז
  מדפיס ג'יבריש; woff2 מדפיס כלום. לכן שני משקלי Heebo **מוטמעים כ-`.ttf`** ב-`src/assets/fonts/`
  (רישיון OFL לצידם) ונטענים כ-data-URI דרך `?inline` — בלי רשת, כדי שגם טעינה לא תוכל להיכשל בשקט.
- **‏`<Ltr>` הוא חובה לכל ערך לטיני/מספרי.** אלגוריתם ה-bidi מסדר רצפי-תווים ולא אסימונים לוגיים:
  ‏`18:00–22:00` שמשורשר למחרוזת עברית מודפס `22:00–18:00`. **שתי דרכים שנבדקו ולא עובדות:**
  ‏`<Text>` מקונן עם `direction:'ltr'`, ותווי-הבידוד LRI/PDI ‏(U+2066/U+2069 — הם מסדרים נכון אבל
  מודפסים כג'יבריש, כי אין להם גליף). **מה שעובד: הערך ב-`<Text>` משלו עם `direction:'ltr'`.**
  לעולם לא לשרשר מק"ט/תאריך/שעה/טלפון/סכום לתווית עברית באותה מחרוזת.
- **⚠️ תצוגה מקדימה בממשק (צעד 3.4) — `<iframe src={blobURL}>`, לא pdf.js.** המציג המובנה של
  Chrome ‏(pdfium) מרנדר את הקבצים האלה מושלם; **pdf.js דוחה את תת-הגופן שהספרייה מטמיעה**
  (`OTS parsing error: maxp: Bad maxZones`) ומשמיט אותיות צרות. זו תכונה מוכרת של react-pdf.
- **הסכומים מחושבים בתוך המסמך מ-`pricing.js`, ולא מתקבלים מבחוץ** — כדי שה-PDF לא יוכל להציג
  מספר שונה מהמסך. ‏`total`/`subtotal` שיוזרקו ב-object פשוט מתעלמים מהם.
- **`QUOTE_TERMS` הן placeholder משפטי.** 2 הראשונות מהטמפלט המקורי; 5 הנוספות נוסחו ב-29/07
  (הכרעת-ישי: עמוד 2 = מדיניות גנרית) וממתינות לניסוח משפטי של החברה.

- **`createQuote`/`saveQuoteEdit` הן עטיפות דקות סביב RPCs, לא כתיבות ישירות** — `create_quote`/
  ‏`replace_quote_lines` (מיגרציה `20260723115000`, F17). ה-`header`/`lines` שנשלחים הם `jsonb`
  שנקרא בשרת עם `->>'key'` — **מפתח שגוי בעצם ה-object לא נכשל בזמן-קריאה**: הוא הופך ל-`NULL`
  בשרת. עבור עמודות `NOT NULL` (כמו `customer_id`) זה מתפוצץ ב-INSERT (רועש, נתפס). עבור עמודות
  עם `coalesce(...,0)` (כמו `manual_discount`) **מפתח מוקלד-לא-נכון הופך בשקט ל-0**, לא לשגיאה.
  שמות-המפתחות המדויקים חייבים להישאר תואמים למיגרציה בית-בבית: `customer_id, event_name,
  recommended_hostess_count, estimated_guests, estimated_event_date, estimated_location,
  estimated_start_time, estimated_end_time, applied_customer_discount, manual_discount, notes`
  (header) ו-`sku, qty, closing_unit_price, color, notes` (כל אובייקט ב-lines).
- **`line_number` ו-`closing_unit_cost` נקבעים בשרת ולא נשלחים מהלקוח** — ה-RPC ממספר לפי סדר
  ה-array (`with ordinality`) ומקפיא עלות מ-`products.cost` באותו רגע. שליחת שדות האלה מה-UI
  היא no-op (השרת מתעלם מהם).
- **`rejectQuote` הוא היחיד שכותב ישירות ל-`quotes.update()`, לא דרך RPC** — מותר כי טריגר-הנעילה
  (§7.50) מתיר UPDATE כל עוד ה-status **הישן** הוא `in_progress`, וזה בדיוק התנאי היחיד שממנו
  מותר לדחות. שינוי סטטוס אחר (למשל "לבטל דחייה") **לא** יעבוד באותה צורה — ינחת על טריגר-הנעילה.
- **`getPricingCatalog()` מחזיר את כל המוצרים, כולל מושבתים — הסינון ל-`active` חי בבורר, לא
  בשאילתה** (שוּנה 31/07/2026, סבב-תיקונים D). עד אז השאילתה סיננה `active` בלבד, ומוצר שהושבת
  **אחרי** שנכנס להצעה נעלם מהקטלוג: השורה איבדה קטגוריה (שמירה נחסמה בהודעת-דיילות מבלבלת)
  ונגיעה בכמות איפסה את המחיר ל-0 ₪ בשקט. §7.34 נאכף היום ב-`QuoteLineEditor` (מוצר לא-פעיל
  אינו מוצע לשורה חדשה; שורה קיימת מקבלת תג "מוצר מושבת" ונשמרת — הכרעת-ישי 31/07, תקן
  Salesforce CPQ). ⛔ החזרת `.eq('status','active')` לשאילתה מחזירה את שני הכשלים.
  ⚠️ **והסינון בבורר הוא פר-שורה** (`productGroupsFor(line.sku)`), לא חישוב אחד לכל הטבלה:
  גרסה שחישבה פעם אחת מקבוצת כל המק"טים-שבשימוש חשפה את המוצר המושבת **בכל** השורות, כולל
  שורה חדשה — היפוך גמור של §7.34. נתפס בצילום של הרשימה **הפתוחה** (הצילומים הקודמים הראו
  אותה סגורה בלבד) ולא בשום בדיקה. ⛔ לא להוציא את החישוב מחוץ ללולאה.
- **⚠️ ניסוחי-ה-RAISE של המסד הם חוזה מול `quoteServerErrorMessage`** (‏`src/lib/quotes.js`,
  סבב D): ההבחנה בין 11 כשלי-P0001 היא לפי תחילית-הטקסט העברי, כי ה-SQLSTATE מבחין רק
  הרשאה/לא-נמצאה/השאר. מיגרציה שמנסחת RAISE מחדש בלי לעדכן שם ⇒ המסך נופל בשקט ל-fallback
  הכללי ואף בדיקה לא נופלת. השורה המקבילה בצד-המסד: `docs/db_roadmap.md` §6 (quotes).
- **⚠️ `listQuotes()` היא בלי פרמטר-סינון — מסננים בצד-הלקוח.** הבלופרינט הבטיח `listQuotes(filters)`;
  הקוד מחקה את מודול 2 (`listCustomers()` + `matchesCustomerFilters`) ומביא את כל מה שה-RLS מתיר.
  **מסך-הניהול (צעד 3.3) חייב לסנן/למיין בתצוגה** — הטאבים, המונים, ותגית "פג בקרוב" כולם צד-לקוח.
  ‏🔁 הקריאה מושכת גם `quote_services(*)` לכל שורה, והצעות **נצברות** (בשונה מלקוחות) — זה המקום
  הראשון שיצטרך סינון-בשאילתה אם הטבלה תגדל. הסטייה מתועדת ב-`module-3.md` §9 (29/07).
- **‏`src/lib/pricing.js` הוא מקור-האמת היחיד לחישוב** — `api.js` **לא** מחשב סכומים; שורות
  שמגיעות מה-DB (`closing_unit_price`) ממופות לצורת `{qty, unitPrice}` בצד-הקורא (UI, Phase 3),
  לא כאן. F26: אין כאן ואף לא ב-`pricing.js` שום סמנטיקת-זמן ("משמרת"/"שעה").
- **⏸️ פטור-knip זמני** על `api.js` (וגם `01_auth/pricesApi.js`) — `knip.jsonc`, עד שמסכי-Phase 3
  יובאו אותם. הסרה: כשצעדים 3.2/3.3/3.6 מחברים בפועל (ר' הערה מלאה בקובץ `knip.jsonc` עצמו).

</div>
