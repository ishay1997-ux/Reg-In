<div dir="rtl">

# מצביע-תיקייה (נטען אוטומטית כשעובדים כאן) — ה-SSOT: כללי הברזל ב-`CLAUDE.md` שבשורש

הכללים שהכי נשכחים תוך-בנייה:
- **כלל 14 (SSOT):** לוגיקה עסקית פעם אחת ב-`src/lib/` + בדיקת-יחידה; שאילתות המודול ב-`src/modules/NN_name/api.js` — ‏UI רק מייבא. לפני שכותבים helper — לחפש קיים.
- **כלל 3:** כל לוגיקה עסקית מקבלת הערת "why" בעברית — בלי שיבקשו.
- **כלל 15:** שינית קוד תחת `src/modules/NN_*/` ⇒ מדריך-המיקרו `docs/micro_guides/module-N.md` מתעדכן באותו סשן (ה-Stop hook אוכף).
- **כלל 9:** מודול חדש מצהיר איך הוא נשען על Auth של מודול 1 (RLS/שערים) — האכיפה האמיתית ב-DB, ה-UI רק נוחות.
- קובץ מחוץ לתיקיית-המודול שלך (`src/lib/**`, ‏`App.jsx`) = משטח-משותף: בדוק את מדריך-המיקרו של המפתח השני, שנה additive (⚠️ shared-surface בטמפלט).
- ייבוא הלקוח: ‏`@/supabaseClient` (לא ‏`@/lib/`).

</div>
