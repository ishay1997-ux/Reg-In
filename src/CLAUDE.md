<div dir="rtl">

# `src/` — מוקשים (נטען אוטומטית כשעובדים בקוד)

> **רק מה שלא ניתן להסיק מקריאת הקוד ולא נאכף מכנית.** מה ש-ESLint/Prettier/CI/hooks כבר אוכפים —
> לא כתוב כאן. *(נבנה 28/07/2026 מסקירת-קוד מאומתת של כל 45 הקבצים; כל שורה מגובה ב-file:line.)*

## 🧨 ייבוא ותשתית

- **`@/supabaseClient`** — הקובץ ב-`src/supabaseClient.js`, **לא** ב-`src/lib/`. הניחוש `@/lib/supabaseClient`
  לא קיים ושובר את הבנייה. 7 קבצים מייבאים אותו.
- **Radix הוא חבילה **מאוחדת** בשם `radix-ui`**, מיובאת עם namespace: `import { Dialog as DialogPrimitive } from 'radix-ui'`.
  **אין ולו חבילת `@radix-ui/react-*` אחת.** קוד shadcn שמועתק מהתיעוד הרשמי מייבא `@radix-ui/react-dialog`
  ו**לא ייפתר** — או גרוע: `npm i` יתקין עותק שני של Radix.
- **`shadcn` היא dependency של ריצה, לא devDependency** — `src/index.css:3` עושה `@import 'shadcn/tailwind.css'`.
  היא *נראית* כמו CLI; העברתה ל-devDependencies שוברת `npm run build` בהתקנת production.
- **Tailwind v4 — אין `tailwind.config.js` ואין `postcss.config.js`.** כל הקונפיגורציה ב-`src/index.css`
  (‏`@theme inline`) + הפלאגין ב-`vite.config.js`. יצירת `tailwind.config.js` **תתעלם בשקט**.
- **ה-alias `@/` מוגדר פעמיים** — `vite.config.js` (בנייה + Vitest) ו-`jsconfig.json` (עורך בלבד). שינוי אחד
  בלי השני שובר צד אחד בלי שגיאה בצד השני.
- **`src/App.css` ריק ואינו מיובא מאף מקום** — כתיבת CSS שם היא no-op שקט.
- **הלוגו חי ב-`src/assets/reg-in-logo.png` בלבד — לא ב-`public/`.** ⚠️ עד 29/07/2026 היו **שני עותקים
  זהים-בייט** בשתי תיקיות ובשני שמות (`public/regin-logo.png` למסכים · `src/assets/reg-in-logo.png`
  ל-PDF), ואוחדו. **למה `src/assets` ולא `public`, ולא להפוך את הכיוון:** ה-PDF חייב את התמונה
  **מוטמעת כ-data-URI** (`?inline`), וזה עובד רק על קבצים שעוברים דרך הבנדלר — כלומר רק מתחת ל-`src/`.
  קובץ ב-`public/` מוגש כ-URL בלבד ולא ניתן להטמעה. שלושת אתרי-הקריאה (Sidebar · LoginPage · quotePdf)
  מייבאים היום את אותו קובץ. **החזרת עותק ל-`public/` תיצור שוב שני מקורות-אמת לאותה תמונה.**
  הגופנים, לעומת זאת, **מעולם לא שוכפלו**: `src/assets/fonts/` בלבד (‏2×TTF + `LICENSE_HEEBO.txt`).
- **Vitest מוגדר בתוך `vite.config.js`** (בלוק `test`) — אין `vitest.config.js`.

## 🔄 RTL — הכי קל להיכשל כאן, והכי שקט

- **הפרויקט משתמש ב-utilities פיזיות בלבד, כבר הפוכות ל-RTL.** ‏`right-0` לסרגל, `mr-60` להיסט התוכן,
  `pr-9`, `text-right`. **אין ולו `ms-`/`me-`/`ps-`/`pe-`/`start-`/`end-` אחד בכל `src/`.**
  ⚠️ ‏`<html dir="rtl">` כבר מוגדר — ולכן utility לוגית כמו `ms-60` נפתרת ל**ימין**, אותו צד שבו יושב
  הסרגל, והתוכן נוחת **מתחת** לסרגל. **זה נראה תקין בתצוגה LTR ושבור באפליקציה.**
- **רכיבי `src/components/ui/` נערכו ידנית ל-RTL, ו-`components.json` אומר `"rtl": false`.**
  ‏(dialog: כפתור-סגירה `left-4`, כותרת `sm:text-right`, footer `sm:justify-start`, תווית `סגור`;
  select: `pr-8 pl-2` + אינדיקטור `right-2`; switch: translate מנוגד ב-`rtl:`).
  ⛔ **`npx shadcn add dialog|select|switch` ידרוס את כל זה ויחזיר ל-LTR — בלי שגיאה ובלי כשל-בדיקה.**
- **כל משטח Radix שנפתח ב-portal מקבל `dir="rtl"` מפורש** (‏`DialogContent`, `SelectContent`,
  `DropdownMenuContent`). אין `DirectionProvider` בפרויקט — התכונה מגיעה כ-DOM attribute.
- **הטורקיז חי בשני מקומות:** ‏`--primary: #0d9488` ב-`index.css` **וגם** `bg-teal-600` מקודד-קשיח בהמון
  אתרי-קריאה. שינוי הטוקן לבדו לא ישנה אותם.
- **`Button` ברירת-מחדל קטן משמעותית ממה שהאפליקציה משתמשת בו** (`h-8 px-2.5`); כמעט כל אתר-קריאה דורס
  ל-`h-auto py-2 px-4`. כפתור חדש בלי דריסה ייראה קטן מהשכנים. **כפתור-אייקון בפרויקט =
  `variant="link"` + `className="h-auto p-0"`** (‏`size="icon"` קיים ואינו בשימוש בשום מקום).

## 🧪 שלושת המעברים לפני שמסך מוצג לישי (נוסף 29/07/2026)

**למה זה כאן:** מסך-הבנייה של מודול 3 עבר ~11 סבבי-תיקון מול ישי, ו-8 מהם היו דברים שניתן היה
לתפוס לבד. **כל 11 נפלו לשלוש משפחות בלבד.** המעברים האלה הם התחליף למשמעת "לשים לב";
הם מוחלים על כל מסך בכל מודול. *(עקרון-הגריעה F1 — זו תוספת; מה שהיא גורעת: את ההסתמכות על
כך שישי יתפוס בעין את מה שאפשר למדוד.)*

**(1) מעבר-כיווניות — מדידה בדפדפן, לא התרשמות.** שתי בדיקות **נפרדות**, כי הראשונה לבדה
עברה בהצלחה על מסך שהיה שבור:
- **יישור:** לכל תווית — `getBoundingClientRect().left` שלה מול זה של הערך שמתחתיה. חייב להיות זהה.
- **סדר-תווים:** לכל סכום — מיקום סימן ה-₪ והמינוס **ביחס לספרות** (דרך `Range` על צומת-הטקסט).
  ‏**חייב להיות זהה בכל המסך.** זה הכשל שהתגלה ב-29/07: אותו סכום הופיע עם ₪ משני צדדים —
  בטבלה מצד אחד ובפאנל-הסיכום מהצד השני, כי לחלק מהאלמנטים היה `direction:ltr` ולחלק לא.

**(2) מעבר-מלאי (הורחב 29/07/2026).** עוברים אלמנט-אלמנט וכותבים לכל אחד **את התפקיד היחיד שלו**.
שלוש בדיקות, לא אחת — הראשונה לבדה פספסה שש הערות של ישי באותו סשן:
- **אין תפקיד ⇒ נמחק.** אישור על מצב תקין ("✓ תואם להמלצה"), מונה-תוצאות שאפשר לספור בעין,
  רמזי-מקלדת מובנים-מאליהם, הערת-הסבר שכתובה למי-שבנה ולא למי-שמשתמש, ושדה שאף החלטה
  במסך אינה נשענת עליו (מספר-אורחים במסך-הניהול).
- **שניים באותו תפקיד ⇒ אחד נמחק.** שני כפתורי "שמור" · אותו נתון באריח וגם בצ'יפ ·
  ‏✕ בתוך צ'יפ שממילא מתחלף בלחיצה.
- **תפקידים שונים במראה זהה ⇒ אחד משנה מראה.** מסנן-פעיל שנצבע כמו הכפתור הראשי.
  **טורקיז מלא שמור לפעולה הראשית האחת של המסך**; מצב-נבחר מקבל גוון בהיר.

**(3) מעבר-אחידות.** כל אייקון/כפתור/צבע/מרווח נבדק מול מה שכבר קיים ב-`src/` לפני שממציאים:
אייקונים `size-4` מ-`lucide-react` · כפתור-אייקון `variant="link"` + `h-auto p-0` · צבעים מ-§4 בלבד
(**אין סגול/כתום בשפת-העיצוב**) · זכוכית-מגדלת בצד **ימין** של תיבת-חיפוש (`CustomersPage.jsx:289`).
⚠️ **פח-אשפה מול ארכיון:** פח = הסרת שורה מטופס שלא נשמר (`CustomerFormDialog.jsx:492`);
רשומה קיימת **מארכבים** ולא מוחקים (`CustomersPage.jsx:527`). לא להחליף ביניהם.

**(4) מעבר-ניסוח (נוסף 29/07/2026 — ישי פסל שתי מחרוזות באותו סשן).** התקלה אינה עברית שגויה
אלא **עברית מתורגמת**: מחשבים את התווית באנגלית ומלבישים אותה. ‏"‏Why we lost" ⇒ "למה הפסדנו",
‏"pipeline" ⇒ "סכום בצנרת". שלושה מבחנים, לפי הסדר:
1. **קודם כול — לחפש מחרוזת שכבר קיימת.** האפיון, עמודות ה-DB והמסכים הקיימים מלאים בעברית
   מאושרת. ל"למה הפסדנו" כבר היה נוסח נכון בריפו: `rejection_reason` = **סיבת דחייה**.
2. **תווית היא צירוף-שם, לא משפט.** ‏"סיבות דחייה" ולא "למה הפסדנו"; "שווי הצעות פתוחות" ולא
   "כמה כסף על השולחן". פועל בתווית = סימן שתרגמתי.
3. **בלי מטאפורות מיובאות** — "צנרת", "משפך", "לידים". לומר את הדבר עצמו.
*(עקרון-הגריעה F1: תוספת. מה שהיא גורעת — ניחוש-ניסוח פר-מקרה, שנכשל פעמיים באותו יום.)*

## 🖱️ משטח-צף שנסגר ב-blur — הבאג שבדיקה אוטומטית עוברת עליו בירוק

**דפוס אסור:** תיבת-קלט שפותחת רשימה/פאנל, וסוגרת אותו ב-`onBlur` דרך `setTimeout`.
**למה זה נשבר:** לחיצה על פריט ברשימה מוציאה קודם את המיקוד מהתיבה. הטיימר מתחיל, וכשלחיצת-אדם
נמשכת יותר ממנו (אצבע יורדת ועולה — לרוב 150–400ms) **הפאנל נעלם באמצע הלחיצה**, ה-`click` נוחת על
אלמנט שכבר לא קיים, ו**שום דבר לא קורה**. אין שגיאה בקונסול.
⚠️ **‏Playwright לוחץ מיידית ולכן תמיד מספיק** — הבדיקה עוברת בירוק על קוד שבור. קרה בפועל
‏29/07/2026 ב-`CustomerPicker` (גם בחירת-הלקוח וגם "+ לקוח חדש"); ישי תפס ביד, הבדיקה לא.

**התבנית הנכונה** (‏`03_quotes/CustomerPicker.jsx`): `onMouseDown={(e) => e.preventDefault()}` על
הפאנל — המיקוד **לא אובד מלכתחילה**, ולכן אין מרוץ. ה-`onBlur` נשאר סגירה **מיידית** בלי טיימר,
ומשמעותו אז תמיד "המשתמש עזב באמת" (לחיצה בחוץ / Tab).
**בדיקה שמכסה את זה חייבת `mouse.down()` → המתנה ≥300ms → `mouse.up()`.** ‏`click()` רגיל לא יתפוס.
*(נכון ל-29/07/2026 זהו המשטח-הצף היחיד מסוג זה ב-`src/`; שאר ה-`setTimeout` הם toast/משוב-העתקה/
סגירת-דיאלוג-אחרי-שמירה — אף אחד מהם אינו מתחרה בלחיצת-משתמש.)*

## 🔤 כיווניות — שני רכיבים חובה, לא בחירה

- **כל סכום כסף עובר דרך `<Money amount={n}/>`** (`src/components/Money.jsx`). הצורה הקנונית היא
  **מספר ואז ₪ מימינו**, זהה למה ש-`quotePdf.jsx` מדפיס ללקוח. כתיבת סכום ידנית ב-JSX **תתהפך**
  לפי ההקשר שסביבה, בלי שגיאה.
- **קבוצת שדות בכיוון הפוך (טווח-שעות, נוסחה) עוברת דרך `<LtrFieldGroup items={…}/>`**
  (`src/components/LtrFieldGroup.jsx`). שורת-התוויות ושורת-הערכים נגזרות מ**אותו מערך**; שתי
  שורות שנכתבות בנפרד מתפצלות בשקט וכל תווית נוחתת מעל הערך של מישהו אחר. קרה שלוש פעמים.
- **טווח מוצג כפקד אחד ב-LTR** — התחלה משמאל, סיום מימין — ולא כשני שדות ש-RTL הופך.
- **⚠️ מעבר-הכיווניות חל גם על מה שיוצא מהמערכת, לא רק על מסכים** (נוסף 30/07/2026, **מופע
  חמישי** של המשפחה הזו — והראשון מחוץ למסך). גוף-מייל עברי שנשלח כ-HTML **בלי `dir="rtl"`
  מוצג LTR אצל הלקוח**, והפיסוק נופל בצד הלא-נכון. ישי תפס זאת בגוף-מייל אמיתי, אחרי
  שהמסמך המצורף עצמו כבר היה תקין — כלומר **תקינות ה-PDF אינה מעידה על תקינות המייל שנושא אותו**.
  הפתרון מבני כמו תמיד כאן: `plainTextToEmailHtml` (‏`src/lib/email.js`) פולטת **עטיפת-כיווניות
  ותוכן יחד**, כך שאי-אפשר לקבל אחד בלי השני — אותו עקרון כמו `Money` ו-`LtrFieldGroup`.
  **הכלל המוכלל: כל תוצר עברי שיוצא ללקוח (מייל · PDF · קובץ · הדפסה) עובר את מעבר-הכיווניות
  בעצמו** — הוא לא "יורש" אותה מ-`<html dir="rtl">` של האפליקציה, כי הוא נקרא בכלי אחר לגמרי.

## ⌨️ שדה-מספר עם ערך התחלתי — `select()` בכניסה

שדה מספרי שנטען עם ערך (כמות=1 · הנחה=0 · המלצה מחושבת) **חייב** `onFocus={(e) => e.target.select()}`.
בלעדיו כל הקלדה מחייבת את המשתמש למחוק ידנית קודם — ישי דיווח על זה כמעצבן בשני שדות נפרדים
(29/07/2026). מיושם ב-`LtrFieldGroup` לכל שדותיו וב-`QuoteLineEditor` לשדה-הכמות.
⚠️ לחיצה **שנייה** בתוך שדה שכבר ממוקד לא תפעיל את זה — וזה נכון (ממקמים סמן, לא מוחקים הכול).
זו גם הסיבה שבדיקה אוטומטית שלוחצת פעמיים ברצף על אותו שדה תיכשל בלי שיש באג.

## 🧪 בדיקה ירוקה אינה הוכחה שהמסלול עובד

שתי תקלות אמיתיות באותו יום (29/07/2026) שבדיקות עברו עליהן בירוק:
- **בדיקה שמילאה טופס ואימתה סכום — אבל מעולם לא לחצה "שמור".** המסלול הקריטי היחיד (האם ההצעה
  בכלל נשמרת?) לא נבדק. **אחרי כל שמירה — לשלוף את השורה מה-DB ולאמת שדות**, לא להסתפק ב-UI.
- **בדיקה שלחצה מיידית על משטח-צף** בעוד לחיצת-אדם נמשכת יותר (ר' סעיף ה-blur). **לחיצה על
  משטח-צף נבדקת עם `mouse.down()` → `waitForTimeout(400)` → `mouse.up()`**, אחרת הבדיקה ירוקה
  והמשתמש חסום.

## 🔐 הרשאות ואבטחה (כלל ברזל 9)

**האכיפה האמיתית היא ב-DB (RLS). ה-UI הוא נוחות בלבד.**

- **מסך חדש הוא פתוח כברירת-מחדל.** כל `<Route>` תחת `<MainLayout>` בלי עטיפת `<ProtectedRoute>` גלוי לכל
  משתמש מחובר-ופעיל. אין lint, אין טסט, אין אזהרה. *(‏`index` ו-`profile` פתוחים במכוון.)*
- **`allow` הוא מחרוזת עברית גולמית שחייבת להיות זהה-בייט לשורה בטבלת `modules`** (`allow="לקוחות"`,
  `allow='דו"חות'` — מרכאות-בודדות ב-JSX בגלל הגרש). טעות-הקלדה **דוחה את כולם בשקט** (deny-by-default).
- **`allow` מערבב שני מרחבי-שמות** — הפונקציה בודקת קודם `roleName` ואז את מפת-ההרשאות. תפקיד שיקבל
  שם זהה למודול יקבל גישה גורפת למסך שלו.
- **הסרגל ושומר-המסלול חלוקים על "אין שורת-הרשאה":** הסרגל מציג מודול ש-`permissions[name] === undefined`
  (כי הוא בודק `!== 'blocked'`), אבל השומר דוחה. **זו התנהגות מכוונת** — המשתמש רואה פריט, לוחץ, ומקבל
  "אין לך הרשאה". נראה כמו באג, אינו באג.
- **הרשאות מודולי-המערכת אינן ניתנות למתן מה-UI** — מסך המטריצה מסנן החוצה את `ניהול הרשאות` ו-`הגדרות
  מערכת`. מתן גישה לתפקיד לא-מנכ"ל דורש UPDATE ישיר ב-DB.
- **ה-session מת עם הטאב** (`sessionStorage`, במכוון). ריענון ו-HMR באותו טאב שורדים; סגירת טאב או טאב שני — לא.
- **נעילת-הכניסה נכשלת פתוח במכוון** — שגיאת ה-RPC `check_login_lock` מושלכת, ואז `lockedUntil=null`
  והכניסה ממשיכה. פונקציית-נעילה שבורה = ביטול שקט של מנגנון 5-הניסיונות.
- **שגיאת OAuth חייבת לעבור דרך `authError` שב-AuthContext** — Google חוזר ל-`/` ולא ל-`/login`, ולכן
  state מקומי במסך-הכניסה לא ייראה כלל.

## 🚨 RLS — הכשל השקט המרכזי של הפרויקט

**טבלה עם RLS מופעל ואפס policies מחזירה תוצאה ריקה עם `error: null`.** הלקוח מדווח "אין שורות",
לעולם לא "נדחה". **מסך ריק = לחשוד ב-policies לפני שמחפשים באג ב-UI.**

- **כרגע deny-all (RLS פעיל, אפס policies):** `projects` · `hostesses` · `salary_reports` ·
  `assignments` · `logistics`. *(‏`login_attempts` — deny-all **מכוון**, הגישה רק דרך 3 פונקציות DEFINER.)*
- **⚠️ כל UPDATE/INSERT חייב `.select()` ובדיקת מספר-שורות אחריו.** בלי זה, כתיבה שנחסמה ע"י RLS מחזירה
  `{data: null, error: null}` — כפתור-שמירה שמציג "נשמר" ולא שינה כלום. **אין כלל-lint שאוכף את זה.**
  התבנית הקיימת: זריקת `RLS_DENIED` סינתטי (`02_customers/api.js`). *(כל אתרי-הכתיבה בקוד עומדים
  בזה נכון ל-29/07/2026 — החריג האחרון, `PermissionsMatrixPage`, נסגר אז.)*
- **ה-policies מזוהות לפי מחרוזת `module_name` בעברית.** שינוי-שם של שורה ב-`modules` הופך את תת-השאילתה
  ל-NULL → הטבלה נעשית deny-all **בשקט**, וגם שער-הלקוח נסגר.
- **משתמש `inactive` רואה כל טבלה עסקית כריקה** — `current_user_role_id()` מסננת על `status='active'`.
  אותה חתימה בדיוק כמו "חסרות policies".
- **חריג יחיד שנכשל בקול:** טריגר-הנעילה של `quotes`/`quote_services` זורק `P0001` עם הודעה בעברית על
  כל UPDATE/DELETE בשורה שאינה `in_progress`.

## 🧩 עריכות מצומדות — לשנות ביחד או שנשבר בשקט

**הוספת מודול עסקי נוגעת ב-5 מקומות; שניים מהם נכשלים בשקט מוחלט:**
1. שורה בטבלת `modules` ‏(DB) · 2. **`MODULE_META` ב-`Sidebar.jsx`** — חסר ⇒ `return null` ⇒ **המודול
לא מופיע בסרגל בכלל** · 3. ‏`<Route>` + `<ProtectedRoute allow="…">` ב-`App.jsx` · 4. **`GROUPS` ב-
`PermissionsMatrixPage.jsx`** — המטריצה עוברת על `GROUPS` ולא על `modules`, ולכן מודול חסר **לא יופיע
במטריצה ואי-אפשר לתת עליו הרשאה** · 5. ‏policies עם אותה מחרוזת-שם בדיוק.

- **רוחב הסרגל הוא מספר-קסם משולש:** `w-60`/`w-16` ↔ `mr-60`/`mr-16` ↔ `right-60`/`right-16`
  בשלושה קבצים, מונעים מאותו בוליאני. שינוי אחד = חפיפה או פער, בלי שגיאה.
- **`CUSTOMER_TYPE_LABELS` חייב להיות 1:1 עם ה-CHECK ב-DB** — והוא גם מפתח-המיון (ממיינים לפי **התווית
  העברית**, לא לפי ה-enum).
- **חוזה ה-E2E:** ערכי `data-testid` **וגם** מחרוזות `title` בעברית (`העבר לארכיון`, `שחזר מהארכיון`,
  מחזור ההרשאות `צפייה ועריכה`/`צפייה בלבד`/`אין גישה`). **שינוי `title` אינו שינוי קוסמטי.**
  ⚠️ ‏`E2E` **אינו רץ ב-CI** ומדלג על עצמו בשקט כשאין credentials — **"ירוק" עלול להיות "הכול דולג".**
- **סימן U+05F4 (גרשיים) ב-`כתובת דוא״ל`** — placeholder שנתפס מילולית ע"י E2E. הקלדה מחדש עם `"` רגיל שוברת.
- **`docs/schema.sql` הוא snapshot** — גופי-ה-RPC של מודול 3 קיימים **רק** בקובץ המיגרציה; ב-snapshot
  יש חתימות בלבד. **ה-DB מקדים את הקוד:** 5 מיגרציות מודול-3 חיות, כולל **2 עבודות pg_cron** שרצות
  יומית ומשנות שורות `quotes` — בלי שום קוד ב-`src/` שמזכיר אותן.

## 📧 שליחת מייל — מנוע אחד לכל המערכת (נוסף 30/07/2026)

**כל מייל שהמערכת שולחת עובר דרך `src/lib/email.js` ופונקציית-השרת `send-email`. אין מסלול שני.**
ב-`params` יש **6 תבניות-מייל** — הצעת-מחיר (#10) · זימון-משמרת (#11) · משוב-לקוח (#12) ·
חשבונית (#13) · ביטול-משמרת (#14) · דוח-שכר (#20) — כלומר **מודולים 4, 8 ו-11 ישלחו מיילים גם הם**.

- **מה חי במנוע** (משותף, נבדק פעם אחת): מילוי-תבנית · חוזה-חמשת-השדות מול Make · תקרת-גודל
  לקובץ המצורף · סיבת-השבתת-הכפתור · **שלושת** מצבי-התוצאה.
- **מה חי אצל המודול השולח:** אילו placeholders יש לתבנית שלו, מה הנושא, ומי הנמען.
  התבנית של הצעת-מחיר: `src/lib/quotes.js` (`fillQuoteEmailTemplate`/`buildQuoteEmailPayload`).
- **⚠️ שלושה מצבי-תוצאה ולא שניים.** פסק-זמן/תקלת-רשת = **"לא ידוע"**, לא "נכשל": המייל אולי
  כן נשלח, ו"נכשל" יגרום למשתמש לשלוח שוב — כלומר הלקוח יקבל את ההצעה פעמיים.
- **⚠️ שם-שדה בחוזה = שינוי גם בתרחיש Make.** שדה שיתווסף/ישונה בקוד בלי שינוי מקביל
  במבנה-הנתונים `regin-quote` פשוט **לא יגיע למייל, בלי שגיאה**.
- **"האם כבר נשלח" נענה רק מטבלת `email_log`** — לא מ-state של קומפוננטה (שמת ברענון-דף).
  הכתיבה ליומן היא של פונקציית-השרת בלבד; יומן שהדפדפן כותב אליו אינו ראיה.
- ⛔ **אל תעתיק את הלוגיקה למודול חדש.** ‏`jscpd` חוסם ב-CI, אבל הסיבה האמיתית היא שתיקון
  (תקרה, ניסוח, מצב-שלישי) חייב לחול על כל השולחים בבת-אחת.

## 📐 מוסכמות ארכיטקטורה (כלל ברזל 14 — SSOT)

- **לוגיקה עסקית פעם אחת ב-`src/lib/` + בדיקת-יחידה לצידה. ה-UI רק מייבא.** לפני כתיבת helper — לחפש קיים.
- **שאילתות מודול מרוכזות ב-`src/modules/NN_name/api.js`** — לא מפוזרות בקומפוננטות.
  ⚠️ **מודול 1 מקדים את המוסכמה ולא הוסב** — 6 קבצים בו פונים ל-`supabase` ישירות. **מודול חדש: תמיד `api.js`.**
- **כל לוגיקה עסקית מקבלת הערת "why" בעברית** (כלל 3) — בלי שיבקשו.
- **איפוס טופס בדיאלוג = remount דרך `key`, לעולם לא effect שמסנכרן props→state.**
  ⚠️ שתי מלכודות: (א) הכללת `dialogOpen` ב-`key` הורגת את אנימציית-היציאה של Radix; (ב) "תיקון" עם
  `useEffect` נופל על `react-hooks/set-state-in-effect` שהוא **שגיאה** בקונפיג הזה.
- **`Date.now()` אסור בגוף render/`useMemo`** — `react-hooks/purity` היא שגיאה קשיחה. לחשב בהאנדלר ולהעביר כ-prop.
- **`SelectItem value=""` נזרק ע"י Radix** — התבנית: מחרוזת-סנטינל (`ANY_TYPE`) שממופה בחזרה ל-`undefined`.
- **שורות-טבלה לחיצות** — כל פקד בתוך שורה חייב `onClick={(e) => e.stopPropagation()}`.

## 🔎 ממצאים פתוחים (עודכן 29/07/2026 — מה שתוקן נמחק מכאן)

- **מצב-כהה נשבר ברמת-המערכת, לא רק ב-`--primary`.** הטוקן תחת `.dark` נשאר אפור (`index.css:96`),
  **ובנוסף** 32 צבעי-אור מקודדים-קשיח (`bg-white`, `text-slate-800`) ב-18 קבצים. אין מתג מצב-כהה
  ולכן `.dark` לא מופעל אף פעם — לא נגיש היום. **תיקון הטוקן לבדו לא ייתן מצב-כהה תקין.**
  ⏸️ נדחה ל-M12 (הכרעת-ישי 29/07); שינוי-צבע דורש אישור ממילא (כלל 8).
- **‏`MarketingPanel` הוא היחיד ב-`src/` בלי שום בדיקה אוטומטית** — לא unit ולא E2E. שינוי בו
  מאומת בצילומי-מסך בלבד. ‏🚧 מ10 (זרימת-שיווק E2E).

> ⚙️ **מורכבות/כפילות/קוד-מת אינם "ממצאים פתוחים" יותר** — מאז 29/07/2026 כללי `sonarjs`
> ברמת `'error'` ו-jscpd/knip חוסמים ב-CI. קוד שעובר את השער *הוא* הראיה; אין רשימה לתחזק.
> החולשה היחידה המפוטרת (`react-router`) מודפסת בכל ריצה של `npm run audit`.
