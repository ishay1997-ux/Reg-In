<div dir="rtl">

# תוכנית תיקונים — סקירת הקוד המקיפה של 31/07/2026

> ✅ **סבב A (שומר המע"מ) נסגר 31/07/2026 09:25 — הפרומפט שלו נמחק מכאן** לפי מחזור-החיים
> שלמטה. הראיות: `docs/micro_guides/module-3.md` §9 · `docs/db_roadmap.md` §10 (מיגרציה
> `20260731085335`) · `src/modules/03_quotes/CLAUDE.md` §"שומר-המע"מ".
> ✅ **סבב B (שלושת ה-`catch`) נסגר 31/07/2026 10:47 — הפרומפט שלו נמחק מכאן.** הראיות:
> קומיטים `35264b3` · `8460737` · `e23faa4` · `e2e/load-failure-guards.spec.js` (3 בדיקות,
> כל אחת הורצה גם עם הכשל מוחזר) · פונקציית `send-email` גרסה 3.
> ✅ **סבב D (הודעות-השרת + מוצר מושבת) נסגר 31/07/2026 14:45 — הפרומפט שלו נמחק מכאן.**
> קומיט `a95cbee`; **שני אישורי-העין של ישי ניתנו** (פריסת תג "מוצר מושבת" · שם-מוצר במקום
> מק"ט במסמך). הראיות: `quoteServerErrorMessage` ב-`src/lib/quotes.js` + 8 בדיקות-יחידה ·
> ‏`e2e/server-messages-and-inactive-product.spec.js` (6 בדיקות, כל שומר נצפה **נכשל**
> כשהוחזר) · `PROJECT_MASTER §7.34` (הרחבת ההכרעה לשורות קיימות) · `module-3.md` §9 (14:20).
> ⚠️ **תוך כדי תוקן גם פליק E2E קיים-מראש** ב-`load-failure-guards.spec.js` (מרוץ
> ‏signOut→remount שרוקן את שדות-הכניסה) — בבדיקה בלבד, קוד-המסך לא נגוע.
> ✅ **סבב G (מסלול-השחזור + ארבעה תיקוני-מסד) נסגר 31/07/2026 16:30 — הפרומפט שלו נמחק מכאן.**
> קומיט `b3470f2`. הראיות: `PROJECT_MASTER §7.86` (הכרעת-ישי: ‏MCP = מסלול-החלה יחיד,
> התיקייה תיעוד) · §7.8↳ (15/IP/שעה) · §7.83↳ (פיצול `products.cost` ל-`product_costs`) ·
> ‏`db_roadmap` §0.0 + §10 (המספרים החיים + אימות-אחרי-החלה מלא) ·
> ‏`e2e/cost-visibility.spec.js` (3 בדיקות, שני הכיוונים) · שער ירוק, 366 יחידה, E2E 39.
> ⚠️ **שתי ההכרעות שסבב C היה מאבד נרשמו סוף-סוף ב-§7** — הן חיו רק בקובץ הזה.
> 🔴 **ותקרית שמתועדת בראש קובץ-המיגרציה:** ההחלה קרסה פעמיים בכלי, ובכיווץ המטען הושמטו
> בטעות שתי פונקציות בעוד מחיקת-העמודה כן רצה. תוקן קדימה. **הלקח: כיווץ הוא עריכה.**
> ✅ **סבב C (שלוש הכרעות-ישי) נסגר בפועל 31/07/2026 16:30 — הפרומפט שלו נמחק מכאן.**
> ‏(1) הגבלת-קצב ו-(3) פיצול-עלות נבנו בתוך סבב G עצמו (`b3470f2`) ונרשמו ב-§7.8↳/§7.83↳.
> ‏(2) מנוע-המייל למודולים 4/8/11 רשום ב-`PROJECT_MASTER §6` שורה 275↳ (בניסוח מדויק ומורחב
> ממה שהוצע כאן במקור). **שלוש-מתוך-שלוש — אין מה לפתוח לו שיחה נפרדת.**
> **נשארו שניים: E · F.**
>
> **מה הקובץ הזה:** שבעה פרומפטים מוכנים, אחד לכל שיחת-תיקון. ישי מעתיק פרומפט, פותח שיחה
> חדשה, ומדביק. **כל פרומפט עצמאי** — הוא לא מסתמך על שיחת-הסקירה שבה נולד.
>
> **מחזור-חיים:** סבב שנסגר → מוחקים את הפרומפט שלו מכאן. הקובץ מתרוקן ונמחק מעצמו.
> אין כאן רשם חדש לתחזק. *(עקרון-הגריעה F1: זו תוספת. מה שהיא גורעת — את הצורך שישי
> יזכור מה נשאר פתוח מהסקירה. שתי השורות ל-`PROJECT_MASTER §6` — מנוע-המייל ו-`listQuotes` —
> נחתו 31/07/2026 16:52; ר' ההערה בתחתית הקובץ.)*

## ⚠️ שלוש האזהרות — חלות על כל שבעת הפרומפטים

> **כל פרומפט נפתח בשורה שמפנה לכאן.** הפרומפטים מועתקים אחד-אחד לשיחות נפרדות, ולכן מה
> שכתוב כאן לא "נוסע" איתם מעצמו — הפניה מפורשת היא הדרך היחידה שהוא ייקרא. *(עקרון
> ההיפרשׂפות ההדרגתית: הפרומפט נשאר קצר, והמשמעת נטענת מהריפו כשהיא נחוצה.)*

**1. מספרי-השורות יזוזו.** הם נמדדו ב-31/07/2026 03:49 על `f1c1f57`. ברגע שהתיקון הראשון
נוחת, השאר זזים. לכן כל פרומפט מצטט **קטע-קוד מילולי + שם-סימבול** ולא רק מספר — והוראה
ראשונה בכל פרומפט היא לאמת שהקטע עדיין שם.

**2. הפרומפטים מתארים בעיה, לא פתרון.** בכמה מהממצאים הפונקציה שצריך לשנות משרתת קוראים
נוספים שלא נעקבו כולם בסקירה. פרומפט שיכתיב "החלף שורה X ב-Y" יגרום לביצוע במקום לבדיקה —
בדיוק הכשל שהסקירה נועדה למנוע. **ההכרעה על צורת התיקון היא של סשן-התיקון, מול הקוד החי.**

**3. שומר שלא נצפה נכשל — אינו שומר.** רוב שבעת הסבבים מוסיפים **הגנה** (סירוב להפיק מסמך,
זריקת שגיאה, מצב "לא ידוע"). הגנה שראית אותה *עוברת* אינה מוכיחה דבר — היא מוכיחה שהמצב
התקין תקין. **לפני שסבב נסגר: החזר את הכשל בכוונה, ראה את ההגנה צועקת, והחזר.** ואז אמור
בפירוש מה נצפה ומה לא.
> יש לזה תקדים מפורש בפרויקט הזה: כשנבנה שער-האבטחה ב-29/07, ישי דרש **להוכיח שהוא נכשל**
> (הפטור הוסר → `exit=1`, הוחזר → `exit=0`) — ולא הסתפק בכך שהוא עובר. שער שנכשל בשקט גרוע
> משער שאינו קיים.

**מתי להפסיק ולבקש כלי אחר:** אם באג לא נפתר אחרי **שני** ניסיונות אמיתיים — לעצור, לומר
זאת לישי, ולהציע להדליק את `superpowers` בשביל `systematic-debugging` (הטריגר כתוב
ב-[`docs/toolbox.md`](toolbox.md); הוא כבוי בכוונה, ⚠️ ומכבים אותו בחזרה אחרי השימוש).
לא להתעקש בלי כלי.

---

<!-- סבב C נסגר 31/07/2026 16:30 — ר' ההערה בראש הקובץ. הפרומפט נמחק לפי מחזור-החיים. -->

---

## 🟡 E — ניקוי: הערות שמשקרות + חמישה איחודים בטוחים (הבא בתור)

```text
לפני הכול: קרא את "שלוש האזהרות" בראש docs/audit_2026-07-31_fix_plan.md — הן חלות על הסבב הזה
(מספרי-שורות זזים · הפרומפט מתאר בעיה ולא פתרון · שומר נבדק בהחזרת-הכשל, לא בכך שהוא עובר).

משימה: אפס שינוי התנהגות. שני חלקים.

חלק 1 — שלוש הערות שסותרות את הקוד שמתחתיהן:

(א) src/modules/01_auth/PriceTiersDialog.jsx, ההערה הפותחת: "הקבוצה כולה נערכת כיחידה
    ונשמרת במחיקה-והכנסה-מחדש (replacePriceTiers) — אותה מוסכמה כמו replaceCustomerContacts
    במודול 2". **זו המסוכנת.** replacePriceTiers הוא היום upsert-ואז-מחיקת-הנגרעות, וסדר
    זה תוקן ב-30/07 אחרי אובדן-נתונים אמיתי (חמש מדרגות של B-REG-TAG נמחקו מהמסד החי).
    ההערה מתארת בדיוק את הסדר שגרם לנזק, ומציגה אותו כ"מוסכמה" — סשן עתידי עלול "ליישר"
    את הקוד לפיה ולהחזיר את הבאג.
(ב) src/modules/01_auth/pricesApi.js: "אותה חולשה קיימת עדיין ב-replaceCustomerContacts
    (מודול 2, מוזג) — נרשמה לסקירת 3.7, לא תוקנה". תוקנה. src/modules/02_customers/api.js
    הוא כבר קריאת-מזהים ← הכנסה ← מחיקת-הישנים, ומתועד שם כמתוקן באותו תאריך.
(ג) src/modules/02_customers/CustomersPage.jsx: שתי הערות צמודות סותרות על §7.34. הראשונה
    אומרת "הוצג לו 30/07 ולא הוכרע. אין לבנות כאן guard לפני הכרעתו" — והשנייה, שורות
    ספורות מתחתיה, אומרת "הכרעת-ישי 30/07/2026: מתריעים, לא חוסמים", והקוד מממש את האזהרה.
    §7.34 מסומן 🟢 סגור. הפסקה הראשונה מיושנת ומורה לא לבנות את מה שכבר נבנה.

חלק 2 — חמישה איחודים. כל אחד נבדק ואומת שההפרדה **אינה** מכוונת:
1. `toError` זהה-בייט בשלושה קבצים (02_customers/api.js · 03_quotes/api.js ·
   01_auth/pricesApi.js). מודול 3 חיקה את מודול 2 במודע — זו העתקת-תשתית, לא בחירה. להוציא
   ל-src/lib/ עם בדיקה. בונוס באותה נגיעה: האידיום
   `if (!data || data.length === 0) throw toError({ code: 'RLS_DENIED' }, '…')` חוזר שש פעמים.
2. `QUOTE_STATUS_LABELS` ב-src/lib/quotes.js **מת** (אימות: הצרכן היחיד הוא קובץ-הבדיקה
   שלו), בעוד אותן שלוש תוויות מוכרזות מחדש ב-QuotesPage.jsx וב-CustomerDetailsPage.jsx.
   הבית הקנוני מת והעותקים חיים. להחיות אותו כמקור משותף. ⚠️ **לא** לדחוף לתוכו את ענף
   "פגה" — זו החלטת-מוצר של עמוד-הלקוח בלבד, והפרדתה מכוונת.
3. `<LoadingOrError>` קיים ומשמש שבעה קבצים, אבל PricingParamsCard.jsx ו-PriceTiersDialog.jsx
   (שנבנו חמישה ימים אחרי חילוצו) כותבים את אותו JSX ביד. שתי החלפות של ארבע שורות.
4. שש מחרוזות עבריות כפולות מילולית בין QuotesPage ל-CustomerDetailsPage (ארבע תוויות-פעולה
   + שני טוסטים). ⚠️ **רק המחרוזות.** החלטת §9 שלא לחלץ רכיב-עטיפה משותף לשני המסכים נבדקה
   ואומתה כנכונה — לא לפתוח אותה מחדש.
5. `isValidPositivePrice` / `isValidPositiveInt` ב-src/lib/validators.js מיוצאים, נבדקים,
   ואין להם אף צרכן-ייצור — בעוד אותו כלל בדיוק כתוב מחדש בתוך validateTierRows ב-pricing.js.
   או לייבא, או למחוק את שניהם עם בדיקותיהם. לא להשאיר כלל שנבדק בבית אחד ונאכף באחר.

מה ייחשב "תוקן": `npm run gate` ירוק, 327 הבדיקות עוברות (או יותר), ואפס שינוי במסכים.
```

---

## 🟡 F — בדיקות בשכבה שבה ההגנה באמת חיה (צומצם 31/07/2026 — רוב הסבב נבלע לתוך פזה 4)

> ⚠️ **הרחבה, לא תחליף — סעיף לישי לפני שממשיכים.** רוב §F חופף כמעט-מילה-במילה למה
> שכתוב כבר במדריך-המיקרו של מודול 3, פזה 4 (הבדיקות ברמת-RLS/RPC/הרשאה-בשרת ל-4.1/4.2/4.3):
> הרצתו כסבב נפרד הייתה כותבת את אותן בדיקות פעמיים. ‏**נשארו כאן רק שלושה פריטים ש-פזה 4
> אינה מכסה** — הובטח לישי שהצמצום לא יפגע בתוצאה; לוודא שאלה נכנסים ל-4.3/4.4 כפריטי-תוכן,
> לא שנעלמים בשקט. הפלייק המתועד (`e2e/permissions.spec.js`, "CEO משנה תא במטריצה") ופער
> ה-`.env.local` שנסגר — כבר מתועדים ב-`module-1.md §9` ו-`module-3.md`, לא כאן.

```text
לפני הכול: קרא את "שלוש האזהרות" בראש docs/audit_2026-07-31_fix_plan.md. אזהרה 3 היא **לב
הסבב הזה** — בדיקה שלא נצפתה נכשלת אינה ראיה.

משימה: שלושה פערים ספציפיים שלא מכוסים במקום אחר בפרויקט (לא סבב-הרשאות כללי — זה כבר
נבנה בפזה 4 של מודול 3, ר' docs/micro_guides/module-3.md §4.1–4.4).

(1) פונקציית השרת send-email מחוץ לכל בדיקה אוטומטית — אומת בהרצה:
    npx eslint supabase/functions/send-email/index.ts
    → "File ignored because no matching configuration was supplied"
    אין שלב deno ב-.github/workflows/ci.yml, ואין בדיקת E2E שנוגעת בה. זהו הקובץ שמחזיק את
    כתובת ה-webhook ואת השער בין משתמש-צפייה לשליחת מסמך ללקוח. למה דחוף: מיגרציות הן
    append-only ומדיניות מזוהה לפי מחרוזת עברית — מיגרציית-מודול עתידית יכולה לשבור מדיניות
    קיימת בשקט, בלי שגיאה ובלי בדיקה אדומה. הצעת-כיוון (לא מחייבת): שלב CI בן-שורה שמריץ
    `deno check`.

(2) src/modules/03_quotes/quotePdf.test.jsx — הבדיקה "מתעלם מסכומים שמוזרקים מבחוץ" מאמתת
    רק `not.toThrow()`. מזריקה `total: 99999, subtotal: 1` ואינה בודקת **אף מחרוזת בפלט** —
    תישאר ירוקה גם אם ההתעלמות תפסיק לעבוד ו-99,999 ₪ יודפס ללקוח.
    🎁 העוזר `collectStrings` והתבנית כבר יושבים באותו קובץ (ר' הבדיקה הצמודה שמאמתת
    ‏`מע"מ (18%)` / `6,319 ₪`) — התיקון הוא הרחבה, לא כתיבה מאפס.

(3) src/lib/quotes.test.js, בדיקות המיון — שתיים משלוש הרשומות זהות גם בסכום וגם
    בתאריך-האירוע, כך שהטענה על הסדר מתקיימת חלקית מיציבות-המיון של JS ולא מהמשווה עצמו.
    זו בדיוק המשפחה שמתועדת ב-src/CLAUDE.md ("נתוני-בדיקה אחידים = בדיקה שעוברת על קוד שבור").

מה ייחשב "תוקן": שלושת הפערים סגורים; אין שינוי-התנהגות במסכים; `npm run gate` ירוק.
```

---

✅ **שתי השורות ל-`PROJECT_MASTER §6` נחתו — 31/07/2026 16:52.** מנוע-המייל: §6 שורה 275↳
(נוסחה מחדש ומדויק יותר משהוצע כאן — כתב אותה סשן-סבב-C ישירות). ‏`listQuotes`: §6 שורה
276↳, הועתקה מכאן במלואה. אין עוד עבודה בקטע הזה.

</div>
