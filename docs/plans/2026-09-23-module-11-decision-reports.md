<div dir="rtl">

# מ11 · דוחות-החלטה — שדרוג מ3 · מ4 · מ6 במקום

> **נכתב 23/09/2026 ע"י סשן-הסגן · עבר ביקורת חיצונית (Opus, קריאה-בלבד) ותוקן לפיה — כל ממצא אומת מחדש לפני שנכנס.**
> 📌 **✏️ 23/09/2026 — גובה לריפו בקומיט הראשון של ענף-הבנייה `ishay/m11-decision-reports`. מכאן והלאה העותק הזה הוא הסמכות; העותק ב-`~/.claude/plans/` הוא טיוטה היסטורית.**
> 📌 **זה גם מסמך-המסירה** (ישי: *"שיהיה מסמך מסירה אחד שמתעדכן בסוף סשן"*) — §10 מתעדכן במקום בכל עצירה.

---

## 1 · העיקרון

**כל דוח חייב לענות על: *"מה המנהלת עושה אחרת מחר?"*** ישי: *"שגורם למנהלת באמת לעשות משו ולא סתם להנהן"*.
מ3 (מגמות) · מ4 (הנחות ורווחיות) · מ6 (קהל מול צוות) — התשובה היא "מהנהנת". שאר ה-13 עוברים או משמשים מבט-על.

## 2 · 🔑 ההחלטה המבנית: **לשדרג במקום, לא למחוק ולבנות**

אותו `rpc` · אותו `slug` · **אותה חתימה** — תוכן חדש, שם-תצוגה חדש.
**למה (נמדד בביקורת 23/09):** מחיקה שוברת שרשרת — קישורי-KPI **במסד** ממ2 (`j3…sql:1556,1567`) וממ14 (`:3105`) שנעלמים בשקט (`ReportsPage.jsx:77-86`) · ספירות מוצהרות שהשער בודק (`screens-approved.md` · `spec.md` §1.4) · `e2e` · הרשאות (`drop`+`create` מאפס ACL ו-`anon=EXECUTE` כברירת-מחדל — כותרת K1).
⇒ **`create or replace` עם אותה חתימה = אין עומס-יתר (42725), ההרשאות נשמרות, הקישורים עובדים, והספירה נשארת 16.** *(ישי 17/09: "לא להסתבך ולא להמציא את הגלגל")*

## 3 · שלושת הדוחות

| במקום | הדוח החדש | מה המנהלת עושה מחר | הנתונים |
|---|---|---|---|
| **מ3** `report_m03_trends` | **ה1 · הצעות: מה נסגר ומה הלך לאיבוד** | רואה כמה כסף אבד על הצעות שפגו בלי מענה, ואילו סוגי הצעות נסגרים ⇒ מתעדפת מעקב. **לרשימה עצמה — קישור לקיימת**, לא בנייה מחדש | דחיות `פג תוקף`/`מחיר` לפי רבעון (**עובד על הנתונים של היום**) · שיעור-סגירה לפי סוג-לקוח · חדש/חוזר · גודל · זמן עד-האירוע. **המכנה בלי `נפתחה בטעות`** (§7.82) |
| **מ4** `report_m04_discounts` | **ה2 · מה הנחה קונה** | יודעת מאיזו הנחה לוותר — *"מעל 10% הסגירה לא עלתה והרווח ירד"* | לפי רצועת-הנחה (`applied_customer_discount + manual_discount`): שיעור-סגירה · רווח (**דרך `finance_project_money()`**, לא `final_profit` שמתמלא רק בארכוב) · שביעות-רצון. ⚠️ חברות-הפקה מקבלות 5–15% לפי סוג (`plan.mjs:231`) ⇒ **לפצל לפי סוג-לקוח או להצהיר** |
| **מ6** `report_m06_staffing` | **ה3 · מה הורס אירוע** | רואה אירועים קרובים בסיכון ומתקנת שיבוץ; יודעת שאירוע רע = לקוח שלא חוזר | ציון-משוב מול: יחס אורחים-לדיילת · היעדרות/איחור (`attendance_status` · `lateness_level`) · ראש-משמרת (`is_shift_lead` — **קיים ברוב האירועים**, `plan.mjs:1151`) · ציון-אירוע-ראשון מול חזרה. **אגרגטים בלבד — אף דיילת בשמה, אפס `hourly_rate`** |

**שלושה עונים על כל שאלות-ישי:** הנחה↔שביעות-רצון (ה2) · יחס-דיילות↔שביעות-רצון (ה3) · מי חוזר (ה3) · מה סוגר הצעה (ה1) · מתי לתת הנחה ומתי לוותר (ה2).

**כללי-חובה לשלושתם:**
- **שורת-החלטה אחת בראש** (`so_what` בחוזה). גרף בלי שורה כזו — לא נכנס.
- **n גלוי ליד כל מספר** · **"אין מספיק נתונים"** מתחת לסף (הכרעה בשלב 1) · **"קשר, לא סיבה"** — שורה קבועה.
- **חוזה C8 כמו שאר 16** — מפתחות: `population/window/tiles/chart/columns/rows/so_what/definitions/drill/meta{measured_at, missing_params, frozen_count, row_total, notes, run, sort, customer_filter_ignored, drill_echo}` (דגם: `j2…sql:486-737`) · מספרים בתוך טקסט עברי עם סימני LRI/PDI · שער `assert_module_permission('כספים', array['edit','view'])`.
- **ייצוא:** גיליון-סיכום מוכן (לפי חודש + הממד הראשי) — **opt-in לפי דגל ב-payload**, כי `src/lib/reportsExport.js:238-256` משותף ל-16 ⇒ 13 האחרים לא משתנים (ובדיקה שמוכיחה זאת).
- **הסברים — בשכבת-ההטמעה בלבד** (ישי 17/09: *"מצב 0 זה מצב מוצר הייטק בוגר... כל ההסברים יהיו שם"*).

➕ **ה4 · אופציונלי, רק אם ה1–ה3 נסגרו לפני ~02/10 — "כיוון ה-Smart Match" בתוך מ17** *(ישי 23/09: "שמתי בדיילות המלצות למנהלת לגבי הפרמטרים בסמארט מאצ? משו כזה?")*
**נמדד:** אין היום שום המלצה על הפרמטרים. יש רק אריח "אימוץ המלצת Smart Match" במ17 (`j2…sql:3873-3884` · `:4003`), שמציג "אין עדיין נתון" עד שמצטבר `assignments.recommended_rank`.
**מה ייכנס — שורות-פעולה על פרמטרים קיימים** (`src/lib/paramsRegistry.js:636-780`, קבוצת `smart_match`), כל אחת עם n וסף:
- ג'יני גבוה ⇒ *"שקלי להעלות את `שיעור_בונוס_הוגנות_לשבוע`"* — עם הערך הנוכחי לצד ההמלצה.
- כמה דיילות `שער_מרחק_קמ` פסל באירועים שנשארו בחוסר ⇒ *"השער פוסל X דיילות זמינות"*.
- אימוץ דרג-1 נמוך (כשיש נתון) ⇒ *"המנהלת בוחרת אחרת מההמלצה ב-Y% — המשקולות לא תואמות את שיקול-דעתה"*.
🚫 **לא סימולציה-לאחור** (הרצת השיבוץ מחדש עם פרמטרים אחרים) — יקר מדי לפני הכנס. **ההמלצה נשענת על מדד + ערך-הפרמטר, ומוצהרת כ"שקלי", לא כ"שני".**
⚠️ **שלב 0 בודק אם יש `recommended_rank` בכלל** — אם לא, השורה השלישית לא נכנסת.

🗂️ **נדחה לאחרי הכנס:** תחזית כוח-אדם · דמי-ביטול שלא נגבו · גיליון-סיכום לכל 16.
✏️ **ירד מהרשימה — כבר קיים** *(נמדד 23/09 בסקירת 13 הדוחות האחרים)*: **ימי-תשלום לפי סוג-לקוח** = כרטיס-התובנה "קצב-התשלום תלוי בסוג הלקוח" במ19 · **הערכה-מול-ביצוע של שעות** = עמודות "שעות-מתוכננות/שעות-בפועל" במ8 (`j2…sql:~1778`).

🔴 **כפילות שחייבים להימנע ממנה ב-ה3:** מ19 כבר מציג כרטיס-תובנה **"שביעות-רצון מנבאת חזרה"** (`j3…sql:~464`, דלת ל-מ20). ⇒ **ה3 מתמקד בגורמים של אירוע רע** (יחס · היעדרות · ראש-משמרת) **ומקשר למ19 לשאלת החזרה** — לא מחשב אותה שוב (כלל-ברזל 14).

### ✅ 13 הדוחות האחרים — נסקרו 23/09 לפי מה שהם מציגים בפועל *(לא לפי השם)*
**כולם עוברים את המבחן**, ולכן לא נוגעים בהם: מ9 (את מי לגבות) · מ21 (למי להתקשר) · מ15 (את מי לא לשלוח) · מ12 (טבלת "כמה להזמין לחודש הקרוב") · מ8 (15 הפרויקטים שחרגו) · מ20 (מה מכעיס) · מ22 (מה ההערות אומרות) · מ16 (מוקפא — חוב `hourly_rate`) · מבטי-העל מ2 · מ7 · מ14 · מ19 (דלתות לדוחות-הפעולה).
**הכי חלש: מ17** (עקומת-לורנץ אקדמית) — **נשאר**, כי סיפור-הכנס "ג'יני 0.46" יושב עליו, וטבלת "הדיילות שעונות הכי לאט" בו היא פעולה אמיתית.
**החיתוכים שכבר אושרו בפזה ב׳ של הטיפוגרפיה** (תוכנית-הטיפוגרפיה §6): 2 גרפים במ20 · כרטיס כפול במ22 *(והשלישי במ3 — מתייתר כאן)*.

## 4 · 🔴 הנתונים — זריעה ממוקדת, לא מלאה

**מודדים את המסד, לא את המחולל:** הציונים החיים שוכתבו אחרי הזריעה (`scripts/enrich-feedback-reasons.mjs:308` — `UPDATE projects SET feedback_score`).

**🚫 זריעה מלאה — נפסלה, והנימוק חייב לנסוע** *(ישי 23/09: "אין בעיה אם נדרשת זריעה מקיפה" — הסגן ממליץ נגד, בראיות)*:
① מספרי-הסיפורים של מ11 (ג'יני 0.46 · 236,382 ₪) **נכתבו ביד דרך MCP ואין קוד שמייצר אותם** (`scripts/seed-snapshot/README.md` §1) ⇒ זריעה מלאה **מוחקת אותם לתמיד** · ② `seed_reset` ייכשל: `salary_report_lines` ⇒ `on delete restrict` (`docs/schema.sql:1318-1345`) · ③ שורות-הדגמה ישנות (לקוחות 46–49, פרויקטים 3/7/8/13/14) מחוץ למרשם ולא יימחקו (`demo-seed.mjs:6-9`).

**✅ זריעה ממוקדת — שדה אחד: `projects.feedback_score` (+ סיבות קוהרנטיות)**, על אירועים שהסתיימו, לפי כללים מוצהרים:
- רצועת-הנחה ⇒ ציון *(לשאלת-ישי הראשונה)* · יחס אורחים-לדיילת נמוך / היעדרות / בלי ראש-משמרת ⇒ ציון נמוך · ציון-אירוע-ראשון ⇒ עקבי עם חזרה בפועל.
- **המנגנון:** Management API עם `SUPABASE_ACCESS_TOKEN` — כמו `enrich-feedback-reasons.mjs`.
  🔴 **נמדד 23/09 ע"י סשן-הטיפוגרפיה: המפתח שב-`.env.local` אינו בתוקף (401).** ⇒ **חלופה שלא דורשת מפתח:** פזה ב׳ החילה את כל כתיבותיה דרך **Supabase MCP** (`apply_migration` / `execute_sql`) — אותו ערוץ יכול להריץ את `UPDATE` הזריעה, **כקובץ SQL בריפו + אישור-ישי**. ⇒ **ברירת-המחדל: MCP. המפתח — רק אם MCP לא זמין.** ואם בכל זאת Management API ⇒ **תנאי-קדם לשלב 3: ישי מנפיק מפתח חדש** (Supabase ⇒ Account ⇒ Access Tokens) ומדביק אותו ל-`.env.local` **בעצמו** — הסשן לא רואה ולא מקליד מפתחות. בלי מפתח תקף — אין זריעה, וה2/ה3 נופלים לנקודת-הוויתור. **לבדוק בשלב 0, לא בשלב 3.** 🚫 **לא** מפתח-המנכ"לית: ל-`projects` אין מדיניות-כתיבה ⇒ 0 שורות **בלי שגיאה** (`20260903180958…`, כותרת ①).
- **לפני:** `scripts/seed-snapshot/capture-seed-snapshot.sql` · **חזרה:** `restore-seed-snapshot.sql`.
- 🚫 **לא נוגעים:** `quotes` (טריגר `quotes_lock_non_in_progress`, `schema.sql:792`; ועדכון מאפס את שעון-התוקף) · `assignments` (`trg_recompute_project_status` + מזין שכר) · הנחות (מזינות הכנסה ⇒ כל דוחות-הכסף).
- ⇒ **הנחה⇒סגירה לא נזרע.** ה1/ה2 מציגים את הנתון הטבעי ביושר — כולל "לא נמצא קשר" אם זה מה שיש.
- **רדיוס:** משתנים רק מספרי שביעות-רצון — מ19 (**כולל כרטיס-התובנה "שביעות-רצון מנבאת חזרה" — הזריעה חייבת לחזק אותו, לא להפוך אותו**) · מ20 · מ21 (עמודת "ציון-אחרון") · מ8 (עמודת "ציון-משוב") · ומה שמסך-הבית מציג מהם. שלב 0 מונה אותם במדויק, ושלב 8 מרענן.
- ⚠️ **ורענון 14/10** (`refresh-before-conference.sql`) מאפס `updated_at` של הצעות פתוחות ⇒ ה1 **לא** יכול לכתוב בכותרת "יפוגו תוך N ימים" — הוא נשען על היסטוריה, לא על התור הפתוח.

## 4ב · 🔤 יישור עם מערכת-הניסוח — כדי שלא יידרש סשן-טיפוגרפיה שני

> ישי 23/09: *"חשוב שלא תעשה עבודה כפולה ותיתישר עם המערכת והנוסחים שלה וכו שלא אצתרך לעשות סשן טיפוגרפיה שוב · אולי אני צריך ליצור ממנו איזה חוזה כזה במערכת"*.
> **הכרעת-הסגן: לא חוזה חמישי.** החוזה כבר קיים, בארבעה מסמכים ושני שערים — מסמך נוסף היה עותק שמתיישן. **מה שחסר הוא שהסשן הבא יעבוד דרכם, ושהם ייפלו לבד כשלא.**

**החוזה הקיים — חובה לקרוא לפני מחרוזת אחת, בסדר הזה:**
1. `docs/specs/module_11_reports/design-contract.md` — **§④א** (כללי-הניסוח) · **§②** רכיבים-משותפים (`Money` · `StatTile` · `ScoreCell` — לא רכיבים חדשים) · **§③** RTL ולשון-נקבה (§3.6) · **§⑤** חוזה-הגרפים · ושורת-ההסבר בראש (**23/09 — "למי שמעתיק מכאן"**).
2. `docs/specs/module_11_reports/stage2-review/m11-copy-rules.md` — R1–R30. **R3 = חוזה שלושת-החריצים.**
3. `docs/specs/module_11_reports/onboarding-layer-contract.md` — הסבר נכנס **רק** לשכבת-ההטמעה (`src/lib/onboardingCopy.m11.exec.js`); מצב 0 נקי.
4. `docs/plans/ui-copy-styleguide.md` — 🔒 נעול, חל על כל מחרוזת.
➕ **והדפוס החדש כתוב כבר בחוזה** (פזה ב׳, 23/09 — ראש `design-contract.md` + `onboarding-layer-contract.md` "מימוש 23/09"): **חובה לכל RPC חדש — `population.summary`** (שבב-היקף אחד, למשל *"246 אירועים שהסתיימו · מתוך 837"*) · אריח = שם · מספר · השוואה, **בלי תת-שורה** · משטח שלא מגיב למסנן ⇒ **עובדה בשורת-המסננים**, לא משפט על המסך · רשימת-שיא ⇒ **כותרת-כנה** (`surface.topN`), לא דפדפן.
5. **הדגם החי:** מ02 אחרי פזה ב׳ (שלב 4 — "דגם לאישורך", **אושר ע"י ישי**) — `Disclosure.jsx` · שבב-היקף אחד · כותרת כנה. **ה1–ה3 נראים ומדברים בדיוק כמוהו.**

**השערים — ומה צריך לשנות בהם:**
- `check:type-scale` (בשער) — אפס `text-[Npx]` בקוד חדש.
- `check:report-copy` = `src/lib/onboardingCopy.budget.test.js` (רץ בתוך `test:run`) — רמז ≤ 220 תווים + אין מפתח-רמז חסר.
  🔴 **יש בו פטור מתוארך בדיוק לשלושת הדוחות האלה:** `REPLACED_SURFACES = ['reports.trends.', 'reports.discounts.', 'reports.staffing.']` — *"הפטור נמחק יחד עם המשטחים"*. **בשדרוג-במקום המשטחים לא נמחקים ⇒ חובה למחוק את הפטור בקומיט שכותב את הרמזים החדשים**, אחרת הרמזים של ה1–ה3 פטורים מהתקציב לנצח.
- ➕ **מה שאף שער לא בודק היום: המחרוזות שבתוך ה-SQL** (`so_what` · תוויות-אריחים · `definitions`). ⇒ **ה-build מוסיף בדיקה אחת** (דפוס: `src/lib/exportSensitiveColumns.test.js`, שקורא קבצי-מיגרציה): בשלוש המיגרציות החדשות — `so_what` עד אורך שנקבע · אין לשון-זכר מתוך רשימת-המילים של §3.6 · מספרים עטופים ב-LRI/PDI. **כך הכלל נופל לבד, ולא נשען על זיכרון.**

✏️ **נמדד 23/09 לילה על הענף הסגור של פזה ב׳:** מיגרציות L1–L4 (`20260923180000…l1` עד `…220000…l4`) **לא נגעו במ3/מ4/מ6** (`grep report_m0[346]_` ב-`phase-b-migration/ops*.mjs` · `bodies.mjs` ⇒ 0). ⇒ הגוף החי שלהם = קבצי-J + K1. **אבל מ2 ומ14 — כן שונו** ⇒ כל נגיעה בדלתות של מ2/מ14 נבנית על **`bodies.mjs` `postL4(fn)`** (הגוף החי), **לא על `j3…sql`** — מלכודת 39 במסירת-הטיפוגרפיה. ואחרי כל החלה: md5 חי מול המחושב (מלכודת 41 שם).
**ומלכודת שפזה ב׳ יצרה:** מיגרציית-הטקסט `20260923180000_module11_l1_report_copy.sql` **שכתבה את הגופים החיים של 13 הדוחות האחרים** (דרך `phase-b-migration/ops.mjs` ⇒ `gen-migration.mjs`, מאומת ב-`md5check.mjs`). מ3/מ4/מ6 הוחרגו ממנה. ⇒ **לפני `create or replace` של מ3/מ4/מ6 — השווה את הגוף החי (`pg_get_functiondef`) לגוף שבמיגרציה האחרונה** (הטכניקה: `md5check.mjs`). הבדל ⇒ עצור, מישהו שינה אותו.

## 4ג · 🔨 שלוש שאריות מפזה ב׳ — **הוכרעו ע"י הסגן בהאצלת-ישי**, ונבנות בסשן הזה *(ענף אחד)*

> ישי 23/09 לילה, על שלוש שאלות-המוצר שפזה ב׳ השאירה: *"אין לי כח להכנס לזה רוצה להחליט לי?"* ⇒ **הכרעות-הסגן, הפיכות, כל אחת עם הנימוק:**

| # | השאלה | ההכרעה | הנימוק | איך |
|:-:|---|---|---|---|
| **ש1** | מ21: המשפט *"עמודות ואריחי ה-₪ בדף זה כפופים להרשאת מודול כספים."* גלוי לכולם | **להציג רק כש-`meta.money_masked = true`** | משפט על סכומים מוסתרים, כשאין סכום מוסתר, הוא רעש — וזה בדיוק מה שפזה ב׳ ניקתה. זו גם המלצת סשן-הטיפוגרפיה. **§7.100 הכריע רק שהמיסוך עובר למסד** — המשפט הוא החלטת-כותב, לא הכרעת-ישי (מסירת-הטיפוגרפיה §ח) | שינוי-לקוח ב-`CustomerSurface.jsx` + בדיקה לשני המצבים |
| **ש2** | מ19: אריחי-התובנה צפופים (שורת-השוואה *"הגדול הראשון שכן: … · מקומו ברשימת המניבים: 12"*, תוויות-זמן ארוכות) | **להחיל עליהם את כלל פזה ב׳**: תת-שורה שהיא **בסיס המספר** נשארת; השוואה שחוזרת על המסך ותוויות-זמן ארוכות — יורדות או מתקצרות | אותו כלל שישי אישר בדגם מ02 — לא כלל חדש. המוקאפ אושר לפני שהכלל נקבע | מיגרציית-טקסט על **`bodies.mjs` `postL4('report_m19_customers_overview')`**, md5 אחרי (מלכודות 39/41 במסירת-הטיפוגרפיה) — אישור-ישי על ההחלה |
| **ש3** | סדר-ההצגה בכנס (10 דק׳) | **מסלול מוצע**: מסך-הבית ⇒ הצעת-מחיר ⇒ כרטיס-פרויקט ⇒ שיבוץ חכם ⇒ כספים ⇒ דוחות (ה1–ה3 בראש) ⇒ ייצוא לאקסל. **ישי יכול להחליף סדר במילה.** | מסע של אירוע אחד מהצעה ועד תובנה — כך הדוחות הם השיא ולא עוד מסך | שלב 8: מעבר על המסלול ב-1536, צילום לכל תחנה, ורישום ב-`~/.claude/plans/conference-qa-2026-10-15.md` |

## 4ד · 🔴 תקן-הכרטיס — **הצעד הראשון של הבנייה, לפני ה1** *(נוסף 23/09 לילה, אחרי צילום-מסך של ישי מהאתר החי)*

> **ישי 23/09, על כרטיסי מ19 באתר החי:** *"לא הבנתי איך זו העבודה שהוא ביצע מזה אמור להיות. ככה לא ניראת מערכת SAAS מקצועית בכלל"*.
> **הוא צודק, והמדידה מאשרת** — הסגן צילם 6 דוחות חיים (מ19 · מ21 · מ14 · מ17 · מ9 · מ7, 1536, כמנכ"ל). **פזה ב׳ קיצרה הסברים, אבל לא תיקנה את מבנה הכרטיס**; היא עצמה סימנה ◐ *("7 מ-12 דוחות 224–366 תווים")*, והסגן העביר את זה כ"גמור" בלי להסתכל על המסכים. **זו טעות של הסגן, והיא הסיבה שהסעיף הזה קיים.**

**מה נמדד על המסך — חוזר ב-6 מתוך 6:**
1. **3–5 שורות מתחת למספר** — פירוקים, תאריכים, "כל הזמנים", "נכון ל-23/09", "12 החודשים האחרונים" **שחוזר בכל כרטיס** בעוד התקופה כבר כתובה בשורת-המסננים ובשבב-ההיקף.
2. **כותרת-טענה במקום שם-מדד** — *"הריכוזיות והנטישה עוד לא נפגשות"* ומתחתיה **"0"**; *"הסיבה השלילית הגדולה אינה קטגוריה"* ומתחתיה **"33"**. המספר לא אומר מה הוא.
3. **ערך-גיבור שאינו מספר** — שם-לקוח (*"ברק שיווק דיגיטלי"*) · מקף (*"—"*) · 0.
4. **כרטיסים בגבהים שונים ולא מיושרים למעלה** · במ17 **6 כרטיסים ⇒ שורה שנייה עם כרטיס יתום**.
5. **באנר-הסבר צהוב במצב 0** (מ21 *"שתי שיטות לאותה שאלה"*) — הסבר ששייך לשכבת-ההטמעה (ישי 17/09: *"מצב 0 זה מצב מוצר הייטק בוגר... כל ההסברים יהיו שם"*).

**התקן — כרטיס = ארבעה חלקים, ולא יותר** *(הדפוס המקובל בדשבורדים של SaaS: שם · ערך · שינוי · פירוט-בריחוף)*:
| חלק | מה | מה אסור |
|---|---|---|
| **שם** | שם-מדד, שם-עצם, ≤ 4 מילים | טענה/מסקנה (היא עוברת ל-`so_what`, השורה הירוקה) |
| **ערך** | מספר + יחידה | שם, מקף, או 0 בלי הקשר ⇒ הכרטיס לא מוצג, או שהמדד מנוסח מחדש ("0 מתוך 5") |
| **השוואה** | שורה **אחת** (אשתקד / מתוך / מול) | תאריכים · "כל הזמנים" · פירוקים |
| **פירוט** | כל השאר — בסמל ⓘ בכרטיס, נפתח בריחוף/לחיצה (**אותו `Disclosure.jsx` של שבב-ההיקף**) | להופיע גלוי |
+ **גובה אחיד, יישור עליון · לכל היותר 5 כרטיסים בשורה** (6 ⇒ מוותרים על החלש, או 3+3) · **התקופה כתובה פעם אחת** (שורת-המסננים), לא בכרטיס · **באנרי-הסבר ⇒ שכבת-ההטמעה בלבד.**

**איך — ולמה זה זול יותר ממה שנשמע:** הכרטיס מרונדר ברכיב אחד (`src/modules/11_reports/components/KpiTile.jsx` · `StatTile` — **לאמת בשלב 0 מי מהם**). ⇒ **שינוי-מבנה ברכיב = 12 דוחות בבת אחת**, בלי מיגרציה: שורות-המשנה מהשרת עוברות ל-ⓘ. ⇒ **מיגרציית-טקסט רק לכותרות-הטענה** (מ19 בעיקר — ש2 ב-§4ג נבלע כאן) ולערכי-גיבור שאינם מספר — **על `bodies.mjs` `postL4`**, md5 אחרי.
**ודגם קודם, כמו מ02 בפזה ב׳:** מ19 (הגרוע ביותר) לפני/אחרי, צילום לישי ⇒ **"מאשר"** ⇒ שאר ה-11. ה1–ה3 נבנים **על התקן הזה**, ולכן הוא לפניהם.
**מבחן-הקבלה המספרי:** בכל 12 הדוחות, בכל כרטיס — ≤ 1 שורה גלויה מתחת לערך · אפס תאריכים בכרטיס · גבהים שווים בשורה (הפרש 0px, `getBoundingClientRect`) · צילום 1024/1536 **במצב 0** (לוודא שהמשתמש המצולם באמת במצב 0 — `notification_preferences.onboarding_mode`).

## 4ה · 📏 חוקי מצב 0 ומצב 2 — **מספרים שנמדדים, לא עקרונות** *(נוסף 23/09 לילה)*

> **ישי:** *"מה הכללים שצריכים להיות כדי שמצב 0 יראה כמו מוצר תוכנה הייטק אמיתי ומצב 2 כמו מצב הטמעה אמיתי מובן וידידותי למשתמש?"* · *"נראלי הכללים שם לא מספיקים"*.
> **האבחנה של הסגן, והיא הסיבה לסעיף:** הכללים הקיימים (הסקיל `ui-copy-and-onboarding-levels`, `m11-copy-rules.md`, `onboarding-layer-contract.md`) מגדירים **מה** כל מצב מכיל — אבל **אין בהם אף מספר שנמדד על המסך.** לכן כרטיס בן חמש שורות עבר את כולם: אף כלל לא נפל. 📊 **והשער היחיד שכן קיים (רמז ≤ 220 תווים) — הוא בדיוק החלק שעבד.** ⇒ **כל כלל כאן = מספר + הבדיקה שמודדת אותו.** *(המספרים — הצעת-הסגן; ישי מזיז כל אחד במילה.)*

### מצב 0 — "מוצר שמישהו מקצועי בנה"

| # | הרכיב | הכלל | המדידה |
|:-:|---|---|---|
| 0.1 | **כרטיס — שם** | שם-מדד, שם-עצם, **≤ 4 מילים**. לא טענה, לא שאלה | ספירת-מילים |
| 0.2 | **כרטיס — ערך** | **מספר + יחידה**, ≤ 12 תווים. לא שם, לא "—" לבד, לא 0 בלי "מתוך" | regex מספרי |
| 0.3 | **כרטיס — מתחת לערך** | ✏️ **כויל מול ישי 23/09** (כרטיס "יתרת-חוב פתוחה": *"אולי אשתקד זה כן נחמד"*): **השוואה (אשתקד/לפני חודש/יעד) + שורת-הקשר אחת קצרה ≤ 30 תווים**. בלי תאריך · בלי "נכון להיום" · בלי סוגריים. אריח בלי ערך — הסיבה גלויה | תווים גלויים בכרטיס (בלי ⓘ) |
| 0.4 | **שורת-כרטיסים** | גובה שווה (הפרש 0px) · ≤ 6 בשורה · ≤ 2 שורות | `getBoundingClientRect` |
| 0.5 | **משפט "אז מה"** | **משפט אחד, ≤ 120 תווים**, מתחיל בפעולה (*"להתקשר…"*, *"לפתוח…"*) | תווים + מילה ראשונה מרשימת-פעלים |
| 0.6 | **כותרת-גרף** | ≤ 6 מילים · לכל היותר שורת-משנה אחת ≤ 60 תווים | ספירה |
| 0.7 | **כותרת-עמודה בטבלה** | ≤ 3 מילים | ספירה |
| 0.8 | **התקופה** | נכתבת **פעם אחת** — בשורת-המסננים. לא בכרטיס, לא בכותרת-גרף | regex תאריך/"חודשים" מחוץ לשורת-המסננים = 0 |
| 0.9 | **סך תווי-הסבר גלויים לדוח** | **≤ 150** (בלי ערכים, טבלאות וצירים) — היעד שתוכנית-הטיפוגרפיה קבעה ולא הושג ב-7 מ-12 | כלי-המדידה של פזה ב׳ (`phase-b-migration/tools/`) |
| 0.10 | **באנר / פסקה / "שימי לב"** | **אפס** במצב 0. חריג אחד: אזהרה לפני פעולה בלתי-הפיכה או כלפי-חוץ | ספירת אלמנטי-באנר |
| 0.11 | **מצב ריק** | משפט אחד + הפעולה הבאה. לא "אין נתונים" לבד | בדיקה לכל דוח |
| 0.12 | **מספרים** | אותו מדד = אותו עיגול ואותה יחידה בכל מקום · מספרים בעברית מבודדים (`<bdi>` / LRI-PDI) | קיים (`formatByType`) |

### מצב 1 — **ריק, בהכרעת-ישי** (`onboarding-layer-contract.md:43`: *"רמה 1 היא לא החור אני בחרתי…"*)
אין כאן כללים למצב 1 כי הוא לא קיים ב-REG-IN. 🚫 **לא ממלאים אותו "כי הסקיל מגדיר שלוש רמות"** — הסקיל עצמו אומר שזו הכרעת-הבעלים.
🔑 **וה-ⓘ שבכרטיס אינו מצב 1:** הוא חלק מהבסיס, קיים בכל מצב, ומחזיק **נתונים** (חלון · פירוק · מכנה) — לא הסברים. הסבר (*למה · מה לעשות*) — רק במצב 2.

### מצב 2 — "מישהו לוקח אותי ביד, ולא מרצה לי"

| # | הכלל | המדידה |
|:-:|---|---|
| 2.1 | **רמז אחד לכל רכיב, צמוד אליו** (מתחת לכרטיס / לגרף / לטבלה שהוא מסביר) — לא ריכוז בראש הדף | בדיקת-מיקום (`onboarding-layer-contract.md §4ב`) |
| 2.2 | **≤ 220 תווים לרמז** | ✅ קיים — `src/lib/onboardingCopy.budget.test.js` |
| 2.3 | **≤ 4 רמזים גלויים בגובה-מסך אחד** — יותר מזה, הדף נהיה מדריך במקום מוצר | ספירה ב-1536×900 |
| 2.4 | **הרמז לא חוזר על מה שעל המסך** — אף מספר ואף צירוף שכבר גלוי | חפיפת-מילים = 0 (הכלי של שלב 11) |
| 2.5 | **מתחיל ב"למה" או "מה לעשות"** — לא בהגדרה ("המדד הזה הוא…") | מילה ראשונה |
| 2.6 | **נראה שונה מהבסיס** — סמל + רקע רך, כך שברור שזו שכבה ולא חלק מהמוצר | בדיקה חזותית |
| 2.7 | **מבחן-המחיקה** — מצב 0 שלם בלי אף רמז: כל משימה עוברת, כל אזהרה חיונית נשארת | הרצת המשימה במצב 0 |
| 2.8 | **לשון נקבה, פנייה ישירה, חמה** (`design-contract.md §3.6`) | רשימת-מילים |
| 2.9 | **המתג מתחלף במקום**, בלי ריענון | קיים |
| 2.10 | 🔴 **מבחן "לא נתקעת"** — ישי 23/09: *"מצב 2 ממש לתחילת השימוש שלא יהיו שאלות למשתמשים תוך כדי עבודה שלא יתקעו"*. לכל דוח: רשימת 1–3 השאלות שמנהלת חדשה תשאל בשבוע הראשון (*"מה זה 'מתרחק'?"* · *"למה הלקוח הזה ברשימה?"* · *"מה אני עושה עם זה?"*). **כל שאלה נענית ברמז צמוד למקום שבו היא נתקעת.** שאלה שרמז לא יכול לענות עליה ⇒ **המסך צריך תיקון, לא עוד מלל** | טבלת שאלה ⇒ רמז, לכל דוח |

📚 **הכללים הוכללו לסקיל** — `~/.claude/skills/ui-copy-and-onboarding-levels/references/data-surfaces.md` (23/09, בהוראת-ישי *"תחשוב איך מכלילים את כל זה ותעדכן את הסקיל"*). **הסשן קורא אותו, וגם את הטבלאות כאן — המספרים כאן הם הכיול של REG-IN.**

### 🔁 איך זה נופל לבד — ולא נשען על זיכרון
**סקריפט "בדיקת-צפיפות"** (על בסיס `phase-b-migration/tools/shoot.mjs` הקיים): רץ על **כל 16 הדוחות** במצב 0 ובמצב 2, כמנכ"ל, ב-1536, ומודד כל שורה בטבלאות למעלה. **כל חריגה = כישלון עם שם הדוח והכרטיס.** נכנס ל-`e2e/` כבדיקה שרצה עם `npx playwright test e2e/reports.spec.js` (לא בשער — השער לא מריץ שרת). ⇒ **ה1–ה3 חייבים לעבור אותו לפני שהם "גמורים", וכך גם 13 האחרים אחרי מיגרציית-הטקסט של §4ד.**
⚠️ **ולוודא שהמשתמש המצולם באמת במצב 0** (`notification_preferences.onboarding_mode`) — אחרת מודדים את מצב 2 וקוראים לו 0.

## 5 · האדוות שעדיין נדרשות *(גם בשדרוג-במקום)*

| מה | איפה |
|---|---|
| 🔴 **דלתות שהמשמעות שלהן משתנה** — אותו slug, תוכן אחר. במ2: אריחים עם `target` ל-מ3/מ4 (`j3…sql:1556,1567`) + הקישור הקבוע "כל השנים בדוח «מגמות רב-שנתיות» →" (`ExecOverviewSurface.jsx:21`) — **"כל השנים" כבר לא יהיה שם.** במ14: "אירועים-עם-חוסר" ⇒ מ6 (`:3105`) — כנראה עדיין הגיוני לה3, **לאמת**. ⇒ **מיגרציה של מ2 (ואולי מ14) נכנסת לשלב 2** | `j3…sql` · `ExecOverviewSurface.jsx` |
| שמות-תצוגה — **מילה-במילה מ-`processes-approved.md` §🏷️** (הקטלוג מחייב, `reportsCatalog.js:8`) | `processes-approved.md` · `reportsCatalog.js` · `e2e/smoke-anchors.json:37` |
| ספירות שהשער בודק (`check-declared-counts.mjs:40-110`) | `screens-approved.md` (שורות ~98/99/101) · `spec.md` §1.4 (~108, תוויות-אריחים) |
| מפתחות-הטמעה של שלושתם (+ "חמש-עשרה" המוצהר) | `src/lib/onboardingCopy.m11.exec.js:23,57,75,89` |
| קוד-הלקוח שלהם | `tabs/ExecutiveTab.jsx:17-28` · `TrendsSurface` · `DiscountsSurface` · `StaffingSurface` · `src/lib/reportsExecutive.js` · `chartShape.js:29` (רק מ4 — `knip` ייפול אם יתייתם) · `ExecOverviewSurface.jsx:21` |
| בדיקות | `ExecutiveTab.test.jsx:140,155,641` · `HostessesTab.test.jsx:174,669` · `ChartCard.test.jsx:163` · `ReportTable.test.jsx:16` · `e2e/reports.spec.js:388-394` (**בלוק-הקידוח: "רק מ3 ו-מ9"** — לשמור קידוח ב-ה1 או לעדכן) |
| ייצוא | `exportFetch.js`: `CAPPED_LABELS` ("50 ההנחות הגבוהות" · "50 הפערים הגדולים") **חייבים להתאים לשורות החדשות** — או להסיר את מ04/מ06 מ-`PAGED_RPCS` **ולהוריד את `p_page`/`p_page_size` מהחתימה** (אחרת הקריאה נופלת, `exportFetch.js:38-44`) · `ExportBar.test.jsx` |

## 6 · סדר הביצוע

| שלב | מה | 🛑 |
|:-:|---|---|
| **0 · מדידה** | קריאה-בלבד על **המסד**: דחיות לפי סיבה ורבעון · שיעור-סגירה לפי סוג/חדש-חוזר/גודל/זמן · רצועות-הנחה · יחס אורחים-לדיילת · היעדרויות · ראש-משמרת · לקוחות חוזרים (**וחלון-צנזורה**) · **האם ה-snapshot קיים ועדכני** · **אילו מספרים ישתנו בזריעה** · **למי יש `כספים`** (ה3 רלוונטי למנהלת-פרויקטים — `processes-approved.md:145` R23) | — |
| **1 · הכרעות** | סבב אחד: ① שמות-התצוגה ② כללי-הזריעה ③ סף-n ④ קהל ה3 (אם אין לה `כספים`) | **ישי** |
| **1.5 · תקן-הכרטיס** | §4ד. ✅ **הרכיב בוצע ע"י הסגן, `baf43c0e` + כיול `ea38925f`** (23/09 לילה, בהוראת-ישי *"אתה יכול לסדר את הכל? אם כן תקן"*): שורה גלויה אחת · ⓘ · 6 בשורה · נבדק חי מקומית (מ17 — 6 כרטיסים בשורה אחת, שורה אחת בכל אחד). ⏳ **נותר: טקסט-השרת** — כותרות-טענה · ערכי-גיבור שאינם מספר · שורות-פירוק · באנר מ21 · + ש1 (מ21) · ש2 נבלע כאן | **ישי** — "מאשר" על מיגרציית-הטקסט |
| **2 · ה1** | **לא תלוי בזריעה.** שדרוג `report_m03_trends` + משטח + אדוות §5 שלו | **ישי** — מיגרציה |
| **3 · snapshot + זריעה ממוקדת** | capture ⇒ סקריפט ⇒ `seed-assert.mjs` ⇒ מדידה חוזרת | **ישי** — כתיבה למסד |
| **4 · ה2 + בדיקת-סיפור** | שדרוג מ4. **האם שורת-ההחלטה אומרת משהו?** לא ⇒ נקודת-ויתור | **ישי** — מיגרציה + צילום |
| **5 · ה3** | שדרוג מ6 | **ישי** — מיגרציה |
| **6 · אדוות** | §5 כולו · שער · e2e | — |
| **7 · אימות חי** | כמנכ"ל + `view` על `כספים` + תפקיד בלי `כספים` · 1024/1536 · ייצוא: שורות = שורת-הכמות, גיליון-סיכום קיים | — |
| **8 · רענון** | סיפורי-הכנס (`~/.claude/plans/conference-qa-2026-10-15.md`) · `module-11.md` §9 · STATUS · LOG · §10 כאן | — |

## 7 · מתי ואיך

- **אחרי שפזה ב׳ של הטיפוגרפיה מוזגה ל-`dev`** — אותם קבצים. ובעדיפות **גם אחרי העלאה ל-`main`** — נקודת-חזרה יציבה לפני שינוי נתונים.
- **סשן אחד · ענף אחד** `ishay/m11-decision-reports` · תיקייה `C:\Users\ishay\Reg-In-dash-wt` · **מיזוג אחד בסוף**.
- **4 לשוניות נשארות** — לשונית = קהל-הרשאה (`reportsCatalog.js:22`); הנהלה וכספים שתיהן `כספים`, ומיזוגן היה יוצר לשונית של 8.
- **עד 08/10** (הקפאה) · 14/10 `refresh-before-conference.sql` · 15/10 הכנס.
- 🛑 **נקודות-ויתור:** ה1 נכנס בכל מקרה. אם עד ~02/10 ה2 לא עבר בדיקת-סיפור — ה2/ה3 נדחים, מ4/מ6 נשארים כמו שהם.

## 8 · סוכנים

- **הבנייה — הסשן עצמו.** השלבים חולקים קבצים ⇒ לא במקביל.
- **① Sonnet, קריאה-בלבד, אחרי שלב 1:** מאמת את טבלת §5 מול הדיסק ומשלים מה שחסר — קובץ:שורה.
- **② Opus יריב, קריאה-בלבד, אחרי כל דוח (עד 3):** האוכלוסייה ב-SQL מול ההגדרה בקוד · שער-ההרשאה · ACL · C8 · עמודות רגישות · RTL · **"המספר בשורת-ההחלטה — נכון? הוכח מהקוד."**
- **③ Sonnet, שלב 7:** ריצת-צילומים לתיקייה זמנית. 🚫 לא מסד, לא קוד.
- שורה אחת לישי לפני כל שיגור · פסק אחרי (שומש / תוקן / נזרק).

## 9 · 🪤 מלכודות

- **מסד אחד לפיתוח ולייצור** — כל כתיבה היא לאתר החי.
- **פונקציה חדשה או `drop`+`create`** ⇒ `anon=EXECUTE` כברירת-מחדל · `create or replace` עם חתימה אחרת ⇒ עומס-יתר (42725). **שדרוג-במקום עם אותה חתימה עוקף את שניהם — ובכל זאת לאמת `proacl` אחרי.**
- **`seed_reset` · מפתח-המנכ"לית · טריגרים על `quotes`/`assignments`** — §4.
- **`is_shift_lead`** — קיים ברוב האירועים (`plan.mjs:1151`); היעדרו הוא אות אמיתי.
- **"פגות בקרוב" כבר קיים פעמיים** (`dashboard.js:312-323` · `QuotesPage.jsx:324`, דרך `deriveQuoteExpiry` — `quotes.js:632`) ⇒ **קישור, לא העתק** (כלל-ברזל 14).
- **צנזורה מימין** — לקוח שהאירוע הראשון שלו קרוב מדי עוד לא יכול היה לחזור.
- **פורט:** `npm run dev -- --port 5199 --strictPort`, ולאמת שהכותרת REG-IN לפני מדידה חיה.
- **`RepositoryTab` מוקפא · `hourly_rate` חוב פתוח עד ~07/10** — ה3 לא נוגע.

## 9ב · שלב 0 + שלב 1 — נמדד 23/09 לילה (MCP, קריאה-בלבד) והוכרע בהאצלת-ישי

> 🔑 **הרשאת-ישי לסשן הזה, בצ׳אט, 23/09 לילה:** *"לא לבקש אישור להכיל מיגרציות בסשן הזה. תכיל בעצמך ותאמת לבד… משימתך לסיים הכל בתקווה שלא תעצור אלי עם שאלות"*. ⇒ מיגרציות וזריעה-ממוקדת מוחלות ע״י הסשן, עם שורת-הסבר בצ׳אט לפני, ואימות md5 + proacl אחרי. **ההרשאה לסשן הזה בלבד** — אינה כלל קבוע.

**נמדד (אוכלוסיות לפי ההגדרות בקוד — §7.82 · `feedback_status=completed` · `finance_project_money`):**
| מה | n | מה זה אומר |
|---|---|---|
| הצעות שהוכרעו (אושרו + נדחו בלי «נפתחה בטעות») | 1,174 | ה1: שיעור-סגירה כללי ~71% |
| חדש מול חוזר | 93 / 1,081 | ה1: חדש 61.3% מול חוזר 72.2% — הפער הגדול ביותר |
| דחיות לפי סיבה (₪ אחרי הנחה, לפני מע״מ) | מחיר 117 (~924K) · תקציב 56 · מתחרה 54 · פג תוקף 33 (~216K) | ה1: "פג תוקף" = כסף שאבד בלי מענה — הפעולה: מעקב |
| הנחה × סגירה, חברה פרטית | 0%: 386 (74.4%) · 10%+: 30 (56.7%) | ה2: הנחה עמוקה לא סוגרת יותר. חברות-הפקה מקבלות הנחה לפי סוג ⇒ פיצול לפי סוג-לקוח |
| הנחה × ציון | 224 / 201 / 84 / 43 · 4.33 / 4.24 / 4.51 / 4.49 | ה2: אין קשר ברור — נאמר ביושר |
| יחס אורחים-לדיילת נוכחת | 552, חציון 42.75, p90 54 | ה3: היום אין קשר לציון (4.34 כמעט בכל רצועה) |
| בלי ראש-משמרת נוכחת | 27 (4.00 מול 4.35) | ה3: האות היחיד הטבעי |
| היעדרות ≥1 / איחור בינוני-כבד | 145 / 103 — בלי הבדל | ה3: אין אות טבעי |
| חזרה אחרי אירוע ראשון (חלון-צנזורה 180 יום) | 40/40 חזרו | **אין שונות — שאלת-החזרה לא נענית בדאטה; ה3 מקשר למ19 בלבד** |
| `assignments.recommended_rank` מלא | 0 | ה4: שורת-האימוץ לא נכנסת |
| `seed_snapshot` | קיים | תנאי-הזריעה מתקיים |
| `כספים` | מנכ״ל (edit, 3 פעילים) · כספים-ולקוחות (edit) · השאר blocked | ה3 רלוונטי למנהלת-פרויקטים — ר׳ ה4 בהכרעות |
| `onboarding_mode` | שורה אחת: ishay1997=0 | בדיקת-הצפיפות קובעת את המצב במפורש לפני מדידה |

**הכרעות שלב 1 — הכרעתי, הפיך (ישי האציל):**
1. **שמות-תצוגה** (שם-עצם בניווט + שאלה ככותרת-משנה, כמו §🏷️): מ3 ⇐ **"סגירת הצעות"** · *"מה סוגר הצעה, ואיפה הכסף הולך לאיבוד?"* · מ4 ⇐ **"הנחה מול סגירה"** · *"מתי הנחה שווה את זה?"* · מ6 ⇐ **"איכות אירועים"** · *"מה מוריד את ציון האירוע?"*.
2. **כללי-הזריעה** — `feedback_score` בלבד, על משובים שהושלמו ולא בוטלו: ‏−1 לכל כלל שמתקיים — אין ראש-משמרת נוכחת · ≥2 היעדרויות · ≥50 אורחים לדיילת נוכחת — רצפה 1. **הנחה לא נזרעת** (ה2 מציג את הנתון הטבעי). ~106 שורות. כרטיס מ19 *"שביעות-רצון מנבאת חזרה"* נמדד לפני/אחרי — היפוך ⇒ שחזור.
3. **סף-n = 20 לקבוצה.** מתחת ⇒ "אין מספיק נתונים" במקום שורת-החלטה. ושורה קבועה בשבב-ההיקף: "קשר, לא סיבה".
4. **קהל ה3** — נשאר בלשונית הנהלה (`כספים`). העברה ללשונית אחרת משנה ספירות-קטלוג ומוקפאת לפני הכנס; נרשם כרעיון לאחרי הכנס.

## 10 · 📍 איפה אנחנו *(מתעדכן במקום — זו המסירה)*

| תאריך | מצב | SHA | הבא |
|---|---|---|---|
| 23/09/2026 | תוכנית נכתבה · ביקורת חיצונית · תוקנה | — | שלב 0, אחרי מיזוג פזה ב׳ |
| 24/09/2026 לילה | **הבנייה הושלמה.** תקן-הכרטיס (L5 · L6 · L7 · L7b) · ה1/ה2/ה3 חיים (שדרוג-במקום) · זריעה ממוקדת (106 ציונים, גיבוי `seed_snapshot.projects_feedback_20260924`) · רשת-כרטיסים ברוחב אחיד (240) · בדיקת-הצפיפות 16×2 = 0 חריגות · reports.spec 19/19 · סוקר-יריב: כל המספרים תאמו. ה4 — **נדחה לאחרי הכנס** (`recommended_rank` ריק; שורת שער-המרחק = סימולציה-לאחור שנאסרה). | `d5ca2adb` | ① **הסגן:** אימות חי + צילומים לישי על `d5ca2adb` (3 תפקידים · 1024/1536 · ייצוא · לפני/אחרי מ19/מ21/מ17 · מסלול-הכנס ש3 ⇒ conference-qa). ② **ישי:** מיזוג ל-dev ואז ל-main. ③ שאלת-סתירה לישי: בה1 "פגו בלי מענה" (53K ₪) גובר בשורת-ההחלטה על "מחיר" (288K ₪) — לפי §3 "מתעדפת מעקב"; להשאיר? ④ מלכודת לפרומפט הבא: ב-`Reg-In/.env.local` אין E2E_* ⇒ reports.spec מדלג בשקט; להעתיק מ-export-wt. |

</div>
