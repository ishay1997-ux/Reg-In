<div dir="rtl">

# תוכנית שבועיים — 06/08/2026 → 20/08/2026

> **מי קורא את זה:** סשן קלוד חדש, חסר-זיכרון. **זה קובץ-הביצוע שלך.**
> **המשימה המרכזית:** מודול 4 (דיילות + Smart Match) **נבנה ומוזג**. מפת-הדרכים נותנת לו 15 יום ודדליין 21/08.
> **מקור:** נגזר מניתוח 06/08/2026 — 29 סוכנים, שלושה זרמים. הראיות בקומיטים של אותו יום וב-`docs/CLAUDE_CODE_LOG.md`.
>
> 🔴 **סשן מבצע **בלוק אחד**, לא את כל התוכנית.** ‏קרא את הבלוק שלך, בצע, עדכן את שורת-הסטטוס שלו, עצור.
> 🚫 **ואל תשכתב את התוכנית הזאת. ממצא ⇒ שורה ב-"יומן סטיות" בתחתית.**

---

## 🧪 מבחן-קבלה — ענה בהודעה הראשונה, לפני כל פעולה

**ארבע שאלות. התשובות על הדיסק. אם אינך יכול לענות — עצור ואמור זאת לישי.**

1. איזה בלוק פעיל עכשיו, ומה תנאי-הסיום שלו? *(טבלת-המצב למטה)*
2. מהם **ארבעת פריטי-החוזה** שהאפיון חייב לייצר, ולמה דווקא הם? *(בלוק ③)*
3. באיזה ענף אתה, ומה **אסור** לך לעשות איתו? *(כללי-הברזל — `CLAUDE.md` כלל 10)*
4. איפה יושב האפיון המאושר של מודול 4, ואיזה קובץ בו **עדיין לא קיים**? *(בלוק ③)*

---

## 📊 טבלת-המצב — **עדכן אותה בסוף כל בלוק**

| בלוק | מה | מצב | תנאי-סיום |
|:-:|---|:-:|---|
| **①א** | תיקוני-מסמכים + סקריפט הספירה *(‏1.1–1.3 · 1.6)* — **קלוד לבד** | ⬜ | ארבע פקודות-האימות מחזירות את הפלט הצפוי |
| **①ב** | ‏🔴 עיגול ה-PDF + מיגרציית ה-RLS *(‏1.4 · 1.5)* — **דורש את ישי** | ⬜ | בדיקה נופלת⇐עוברת · והמיגרציה **הוצגה** *(ההחלה שלו)* |
| ② | M2 — מיזוג `dev`→`main` | ⬜ | 👤 **ישי בלבד.** ‏**אחרי ①ב** |
| ③ | סיום Discovery — מעבר על 4 המאושרים, ואז 4 הנותרים | ⬜ | ‏8/8 משטחים · ארבעת פריטי-החוזה כתובים |
| ④ | התאמת `module-blueprint` (שלב 4 בתוכנית-העל) | ⬜ | הסקיל מפנה ל-`docs/specs/` · ראיון-ה-PM הוסר |
| ⑤ | בלופרינט מודול 4 | ⬜ | `docs/micro_guides/module-4.md` קיים ומאושר |
| ⑥ | בניית מודול 4 | ⬜ | DoD חתום |
| ⑦ | תחזוקה — ‏6 פריטים **בלי תלות בשאר** | ⬜ | כל פריט ✅ או הועבר ל-`§6` עם נימוק |

**סטטוסים:** ⬜ לא התחיל · 🔨 בעבודה · ✅ הושלם · ❌ חסום (+סיבה)
🔴 **ובלוק ① פוצל ב-06/08 אחרי מעבר-קוהרנטיות: ‏1.5 היא מיגרציה שדורשת typed-echo של ישי** ⇒ סשן שהיה מתחיל את הבלוק כמקשה אחת **נתקע באמצע וממתין לו.** ‏**①א רץ עד הסוף בלי אף עצירה.**

---

## ① ניקוי קרקע — חצי סשן

> **למה ראשון:** שלוש מהן נכנסות לבלופרינט אם לא יתוקנו עכשיו, ואחת מהן על המסך בהגשה.

**1.1 — שם-עמודה חוצה-מודולים שגוי**
`docs/specs/module_04_hostesses/screens-approved.md:344` כותב `projects.hostess_count`.
**אין עמודה כזו.** ‏`docs/schema.sql:126` = `required_hostess_count` (על `projects`) · `:93` = `recommended_hostess_count` (על `quotes`).
**זו העמודה שה-RPC של מודול 3 כותב.** תקן ל-`required_hostess_count`.
**אימות:** `grep -n "hostess_count" docs/specs/module_04_hostesses/screens-approved.md` ⇒ אין `projects.hostess_count`.

**1.2 — מחרוזת שנפסלה ועדיין חיה במוקאפ מאושר**
`docs/mockups/hostesses-screen/approved/04_rowmenu_approved.html:152` מכיל `סמן: אושרה סופית`.
`screens-approved.md:175` קובע שהשם הנכון הוא `אושרה סופית — סוכם בטלפון`. **הכרטיס תוקן, המוקאפ לא.**
**אימות:** `grep -rn "סמן: אושרה סופית" docs/` ⇒ 0 תוצאות.

**1.3 — שתי עמודות שהאפיון טוען שנרשמו ולא נרשמו**
`processes-approved.md:303` מצהיר *"מה שנוסף היום נרשם ב-`db_roadmap`"*, ומונה `has_car` ו-`travel_amount`.
נמדד: **0 ב-`db_roadmap.md`, 0 ב-`schema.sql`.** הוסף שורות ל-`docs/db_roadmap.md` (זהו ה-SSOT לשינויי-DB בסשן שאינו כותב מיגרציות — `docs/CLAUDE.md` כלל 13ב).
**אימות:** `grep -c "has_car\|travel_amount" docs/db_roadmap.md` ⇒ ≥2.

**1.4 — 🔴 חשבון ה-PDF לא מסתדר** *(הפגם החמור ביותר — הוא על התוצר שהלקוח מקבל)*
`src/lib/pricing.js:145-146` מצהיר *"סכום השורות המוצגות תמיד מתחבר לסה״כ המוצג"* — **וזה שקר**, כי כל שורה מעוגלת בנפרד.
דוגמה מהמנוע האמיתי: `1×06ST`(800) + `30×REG-TAG`(3), הנחה 5%, מע"מ 18% ⇒ ה-PDF מדפיס `890 ₪` · `-45 ₪` · `846 ₪`. **890−45=845.**
ובנוסף: המסך מדפיס `-44 ₪` לאותה שורה (`Math.round(-44.5)=-44`) וה-PDF `-45 ₪` (`Math.round(44.5)=45`).
**נמדד על 4,480 הצעות מהקטלוג: 38.5% מה-PDF-ים לא מסתדרים · 28.6% מסך≠PDF.**
**המוקדים:** `src/modules/03_quotes/QuoteSummaryPanel.jsx:44-45,54,59` · `src/modules/03_quotes/quotePdf.jsx:286,389-410,450-452`.
**התיקון העקרוני:** לעגל **פעם אחת**, ולגזור את שורות-התצוגה מהערכים המעוגלים — לא לחשב מחדש בכל מקום. **וההנחה החישובית עוברת ל-`pricing.js` בלבד** (כלל ברזל 14 — ה-UI לא מחשב).
🔴 **ולפני התיקון: כתוב בדיקה שנופלת** על מקרה שהטור לא מסתכם בו, ורק אז תקן.
**אימות:** `npm run test:run` ⇒ הבדיקה החדשה עוברת · והרץ מדגם שמאשר שהטור מסתכם.

**1.5 — 5 טבלאות עם RLS דלוק ואפס מדיניות**
`supabase/migrations/20260710160735_module2…sql:35-39` מדליק RLS על `projects · hostesses · salary_reports · assignments · logistics`. **אין להן אף `create policy` בכל המיגרציות.**
⇒ **המסך הראשון של מודול 4 יחזור ריק, עם `error: null`** — כישלון שנראה כמו הצלחה.
🔴 **זה לא תיקון-דרך-אגב — זו מיגרציה, ולכן שער `👤 typed-echo`** (`supabase/migrations/CLAUDE.md`).
**מה כן לעשות בבלוק הזה:** לכתוב את המיגרציה ולהציג אותה. **ההחלה היא של ישי.**
**וגם:** לרשום שורת `🚧 מ4` ב-`PROJECT_MASTER §6` עם חמשת שמות-הטבלאות — כרגע `projects` רשומה כעבודה של מודול 6 בלבד.

**1.6 — כותב אחד לספירת-המשטחים** *(שתל G3)*
ספירת `N מתוך M` חיה היום בשלושה מקומות ו**כבר סותרת את עצמה**: `discovery_lessons.md:704` אומר *"3 סגורים"*, `screens-approved.md:93` אומר *"4 מתוך 8"*, ו-`next-session-prompt.md:84` נושא ספירה משלו.
**התיקון:** הספירה חיה **בטבלה אחת** (`screens-approved.md`), וכל השאר מקשר.
**האכיפה:** ~10 שורות ל-`scripts/check-docs-structure.mjs` (79 שורות, כבר סורק `docs/specs`, כבר מחווט ל-`npm run gate`) — כל `\d+ מתוך 8` או `\d/8` מחוץ לטבלה = ממצא.
🔑 **הרץ אותו לפני התיקון והראה לישי את הפלט האדום האמיתי — זו ההוכחה שהבדיקה עובדת.** בדיקה שרק עוברת היא ביטחון-שווא.

**1.7 — 🔴 בדיקת RLS קבועה בשער האוטומטי** ✅ *(אושר ע"י ישי 06/08 — היה `8.3`)*
**היום `npm run gate` מריץ `lint · format · tests · build · כפילויות · קוד-מת · תלויות · הקשר · מבנה-מסמכים` — ואין בו שום בדיקת-מסד. אף אחת.**
בדיקת יועצי-Supabase כן קיימת, **אבל היא רצה רק בסגירת מודול ורק על *"מה שהמודול הזה הכניס"*** (`module-close/template.md:58`) ⇒ **חמש הטבלאות של 1.5 שרדו כי הן נוצרו במיגרציה של מודול 2 ושייכות למודולים 4·5·6·8 — הן לא היו "של" אף מודול שנסגר.**
⇒ **לא יכולת חדשה — הורדת הסינון.** בדיקה על **כל המסד**, בכל `gate`:
```sql
select c.relname from pg_class c
where c.relrowsecurity
  and not exists (select 1 from pg_policies p where p.tablename = c.relname);
```
**⚠️ והבדיקה השנייה מאותה הכרעה — "הטור המודפס מסתכם לסה"כ המודפס" — כלולה כבר ב-1.4.**

---

## ② M2 — מיזוג `dev` → `main` · 👤 **ישי בלבד**

**המצב, מדוד 06/08:** `main` — קומיט אחרון **12/07/2026** · `main..dev` = **360 קומיטים**.
⇒ **מה שעלה לוורסל הוא מודולים 0·1·2 בלבד. מודול 3 — מנוע-הכסף, ההצעות, ה-PDF — אינו שם.**
`docs/guides/00_roadmap.md §4` קובע אבן-דרך **`M2 — מנוע כסף · 07/08/2026`**, המוגדרת כ*"נקודה שבה ממזגים ל-`main`"*.

**שתי אפשרויות, וההכרעה של ישי:**
- **(א)** M2 בתוקף ⇒ למזג `dev`→`main`. **מומלץ — ואז הדמו בוורסל כולל את מנוע-הכסף.**
- **(ב)** M2 מת ⇒ למחוק את התאריך מ-`00_roadmap §4`, כדי שלא ירדוף.

🚫 **קלוד לא ממזג ולא פותח PR בשום תרחיש** (כלל ברזל 10).
⚠️ **ולפני מיזוג — בלוק ① חייב להיות סגור**, אחרת באג ה-PDF עולה לוורסל.

---

## ③ סיום ה-Discovery — 4 משטחים · `spec.md` · ארבעת פריטי-החוזה

**המצב:** 4 מתוך 8 משטחים סגורים (`screens-approved.md:82-93`). נשארו: **3ב · 3ג · 3ד · 5**.

### 🔴 3.0 — כלל-הסשן, ולפניו החשבון שממנו הוא נגזר

> **הבעיה בסיבוב הקודם לא הייתה אורך הפרומפט — הייתה כמות העבודה בסשן אחד.**

| | טוקנים |
|---|---|
| הפרומפט כולו (~650 שורות) | **~20K — 2% מהחלון** |
| **משטח אחד**, מדוד | **~85K** |
| 8 משטחים | **~680K** — מעל קו-הסיבוב (600K) |
| **מה שנשאר: 4 משטחים** | **~340K ⇒ נכנס בסשן אחד** |

⇒ **פיצול הפרומפט חוסך 2% ולא פותר כלום. מה שפותר הוא איפה הסשן נשבר.**

**ארבעה כללים, וכולם כבר הוכרעו — הם פשוט לא רצו:**
1. **2–3 משטחים לסשן.** לא שמונה.
2. **עוצרים בגבול-כרטיס נקי — לעולם לא במעבר בין שלבים.** *(נמדד: הסשן השני היה פי-2.4 צפוף-מוצר יותר, והוא היה **המשך לולאה**. הקומפקט נחת בדיוק במעבר.)*
3. **קימוט אחרי כל כרטיס מאושר** ⇒ קטיעה עולה חצי תור, לא סשן.
4. 🚫 **בלי קומפקט.** סשן טרי + מבחן-הקבלה שבראש הקובץ הזה. *(הקומפקט אינו מסנן שגיאות — הוא משכפל אותן בנאמנות מלאה. נמדד: תקציר נשא "סטייה מ-C5" שגויה, ומה שתיקן היה קריאת המקור.)*

### 🔍 3.1 — **קודם: מעבר על ארבעת המסכים שכבר אושרו** *(הכרעת-ישי 06/08 — לפני שממשיכים לנותרים)*

> **הנימוק:** ‏**כבר נמצאו שם שלושה פגמים ב-06/08 — ולא חיפשנו.** ‏שם-עמודה שגוי · מחרוזת שנפסלה ונשארה במוקאפ · ושתי עמודות שהאפיון טוען שנרשמו ולא נרשמו. ⇒ **סביר שיש עוד.**
> 🔴 **וכל פגם שם הופך לפגם בקוד:** הבלופרינט יקרא את ארבעת הכרטיסים ויבנה מהם. **שם-עמודה שגוי באפיון הופך לשאילתה שבורה — ואיש לא יידע שהמקור היה האפיון.**

**לכל אחד מארבעת הכרטיסים, ארבע בדיקות בלבד — וזה בדיוק ארבעת פריטי-החוזה, מוחלים אחורה:**
1. **כל שם-עמודה מול `docs/schema.sql`** — קיים? מאוית נכון? על הטבלה הנכונה?
2. **כל תווית מול המוקאפ** — הכרטיס והמוקאפ אומרים **אותה מחרוזת בדיוק**?
3. **כל מספר שמוצג** — יש לו עמודה בשם או חישוב מפורש? *(מספר בלי בית = ממצא חוסם)*
4. **כל פעולה שמופיעה במוקאפ** — מופיעה גם ב-`① מפת-לחיצות` של הכרטיס?

**זמן:** ~חצי סשן. **ופלט:** טבלת ממצאים — **דיווח, ואז תיקון באישור.**

**3.2 — ארבעת המשטחים שנותרו** *(רק אחרי 3.1)*. לפי הפרומפט הקיים: `docs/claude mega plans/regin_workflow_short_updated.md`, שורות 194–843. **הוא טוב — אל תשכתב אותו.**
לכל משטח: **מוקאפ HTML ראשון** (לא כרטיס מילולי — הכרעת-ישי 06/08), ולצידו רק ההכרעות שתמונה אינה יכולה להראות.
🔴 **ופלטה נשאבת מ-`src/`, לא ממוקאפ קודם:**
```
grep -rhoE "(bg|text|border)-(green|red|amber|slate|teal)-[0-9]{2,3}" src/ | sort | uniq -c | sort -rn
```

**3.3 — נקודת-הסיום.** *(מוספר מחדש 06/08 — כאן היה `3.2` **פעמיים**, וזו בדיוק המחלקה ש-`check-docs-structure.mjs` נבנה לתפוס.)* `docs/CLAUDE.md:32` קובע שכל תיקיית-אפיון מחזיקה `spec.md`, והוא לא נוצר — **ולכן לקישור אפיון→בלופרינט אין נקודת-סיום מוגדרת.**

> 🔴 **תיקון שנכנס אחרי ביקורת (06/08) — קרא אותו לפני שאתה פועל:**
> **המצביע בבלוק ④ יפנה ל-`docs/specs/module_04_hostesses/*.md` — לתיקייה, לא ל-`spec.md`.**
> **הנימוק:** חמשת הקבצים הקיימים כבר נושאים ~91KB של הכרעות מאושרות. **להתנות את המצביע בקובץ שאולי לעולם לא ייכתב תחת לחץ-דדליין הוא בדיוק מנגנון-הכישלון שהתוכנית הזאת מנסה למנוע.**

### 🚫 וה-PRD בן 12 הפרקים — **בוטל. הכרעת-ישי 06/08/2026.**

> **הנימוק, והוא מכריע:** ‏**האפיון האקדמי כבר הוגש וקיבל 90** (דו"ח 2). ‏**האפיון שבריפו אינו תוצר —
> הוא כלי-עבודה, ויש לו תפקיד אחד: שקלוד יבנה בלי לנחש.** ‏12 הפרקים היו **ירושה ממסמך אקדמי שכבר
> נסגר**, לא צורך מדוד. ⇒ **אין פרקים · אין APA · אין גוף שלישי · אין Word.**

**מה שנכנס במקומם — וזה כל התוכן של `spec.md`:**
1. **מצביע** ל-`processes-approved.md` · `screens-approved.md` · תיקיית המוקאפים המאושרים. **בלי לשכפל שורה.**
2. **ארבעת פריטי-החוזה** (3.4 למטה).
3. **פרק אחד בלבד מהמבנה הישן: *"מה אסור לבלופרינט לנחש"*** — **וגם הוא כבר קיים** כטיוטה בת 84 שורות ב-`processes-approved.md:384-467`. **מעתיקים, לא כותבים מחדש.**

🔑 **ומדד-האיכות היחיד של הקובץ הזה, והוא בר-הפרכה:**
**אם הבנייה יצאה נכונה — האפיון היה מספיק. אם קלוד ניחש — הוא לא היה.** *(ולכן הקנרית בבלוק ④ שווה יותר מכל בדיקת-שלמות.)*

**3.4 — ארבעת פריטי-החוזה. 🔑 זה הלב.**

> **הנימוק, ואינו סגנוני:** הפרומפט הבא לא צריך **להבין** את האפיון — הוא צריך **לבדוק את עצמו מולו**.
> טקסט אפשר לקרוא בכמה דרכים; ארבעת אלה **או תואמים או לא**, ולכן הם העוגנים היחידים שבקרה יכולה להישען עליהם.
> *(מדוד: קלוד תופס פגמים בארטיפקט שהוא עצמו כתב — 0 מתוך 5 נקודות-מדידה. מה שכן עבד היה תמיד השוואה מול עוגן חיצוני.)*

| # | הפריט | הצורה | למה — כשל מדוד |
|:-:|---|---|---|
| **1** | 🔒 **אוצר-מילים נעול** | טבלה אחת: כל סטטוס · כל ישות · **כל תווית על מסך**, מילה-במילה | שני שמות לאותה פעולה בשני מוקאפים מאושרים (06/08) · ומחרוזת שנפסלה שרדה במוקאפ (1.2) |
| **2** | ➡️ **סדר מוכרע** | מי מתחיל · מה נשמר · מה קורה לפני מה | זה מה שסעיפי-התהליך של C5 נתנו, ומזה נגזר מסך במקום להמציא אותו |
| **3** | 🔢 **לפחות מספר אחד בר-בדיקה** | מקרה מלא עם ערכים אמיתיים | **השרשרת:** `6,319` מופיע ב-C5 **פעם אחת** ⇒ ב-`module-3.md` **33 פעמים** ⇒ ב-`e2e/smoke-anchors.json` **לתמיד**. 🔴 **למודול 4 אין ערך כזה ב-C5 — אם לא ייווצר כאן, השרשרת לא נוצרת** |
| **4** | 🔗 **שורת חוצה-מודולים** | מה המודול קורא/כותב בטבלאות של אחרים, **בשם העמודה המדויק, מאומת מול `schema.sql`** | `projects.hostess_count` (1.1) — האפיון של מ4 נקב בעמודה של מ3 בשם שגוי |

**אימות מכני לשלושה מהארבעה:**
```
grep -c "רשימת אוצר-המילים" docs/specs/module_04_hostesses/spec.md
grep -oE "[a-z_]+\.[a-z_]+" docs/specs/module_04_hostesses/spec.md | sort -u   # ואז מול schema.sql
```

**3.5 — 🔍 בדיקת מפת-הלחיצות, אוטומטית** *(שתל G1 — ~40 שורות, `scripts/check-screens.mjs`)*
**כלל אחד:** כל `<button>` במוקאפ מאושר חייב להופיע **בשמו הגלוי** ב-`① מפת-לחיצות` של הכרטיס שלו. *(regex מספיק לגרסה ראשונה; `jsdom` כבר ב-`package.json:67` אם צריך.)*
🔑 **למה דווקא זה:** זה הכשל היחיד שנמדד ונוסח כעיקרון — `screens-approved.md:296-298`: *"**הרכיב לא הופיע במפת-הלחיצות כלל** … 🔑 **מפה חלקית גרועה ממפה שאינה קיימת: היא נראית כמו בקרה ואינה.**"* ⇒ **זה מעביר את התפיסה מ"ישי" ל"סקריפט".**
**הרץ אותו גם על ארבעת המשטחים שכבר אושרו.** אפס ממצאים הוא תוצאה אמיתית; ממצא הוא המופע השני של אותו כשל.
🚫 **ואל תוסיף בדיקת-פלטה.** נבדקה והופרכה: `src/index.css` מכיל **הצהרת-hex חיה אחת** (`--primary: #0d9488`), והמוקאפים המאושרים משתמשים ב-**26**. בדיקה כזו הייתה מכשילה את שלושת המוקאפים המאושרים ביום הראשון.

**3.6 — לפני סגירה:** הדפס את **כל** המשטחים משלב א עם `כוסה / לא כוסה / לא נדרש`, ובדיקת-כיסוי הפוכה מ-C5/C6 ומהמוקאפים.

---

## ④ שלב 4 — התאמת `module-blueprint`

**המקור:** תוכנית-העל, שורות 1005–1220. **בצע אותו — אבל עם שלושת התיקונים האלה, שנמדדו 06/08:**

**4.1 — 🔴 סתירה פנימית בשלב 4 עצמו.** השלב מורה *"הגדר את היררכיית האמת כך…"* (שורות 1095–1107) **ואוסר לגעת ב-`CLAUDE.md`** (שורה 1071) — **בעוד ההיררכיה יושבת ב-`CLAUDE.md:60-67`.**
⇒ **אל תיצור היררכיה שנייה בתוך הסקיל.** ההכרעה על נגיעה ב-`CLAUDE.md` היא של ישי. **הצף אותה לפני שאתה כותב.**

**4.2 — 🔴 שורה 1125 מורה *"הסר תלות ב-`_shared/discipline.md`"* — הטענה הזאת הופרכה.**
`docs/plans/NEXT-SESSION-handoff.md:52-55`: הקובץ מחזיק את **טבלת-האימות**, **8 סקילים מצביעים אליו**, והאזכור היחיד ל"מנהל" בו הוא *"Ishay is the product manager"*. 🚫 **אל תבצע את ההוראה הזאת.**

**4.3 — שורה 1042 מורה לקרוא `.claude/skills/module-discovery/`.** ‏**הסקיל הזה לא ייבנה** (הכרעת 06/08 — ר' "מה מחוץ להיקף"). ⇒ **מחק את שורת-הקריאה**, בדיוק כפי שהתוכנית עצמה מורה במסלול-הנסיגה שלה (שורות 850–853).

**4.4 — מה כן לשנות בסקיל, ואלה שני הדברים:**
- **מצביע ל-`docs/specs/module_NN_*/`** — נמדד: `docs/specs` מופיע **0 פעמים** בכל `.claude/skills/`, **0** ב-`CLAUDE.md`, **0** ב-`docs/guides/modules/module_04_hostesses.md` *(הקובץ שישי בפועל מדביק ממנו)*. ⇒ **הוסף גם שם.**
- **הסר את מה שהאפיון כבר עשה** (תוכנית-העל 1118–1120): ראיון-PM מלא · סיכום-יכולות · שאלות שמבקשות מישי להכריע מחדש. **הכרעת-ישי 06/08.**
  ⚠️ **וסייג:** ההסרה תקפה **רק כשקיים אפיון מאושר**. אם אין — הסקיל **עוצר ומחזיר**, לא ממשיך בלי ראיון.

**מה לשמר במפורש:** בקר ההקשר-הטרי (12 ממצאים על בלופרינט מ3) · בדיקת-כיסוי מול C5 · `🗡️` · מיפוי RLS · פורמט-הצעד `Goal / Files / What / Verify` · שער ה-typed-echo.

**4.6 — 🔴 לדרוש את הצהרת-ההשפעה, שמעולם לא הופקה** ✅ *(אושר ע"י ישי 06/08 — היה `8.4`)*
‏`module-blueprint/template.md:27` **כבר מחייבת** פסקה שעונה על ארבעה דברים: **אילו קבצים *קיימים* המודול משנה · הוכחה שכל השאר הוא הוספה בלבד · מה עלול להישבר · ואיזו בדיקה שומרת על זה** — ‏➕ **ובדיקת-התנגשות מול כל מדריך-מיקרו פתוח אחר.**
🔴 **נמדד: `grep -rn "Integration Impact" docs/micro_guides/*.md` ⇒ אפס. בשלושת המודולים. הדרישה קיימת מ-08/07 ומעולם לא בוצעה.**
⇒ **זו התשובה המובנית לשאלה של ישי — *"איך אני מתקן דבר אחד בלי לשבור אחר"*.**
**והמקרה החי שמוכיח למה:** ה-RPC של מודול 3 **כבר מכניס שורות ל-`logistics`, הטבלה של מודול 5** — ולמודול 5 **אפס חובות רשומים.** ‏**פסקה כזו הייתה תופסת את זה.**
**מה לעשות:** לוודא שהדרישה שורדת את ההתאמה, **ולדרוש אותה בפועל בבלופרינט של מודול 4.**

### 🐦 4.5 — הקנרית: איך נדע תוך שבוע אם המצביע באמת עבד

> **הסיכון היחיד של בלוק ④, ואני מנסח אותו במפורש:** כל ערכו הוא **שינוי-ניתוב** — הוא משנה מה סשן *קורא*, ומניח שזה משנה מה הוא *בונה*. **אף מודול מעולם לא עבר בלופרינט כש-`docs/specs/` היה קיים.** זו ההנחה הלא-בדוקה.

**המבחן קיים כבר על הדיסק, והוא בינארי:**
`C5:596` דורש מילולית **אייקון פח-אשפה למחיקה** · `processes-approved.md:131` הכריע **"אין מחיקה בשום מקום"**. ‏**בבלופרינט של מודול 4 שני המסמכים ייכנסו לאותו סשן.**

- ✅ **הניתוב עבד** — הבקר מזהה שההכרעה גוברת, ו-`grep -c "C5-coverage" docs/micro_guides/module-4.md` ≥ 1 עם שורה בטבלת §9 שמצביעה על הפרק המכריע.
- ❌ **הניתוב לא עבד** — הבקר מסמן את אייקון-המחיקה כ**השמטה שקטה**, כלומר `template.md:15` (*"extract directly from C5+C6+mockups"*) ניצח את שורת-ההיררכיה החדשה.
  **הנפילה במקרה כזה:** לייצר את רשימת-הנדרש **כדאטה** — סקריפט שמחלץ אותה מהאפיון המאושר — במקום להסתמך על כך שהבקר יקרא נכון.

---

## ⑤ בלופרינט מודול 4 — תוכנית-העל, שורה 1221

**קלט:** `docs/specs/module_04_hostesses/` (כולל `spec.md` וארבעת הפריטים) + `docs/module4_smart_match_research.md` §9 ו-§11 — **מפרט-האלגוריתם, כבר מוכרע, לא נפתח מחדש.**
**פלט:** `docs/micro_guides/module-4.md`. **שער אישור: Plan Mode.**

---

## ⑥ בניית מודול 4 — תוכנית-העל, שורה 1313

`module-build`, לפי המדריך. **סדר: DB/RLS ⇒ לוגיקה ב-`src/lib` ⇒ UI ⇒ בקרה.**
לכל יחידת-בנייה: **🗣️ תקציר-חוויה בעברית + המתנה לאישור לפני קוד.**

**6.1 — 🔑 דוח הבדיקות בעברית לישי, בסגירת המודול** ✅ *(אושר ע"י ישי 06/08 כניסוי — היה `8.1`)*
**קיימות 78 בדיקות E2E ב-13 קבצים, וכותרותיהן הן משפטי-מוצר בעברית.** ‏**ישי מעולם לא ראה אותן — הוא רואה `73/73`.** ‏`playwright.config.js:26` **כבר מוגדר עם דוח HTML.**
```bash
npx playwright show-report
```
**למה זה שווה, בשמות אמיתיים שנשלפו 06/08:** ‏*"**מחיר מתחת לעלות מזהיר אך אינו חוסם** — והשמירה אכן יוצאת (LOCAL-20)"* · *"סיסמה שגויה… **כדי לא לגרום לנעילת חשבון**"* · *"**ארכוב לקוח עם הצעה פתוחה מתריע**"* · *"**עמודת המנכ"ל נעולה** (הגנת self-lockout)"*.
⇒ **אלה כללים עסקיים שכבר אכופים בקוד — וחלקם הוכרעו בלי אישורו** *(התג `LOCAL-20` אומר במפורש שההכרעה נעשתה תוך-כדי בנייה)*. ‏**ובדיקה שמבטאת כלל שגוי עוברת בירוק לנצח ומקפיאה את הטעות — גרוע מלא-לבדוק, כי זה נראה כמו הגנה.**
⚠️ **הגבולות:** רובן יהיו בבירור נכונות · **והיא אינה תופסת מה חסר** — כלל בלי בדיקה לא יופיע · וזו תמונת-רגע.
🧪 **וזהו ניסוי עם תנאי-כיבוי מפורש: מריצים פעם אחת. אם ישי קורא ולא אומר ולו פעם אחת "רגע, זה לא נכון" — זורקים את הרעיון.**

---

## 🚫 מה **מחוץ** להיקף השבועיים — והחלטה, לא שכחה

| מה | למה |
|---|---|
| סקיל `module-discovery` | הריפו רשם בעצמו (`d2705e6`): *"לא יכול לעזור למודול 4 — הוא נבנה אחרי שה-Discovery שלו נגמר"*. נבנה עבור 7 פריטים, 6 מהם בני 2–3 ימים |
| `quality-fix` · התאמת `quality-audit`/`module-close`/`post-merge` | תקרת-ההחזר על השקעת-שיטה היא **~7 ימים**, ואלה מחזירים מול מודולים שחלקם ייחתכו |
| כל סקיל חדש | — |
| ~~סידור מבנה-הקבצים~~ | 🔴 **תוקן 06/08: כאן היה כתוב "נרשם, לא עכשיו" — וזה היה שקר, הוא לא היה רשום בשום מקום. עבר לבלוק ⑦.1.** *(אותה מחלקה בדיוק כמו `processes-approved.md:456` — טענת-ציות בטבלה שאין מאחוריה כלום.)* |

**זה מה שחוסך 5–12 יום.**

---

## 👤 הכרעות שממתינות לישי — **אל תכריע בעצמך**

1. **M2** — למזג `dev`→`main`, או למחוק את התאריך?
2. **`CLAUDE.md`** — האם מותר לגעת בהיררכיית-האמת (בלוק ④, סעיף 4.1)?
3. **15 דקות או שעה** — כמה זמן המערכת עצמה מוצגת? *(הריפו סותר את עצמו: `CLAUDE.md:18` "שעה" מול שלושה קבצים שמצטטים "רבע שעה". נענה חלקית 06/08: **שעה סה"כ, המערכת ~30 דקות**. טעון אישור סופי.)*

---

## ⑦ תחזוקה — שישה פריטים שעלו בניתוח 06/08 ואין להם בית · **סשן אחד, בלי תלות בשאר**

> **מתי:** בכל נקודה שבה בלוק אחר חוסם, או כשמחכים להכרעה של ישי. **אינו חוסם אף בלוק אחר.**
> **תנאי-סיום:** ששת הפריטים סומנו ✅ או הועברו ל-`PROJECT_MASTER §6` עם נימוק.

**7.1 — 🔴 סריקת חי/יתום/מת על `docs/`** *(זו התשובה ל"הבלגן בקבצים", ולא סידור תיקיות)*
**הבלגן אינו מספר הקבצים — הוא שאי-אפשר לדעת אילו חיים.** ראיות מדודות 06/08: `STATUS.md` מונה ענף מת · `00_roadmap.md` אומר שמודול 3 "בעבודה" · `discovery_lessons.md:704` אומר 3 משטחים סגורים מול 4 ב-`screens-approved.md:93` · 8 מצביעים שבורים במדריכים · ו-`ideas-backlog.md:98` מצהיר בעצמו *"אין שום מנגנון בפרויקט שקורא את הקובץ הזה"*.
**המבחן כבר כתוב** — `docs/CLAUDE.md` כלל-המיקום: **① מי הקורא? ② איך הוא מגיע לשם?** ‏*"אין תשובה ל-② ⇒ הקובץ מת"*.
**מה לעשות:** להריץ אותו על כל `docs/` ולהחזיר **טבלה בת שלוש ערמות: חי · יתום · מת** — ‏**דיווח בלבד, בלי למחוק דבר** *(מחיקה היא פעולת-ישי; היעד הוא `docs/delete/`)*.
**זה בדיוק מה ש-`skill-scan` נבנה בשבילו — 0 הפעלות עד היום.**

**7.2 — הרודמפ מיושן ב-14 יום, והוא התצוגה החודשית היחידה**
`docs/guides/00_roadmap.md` — קומיט אחרון **23/07**. שורה **51** עדיין `3 (בעבודה)` · שורה **161** עדיין `🔨 בבנייה`. **מודול 3 מוזג 05/08.**
**מה לעשות:** שתי שורות סטטוס. **‏🚫 ואל תכתוב תוכנית חודשית חדשה** — הכרעת-ישי 06/08: *מסמך חדש היה נהיה התוכנית המיושנת השלישית*, והריפו כבר רשם למה (*"פרומפט שנכתב הרבה לפני שהוא רץ צובר טענות שגויות, כי הריפו זז תחתיו"*).
**וגם:** לבדוק אם הקצאות-הימים בזנב עדיין אמינות — הן נגזרו ממודולים 0+1+2 בלבד, **לפני שמודול 3 (21 יום) קרה.**

**7.3 — `regin-docs-sync` לא רץ מאז 31/07**
לא בסגירת מודול 3, לא בכל הדיסקברי, ולא ביום שחמש סתירות-הקוהרנטיות נחתו. **וזו הרוטינה שכל תפקידה הוא בדיוק המחלקה הזאת.**
🔴 **קלוד אינו מריץ רוטינות** (`docs/CLAUDE.md` כלל 13ז) ⇒ **המלצה לישי + נימוק למה עכשיו, והמתנה.**

**7.4 — `STATUS.md` = 303,060 בייט, פי-34 בתשעה ימים**
`docs/CLAUDE.md:12` מגדיר אותו **"מצב-עכשיו בלבד, בלי היסטוריה"**. הוא היה 8,922 בייט ב-28/07.
**מה לעשות:** להציע לישי מדיניות-גודל (~100 שורות מצב חי) + העברת הזנב ל-`docs/archive/STATUS_full_2026-08-06.md` *(התקדים כבר קיים)*. ‏**הצעה, לא ביצוע — זה הלוח שלו.**

**7.5 — `module-3.md` לא נדחס בסגירה, בניגוד לכלל שקיים**
`module-close/template.md:75` מחייב דחיסה בסגירת מודול. המדריך = **2,273 שורות**, סטטוס `🔒 CLOSED`, ובארכיון יש רק עותקי-אמצע מ-28/07 ו-29/07. ➕ **וכותרת-המצב שלו היא בצל append-only** — תאים של ~4,500 תווים עם 15 שכבות `*(previous entry:` מקוננות, בעוד מודולים 1 ו-2 נקיים לגמרי.
**מה לעשות:** לדחוס לפי הכלל, ולהוסיף לטמפלט שורת **"מחליפים, לא מקננים"** לכותרת-המצב.

**7.6 — 14 הימים שאיש לא הסביר**
בלופרינט מודול 3 אושר **15/07** (`d8905e6`); שורת-הקוד הראשונה שלו **29/07** (`107acda`). **‏14 יום — יותר מכל חלון-הקידוד של המודול (4 ימי-קומיט).**
**זה הפער היחיד בציר-הזמן שאיש לא הסביר, והוא הגדול ביותר.** **מה לעשות:** לקרוא את `CLAUDE_CODE_LOG` בין התאריכים ולהחזיר **שורה אחת: לאן הלכו.** ‏**אם התשובה היא "כיול ותשתית" — זה נתון תכנוני למודול 4, לא הערה היסטורית.**

---

## ✅ מה כבר בוצע ב-06/08 — אל תעשה שוב

| מה | איפה |
|---|---|
| היררכיית-האמת קיבלה דרגה ל-`docs/specs/` | `CLAUDE.md` §היררכיית מקורות אמת |
| "רעיון ≠ הכרעה" נכנס ככלל כללי | `CLAUDE.md` כלל ברזל 1 |
| 4 כשלי-אכיפה + 3 שגיאות-רשמים + שער-Plan-Mode | `PROJECT_MASTER §6`, שלוש שורות `🚧 מ4` |
| 5 הכרעות-שיטה של ישי + תיקון-פרפרזה | `_shared/ishay-calibration.md §8` |
| **פרומפט הדיסקברי הגנרי תוקן בארבעה מקומות** | `docs/guides/prompt_module_discovery.md` — **ד+ה מוזגו** (מסך=מוקאפ) · **§ה החדש = כלל-הסשן** · **§ז נוקב בנתיב** ו-`spec.md` צומצם · **§11 חדש = ארבעת פריטי-החוזה** |

---

## ⑧ המלצות שניתנו בשיחת 06/08 ולא הוכרעו — **ממתינות לישי, לא משימות**

> **למה הפרק הזה קיים:** נאמרו בצ'אט, ישי לא אמר עליהן כלום, **וצ'אט שלא הומר לצעד = לא קיים.**
> ‏🚫 **אל תבצע פריט מכאן בלי אישור מפורש.** ‏**כל אחד נושא: מה · למה · כמה · והמלצה.**

**8.5 — Discovery ירוץ שוב רק על מודולים 6 ו-8** ✅ *(אושר 06/08)* — 6 מתוך 8 הפריטים שנשארו הם מודולים בני 2–3 ימים, ושלושה מהם כבר מסומנים כראשונים ליפול. **ואחרי מודול 4 נבדוק אם זה בכלל עזר** (דרישת-ישי).

**8.6 — פרומפט-ההמשך גנרי** — `next-session-prompt.md` הוא 188 שורות ומודול-4-בלבד. לחלץ ממנו את הצורה לתבנית — **אחרי שמודול 4 ייסגר, לא לפני.**

---

## 📝 יומן סטיות — append-only

*(שורה מתוארכת לכל סטייה מהתוכנית הזאת. אל תשכתב את התוכנית — הוסף כאן.)*

- **06/08/2026** — התוכנית נכתבה.

</div>
