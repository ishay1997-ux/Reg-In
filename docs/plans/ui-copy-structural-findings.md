<div dir="rtl">

# ממצאי-מבנה שהאיפוס חשף — לאודיט-ה-UCD, לא לתיקון הלילה

> **מה הקובץ הזה:** רשם כשלי-המבנה שלילה-הטקסטים חושף ואינם עבודת-קופי (`ui-copy-session-brief.md` ⑥⑥ · `PROJECT_MASTER §4א`).
> **מתי נרשם:** כשעומדים לכתוב משפט שכל תפקידו לומר **מה המסך הזה** · **מה עושים כאן ראשון** · **לאן העבודה ממשיכה** — שלושת הסוגים
> שכותרת, מצב-ריק, תגית או פירורי-לחם פותרים טוב יותר ממילים. **המבחן:** *האם תווית אחרת, עמודה או תגית היו מייתרות את המשפט?*
> **ספק ⇒ רושמים** (המדריך §2, ההבהרה השלישית). 🚫 **הלילה אינו מזיז מסכים, אינו מוסיף עמודות ואינו משנה פריסה** — זו הכרעת-מוצר של ישי ואודיט נפרד.
> **הצרכן:** אודיט-ה-UCD (`PROJECT_MASTER §4ב`), שהוכרע על כל המערכת. **הצורה קבועה:** מסך · המשימה שנתקעה · מה נאלצה לדעת על המערכת כדי להמשיך · תיקון-מבנה מוצע *(לא מוכרע כאן)*.
> **נוצר:** 08/09/2026 23:3X, סשן 2 של הלילה, פתיחת שלב 2. **מי כותב:** הסשן הממזג בלבד, מדיווחי הכותבים.

## ממצאים שכבר נרשמו לפני השלב הזה (המדריך §4 · §7)

| # | מסך | המשימה שנתקעה | מה נאלצה לדעת | תיקון-מבנה מוצע | מקור |
|---|---|---|---|---|---|
| S-01 | מסך-הבית — באנר הסכומים החלקיים (`DashboardPage.jsx`) | להבין למה הסכומים נמוכים מהאמת | שיש פרויקט בלי הצעת-מחיר מקושרת, ואיזה | דגל פר-פרויקט על אריחי-הכסף (אילו פרויקטים לא נספרו) — היה מייתר את רוב הבאנר | המדריך §4 דוגמה 4 |
| S-02 | ניהול משתמשים — דיאלוג "הוספת משתמש" (`UsersManagementPage.jsx`) | לדעת מי מהמשתמשות עדיין אינה יכולה להתחבר | שחשבון-הכניסה נוצר בנפרד ואינו מסומן ברשימה | סימון ברשימת-המשתמשים למי שעדיין אין חשבון-כניסה | המדריך §7 סבב 2 |
| S-03 | פרויקטים (`ProjectsPage.jsx`) — `<h1>פרויקטים</h1>` מול `<Th>אירוע</Th>` | לקרוא את הטבלה בלי לתהות אם זו רשימת-פרויקטים או רשימת-אירועים | ש"פרויקט" = רשומת-הניהול ו"אירוע" = העובדות (תאריך · מקום · אורחים) — הבחנה שקיימת בקוד ולא על המסך | הכרעת-מינוח אחת לכותרת ולעמודה, או תת-כותרת שמסבירה את הזוג | המדריך §3ד · המצאי §5ג |

## שלב 2 · רכיבים משותפים + לשון-נקבה

| # | מסך | המשימה שנתקעה | מה נאלצה לדעת | תיקון-מבנה מוצע | מקור |
|---|---|---|---|---|---|
| S-04 | מסך-הכניסה — חשבון לא-פעיל / לא-מורשה (`LoginPage.jsx` · `MainLayout.jsx` · `AuthContext.jsx`) | להיכנס למערכת כשהחשבון הושבת או טרם הורשה | שהפעלה/הרשאה נעשות ע"י המנכ"ל בניהול-המשתמשים — המסך יכול רק לומר "יש לפנות למנכ\"ל" | אין תיקון-מבנה במסך-הכניסה עצמו; הממצא הוא **S-02** מהצד השני — סימון ברשימת-המשתמשים למי שממתינה להפעלה/הרשאה | W1/W2 08/09 |
| S-05 | הפרופיל — שדה "שם מלא" (`ProfileSettingsPage.jsx` · `UsersManagementPage.jsx`) | למלא את השם בפעם הראשונה | placeholder `שם פרטי ומשפחה` משמש כתווית-משנה במקום דוגמה (R5) — ומשוכפל בשני מסכים | placeholder בצורת `לדוגמה: …` בשני המסכים יחד (R30); ביצוע בשלב 8 (מ1) | W1 08/09 |
| S-06 | דיאלוגים משותפים (`ConfirmDialog.jsx`, ברירות-המחדל `אישור`/`ביטול`) | לדעת מה יקרה בלחיצה כשקורא לא העביר `confirmLabel` | R12: כפתור-אישור גנרי אינו אומר מה יקרה | אודיט של כל אתרי `useConfirm()` בלי `confirmLabel`; אם אין כאלה — ברירת-המחדל לעולם אינה מוצגת ואין ממצא | W1 08/09 |

## שלב 3 · מ7 מסך-הבית

| # | מסך | המשימה שנתקעה | מה נאלצה לדעת | תיקון-מבנה מוצע | מקור |
|---|---|---|---|---|---|
| S-07 | מסך-הבית — באנר פרמטרים חסרים (`src/lib/quotes.js` `missingParamsMessage`, מוזרם דרך `dashboard.js`) | להבין מה חסר כשפרמטר-מערכת נמחק | שם-הפרמטר הגולמי (`ימי_אזהרה_קדם_אירוע`) מודפס על המסך — **בכוונה מוצהרת בקוד** ("זה מה שמחפשים ב-Table Editor"), כלומר מסך-אבחון למנכ"ל בתחפושת באנר | קישור ישיר ל"פרמטרים" בהגדרות במקום שם-עמודה; או להשאיר כמסך-אבחון ולסמן זאת. הכרעת-מוצר — לא שונה הלילה (B8 מול כוונה מתועדת) | W7 09/09 |

## שלב 4 · מ6 פרויקטים

| # | מסך | המשימה שנתקעה | מה נאלצה לדעת | תיקון-מבנה מוצע | מקור |
|---|---|---|---|---|---|
| S-08 | ביטול פרויקט — טוסט-ההצלחה + תג-הסטטוס (`CancelProjectDialog.jsx` · כרטיס-הפרויקט) | לדעת שפרויקט מבוטל עדיין ממתין לכספים לבירור-הוצאות (spec §🏁6) | שהביטול אינו סוף-הדרך — המסך אומר רק "בוטל" | תג-סטטוס שמבחין `בוטל` מ-`בוטל — ממתין לבירור-הוצאות`, או שהטוסט נוקב במסירה לכספים | W8d 09/09 |
| S-09 | שינוי-תכולה — שדה-כמות מושבת (`ScopeChangeDialog.jsx` `QtyInput` `disabledReason`) | להבין למה אי-אפשר לשנות כמות של פריט | הסיבה ("לפריט אין שורת-לוגיסטיקה מקושרת…") יושבת ב-`title=` בלבד — ולפקד מושבת אין hover, כלומר לעולם לא נראית | אותו תיקון ש-`RemovalButton` באותו קובץ כבר עשה: טקסט גלוי + `aria-describedby` | W8c 09/09 |

## שלב 7 · מ2 לקוחות + מ3 הצעות מחיר

| # | מסך | המשימה שנתקעה | מה נאלצה לדעת | תיקון-מבנה מוצע | מקור |
|---|---|---|---|---|---|
| S-15 | מסמך-ההצעה ללקוח — תנאי-ההצעה (`quotePdf.jsx` `QUOTE_TERMS[2]`) | הלקוח קורא "ההצעה בתוקף 30 יום" בעוד תאריך-התוקף המודפס נגזר מהפרמטר `ימי_תוקף_הצעה` | שהמספר בתנאים קבוע-בקוד והתאריך חי — שינוי-פרמטר יוצר סתירה **בתוך מסמך שיוצא ללקוח** | לגזור את המספר מהפרמטר (או להשמיט את המספר מהתנאי ולהשאיר את התאריך) — קוד, לא קופי; והתנאים מסומנים "ממתינים לניסוח משפטי" | W11d 09/09 |
| S-16 | שלושה נוסחים ל"מע"מ לא מוגדר" (`quotePdf.jsx` `MISSING_VAT_MESSAGE` · `QuoteBuilderPage.jsx` · ה-`raise` ב-RPC של אישור-ההצעה) | להבין שזו אותה תקלה, בשלושה מקומות | שהמסמך, המסך וה-RPC אומרים אותו דבר במילים שונות (R30) — ולא קשורי-בייט | קבוע אחד ב-`lib/quotes.js` שכל השלושה מייבאים (ה-`raise` נשאר כפי שהוא; הלקוח ממפה לפי קידומת) | W11d 09/09 |
| S-17 | רשימת-לקוחות — גיליון-המסננים, מסנן "שביעות רצון" (`CustomersFilterSheet.jsx`) | לסנן לפי שביעות-רצון | שהמסנן מושבת עם `אין נתונים עדיין` בעוד עמודת-המשוב כבר חיה — הערת-הקוד ב-`CustomersPage.jsx` מסמנת זאת כהכרעת-ישי (הוסר בסגירת-מ2 והוחזר) | להפעיל את המסנן או להסיר אותו — הכרעת-מוצר, לא נוסח | W11a 09/09 |
| S-18 | בונה-ההצעה — שדה ההנחה הידנית "נוספת" (`QuoteBuilderPage.jsx`, בתוך `LtrFieldGroup` `quote-discounts`) | לדעת שהשדה אינו חובה | R6: שדות-החובה איבדו את הכוכבית, אבל השדה הלא-חובה היחיד בקבוצה אינו נושא `(לא חובה)` — `LtrFieldGroup` הוא רכיב-משותף שרוחבו מכויל (`src/CLAUDE.md`) | תווית `(לא חובה)` בתוך `LtrFieldGroup` אחרי בדיקת-פריסה חיה — רכיב משותף, מחוץ ללילה | W11b 09/09 |
| S-19 | מסמך-ההצעה — כותרת הסעיף `פרטי הפרויקט` (`quotePdf.jsx`) | — | הסעיף מציג עובדות-אירוע (תאריך · מיקום · שעות) תחת המילה "פרויקט" — מופע שני של S-03 (פרויקט/אירוע), הפעם במסמך שיוצא ללקוח | אותה הכרעת-מינוח של S-03, מוחלת גם על ה-PDF | W11d 09/09 |

## שלב 6 · מ4 דיילות + Smart Match

| # | מסך | המשימה שנתקעה | מה נאלצה לדעת | תיקון-מבנה מוצע | מקור |
|---|---|---|---|---|---|
| S-11 | מבט-על השיבוצים — מצב "אין הרשאה" (`OverviewTab.jsx` `EmptyState`) | מנהלת עם 'דיילות'=עריכה ו'פרויקטים'=חסום רואה קיר-הרשאה ומופנית למנכ"ל | שמבט-העל קורא מ-`projects` ולכן תלוי בהרשאת 'פרויקטים' — צימוד שמטריצת-ההרשאות אינה מראה | במטריצת-ההרשאות: סימון/הערה ש'דיילות' דורש קריאה ב'פרויקטים'; או שהמסך ינקוב במודול החסר (הוא כבר עושה — אבל המנכ"ל בצד השני לא רואה זאת). ⚠️ אינו נורה במטריצה של היום (אין תפקיד כזה) — `04_hostesses/CLAUDE.md` | W10a 09/09 |
| S-12 | מאגר הדיילות — עמודת "אירועים · רבעון אחרון" והגלולה "לא ענתה ל-N" (`RepositoryTab.jsx`) | לדעת שמאחורי המספר יש הגדרה (חלון 90 יום · רק אושרה-סופית · השורה המכריעה) | תאי-טבלה רגילים בלי שום סימן שקיימת הגדרה — גם כשתיכתב השכבה, במצב כבוי אין אות להדליק | סימן-הגדרה קטן בכותרת-העמודה (או תת-כותרת "90 יום אחרונים") — הכרעת-מוצר | W10a 09/09 |
| S-13 | כרטיס-דיילת — סעיף "העדפות של לקוחות" (`HostessViewCard.jsx`) | אין משימה שנתקעת; הסעיף ריק תמיד עד שמודול 6 יכתוב את הטבלה | שהריק הוא "עוד לא נבנה" ולא "אין נתונים" — הבסיס אומר עכשיו `טרם נרשמו העדפות`, שהוא נכון מילולית אך מסתיר שאין מה שייכתב | להסתיר את הסעיף כל עוד אין שורות בטבלה (אין פאנל ריק קבוע לשופט) — הכרעת-מוצר | W10b 09/09 |
| S-14 | טופס הוספת דיילת — שדה תעודת-זהות (`HostessFormDialog.jsx`) | למלא את הטופס בלי להיתקל בשגיאה לפני שהקלידה | דבר — השגיאה `יש למלא תעודת זהות` מוצגת באדום כבר ברגע פתיחת הטופס (נמדד בצילומי לפני/אחרי של 09/09, קדם-ללילה) | R21: לירות את שגיאת-החובה ביציאה מהשדה או בניסיון-שמירה, לא בפתיחה — התנהגות, לא נוסח; לא שונה הלילה | S3 09/09 |

## שלב 5 · מ8 כספים

| # | מסך | המשימה שנתקעה | מה נאלצה לדעת | תיקון-מבנה מוצע | מקור |
|---|---|---|---|---|---|
| S-10 | דוח-שכר — כפתור "ייצאי ושלחי" בזמן טעינת-ההיסטוריה (`SalaryReportDialog.jsx`) | להבין למה הכפתור מושבת לרגע | הסיבה (`בודקת אם כבר הופק דוח לחודש זה`) יושבת ב-`title=` בלבד — פקד מושבת אינו מציג hover | שורה גלויה קצרה ליד הפוטר בזמן הטעינה (אותו תיקון של "שלחי שוב") | W9c 09/09 |

</div>
