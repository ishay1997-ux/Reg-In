<div dir="rtl">

# REG-IN — ליטושים אחרונים לפני הכנס: מצב 0 בוגר · מצב 2 "לא נתקעת" · AI גלוי

> **נכתב 23–24/09/2026 בלילה ע"י סשן-הסגן, לסשן-בנייה חדש.** ישי: *"חשוב שסשן חדש יבין היטב ולא יצטרך לנחש כלום"*.
> **גרסה 2** — אחרי שני מבקרים עצמאיים (Opus). מבקר 1 בדק 51 טענות: 22 נכונות · 16 חלקית · 7 שגויות · 6 לא ניתנות לבדיקה, ומצא 8 חוסמים. מבקר 2 מצא 16 חורים. **כולם תוקנו כאן**, כל אחד עם הראיה שלו.
> 📏 **נקרא לפני הכתיבה:** כל מה ששני הצדדים כתבו, 16:21 (17/09) → 23:16 (23/09): ‏192 הודעות של ישי (249KB) ו-598 שלי (443KB). נקראו במלואן **153 ההודעות שישי הקליד בעצמו**, ואת שלי מהקומפקט של 23/09 22:01 והלאה. **את ההודעות שלי שלפני כן לא קראתי.**
> **סימון-מקורות:** 📏 נמדד (הפקודה או הקובץ לידו) · 🤖 מדוח-סוכן, **לא אומת** · 💭 שיפוט.
> **🏠 המקור היחיד:** `docs/plans/2026-09-24-system-polish.md` בענף `ishay/system-polish`. העותק ב-`~/.claude/plans/` הוא טיוטה. **§12 מתעדכן רק בקובץ שבריפו.**

---

## 1 · 📖 רשימת-קריאה — לפני כל פעולה, בסדר הזה

**כלל-הקריאה:** *«גריפ רק כדי **למצוא איפה**, לעולם לא כדי **לענות**. בטווחים גדולים ככל שהכלי מרשה. והגדרה שמספר נשען עליה — עד סופה.»*

| # | מה | מה לקחת |
|:-:|---|---|
| 1 | **הקובץ הזה, במלואו** | הכל. §12 היא המסירה |
| 2 | `docs/plans/ui-copy-styleguide.md` | מדריך-הסגנון המאושר: §1 פנייה (ציווי-נקבה מול אינפיניטיב, בלי לוכסן) · R1–R30 · **R14: אין נקודה בסוף כותרת** · מילון §3 |
| 3 | `~/.claude/skills/ui-copy-and-onboarding-levels/SKILL.md` + `references/copy-rules.md` + `references/data-surfaces.md` | שיטה וכללים. ‏📌 **שלבים A–C של הסקיל כבר בוצעו בפרויקט** (המדריך מאושר, המנגנון קיים) ⇒ כאן מבצעים רק D (לפי יחידה), E, F |
| 4 | `docs/specs/module_11_reports/onboarding-layer-contract.md` | `<Hint id>` מחזיר `null` בשקט על מפתח חסר · מצב 1 ריק בהכרעת-ישי (`:43`) |
| 5 | `docs/plans/2026-09-23-module-11-decision-reports.md` §4ד + §4ה | תקן-הכרטיס + חוקי מצב 0/2 (2.1–2.10). ‏**2.3 = ≤4 רמזים בגובה-מסך אחד**, לא "לדוח" |
| 6 | `src/lib/onboardingCopy.js` — **כולו**, כולל כלל-הבית בשורות 59-60: *אפס ספרות, וסף נכתב "שנקבע בהגדרות"* · `src/components/Hint.jsx` (`:44-58`: טקסט סטטי, בלי אינטרפולציה) · `src/lib/onboardingCopy.budget.test.js` | איך רמז נכתב, מוצג ונבדק |
| 7 | `src/modules/11_reports/components/KpiTile.jsx:167-195` (`TileDetails`) | הדגם ל-ⓘ: `<details>` **כאח** לכפתור-הדלת |
| 8 | `src/modules/11_reports/tabs/customers/AnalysisRunBar.jsx` (כולו) · `supabase/functions/classify-feedback/index.ts` + `README.md` (כולו) | D1 · **התבנית ל-D2**: סדר השער `:722-778` (סוד ⇐ זהות ⇐ הרשאה ⇐ גוף) · **החלטת-הפרטיות `README:93-95`** · המכסה `README:151-183` |
| 9 | `src/CLAUDE.md` — **§4** (מעבר-האיכות בחמישה צעדים למסך חדש · אייקונים lucide `size-4`) · `e2e/CLAUDE.md` §2.1 + §4.3 · `src/modules/11_reports/CLAUDE.md` §2.4 (מפתחות `reports.<slug>.<field>`) · `docs/CLAUDE.md` §4 (פרוטוקול-אדוות) | כללים שהתוכנית נשענת עליהם |
| 10 | `.claude/hooks/check-docs-updated.sh:1-30` + `:154-190` | ה-Stop-hook: מדריך-מיקרו לכל מודול ששונה · אכיפה **0ד**: תוכנית ב-`docs/plans/` חייבת אזכור ב-`docs/PROJECT_MASTER.md` · **LOG ו-STATUS נערכים אחרונים** (mtime) |

🟡 **כנראה לא תצטרך. פתח רק על פער אמיתי, ואמור שפתחת:** `docs/guides/do-not-touch.md` · `docs/PROJECT_MASTER_sec7.md` (§7.80 — רק ל-C3) · `docs/guides/01_estimation_log.md` §1.

## 2 · 📍 איפה לעבוד — אמת, אל תניח
```
git -C C:\Users\ishay\Reg-In-export-wt fetch origin
git -C C:\Users\ishay\Reg-In-export-wt switch ishay/system-polish     # נוצר ונדחף ע"י הסגן; הקומיט הראשון = הקובץ הזה
git -C C:\Users\ishay\Reg-In-export-wt log --oneline origin/dev..HEAD  # חייב: קומיט-התוכנית בלבד
git -C C:\Users\ishay\Reg-In-export-wt status --short                 # חייב: רק .agents/ .codex/ (לא שלך, לא לגעת)
```
- **התקנה:** `npm ci` ולא `npm install`, כי `install` עלול לשכתב את `package-lock.json`, ו-CI משתמש ב-`npm ci` (`.github/workflows/ci.yml:24`). אם `npm ls --depth=0` יוצא 0, אין צורך להתקין.
- **`.env.local`:** 🚫 **לעולם אל תדפיס אותו.** מותר רק לבדוק שמות-משתנים: `grep -oE '^[A-Z0-9_]+=' .env.local` (עם **0-9**, אחרת `E2E_` לא נתפס). אמורים להיות `E2E_CEO_*` · `E2E_RECRUIT_*` · `E2E_FINANCE_*` · `E2E_PROJECTS_*` · `E2E_STAFF_*`.
- **🚫 לא עובדים בתיקיות האלה:** `C:\Users\ishay\Reg-In` (שינויים ישנים לא-מקומטים שאינם של אף אחד) · `Reg-In-dash-wt` (סשן-הדוחות, הסתיים) · `Reg-In-copy-wt`.
- **מי עוד עובד:** סשן-הסגן, בקריאה-בלבד. **אתה הכותב היחיד בענף.**

## 3 · מצב נוכחי — עם הפקודה שמאמתת
- **דוחות-ההחלטה בייצור:**
  - `gh pr view 155 --json state,mergeCommit` ⇒ MERGED, `main` @ `4d718709`.
  - Vercel Production deployment `6623623902` = success.
  - ‏📏 אומת חי ע"י הסגן, 23/09 23:4X.
- **הענף שלך יוצא מ-`origin/dev`,** ובו כל עבודת-הדוחות.
- 🔴 **תקלה חיה שהתגלתה בביקורת — חבילה 0 למטה.**

## 4 · 💬 מה ישי אמר — מילה-במילה, מתוארך

**הכרעות:**
- 23/09 23:02 — *"ריפקטורינג שכל המערכת תהיה מצב בוגר ומצב 2 כמו הדוחות נראלי גם צריך? או המצב הקיים בסדר? שלח כמה סוכני סונאט לסריקות ואז בנה תוכנית מתאימה לכל הליטושים האחרונים למערכת"* ⇒ אחרי 4 סריקות: **ליטוש ממוקד, לא ריפקטורינג** (§5).
- 23/09 20:31 — *"מצב 0 מוצר בוגר אחרי הטמעה … מצב 2 ממש לתחילת השימוש שלא יהיו שאלות למשתמשים תוך כדי עבודה שלא יתקעו"*. **רק 0 ו-2. מצב 1 ריק בהכרעתו.**
- 23/09 20:40 — *"זה גם תיקוני נוסחים שיהיה מובן וידידותי זה חלק חשוב"* · 23/09 20:02 — *"ככה לא ניראת מערכת SAAS מקצועית בכלל"*.
- 23/09 23:06 — *"אין כפתור אבל של נתח הערות במסך אז לא כל כך ברור"* ⇒ D1.
- 23/09 23:1X, על השם בשורת ה-AI — *"פה זה מה שהציק לי אבל תכלס גם לא דרמטי"* ⇒ נכנס ל-D1, כי השורה משתנה ממילא.
- 23/09 23:12 — *"בכנס אני אכנס משלי זה לא מה שמפריע"* ⇒ המשתמש שלו והשם בכותרת **לא משתנים**.
- 23/09 23:1X, על 825 הפרויקטים על שמו — *"הרווח של זה שולי ניתן לוותר"* ⇒ 🚫 לא נוגעים ב-`projects.owner_name`.
- 23/09 23:15 — *"מאשר הכל לפי המלצתך"* ⇒ **חבילות 0 · A · B · C · D · F כפי שהן כאן, כולל D2.**
- 23/09 23:15 — *"תנסה לחלק והמליץ על שימוש בסוכנים וכו בתוכנית ולנסות לצמצם את מספר הסשנים"* ⇒ §7.
- 17/09 19:12 — *"זה פרויקט אקדמי אין לי את מי לשאול אתה יכול להניח מה שאתה רוצה פה"* ⇒ **את שאלות השבוע-הראשון מנסחים בעצמנו.**
- 23/09 16:41 — *"תחסוך ממני מוקאפים"* ⇒ מראים לו צילום לפני/אחרי על המסך האמיתי.
- 22/09 00:07 — *"תמיד תתכנן כמו שצריך … והעזר בעקרונות הUCD"* · 23/09 21:47 — *"אני רוצה להיות מקצועי בסוף"* · 23/09 16:59 — *"בלי קיצורי דרך"*.

**רעיון, לא הכרעה:** 23/09 — *"רציתי להראות יכולות שימוש בAI אמיתי"* ⇒ D2 (אושר).

## 5 · 🧭 למה ליטוש ולא ריפקטורינג
- 🤖 **ארבעה סוכני Sonnet סרקו את מודולים 01–09 בקריאה-בלבד** (23/09): כולם דיווחו שהמודולים עברו סבב-ניסוח (07–09/09), ושהם "קרובים" לתקן-הדוחות. אף אחד לא "רחוק".
- **אבל הסריקות סטטיות:** ‏📏 סורק אחד כתב "לא נמצאו שמות-מסד על המסך", ואחר מצא את `אחוז_מעמ` ואת "מודול 8".
- **ושני המבקרים מצאו עוד שבעה פריטים שהסורקים החמיצו.** ⇒ **מבחן "לא נתקעת" נעשה מול המסך החי.**

## 6 · 🔨 העבודה — ענף אחד, מיזוג אחד

### 🔴 חבילה 0 · תקלה חיה מהערב — ראשונה
**דוח "סגירת הצעות" (ה1): לחיצה על שורה פותחת טופס-עריכה ריק.**
- 📏 **אומת בייצור, 23/09 23:5X:**
  - שורה #2317 (סיבה "חוסר זמינות/לו"ז") ⇐ `/quotes/2317/edit`.
  - הכותרת "עריכת הצעה #2317", והטופס **ריק**: סה"כ `0 ₪`, בלי פריטים, **וכפתור `עדכני ושלחי` פעיל**.
- **למה:**
  - הדלת היא `quote: (id) => '/quotes/${id}/edit'` (`src/modules/11_reports/ReportsPage.jsx:105`).
  - כל שורות ה1 הן הצעות שהוכרעו, ולא `in_progress`.
  - `QuoteBuilderPage.jsx:187-188` מסרב לטעון הצעה שאינה `in_progress`, ובכל זאת הטופס הריק מוצג.
  - ה-e2e (`reports.spec.js:398-407`) בודק רק את ה-URL, ולכן עבר.
- **התיקון — דלת לתצוגה, לא לעריכה:**
  1. `QuotesPage` (`src/modules/03_quotes/QuotesPage.jsx`) מקבל פרמטר-URL `?view=<quoteId>`, שפותח את `QuoteDocumentDialog` (כבר קיים ומיובא, `:62`, `:893`) על ההצעה, בכל סטטוס. **מצב ב-URL:** `CLAUDE.md` §4.2.
  2. הדלת ב-`ReportsPage.jsx:105` ⇐ `/quotes?view=${id}`. **ההערה ב-`:90-91` מתעדכנת** (היא מסבירה למה נבחר `/edit`).
  3. **ובאותו קומיט — הכשל השני שהתגלה:** `QuoteBuilderPage` מציג טופס ריק ופעיל כש-`loadError` קבוע. אחרי `setLoadError` הטופס **לא מרונדר**, ובמקומו הודעה + `חזרה לרשימת ההצעות`.
  4. **e2e:** `reports.spec.js:398-407` בודק שהחלון נפתח **ובו מספר ההצעה**, לא רק URL. + בדיקת-יחידה ל-`QuoteBuilderPage` על הצעה `rejected`.
- ‏💭 **הכרעת-סגן:** הדלת ל"מסמך ההצעה" ולא לעריכה, כי זו כוונת הדלת (*"שורה פותחת את הצעת-המחיר שאבדה"*), והמסך הקיים שמציג הצעה סגורה הוא חלון-המסמך. ‏**הפיך.**

### 🔜 חבילה 0ב · "חזרה למקור" — **לסשן 2, לא נבנתה בסשן 1** *(נוספה 24/09 בבוקר, ע"י הסגן)*
**ישי 24/09:** *"שלוחצים על דיילת ואז סוגרים אתה לא חוזר למסך בדוחות… יכול להיות שהבעיה יותר רחבה"*.
- **כל דלת מדוח** (`ReportsPage.jsx` `DOOR_PATHS`: `hostess` · `project` · `customer` · `quote`) **ומפריטי "דורש תשומת לב" במסך הבית** (`src/lib/dashboard.js`, `href`) מעבירה פרמטר `returnTo`.
  - 🔒 **נתיב פנימי בלבד:** מתחיל ב-`/reports` או ב-`/`. כל ערך אחר ⇐ מתעלמים ממנו (לא מנווטים החוצה).
- **ביעד:**
  - סגירת כרטיס-הדיילת (`HostessesPage`, `closeCard`) ⇐ חזרה ל-`returnTo`.
  - סגירת חלון-המסמך (`QuotesPage`, `?view=` — נבנה בחבילה 0) ⇐ חזרה ל-`returnTo`.
  - כפתורי "חזרה" בכרטיס-פרויקט ובכרטיס-לקוח ⇐ `returnTo`.
  - ומוצג קישור **"← חזרה לדוח"**.
- **כפתור-אחורה של הדפדפן ממשיך לעבוד** (לא דורסים היסטוריה).
- **e2e:** דוח ⇐ שורה ⇐ סגירה ⇐ **חוזרים לאותו דוח ואותו מסנן**.

### חבילה A · ניסוח מצב 0 — כל שורה אומתה בקוד, ובידי מבקר 1
| # | קובץ:שורה | היום ⇐ אחרי | נעול ע"י — **כולם זזים באותו קומיט** |
|:-:|---|---|---|
| A1 | `src/modules/01_auth/LoginPage.jsx:65` | `'מייל או סיסמה שגויים — נסי שוב.'` ⇐ `'המייל והסיסמה לא תואמים — נסי שוב.'` (R18 אוסר "שגוי") | `e2e/auth.spec.js:33` (regex) |
| A2 | `src/modules/06_projects/ProjectCardPage.jsx:560,565` | `מנהל/ת הפרויקט` · `לא הוגדר/ה מנהל/ת פרויקט` ⇐ `מנהלת הפרויקט` · `לא הוגדרה מנהלת פרויקט` | אין (`test:187` בודק testId) |
| A3 | `src/modules/08_finance/FinancePage.jsx:83` | `לסינון שבחרת` ⇐ `למסנן שבחרת` (כמו `ProjectsPage.jsx:605`, `LogisticsPage.jsx:98`). **🚫 לא מוסיפים נקודה לכותרות-הריק** — R14 | `FinancePage.test.jsx:470` |
| A4 | `src/modules/05_logistics/ChecklistDialog.jsx:538-560` + `CancelBanner` `:587-606` | **① פסקת-ההסבר** (`:541-551`, "מודול 8" ב-`:548`) ⇐ בסיס: `כל שינוי נשמר מיד.` · המילוי-האוטומטי ⇐ רמז חדש `checklist.autoSave`, עם "חישוב הרווחיות" במקום "מודול 8". **② `checklist-locked-note` (`:555-560`) הוא רכיב מרונדר, לא הערה** ⇐ **נשאר**, ומתקצר לעובדה: `הפרויקט בוטל — אי-אפשר לשנות כאן.` **③ `CancelBanner`** ⇐ העובדה + החריג (אפשר עדיין לסמן סחורה שהגיעה). **④ הבלוק מסומן "§3.7 מילה-במילה" (`:538`)** ⇐ כתיבה-חזרה לאפיון §3.7 של מודול 5 (UC42) | יחידה `ChecklistDialog.test.jsx:173-176`, `:722-723`, `:745` · **e2e `logistics.spec.js:63-64`, `:629-632`** |
| A5 | **מחלקה, לא מופע:** שמות-פרמטר גולמיים בהודעות "חסר פרמטר". `src/lib/quotes.js:545-555` (`missingPricingParamsMessage`, מוצג ב-`QuotesPage.jsx:272`) · `src/lib/quotes.js:574-576` · `src/lib/reportsParams.js:61-66` · `src/modules/03_quotes/quotePdf.jsx:311` | עוזר אחד `paramLabel(name)` שקורא את `label` מ-`src/lib/paramsRegistry.js` (SSOT; `אחוז_מעמ` ⇐ `'אחוז מע"מ'`, `:280-281`). כל ארבעת האתרים עוברים אליו. `quotePdf.jsx:311` ⇐ `'חסר אחוז מע"מ בהגדרות המערכת — בלעדיו אי-אפשר להפיק הצעה ללקוח.'` | `quotes.test.js:901-963` · `quotePdf.test.jsx:227` · **e2e `quote-document.spec.js:59,66`** |
| A6 | `src/modules/06_projects/ClosingTab.jsx:935` | `` `${Number(row.rateSnapshot ?? 0)} ₪` `` ⇐ `<Money amount={…} exact />`. 📏 **חובה `exact`:** ברירת-המחדל מעגלת, ו-7 שורות חיות הן שבר (42.50 ⇐ "43 ₪") | — |
| A7 | `src/modules/08_finance/ClosingWindowDialog.jsx` **`:811`, `:1081`, `:1116`, `:1883`** | כפתור `סגור ללא תשלום` ⇐ `סגרי ללא תשלום`, **בארבעת האתרים**. שם-הסטטוס בתגית ובטקסט **נשאר** `סגור ללא תשלום` (מילון §3ב). ⇐ **לעדכן את שורת-המילון** (`ui-copy-styleguide.md:137`, "אחיד (4)"): כפתור = פועל, תגית = שם | אין (testIds בלבד) |

### חבילה B · מבחן "לא נתקעת" — מצב 2, על המסך החי
**קודם כל, רשת-ביטחון** (📏 מבקר 1+2): `onboardingCopy.budget.test.js:34-50` בודק מפתחות **רק** ב-`src/modules/11_reports`, ורק `reports.X.Y`.
⇒ **מרחיבים אותו לכל `src/modules/**`, ולכל `<Hint id="…">`, לפני שכותבים רמז אחד.** אחרת מפתח שהוקלד שגוי ב-01–09 מרונדר כ-`null`, בשקט, והשער ירוק. **זה כלל "מה שנכשל פעמיים".**

**התוצר:** טבלה אחת, **בתוך הקובץ הזה, §13** (לא קובץ חדש, כדי לא להפעיל את אכיפה 0ד). עמודות: **מסך · שאלת השבוע-הראשון · איפה נתקעים · מפתח-רמז או "ממצא-מבנה" · צילום לפני · צילום אחרי**. כוללת את מסכי 01–09 **ואת 16 הדוחות**.

**כללי-רמז:**
- ≤220 תווים גלויים.
- צמוד לאלמנט.
- נפתח ב"למה" או ב"מה לעשות", לא בהגדרה.
- לא חוזר על מה שעל המסך.
- ציווי-נקבה.
- **אפס ספרות.** סף נכתב `הסף שנקבע בהגדרות` (`onboardingCopy.js:59-60`).
- **≤4 רמזים גלויים בגובה-מסך, כולל הקיימים.** ספור קודם.
- **היכן:** מודולים 01–09 ⇐ `src/lib/onboardingCopy.js`, תחת בלוק-ההערה `// מN ·` הקיים. דוחות ⇐ `onboardingCopy.m11.<tab>.js`, מפתח `reports.<slug>.<field>`.

**נקודת-פתיחה** (לא רשימה סגורה; הסוכנים משלימים):
| מסך | השאלה | הכרעה |
|---|---|---|
| לקוחות · רשימה | "מה זה לקוח רדום?" (`CustomersPage.jsx:850`) | רמז, בלי מספר |
| הרשאות | "למה עמודת המנכ"ל נעולה?" (רק `title`, `PermissionsMatrixPage.jsx:181`) | רמז: מונע נעילה-עצמית |
| מחירים | "שינוי מחיר משנה הצעות פתוחות?" | 📏 המחיר נשמר בשורת-ההצעה (`closing_unit_price` — `src/modules/03_quotes/api.js:158`, `src/lib/quotes.js:238,410,618`). **לפני רמז: לבדוק ב-`QuoteBuilderPage` אם עריכת הצעה פתוחה מחשבת מחיר מחדש.** רמז שעלול לשקר גרוע מאין-רמז |
| הגדרות · שיבוץ חכם (`09_settings/SmartMatchPane.jsx`, 0 רמזים) | "מה משתנה כשמכבים אמינות?" | רמז |
| הגדרות · עורך-תבניות | 🤖 מלכודת-המשתנים שבהערת-הכותרת | על המסך קודם |
| מסך הבית | "למה אירוע מבוטל עדיין ביומן?" | רמז. ⚠️ **`dashboard.partialSums` נשאר בענף-השגיאה בלבד.** הנוסח שלו (`onboardingCopy.js:70-73`) נכון רק כשהסכומים חלקיים; רמז קבוע היה משקר |
| הצעות · רשימה | "מה ההבדל בין 'פג בקרוב' ל'אירועים קרובים'?" | רמז ליד הצ'יפים |
| בונה-הצעה (`QuoteLineEditor.jsx`) | "למה אי-אפשר לשנות מחיר בשורה?" | אייקון-נעילה (lucide `Lock`, `size-4`) + רמז: "להנחה — השדה 'הנחה ידנית'" |
| שיבוץ חכם (`04_hostesses/SmartMatchPage.jsx`) | "למה היא ראשונה?" כשאמינות **דלוקה** | רמז `smartMatch.weightsWhy` |
| כספים | "למה פרויקט ששולם עדיין ב'ממתין לתשלום'?" (`FinancePage.jsx:225-231` — רק הערה) | רמז |
| לוגיסטיקה · צ'קליסט | "למה אין כפתור שמירה?" · שדה-כמות נעול רק ב-`title` | `checklist.autoSave` · נעילה גלויה |
| ביטול פרויקט (`06_projects/CancelProjectDialog.jsx`) | "מה קורה לכסף?" | רמז |
| 16 הדוחות | 1–3 שאלות לדוח, מול הרמזים שכבר קיימים | משלימים רק מה שחסר |

### חבילה C · מבנה
| # | מה | הראיה | איך |
|:-:|---|---|---|
| C1 | 🔴 **כרטיס-דיילת: `טרם נרשמו העדפות` מקודד לכולן** (`04_hostesses/HostessViewCard.jsx:351`) | 📏 1,990 העדפות על 157 דיילות. ההערה ב-`:347-349` ("עד שמודול 6 ייבנה") מיושנת | **① קריאה:** `customer_hostess_preference` לפי `hostess_id` (עמודות: `customer_id, hostess_id, preference, preference_reason`). **② שם-הלקוח — לא מ-`customers`:** מנהלת גיוס ושיבוץ **חסומה** על 'לקוחות', וה-embed מחזיר `null` בשקט (`04_hostesses/api.js:249-251`, תקדים מתועד) ⇒ שם מ-`projects.customer_name` של פרויקט כלשהו של אותו `customer_id`. **③ תוויות:** מפת-התוויות הקיימת (`closingDraft.js:306`) — `לא_לשלוח` לעולם לא מגיע גולמי. **④ מצב-טעינה תלת-ערכי:** שגיאה ⇐ "לא נטען — נסי שוב", לעולם לא "טרם נרשמו". **⑤ הרמז `hostess.clientPreferences` (`:352`) נשאר.** **⑥ אימות-חי בתפקיד `E2E_RECRUIT_*`**, לא רק מנכ"ל |
| C3 | לקוחות · מסנן "שביעות רצון" מושבת: `אין נתונים עדיין` (`02_customers/CustomersFilterSheet.jsx:132-146`) | 📏 הערת-הקוד: "חוזרת לפעולה במ8". מ8 נבנה. ישי (בהערת-קוד בלבד, 11/07): *"סינון חשוב"* | **ערכי-המסנן = תגיות §7.80:** מצוין (5) · טוב (4) · בינוני (3) · טעון בירור (1–2). **"טעון בירור" לא נכתב כ-`<3` ביד:** הוא נגזר מ-`needsSatisfactionAttention` (`src/lib/customers.js:50`), שקורא את הפרמטר `סף_שביעות_רצון`. **והצ'יפ הקיים `customers-preset-low-satisfaction` (`CustomersPage.jsx:881`) = קיצור לאותו מסנן:** **פרמטר-URL אחד**, והצ'יפ מציב אותו על "טעון בירור". שני מסננים שמתפצלים זה באג |
| ~~C2~~ | ~~ⓘ ל-`StatTile`~~ | **נדחה בביקורת:** השימוש היחיד המתוכנן (`FinancePage.jsx:394-400`, "לא כולל N דמי-ביטול") הוא **אזהרת-נכונות**, שכלל 2.7 מחייב להשאיר גלויה. היא נעולה ב-`FinancePage.test.jsx:616,690`, ששם-הבדיקה שלהם אומר "בגלוי". בלי צרכן, פרופ חדש = קוד-מת (knip) | — |

### חבילה D · AI גלוי — אושר ע"י ישי 23/09
**אייקון אחד לשניהם:** lucide `Sparkles` `size-4` (`src/CLAUDE.md` §4.3), לא אמוג'י. **המילה "AI" באותו נוסח.**

**D1 · ניתוח-ההערות (מ22).** קובץ: `AnalysisRunBar.jsx`.
- **`approvedState`** (`:195-203`) ⇐ שורה: `[Sparkles] סווג בעזרת AI · <תאריך>`.
  - מספר-הריצות ושם-המאשר עוברים ל-ⓘ, **ברכיב `TileDetails` שחולץ מ-`KpiTile.jsx:167-195` לקובץ משותף** — לא שכפול.
  - ⚠️ `approvedState` מחזיר `{text,sub,action,tone}` ואין בו שדה לפרטים ⇐ מוסיפים `details`.
- **כפתור `הרץ שוב`** במצב המאושר, רק ל-`canEdit` (`edit` על 'דו"חות', כמו השער בשרת, `index.ts:126,767`).
- **אחרי `noop`:** השורה `✨ סווג בעזרת AI · …` **נשארת**, ו-`אין הערות חדשות לסיווג.` מופיע **מתחתיה** כ-`sub`. היום `localState` (`:207`) מחליף את השורה עד רענון.
- **נעילות:**
  - `CustomersTab.test.jsx:843,845,846,855,872`.
  - הנוסח נעול גם בכרטיס ⑧22.4 (`:178`) ⇐ **כתיבה-חזרה לאפיון** (UC42).

**D2 · טיוטת מייל-מעקב בעזרת AI.**
- **איפה:** בחלון-המסמך של הצעה (`QuoteDocumentDialog.jsx`).
  - **למה שם, ולא בשורת-הדוח:** השורה ב-`ReportTable` היא דלת שלמה (`ReportTable.jsx:86-116`, הכרעה 19: *"השורה כולה לחיצה"*), ו-`ReportTable` משותף ל-16 הדוחות.
  - **חבילה 0 מובילה את השורה בדיוק לחלון הזה** ⇐ דוח ⇐ שורה ⇐ חלון ⇐ `נסחי מייל מעקב`: **2 לחיצות.**
  - ובונוס: עובד גם מ"פג בקרוב" ברשימת-ההצעות.
- **מתי מופיע:** הצעה `in_progress` (פתוחה) **או** `rejected` עם `rejection_reason = 'פג תוקף'`.
  - ‏📏 **אין סטטוס `sent`.** הסטטוסים במסד: `in_progress` · `approved` · `rejected` (24/09: ‏33 · 837 · 352, מהן 33 "פג תוקף").
  - אם ההצעה נשלחה, זה יודעים מיומן-המיילים, כמו ש-`QuotesPage` כבר בודק (`:449`). ⇐ בהצעה פתוחה שטרם נשלחה, הכפתור **מושבת** עם הנימוק `ההצעה עוד לא נשלחה ללקוח`. **למי:** `edit` על `'הצעות מחיר'` (שם-המודול, `20260723113500_module3_rls_quotes_and_catalog.sql:21`).
  - **למה `edit` ולא `view`:** כל קריאה צורכת מכסת-AI. אותו נימוק שבגללו `classify-feedback` דורש `edit` (`index.ts:766-767`).
- **החלון:**
  1. שלד בזמן טעינה.
  2. טקסט **ניתן-לעריכה** (נושא + גוף), ומעליו קבוע: `טיוטה — בדקי לפני שליחה.`
  3. כפתורים: `העתיקי` · `פתחי במייל`.
     - `פתחי במייל` = `mailto:` לאיש-הקשר הראשי: `customer_contacts.is_primary = true`, `email`.
     - 📏 **זה המקור היחיד.** עמודות-הקשר ב-`customers` נמחקו: מיגרציה `n2d_drop_customers_contact_columns`, ובמסד אין אף עמודת `*contact*` ב-`customers`.
     - `email` יכול להיות null ⇐ הכפתור מושבת, עם הנימוק `אין מייל לאיש הקשר הראשי`.
  4. **🚫 המערכת לעולם לא שולחת.**
- **הקליינט:** `draftFollowupEmail(quoteId)` ב-`src/modules/03_quotes/api.js` (מודול 3, כי החלון שלו), עם דפוס-ה-timeout של `src/api/email.js:22`.
- **השרת:** Edge Function חדשה `supabase/functions/draft-followup/index.ts`, **בתבנית `classify-feedback`**.
  - סדר-השער: סוד ⇐ זהות ⇐ הרשאה ⇐ גוף.
  - אותם `GEMINI_API_KEY` / `GEMINI_MODEL`, ואותו `verify_jwt` (כמו `classify-feedback`).
  - **בלי `_shared/`.**
- **חוזה:**
  - `POST {quote_id}` ⇐ `200 {draft:{subject, body}, to: email|null}`
  - `400` גוף שגוי · `401` · `403` · `404` הצעה לא קריאה (RLS, עם ה-JWT של הקוראת)
  - `429 {status:'quota'}` · `502` ספק.
- **🔒 פרטיות — שומרים על ההחלטה הקיימת** (`README:93-95`: המודל מקבל *"טקסט וציון בלבד… בלי שם לקוח, בלי כסף, בלי שם פרויקט"*):
  - **ל-Gemini נשלחים רק מצייני-מקום** — `{{איש_קשר}}` · `{{אירוע}}` · `{{תאריך}}` · `{{תוקף}}` — ועובדות לא-מזהות: ממתינה או פגה · כמה ימים מאז השליחה.
  - **השרת ממלא את מצייני-המקום אחרי התשובה.**
  - ⇒ שם, כסף ואירוע לא יוצאים מהמערכת.
- **נוסח-ההנחיה:** עברית · עסקי-מנומס · 4–6 שורות · **בלי להמציא הנחה או מחיר.** ממתינה ⇐ "לבדוק אם יש שאלות". פגה ⇐ "לבדוק אם עדיין רלוונטי ולחדש".
- **כשל:** `הניסוח נכשל — נסי שוב.` · מכסה: `הגעת למכסת ה-AI — נסי שוב מאוחר יותר.` **לעולם לא טיוטה ריקה.**
  - ⚠️ **המכסה לא ידועה.** "20 ביום" נמדד על `gemini-3.8-flash`, שננטש. הדגם היום הוא `gemini-3.5-flash-lite` (`index.ts:98`), ומכסתו "לא אומתה" (`README:168-183`). **הדלי משותף לשתי הפונקציות רק כשהן על אותו דגם.**
- **בדיקות:**
  - Vitest לחלון, עם `functions.invoke` מדומה.
  - `deno check`.
  - **צעד-CI חדש** ב-`.github/workflows/ci.yml`: כל פונקציה רשומה בנתיב מפורש (`:96-100`), **כצעד נפרד**. אין `supabase/config.toml`.
  - curl ל-401, ל-403-לפני-400 ול-200, **מתועד ב-README החדש** (כמו `classify-feedback/README.md:190-229`).
  - **e2e חייב `page.route('**/functions/v1/draft-followup')`** — לא צורכים מכסה בבדיקות.
- **פריסה:**
  - `deploy_edge_function` דרך MCP. **הפריסה חיה מיד**; אושרה במסגרת D2. **לדווח עליה בשורה נפרדת.**
  - **אין דרך לקרוא סודות.** ההוכחה ש-`GEMINI_API_KEY` מוגדר: הפונקציה **לא** מחזירה 500 `מפתח ה-AI לא הוגדר` (`classify-feedback/index.ts:729-730`).
- **אדוות:**
  - `docs/CODE_MAP.md:78` אומר שמודול 11 "קריאה-בלבד". צריך להוסיף שורת `supabase/functions` ולתקן.
  - אפיון ומדריך-מיקרו של מודול 3 (החלון שם).
  - **מעבר-האיכות בחמישה צעדים של `src/CLAUDE.md` §4** על החלון החדש.
  - `check:type-scale`: בלי `text-[Npx]`.

### חבילה F · משק-בית
- **`docs/specs/module_11_reports/data-set.md:1154,:1201`** — שורת-הערה: המספרים (4.41 · 4.25) קדמו לזריעה הממוקדת של 24/09. היום, ממוצע 2025 = **4.02 (n=222)**. ‏📏 מבקר 1 הריץ את השאילתה.
- **`STATUS.md`** — הבלוק "18/09/2026 03:1X–04:0X — סגן-לילה" (ענף `reportsfinance-round-helper`, ‏📏 מוזג: `git merge-base --is-ancestor 749072a1 origin/dev` = 0) **נמחק**. שורה ב-LOG אומרת שנמחק ולמה. ההיסטוריה ב-git.
- **`docs/PROJECT_MASTER.md`** — מצביע לתוכנית הזו (אכיפה 0ד). **הסגן כבר הוסיף אותו בקומיט-התוכנית.**
- **ClosingTab — פער-מיסוך קיים:** `rateSnapshot` הוא תעריף שעתי (`closingDraft.js:44`). הכלל בפרויקט: רק מי שיש לה `edit` רואה תעריף (`RepositoryTab.jsx:404`). ‏ClosingTab מציג אותו גם ב-`view` (`:704`). **אין חשיפה היום**, כי אף תפקיד חי אינו `view` על דיילות. ⇐ **לא מתקנים ולא פותחים שורת-חוב.** הסגן מדווח לישי.

## 6ה · 🧭 UCD, ניסוח, שני המצבים ונגישות — עוברים על **כל** פריט בחבילות 0–D
ישי 24/09 00:0X: *"שווה לחשוב מנקודת מבט של עקרונות הUCD ניסוחים טובים התאמה ל2 המצבים נגישות דברים נוספים שאתה מזהה וכו"*.
**איך משתמשים בטבלה:** לכל מסך שנגעת בו, עוברים על השורות. שורה שנכשלת = הפריט לא גמור.

**א. עקרונות ה-UCD** (`~/.claude/skills/human-walkthrough/references/eyes.md` §F)
| עיקרון | איך זה נראה כאן | הבדיקה |
|---|---|---|
| **מודל-מנטלי: מילים מהעבודה, לא מהמסד** | אין `אחוז_מעמ`, "מודול 8" או שם-שדה על המסך (A4, A5) | קוראים בקול. אם מנהלת הייתה שואלת "מה זה?" — כותבים מחדש |
| **עקביות — אותו רעיון, אותן מילים** | `מסנן`, לא `סינון` (A3) · כפתור = פועל, תגית = שם (A7) · אותו אייקון ואותה מילה ל-AI (D1, D2) · `נסי שוב` בכל כשל | שני מסכים ברצף: אותו קול? מילון §3 מעודכן? |
| **היררכיה** | משפט-פעולה אחד בראש, והמשימה הראשית בגובה-המסך הראשון | צילום 1536 ו-1024 |
| **חשיפה הדרגתית** | הבסיס (מצב 0) נקי · פירוט ב-ⓘ · הסבר ברמז (מצב 2) | **מבחן-המחיקה:** מצב 0 לבד משלים כל משימה |
| **זיהוי במקום זכירה** | רמז **צמוד** לאלמנט, לא בראש הדף · נעילה = אייקון גלוי + מילה, לא רק `title` ב-hover | אין מידע שנגיש רק בריחוף-עכבר (H4) |
| **מניעת-טעות והתאוששות** | חבילה 0: אין טופס-עריכה ריק ופעיל · D2: טיוטה בלבד, ו"פתחי במייל" מושבת **עם נימוק גלוי** כשאין מייל · כשל ⇐ מה לעשות, לא מה נשבר | לכל כשל: יש דרך קדימה במשפט אחד |
| **מצב-המערכת** | שלד-טעינה בחלון-הטיוטה · "מסווג…" בזמן ריצת-AI · `אין הערות חדשות לסיווג.` נשאר גלוי אחרי `noop` · מצב-טעינה תלת-ערכי (C1) | אין שקט: "אף אחד לא בדק" ≠ "הכל בסדר" |
| **למידות (מצב 2)** | טבלת §13 — כל שאלה ⇐ רמז במקום שבו נתקעים | כל שורה ב-§13 סגורה |
| **יעילות** | דוח ⇐ שורה ⇐ חלון-ההצעה ⇐ טיוטה | ≤2 לחיצות |

**ב. ניסוח — מבחני-קבלה לכל מחרוזת שנגעת בה**
- **שם כרטיס או כותרת:** מילים שמנהלת הייתה אומרת בטלפון. בלי ז'רגון.
- **משפט שפונה אליה:** ציווי-נקבה (`נסי`, `בחרי`). **משפט שמתאר עבודה שממתינה:** אינפיניטיב (מדריך §1).
- **מצב-ריק:** מה אין + הדרך קדימה. **ומבחין בין "אין נתונים" לבין "הסינון סינן הכל"** (R23).
- **שגיאה:** מה קרה + מה לעשות. בלי "שגוי", "אנא", "אופס" (R17, R18).
- **רמז:** "למה" או "מה לעשות", חם, גוף שני, ≤220, בלי ספרות.
- **טיוטת-המייל (D2):** פנייה עסקית-מנומסת, **לא** ציווי-נקבה — היא נשלחת ללקוח, לא למשתמשת.

**ג. שני המצבים**
- **מצב 0:** מבחן-המבט — המספר והמשמעות תוך שניות, בלי לקרוא משפט. ⚠️ **ומה שלעולם לא עובר לרמז:** אזהרה על פעולה בלתי-הפיכה או חיצונית (שליחה, מחיקה, ביטול), ואזהרת-נכונות (למשל "לא כולל N דמי-ביטול").
- **מצב 2:**
  - ≤4 רמזים בגובה-מסך, **כולל הקיימים**.
  - רמז לא חוזר על מה שכתוב על המסך.
  - מראה נבדל: אייקון + רקע רך, דרך `<Hint>` הקיים.
- **שני המצבים מצולמים לכל מסך ששונה.**

**ד. נגישות** (WCAG 2.2 AA — 💭 ידע כללי. הכלי בפרויקט: `e2e/accessibility.spec.js`, axe-core, ‏`critical`/`serious` נכשלים)
| נושא | מה נדרש כאן |
|---|---|
| **מקלדת** | כל פקד חדש נגיש ב-Tab, עם פוקוס גלוי. החלון החדש: פוקוס נכנס אליו, **נלכד בו**, `Esc` סוגר, והפוקוס **חוזר לכפתור שפתח** (Radix Dialog נותן את זה — לא לעקוף). **מבחן:** זרימת D2 כולה בלי עכבר — פתיחה, ניסוח, העתקה |
| **שמות נגישים** | כפתור-אייקון ⇐ `aria-label`. אייקון שליד מילה (`Sparkles`, `Lock`) ⇐ `aria-hidden="true"`. `<summary>` של ⓘ ⇐ שם כמו `פרטים נוספים`, לא רק "ⓘ". תיבת-הטיוטה ⇐ `<label>` |
| **הודעות אסינכרוניות** | "הטיוטה מוכנה" · "הניסוח נכשל" · תוצאת "הרץ שוב" ⇐ `aria-live="polite"` (דפוס קיים: סינון-צולב במ12, `reports.spec.js` "מכריזה באזור-החי") |
| **מושבת עם נימוק** | "פתחי במייל" בלי מייל ⇐ הנימוק **כטקסט גלוי** ליד הכפתור, לא רק `title` |
| **לא רק צבע** | נעילה, סטטוס ו"טעון בירור" — עם מילה או אייקון, לא רק גוון |
| **כיווניות** | מספר, מייל או מזהה בתוך משפט עברי ⇐ `isolateLtr` / `<Ltr>`. כתובת-המייל בחלון ⇐ `dir="ltr"` |
| **תנועה** | אנימציית-שלד ⇐ `motion-safe:` |
| **ניגודיות** | ⚠️ פתוח **בכוונה** בכותרת `accessibility.spec.js`: לבן על `bg-teal-600` (3.66) ו-`text-slate-400` (2.63). ‏💭 ייתכן שכבר תוקן: `conference-qa` מזכיר "החלפת טוקן-נגישות ⇐ 4.76:1". **לבדוק בסריקה. ואם עדיין פתוח — לא משנים צבע בלי ישי** (כלל-ברזל 8). מדווחים |
| **הרחבת הסריקה** | היום axe סורק רק את דפי-הסקירה (`accessibility.spec.js:376-381`). ⇐ **להוסיף:** רשימת-ההצעות עם חלון-המסמך פתוח · חלון-הטיוטה (עם `page.route` מדומה) · הצ'קליסט · כרטיס-דיילת · פס ה-AI במ22 |

**ה. יעדים מדידים — מדווחים בסוף סשן 2, מהצילום ומהריצה**
- דוח ⇐ טיוטה: **≤2 לחיצות** · הטיוטה מוכנה תוך **≤10 שניות** (מדידה אחת חיה) · **0** שליחות אוטומטיות.
- axe על המסכים ששונו: **0** `critical`/`serious` חדשים.
- זרימת D2 **במקלדת בלבד**: עוברת (e2e).
- כל רמז ≤220 · ≤4 בגובה-מסך · **0** מפתחות-רמז שבורים (הבדיקה המורחבת).
- §13: **כל** שאלה סגורה — ברמז, או כממצא-מבנה מתועד.

## 7 · 🤖 שני סשנים, מוגדרים מראש — ככה מצמצמים
**בכנות:** ‏📏 סריקה אחת של 4 סוכנים עלתה 1.37M. חבילה B בלבד = 3 סוכני-צילום (~250K כל אחד, `01_estimation_log.md` §1). ועוד ~20–25 דקות מסמכים לכל מודול, ×10 מודולים. ⇐ **סשן אחד לא ריאלי. מתכננים שניים, עם נקודת-מסירה אחת וקבועה.** ‏💭

### סשן 1 — חבילות 0, A, C, B
| שלב | מה | מי |
|:-:|---|---|
| ① | מבחן-קבלה (§10) · `npm ci` אם צריך · **קו-בסיס:** `npm run gate` ו-`npm run test:e2e`. **רושמים ב-§12 את קוד-היציאה ואת רשימת-הדילוגים בשמם** (דילוגים קיימים, למשל `e2e/project-closing.spec.js:25,98,…`). ⇐ **"גמור" = אין דילוג חדש ואין כשל חדש לעומת הקו-הזה**, לא "0 skipped". אדום על `origin/dev` = קו-בסיס; מדווחים וממשיכים | הסשן |
| ② | **הרחבת בדיקת-המפתחות** (חבילה B, "רשת-ביטחון") ⇐ קומיט | הסשן |
| ③ | **3 סוכני Sonnet, ברקע, בונים את §13, "לפני" — קריאה + צילום בלבד:** (א) מודולים 01, 02, 07, 09 · (ב) 03, 04, 05, 06, 08 · (ג) 16 הדוחות. **מנכ"ל בלבד**; הבדלי-תפקיד מחוץ להיקף, חוץ מ-C1. | סוכנים |
| ④ | **בזמן שהם רצים:** חבילה 0 ⇐ קומיט · A ⇐ קומיט · C1 ⇐ קומיט · C3 ⇐ קומיט | הסשן |
| ⑤ | רמזים מ-§13 ⇐ `check:report-copy` ⇐ קומיט | הסשן |
| ⑥ | צילומי "אחרי" לכל שורה ב-§13 (אותו תסריט, `after/`) · **מעבר §6ה (א–ד) על כל מסך שנגעת בו** · `gate` · e2e · מדריכי-מיקרו · **LOG ו-STATUS אחרונים** · §12 · push. **⏸ נקודת-המסירה לסשן 2** | הסשן |

**פרטי שלב ③:**
- **הקוד לא זז עד שהסוכנים דיווחו** — חבילה 0 ו-A מתחילות **רק אחרי** שצילומי ה"לפני" הושלמו. HMR באמצע צילום מצלם קוד חצי-ערוך (`playwright.e2e.config.js:6-19`).
- **איך מצלמים בלי לגעת בריפו:**
  - `npx vite build --outDir <scratchpad>/shots-dist` **פעם אחת** (ה-`dist` הרגיל נדרס ע"י השער).
  - כל סוכן: `npx vite preview --outDir <scratchpad>/shots-dist --port 523N --strictPort` (5231 / 5232 / 5233).
  - ספק ותצורת-Playwright **חד-פעמיים**, ב-scratchpad של הסשן. `testDir` מצביע לשם, ו-cwd = התיקייה, כדי ש-`.env.local` ייטען.
  - **🚫 לעולם לא ב-`e2e/`** — ה-lint של השער היה תופס אותם.
  - כפיית-מצב ביירוט: העתק את `forceOnboardingMode` מ-`e2e/report-density.spec.js:64-72`.
- **כל סוכן מקבל:**
  - **רשימת-נתיבים מפורשת** — הסשן מפיק אותה מ-`src/App.jsx`.
  - את §4 כאן (מצב 2, בציטוט), את כללי-הרמז, ואת תבנית-השורה של §13.
- **הצילומים:** `C:\Users\ishay\Reg-In-evidence\system-polish-2026-09-24\{before,after}\{mode0,mode2}\` (תקדים: `Reg-In-evidence\m11-2026-09-16\`, `STATUS.md:109`).

### סשן 2 — חבילה D, ביקורת, מסירה לסגן
| שלב | מה |
|:-:|---|
| ⑥ב | **חבילה 0ב — "חזרה למקור"** (§6): `returnTo` מכל דלת-דוח ומפריטי "דורש תשומת לב" ⇐ סגירה/חזרה ביעד ⇐ e2e ⇐ קומיט |
| ⑦ | ✅ **בוצע בסשן 1** (`c61f68f8`) — חילוץ `TileDetails` לקובץ משותף ⇐ D1 ⇐ קומיט |
| ⑧ | D2: פונקציה + README + צעד-CI + חלון + בדיקות ⇐ פריסה ⇐ curl ⇐ קומיט |
| ⑨ | **מבקרי-ניסוח לפי מצב** (הסקיל §F): **אחד שרואה רק מצב 0, ואחד שרואה רק מצב 2** — Opus, בלי יומן-השינויים ובלי הנימוקים. ‏💭 **אחד לכל מצב ולא שניים (רצפת-הסקיל):** בדיקת-הצפיפות ו-§13 כבר מודדים את רוב מה שמבקר שני היה תופס. **אם הראשון מוצא יותר מ-3 ❌ — שולחים שני.** טבלת-הפסיקה מגיעה לישי **לפני** הצילומים |
| ⑩ | תיקונים · **הרחבת סריקת-ה-axe (§6ה-ד) ומבחן-מקלדת ל-D2** · `gate` · e2e מול הקו-בסיס · אדוות (CODE_MAP, אפיונים, מדריכים) · LOG+STATUS אחרונים · §12 · push · **היעדים של §6ה-ה בדוח** |
| ⑪ | **לישי — דף אחד (Artifact):** טבלת-הפסיקה ⇐ זוגות לפני/אחרי בשני המצבים (כרטיס-דיילת עם העדפות אמיתיות · שורת-ה-AI · טיוטת-מייל אמיתית · חלון-ההצעה מדוח ה1) |

**כל סוכן:** `model` מפורש · שורת-עלות לישי לפני השיגור · ובסיום: `שומש כפי שהוא` / `נדרש תיקון` / `נזרק` (`~/.claude/references/estimation-and-agents.md` §2).
**"גמור" לכל חבילה:**
- הבדיקות שלה זזו באותו קומיט.
- `npm run gate` = 0.
- צילום לפני/אחרי בשני המצבים.
- שורת `↳` במדריך-המיקרו.
- קומיט בפאת'ספק מפורש (`git commit -- <נתיבים>`).

## 8 · 🪤 מלכודות — נבנו מסריקת המנגנונים ומשני המבקרים
| # | המלכודת | הראיה | מה לעשות |
|:-:|---|---|---|
| 1 | **פורט תפוס = בדיקה על קוד של מישהו אחר.** `playwright.config.js:21` עם `reuseExistingServer: true` | 📏 קרה לסגן 23/09 על 5199 | פורט ייחודי · `--strictPort` · `E2E_BASE_URL`. **לפני ריצה:** `Get-NetTCPConnection -LocalPort N` ⇐ הבעלים הוא התיקייה שלך? ⚠️ **`npm run test:e2e` קשיח על 4173** (`playwright.e2e.config.js:25-30`) ו-`E2E_BASE_URL` לא חל עליו |
| 2 | Stop-hook: קוד-מודול ששונה ⇒ מדריך-מיקרו · תוכנית ב-`docs/plans/` ⇒ מצביע ב-`PROJECT_MASTER.md` · LOG+STATUS mtime אחרון | `check-docs-updated.sh:7-15,154-190` | **10 מודולים** (01–09 + 11) ⇒ 10 מדריכים. **הרשימה של ה-hook היא הקובעת, לא המספר כאן** |
| 3 | `block-shell-dialect-mixup.sh` חוסם `gate \| tail` | — | `npm run gate > log 2>&1; echo "exit=$?"` |
| 4 | `protect-frozen-files.sh` חוסם עריכת-Bash לפי תוכן | 📏 נחסם לסגן על `rm` ב-scratchpad | `Edit`/`Write` |
| 5 | מחרוזות נעולות ב-**יחידה וב-e2e** | חבילה A מונה כל אחת | לפני כל שינוי: `grep -rn "<מחרוזת>" src e2e` |
| 6 | רמז עם מפתח שגוי = `null` שקט | `onboardingCopy.budget.test.js:34-50` מכסה רק מ11 | חבילה B, "רשת-ביטחון" — **ראשונה** |
| 7 | `knip` + `jscpd` (3%, `src` בלבד) | `package.json:20`, `.jscpd.json` | ⓘ — חילוץ, לא העתקה. בלי ייצוא-יתום |
| 8 | `check:type-scale` בשער | — | בלי `text-[Npx]` |
| 9 | RTL: `ms-`/`ps-`/`start-` | `CLAUDE.md` §3 | `mr-`/`pr-`/`right-` |
| 10 | `RepositoryTab.jsx` מוקפא עד ~07/10 | `PROJECT_MASTER.md:906` | לא נוגעים |
| 11 | שגיאה ⇐ `[]` = שקר | `CLAUDE.md` §4.3 | C1 |
| 12 | מכסת-AI לא ידועה ומשותפת | `README:168-183` | בבדיקות: לדמות. קריאה אמיתית אחת לצילום |
| 13 | מסד אחד = גם ייצור | `CLAUDE.md` §2.2 | **אין מיגרציה בחבילות האלה.** אם מתברר שנדרשת ⇐ **עצור ובקש "מאשר" מישי** (כלל-ברזל 4) |
| 14 | `git merge-base --is-ancestor HEAD origin/dev` מצליח גם על ענף טרי | `CLAUDE.md` כלל 10 | המבחן: `git log origin/dev..HEAD` |
| 15 | **טופס-עריכה ריק על הצעה שאינה `in_progress`** | חבילה 0 | e2e שבודק URL בלבד אינו בדיקה. **בודקים תוכן** |

## 9 · 🚫 נשקל ונדחה — אל תבנה מחדש
- **ריפקטורינג מלא** — הסבב של 07–09/09 כבר עשה את רובו.
- **חיזוי** — על נתונים סינתטיים המודל לומד את המחולל.
- **משתמש-דמות לכנס** · **חלוקת `owner_name`** — ישי ויתר (§4).
- **C2 (ⓘ ל-StatTile)** — האזהרה בכספים חייבת להישאר גלויה (§6C).
- **`dashboard.partialSums` כרמז קבוע** — היה משקר.
- **נקודה בסוף כותרות-הריק** — R14.
- **כפתור D2 בתוך שורת-הדוח** — הכרעה 19 + רכיב משותף ל-16.
- **ממצא-סורק "בחלון-השיווק אין שורת-נמענים"** — שגוי (`MarketingPanel.jsx:7,47,114`).
- **D2 ששולח בעצמו** · **D2 ששולח ל-Gemini שם, כסף או אירוע** — ההחלטה ב-`README:93-95`.

## 10 · ✅ מבחן-קבלה — ההודעה הראשונה שלך, לפני כל פעולה
1. **מכני:** פלט שלוש פקודות §2 · `npm ls --depth=0` ⇒ exit · `grep -oE '^[A-Z0-9_]+=' .env.local` — **שמות בלבד**.
2. **צירוף-מקורות — אי-אפשר לענות מהקובץ הזה ולא בגריפ אחד:**
   - א. באיזה רכיב מרונדרות שורות ה1? מה הופך שורה ללחיצה? ולמה אי-אפשר להוסיף בה כפתור בלי לשבור הכרעה?
   - ב. מה בדיוק מרונדר היום ב-`QuoteBuilderPage` כש-`loadError` קבוע? מאיזה ענף ב-JSX מגיע הטופס הריק?
   - ג. איך `needsSatisfactionAttention` מקבל את הסף? ואיפה הצ'יפ "טעון בירור" שומר את מצבו?
3. **הצהרת-קריאה:** טבלה לכל פריט ב-§1 — נקרא במלואו / חלקית (טווח) / לא נפתח.

## 11 · 🔚 אחרי התוכנית
- **המיזוג:** ל-`dev`, ואחר כך ל-`main` — **רק במילה מפורשת של ישי, בזמן-אמת** (כלל-ברזל 10). הסגן מבצע ומאמת פריסה **וחי**.
- **מסלול-הכנס (ש3), אחרי הייצור:** מסך-הבית ⇐ הצעה ⇐ כרטיס-פרויקט ⇐ שיבוץ חכם ⇐ כספים ⇐ דוחות (ה1–ה3) ⇐ **חלון-ההצעה + טיוטת-AI** ⇐ ייצוא לאקסל. צילום לכל תחנה, ורישום ב-`~/.claude/plans/conference-qa-2026-10-15.md`. **הסגן.**
- **⏰ לוח:** הקפאה 08/10 · ‏14/10 `scripts/conference-refresh/refresh-before-conference.sql` (חובה) · ‏15/10 הכנס · ‏16/10 ארבעה עדכוני dependabot.

## 12 · 📍 איפה אנחנו — מתעדכן במקום, בכל נקודת-עצירה
| תאריך | מצב | SHA | הבא |
|---|---|---|---|
| 24/09/2026 | תוכנית v2, אחרי שני מבקרים. דוחות-ההחלטה בייצור (`4d718709`) | `git log -1` | סשן 1, שלב ① |
| 24/09/2026 00:2X | 🗣️ **אושר ע"י ישי בצ'אט** (*"מבחינתי מעולה תמשיך עד מה שתוכל בלי לעצור"*) — סשן 1, ואם נשאר מקום ממשיכים ל-D1 ואחריו D2. **קו-בסיס על `953b1cdc`** (📏 ריצה בפועל): `gate` נעצר ב-`test:run` ⇐ 3 כשלים, **שלושתם `Test timed out in 5000ms` תחת עומס** (3 סוכנים ודפדפנים רצו במקביל): `CustomerDetailsPage.export.test.jsx` ×1 · `RepositoryTab.pagination.test.jsx` ×2 — **בריצה בודדת 11/11 עוברים** ⇐ עומס, לא באג. 3,221 אחרים ירוקים. lint: אזהרה קיימת אחת (`UsersManagementPage.jsx:76`, exhaustive-deps). dup · deadcode · audit · bidi · context · docs-structure · iron-rules · declared-counts · type-scale = 0. **e2e: ר' השורה הבאה.** | `953b1cdc` | שלב ② |
| 24/09/2026 00:5X | **קו-בסיס e2e על `953b1cdc`** (📏 `npm run test:e2e`, רץ **במקביל** ל-3 סוכני-צילום): ‏188 עברו · **5 נכשלו** · 7 דולגו. הכשלים: `customer-page.spec.js:130` · `dashboard.spec.js:82` · `finance.spec.js:141` · `prices.spec.js:218` · `projects.spec.js:235` — **טעון בדיקה** אם עומס או אמת; מריצים שוב בשלב ⑥ לפני השוואה. | `953b1cdc` | — |
| 24/09/2026 02:4X | ⏸ **נקודת-המסירה של סשן 1 — ‏0 · A · C1 · C3 · B · F · ו-D1 (מעבר לתוכנית, בבקשת ישי "תמשיך עד מה שתוכל")**. **קומיטים:** ② `50c9833f` · 0 `9a2dbbfd` · A `2349ae24` · C1 `d8dd7ad2`+`75905e0e` · C3 `bf58cf1b` · B `9c21b26d` · D1 `c61f68f8` · e2e `3b37b3d7`. **אימות (📏 ריצות בפועל):** `gate` = **0** (קו-הבסיס: 3 timeouts) · vitest 3,240+ ירוק · `check:report-copy` 0 · e2e **187 עברו · 6 נכשלו · 7 דולגו**: **5 מהכשלים זהים בשמם לקו-הבסיס** (נכשלו גם על `953b1cdc`, לפני כל שינוי) · הכשל השישי (`load-failure-guards:165`) נגרם מחבילה 0 — החלון נפתח מחדש מ-`?view=` אחרי רענון — **תוקן בבדיקה**, ריצה-חוזרת 5/5 · **הדילוגים זהים בשמם** (7). **אימות-חי:** דלת-ה1 ⇐ חלון "הצעת מחיר 2317" · `/quotes/2317/edit` ⇐ הודעה + "פתחי את מסמך ההצעה", בלי טופס · כרטיס-דיילת #438 עם העדפות אמיתיות **כמנהלת גיוס וכמנכ"ל** · פס ה-AI (✨ + ⓘ + "הריצי שוב"). ≤4 רמזים בגובה-מסך בכל המסכים ששונו, 0 רמזים במצב 0. צילומים: `Reg-In-evidence\system-polish-2026-09-24\after\{mode0,mode2,recruit}\`. **שני דברים שנמצאו תוך-כדי:** (א) סורק A הציע רמז מחירון שהיה **משקר** (ר' §13) · (ב) בצילום של D1 — ⓘ של אריח הופיע **מעל** חלון-הפירוט של הפס; תוקן (`z-20`) וצולם שוב. **מה לא נעשה:** D2 · 0ב · הרחבת סריקת-ה-axe (§6ה-ד) · מבקרי-הניסוח לפי מצב (⑨) · ניגודיות לא נבדקה בסריקה. **ה-5 הכשלים הישנים** — הוצעו כמשימה נפרדת (צ'יפ בסשן). | `3b37b3d7` | **סשן 2: ⑥ב (0ב) ⇐ ⑧ (D2) ⇐ ⑨ ⇐ ⑩ ⇐ ⑪** |
| 24/09/2026 בוקר | **הסגן הוסיף חבילה 0ב — "חזרה למקור"** (הכרעת-ישי 24/09) ⇐ §6 ו-§7 (שלב ⑥ב, **סשן 2**). **לא נבנתה בסשן 1.** בינתיים: ② `50c9833f` · 0 `9a2dbbfd` · A `2349ae24` | `2349ae24` | C1 |

## 13 · 🧾 טבלת "לא נתקעת" — ממולאת בשלב ③ ומעודכנת בשלב ⑥
> **מקור:** 3 סוכני Sonnet, 24/09/2026 00:3X–01:1X, מנכ"ל, 1536, בנייה קפואה של `953b1cdc` (🤖 — **כל טענה שנכנסה לרמז אומתה בקוד ע"י הסשן**, והעוגן כתוב ליד המפתח ב-`onboardingCopy.js`). הדוחות המלאים של הסוכנים: 65 שורות (A 15 · B 16 · C 34); כאן — כל שאלה, מקובצת. **צילומים:** `C:\Users\ishay\Reg-In-evidence\system-polish-2026-09-24\{before,after}\{mode0,mode2}\` (קידומת `a-`/`b-`/`c-` = הסוכן).
> **שימוש-בסוכנים:** A `שומש כפי שהוא` · B `שומש כפי שהוא` · C `שומש כפי שהוא` — **ותיקון אחד מהותי:** הרמז שסוכן A הציע למחירון (*"ישפיע רק על הצעות חדשות"*) **היה משקר** — `QuoteLineEditor.jsx` `repriceLine` מתמחר מחדש שורה שכמותה או מוצרה שונו בהצעה פתוחה. הנוסח שנכתב אומר זאת.

| מסך | שאלת השבוע-הראשון | איפה נתקעים | רמז / ממצא-מבנה | לפני | אחרי |
|---|---|---|---|---|---|
| מסך הבית | "למה אירוע מבוטל עדיין ביומן?" | משבצת-היומן | נענה ע"י `dashboard.attention` (קיים) | `a-dashboard` | — |
| מסך הבית | "למה הסכום נמוך ממה שציפיתי?" | אריחי-הסכום | **ממצא-מבנה** (קיים, מדריך-הסגנון §4 דוגמה 4): אין סימון אילו פרויקטים לא נספרו. `dashboard.partialSums` נשאר בענף-השגיאה **בכוונה** (§6ב כאן) | `a-dashboard` | — |
| הרשאות | "למה עמודת המנכ"ל נעולה?" | עיגולי-העמודה (רק `title`) | 🆕 `permissions.ceoLocked` | `a-permissions` | ⑥ |
| מחירון · דיאלוג-מוצר | "שינוי מחיר משנה הצעות פתוחות?" | שדה "מחיר בסיס" | 🆕 `prices.openQuotes` — **נוסח מתוקן** (ר' למעלה) | `a-prices-product-dialog` | ⑥ |
| מחירון | "מה זה 'מדרגות מחיר'?" · "איך נבחרת מדרגה?" | כפתור-המדרגות · הדיאלוג | נענה ע"י `prices.tiers` · `prices.tierRule` (קיימים) | `a-prices*` | — |
| לקוחות · רשימה | "מה זה לקוח רדום?" | צ'יפ "רדומים" | 🆕 `customers.dormantWhy` — מוצג כשהצ'יפ דלוק (אז הרשימה מסוננת לפיו) | `a-customers-list` | ⑥ |
| לקוחות · פאנל-סינון | "למה 'שביעות רצון' מושבת?" | שדה בפאנל | **תוקן במבנה — חבילה C3** (שדה פעיל, מסנן אחד עם הצ'יפ) | `a-customers-filter-sheet` | ⑥ |
| כרטיס-לקוח | "מה מדליק 'רדום'?" באריח "אירוע אחרון" | תגית "רדום" | **ממצא-מבנה, קטן:** אותה הגדרה בלי רמז בכרטיס. לא נוסף (אותו מפתח בשני מסכים = אותו נוסח — אפשרי בסשן 2) | `a-customers-details` | — |
| לקוחות · שליחת חומר שיווקי | "למי זה יגיע?" | רשימת-הנמענים | 🤖 **טעון בדיקה** — הסוכן לא קרא את לוגיקת-ההסכמה; לא נכתב רמז | `a-customers-marketing` | — |
| הגדרות · תמחור ותזמון | "שינוי כאן יזיז הצעות שאושרו?" | משפט-הקבוצה | נענה ע"י `params.pricingTimingGroup` (קיים) | `a-params-pricing-timing` | — |
| הגדרות · שיבוץ חכם | "מה משתנה כשמכבים אמינות?" | המתג | 🆕 `settingsSmartMatch.reliabilityOff` | `a-params-smart-match` | ⑥ |
| הגדרות · עורך-תבניות | "לאן נכנס משתנה כשלוחצים עליו?" | שבבי-המשתנים | 🆕 `templates.chipInsertPoint` (רק כשהתבנית ניתנת לעריכה) | `a-params-templates` | ⑥ |
| ההגדרות שלי | "השינוי כאן משפיע על כל המשרד?" | הפאנלים | 🤖 **טעון בדיקה** — לא נכתב רמז | `a-my-settings` | — |
| פרופיל | "אם אכבה הסברים, זה חוזר?" | המתג | נענה ע"י `onboarding.self` (קיים) | `a-profile` | — |
| הצעות · רשימה | "מה ההבדל בין 'פג בקרוב' ל'אירועים קרובים'?" | שני הצ'יפים | 🆕 `quotes.expiringVsEventSoon` | `b-quotes-list` | ⑥ |
| בונה-הצעה | "למה אי-אפשר לשנות מחיר בשורה?" | תא-המחיר (טקסט, בלי שום סימן) | 🆕 **מנעול גלוי** (lucide `Lock`, שם-נגיש "מחיר מהמחירון") + `quoteBuilder.priceLocked` | `b-quotes-new` · `b-quotes-edit` | ⑥ |
| דיילות · מבט-על | "למה רחוק מעל קרוב?" | סדר-השורות | נענה ע"י `hostesses.overviewSort` (קיים) | `b-hostesses-overview` | — |
| שיבוץ חכם | "למה היא ראשונה?" (אמינות דלוקה) | ראש רשימת-המועמדות | 🆕 `smartMatch.weightsWhy` — **המפתח לא היה קיים** (התוכנית הניחה שכן); רק כשהאמינות דלוקה | `b-hostesses-smartmatch` | ⑥ |
| כרטיס-דיילת | "למה תמיד 'טרם נרשמו העדפות'?" | סעיף ההעדפות | **תוקן במבנה — חבילה C1** (העדפות אמיתיות) | `b-hostesses-card` | ⑥ |
| לוגיסטיקה · צ'קליסט | "למה אין כפתור שמירה?" | הדיאלוג | בסיס: `כל שינוי נשמר מיד.` + 🆕 `checklist.autoSave` (חבילה A4) | `b-logistics-checklist` | ⑥ |
| לוגיסטיקה · צ'קליסט | "למה אי-אפשר להקליד כמות?" | שדה-כמות מושבת (רק `title`) | 🆕 **מנעול גלוי** + `checklist.qtyLocked` | `b-logistics-checklist` | ⑥ |
| פרויקטים · רשימה / לוגיסטיקה | סדר · סטטוסים · אורחים | — | נענה ע"י `projects.sort` · `project.logisticsStatuses` · `project.guestsEstimate` (קיימים) | `b-projects-*` | — |
| כרטיס-פרויקט · צוות | "למה הסטטוס בהיסטוריה שונה?" | טבלת-ההיסטוריה | 🤖 `team.rawStatus` קיים ולא הופיע בצילום — **טעון בדיקה** (כנראה תלוי-נתונים) | `b-projects-card-team` | — |
| כרטיס-פרויקט · סגירה | "זה התעריף הנוכחי או שהוקפא?" | התעריף לשעה | **ממצא-מבנה:** אין "הוקפא" על המסך. התצוגה תוקנה ל-`Money exact` (A6); ההסבר — לסשן 2 אם יש מקום בגובה-המסך (כבר 3 רמזים) | `b-projects-card-closing` | ⑥ |
| ביטול פרויקט | "מה קורה לכסף?" | שלוש הכרטיסיות | **נענה על המסך עצמו** (`moneyLine` בכל כרטיסייה) — רמז היה חוזר על המסך | `b-projects-cancel` | — |
| כספים | "למה תיק ששולם עדיין ב'ממתין לתשלום'?" | הלשונית | 🆕 `finance.paidWhileAwaiting` (בלשונית הזו בלבד) | `b-finance-list` | ⑥ |
| כספים · חלון-סגירה · דוח-שכר | "הסכום סופי?" · "איך מחושבת שורת-שעות?" | — | 🤖 `closing.*` ו-`salary.formula` קיימים ולא הופיעו בצילום — **טעון בדיקה** (שורה/המתנה) | `b-finance-closing` · `b-finance-salary` | — |
| 16 הדוחות | 27 שאלות | — | **כולן נענות ברמזים הקיימים** (4 דפים כבר בתקרת 4 רמזים) — פירוט בדוח סוכן C | `c-*` | — |
| דוח ה3 · איכות אירועים | "אירוע שכבר עבר — מה עושים איתו?" | הטבלה | **ממצא-מבנה:** אין הבחנה עבר/עתיד בטבלה (טעון בדיקה מול `StaffingSurface`) | `c-staffing` | — |
| דוח מ12 · צריכת ציוד | "כמה באמת להזמין?" | "כמות להזמנה" | **ממצא-מבנה:** הרמז אומר "להפחית מה שכבר הוזמן", ואין על המסך "כבר הוזמן" | `c-equipment` | — |
| דוח מ17 · הוגנות | "מאיפה מתחילים בשתי טבלאות ענק?" | טבלת-ההתפלגות העליונה | **ממצא-מבנה:** אין שורה שמסבירה אותה (לא נכתב רמז — העובדות לא אומתו) | `c-fairness` | — |
| דוח מ22 · ניתוח הערות | "איפה כפתור 'נתח הערות'?" · "למה 426 מתוך 552?" | ראש הדף | **חבילה D1 (סשן 2)** — כפתור `הרץ שוב` · הפער 552/426 **טעון בדיקה** | `c-notes` | — |

</div>
