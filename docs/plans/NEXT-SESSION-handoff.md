<div dir="rtl">

# מסירה לסשן הבא — נכתב 05/08/2026 09:25

> **למי זה נכתב:** לסשן קלוד חדש, חסר-זיכרון, שממשיך את העבודה של ישי אחרי לילה ארוך.
> **הקובץ הזה הוא נקודת-ההתחלה שלך.** אל תסמוך על תמליל, על נרטיב, או על מה שישי זוכר —
> **מדוד מהדיסק, ואז המשך.**
>
> ⚠️ **הכלל היחיד שאסור להפר:** ‏**ישי אינו מתכנת ואינו מריץ פקודות.** הוא מדביק פרומפטים
> ומקבל דוחות. פלט שהוא לא יכול לבקר, או שאלה טכנית שהוא לא יכול לענות עליה — נכשלו.

---

## 0. צעד ראשון שלך — לפני כל דבר אחר

```
git status --short --branch
git log --oneline -5
```

**המצב הצפוי (נמדד 05/08 09:25):** ענף `ishay/post-m3-followups`, מסונכרן מול origin,
עץ נקי פרט לשתי תיקיות `??` — `Nate's Frontend Design` ו-`Skill Builder`.
**שתיהן של ישי, במכוון לא-מנוטרות. אל תקמט ואל תנקה אותן.**

**כל חריגה מזה ⇒ עצור, דווח לישי, אל תמשיך.**

---

## 1. איפה הדברים עומדים — עובדות מדודות

| | |
|---|---|
| **מודול 3** | ✅ **מוזג ל-`dev`** (PR #10, merge commit `792456f`, 05/08 05:03). אודיט-סגירה [YES] · 21/21 תיבות DoD · ישי חתם typed-echo |
| **הענף של מודול 3** | **מת ונמחק** מקומית ומהשרת. 🔴 **לעולם אל תערום עליו** (כלל ברזל 10) |
| **מודול 4** | **טרם נפתח.** `docs/micro_guides/` מכיל 1, 2, 3 בלבד |
| **work-manager** | **עדיין קיים ופעיל.** ההוצאה טרם רצה |
| **הענף הנוכחי** | `ishay/post-m3-followups` — עבודת תשתית, לא מודול |

**נוצר בלילה האחרון ומקומט:** ‏`.claude/skills/skill-scan/` · `.claude/skills/_shared/failure-modes.md` ·
שתי שורות `🚧 מ4` ב-`PROJECT_MASTER §6` · תיקוני `CLAUDE.md` · סעיף בדיקת-סקילים ב-`docs/toolbox.md`.

---

## 2. 🔴 הדבר החשוב ביותר שאתה צריך לדעת

**התוכנית של ישי — `C:\Users\ishay\עבודה עם קלוד\regin_workflow_short_updated.md` (1,839 שורות) —
נכתבה על-ידי מודל שלא הייתה לו גישה לקבצים של הפרויקט.**

⇒ **כל טענה עובדתית בה על הריפו היא הסקה, לא מדידה.** ‏והיא מאשרת תשעה שלבים של שינויים
בלתי-הפיכים.

**דוגמה שכבר אוששה (05/08 09:1X):** התוכנית מורה **בחמישה שלבים** *"הסר תלות ב-`_shared/discipline.md`"*.
**נמדד:** בקובץ ההוא יש **אזכור אחד** של המילה manager, והוא *"Ishay is the **product manager**"* — כלומר ישי.
**הקובץ מכיל את טבלת-האימות** ("איזו טענה מאומתת איפה") **ו-8 סקילים מצביעים אליו.**
⇒ **ההוראה הייתה מנתקת שישה סקילים מהמנגנון שהכי הרבה פעמים תפס טעויות.**

🔴 **המסקנה המעשית: אל תריץ אף שלב מהתוכנית לפני שהטענות שלו אומתו מול הריפו.**

---

## 3. סדר העבודה — עשה לפי הסדר הזה

### ⏳ שלב 0 — לאסוף שלושה דוחות שכבר רצים *(ייתכן שכבר חזרו)*

שלושה סוכני-ביקורת הופעלו ב-05/08 ~09:00 ולא נאספו:

| מה | מה הוא מחזיר |
|---|---|
| **שחזור ביקורת פרומפט-ההוצאה** | ‏5 טענות שגויות + 13 משטחים חסרים בפרומפט של שלב 1 |
| **אימות עובדתי של התוכנית** | אילו טענות ב-1,839 השורות שגויות מול הריפו |
| **ביקורת ישימות של התוכנית** | איפה מבצע ייתקע/ינחש · הצעות שיפור |

**אם ישי לא מסר לך אותם — שאל אותו.** ‏הם התמליל של עבודה שכבר בוצעה; אל תריץ אותה מחדש.

🔴 **שני מבקרים שהופסקו ולא ניתנים לשחזור — הרץ אותם מחדש. זו המשימה הראשונה שלך.**
שני סוכנים בהקשר טרי, במקביל, **קריאה בלבד**, על `regin_workflow_short_updated.md`:

| הסוכן | השאלה היחידה שלו |
|---|---|
| **אימות עובדתי** | *"אילו טענות עובדתיות במסמך שגויות או חסרות-מקור?"* — **פתח כל קובץ שהמסמך מזכיר.** ‏מנה משטחים לפני שאתה בודק · חפש **סתירות בין שלבים** (תשעה שלבים, אחד עלול להרוס מה שאחר בונה) · **שורה לכל טענה, כולל "אומת ✓"** |
| **ישימות ושיפור** | *"איפה מבצע ייתקע, ינחש, או יעשה את הלא-נכון — ומה היה משפר?"* — ארבע עדשות: מה חסר (במיוחד **הפרעה**) · מה יפורש בשתי דרכים · **האם דילוג ייראה** (ישי לא קורא קוד) · מה מיותר. **הצעות שמסירות שוות כמו שמוסיפות** |

**לשניהם:** ‏🔴 **אל תמסור להם את ממצאי §3א** — זו מסירת-תשובה-צפויה. ‏**ו"אין ממצאים" היא תשובה
מלאה ומוערכת** — עדיף דף ריק מממצא מומצא. **ושיאמרו מה לא הספיקו לבדוק.**

### 📎 זהב מתיקיית `C:\Users\ishay\עבודה עם קלוד\` — לסקיל "מעצב-פרומפט" העתידי

זוקק מ-`בניית הפרומט לקלוד.pdf` (05/08). ‏**שני דברים בלבד שווים, השאר גנרי:**

1. **הסגר-רעיונות** — כותרת `/ideas_to_evaluate` שמקבלת כל מה שישי כבר חשב עליו, עם השורה
   *"אלו רעיונות שעלו לי. אל תניח שהם נכונים"*. ‏**זה מה שמאפשר לו לשפוך הכל בלי לקנות תשובה
   מלטשת במקום תשובה חושבת.**
2. **מיון לשלושה דליים לפני שליחה** — הבעיה · הרעיונות שלי · הפתרון. ‏*"אתה מערבב שלושה דברים,
   וכתוצאה קלוד נכנס ל-confirmation bias."*

🔴 **ומתח שנחשף ואינו פתור באף קובץ:** ‏`prompts.md` כלל 2 מורה לצטט את ישי **מילה-במילה**
(*"phrasing is the spec"*) — **אבל ציטוט של הכרעה מחייב, וציטוט של רעיון שלא נבדק צריך הסגר.**
**ההבחנה הזו חסרה.**

**וכל שלושת הקבצים נסרקו (05/08). זה הזיקוק המלא — התיקייה עצמה כבר לא צריכה קריאה חוזרת:**

**🔴 הממצא היחיד ששלושת הסוכנים הגיעו אליו בנפרד — וזה הפער האמיתי:**
> **אין בפרויקט שום כלל שמבחין בין *הכרעה* של ישי לבין *רעיון* של ישי.**
> ‏`prompts.md` כלל 2 מורה לצטט אותו **מילה-במילה** כ-spec מחייב. ‏**אבל רעיון שהוא זרק ולא בדק
> אינו spec — והתייחסות אליו ככזה היא בדיוק הרגע שבו קלוד הופך ל-yes-man.**
> **התיקון:** רעיון נכנס תחת `/ideas_to_evaluate` עם *"אל תניח שהם נכונים"*; הכרעה מצוטטת כמחייבת.

**מ-`תכנון פרומפט לקלוד משופר.pdf` — ‏🔴 עמוד אחד, רשימת 16 כותרות בלי נימוק לאף אחת:**
- **‏7 מהחלקים אין להם סיבה בשום מקום** (`root_cause_analysis` · `analysis_principles` ·
  `agent_usage` · `success_criteria` · `anti_patterns` · `deliverables` · `current_dev_os`) — **קישוט.**
- **הלקח החד:** הקובץ הקודם כתב *"‏8 חלקים. לא 40."* — והמשופר הגיע ל-**16**.
  **מה שנוסף בשדרוג הוא בדיוק מה שאין לו נימוק. הכפלת השלד אינה שיפור.**
- ✅ **אבל כן ממלא חלל אמיתי:** ‏`prompts.md` מכסה **רק פרומפט קלוד→בנאי**.
  **אין בפרויקט שום ארטיפקט לפרומפט ישי→קלוד.**

**מ-`פרומט מקורי לשיפור.docx` — הדפוסים של ישי כשהוא כותב חופשי (43K תווים):**

| מה | הראיה |
|---|---|
| ✅ **בשפע: כאב מהשטח בגוף ראשון** | *"אני לא מבין שורת קוד אז אני חושש שהוא פיספס משהו… וזה די מתיש"* — **חומר-גלם שרוב האנשים לא יודעים לספק** |
| ✅ **מדרג אמון במקורות** | *"קח בערבון מוגבל… לא הייתה לו גישה לקוד ולך כן"* — **נדיר מאוד** |
| 🔴 **משמיט **כל** אילוץ** | **אפס אזכורי דדליין/זמן/תקציב ב-43K תווים.** מבקש להכריע בין שיטות בלי המשתנה שקובע איזו אפשרית |
| 🔴 **מזהיר מהטיה — ואז מספק את העוגן** | *"אל תניח שהשיטה הקיימת טובה"* נכתב **אחרי 350 שורות של פתרון מוגמר** |
| 🟠 **דיוק בקונקרטי, ערפול במופשט** | "4 פאזות: מיגרציות, לוגיקה, מסכים, בדיקות" — חד · "שיטת עבודה" — לא מוגדר |
| 🟠 **סוגר בבדיקת-הבנה בשורה 780** | *"המשימה ברורה? יש לך שאלות?"* — מנגנון שעובד, מופעל בסוף במקום בהתחלה |

**שני השינויים שהיו משפרים הכי הרבה:**
1. **להפוך את הסדר** — חמש שורות בפתיחה (*מה כואב · מה האילוץ · מה ייחשב תשובה טובה · במה לא לגעת*),
   וכל השאר הופך לנספח מסומן **"רקע, לא הנחיה"**. ‏**זה גם מנטרל את בעיית העוגן בלי לוותר על החומר.**
2. **לסמן איזו שאלה היא ההכרעה** — שש שאלות שוות-משקל מפזרות את התשובה על הכל.

🔴 **וההערה שאסור להשמיט:** ‏*"זה **לא** פרומפט גרוע. שני הכשלים היחידים הם **סדר** ו**אילוץ חסר** —
לא סגנון, לא ניסוח, ולא חוסר-מחשבה."*

### 1️⃣ לתקן את התוכנית מול הממצאים
**לא לשכתב אותה.** ‏לתקן טענות שגויות, להשלים משטחים חסרים, **ולהכריע את סתירת `_shared/`**
(סעיף 2 למעלה) — **זו הכרעה של ישי, לא שלך.**

**דוח ביקורת-ההקשר-הטרי על שלב 1 חזר במלואו. הממצאים בסעיף 3א למטה — קרא אותם לפני כל תיקון.**

---

## 3א. ממצאי ביקורת שלב 1 (הוצאת work-manager) — נאספו 05/08 09:3X

> **מקור:** סוכן בהקשר טרי, שתי שאלות בלבד. **כל הממצאים מדודים מול הריפו.**

### ① שש טענות שגויות בפרומפט של שלב 1

| # | הטענה בפרומפט | מה נמדד | הנוסח הנכון |
|---|---|---|---|
| **A** | *"סעיף ה-⚡ = שורות 10–32"* | הסעיף רץ עד **שורה 52**; `## Pre-flight` בשורה 54 | **10–52** |
| **B** 🔴 | *"שבעה מקומות **מחוץ** לסעיף ה-⚡"* | **ארבעה מהשבעה (36 · 42 · 46 · 48–49) בתוכו.** רק 55 · 69 · 91 מחוצה לו | **שלושה מחוץ, ארבעה בתוך — להכריע לפני מחיקה** |
| **C** | ‏`check-context.mjs`: *"202–218 · push ב-218 · exit ב-240"* | **202–221 · push ב-220 · exit ב-243** · ויש **push שני ב-206** שלא הוזכר | לתקן מספרים |
| **D** 🔴 | ‏`concurrency.md`: *"גזום את **הסעיף** תקצירי-חילופין ואת **הסעיף** מענה-במקום-ישי"* | **שניהם בולטים** (51–54 · 58–61) **בתוך** סעיף אחד (36–62). ‏**"מצב 8" לא קיים בקובץ בכלל** | **לגזום שני בולטים, לא סעיף** |
| **E** | *"שלושה קובצי-זיכרון קשורים לתפקיד"* | **ארבעה** — `MEMORY.md` הוא הרביעי | ספירה בלבד, הפעולה נכונה |
| **F** | — | ‏`STATUS.md:52` מפנה ל-`docs/work_manager_situations_draft.md`; הקובץ ב-`docs/**delete**/` | **הנתיב כבר שבור היום** |

🔴 **‏B ו-D הן המסוכנות:** מי שיבצע כלשונן **ימחק ארבעה כללים שהפרומפט עצמו ביקש לשמר**
(פרוטוקול-הפער · כלל ה-🧭 · כלל העובדות-הנדיפות · *"You are a single point of failure"*).

### ② שלושה-עשר משטחים שהפרומפט לא מזכיר

**🔴 חמישה קריטיים:**

1. ~~**`.claude/skills/module-blueprint/template.md`** — סעיף ⚡ **שלם** בשורות 12–34, **גדול מזה שב-module-build**, **+ ציטוט-נתיב קשיח** לשורה 48: `work-manager/references/ishay.md` situation 10.~~
   ~~⚠️ **‏`module-blueprint/SKILL.md` נקי לגמרי (29 שורות, 0 התאמות)** — מי שיבדוק אותו יסיק "נקי" ויעבור. **כל הפרוצדורה חיה ב-`template.md`.**~~
   ~~🔴 **ואין לזה פתרון בפרומפט:** הכרעה #3 אוסרת להציל את `ishay.md` ⇒ **סקיל חי יצביע לתוך הארכיון.**~~

   > 🔴 **הופרך במדידה — 05/08/2026 10:0X. אל תפעל לפי הפסקה המחוקה שמעליה.**
   > ‏`module-blueprint/template.md` הוא **81 שורות**, ו**אף אחת** מהמחרוזות `⚡` · `When a manager
   > session exists` · `situation 10` · `work-manager` · `references/ishay` · `current_manager`
   > **אינה בקובץ** (נמדד בשלוש שיטות בלתי-תלויות). **שורות 12–34 הן `### ⚠️ Workflow & Plan Rules`
   > — על Plan Mode. שורה 48 ריקה.** שתי ההתאמות היחידות בקובץ (46 · 63) הן *"מנהל-מוצר" = ישי*.
   > ⇒ **הפריט הזה לא היה "משטח חסר" — הוא היה טענה שגויה**, והיא נשאה `⏸️ ממתין להכרעת ישי` על
   > תלות שאינה קיימת. **אושר שוב בעצמאות על-ידי הסשן שביצע את ההוצאה בפועל** (יומן 05/08 10:55).
   > 📌 **הלקח שנשמר:** ‏`module-blueprint` **נקי לגמרי** משני הקבצים. **אל תייצר שם ממצא.**
2. **`docs/delete/work_manager_situations_draft.md`** — ‏58KB, **מנוהל-גיט**, ובתוכו **§ז' כללים חסרי-בית הממתינים להכרעת-ישי.** לא מוזכר באף רשימה.
3. **`STATUS.md` — 58 התאמות, ואינו מופיע בפרומפט אף פעם.** ארבעה מצביעים חיים ימותו: שורות **17 · 46 · 319 · 52**. ‏**STATUS נקרא בפתיחת כל סשן** — מצביע מת שם הוא הנראה ביותר בפרויקט.
4. **`docs/CLAUDE.md` שורות 19 · 24 · 25** — טבלת מקורות-אמת המפרטת את **שלושת** הקבצים המארכבים. נטענת בכל נגיעה ב-`docs/`.
5. **`docs/toolbox.md:53`** — `work-manager` מנוי כ"תמיד זמין", ו-CLAUDE.md מורה *"לקרוא לפני שמציעים כלי"*. ✅ *(אומת: עריכתו לא תשבור את `check:context`.)*

**🟠 חמישה בינוניים:**

6. **`check-context.mjs` שורות 185–197 — בדיקת-מנהל שנייה** (סריקת `handoff_manager_*.md`). דוחפת `note` בלבד ⇒ **לא תפיל את השער**, אך תשרוד כבדיקה חיה לתפקיד מחוסל.
7. **`prompts.md` כלל 18** (157–159) *"Ishay reads the manager"* — לא בגיזום ולא בשימור; החלפה גורפת תהפוך אותו ל**"ישי קורא את ישי"**. ‏**וכלל 9ב** (117, המלצת-דגם) — כללי ושימושי, עלול להיגזם יחד עם 9 ו-10.
8. **`concurrency.md` שורות 63–69** — מנתב **סמכות-כתיבה** (*"builders REPORT and **you** filter and write"*). החלפה גורפת ⇒ **"ישי מסנן וכותב"**, טענה שקרית. **נתפס רק במעבר-ההתנהגות, לא במילולי.**
9. **ספירות-עצמיות:** ‏`discipline.md:1` *"six repo-local skills"* (יהפוך ל-5) · `quality-audit/SKILL.md:10` *"the other five"* (יהפוך ל-4).
10. **ה-Stop hook** (`check-docs-updated.sh` 149–150 · 165) **חוסם סיום-סשן** עד עדכון `CLAUDE_CODE_LOG` **ו-**`STATUS`. ‏**‏12 סעיפי הדוח בפרומפט אינם כוללים את פרוטוקול-סוף-הסשן** ⇒ הסשן ייחסם ויאלתר.

**🟡 שלושה קלים:** ‏11. הפלייבוק הקנוני (`~/.claude/references/…`) — 14 אזכורים, **מחוץ לתחום בצדק**, אבל ראוי לשורת "שארית ידועה" · 12. ‏`skill-scan` **כבר קיים ומיישם את המפרט** — הכרעה #5 מגינה מפני משהו שכבר נעשה · 13. מדריכי-המיקרו — היסטורי בלבד, ראוי לשורת "נבדק, נשאר".

### ✅ ציון חיובי — חוסך עבודה
**`module-close` נקי לחלוטין מניתובי-תפקיד.** ‏שלוש ההתאמות שם הן *"Release Manager"* ו*"product manager"*.
**הכרעה #1 נוקבת בו ולא תניב דבר — שיידע מראש ולא ימציא ממצא.**

### ✅ מה כבר אומת ואין לבדוק שוב
‏`module-build` = 99 שורות וכל 7 השורות במיקומן · הצימוד ל-`current_manager` **אמיתי** ·
‏`prompts.md` כללים 0/8/9/10/14/16/17 ו-1ב/4/4ב/5/12/15 במיקומם · `miss-ledger` = 582 שורות ·
`failure-modes.md` קיים · `discipline.md` — הטבלה **בדיוק 4 שורות** · זיכרון: 23 קבצים, הגדלים מדויקים ·
**ארבעת המשטחים שאינם סקילים — נקיים** · ‏`CLAUDE.md` בשורש — **0 אזכורים** ·
‏`check:context` **עובר exit 0** · `protect-frozen-files` **לא יחסום** העברה ל-`docs/archive/`.

### ⚠️ מה הסוכן **לא** הספיק — אל תתייחס כמכוסה
1. **לא השווה את `failure-modes.md` מילה-במילה** לנוסח שבפרומפט (רק קיום ו-8 שורות פתיחה).
2. **לא קרא את תוכן `manager_evidence_regin.md`** — רק גודל ותאריך.
3. **לא סרק את `work-manager/references/` לאיתור ידע שווה-הצלה** מעבר לשני הקבצים שהוכרעו (`boot-and-handover` · `builders` · `queue` · `learning` · `watching` · `ishay`).
4. **מיון לא-מדוד:** `PROJECT_MASTER` (43) · `C5` (86) · `C6` (10) · `module4_smart_match_research` (22) ועוד — **סווגו לפי אופי הקובץ, לא ב-grep ממוקד.**
5. **`docs/CLAUDE_CODE_LOG.md` (138 אזכורים) — לא מוין כלל.**

### 2️⃣ להריץ את שלב 1 — הוצאת work-manager
**הפרומפט חי בתוך התוכנית, שורות 39–474.** ‏**ענף חדש מ-`dev` טרי.**
⚠️ **אל תריץ אותו בגרסתו הנוכחית** — ראה שלב 1️⃣.

### 3️⃣ Discovery למודול 4
**הפרומפט: התוכנית, שורות 481–814.** ‏זה פרומפט ארוך ומפורט שישי כתב בעצמו — **הוא טוב.**
המטרה: אפיון מודול מאושר + מוקאפים, **בלי לכתוב קוד ובלי Blueprint.**

### 4️⃣ ואילך — לפי התוכנית
שלב 3 (יצירת `module-discovery` **מהניסיון האמיתי**) → 4 (התאמת blueprint) → 5 (בלופרינט מ4) →
6 (התאמת build) → 7 (בנייה) → 8 (audit + `quality-fix`) → 9 (close + מיזוג).

---

## 4. הכרעות שממתינות לישי — אל תכריע בעצמך

| # | ההכרעה | הרקע |
|---|---|---|
| **1** | **סתירת `_shared/`** — האם הסקילים החדשים מנותקים ממנו, ואם כן לאן עוברים כללי-האימות | סעיף 2 |
| **2** | **סריקת-סודות** — `gitleaks` **אינו מותקן מקומית** ורץ רק על PR. שלוש אפשרויות רשומות ב-`🚧 מ4` | סוד בענף אישי כבר ב-GitHub |
| **3** | ‏`docs/current_manager.md` | יורד רק יחד עם הבדיקה ב-`check-context.mjs:218`, אחרת `npm run gate` נשבר |
| **4** | **שני סקילים שביקש** — "מעצב-פרומפט" ו"יועץ" | **המלצה: לבנות אחרי שימוש אמיתי, לא מתמליל** — בדיוק כמו `module-discovery` |

---

## 5. חובות שנרשמו ויימצאו לבד

**‏`grep '🚧 מ4' docs/PROJECT_MASTER.md`** מחזיר **8 שורות** — פתיחת מודול 4 קוראת אותן אוטומטית.
כולל: פגמי נתיב-הבלופרינט · שני מנגנוני-אכיפה שיורים חלקית · **ותפוגת פיקסצ'רי ה-E2E**
(הצעה #8 סביב 28/08, #22 סביב 31/08 — **ייפלו מעצמם**, פעם שלישית).

---

## 6. איך לעבוד עם ישי — חמישה דברים שנמדדו, לא סברות

1. **‏`_shared/failure-modes.md` — קרא אותו.** חמש הדרכים שהפרויקט הזה נכשל, ו-6 שאלות בדיקה-עצמית.
2. **טענת-היעדר היא המלכודת המרכזית.** *"X לא קיים"* אחרי חיפוש במקום אחד = מדידה של מקום אחד.
   **מנה את המשטחים לפני שאתה מחפש.** *(נכשלנו בזה 5 פעמים ב-24 שעות; ישי תפס את כולן.)*
3. **פרומפט שאתה כותב לסשן אחר ומכיל טענות על הריפו — עובר ביקורת הקשר-טרי.**
   שתי שאלות בלבד: *"אילו טענות כאן חסרות מקור?"* · *"מה זה לא מזכיר ושהיה צריך?"*
   *(הכלל כתוב ב-`~/.claude/CLAUDE.md`.)*
4. **לעולם אל תמסור לבודק מספר צפוי.** *"תריץ ודווח כמה"*, לא *"אמורות להיות 71"*.
5. **הכרעות מוצר/UX/הרשאות — של ישי.** פרט טכני הפיך — שלך, תעד והמשך. **פער מוצרי לא נפתר בניחוש.**

---

## 7. מה לא לעשות

🚫 לא למזג · לא לדחוף ל-`dev`/`main` · לא לפתוח PR — **המיזוג של ישי בלבד, בכל תרחיש.**
🚫 לא ליצור Agent/Manager/routing חלופי.
🚫 לא לגעת בשתי התיקיות ה-`??`.
🚫 לא למחוק `docs/current_manager.md` בלי להסיר את הבדיקה ב-`check-context.mjs` באותו שינוי.
🚫 לא להוסיף כללים ל-`CLAUDE.md` — נגזם בכוונה ב-28/07, וההכרעה בתוקף.

</div>
