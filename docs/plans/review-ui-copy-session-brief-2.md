<div dir="rtl">

# ביקורת הקשר-טרי — `ui-copy-session-brief.md` · **סבב שני**

> **מה זה:** ביקורת חיצונית על **הפרומפט**, לא על התוכנית ולא על הקוד. הנבדק: `docs/plans/ui-copy-session-brief.md` (285 שורות, כפי שהוא על הדיסק ב-07/09/2026).
> **מתי:** 07/09/2026 16:10, לפני ההדבקה המתוכננת בשלישי 08/09/2026 22:00.
> **מי:** סוכן-ביקורת בהקשר-טרי. לא כתב את הבריף ולא ראה את השיחה שהולידה אותו. **קרא קודם את הבריף לבדו, ואז רק את הקבצים שהבריף עצמו מפנה אליהם (רשימת-הקריאה ③); את `ui-copy-session-brief-ledger.md` — אחרון.**
> 🚫 **אינו רשאי לקבוע כלל-מוצר, לתקן דבר, או לגעת במסד.** כל ההצעות כאן הן **המלצות להכרעת ישי**.
> **סבב שני:** הסבב הראשון (`review-ui-copy-session-brief.md`, 07/09 04:00) מצא 7 חוסמים ב-141 שורות; מאז הקובץ נכתב מחדש וגדל ל-285. §4 בודק כל אחד מהשבעה בנפרד.
> 🔴 **השורה התחתונה: יש חוסם אחד שמכשיל את הלילה בדקה הראשונה — ‏`dev` אינו מכיל את רשימת-הקריאה.** פירוט: §2 ה1.

---

## §0 · שלוש שורות לישי, לפני הפירוט

**מצאתי דבר אחד שחייב תיקון לפני ההדבקה, ואפשר לתקן אותו בשורה אחת:** הבריף מורה לסשן לפתוח ענף חדש מ-`dev`, אבל **כל התוכניות, המחקרים, המצאי והכרעה 28 יושבים רק על הענף של מודול 11 — הם אינם ב-`dev`, וגם עדיין לא נדחפו לשרת.** ברגע שהסשן יבצע את ההוראה, שמונת הקבצים שהוא אמור לקרוא **נעלמים מהדיסק**, והוא נשאר עם פרומפט שמפנה לקבצים שאינם קיימים.

מעבר לזה — הבריף במצב טוב בהרבה מהסבב הראשון: **כל שבעת החוסמים הקודמים טופלו**, שניים מהם חלקית. מה שנשאר הוא ארבעה מוקשים תפעוליים שהסשן ייתקל בהם בלילה (ההוק שחוסם סיום-תור · שער-ההקלדה של המיגרציה · טקסטי-המייל ללקוחות · בדיקות שיישברו ואף שלב לא מריץ אותן), ושש טענות עובדתיות לא-מדויקות.

---

## §1 · הטענות — כל אחת נבדקה בפועל

**מקרא:** ✅ נמדד ונכון · ⚠️ מדויק-חלקית או מיושן · ❌ שגוי · ❓ אין מקור, לא ניתן לאמת.

### 1א · הטענות שעמדו במדידה (23)

| # | הטענה בבריף | הפקודה שהרצתי | מה נמדד | |
|:-:|---|---|---|:-:|
| 1 | `2,311` מחרוזות | `sed -n '108,158p' docs/plans/ui-copy-and-onboarding-mode.md` | "3,005 מופעים ⇒ 2,311 מחרוזות ייחודיות", וטבלת-הסיווג מסתכמת ל-2,311 | ✅ |
| 2 | `74` זכר ב-24 קבצים · `9` נקבה ב-8 · `6` רבים | `sed -n '17,40p' docs/plans/research-ui-copy-standards.md` | §0 פריט 3, מילה-במילה, כולל "כל חמש המשתמשות נשים" ו-`S-28` | ✅ |
| 3 | `939` · `58` · `1,372` · `59.4%` | `sed -n '80,100p' docs/plans/inventory-ui-copy.md` | שורות 93–97 של המצאי — ארבעת המספרים, באותו ניסוח | ✅ |
| 4 | `onboarding_hints` ב-`db_roadmap.md:287` | `sed -n '280,295p' docs/db_roadmap.md` | שורה 287 היא `M11-1`, ובה "proposed name `onboarding_hints`" ו-"No new table, **no new policy**, no new index" | ✅ |
| 5 | `default true` ב-`db_roadmap.md:289` | `grep -n "default true" docs/db_roadmap.md` | שורה 289 בדיוק: "**Recommendation: `default true`**" | ✅ |
| 6 | `schema.sql:172·173·200·214·215` נושאות `role_name = 'מנכ"ל'` | `grep -n 'מנכ' docs/schema.sql` | חמש השורות בדיוק — לא חסרה ולא עודפת אחת | ✅ |
| 7 | התקדים `users_write_ceo_only` | `grep -n "^create policy" docs/schema.sql` | `create policy users_write_ceo_only on users` בשורה 212 | ✅ |
| 8 | `e2e/smoke-anchors.json:21` נועץ `"sortLine": "ממוין: לפי קרבת האירוע"` | `sed -n '15,30p' e2e/smoke-anchors.json` | שורה 21 בדיוק, מחרוזת זהה | ✅ |
| 9 | `smoke` אינו ב-`gate` ואינו ב-CI | `node -e "console.log(require('./package.json').scripts)"` | `gate` = verify+dup+deadcode+audit+bidi+context+docs-structure. `smoke` אינו שם | ✅ |
| 10 | `src/modules/01_auth/` — שבעה מסכים, אפס בדיקות | `ls src/modules/01_auth/` · `find src/modules/01_auth -name "*.test.*" \| wc -l` | שבעה `.jsx` · **0** קובצי-בדיקה | ✅ |
| 11 | `ProfileSettingsPage` ב-`src/components/`, לא במ9 | `ls src/components/ProfileSettingsPage*` | `src/components/ProfileSettingsPage.jsx` + `.test.jsx` | ✅ |
| 12 | `LoadingOrError.test.jsx` נועלת את `נסה שוב` | `cat src/components/LoadingOrError.test.jsx` | "הכפתור נשאר 'נסה שוב' בייט-בייט" + `expect(...).toBe('נסה שוב')` | ✅ |
| 13 | `LoadingOrError` בנוי לשני הכיוונים, ברירת-מחדל `נסה שוב` | `grep -rn "retryLabel" src/components/LoadingOrError.jsx` | `retryLabel = 'נסה שוב'` בשורה 112 | ✅ |
| 14 | `PROJECT_MASTER §4א` — שלושה חוסמים · "שלבים 0–5 בלבד" | `sed -n '302,345p' docs/PROJECT_MASTER.md` | §4א קיים; החוסמים ממוספרים ①②③; שורה 336 נושאת "ההמלצה: לאשר שלבים 0–5 בלבד" | ✅ |
| 15 | Nielsen — מעריך אחד `~35%`, שלושה `~75%` | `sed -n '409,455p' docs/plans/ui-copy-and-onboarding-mode.md` | שלב 10 של התוכנית, אותם מספרים | ✅ |
| 16 | ציטוט-ישי `2+2` | אותה פקודה | "2 סוכנים שבודקים את המצב הבוגר ו-2 סוכנים בודקים את מצב ההטמעה, כי זה 2 דברים שונים לגמרי" — מילה-במילה | ✅ |
| 17 | ציטוטי-ישי `"1. בסדר"` · `"2. בסדר"` · `"וכן יכול לאחד 5 נוסחים"` · `"עושים עבודה פעם אחת"` | `sed -n '560,624p' docs/plans/ui-copy-and-onboarding-mode.md` | ארבעתם בטבלת §10, וגם בהכרעה 28⑫ | ✅ |
| 18 | `R27`/`R28` ב-`§1.5`, ומבחניהם | `sed -n '85,120p' docs/plans/research-ui-copy-standards.md` | R27 = "כסי את ההסבר: משהו לא-ברור נשאר?" · R28 = "מה המשתמשת עושה אחרת אחריו?" | ✅ |
| 19 | "~20 הכללים של §1" · `R1…R30` | `grep -o "R[0-9]\{1,2\}" … \| sort -u -V` | R1–R30 רצופים, כולם ב-§1.1–§1.5 | ✅ |
| 20 | `§2.2` שש דרכים · `§2.3` ששת התנאים · איסור-מיקרוסופט | `sed -n '132,167p' docs/plans/research-ui-copy-standards.md` | שש דרכים ממוספרות · טבלת §2.3 בת שש שורות · האיסור מצוטט עם הנימוק | ✅ |
| 21 | `worktree` לא עובד (`node_modules` + `*.local` ב-gitignore) | `grep -n "node_modules\|\.local" .gitignore` | שורה 10 `node_modules` · שורה 13 `*.local`; `.env.local` קיים ומחזיק `E2E_CEO_*` | ✅ |
| 22 | שלב 1 מדולג · סדר שלבים 3–8 | `sed -n '237,295p'` + `grep -n "^### שלב"` | התוכנית: "בלילה של שלישי אין לשלב הזה על מה לרוץ… הסשן של שלישי מדלג עליו ואומר זאת". סדר המודולים 7·6·8·4·2+3·5+9+1 — **זהה לבריף** | ✅ |
| 23 | הכנס ב-**15/10** | `grep -n "15/10" docs/guides/00_roadmap.md CLAUDE.md` | `00_roadmap.md` §3: "15/10/2026 — מפגש-הכנס הסופי" (עודכן 07/09 מפי ישי). `CLAUDE.md` בשורש **כבר תוקן** ל-15/10 ⇒ דגל 3 בליג'ר סגור | ✅ |

### 1ב · הטענות שנפלו או שדויקו רק חלקית (9)

| # | הטענה בבריף | הפקודה | מה נמדד | |
|:-:|---|---|---|:-:|
| **ט1** | שורה 145: "היא יושבת **בתוך ארבע מדיניות-RLS**" | `grep -n "^create policy" docs/schema.sql \| awk -F: '$1>150 && $1<230'` | **שלוש** מדיניות, לא ארבע: `permissions_write_ceo_only` (172,173) · `users_select_self_or_ceo` (200) · `users_write_ceo_only` (214,215) — חמישה **מופעים** בשלוש מדיניות. ⚠️ **ומקור הטעות הוא ביקורת-הסבב-הראשון עצמה** (`review-ui-copy-session-brief.md:51`, ח1: "בתוך ארבע מדיניות RLS") | ❌ |
| **ט2** | שורה 146: "וב-**37 קובצי-קוד** (`CEO_ROLE_NAME`)" | `grep -rl "CEO_ROLE_NAME" src/ e2e/ \| wc -l` · `grep -rl 'מנכ"ל' src/ \| wc -l` | `CEO_ROLE_NAME` מופיע ב-**3 קבצים בלבד** (`constants.js` · `ProfileSettingsPage.jsx` · `PermissionsMatrixPage.jsx`). המחרוזת הגולמית — ב-**41** קבצי `src/` (54 עם `e2e/`). המצאי §5ב נוקב ב-"47 **מופעים**", לא 37 קבצים. **אין מקור למספר 37 באף קובץ שהבריף מפנה אליו** | ❌ |
| **ט3** | שורות 174–175: "`retryLabel="נסי שוב"` ש**מודול 6** מעביר הופך למיותר — להסיר אותו באותו סבב" | `grep -rn "retryLabel" src/modules --include=*.jsx` | **מודול 6 אינו מעביר `retryLabel` באף קובץ.** שלושת אתרי-הקריאה האמיתיים: `08_finance/SalaryReportDialog.jsx:1154` · `09_settings/MySettingsPage.jsx:186` · `09_settings/ParamsTab.jsx:204` — כלומר **מ8 (שלב 5) ומ9 (שלב 8)**, לא מ6 (שלב 4). **מקור הטעות:** `src/modules/06_projects/CLAUDE.md:36` טוען "`retryLabel="נסי שוב"` בכל משטחי המודול" — טענה שאינה נכונה בקוד | ❌ |
| **ט4** | שורה 83: "'מספר שנרקב נמחק, לא מעודכן' (`writing-prompts.md`)" | `grep -n "נרקב" .claude/skills/_shared/writing-prompts.md docs/specs/module_11_reports/processes-approved.md` | הביטוי מופיע **פעמיים ב-`processes-approved.md`** (שורות 908 · 1033) ו**אפס פעמים** ב-`writing-prompts.md`. הייחוס שגוי; העיקרון עצמו נכון | ❌ |
| **ט5** | שורה 158: "סדר-העבודה — **המקור: §4.2 של התוכנית**" | `grep -n "^#\{1,4\} " docs/plans/ui-copy-and-onboarding-mode.md` · `grep -n "4\.1\|4\.2" …` | **לתוכנית אין §4.1 ואין §4.2** — §4 מתחלק ל"שלב 0…שלב 10". §4.1/§4.2 הם סעיפים ב-`research-ui-copy-standards.md`, והתוכנית עצמה מצטטת אותם כ-"§4.2 של **המחקר**" (שורה 43). הבריף מפנה את הסשן למקום שאינו קיים | ❌ |
| **ט6** | שורה 195: "**שמירה:** `upsert` על `notification_preferences`… לקרוא את השורה, למזג, ואז לכתוב" | `sed -n '159,175p'` ו-`sed -n '515,530p'` של התוכנית | 🔴 **סתירה חזיתית לתוכנית.** §3 פריט 7: "**`update` של העמודה בלבד — 🚫 לא `upsert` של כל השורה**"; §7-ו חוזר: "`update` של עמודה בודדת, לא `upsert` של שורה שלמה". הבריף מכריע הפוך — **בסעיף שכותרתו "פרטי-מימוש שהפרומפט חייב לקבוע, אחרת כל סוכן ימציא אחרת"** | ❌ |
| **ט7** | שורה 113: "**58 מופעי-⑥**" | קריאת הכרעה 28⑦ במלואה | הכרעה 28⑦ **פוסלת במפורש** את המספר לתכנון: "`grep -c "⑥"` מחזיר 58 … **המספר לתכנון הוא 96 שורות-קופי, לא 58 סעיפים**". הבריף מייבא 58 בלי הסייג, ומפנה את הסשן לקרוא את 28 (פריט 5) — כלומר הסשן ייתקל בסתירה | ⚠️ |
| **ט8** | שורה 112: "רק **ארבעה** מודולים (4·5·6·8) מחזיקים `screens-approved.md`" | `ls docs/specs/*/screens-approved.md` | **חמישה** קבצים קיימים היום — נוסף `docs/specs/module_11_reports/screens-approved.md`. הטענה נכונה למודולים **הבנויים** ומיושנת כטענה על הריפו | ⚠️ |
| **ט9** | שורה 24: "**סטייה מוצהרת** מהכרעה 28⑤" | קריאת 28⑤ במלואה | 28⑤ **כבר עודכנה על הדיסק** (07/09 05:1X, "בהכרעת-ישי ('מאשר')") ואומרת בדיוק את מה שהבריף אומר. **אין סטייה חיה** — הבריף מתאר את עצמו כסוטה מ-SSOT שכבר מסכים איתו | ⚠️ |

### 1ג · טענה בלי מקור (1)

| # | הטענה | מה בדקתי | |
|:-:|---|---|:-:|
| **ט10** | שורה 15: "~40–60 סוכנים … **6–9 מיליון טוקנים**" | חיפוש בתוכנית §6 (העלות) ובבריף — אין חישוב, אין מדידה, אין הפניה | **לא ניתן לאימות.** ייתכן שהוא סביר; הוא פשוט אינו נגזר משום דבר כתוב. הסבב הראשון כבר ביקש לסמן `הנחתי`, וזה לא נעשה | ❓ |

### 1ד · מבחן-הקבלה (עשר השאלות) — האם התשובה באמת נמצאת?

**בדקתי כל שאלה מול הבריף ומול הקבצים שהוא מפנה אליהם. אף אחת אינה דורשת ניחוש.**

| # | השאלה | איפה התשובה |
|:-:|---|---|
| 1 | המוקש העליון | **בגוף** — שורה 70 ("אחידות-קול… וכל השאר משני לו") |
| 2 | כמה עצירות ואחרי מה | **בגוף** — שורות 13 · 54 · 56 · 215–218. ⚠️ **אבל התשובה "שתיים" אינה שלמה** — ר' §2 ה2: שער-ההקלדה של המיגרציה הוא עצירת-אדם שלישית, בתוך שלב 0 |
| 3 | איזו מחרוזת אסור לגעת | **בגוף** — שורות 145–148 |
| 4 | שם העמודה · הטבלה · הענף · מאיפה נחתך | העמודה והענף **בגוף** (23 · 150). 🟡 **שם הטבלה אינו נאמר במפורש** — ניתן להסקה משורה 195, ומפורש רק ב**תוכנית §3 פריט 1** (`notification_preferences`), שהיא פריט 1 ברשימת-הקריאה |
| 5 | מתי נכתב מלל-ההטמעה | **בגוף** — שורות 58 · 67 · 163–165 |
| 6 | מה רץ בכל מחזור מעבר לבדיקות, ולמה במפורש | **בגוף** — שורות 207–210 |
| 7 | מה שלב 1 עושה בלילה | **בגוף** (55) + **תוכנית §4 שלב 1** — שניהם "מדולג", באותו נימוק |
| 8 | "לאפס" או "לערוך" + הצעד השני | **בגוף** — שורה 63 |
| 9 | כמה בסך-הכול · במ6 · ב-`src/lib` | 2,311 **בגוף**; **מ6 = 300** ו-**`lib` = 691 (30%)** רק ב**תוכנית §2** ו-**§7-ד** (פריט 1 ברשימת-הקריאה) |
| 10 | כמה מעריכים · החלוקה · מה רק הממזג בודק | **בגוף** — שורות 225–237 |

**מסקנה:** מבחן-הקבלה תקין ואינו דורש ניחוש. שתי הערות: שאלה 4 תשתפר אם שם-הטבלה ייאמר בבריף עצמו, ותשובת שאלה 2 חייבת לכלול את שער-ההקלדה.

### 1ה · הבריף מול התוכנית — שלוש נקודות שבהן הם התפצלו

| | הבריף אומר | התוכנית אומרת | המשמעות |
|---|---|---|---|
| **שלב 1** | "מדולג — כלל למודולים שטרם נבנו" | "כלל, לא שלב של אותו לילה… הסשן של שלישי מדלג עליו ואומר זאת" | ✅ **מסכימים.** אין פיצול |
| **שלב 10 (2+2)** | 2+2 · בלתי-תלויים · המיזוג של הסשן · העמודה השלישית | זהה, ובנוסף: "**ברירת-המחדל היא 2+2; ישי רשאי להעלות ל-3+3**", ו**המעריכים כותבים סעיף-ורדיקט לתוך `ui-copy-night-report.md`** | ⚠️ **שני הבדלים.** אופציית 3+3 לא הגיעה לישי; ו"ארבעה סוכנים כותבים לאותו קובץ" מתנגש בכלל-הברזל 16 שהבריף עצמו מצטט (שורה 102) — ר' §2 ה9 |
| **קריטריון-סיום שלב 0** | ארבעה פריטים | **שמונה**: מתג חי בלי רענון · החזרה-לאחור בכשל · הדלקה חוצת-משתמשות · **`npm run gate`** · **הרצת-CI-parity** · **`e2e/settings.spec.js`** · קובץ-בדיקות ל-`UsersManagementPage` · שני צילומים | 🔴 **הבריף מוריד ארבעה שערים**, ביניהם `gate` — היחיד שמריץ `format:check`, שהוא היחיד שתופס את מוקש-סופי-השורה שהבריף עצמו מזהיר ממנו בשורה 156 |

---

## §2 · ההשמטות — מה חסר · מהיכן ידעתי · ונוסח מוצע

### ה1 · 🔴 **חוסם-על — `dev` אינו מכיל את רשימת-הקריאה, ואף לא אחד משמונת הקבצים**

**מהיכן ידעתי:** הבריף שורה 23 מורה "ענף `ishay/ui-copy-rewrite`, **נחתך מ-`dev` טרי**". בדקתי מה יש על `dev`.

**מה נמדד** (`git fetch origin` ואז `git cat-file -e origin/dev:<path>` לכל קובץ):

| הקובץ שהבריף מחייב לקרוא | קיים על `origin/dev`? |
|---|:-:|
| `docs/plans/ui-copy-session-brief.md` (הבריף עצמו) | ❌ חסר |
| `docs/plans/ui-copy-and-onboarding-mode.md` (התוכנית) | ❌ חסר |
| `docs/plans/research-ui-copy-standards.md` | ❌ חסר |
| `docs/plans/research-onboarding-content.md` | ❌ חסר |
| `docs/plans/inventory-ui-copy.md` | ❌ חסר |
| `docs/specs/module_11_reports/processes-approved.md` (הכרעה 28 — ה-SSOT) | ❌ חסר |
| `docs/PROJECT_MASTER.md` §4א | ❌ אין סעיף כזה שם |
| `docs/db_roadmap.md` בלוק `A-M11` / `M11-1` | ❌ אין |

**וגם לא על השרת:** `git rev-list --count origin/ishay/module-11-reports..HEAD` ⇒ **44 קומיטים לא-דחופים**. כלומר הקבצים חיים **רק בעץ-העבודה המקומי ובענף המקומי `ishay/module-11-reports`** — לא ב-`dev`, ולא ב-`origin/ishay/module-11-reports`.

**למה זה חוסם, ולמה זה גרוע מ"הסשן ימצא דרך":** `git switch -c ishay/ui-copy-rewrite origin/dev` **מוחק את הקבצים מהדיסק** (הם קומטו בענף אחר, `git status` נקי). הסשן יבצע את שורה 23 כפעולה ראשונה — **לפני** רשימת-הקריאה ③ — ואז יגלה שאין מה לקרוא. **מבחן-הקבלה בן עשר השאלות ייכשל על שאלות 4, 7 ו-9**, שתשובותיהן יושבות בתוכנית.

**מה כן נמדד לטובה:** `git diff --name-only origin/dev..HEAD -- src/ supabase/` מחזיר **ריק** — 98 הקומיטים שמעל `dev` הם **54 קבצי-תיעוד בלבד, אפס קוד ואפס מיגרציות.** ⇒ ענף שנחתך מ-`ishay/module-11-reports` נושא **קוד זהה ל-`dev`** ועוד את כל התיעוד.

**נוסח מוצע להוספה (מחליף את שורה 23):**
> 🔴 **ענף: `ishay/ui-copy-rewrite`, נחתך מ-`ishay/module-11-reports` — ולא מ-`dev`.** *(נמדד 07/09: כל שמונת קבצי רשימת-הקריאה, כולל הכרעה 28, קיימים **רק** על ענף מ11 ואינם על `dev`; חיתוך מ-`dev` מוחק אותם מהדיסק. ובכיוון השני: `git diff origin/dev..ishay/module-11-reports -- src/ supabase/` **ריק** ⇒ הקוד זהה ל-`dev`, ואין סיכון-קוד בחיתוך משם.)*
> **הפקודות, בסדר הזה:**
> ```
> git fetch origin
> git switch ishay/module-11-reports
> git switch -c ishay/ui-copy-rewrite
> git rev-parse --abbrev-ref HEAD    # חייב להחזיר ishay/ui-copy-rewrite
> ls docs/plans/ui-copy-and-onboarding-mode.md docs/specs/module_11_reports/processes-approved.md
> ```
> **שתי השורות האחרונות הן שער: אם קובץ חסר — עצור ודווח לישי, אל תתחיל את הלילה.**
> ⚠️ **ובדיקת "ענף-מת" של כלל-ברזל 10 אינה חלה כאן:** על ענף טרי `git merge-base --is-ancestor HEAD origin/dev` **מצליח** — זה אינו סימן שהענף מוזג. המבחן המפריד הוא `git log origin/dev..HEAD --oneline`; **לא-ריק ⇒ הענף בסדר, המשך.**

**⚠️ וזו הכרעת-ישי ולא שלי** — החלופות: (א) לחתוך מענף מ11 כמוצע · (ב) לדחוף ולמזג את קומיטי-התיעוד ל-`dev` לפני שלישי · (ג) לחתוך מ-`dev` ולהעביר את שמונת הקבצים ידנית. **המלצתי: (א)** — היא היחידה שאינה דורשת פעולה נוספת לפני 22:00, ואינה מכניסה קוד זר.

---

### ה2 · 🔴 **שער-ההקלדה של המיגרציה הוא עצירת-אדם שלישית — ומודל-העצירות מכחיש אותה**

**מהיכן ידעתי:** הבריף שורה 153 מפנה נכון ל-`supabase/migrations/CLAUDE.md`. קראתי את הקובץ.

**מה נמדד** (`sed -n '1,80p' supabase/migrations/CLAUDE.md`): השער הראשון בקובץ הוא **🔻 אישור-הקלדה (typed-echo)** — *"‏(1) Claude מסביר בעברית מה המיגרציה עושה… (2) **ישי מקליד את שם-המיגרציה** — לא 'כן', לא 'אישור'"*. זהו אחד משני השערים היחידים מסוגם בפרויקט כולו.

**למה זה חוסם:** הבריף שורה 66 קובע "**שתי עצירות בלילה**", שורה 215 חוזרת "**שתי עצירות-אמת בלבד בכל הלילה**", ושורה 220 סוגרת "🚫 לא עוצרים על 'רציתי לוודא'". **שאלה 2 במבחן-הקבלה תובעת מהסשן להצהיר "שתיים".** סשן שהפנים "שתיים" ומגיע למיגרציה בשלב 0 — נמצא במצב שבו הפרומפט אומר לו לא לעצור והפרוטוקול אומר לו שאסור להחיל בלי הקלדה. **זה בדיוק המקום שבו סשן ממציא פשרה.**

**ומה עוד חסר באותו סעיף — מה קורה *אחרי* ההחלה:** אותו קובץ מחייב (צעדים 3–4) **לרענן את `docs/schema.sql` מ-`pg_catalog`/`information_schema`** (לא מהממשק), **לעדכן את `docs/db_roadmap.md` §10 באותו סשן**, ולקמט את המיגרציה ואת `schema.sql` **יחד**. ה-Stop hook אוכף את זה מכנית (אכיפה 0ב). הבריף אינו מזכיר אף אחד מהשלושה.

**נוסח מוצע (מתווסף למודל-העצירות, שורה 215):**
> 🛑 **עצירה 0 — שער-ההקלדה, בתוך שלב 0 ולפני שתי העצירות.** החלת מיגרציה היא בלתי-הפיכה: אתה מסביר בעברית מה היא עושה, **ישי מקליד את שם-המיגרציה**, ורק אז אתה מחיל דרך `apply_migration` (‏`supabase/migrations/CLAUDE.md` — השער המלא). **זו אינה "עצירה שלישית" שמפרה את המודל — היא שער-פרוטוקול קיים שחל תמיד.**
> **ומיד אחרי ההחלה, באותו תור:** רענן את `docs/schema.sql` מהמסד · עדכן את `docs/db_roadmap.md` §10 · וקמט את שניהם עם המיגרציה בקומיט אחד. **בלי זה ה-Stop hook חוסם את סיום התור.**

---

### ה3 · 🔴 **ה-Stop hook — הסשן ייתקל בו בכל תור, והבריף אינו מזכיר אותו כלל**

**מהיכן ידעתי:** `.claude/settings.json` רושם `Stop → .claude/hooks/check-docs-updated.sh`. קראתי את ההוק.

**מה נמדד** (`grep -n "אכיפה\|PLAN_STD" .claude/hooks/check-docs-updated.sh` + `ls docs/micro_guides/`):

1. **אכיפה 0** — כל קובץ שהשתנה תחת `src/modules/NN_*/` מחייב ש-`docs/micro_guides/module-N.md` יעודכן **אחריו**. ‏`ls docs/micro_guides/` מחזיר **module-1 … module-9 — כולם קיימים** ⇒ האכיפה חלה על **כל תשעת המודולים** שהלילה נוגע בהם.
2. **אכיפה 0ד** — קובץ ב-`docs/plans/` שנערך בסשן חייב שיוזכר בשמו באחד מ-`PLAN_STD`. בדקתי את שלושת הקבצים שהבריף מורה ליצור:

| הקובץ שהבריף יוצר | מוזכר באחד מקובצי `PLAN_STD`? |
|---|:-:|
| `docs/plans/ui-copy-styleguide.md` (שורה 179) | ❌ **לא** ⇒ ההוק חוסם |
| `docs/plans/ui-copy-candidates.md` (שורה 165) | ❌ **לא** ⇒ ההוק חוסם |
| `docs/plans/ui-copy-night-report.md` (שורה 221) | ❌ **לא** ⇒ ההוק חוסם |
| `ui-copy-and-onboarding-mode.md` · `ui-copy-session-brief.md` | ✅ מוזכרים ב-`docs/PROJECT_MASTER.md` |

3. **אכיפה 1–3** — `CLAUDE_CODE_LOG.md` ו-`STATUS.md` חייבים לשאת את ה-mtime **החדש ביותר**.

**למה זה חוסם בפועל, וזה העיקר:** להוק יש יציאה מוקדמת — **אם אין שינויים לא-מקומטים, הוא עובר בשקט.** אבל **סדר המחזור בבריף מבטיח שיהיו:** ④ "דיווח קצר בצ'אט" (= סיום-תור) ואחריו ⑤ "קומיט". כלומר **בכל אחד מתשעת המודולים הסשן מסיים תור עם קוד לא-מקומט** — ואז אכיפה 0 יורה על מדריך-המיקרו, ואכיפות 1–3 על היומן ולוח-המצב. **בשלב 0 זה קורה מוקדם יותר:** יצירת מדריך-הסגנון היא קובץ `docs/plans/` חדש ⇒ אכיפה 0ד יורה **לפני עצירה 1**, כלומר בעצירה שישי ממתין לה.

**נוסח מוצע (א' — היפוך שתי שורות במחזור-המודול, ⑤ לפני ④):**
> ④ **קומיט** *(`git commit -- <נתיבים>`)*, ועדכון טבלת-המצב ב-`ui-copy-and-onboarding-mode.md` §4.
> ⑤ **ואז** דיווח קצר בצ'אט — צילום לפני/אחרי + עד 10 שורות. 🚫 אל תמתין לתשובה, המשך מיד למודול הבא.
> 🔴 **למה דווקא בסדר הזה: ה-Stop hook (`check-docs-updated.sh`) חוסם סיום-תור כשיש שינויים לא-מקומטים** — הוא ידרוש מדריך-מיקרו מעודכן לכל `src/modules/NN_*/` שנגעת בו, ואת `CLAUDE_CODE_LOG.md` + `STATUS.md` כאחרונים. **קומיט לפני הדיווח מוציא אותך מהמסלול הזה בכל מחזור.**

**נוסח מוצע (ב' — שורה אחת ב-⑤ "מה אסור", לשלושת הקבצים החדשים):**
> ⚙️ **הקובץ הראשון שאתה יוצר ב-`docs/plans/` דורש מצביע, אחרת ה-Stop hook חוסם** (אכיפה 0ד): הוסף שורה ב-`docs/PROJECT_MASTER.md §4א`, בטבלת "החומר", לשלושת הקבצים — `ui-copy-styleguide.md` · `ui-copy-candidates.md` · `ui-copy-night-report.md`. **שורה אחת לכל אחד, באותו תור שבו נוצר הקובץ.**

---

### ה4 · 🔴 **טקסטי-המייל ללקוחות ותבניות-ההצבה — בתוך המצע, ואף מילה עליהם**

**מהיכן ידעתי:** הבריף שורה 111 מכריז שהמצאי הוא "מצע-העבודה", ושורה 91 מכריזה על שכתוב "ב-9 המודולים המוזגים". בדקתי מה יש שם מלבד מסכים.

**מה נמדד:**
- `grep -c "lib/email" docs/plans/inventory-ui-copy.md` ⇒ **37 שורות** במצאי מגיעות מ-`src/lib/email.js` ו-`src/lib/emailTemplates.js`.
- ‏`sed -n '1,40p' src/lib/emailTemplates.js` ⇒ הקובץ מחזיק את `TEMPLATE_PLACEHOLDERS` — **אסימוני-הצבה בעברית בתוך מרכאות**: `'[שם_איש_קשר]'` · `'[שם_פרויקט]'` · `'[לינק_אישור_משמרת]'` · `'[חודש_דיווח_ושנה]'` ועוד. הם **חייבים להתאים בייט-בייט** לגופי-התבניות ששמורים במסד (11 תבניות), וההערה בראש הקובץ מפרשת: *"לא ממציאים regex שני — איתור-משתנה-לא-מוכר משתמש ב-`findUnknownPlaceholders` הקיים"*.
- **והמלכודת:** המצאי מסווג אותם **①** — *"קצר — כותרת/תווית סבירה"* (למשל שורה 1492: `[שם_פרויקט] | lib/emailTemplates.js:19 (×16) | ①`). כלומר סוכן-כתיבה שקיבל "לאפס תוויות" רואה אותם כתוויות רגילות.

**למה זה חוסם:** שינוי אסימון שובר שליחת-מייל אמיתית ללקוחות ולדיילות **בשקט** — הבדיקות לא בהכרח יתפסו, המסך ייראה תקין, וזה בדיוק דפוס-הנזק של `מנכ"ל` שהבריף כן מכסה. **ובנפרד:** האם גוף-המייל שנשלח **ללקוח** בכלל בהיקף? הבריף מדבר על "המשתמש במרכז" ועל חמש המשתמשות הפנימיות; טקסט ללקוח חיצוני הוא קהל אחר לגמרי. הסבב הראשון שאל את זה (ח10) ואיש לא ענה.

**נוסח מוצע (מתווסף ל-⑤ "מה אסור"):**
> 🔴 🚫 **אל תיגע באסימוני-ההצבה של המיילים.** `src/lib/emailTemplates.js` מחזיק את `TEMPLATE_PLACEHOLDERS` — `[שם_איש_קשר]` · `[שם_פרויקט]` · `[לינק_אישור_משמרת]` וכו'. **הם ערכי-דאטה שחייבים להתאים מילה-במילה לגופי-התבניות שבמסד**, בדיוק כמו `מנכ"ל`. ⚠️ **והמלכודת: המצאי מסווג אותם `①` "תווית סבירה"** — אל תסתמך על הסיווג שם.
> ⏳ **ושאלה פתוחה שאינה שלך:** האם **גוף** המייל שנשלח ללקוח/לדיילת (‏`src/lib/email.js`, 37 שורות במצאי) בהיקף הלילה? **ברירת-המחדל: לא** — קהל חיצוני, לא חמש המשתמשות. **הכרעת-ישי אם אחרת.**

---

### ה5 · 🔴 **‏`נסה שוב` — הבריף מכיר קובץ-בדיקה אחד; יש שבעה, ושלושה מהם E2E שלא ירוצו**

**מהיכן ידעתי:** הבריף שורות 170–175 מכריז על היפוך ברירת-המחדל ומורה "מתקנים את `LoadingOrError.test.jsx` באותו קומיט". בדקתי מי עוד נועל את המחרוזת.

**מה נמדד** (`grep -rl "נסה שוב" src/ e2e/`, 21 קבצים; מתוכם):

| הקובץ | מה יש בו | ירוץ בלילה לפי הבריף? |
|---|---|:-:|
| `src/components/LoadingOrError.test.jsx` | הטענה על ברירת-המחדל | ✅ (מוזכר) |
| `src/components/layout/ProtectedRoute.test.jsx:65,78` | `getByRole('button', { name: 'נסה שוב' })` ×2 | ✅ ברגרסיה, **אך לא מוזכר** |
| `src/modules/02_customers/CustomerDetailsPage.projects.test.jsx:203` | `toHaveTextContent('נסה שוב')` | ✅ ברגרסיה, **אך לא מוזכר** |
| `e2e/hostesses.spec.js:217,398` · `e2e/load-failure-guards.spec.js:263` · `e2e/smart-match.spec.js` | טענות על הכפתור | ❌ **לא ירוץ — `test:e2e` אינו במחזור, ואינו ב-CI** |
| ‏8 קובצי-מקור (`LoginPage` · `UsersManagementPage` · `PermissionsMatrixPage` · `CustomersPage` · `CustomerFormDialog` · `CustomerDetailsPage` · `AuthContext` · `ProfileSettingsPage`) | המחרוזת **מקודדת בהם ישירות**, לא דרך ברירת-המחדל | — |

**למה זה חשוב:** היפוך ברירת-המחדל **אינו מסיים את העבודה** (8 מופעים קשיחים נשארים בזכר), ו**שובר שלוש בדיקות E2E שאף שלב בבריף אינו מריץ** — בדיוק דפוס-הכשל של `smoke-anchors` שהבריף כן תפס. התוכנית שלב 2 דורשת `npm run test:e2e` **וגם** `smoke`; הבריף הוריד את הראשון.

**נוסח מוצע (מחליף את סוף שורה 173, ומתווסף למחזור ③):**
> …ומתקנים **את כל מי שנועל את המחרוזת, לא רק את `LoadingOrError.test.jsx`**: `ProtectedRoute.test.jsx` · `CustomerDetailsPage.projects.test.jsx` · ושלוש בדיקות E2E (`hostesses.spec.js` · `load-failure-guards.spec.js` · `smart-match.spec.js`). **ובנוסף — 8 קבצי-מקור מקודדים את "נסה שוב" ישירות ואינם עוברים דרך ברירת-המחדל** (`grep -rl "נסה שוב" src/`); הם חלק מיישור-המגדר, לא נספחים לו.
> ⇒ **במחזור-המודול ③, בכל שלב שנגע ב-`src/components/` או ב-`src/lib/`: להריץ גם `npm run test:e2e` וגם `npm run gate`** — לא רק חבילת-היחידה ו-`smoke`. *(תוכנית §4 שלב 2, קריטריון-סיום ②③④.)*

---

### ה6 · ⚠️ **‏`npm run smoke` דורש שרת-פיתוח חי — והבריף מריץ אותו תשע פעמים בלי להפעיל אותו**

**מהיכן ידעתי:** הבריף שורה 207 עושה את `smoke` שער פר-מודול. קראתי את הסקריפט.

**מה נמדד** (`sed -n '1,25p' scripts/smoke.mjs`): `npm run smoke` = `node scripts/smoke.mjs`, שמריץ `playwright test e2e/smoke.spec.js` מול `http://localhost:5173`, ומחזיר **exit 3 — "השרת לא רץ על :5173"** או **exit 2 — "אין `E2E_CEO_*` ב-`.env.local`"**. ‏`.env.local` קיים ומחזיק את המפתחות; **השרת אינו רץ מעצמו.**

**נוסח מוצע (מתווסף לשלב 0, אחרי קריטריון-הסיום):**
> ⚙️ **לפני ה-`smoke` הראשון: הפעל את שרת-הפיתוח ברקע (`npm run dev`, פורט 5173) והשאר אותו חי כל הלילה.** בלעדיו `npm run smoke` מחזיר **exit 3** בכל מודול — וזו אינה בדיקה אדומה אלא בדיקה שלא רצה. **קודי-היציאה:** `0` תקין · `1` באג אמיתי · `2` בעיית-זהות · `3` אין שרת.

---

### ה7 · ⚠️ **רשימת ה-🔒 של התוכנית (§1ג) ושתיים משלוש פעולות-הפתיחה — לא הגיעו לבריף**

**מהיכן ידעתי:** הבריף (מחזור ①) לוקח רק את פעולת-הפתיחה הראשונה. קראתי את התוכנית לפני §4.

**מה נמדד** (`sed -n '96,107p'` ו-`sed -n '205,213p'` של התוכנית): שלוש פעולות-פתיחה קבועות — ① grep על מחרוזות מול `e2e/` ו-`*.test.*` *(יש בבריף)* · ② **סינון מחרוזות שאינן נראות למשתמשת** (שגיאות-מפתח כמו `useToast חייב להיקרא בתוך <ToastProvider>`) *(חסר)* · ③ **קריאת רשימת ה-🔒 מ-§1ג** *(חסר)*. ‏**§1ג מוסיף דבר שאין ב-§🔒 של `processes-approved`:** *"טבלאות ה-⑥ במ4/מ5/מ6/מ8 — **96 שורות-קופי מנוסחות שכבר אושרו** … הן **מקור** לשכבה, **לא מטרה לשכתוב**"*.

**למה זה חשוב:** בלי ③, סוכן-כתיבה יכול "לאפס" 96 שורות קופי שישי כבר אישר בשיחות Discovery — כלומר לבטל עבודה מאושרת בשם "לאפס, לא לערוך". ובלי ②, סוכנים ישקיעו זמן בשכתוב הודעות-מפתח שאיש לא רואה.

**נוסח מוצע (מתווסף למחזור-המודול, לפני ①):**
> ⓿ **שלוש פעולות-פתיחה, לפני שסוכן אחד משוגר** *(תוכנית §4)*: ① `grep` על כל מחרוזת שעומדת להשתנות בתוך `e2e/` ו-`*.test.*` · ② **סינון מחרוזות שאינן נראות למשתמשת** (שגיאות-מפתח למפתחים — אינן מטרה לשכתוב) · ③ **קריאת רשימת ה-🔒 של תוכנית §1ג** — שמונת המונחים הנעולים **וגם 96 שורות-הקופי המאושרות בטבלאות-⑥ של מ4/מ5/מ6/מ8: הן מקור לשכבה, לא מטרה לשכתוב.**

---

### ה8 · ⚠️ **‏305 תבניות-מחרוזת דינמיות — סיכון מדוד בתוכנית שלא הגיע לבריף**

**מהיכן ידעתי:** תוכנית §7-ג. **מה נמדד:** *"‏**305 תבניות-מחרוזת עם עברית + ביטוי מוטמע, ב-70 קבצים**… הודעת-הסיום ב-`ChecklistDialog.jsx:619` מורכבת מ-8 שברי-`SHORTFALL` בתוך JSX ומשנה צורה לפי יחיד/רבים"*, עם הוראה מפורשת: *"🚫 **לא לדחוף אותן ל-`onboardingCopy.js`** — הן נשארות במקומן ומשוכתבות שם"*. הבריף אינו מזכיר אותן.

**נוסח מוצע (מתווסף לשלושת המוקשים, כסייג למוקש ①):**
> ⚙️ **‏305 מחרוזות אינן טקסט פשוט — הן תבניות עם ביטוי מוטמע, ב-70 קבצים** *(תוכנית §7-ג)*. 🚫 **אינן עוברות ל-`onboardingCopy.js`** — משוכתבות במקומן, עם הביטוי שלם. **המקרה הקיצוני:** `ChecklistDialog.jsx:619` (שמונה שברים + יחיד/רבים) — סוכן שנוגע בו מחזיר לפני/אחרי מלא, לא מחרוזת.

---

### ה9 · ⚠️ **ארבעת מעריכי שלב 10 כותבים לאותו קובץ — בניגוד לכלל שהבריף עצמו מצטט**

**מהיכן ידעתי:** הבריף שורה 102 קובע "🚫 אל תשגר שני סוכנים על אותו קובץ (כלל-ברזל 16)". התוכנית שלב 10 קובעת: *"**מה הוא כותב:** סעיף-ורדיקט ב-`docs/plans/ui-copy-night-report.md`"* — ‏**לכל אחד מהארבעה.**

**נוסח מוצע (מתווסף לשלב 10):**
> ⚙️ **כל מעריך מחזיר את הממצאים **בתשובתו**, ואינו כותב לקובץ.** ‏`ui-copy-night-report.md` נכתב **רק על-ידך** — ארבעה סוכנים על קובץ אחד מפרים את כלל-ברזל 16 שאתה מפעיל על סוכני-הכתיבה.

---

### ה10 · ⚠️ **רשימת חמש-האמירות-השקריות אינה שלמה — שתיים נוספות יישארו על הדיסק**

**מהיכן ידעתי:** הבריף שורות 140–143 מונה חמש. חיפשתי את אותן עובדות במקומות נוספים.

**מה נמדד:**

| המקום | מה כתוב שם היום | מדוע שקרי |
|---|---|---|
| `docs/db_roadmap.md:393` (שורת-המצב של הטבלה) | *"shipped inside m11's own migration; **no new policy or index needed**"* + *"Its `default` is an OPEN product question"* | שתי עובדות שהוכרעו — מדיניות רביעית ו-`default false`. **הבריף מונה רק את שורה 287 ואת 289** |
| `docs/PROJECT_MASTER.md §4א` | *"**נבנה בתוך המיגרציה של מ11**"*, ובסוף הסעיף: *"~~ערך ברירת-המחדל~~ … **ההמלצה: דלוק**"* | הופרך ב-28⑤ ובהכרעת "כבוי". **הבריף מונה מ-§4א רק את "שלבים 0–5 בלבד"** |

**נוסח מוצע (מתווסף לרשימה בשורה 143):**
> ‏➕ **ועוד שתיים באותם קבצים:** `db_roadmap.md:393` (שורת-המצב של `notification_preferences` — *"no new policy… default is an OPEN question"*) · ו-`PROJECT_MASTER §4א` (*"נבנה בתוך המיגרציה של מ11"* + *"ההמלצה: דלוק"*). **⇒ שבע, לא חמש.**

---

### ה11 · ⚠️ **סוכן-כתיבה שמחזיר סתירה — אין לו מסלול**

**מהיכן ידעתי:** הבריף שורה 73 מחייב כל סוכן להחזיר "ציטוט של הכלל שהחיל", ושורה 219 מגדירה מתי **הסשן** עוצר. מה עושה **הסוכן** כשהוא עצמו נתקע — לא נאמר.

**למה זה חשוב במיוחד הלילה:** המדריך ייצא עם R1–R30 **וגם** 📐23 **וגם** מילון-מונחים; התנגשות בין R30 (אותו נוסח בכל המערכת) לבין 📐23 ("מה המשתמש צריך לעשות") היא ודאית ברמת מחרוזת בודדת. סוכן שמכריע לבד מייצר בדיוק את מאה-הקולות שהמוקש העליון נועד למנוע.

**נוסח מוצע (מתווסף למוקש ①):**
> ⚙️ **סוכן-כתיבה שנתקל בסתירה בין שני כללי-המדריך — מחזיר את שתי האפשרויות ואת המחרוזת, ואינו מכריע.** ההכרעה שלך, לפי המדריך, ונרשמת בו כתיקון-מדריך *(כדי שהסוכן הבא לא ייתקל בה שוב)*. **הלילה אינו נעצר בשביל זה** — עוצרים רק אם הסתירה היא בין שתי **הכרעות-ישי** רשומות.

---

### ה12 · ⚠️ **📐23 אינו ברשימת מה שחייב להיות במדריך-הסגנון**

**מהיכן ידעתי:** הבריף מצטט את המשפט עצמו בבלוק-הכוונה (שורה 33) — *"מה המשתמש צריך לעשות, ולא מה המערכת עושה"* — אבל מפרט המדריך (⑥ שלב 0, פריטים ①–⑤) מונה רק את R1–R30, החלטת-הלשון, מילון-המונחים, שלוש דוגמאות ומבחן-קבלה.

**מה נמדד:** הכרעה 28⑬ (07/09 14:1X) רושמת את המשפט הזה **ככלל ממוספר 📐23** וקובעת שהוא *"**מחייב את קובץ-הקופי**: משפט-הטמעה שמתאר מה המערכת עושה **נכשל**"*. ‏`PROJECT_MASTER §4ב` מצטט אותו אף הוא.

**נוסח מוצע (פריט ⑥ במפרט המדריך):**
> ‏⑥ **‏📐23 — כלל-הפתיחה של כל משפט-שכבה** *(הכרעה 28⑬, במילות ישי)*: **מה המשתמשת צריכה לעשות, לא מה המערכת עושה.** הפתיחה היא הפעולה, ה"למה" הוא הנימוק שאחריה. **משפט שמתאר את המערכת — נכשל, גם אם הוא נכון.**

---

### ה13 · ⚪ **גדר-היקף מול `PROJECT_MASTER §4ב` — התוכנית שמה 🚫, הבריף לא**

**מה נמדד:** תוכנית §0 שורות 57–58: *"חיים ב-`PROJECT_MASTER.md §4ב` כתוכנית נפרדת, לא כאן. 🚫 **אל תרחיב את התוכנית הזו אליהם** — היא נשארת על טקסט"*. הבריף שולח את הסשן לקרוא את **§4א**, ו-§4ב יושב שתי שורות מעליו ועוסק בביקורת *"נבנתה על-ידי מפתח"* עם מסקנות UCD — בדיוק פיתוי ההרחבה.

**נוסח מוצע (מתווסף לפריט 6 ברשימת-הקריאה):**
> ‏6. `docs/PROJECT_MASTER.md §4א` … 🚫 **ו-§4ב שמעליו הוא תוכנית נפרדת (‏UCD, זרימות ומסכים) — קרא כדי לא לסתור, ואל תיגע בה. הלילה הזה על טקסט בלבד.**

---

## §3 · דירוג

### 🔴 חוסם — לא להדביק לפני שאלה מטופלים

| | הממצא | למה חוסם |
|:-:|---|---|
| **1** | **ה1 · `dev` אינו מכיל את רשימת-הקריאה** (8 קבצים · 44 קומיטים לא-דחופים) | ההוראה הראשונה בבריף מוחקת מהדיסק את כל מה שהבריף מפנה אליו. **הלילה נגמר ב-22:05** |
| **2** | **ה2 · שער-ההקלדה אינו במודל-העצירות** | הסשן הצהיר "שתי עצירות" במבחן-הקבלה, ומגיע לפעולה בלתי-הפיכה שאסורה בלי הקלדת-ישי |
| **3** | **ה3 · ה-Stop hook** (אכיפות 0 · 0ד · 1–3) + סדר ④⑤ | חסימת סיום-תור בכל אחד מתשעת המודולים, והראשונה נופלת בעצירה 1 שישי ממתין לה |
| **4** | **ה4 · אסימוני-ההצבה של המיילים** מסווגים במצאי כ"תווית" | שינוי שובר שליחת-מייל אמיתית **בשקט**; אותו דפוס-נזק כמו `מנכ"ל` — ובלי אזהרה |
| **5** | **ט6 · `upsert` מול `update`** | הבריף מכריע הפוך מהתוכנית **בסעיף שנועד למנוע אלתור**; דורס העדפת-מייל בשקט (§7-ו) |
| **6** | **ה5 · `נסה שוב` בשבעה קובצי-בדיקה, שלושה מהם E2E שלא ירוצו** | שבירה שתתגלה רק אצל ישי — בדיוק הכשל ש-`smoke-anchors` תוקן בגללו |
| **7** | **ט3 · "מודול 6 מעביר `retryLabel`"** | מפנה את הניקוי למודול הלא-נכון; שלושת אתרי-הקריאה במ8 ומ9, בשלבים 5 ו-8 |

### 🟡 שיפור — כדאי לפני ההדבקה, לא חוסם

**ה6** (‏`smoke` צריך `npm run dev`) · **ה7** (§1ג + שתי פעולות-הפתיחה) · **ה10** (שבע אמירות שקריות, לא חמש) · **ה11** (סוכן שנתקל בסתירה) · **ה12** (📐23 במדריך) · **1ה** (קריטריון-סיום שלב 0 — ארבעה מול שמונה; להחזיר את `gate` ואת הרצת-CI-parity) · **ט7** (58 בלי סייג 28⑦) · **ט1** (שלוש מדיניות, לא ארבע) · **ט2** (37 קובצי-קוד — למחוק או להחליף ב-"41 קבצים מכילים את המחרוזת").

### ⚪ ניסוח בלבד

**ט4** (הייחוס ל-`writing-prompts.md`) · **ט5** ("§4.2 של התוכנית" ⇐ "של המחקר") · **ט8** (ארבעה מודולים ⇐ "ארבעת המודולים הבנויים") · **ט9** ("סטייה מוצהרת" — 28⑤ כבר עודכנה) · **ט10** (6–9 מיליון טוקנים — לסמן `הנחתי`) · **ה13** (גדר מול §4ב) · **ה9** (מי כותב את דוח-הבוקר) · שורה 53 אומרת "**הרשאה** רביעית" במקום "**מדיניות** רביעית" (שורה 90 נכונה) · שאלה 4 במבחן-הקבלה תובעת שם-טבלה שאינו בגוף הבריף.

### ✅ ומה שנמצא תקין — כי "אין ממצאים" חייב להיות מגובה

**‏23 מתוך 33 הטענות שנבדקו עמדו במדידה עצמאית**, כולל כל אלה שהתבקשתי לבדוק במפורש: `2,311` · `74/9/6` · `939/58/1,372/59.4%` · `onboarding_mode` · `users_write_ceo_only` · חמש שורות ה-`schema.sql` · `smoke-anchors.json:21` · `Hint.jsx` · `R27/R28` · `Nielsen 35%/75%` · `2+2` · ארבעת ציטוטי-ישי · ותאריך-הכנס. **מבחן-הקבלה בן עשר השאלות עבר במלואו** — אין בו שאלה שדורשת ניחוש. **`742px`/`977px`, `146`, `B13/B14`, `H1–H6` ו-`📐23` אינם מופיעים בבריף כלל** (‏`grep -c` = 0 לכל אחד) — אין מה לאמת בהם; לגבי 📐23 זו עצמה השמטה, ר' ה12.

---

## §4 · מה השתפר מהסבב הראשון — שבעת החוסמים, אחד-אחד

| | החוסם בסבב 1 | מה יש בבריף היום | הכרעה |
|:-:|---|---|:-:|
| **1** | **ח1 · `מנכ"ל` ערך-RLS, והפרומפט דוחף לניקוב-נקבה** | שורות 145–148: בלוק 🔴 מלא — חמש שורות `schema.sql` בשמן, "שינוי שלה שובר הרשאות בשקט", והסייג המפורש *"וזה חל גם כשמיישרים לשון-מגדר: שמות-תפקידים אינם פנייה למשתמשת"* | ✅ **נסגר** *(עם שתי אי-דיוקים שנולדו כאן — ט1, ט2)* |
| **2** | **ח2 · ענף מ-`dev` סותר את 28⑤, ומ11 חסר מיגרציה** | שורות 23–26: הסטייה מוצהרת, והמיגרציה עצמאית. ‏28⑤ עצמה עודכנה על הדיסק | ◐ **נסגר חצי.** חצי-המיגרציה נסגר; **חצי-התיעוד לא נבדק כלל** — איש לא שאל אם ה*מסמכים* קיימים על `dev`. **זה החוסם החדש ה1** |
| **3** | **ח4 · `worktree` בלי `node_modules` ו-`.env.local`** | שורות 98–102: איסור מפורש עם המדידה, **וחלופה מלאה** (חלוקת-קבצים בלעדית על עץ אחד) + תנאי כלל-16 | ✅ **נסגר, וטוב** |
| **4** | **ח3 · ⑤ אוסר את הכתיבות-לאחור שכלל 13 מחייב** | שורות 140–143: חריג ✅ מפורש, חמש אמירות עם נתיב ושורה, כל אחת עם הערת-✏️ | ◐ **נסגר חלקית** — הרשימה חסרה שתיים (ה10) |
| **5** | **ח5 · `smoke-anchors.json` נועץ את מחרוזת-"ממוין:"** | שורות 207–210: `smoke` נכנס למחזור, עם `e2e/smoke-anchors.json:21` מצוטט והנימוק *"בלי הרצה מפורשת, השבירה תתגלה רק אצל ישי"* | ✅ **נסגר, וזה התיקון הטוב בקובץ** |
| **6** | **שם העמודה לא נאמר, ושני שמות חיים** | שורות 149–151: `onboarding_mode` מחייב, `onboarding_hints` מסומן כשגוי עם `db_roadmap.md:287`, ו-*"במיגרציה זה בלתי-הפיך — אל תריץ לפני שיושר"* | ✅ **נסגר** |
| **7** | **"מ1+מ9" — הרגרסיה מופנית למודול הלא-נכון** | שורות 199–200: *"הרגרסיה ירוקה על **מ1** (`UsersManagementPage`) ועל **`src/components/ProfileSettingsPage`** — ⚠️ הוא ב-`src/components/`, לא במ9"* | ✅ **נסגר** |

**ומה שהסבב הראשון סימן 🟡 ולא נסגר עד היום:** ‏**ח7** (שתי פעולות-פתיחה + §1ג — ר' ה7) · **ח8א** (`upsert` — **נסגר הפוך**, ט6) · **ח8ב** (305 תבניות — ה8) · **ח10** (מיילים — ה4) · **ח12** (מדריכי-מיקרו + ה-Stop hook — ה3). ‏**ח6 · ח9 · ח11 נסגרו** (מפרט-המדריך ①–⑤ · `Hint.jsx` + מפתח חסר · `git restore` + בדיקת-ענף).

**חמישה מתוך שישה מהפריטים ה"צהובים" שלא נסגרו חזרו אליי כממצא עצמאי בסבב הזה** — כלומר הם לא היו רעש בסבב הראשון.

### ⚠️ והערה על הליג'ר עצמו

**הליג'ר טוען שהגרסה החדשה בת 269 שורות** (שורות 5–6 שלו). ‏`wc -l docs/plans/ui-copy-session-brief.md` ⇒ **285**, וכך גם בקומיט (`git show HEAD:… | wc -l`). ⇒ **מיפוי "איפה בחדשה" בכל 187 הפריטים מוסט**, ו"האימות המכני" רץ על גרסה שאינה זו שעל הדיסק. **הליג'ר עדיין שימושי כרשימת-כוונות; הוא אינו ראיה תקפה ל"אפס אובדן" בגרסה הנוכחית.** *(שלושת הדגלים שבסופו: #1 ו-#2 יושרו נכון לדעתי; **#3 — תאריך-הכנס — כבר נסגר:** `00_roadmap.md` ו-`CLAUDE.md` שניהם אומרים 15/10, והבריף צודק.)*

---

## §5 · הפקודות שהרצתי — מילה-במילה

```bash
git rev-parse --abbrev-ref HEAD
ls -la docs/plans/
wc -l docs/plans/ui-copy-session-brief.md
grep -n "^#\{1,4\} " docs/plans/ui-copy-and-onboarding-mode.md
grep -n "4\.1\|4\.2\|§4" docs/plans/ui-copy-and-onboarding-mode.md
grep -n "^#\{1,4\} " docs/plans/research-ui-copy-standards.md
grep -n "^#\{1,4\} " docs/plans/research-onboarding-content.md
sed -n '17,40p'  docs/plans/research-ui-copy-standards.md
sed -n '85,120p' docs/plans/research-ui-copy-standards.md
sed -n '132,167p' docs/plans/research-ui-copy-standards.md
grep -no "R[0-9]\{1,2\}" docs/plans/research-ui-copy-standards.md
grep -o  "R[0-9]\{1,2\}" docs/plans/research-ui-copy-standards.md | sort -u -V
sed -n '108,158p' docs/plans/ui-copy-and-onboarding-mode.md
sed -n '159,236p' docs/plans/ui-copy-and-onboarding-mode.md
sed -n '237,295p' docs/plans/ui-copy-and-onboarding-mode.md
sed -n '409,455p' docs/plans/ui-copy-and-onboarding-mode.md
sed -n '515,558p' docs/plans/ui-copy-and-onboarding-mode.md
sed -n '560,624p' docs/plans/ui-copy-and-onboarding-mode.md
sed -n '80,100p'  docs/plans/inventory-ui-copy.md
sed -n '2625,2645p' docs/plans/inventory-ui-copy.md
grep -n "1,372\|939\|59\.4" docs/plans/inventory-ui-copy.md
grep -c "lib/email" docs/plans/inventory-ui-copy.md
grep -n "lib/email" docs/plans/inventory-ui-copy.md | head -5
grep -rn "ממוין" docs/specs/module_05_logistics/screens-approved.md
grep -n "^#\{1,4\} \|⬜" docs/specs/module_05_logistics/screens-approved.md
grep -n "🗳️" docs/specs/module_11_reports/processes-approved.md
grep -n "🔒"  docs/specs/module_11_reports/processes-approved.md
awk 'NR>877 && /^\| \*\*28\*\*/{print NR": "$0}' docs/specs/module_11_reports/processes-approved.md
grep -n "נרקב" .claude/skills/_shared/writing-prompts.md docs/specs/module_11_reports/processes-approved.md
sed -n '170,175p;198,216p' docs/schema.sql
grep -n 'מנכ' docs/schema.sql
grep -n "^create policy" docs/schema.sql | awk -F: '$1>150 && $1<230'
grep -rl "CEO_ROLE_NAME" src/ e2e/ | wc -l
grep -rl 'מנכ"ל' src/ | wc -l
grep -rl 'מנכ"ל' src/ e2e/ | wc -l
grep -rn "CEO_ROLE_NAME *=" --exclude-dir=node_modules --exclude-dir=.git .
grep -rn "נסה שוב\|נסי שוב\|retryLabel" src/components/LoadingOrError.jsx
cat src/components/LoadingOrError.test.jsx
grep -rn "retryLabel" src/ | head -20
grep -rn "retryLabel" src/modules --include=*.jsx
grep -rn "נסה שוב" src/ e2e/ --include=*.test.jsx --include=*.test.js --include=*.spec.js
grep -rl "נסה שוב" src/ e2e/
node -e "const p=require('./package.json');console.log(JSON.stringify(p.scripts,null,1))"
sed -n '15,30p' e2e/smoke-anchors.json
grep -n "smoke-anchors\|sortLine\|spec" scripts/smoke.mjs
sed -n '1,25p' scripts/smoke.mjs
ls src/modules/01_auth/
find src/modules/01_auth -name "*.test.*" | wc -l
ls src/components/Hint.jsx
grep -n "vi.mock" src/components/ProfileSettingsPage.test.jsx
sed -n '280,295p' docs/db_roadmap.md
grep -n "onboarding" docs/db_roadmap.md
grep -n "default true\|Recommendation: \`default" docs/db_roadmap.md
grep -n "§4א\|## 4\|4א" docs/PROJECT_MASTER.md
grep -n "0–5\|0-5"  docs/PROJECT_MASTER.md
sed -n '302,345p' docs/PROJECT_MASTER.md
wc -l supabase/migrations/CLAUDE.md
sed -n '1,80p'   supabase/migrations/CLAUDE.md
sed -n '120,217p' supabase/migrations/CLAUDE.md
ls .claude/hooks/
node -e "const s=require('./.claude/settings.json'); console.log(JSON.stringify(s.hooks,null,1))"
grep -n "^# \|^## \|exit 2\|אכיפה" .claude/hooks/check-docs-updated.sh
sed -n '160,200p' .claude/hooks/check-docs-updated.sh
ls docs/micro_guides/
for n in ui-copy-styleguide.md ui-copy-candidates.md ui-copy-night-report.md ui-copy-session-brief.md ui-copy-and-onboarding-mode.md; do grep -rlF "$n" docs/PROJECT_MASTER.md .claude/skills/module-discovery/template.md .claude/skills/module-blueprint/template.md .claude/skills/module-build/SKILL.md .claude/skills/module-close/template.md; done
grep -n "node_modules\|\.local\|env" .gitignore
grep -o "^E2E_[A-Z_]*" .env.local | sort -u
sed -n '1,40p' src/lib/emailTemplates.js
git branch -a
git status --porcelain
git fetch origin
git cat-file -e origin/dev:docs/plans/ui-copy-and-onboarding-mode.md    # ולכל אחד משמונת הקבצים
git show origin/dev:docs/PROJECT_MASTER.md | grep -c "4א · שפת-הכתיבה"
git show origin/dev:docs/db_roadmap.md | grep -c "M11-1"
git rev-list --count origin/dev..HEAD
git rev-list --count origin/ishay/module-11-reports..HEAD
git diff --name-status origin/dev..HEAD
git diff --name-only origin/dev..HEAD -- src/ supabase/
git merge-base --is-ancestor origin/dev HEAD
ls docs/specs/*/screens-approved.md
ls src/modules/ ; find src -name "*.test.*" | wc -l ; ls e2e/*.spec.js | wc -l
grep -n "15/10\|01/10" docs/guides/00_roadmap.md CLAUDE.md
sed -n '204,245p' docs/plans/ui-copy-session-brief-ledger.md
git show HEAD:docs/plans/ui-copy-session-brief.md | wc -l
grep -n "חוסם\|^## \|^### " docs/plans/review-ui-copy-session-brief.md
sed -n '316,356p' docs/plans/review-ui-copy-session-brief.md
```

</div>
