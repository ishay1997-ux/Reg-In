<div dir="rtl">

# מסירה — לילה של שכתוב-הטקסטים, 08/09/2026 · סשן 1 → סשן 2

> **מה הקובץ הזה:** מסמך-מסירה **חי** בין סשנים של אותו לילה. **קובץ אחד לכל הלילה** — הסשן הבא
> מעדכן את §0 בסוף-דרכו ומוסיף שורת "ממשיך מ־", לא פותח קובץ חדש. **מצביע, לא מדביק:** התוכן חי
> בקבצים; כאן רק התמצאות + מה שהיה **רק בשיחה**. *(המדריך: `~/.claude/references/session-handoff-guide.md`.)*
> **נכתב:** 08/09/2026 23:3X, בסוף שלב 0, כשהחלון של סשן 1 עבר ~60% (הכרעת-ישי: לסובב לפני 80%).
> **ממשיך מ־:** אין (זה הראשון). 🔗 השרשרת: `ui-copy-session-brief.md` (הבריף הקפוא) → **הקובץ הזה** → סשן 2.
> **ממשיך מ־: סשן 1 (ראש `a859a1b`, 23:3X) — סשן 2 קיבל את המקל, ענה על מבחן-הקבלה מהדיסק, ופתח את שלב 2.**

---

## §0 · מצב-נוכחי — חותמת-טריות 09/09/2026 01:2X *(רק מה שזז הלילה; מה שנכון גם בעוד שבוע — בקבצים)*

| | |
|---|---|
| 🎯 **המקל אצל** | **סשן 3** — מרגע שסשן 2 סיים לקמט ולדחוף (09/09/2026 01:2X) ושלח את הודעת-המקל. **ראש-הענף = ההאש שהודעת-המקל נוקבת בו** (= `git log -1 origin/ishay/ui-copy-rewrite`; קומיט שלב 5 הוא `36aa9f3`, ואחריו קומיט-המסירה); הנכנס מאמת שוויון אחרי `git fetch`. **סשן 1 וסשן 2 אינם כותבים יותר.** *(היה: סשן 2 מ-08/09 23:5X.)* |
| **ענף** | `ishay/ui-copy-rewrite` (נחתך מ-`ishay/module-11-reports`), **נדחף**. קומיטים של הלילה: סשן 1 `c04e8cc` · `bc9c524` · `9d174f7` · `d081b54` · `a859a1b`; סשן 2 **שלב 2** `8a8005d` · **שלב 3** `aebde75` · **שלב 4** `1f53c91` · **שלב 5** `36aa9f3`. עץ נקי. |
| **שלב** | **שלב 0 ✅ · שלב 2 ✅ (00:3X) · שלב 3 ✅ (01:2X) · שלב 4 ✅ (02:0X) · שלב 5 ✅ (09/09/2026 01:2X) *(היה: 09/09/2026 00:5X).** ~~🛑 עצירה 2~~ **בוטלה** (ישי 09/09 00:0X: *"בטל עצירה מולי. עצירה רק שהכל מבוצע"*). **הצעד הבא הבטוח: שלב 6 — מ4 דיילות + Smart Match (269 מחרוזות · 4 מחרוזות-④ · 6 קובצי-lib · `screens-approved.md` עם 14 בלוקי-⑥); אחריו 7 (מ2+מ3) · 8 (מ5+מ9+מ1) · 9 · 10.** הבריף לכותב כבר כתוב: `C:/Users/ishay/AppData/Local/Temp/claude/C--Users-ishay-Reg-In/825404d6-3913-4712-9f10-7e3373935110/scratchpad/w7-module7.md` *(סקראצ'פד של סשן 2 — אם נמחק, §4 כאן נושא את תמציתו)*. אחריו: שלבים 4–10 בלי עצירה; העצירה היחידה — בסוף, לקבלה. |
| **המיגרציה** | `20260908221959_onboarding_mode` **הוחלה 22:2X** — הד-ההקלדה נפטר חד-פעמית (ר' §2). `schema.sql` §32 ו-`db_roadmap` M11-1/§10ב מעודכנים. |
| **שערים** | שלב 5 (האחרון): CI-parity **מלא** ירוק · build · eslint · prettier על הקבצים ששונו · `npm run smoke` ירוק · E2E finance + public-feedback ירוק. הכשל הקדם-ללילה ב-`customer-page.spec.js:130` (מונה-לשונית מול חלון-3-חודשים) עדיין קיים — לא של הלילה; `check:docs-structure` עדיין נופל על `cards-finance.md` של מ11 — לא שלנו. |
| **סוכנים** | **20 מתוך 50** נוצלו (שלב 0: 6 · שלב 2: 6 · שלב 3: 1 · שלב 4: 4 · שלב 5: 3 — Sonnet כולם; ~300–440K לכותב). **30 נותרו.** תוכנית: שלב 6 (מ4) 3 · שלב 7 (מ2+מ3) 3–4 · שלב 8 (מ5+מ9+מ1) 3 · שלב 9 2 · שלב 10 = 4 (Opus) ⇒ ~16, מרווח 14. ⚠️ שיגור עם פרומפט ארוך בגוף ההודעה נחסם ע"י מסנן-ההרשאות — הבריף לקובץ, שיגור בשורה אחת. |
| **שרת-dev** | פורט 5173 תפוס ע"י תהליך שאינו של סשן 2 (pid חי; `netstat`) — `preview_start reg-in-dev` עלה על 64342, ו-`npm run smoke` משתמש ב-5173 הקיים (`reuseExistingServer`). שניהם משרתים את עץ-העבודה החי. |
| **מה לא נעשה** | אף משפט-הסבר מעבר ל-`onboarding.self` (כלל ⑥) · `NO_PRIMARY_MSG` נשאר בזכר (C-01, שלב 7 עם מיגרציה) · `ConfirmDialog` ברירות-המחדל (S-06) · placeholder `שם פרטי ומשפחה` (S-05, שלב 8) · **שלוש שאלות-מדריך פתוחות לשלב 9** (נקודתיים בצמדי `label: value` מול R2 · כפתורי-פתיחה בשם-עצם `+ הוספת X` מול R12 · `— חובה` מותנה מול R6) — **לא להכריע בשלבים 6–8, לרשום עקבי עם מה שכבר נעשה** · `SmartMatchPage.jsx:364` "אנחנו ויתרנו עליה" (R7) — שלב 6 · הערות-קוד שמצטטות נוסחים ישנים — אינן טקסט-מסך. |

---

## §1 · 📖 רשימת-הקריאה של סשן 2 — לקרוא, לא לחפש (100 שורות כל פעם, לא grep)

**חובה, בסדר הזה, ולכל מקור מה לקחת ממנו:**
1. **`docs/plans/ui-copy-session-brief.md`** — כולו. **זו ההוראה.** לקחת: ששת הכללים · שלושת המוקשים · ⑤ האסור · ⑥ מחזור-המודול (כולל צעד ⑥ ממצאי-מבנה) · מודל-העצירות · ⑦ צורת-הפלט · ⑧ מה עושים אם המכסה נגמרת · **ועשר שאלות-הקבלה — ההודעה הראשונה עונה עליהן מהדיסק.**
2. **הקובץ הזה** — §2 (מה היה רק בשיחה) ו-§3 (מבחן-הקבלה הנוסף).
3. **`docs/plans/ui-copy-styleguide.md`** — כולו. **🔒 נעול ומאושר.** לקחת: §1 (הלשון — כולל ציווי-מול-אינפיניטיב ו"מי שמדברים עליה") · §2 (הכללים; ההבהרות תחת R30) · §3 (המילון; 3ד) · §4 (4 דוגמאות) · §5 (לא-נוגעים) · §8 (מה סוכן מחזיר). **כל סוכן-כתיבה מקבל את הקובץ הזה בגוף הבריף שלו.**
4. **`docs/plans/ui-copy-and-onboarding-mode.md`** — §4 (טבלת-המצב בראשו + שלבים 2–10, כל אחד עם קריטריון-סיום ודוגמה) · §7 (הסיכונים — א · ג · ד · ח · ט) · §1 (B1–B14 · H1–H6 · §1ג רשימת-🔒).
5. **`docs/plans/research-onboarding-content.md`** — §1 (שבעת סוגי-האמירה) · §3 (מה הופך הסבר לחסר-ערך) · §7 (חמש הדוגמאות). נדרש **לפני שלב 9**; לשלב 2 מספיק §1.
6. **`src/CLAUDE.md`** — לפני קוד: RTL · חמשת המעברים · RLS · SSOT. **`e2e/CLAUDE.md`** — לפני כל ספק-ראיות (דפוס §"ספק-הראיות": יירוט-רשת, אפס שורות, נמחק לפני הקומיט).
7. **`.claude/skills/_shared/writing-prompts.md`** — לפני שמבריפים סוכן (סוג-הסשן · מוקש-על אחד עם דרישת-ספירה · "כל עובדה ניתנת לערעור").
8. **`docs/plans/inventory-ui-copy.md`** — §1 · §3 · §4 · §5 (לא טבלת §2 — פותחים בה רק את בלוק המודול שעובדים עליו).

**🟡 כנראה לא תצטרך — פתח רק על פער אמיתי, ואמור שפתחת:** `research-ui-copy-standards.md` (הכללים כבר מועתקים למדריך; §2.2–2.3 רק אם מתלבטים על השכבה) · `processes-approved.md §🗳️ 28` (הכרעה 28 מיושמת; פתח רק אם משהו סותר) · `docs/specs/module_0*/screens-approved.md` (רק כשמחרוזת לא מובנת) · `docs/CLAUDE_CODE_LOG.md` (הרשומה של 08/09 היא סיכום-הלילה; לא נדרש מעבר ל-§2 כאן).

---

## §2 · 💬 מה שהיה רק בשיחה — ואין לו קובץ אחר

### מה ישי אמר — מילה-במילה, מתוארך, הכרעה מול רעיון

| מתי | מילותיו | סוג | מה זה מחייב |
|---|---|---|---|
| 22:2X | *"ואת המיגרציה ראיתי ישלך אישור ממני להריץ אין צורך שאקליד חד פעמי"* | **הכרעה** (חד-פעמית) | הד-ההקלדה נפטר **פעם אחת**; אינו תקדים. השער עומד לכל מיגרציה עתידית. רשום גם ב-`db_roadmap` §10ב. |
| 22:2X | *"מכיוון שהתחדשה מכסה ישלך מכסה של 50 סוכנים אופוס או סונאט לבחירתך שים לב המכסה לאורך כל הסשן אתה יכול להשתמש ב 1 גם מה שאתה מחליט"* | **הכרעה** | 50 = תקרה ללילה כולו; **6 נוצלו**. הבחירה — של הסשן, עם נימוק לפני כל גל. |
| 22:2X | *"אתה לא צריך עוד אישור ממני אבל רק שאראה תוכנית עם חלוקה משימות זמנים תקציב"* | **הכרעה** | התוכנית הוצגה (בהודעת עצירה 1); **אין עצירות נוספות מעבר ל-🛑 עצירה 2.** |
| 22:3X | *"מה למה מנכל בלבד? רצינו שיהיה לכל משתמשת אני טועה?"* | **שאלה, נענתה** | שתי דלתות (28⑨): כל משתמשת לעצמה בפרופיל; המנכ"ל לאחרות בניהול-משתמשים. **"מנכ"ל בלבד" חל רק על העמודה בניהול-משתמשים.** הוא קיבל. |
| 22:5X | *"ה62 אחוז הזה גם זה רק שאמינות כבוי אם לא טועה"* | **תיקון עובדתי** (נכון) | משקל-ההיענות תלוי בפרמטר **וגם** במתג-האמינות ⇒ כלל במדריך (דוגמה 3): **מספר שחי בפרמטר/מתג לא נכתב כקבוע במשפט-שכבה.** |
| 23:2X | *"אני מאשר."* | **הכרעה** | מדריך-הסגנון 🔒 נעול. עצירה 1 עברה. |
| 23:2X | *"חשבתי לפתוח לך עכשיו 3 סשנים בטנדביי ליתר ביטחון"* | **רעיון** (לא הכרעה) | המלצת סשן 1: **סשן אחד בכל רגע** — שני סשנים כותבים לאותו עץ מתנגשים (כלל-ברזל 16). סטנדביי קריאה-בלבד — לא מזיק, לא נדרש. |
| 09/09 00:0X | *"קבל שינוי. בטל עצירה מולי. עצירה רק שהכל מבוצע מאשר קבלה?"* | **הכרעה** (סשן 2) | 🛑 **עצירה 2 בוטלה.** אחרי קומיט שלב 2 — דיווח קצר וממשיכים מיד לשלבים 3–10 בלי להמתין. **העצירה היחידה שנותרה: בסוף, לקבלה** (קובץ-הבוקר + הוורדיקט של שלב 10). מודל-העצירות בבריף (⑤) נקרא מעכשיו: עצירה 1 עברה · עצירה 2 בוטלה · עוצרים רק על סתירה בין הכרעות / רגרסיה אדומה / דבר ללא תקדים. |

**וגם — הערה מסשן אחר (הובאה ע"י ישי 22:0X "כהמלצה בלבד"):** התקבלה במלואה — צעד ⑥ (ממצאי-מבנה) בבריף + השורה ב-`PROJECT_MASTER §4א` + החצי-שורה "אותו משפט יכול להיכנס לשני הקבצים". קומיט `c04e8cc`.

### מה קלוד המליץ / הכריע לבד (ביצוע-טכני) — וישי רשאי לעקוף
- שם-המיגרציה `…_onboarding_mode` בלי `m11_` (היא עצמאית). · שם-הפונקציה `saveOnboardingMode` (email אופציונלי = דלת-המנכ"ל) · `listOnboardingModes` · `AuthContext.updateOnboardingMode` (הפיכה חיה בלי `reload()`).
- **בניהול-משתמשים:** כשל בטעינת-הרמות מציג "—" (לא מתג "כבוי" כוזב); העמודה רק ל-`roleName === CEO_ROLE_NAME`.
- **עיצוב ה-Hint:** גבול-ימני טורקיז `border-r-2 border-teal-500`, טקסט `text-slate-600`, בלי מילוי (כלל-המילוי). **לא אושר במפורש** — ישי ראה את הצילום ואמר "אני מאשר" על המדריך; אם יעיר על המראה — זה שינוי-עיצוב שלו.
- **מבחן-הקבלה של המדריך רץ פעמיים (2+2 כותבים) ולא שלוש** — תשואה פוחתת; סבב 2 התכנס.
- **שלושת המונחים הדו-משמעיים** (§3ד במדריך): "רדום" = ירושה ממ2 · "חברה" = הלקוח למעט "ממוצע-החברה" · "פרויקט/אירוע" = ממצא-מבנה. **הוגשו כביצוע-טכני, לא כשאלה.**

### מה נשקל ונדחה
- **`update` במקום `upsert`** — נדחה (רוב המשתמשות בלי שורה ⇒ 0 שורות ⇒ "אין הרשאה" כוזב). התוכנית §7ו עודכנה.
- **לכתוב את ניהול-המשתמשים דרך `supabase.from` ישירות** כמו שאר המסך — נדחה; הטבלה של מ9 ⇒ `api.js` של מ9 (כלל 14).
- **סבב 3 של מבחן-הקבלה** — נדחה (הפערים שנותרו הם בדיוק מה ששני הכותבים דיווחו כחסר ⇒ תוקנו).
- **תיקון `cards-finance.md:483`** (השער) — נדחה: קובץ של אפיון-מ11, לא של הלילה.

### במה קלוד טעה — והנוסח המתוקן
- **"62% מהדירוג"** כקבוע בדוגמה 3 של המדריך ⇒ תוקן: המשקל הוא פרמטר + תלוי-מתג; המשפט אומר "חלק מהדירוג".
- **דיווח "gate exit 0"** על סמך סטטוס-המשימה, בעוד בתוך הלוג `gate exit=1` ⇒ הלקח: `cmd > log; echo exit=$? >> log` מדווח את המעטפת. **קרא את שורת-`exit=` בתוך הלוג.** (ובפעם השנייה — `exit=1` היה `check:docs-structure` הקיים-מראש.)
- **מחיקת ספק-הראיות באמצע ריצת-השער** ⇒ השער נפל על ENOENT. מוחקים ספק **לפני** שמשגרים שער או **אחרי** שסיים.

### דפוסים שהתגלו הלילה — ונכונים גם בעוד שבוע (כבר בבית קבוע, כאן רק מצביע)
- **מבחן-הקבלה של המדריך עובד:** סבב 1 מצא 3 כללים פתוחים שאף קריאה-חוזרת לא הייתה מוצאת (ציווי-מול-אינפיניטיב · שתי-סיבות · ספק-רשום). **בית:** `ui-copy-styleguide.md` §7.
- **דפוס ספק-הראיות** (צילומי-מסך מחוברים בלי כתיבה): `e2e/CLAUDE.md`. הספק של הלילה נמחק; מבנהו: `login` מ-`settings.spec.js` + `page.route('**/rest/v1/notification_preferences*')` שמחזיר את המטען.
- **הוכחת-RLS בטרנזקציה מגולגלת:** `do $$ … set_config('role','authenticated') + set_config('request.jwt.claims',…) … raise exception 'ROLLED_BACK_PROBE %' $$` — התוצאות נוסעות בהודעת-החריגה, וכלום לא נשמר. **בית:** `db_roadmap` §10ב (הרשומה של 08/09).

---

## §3 · 🔬 מבחן-הקבלה של סשן 2 — עונים מהדיסק, לפני פעולה ראשונה

**א. עשר השאלות של הבריף** (`ui-copy-session-brief.md` §✅) — כולן עדיין תקפות; ההודעה הראשונה עונה עליהן.

**ב. ארבע שאלות על מה שסשן 2 כתב לדיסק** (התשובות בקבצים, לא כאן):
1. **איך מפיקים צילום-"לפני" של מסך בלי `git stash` בזמן שכותבים עורכים את העץ — ואיפה המנגנון?** *(`CLAUDE_CODE_LOG.md` רשומת שלב 3 · הסקראצ'פד של סשן 2: `playwright.before.config.js` + `before-tree/`; אם הסקראצ'פד נמחק — הדפוס: `git archive <commit>` + junction ל-`node_modules` + `vite build` + `vite preview` על 4175)*
2. **מהי הסטייה המוצהרת של שלב 3 מהכרעת-סגירת-מ7, ומה ערוץ-האבחון שנשאר?** *(`module-7.md` §9 הרשומה של 09/09)*
3. **אילו שלוש שאלות-מדריך נפתחו בשלב 5 ומה עושים איתן בשלבים 6–8?** *(§0 כאן, "מה לא נעשה" · `module-8.md` §10)*
4. **כמה מחרוזות קשורות-`raise` יש במ6, ואיך סופרים אותן — לא מהמדריך?** *(`src/modules/06_projects/CLAUDE.md` הערת 09/09)*

**ג. מכני:** כל נתיב בקובץ הזה נפתח · `git status` נקי על `ishay/ui-copy-rewrite` · `docs/plans/ui-copy-candidates.md` (C-01…C-04) ו-`…structural-findings.md` (S-01…S-06) **קיימים** מאז שלב 2.

**ואחרי הכול: "האם המשימה ברורה? יש לך שאלות?" — שאלה רק על סתירה.**

---

## §4 · הצעד הבא הבטוח — שלב 6 · מ4 דיילות + Smart Match, איפוס מלא

- **התוכנית:** `ui-copy-and-onboarding-mode.md` §4 שלב 6 (הדוגמה: `שכבה 2 של Smart Match` ⇒ `העדפות של לקוחות`, `HostessViewCard.jsx`). **המצאי:** 269 מחרוזות · 4 מחרוזות-④ (`HostessViewCard` ×2 · `OverviewTab` · …, `inventory-ui-copy.md` §3 קבוצות ג׳/ד׳) · שישה קובצי-`lib` (`hostesses.js` · `smartMatch.js` · `sortAngles.js` · `shiftInvite.js` · `shiftEmails.js` · `assignmentActions.js` — האחרון כבר אופס בשלב 2).
- **מה כבר נעשה במ4 בלילה:** לשון-נקבה (שלב 2, W5) · כיתוב-המיון של מבט-העל אוחד למ6 (שלב 4) · `יש לפנות למנכ"ל להרשאה` (שלב 2).
- **הבריפים:** אין עדיין. לכתוב לפי הדפוס של `stage4-shared.md`/`stage5-shared.md` (שני הקבצים בסקראצ'פד של סשן 2; אם נמחקו — המבנה: רשימת-קריאה (בריף-משותף · מדריך · תוכנית §4 שלב · מחקר §1/§3 · `04_hostesses/CLAUDE.md` · `screens-approved.md` **לפי סעיפי-המשטח בלבד** · הקבצים) · סקופ = איפוס מלא · **מוקש-על אחד עם דרישת-ספירה** · בדיקות · צורת-דיווח §8). **חלוקה מוצעת (3 כותבים):** ① `OverviewTab` + `RepositoryTab` + `HostessesPage` + `api.js` + `lib/hostesses.js` · ② `HostessViewCard` + `HostessFormDialog` + `lib/shiftInvite.js` + `lib/shiftEmails.js` · ③ `SmartMatchPage` + `lib/smartMatch.js` + `lib/sortAngles.js` (+ `SmartMatchPane` אם קיים). **מוקש-על למ4:** `smartMatch.js` — *"0.62/0.38 הוא המחשה, לא קבוע"*: מספר שחי בפרמטר/מתג לא נכתב כקבוע (המדריך §4 דוגמה 3); ו-`מנכ"ל`/`אחראית משמרת`/סטטוסי-שיבוץ הם דאטה.
- **מחרוזות קשורות-`raise` במ4:** לספור לפני השיגור — `grep -rn "raise exception" supabase/migrations/*.sql` ⇒ להצליב מול `src/modules/04_hostesses` ו-`src/lib/hostesses.js`/`smartMatch.js` (הסקריפט הפשוט: לכל טקסט-raise, `grep -rlF` ב-`src`).
- **E2E נעוצים:** `e2e/hostesses.spec.js` · `e2e/smart-match.spec.js` (מסנני-menuitem על `שלחי את הקישור שוב`/`פתחי זימון חדש` — כבר מעודכנים) · `smoke-anchors.json` `hostesses.sidebarLink`.
- **הסדר (זהה לשלבים 3–5):** בריפים לקבצים → רשומת-הערכה (**תמדוד**) → שיגור בשורה אחת → צילומי-"לפני" מהבנייה הארכיונית (`before-tree` = `8a8005d`; מ4 לא השתנה מאז מלבד שלב 2/4 — לבנות מחדש מ-`{head}` אם רוצים "לפני" מדויק) → מיזוג עם grep-רוחבי → חבילה מלאה CI-parity + `smoke` + E2E של המודול → `↳ as-built` ב-`module-4.md` · רשמים · טבלת-§4 · LOG · STATUS → קומיט בפאת'ספק → דיווח קצר וממשיכים.
- **ואחרי מ4:** שלב 7 (מ2+מ3 — `NO_PRIMARY_MSG` דורש מיגרציה: להציע לישי כפריט-בוקר, לא להריץ) · שלב 8 (מ5+מ9+מ1; מ1 בלי בדיקות-יחידה חוץ מ-`UsersManagementPage` — מעבר ידני על ארבעת המסכים) · שלב 9 (מסמך-שלם + מלל-ההטמעה מ-`ui-copy-candidates.md` C-01…C-18 + מסך-הדגל + `ui-copy-night-report.md`) · שלב 10 (2+2 Opus).

## §5 · 🏃 פרוטוקול-המקל — הכרעת-ישי 08/09/2026 23:5X *("תחליטו איך הולך ויתנהל ההעברת המקל כי זה ישפיע לכל הלילה")*

**העיקרון האחד: המקל הוא קובץ, לא הודעה.** מי שכותב לעץ הוא מי ש-§0 אומר שהמקל אצלו — **ורק הוא.** כל השאר (סטנדביי) קראו, ענו על מבחן-הקבלה, וממתינים. שני סשנים שכותבים לאותו עץ מתנגשים (כלל-ברזל 16) — זו הסיבה לכלל.

**מתי מעבירים — הראשון מבין השניים:**
- **גבול-מודול** (קומיט ירוק) **וגם** ההקשר עבר **~750K** ⇒ מעבירים. *(הכרעת-ישי 08/09 23:5X: "55 מוגזם, 750 זו נקודת עצירה סבירה" — במקום ~55% שסשן 1 הציע. מודול = 45–75 דק'; מי שעובר 750K באמצע מודול מסיים אותו אם הוא מתחת ל-850K.)*
- **ההקשר עבר ~850K באמצע מודול** ⇒ לא ממשיכים: `git restore -- <הקבצים שנגעת בהם>` על מה שלא קומט (הבריף ⑧ — 🚫 לא `reset --hard`, 🚫 לא `revert`), רושמים ב-§0 *"מודול N נקטע ב-<שלב>, הוחזר; מתחילים מחדש"*, ומעבירים. **חצי-מודול על הדיסק אסור.**

**היוצא — שבעה צעדים, בסדר הזה, ואף אחד לא מדולג:**
1. `git status` נקי אחרי קומיט בפאת'ספק מפורש (🚫 `add -A`) · `git push`.
2. **§0 כאן:** חותמת-טריות חדשה · "המקל אצל: סשן N+1" · ראש-הענף · השלב הבא הבטוח · סוכנים נוצלו/נותרו · מה לא נעשה.
3. **§2 כאן — מה היה רק בשיחה:** ציטוטי-ישי **מילה-במילה ומתוארכים**, הכרעה מול רעיון · המלצות שקלוד נתן · מה נשקל ונדחה · במה טעה. *(הבדיקה: `git log` של הסשן מול הרשם — לא זיכרון.)*
4. **§3 כאן:** ארבע שאלות עובדתיות חדשות שהתשובה להן במה שהרגע נכתב לדיסק (מחליפות את הקודמות).
5. טבלת-המצב ב-`ui-copy-and-onboarding-mode.md` §4 · `CLAUDE_CODE_LOG.md` · `STATUS.md` (אחרונים — ה-Stop hook בודק mtime). קומיט + push שני.
6. **הודעת-המקל לנכנס** — דרך כלי-ההודעות-בין-סשנים אם זמין, אחרת ישי מעביר. **הנוסח קבוע:** *"המקל אצלך. ראש-הענף `<hash>`. קרא `docs/plans/ui-copy-handoff.md` §0 ואמת ש-`git rev-parse HEAD` שווה לו אחרי `git fetch`. ענה על §3 מהדיסק, ואז המשך משלב <N>. **תמדוד** — רשומת-הערכה ב-`01_estimation_log.md` לפני המחרוזת הראשונה."*
7. **עוצר.** לא נוגע בקובץ אחר. *(סשן שמת לפני צעד 6: הנכנס מזהה לפי §0 + `git log` — "המקל אצל" הוא הראיה, לא ההודעה.)*

**הנכנס — שלושה צעדים לפני מחרוזת ראשונה:**
1. `git fetch` · `git switch ishay/ui-copy-rewrite` · `git pull --ff-only` · `git rev-parse HEAD` == הראש שב-§0. **לא שווה ⇒ עצור ודווח** (או שהיוצא לא סיים, או שיש סשן שלישי שכותב).
2. קורא את רשימת-הקריאה §1 (**קריאה, לא grep**) ועונה על §3 + עשר שאלות-הבריף **בהודעה הראשונה**.
3. **"ממשיך" של ישי אינו נדרש להעברת-מקל** — הכרעתו 22:2X: *"אתה לא צריך עוד אישור ממני"*; העצירה היחידה שנותרה היא 🛑 עצירה 2 (סוף שלב 2). **הודעת-המקל + §0 = האישור.** ⚠️ אבל אם §0 אומר "ממתין לעצירה 2" — ממתינים לישי.

**מספור וסדר (הכרעת-ישי 08/09 23:5X — "אולי גם ניתן להם מספר?"):** כל סשן-סטנדביי נפתח עם מספר בפרומפט שלו — **S2 · S3 · S4 · S5 · S6** — והמקל עובר תמיד ל**מספר הפנוי הנמוך ביותר**. אין מתאם חי (סשן-מנהל היה מת עם החלון שלו ולוקח את הניתוב איתו); הסדר הקבוע + §0 הם המתאם. **הלוח היחיד של ישי:** §0 כאן + שורת "עודכן לאחרונה" ב-`STATUS.md`; כל סשן מדווח לו ישירות לפי הבריף ⑦, לא דרך סשן אחר.

**שלושה כללים שנמדדו בהעברה הראשונה (S1→S2, 09/09 00:1X):** ‏(א) מדריכי-המיקרו והיומן של מודול שנוגעים בו הם של **מי שמחזיק את המקל** — הסשן היוצא לעולם לא כותב אותם "כדי לשקט את ההוק" · (ב) הסשן היוצא, אחרי צעד 7, **נסגר** — ישי סוגר את הלשונית — כי סשן יוצא שנשאר פתוח מקבל חסימות-הוק על עבודת הנכנס בכל תור ואין לו דרך כשרה לספק אותן · (ג) הנכנס יודע שההוק שלו ידרוש `↳ as-built` בכל `module-N.md` שנגע בו — **לפני הקומיט, לא אחרי הדיווח**; כשכותבים במקביל עדיין רצים, שורה זמנית מסומנת ⏳ ומוחלפת בקומיט. ➕ **וכלל-מדידה:** ישי מדווח את גודל-ההקשר בצ'אט ("אתה עם 520 אלף") — הסשן מתייחס לזה כמדידה, לא להערכה עצמית. **סף-הסיבוב: 750K** (ישי 09/09 00:4X) — בגבול-המודול הראשון אחריו. 🔴 **וזה חל על כל סשן בשרשרת** (ישי 09/09 01:2X: *"תעביר הלאה שהם גם צריכים למדוד כדי שהעברת המקל תהיה בזמן המתאים"*): הנכנס עוקב אחרי גודל-ההקשר שלו לאורך כל הדרך — ישי מדווח מספרים בצ'אט; הסשן רושם אותם ב-§0 עם חותמת, ומתכנן את גבול-המודול שאחרי 750K **מראש**, לא כשמגלים. ⚠️ **ודוחות-סוכנים שרצים ברקע מגיעים רק לסשן ששיגר אותם** — לפני העברה, כל דוח שטרם מוזג נכתב לקובץ בסקראצ'פד ומצוין ב-§0, אחרת הוא אובד.

🔴 **תמדוד** (ישי 09/09 00:4X: *"מזכיר תמדוד לא לשכוח — להגיד גם בהעברת המקל"*): לפני כל שלב — רשומה חדשה ב-`docs/guides/01_estimation_log.md` §2 (הערכה + חיזוי בר-הפרכה, **לפני** השיגור); בקומיט — סגירתה (זמן-קיר · טוקנים לסוכן · הפער והסיבה). **הודעת-המקל נושאת את המילה "תמדוד"** — הנכנס פותח רשומה לפני המחרוזת הראשונה שלו.

**סטנדביי:** סשן שקיבל את פרומפט-ההדבקה, קרא, ענה על מבחן-הקבלה — ו**לא קיבל הודעת-מקל**: ממתין. אסור לו לכתוב, לקמט, או להריץ סוכנים. מותר לו לקרוא ולהתכונן (למשל להכין את רשימת-הקבצים של המודול הבא). כשהמקל מגיע — צעד 1 של הנכנס מחדש (הראש זז מאז שקרא).

**שרשרת:** כל סשן מוסיף בראש הקובץ שורת **"ממשיך מ־: סשן N (ראש `<hash>`, HH:MM)"** מתחת לקודמת — כך רואים את כל הלילה בחמש שורות.

---

## §6 · 🌙 איך הלילה מתנהל — מה שסוכם עם ישי, ואיפה כל חלק רשום

**זה הסיכום שהיה רק בצ'אט (הודעת עצירה 1, 08/09 23:1X) — נכתב כאן בהוראתו: *"דברים שהיו רק בשיחה חשוב שיהיו במסמך… כל מה שסיכמנו ואיך הולך להיות הלילה והתוכנית"*.**

**חמישה עקרונות-הפעלה, כולם הכרעות-ישי:**
1. **שתי עצירות בלילה כולו — ✅ עצירה 1 עברה; נותרה 🛑 עצירה 2 (סוף שלב 2).** מעבר לה: דיווח קצר בצ'אט אחרי כל מודול **בלי להמתין** (צילום לפני/אחרי + ≤10 שורות), ועצירה רק על סתירה בין הכרעות שלו · רגרסיה אדומה שלא מתיישרת · דבר שמשתמשת תראה ואין לו תקדים. *(הבריף §מודל-העצירות · ישי 22:2X "אתה לא צריך עוד אישור ממני".)*
2. **קובץ-בוקר אחד** — `docs/plans/ui-copy-night-report.md` (שלב 9–10): הוורדיקט של 2+2 ראשון, ואז כל הלפני/אחרי לפי מודול. זה מה שישי קורא בבוקר, לא הצ'אט.
3. **תקציב: 50 סוכנים ללילה, Sonnet לכתיבה, Opus לביקורת (שלב 10).** הסשן מחליט כמה ומנמק לפני כל גל. **6 נוצלו בשלב 0.**
4. **שרשרת-סשנים בסטנדביי, מקל = קובץ** (§5). ישי פותח סשנים מראש עם פרומפט-הסטנדביי (למטה); רק אחד כותב.
5. **מודול = קומיט אחד** רק אחרי רגרסיה ירוקה + `npm run smoke`; מודול שנקטע — `git restore`, לא חצי.

**התוכנית — כפי שהוצגה לישי ואושרה (23:1X; "אני מאשר" 23:2X):**

| שלב | מה | סוכנים (Sonnet אלא אם צוין) | זמן משוער | קומיט |
|---|---|---|---|---|
| ✅ 0 | המנגנון + המדריך | 6 (מילון · בדיקות-בסיס · 2+2 כותבים) | 22:00–23:20 (בפועל) | `bc9c524` |
| **2** | רכיבים משותפים + **לשון-נקבה בכל המערכת** (74–100 מחרוזות; 22 קובצי-`נסה שוב`) → רגרסיה מלאה | ~6 כותבים בחלוקת-קבצים בלעדית | ~75 דק' | 1 → 🛑 **עצירה 2** |
| 3 | מ7 מסך-הבית (35 מחרוזות) | 2 | ~30 דק' | 1 |
| 4 | מ6 פרויקטים (300 + `lib`; איחוד "ממוין:" — 2 מתאחדים, 3 נשארים באותה תבנית) | 4 | ~75 דק' | 1 |
| 5 | מ8 כספים (304; מעבר-H2 פר-משפט) | 4 | ~75 דק' | 1 |
| 6 | מ4 דיילות (269) | 3 | ~60 דק' | 1 |
| 7 | מ2 + מ3 (378) | 4 | ~60 דק' | 1 |
| 8 | מ5 + מ9 + מ1 (273; מעבר ידני על מסכי מ1) | 4 | ~60 דק' | 1 |
| 9 | מסמך-שלם · **מלל-ההטמעה** (מ-`candidates.md`) · מסך-הדגל · קובץ-הבוקר | 2 | ~60 דק' | 1 |
| 10 | עין טרייה 2+2 (**Opus**) + מיזוג — כלל-ההחלטה 2+2/3+3 בתוכנית §4 שלב 10 | 4–6 | ~45 דק' | 1 |
| | **סה"כ** | **~35–41 מתוך 50** (רזרבה ~10) | סיום ~07:00–08:00 | |

**הזמנים הם הערכה** — מה שמכתיב אותם הוא הרגרסיה המלאה אחרי כל מודול (CI-parity ≈ 3.5 דק' + smoke ≈ 1 דק' + תיקוני-בדיקות), לא הכתיבה. **חריגה מהתקציב ⇒ אומרים לישי לפני, לא אחרי.** *(`docs/guides/01_estimation_log.md` §1 — גורמי-הסיכון המדודים לכל שורה.)*

**פרומפט-הסטנדביי** (מה שישי מדביק בכל סשן-המתנה; ההודעה שמעירה אותו היא הודעת-המקל של §5):
```
אתה סשן-סטנדביי מספר S<N> בלילה שכתוב-הטקסטים של REG-IN (08/09/2026). המקל אינו אצלך עד שתקבל הודעת-מקל
(מסשן אחר או מישי) עם האש של ראש-הענף. עד אז: 🚫 לא כותב, לא מקמט, לא משגר סוכנים.
עכשיו: קרא במלואם, שורה-שורה ולא בחיפוש — docs/plans/ui-copy-session-brief.md (הבלוק שבין ה-```
וכל מה שהוא מפנה אליו) · docs/plans/ui-copy-handoff.md (כולו; §5 = איך המקל עובר, §6 = התוכנית) ·
docs/plans/ui-copy-styleguide.md (🔒). טען את הסקיל הגלובלי ui-copy-and-onboarding-levels; בסתירה —
הבריף גובר. ההודעה הראשונה שלך: התשובות לעשר שאלות-הבריף + ארבע השאלות ב-§3 של המסירה, מהדיסק —
ואז "ממתין למקל". כשהמקל יגיע: git fetch · pull --ff-only · אמת HEAD == ההאש שבהודעה · קרא שוב את
§0 של המסירה (הוא זז מאז שקראת) · והמשך מהשלב שכתוב שם.
```

</div>
