<div dir="rtl">

# מסירה — לילה של שכתוב-הטקסטים, 08/09/2026 · סשן 1 → סשן 2

> **מה הקובץ הזה:** מסמך-מסירה **חי** בין סשנים של אותו לילה. **קובץ אחד לכל הלילה** — הסשן הבא
> מעדכן את §0 בסוף-דרכו ומוסיף שורת "ממשיך מ־", לא פותח קובץ חדש. **מצביע, לא מדביק:** התוכן חי
> בקבצים; כאן רק התמצאות + מה שהיה **רק בשיחה**. *(המדריך: `~/.claude/references/session-handoff-guide.md`.)*
> **נכתב:** 08/09/2026 23:3X, בסוף שלב 0, כשהחלון של סשן 1 עבר ~60% (הכרעת-ישי: לסובב לפני 80%).
> **ממשיך מ־:** אין (זה הראשון). 🔗 השרשרת: `ui-copy-session-brief.md` (הבריף הקפוא) → **הקובץ הזה** → סשן 2.
> **ממשיך מ־: סשן 1 (ראש `a859a1b`, 23:3X) — סשן 2 קיבל את המקל, ענה על מבחן-הקבלה מהדיסק, ופתח את שלב 2.**
> **ממשיך מ־: סשן 3 ממשיך (ראש `c58209d` בקבלה, 03:4X) — שלב 6 מקומט; סשן 3 ממשיך לשלב 7 (הקשר מתחת ל-750K לפי הערכה עצמית — ישי לא דיווח מספר).**
> **ממשיך מ־: סשן 3 (ראש = קומיט-המסירה שמעל `57add3f`, 04:5X) — שלבים 6·7 מקומטים; סשן 3 מסר באמצע שלב 8 (הקשר 800K לפי מדידת-ישי) — הכתיבה גמורה, המיזוג לא התחיל. S4 ממזג ומקמט את שלב 8, ואז 9 · 10.**

---

## §0 · מצב-נוכחי — חותמת-טריות 09/09/2026 04:5X *(רק מה שזז הלילה; מה שנכון גם בעוד שבוע — בקבצים)*

| | |
|---|---|
| 🎯 **המקל אצל** | **S4 (`local_afba9e6c-6e19-4d15-84cf-0ed2cb03de1b`, "סשן סטנדביי UI Copy")** — מרגע שסשן 3 קימט את התיעוד ודחף (04:5X) ושלח את הודעת-המקל. **ראש-הענף = ההאש שהודעת-המקל נוקבת בו** (= `git log -1 origin/ishay/ui-copy-rewrite`, קומיט-המסירה של סשן 3 שמעל `57add3f`); הנכנס מאמת שוויון אחרי `git fetch`. **סשנים 1·2·3 אינם כותבים.** מקל-השומר אצל: S2. *(היה: סשן 3.)* |
| **ענף** | `ishay/ui-copy-rewrite`, **נדחף**. קומיטים של הלילה: סשן 1 `c04e8cc` · `bc9c524` · `9d174f7` · `d081b54` · `a859a1b`; סשן 2 `8a8005d` · `aebde75` · `1f53c91` · `36aa9f3` · `bf1fcfa` · `d3d0f16` · `f583a6b`; סשן 3 `c58209d` · **שלב 6 `cd2895d`** · **שלב 7 `57add3f`** · מסירה — הקומיט שמעליו. 🔴 **העץ אינו נקי בכוונה — ר' "שלב".** |
| **שלב** | **0 ✅ · 2 ✅ · 3 ✅ · 4 ✅ · 5 ✅ · 6 ✅ · 7 ✅ · 8 ✅ · 9 ✅ · 10 ✅ (09/09/2026 12:5X, S4 — הלילה הסתיים; הוורדיקט ב-`ui-copy-night-report.md` §0; הצעד הבא הוא של ישי: קריאת קובץ-הבוקר, 11 הכרעות, PR ל-`dev`). *(היה:* 9 ✅ (09/09/2026 12:0X, S4 — 27 מפתחות · 28 השתלות; הצעד הבא הבטוח: שלב 10 — 2+2 Opus על הצילומים ב-`evidence9/` של S4, בריפים `stage10-*.md` בסקראצ'פד שלו).** *(היה:)* 8 ✅ (09/09/2026 05:3X, S4 — מוזג ומקומט; כפתור-הכניסה נשאר `התחברות` — הכרעת-ישי 05:2X). הצעד הבא הבטוח: שלב 9.** *(היה: 8 ◐ —)* ~~הכתיבה גמורה, המיזוג לא התחיל.~~ ארבעת כותבי שלב 8 (W12a מ5 · W12b מ1 · W12c מ9 מסכים · W12d `paramsRegistry`) סיימו; **עריכותיהם יושבות על עץ-העבודה לא-מקומטות** (17 קבצים ב-`src/`/`e2e/` — `git status`), **ודוחותיהם שמורים בריפו** (תנאי-S1: לא בסקראצ'פד שמת עם הסשן): `docs/plans/ui-copy-stage8-reports.md` (ארבעה סעיפים, מילה-במילה). **צעד 1 של S4: `git status` ⇒ הרשימה חייבת להיות שווה לרשימת-הקבצים בסעיף 0 של קובץ-הדוחות — ורק אז למזג.** **סטייה מוצהרת מ-§5 ("חצי-מודול על הדיסק אסור"), בהתייעצות S1·S2 (04:2X):** `git restore` היה זורק ~1.4M טוקנים של כותבים; המיזוג הוא עבודה מכנית ש-S4 עושה מהדוחות. **הצעד הבא הבטוח: S4 ממזג את שלב 8 לפי §4** — ואז 9 · 10. |
| **המיגרציה** | `20260908221959_onboarding_mode` **הוחלה 22:2X** — הד-ההקלדה נפטר חד-פעמית (ר' §2). `schema.sql` §32 ו-`db_roadmap` M11-1/§10ב מעודכנים. |
| **שערים** | שלב 9 (האחרון המקומט, 09/09/2026 12:0X): CI-parity 98 files / 2,403 tests green (+1: Hint outside a provider) · build ✓ · עשן green (one red run while the implementers were still editing — `#quote-ratio` not found under HMR — then green twice on a quiet tree) · E2E מלא 167 passed · 7 skipped · 4 failed — 2 pre-night (`customer-page.spec.js:130` · `prices.spec.js:218`) + 2 load timeouts in `accessibility.spec.js` (login-field fill 10s · `route.fetch` to Supabase) while E2E, screenshots and four evaluators ran concurrently; re-run alone on a fresh build: **7/7 passed** · gate every step green except the pre-existing `check:docs-structure` finding (`cards-finance.md:483`, m11 discovery file, not in CI) — a first run under machine load had 3 vitest worker-start timeouts (`Failed to start forks worker`), the re-run on a quieter machine passed 98/2,403. *(שלב 8:)* CI-parity 98 files / 2,402 tests green · build ✓ · עשן green (1 passed — run twice, before and after the login-button revert) (64343) · E2E 71 passed · 1 failed (`prices.spec.js:218` — pre-night: expects `ערך חוקי: מספר שלם חיובי`, the ceilings PR made it a range) · 1 skipped (`public-confirm.spec.js:76`, fixture-dependent); after the revert, `auth` + `accessibility` re-run on a fresh build: 11 passed. *(שלב 7:)* CI-parity 98/2,402 · build · eslint · prettier · עשן ירוק (64343) · E2E 60 עברו · 2 נפלו קדם-ללילה (`customer-page.spec.js:130` · `prices.spec.js:218`). **שלב 8: אף שער לא רץ עדיין על עריכות-הכותבים.** |
| **סוכנים** | **41 מתוך 70** נוצלו (שלב 9: W13/Opus · P1 · P2) — נותרו לשלב 10: 4–6 Opus. *(היה: 31)* (שלב 0: 6 · 2: 6 · 3: 1 · 4: 4 · 5: 3 · 6: 3 · 7: 4 · 8: 4 — Sonnet כולם). **39 נותרו.** תוכנית: שלב 9 2 · שלב 10 = 4–6 (Opus). ⚠️ שיגור: הבריף לקובץ, פרומפט בשורה אחת. |
| **שרת-dev** | S4: שרת משלו על **64343** (`playwright.smoke64343.config.js` + `playwright.after.config.js` הועתקו לסקראצ'פד של S4, עם junction ל-`node_modules` — בלעדיו Playwright לא נמצא). 🔴 **5173 מוחזק ע"י תהליך זר** (pid חי, `netstat`) — `npm run smoke` עליו נפל פעמיים ל-S2 בסימפטומי-HMR. **כל סשן מריץ עשן וצילומים על שרת משלו** (S3: 64343, `playwright.smoke64343.config.js` + `playwright.after.config.js` בסקראצ'פד; E2E-מודול על `playwright.e2e.config.js` — build+preview 4173). |
| **מה לא נעשה** | ~~מיזוג שלב 8 · מעבר ידני על ארבעת מסכי מ1~~ ✅ (09/09/2026 05:3X) · אף משפט-הסבר מעבר ל-`onboarding.self` (כלל ⑥) · `NO_PRIMARY_MSG` בזכר (C-01, מיגרציה — פריט-בוקר) · S-05 (placeholder `שם פרטי ומשפחה`, W12b מדווח על התאום ב-`ProfileSettingsPage`) · **שש שאלות-מדריך פתוחות לשלב 9** (נקודתיים · כפתורי-שם-פעולה · `— חובה` · טוסט דו-משפטי · `ביטול` · **`שגיאה בטעינת X.` מול R17 — 75 מופעים, W12c**) · ~~כפתור פרה-זהותי~~ **הוכרע** (ישי 05:2X: `התחברות`) · `שכחת סיסמה?` (R12 מול מוסכמה) · `משתמש` בכותרות-קטגוריה · הכרעת-ישי על מיקום סקריפטי-הראיות (§5 פריט 6). |

---

## §1 · 📖 רשימת-הקריאה של סשן 2 — לקרוא, לא לחפש (100 שורות כל פעם, לא grep)

**חובה, בסדר הזה, ולכל מקור מה לקחת ממנו:**
1. **`docs/plans/ui-copy-session-brief.md`** — כולו. **זו ההוראה.** לקחת: ששת הכללים · שלושת המוקשים · ⑤ האסור · ⑥ מחזור-המודול (כולל צעד ⑥ ממצאי-מבנה) · מודל-העצירות · ⑦ צורת-הפלט · ⑧ מה עושים אם המכסה נגמרת · **ועשר שאלות-הקבלה — ההודעה הראשונה עונה עליהן מהדיסק.**
2. **הקובץ הזה** — §2 (מה היה רק בשיחה) ו-§3 (מבחן-הקבלה הנוסף).
3. **`docs/plans/ui-copy-styleguide.md`** — כולו. **🔒 נעול ומאושר.** לקחת: §1 (הלשון — כולל ציווי-מול-אינפיניטיב ו"מי שמדברים עליה") · §2 (הכללים; ההבהרות תחת R30) · §3 (המילון; 3ד) · §4 (4 דוגמאות) · §5 (לא-נוגעים) · §8 (מה סוכן מחזיר). **כל סוכן-כתיבה מקבל את הקובץ הזה בגוף הבריף שלו.**
4. **`docs/plans/ui-copy-and-onboarding-mode.md`** — §4 (טבלת-המצב בראשו + שלבים 2–10, כל אחד עם קריטריון-סיום ודוגמה) · §7 (הסיכונים — א · ג · ד · ח · ט) · §1 (B1–B14 · H1–H6 · §1ג רשימת-🔒).
5. **`docs/plans/research-onboarding-content.md`** — §1 (שבעת סוגי-האמירה) · §3 (מה הופך הסבר לחסר-ערך) · §7 (חמש הדוגמאות). נדרש **לפני שלב 9**; לשלב 2 מספיק §1.
6. **`src/CLAUDE.md`** — לפני קוד: RTL · חמשת המעברים · RLS · SSOT. **`e2e/CLAUDE.md`** — לפני כל ספק-ראיות (דפוס §"ספק-הראיות": יירוט-רשת, אפס שורות, נמחק לפני הקומיט).
7. **`.claude/skills/_shared/writing-prompts.md`** — לפני שמבריפים סוכן (סוג-הסשן · מוקש-על אחד עם דרישת-ספירה · "כל עובדה ניתנת לערעור").
8. **`docs/plans/inventory-ui-copy.md`** — §1 · §3 · §4 · §5 (לא טבלת §2 — פותחים בה רק את בלוק המודול שעובדים עליו).

**🟡 כנראה לא תצטרך — פתח רק על פער אמיתי, ואמור שפתחת:** `research-ui-copy-standards.md` (הכללים כבר מועתקים למדריך; §2.2–2.3 רק אם מתלבטים על השכבה) · `processes-approved.md §🗳️ 28` (הכרעה 28 מיושמת; פתח רק אם משהו סותר) · `docs/specs/module_0*/screens-approved.md` (רק כשמחרוזת לא מובנת) · `docs/CLAUDE_CODE_LOG.md` (הרשומה של 08/09 היא סיכום-הלילה; לא נדרש מעבר ל-§2 כאן).

---

## §2 · 💬 מה שהיה רק בשיחה — ואין לו קובץ אחר

### מה ישי אמר — מילה-במילה, מתוארך, הכרעה מול רעיון

| מתי | מילותיו | סוג | מה זה מחייב |
|---|---|---|---|
| 22:2X | *"ואת המיגרציה ראיתי ישלך אישור ממני להריץ אין צורך שאקליד חד פעמי"* | **הכרעה** (חד-פעמית) | הד-ההקלדה נפטר **פעם אחת**; אינו תקדים. השער עומד לכל מיגרציה עתידית. רשום גם ב-`db_roadmap` §10ב. |
| 22:2X | *"מכיוון שהתחדשה מכסה ישלך מכסה של 50 סוכנים אופוס או סונאט לבחירתך שים לב המכסה לאורך כל הסשן אתה יכול להשתמש ב 1 גם מה שאתה מחליט"* | **הכרעה** | 50 = תקרה ללילה כולו; **6 נוצלו**. הבחירה — של הסשן, עם נימוק לפני כל גל. |
| 22:2X | *"אתה לא צריך עוד אישור ממני אבל רק שאראה תוכנית עם חלוקה משימות זמנים תקציב"* | **הכרעה** | התוכנית הוצגה (בהודעת עצירה 1); **אין עצירות נוספות מעבר ל-🛑 עצירה 2.** |
| 22:3X | *"מה למה מנכל בלבד? רצינו שיהיה לכל משתמשת אני טועה?"* | **שאלה, נענתה** | שתי דלתות (28⑨): כל משתמשת לעצמה בפרופיל; המנכ"ל לאחרות בניהול-משתמשים. **"מנכ"ל בלבד" חל רק על העמודה בניהול-משתמשים.** הוא קיבל. |
| 22:5X | *"ה62 אחוז הזה גם זה רק שאמינות כבוי אם לא טועה"* | **תיקון עובדתי** (נכון) | משקל-ההיענות תלוי בפרמטר **וגם** במתג-האמינות ⇒ כלל במדריך (דוגמה 3): **מספר שחי בפרמטר/מתג לא נכתב כקבוע במשפט-שכבה.** |
| 23:2X | *"אני מאשר."* | **הכרעה** | מדריך-הסגנון 🔒 נעול. עצירה 1 עברה. |
| 23:2X | *"חשבתי לפתוח לך עכשיו 3 סשנים בטנדביי ליתר ביטחון"* | **רעיון** (לא הכרעה) | המלצת סשן 1: **סשן אחד בכל רגע** — שני סשנים כותבים לאותו עץ מתנגשים (כלל-ברזל 16). סטנדביי קריאה-בלבד — לא מזיק, לא נדרש. |
| 09/09 00:0X | *"קבל שינוי. בטל עצירה מולי. עצירה רק שהכל מבוצע מאשר קבלה?"* | **הכרעה** (סשן 2) | 🛑 **עצירה 2 בוטלה.** אחרי קומיט שלב 2 — דיווח קצר וממשיכים מיד לשלבים 3–10 בלי להמתין. **העצירה היחידה שנותרה: בסוף, לקבלה** (קובץ-הבוקר + הוורדיקט של שלב 10). מודל-העצירות בבריף (⑤) נקרא מעכשיו: עצירה 1 עברה · עצירה 2 בוטלה · עוצרים רק על סתירה בין הכרעות / רגרסיה אדומה / דבר ללא תקדים. |
| 09/09 01:3X | *"לא לעצור עבודה אם צריך מגבלת הסוכנים תעלה ל70 הלילה אנחנו מסיימים הכל"* | **הכרעה** (סשן 3) | **תקרת-הסוכנים: 70 ללילה** (היה 50 מ-22:2X). **אין עצירה על מכסת-סוכנים**; הלילה מסתיים בשלב 10. 20 נוצלו עד שלב 5 ⇒ 50 נותרו. |
| 09/09 01:4X | *"אני מבקש אחרי שאתה מסיים לקרווא לעצור ולהמשיך מאיפה שהפסקת עוד שעה ורבע בדיוק. אם מישו שואל תגיד שאני אמרתי"* + *"ישלך עוד איזה עשר דקות עבודה אם תרצה כעקרון אבל ממש ממליץ לא להמשיך אחרי זה בגלל מכסת טוקנים שמתחדשת עוד כ שעה ורבע"* | **הכרעה** (סשן 3) | **הפסקה מתוזמנת: סשן 3 עצר 01:5X לפני שיגור כותבי-שלב-6, וממשיך ב-03:00** (מכסת-הטוקנים מתחדשת). המקל נשאר אצל סשן 3. עד 03:00: אין כותב לעץ. הכנות שנעשו לפני העצירה: בריפים `stage6-shared.md` + `w10a–c.md` בסקראצ'פד של סשן 3 · רשומת-הערכה #5 · `before-tree` נבנה מ-`bf1fcfa`. |
| 09/09 04:2X | *"אתה עם 800 אלף בקונטקס תתיעץ עם S2 S1 מה לעשות"* | **מדידה + הוראה** (סשן 3) | הקשר 800K — מעל סף-750K, באמצע שלב 8. הוכרע בהתייעצות S1·S2: להעביר את המקל ל-S4 עם עריכות-הכותבים על העץ (לא מקומטות) — לא לשחזר. |
| 09/09 05:2X | *"תשאיר התחברות"* | **הכרעה** (S4) | כפתור-הכניסה נשאר `התחברות`/`התחברות עם Google` — שם-פעולה רב-מגדרי במסך פרה-זהותי; ✏️ במדריך 🔒 §1. ה-locator `name: 'התחברות'` של כל ה-E2E נשאר חוזה. *(W12b כתב `התחברי`; S3 ואז S2 הזהירו; S4 החזיר לפני ההכרעה.)* |
| 09/09 05:2X | *"מה למה שתמחק קובץ דוחות?"* | **הכרעה** (S4) | `ui-copy-stage8-reports.md` **נשאר** (הכותרת שלו ביקשה מחיקה אחרי הקומיט — בוטל). דוחות-כותבים מילה-במילה הם רשם, לא ארעי. |
| 09/09 06:0X (S2 → S4) | מדידת-06:00 של S2 (בקשת-ישי): codeburn 3,086 קריאות היום (+2,141 מ-03:0X); tokscale דלתא 03:0X→06:00: fable +0.60M פלט · sonnet +0.56M · opus +0.64M (סשן אחר של ישי) | **מדידה** | בקרת-סבירות לרשומות #5–#8; המספרים הפר-סוכן — מדיווחי-הסוכנים. "מקל-השומר אצל: S2" עומד ב-§0. |

**וגם — הערה מסשן אחר (הובאה ע"י ישי 22:0X "כהמלצה בלבד"):** התקבלה במלואה — צעד ⑥ (ממצאי-מבנה) בבריף + השורה ב-`PROJECT_MASTER §4א` + החצי-שורה "אותו משפט יכול להיכנס לשני הקבצים". קומיט `c04e8cc`.

### מה קלוד המליץ / הכריע לבד (ביצוע-טכני) — וישי רשאי לעקוף
- **שלב 9 (S4):** `<Hint>` בולע `useAuth` שזורק מחוץ ל-provider (רמה 0) — קישוט לא מפיל מסך · המעריכים של שלב 10 עובדים על **צילומים** ולא על דפדפן חי (שני מצבים הפוכים על חשבון-מנכ"ל אחד אינם יכולים לדור יחד; הצילום = המתג הנעול) · 3 תוספות-הכותב אומצו (E1–E3) · `dashboard.partialSums` הושתל מגודר בענף-השגיאה + S-01 חודד · יישור-מילון על 3 מחרוזות-בסיס (R11) · `closing.doNotSendAgain` נשאר למרות R27 דק — מסומן ראשון למחיקה אם המעריכים יראו חזרה.
- שם-המיגרציה `…_onboarding_mode` בלי `m11_` (היא עצמאית). · שם-הפונקציה `saveOnboardingMode` (email אופציונלי = דלת-המנכ"ל) · `listOnboardingModes` · `AuthContext.updateOnboardingMode` (הפיכה חיה בלי `reload()`).
- **בניהול-משתמשים:** כשל בטעינת-הרמות מציג "—" (לא מתג "כבוי" כוזב); העמודה רק ל-`roleName === CEO_ROLE_NAME`.
- **עיצוב ה-Hint:** גבול-ימני טורקיז `border-r-2 border-teal-500`, טקסט `text-slate-600`, בלי מילוי (כלל-המילוי). **לא אושר במפורש** — ישי ראה את הצילום ואמר "אני מאשר" על המדריך; אם יעיר על המראה — זה שינוי-עיצוב שלו.
- **מבחן-הקבלה של המדריך רץ פעמיים (2+2 כותבים) ולא שלוש** — תשואה פוחתת; סבב 2 התכנס.
- **שלושת המונחים הדו-משמעיים** (§3ד במדריך): "רדום" = ירושה ממ2 · "חברה" = הלקוח למעט "ממוצע-החברה" · "פרויקט/אירוע" = ממצא-מבנה. **הוגשו כביצוע-טכני, לא כשאלה.**

### מה נשקל ונדחה
- **`update` במקום `upsert`** — נדחה (רוב המשתמשות בלי שורה ⇒ 0 שורות ⇒ "אין הרשאה" כוזב). התוכנית §7ו עודכנה.
- **לכתוב את ניהול-המשתמשים דרך `supabase.from` ישירות** כמו שאר המסך — נדחה; הטבלה של מ9 ⇒ `api.js` של מ9 (כלל 14).
- **סבב 3 של מבחן-הקבלה** — נדחה (הפערים שנותרו הם בדיוק מה ששני הכותבים דיווחו כחסר ⇒ תוקנו).
- **תיקון `cards-finance.md:483`** (השער) — נדחה: קובץ של אפיון-מ11, לא של הלילה.

### במה קלוד טעה — והנוסח המתוקן
- **"62% מהדירוג"** כקבוע בדוגמה 3 של המדריך ⇒ תוקן: המשקל הוא פרמטר + תלוי-מתג; המשפט אומר "חלק מהדירוג".
- **דיווח "gate exit 0"** על סמך סטטוס-המשימה, בעוד בתוך הלוג `gate exit=1` ⇒ הלקח: `cmd > log; echo exit=$? >> log` מדווח את המעטפת. **קרא את שורת-`exit=` בתוך הלוג.** (ובפעם השנייה — `exit=1` היה `check:docs-structure` הקיים-מראש.)
- **מחיקת ספק-הראיות באמצע ריצת-השער** ⇒ השער נפל על ENOENT. מוחקים ספק **לפני** שמשגרים שער או **אחרי** שסיים.

### דפוסים שהתגלו הלילה — ונכונים גם בעוד שבוע (כבר בבית קבוע, כאן רק מצביע)
- **מבחן-הקבלה של המדריך עובד:** סבב 1 מצא 3 כללים פתוחים שאף קריאה-חוזרת לא הייתה מוצאת (ציווי-מול-אינפיניטיב · שתי-סיבות · ספק-רשום). **בית:** `ui-copy-styleguide.md` §7.
- **דפוס ספק-הראיות** (צילומי-מסך מחוברים בלי כתיבה): `e2e/CLAUDE.md`. הספק של הלילה נמחק; מבנהו: `login` מ-`settings.spec.js` + `page.route('**/rest/v1/notification_preferences*')` שמחזיר את המטען.
- **הוכחת-RLS בטרנזקציה מגולגלת:** `do $$ … set_config('role','authenticated') + set_config('request.jwt.claims',…) … raise exception 'ROLLED_BACK_PROBE %' $$` — התוצאות נוסעות בהודעת-החריגה, וכלום לא נשמר. **בית:** `db_roadmap` §10ב (הרשומה של 08/09).

---

### 💬 סשן 2 (08/09 23:2X – 09/09 03:0X) — מה היה רק בשיחה

**מה ישי אמר — מילה-במילה, מתוארך:**

| מתי | מילותיו | סוג | מה זה מחייב |
|---|---|---|---|
| 23:2X | *"עצור"* → *"חכה להעברת המקל מסודרת ממנו"* | הכרעה | סשן 2 לא נגע בקובץ עד שהודעת-המקל הגיעה (23:5X) — זה התקדים לכלל 16 בשרשרת |
| 00:0X | *"קבל שינוי. בטל עצירה מולי. עצירה רק שהכל מבוצע מאשר קבלה?"* | הכרעה | 🛑 עצירה 2 בוטלה; העצירה היחידה — בסוף, לקבלה |
| 00:1X · 00:3X · 01:2X | *"אתה עם 400/520/640 אלף קונטקסט"* | מדידה | ישי מודד את ההקשר מבחוץ ומדווח; הסשן רושם ומתכנן את הסיבוב לפי זה |
| 00:4X | *"לא חושב שאתה צריך להעביר מקל מעבירים ב750"* | הכרעה | סף-הסיבוב 750K (לא 55%) |
| 00:4X | *"מזכיר תמדוד לא לשכוח (להגיד גם בהעברת המקל) 'תמדוד'"* | הכרעה | רשומת-הערכה לפני כל שלב; הודעת-המקל מסתיימת ב"תמדוד" |
| 01:2X | *"תעביר הלאה שהם גם צריכים למדוד כדי שהעברת המקל תהיה בזמן המתאים"* | הכרעה | כל סשן בשרשרת מודד את ההקשר שלו (§5) |
| 03:1X | *"זה חייב לעבוד כמו שצריך… שים לב לפרטים הקטנים. תצטט אותי אולי זה יעזור. צריך בהעברת מקל לכתוב הכל כמו שכותבים לסשן טרי — להגיד לו מה לקרוא ופרומט קטן עם הדברים ודברים חשובים, וכמובן קובץ מסירה מסודר"* | הכרעה | הודעת-המקל = **פרומפט לסשן טרי**: רשימת-קריאה ממוספרת · מה הוכרע · ההודעה הראשונה · הצעד הבא · "כל עובדה ניתנת לערעור" · **ומסתיימת ב"תמדוד"**. התבנית המלאה — §5 למטה, ומשם מעתיקים, לא מנסחים מחדש |
| 03:0X | *"כל הסשנים בסטנדביי עד S6 אם צריך. חובה עליכם בהעברת המקל לשלוח את הפרומט כמו שצריך עם המילה תמדוד בסוף"* · *"תחשוב את כל מה שהיה בשיחה ואף אחד לא יודע אם לא תכתוב בקובץ ותגיד לו לקרוא"* | הכרעה | הודעת-המקל נשלחת **בכלי-ההודעות** (לא דרך ישי; הוא ישן) לסשן שהצהיר "אני S3" (מזוהה ב-`search_session_transcripts`); והסעיף הזה — כל מה שהיה בשיחה נכתב לכאן |

**מסשן 1 (הודעות בין-סשנים, 00:1X):** (א) רשימת מחרוזות שה-E2E נועץ ברכיבים המשותפים — `הגדרות פרופיל` · `העדפות והתראות` · `אין ערוץ SMS במערכת` · `ההגדרות נשמרו` · `אין לך הרשאה לצפות במסך זה` · `לא הצלחנו לטעון את ההרשאות` — אומתו שלא זזו (settings.spec ירוק) · (ב) שלושת כללי-המקל ב-§5 · (ג) `de53103` — S1 קימט על העץ המשותף בזמן שסשן 2 עבד (רק המסירה; לא פגע).

**מה קלוד הכריע לבד (ביצוע-טכני) — וישי רשאי לעקוף:**
- **ניסוח-העקיפה של "פנה למנכ"ל" אוחד ל-`יש לפנות למנכ"ל…`** (שלושה כותבים כתבו שלושה נוסחים; התקדים: `lib/hostesses.js`/`quotes.js`); כותב אחד המציא עובדה ("ההרשאה ניתנת בניהול המשתמשים" — היא ניתנת במטריצת-ההרשאות) — הוחזר.
- **משתתפי-ההתקדמות (`שומר...`) ⇒ נקבה + שלוש-נקודות אחת (`שומרת…`)** בכל המערכת — תקדים `מפיקה ושולחת…` של מ8.
- **`NO_PRIMARY_MSG` (מ2) הוחזר לזכר** — קשור-בייט ל-`raise` במיגרציה ונוסח-ישי; C-01, שלב 7 עם מיגרציה.
- **תווית-הסגירה הנסתרת של הדיאלוג המשותף `סגור` ⇒ `סגירת החלון`** (התנגשה בבדיקה של מ6 שחיפשה `סגירה` במדויק).
- **שגיאות-הצורה של מסך-הבית ⇒ `יש תקלה בנתונים.`** — סטייה מוצהרת מהכרעת-סגירת-מ7 (03/09), המדריך הנעול גובר; ערוץ-האבחון נשאר ב-`console.error`.
- **הסיומת `(כרטיס-P1)` הוסרה** מהודעת-החשבונית (אינה במיגרציה; UC30); **`ציר הפעיל` ⇒ `להיות פעיל`**; **`למשל:` ⇒ `לדוגמה:`** בכל מ6/מ8; **כיתוב-המיון של מ4 אוחד למ6** (הכרעת 07/09).
- **הרצת כותבי-מודול N+1 במקביל למיזוג מודול N** (מ8 בזמן מיזוג מ6) — חסך ~25 דק'; המחיר: חבילת-היחידה של מודול N רצה "בלי המודול שבעריכה", והמלאה רק בקומיט הבא.
- **צילומי-"לפני" מבנייה ארכיונית** (`git archive` + junction ל-`node_modules` + `vite preview` על 4175) במקום `git stash` בזמן שכותבים עורכים.

**מה נשקל ונדחה:** לתקן את `customer-page.spec.js:130` (קדם-ללילה, לא שלנו) · לשנות את `ConfirmDialog` ברירות-המחדל (S-06) · להכריע לבד בשלוש שאלות-המדריך של שלב 5 · לחכות עם שיגור מ8 עד קומיט מ6 · להעביר מקל ב-55% (ישי: 750).

**במה קלוד טעה — והתיקון:**
- **גרשיים-הפוכים בטקסט שעבר דרך `bash`** — `$(…)` בלע שני שמות-קבצים במשפט-השערים של שלב 5 בארבעה מסמכים (תוקן ב-`bf1fcfa`). **הלקח:** טקסט עם `` ` `` נכנס לסקריפט-פייתון בקובץ, לא לארגומנט של shell.
- **`git commit -- <paths> -F -`** — `--` הפך את `-F -` לנתיבים; הסדר: `git commit -F file -- <paths>`.
- **נתיבי POSIX (`/c/Users/…`) בקונפיג-Playwright** נקראים ע"י Node כ-`C:\c\Users\…` — Windows-form (`C:/Users/…`) + `MSYS_NO_PATHCONV=1` לארגומנטים.
- **הודעת-המקל נכתבה לישי להדבקה במקום להישלח בכלי** — ישי ישן; תוקן: נשלחה ל-S3 דרך `send_message` אחרי זיהוי ב-`search_session_transcripts` ("אני סשן-סטנדביי S3"). **הכלל:** מחפשים את הסשן, לא מבקשים מישי.
- **דיווח "gate exit"** — לא חזר; אבל **`npm run smoke` על 5173 המשותף** נפל פעמיים בסימפטומי-HMR ועבר על שרת טרי — לא לייחס לקוד לפני ריצה על שרת משלך.

**מדידות שישי ביקש (03:0X, `codeburn`):** היום 945 קריאות (~$180 תמחור-API תיאורטי — אינו המדד) · החודש 37,302 קריאות. `tokscale` (מצטבר, כל הפרויקטים): fable-5-1 — 481K קלט · 4.77M פלט · 41.7M כתיבת-מטמון. **המדד ליומן-הכיול:** זמן-קיר · טוקנים-לסוכן · הקשר (סשן 2: 23:2X→03:0X, ~800K הקשר, 14 סוכנים ≈ 5.0M).

## §3 · 🔬 מבחן-הקבלה של סשן 2 — עונים מהדיסק, לפני פעולה ראשונה

**א. עשר השאלות של הבריף** (`ui-copy-session-brief.md` §✅) — כולן עדיין תקפות; ההודעה הראשונה עונה עליהן.

**ב. ארבע שאלות על מה שסשן 4 כתב לדיסק בשלב 10** (התשובות בקבצים, לא כאן):
1. **למה `projects.sort` הוחלף במסך-הדיילות ובמה, ומה ההוכחה שהמעריך הביא מהמסך עצמו?** *(`ui-copy-night-report.md` §0 · `module-4.md` §9 · `onboardingCopy.js` הערת-הקבוצה)*
2. **אילו שלושה ממצאי-מעריכים לא תוקנו בכוונה, ומה משותף לשלושתם?** *(קובץ-הבוקר §3 פריטים 7–9 — טקסט מאושר-באפיון/נעול)*
3. **מה תוקן ב-`projects.js` ומה העוגן לכל אחד משלושת השינויים?** *(`module-6.md` §9 · `screens-approved.md §⑥` שורה 222)*
4. **למה המעריכים עבדו על צילומים ולא על דפדפן חי, ומה זה עלה?** *(`CLAUDE_CODE_LOG.md` רשומת שלב 10)*
**ג. מכני:** כל נתיב בקובץ הזה נפתח · `git status` **מלוכלך בכוונה** על `ishay/ui-copy-rewrite` (רק `src/`/`e2e/` של מ1/מ5/מ9 — אף קובץ אחר) · `docs/plans/ui-copy-candidates.md` (C-01…C-04) ו-`…structural-findings.md` (S-01…S-06) **קיימים** מאז שלב 2.

**ואחרי הכול: "האם המשימה ברורה? יש לך שאלות?" — שאלה רק על סתירה.**

---

## §4 · הצעד הבא הבטוח — מיזוג שלב 8 מהדוחות (S4), ואז שלב 9 · 10

- **מה יש על הדיסק:** עריכות ארבעת הכותבים על `src/modules/01_auth` · `05_logistics` · `09_settings` · `src/lib/projectLogistics.js` · `paramsRegistry.js` · `permissions.js` · `listWindow.js` · `constants.js` (+ בדיקותיהם) — לא מקומטות. **הדוחות** (המילה-במילה של כל כותב, לפי המדריך §8): `docs/plans/ui-copy-stage8-reports.md` — **בריפו, מקומט** (סעיף 0 שם = רשימת הקבצים המלוכלכים הצפויה). **הבריפים:** `stage8-shared.md` + `w12a–d.md` באותה תיקייה. **צילומי-לפני:** `C:/Users/ishay/AppData/Local/Temp/claude/C--Users-ishay-Reg-In/2e68cb1a-2194-4372-8b9f-850971389f23/scratchpad/evidence/before-m1-*` · `before-m5-*` · `before-m9-*` (11). **רשומת-הערכה #7 פתוחה** — לסגור בקומיט.
- 🔴 **לפני הכול — כפתור-הכניסה:** W12b שינה `התחברות` ⇒ `התחברי` (+Google) ב-`LoginPage.jsx`, ו-**24 קובצי-E2E + העשן + ספקי-הראיות** מאתרים `name: 'התחברות'`. הכותב דיווח "0 פגיעות" — שגוי. **המלצת S3: להחזיר את שני הכפתורים** (`התחברות` · `התחברות עם Google`) ולרשום שאלת-מדריך שישית לשלב 9; החלופה — עדכון 24 קבצים באותו קומיט. גם `דוא"ל` ⇒ `דוא״ל` ב-`UsersManagementPage` — לגרפ ב-`e2e/` לפני השערים.
- **סדר המיזוג (כמו שלבים 6–7):** ① קרא את ארבעת הדוחות (לא את הדיפים המלאים — הדוחות מפורטים) · ② grep-רוחבי: כל תווית-כפתור ששונתה מצוטטת במשפט אחר? (המלכודת של שלב 7: `שלחי במייל`) · חמש מחרוזות-ה-`raise` במ5 בייט-זהות (`stage8-shared.md`) · `QUEUE_SORT_LINE` = `ממוין: לפי קרבת האירוע` (עוגן-עשן) · `projectLogistics.test.js:786` · ה-`title=` של מטריצת-ההרשאות · `כתובת דוא״ל` U+05F4 · `constants.js` ערכים · ③ eslint + prettier על הקבצים ששונו בלבד + ספירת-CR · ④ ברקע במקביל: CI-parity מלא · `vite build` · עשן על שרת משלך (`C:/Users/ishay/AppData/Local/Temp/claude/C--Users-ishay-Reg-In/2e68cb1a-2194-4372-8b9f-850971389f23/scratchpad/playwright.smoke64343.config.js` — או שרת חדש) · ⑤ E2E: `logistics` · `settings` · `auth` · `permissions` · `prices` (`:218` אדום קדם-ללילה) · `load-failure-guards` · `server-messages` על `playwright.e2e.config.js` · ⑥ **מעבר ידני על ארבעת מסכי מ1** (כניסה · ניהול משתמשים · מטריצה · קטלוג-מחירים) — צילומי-אחרי דרך `C:/Users/ishay/AppData/Local/Temp/claude/C--Users-ishay-Reg-In/2e68cb1a-2194-4372-8b9f-850971389f23/scratchpad/evidence_m159.spec.js` + `playwright.after.config.js` (`EVIDENCE_TAG=after`), ולהסתכל בעין · ⑦ רשמים (C-31… · S-20…) · `↳ as-built` סופי ב-`module-1.md` · `module-5.md` · `module-9.md` (מחליף את שורות-ה-⏳) · טבלת-§4 בתוכנית · סגירת הערכה #7 · LOG · STATUS · ⑧ קומיט בפאת'ספק (הקבצים מ-`git status` + התיעוד) · push · דיווח קצר · ⑨ שלב 9 (מסמך-שלם · מלל-ההטמעה מ-`ui-copy-candidates.md` C-01…C-30+ · מסך-הדגל · `ui-copy-night-report.md`) · ⑩ שלב 10 (2+2 Opus — כלל-ההחלטה בתוכנית §4).
- ⚠️ **אם הסקראצ'פד של S3 נמחק:** הדוחות שרדו (בריפו); הבריפים והצילומים לא — הבריפים ניתנים לשחזור מ-§4 של שלב 7 בהיסטוריית-הקובץ הזה, וצילומי-לפני מ-`git archive 57add3f` (מ1/מ5/מ9 לא השתנו בין `bf1fcfa` ל-`57add3f`).

## §5 · 🏃 פרוטוקול-המקל — הכרעת-ישי 08/09/2026 23:5X *("תחליטו איך הולך ויתנהל ההעברת המקל כי זה ישפיע לכל הלילה")*

**העיקרון האחד: המקל הוא קובץ, לא הודעה.** מי שכותב לעץ הוא מי ש-§0 אומר שהמקל אצלו — **ורק הוא.** כל השאר (סטנדביי) קראו, ענו על מבחן-הקבלה, וממתינים. שני סשנים שכותבים לאותו עץ מתנגשים (כלל-ברזל 16) — זו הסיבה לכלל.

**מתי מעבירים — הראשון מבין השניים:**
- **גבול-מודול** (קומיט ירוק) **וגם** ההקשר עבר **~750K** ⇒ מעבירים. *(הכרעת-ישי 08/09 23:5X: "55 מוגזם, 750 זו נקודת עצירה סבירה" — במקום ~55% שסשן 1 הציע. מודול = 45–75 דק'; מי שעובר 750K באמצע מודול מסיים אותו אם הוא מתחת ל-850K.)*
- **ההקשר עבר ~850K באמצע מודול** ⇒ לא ממשיכים: `git restore -- <הקבצים שנגעת בהם>` על מה שלא קומט (הבריף ⑧ — 🚫 לא `reset --hard`, 🚫 לא `revert`), רושמים ב-§0 *"מודול N נקטע ב-<שלב>, הוחזר; מתחילים מחדש"*, ומעבירים. **חצי-מודול על הדיסק אסור.**

**היוצא — שבעה צעדים, בסדר הזה, ואף אחד לא מדולג:**
1. `git status` נקי אחרי קומיט בפאת'ספק מפורש (🚫 `add -A`) · `git push`.
2. **§0 כאן:** חותמת-טריות חדשה · "המקל אצל: סשן N+1" · ראש-הענף · השלב הבא הבטוח · סוכנים נוצלו/נותרו · מה לא נעשה.
3. **§2 כאן — מה היה רק בשיחה:** ציטוטי-ישי **מילה-במילה ומתוארכים**, הכרעה מול רעיון · המלצות שקלוד נתן · מה נשקל ונדחה · במה טעה. *(הבדיקה: `git log` של הסשן מול הרשם — לא זיכרון.)*
4. **§3 כאן:** ארבע שאלות עובדתיות חדשות שהתשובה להן במה שהרגע נכתב לדיסק (מחליפות את הקודמות).
5. טבלת-המצב ב-`ui-copy-and-onboarding-mode.md` §4 · `CLAUDE_CODE_LOG.md` · `STATUS.md` (אחרונים — ה-Stop hook בודק mtime). קומיט + push שני.
6. **הודעת-המקל לנכנס** — דרך כלי-ההודעות-בין-סשנים אם זמין, אחרת ישי מעביר. **הנוסח קבוע — פרומפט לסשן טרי, מעתיקים ומחליפים רק את `<…>` (הכרעת-ישי 09/09 03:1X: *"לכתוב הכל כמו שכותבים לסשן טרי — להגיד לו מה לקרוא ופרומט קטן עם הדברים ודברים חשובים"*):**

```
🏃 הודעת-מקל — לילה שכתוב-הטקסטים 08–09/09/2026, <S-יוצא> → אתה (<S-נכנס>).

המקל אצלך. ראש-הענף `<hash>` על `ishay/ui-copy-rewrite` (origin). לפני כל דבר: `git fetch` · `git switch ishay/ui-copy-rewrite` · `git pull --ff-only` · ואמת ש-`git rev-parse --short HEAD` == `<hash>`. לא שווה ⇒ עצור ודווח.

אתה סשן-בנייה. קרא במלואם, שורה-שורה ולא בחיפוש, בסדר הזה:
1. docs/plans/ui-copy-session-brief.md — הבלוק שבין ה-``` הוא ההוראה; כל מה שהוא מפנה אליו נקרא גם.
2. docs/plans/ui-copy-handoff.md — המסירה: §0 מצב · §1 רשימת-הקריאה · §2 מה היה רק בשיחה (כולל הבלוק של הסשן היוצא) · §3 מבחן-הקבלה · §4 הצעד הבא · §5 פרוטוקול-המקל (כך גם אתה תעביר הלאה).
3. docs/plans/ui-copy-styleguide.md — 🔒 נעול ואושר ע"י ישי; כל סוכן-כתיבה מקבל אותו בגוף הבריף.
4. <ספציפי לשלב הבא: אפיון-המודול, מדריך-המיקרו, CLAUDE.md של המודול>
טען את הסקיל הגלובלי ui-copy-and-onboarding-levels כמדריך-השיטה; בסתירה — הבריף גובר. שתי רשימות-כללים נפרדות: R1–R30 (ריפו) · UC1–UC42 (סקיל).
מה שהוכרע: <שלבים שקומטו> · עצירה 2 בוטלה — העצירה היחידה בסוף, לקבלה · <סוכנים נוצלו>/50 · סף-סיבוב 750K · <הכרעות חדשות של ישי מהסשן היוצא, מילה-במילה>.
ההודעה הראשונה שלך לישי: התשובות לעשר שאלות-הקבלה שבסוף הבריף + ארבע השאלות ב-§3 של המסירה, מהדיסק. ואז — שלב <N> (§4 במסירה), בלי להמתין ל"ממשיך".
דברים חשובים שנמדדו ואינם בקבצים: <עד 3 שורות>.
כל עובדה במסירה ניתנת לערעור — מדדת אחרת? תקן עם המדידה.
תמדוד.
```

**איך שולחים:** בכלי-ההודעות-בין-סשנים (`send_message`), לסשן שהצהיר על מספרו — מאתרים ב-`search_session_transcripts` ("אני סשן-סטנדביי S<N>"), לא מבקשים מישי להדביק. ⚠️ **קודם קומיט+push של המסירה, ורק אז ההודעה** — ה-`<hash>` בהודעה חייב להיות ראש-הענף הסופי.
7. **עוצר.** לא נוגע בקובץ אחר. *(סשן שמת לפני צעד 6: הנכנס מזהה לפי §0 + `git log` — "המקל אצל" הוא הראיה, לא ההודעה.)*

**הנכנס — שלושה צעדים לפני מחרוזת ראשונה:**
1. `git fetch` · `git switch ishay/ui-copy-rewrite` · `git pull --ff-only` · `git rev-parse HEAD` == הראש שב-§0. **לא שווה ⇒ עצור ודווח** (או שהיוצא לא סיים, או שיש סשן שלישי שכותב).
2. קורא את רשימת-הקריאה §1 (**קריאה, לא grep**) ועונה על §3 + עשר שאלות-הבריף **בהודעה הראשונה**.
3. **"ממשיך" של ישי אינו נדרש להעברת-מקל** — הכרעתו 22:2X: *"אתה לא צריך עוד אישור ממני"*; העצירה היחידה שנותרה היא 🛑 עצירה 2 (סוף שלב 2). **הודעת-המקל + §0 = האישור.** ⚠️ אבל אם §0 אומר "ממתין לעצירה 2" — ממתינים לישי.

**מספור וסדר (הכרעת-ישי 08/09 23:5X — "אולי גם ניתן להם מספר?"):** כל סשן-סטנדביי נפתח עם מספר בפרומפט שלו — **S2 · S3 · S4 · S5 · S6** — והמקל עובר תמיד ל**מספר הפנוי הנמוך ביותר**. אין מתאם חי (סשן-מנהל היה מת עם החלון שלו ולוקח את הניתוב איתו); הסדר הקבוע + §0 הם המתאם. **הלוח היחיד של ישי:** §0 כאן + שורת "עודכן לאחרונה" ב-`STATUS.md`; כל סשן מדווח לו ישירות לפי הבריף ⑦, לא דרך סשן אחר.

**שלושה כללים שנמדדו בהעברה הראשונה (S1→S2, 09/09 00:1X):** ‏(א) מדריכי-המיקרו והיומן של מודול שנוגעים בו הם של **מי שמחזיק את המקל** — הסשן היוצא לעולם לא כותב אותם "כדי לשקט את ההוק" · (ב) הסשן היוצא, אחרי צעד 7, **נסגר** — ישי סוגר את הלשונית — כי סשן יוצא שנשאר פתוח מקבל חסימות-הוק על עבודת הנכנס בכל תור ואין לו דרך כשרה לספק אותן · (ג) הנכנס יודע שההוק שלו ידרוש `↳ as-built` בכל `module-N.md` שנגע בו — **לפני הקומיט, לא אחרי הדיווח**; כשכותבים במקביל עדיין רצים, שורה זמנית מסומנת ⏳ ומוחלפת בקומיט. ➕ **וכלל-מדידה:** ישי מדווח את גודל-ההקשר בצ'אט ("אתה עם 520 אלף") — הסשן מתייחס לזה כמדידה, לא להערכה עצמית. **סף-הסיבוב: 750K** (ישי 09/09 00:4X) — בגבול-המודול הראשון אחריו. 🔴 **וזה חל על כל סשן בשרשרת** (ישי 09/09 01:2X: *"תעביר הלאה שהם גם צריכים למדוד כדי שהעברת המקל תהיה בזמן המתאים"*): הנכנס עוקב אחרי גודל-ההקשר שלו לאורך כל הדרך — ישי מדווח מספרים בצ'אט; הסשן רושם אותם ב-§0 עם חותמת, ומתכנן את גבול-המודול שאחרי 750K **מראש**, לא כשמגלים. ⚠️ **ודוחות-סוכנים שרצים ברקע מגיעים רק לסשן ששיגר אותם** — לפני העברה, כל דוח שטרם מוזג נכתב לקובץ בסקראצ'פד ומצוין ב-§0, אחרת הוא אובד.

🔴 **תמדוד** (ישי 09/09 00:4X: *"מזכיר תמדוד לא לשכוח — להגיד גם בהעברת המקל"*): לפני כל שלב — רשומה חדשה ב-`docs/guides/01_estimation_log.md` §2 (הערכה + חיזוי בר-הפרכה, **לפני** השיגור); בקומיט — סגירתה (זמן-קיר · טוקנים לסוכן · הפער והסיבה). **הודעת-המקל נושאת את המילה "תמדוד"** — הנכנס פותח רשומה לפני המחרוזת הראשונה שלו.

**✏️ 03:4X 09/09 — תיקוני-פרוטוקול בהסכמת S1·S2·S3 (בקשת-ישי 03:1X "תדבר עם S1, S2… מעניין אם תגיעו להסכמה"), נמדדו על שתי העברות:**
1. **סטנדביי קורא רזה:** בריף + מסירה + מדריך-הסגנון בלבד (~60K); חומר-השלב (מצאי המודול · אפיון · מחקר §7) **אחרי** המקל. עשר שאלות-הבריף נענות בסטנדביי; ארבע שאלות-§3 אחרי המקל. *(נמדד: S3 קרא ~250K לפני המקל, רובו לא נדרש לשלב 6.)* ⚠️ מי שמקבל מקל לשלב 9 קורא `research-onboarding-content.md` §1/§3/§7 לפני.
2. **הודעת-מקל אחת, אחרי הפוש האחרון.** *(S1 שלחה שלוש עם שלושה האשים, S2 שתיים — ליניארי במקרה.)* אם חייבים לתקן — §0 מכריע, וההודעה אומרת "מחליפה".
3. **היוצא נסגר אחרי ההודעה** (ישי סוגר את הלשונית). *(S1: ההוק חסם 8 סיומי-תור על עבודת הנכנס.)*
4. **חותמת-שעה רק מ-`date`**, לעולם לא מהיד. *(S2 תיקנה שתיים.)*
5. **טבלת מזהי-סשן** של הסטנדביי כאן, לא רק מספר *(S2: 20 דק' לאתר את S3)*: S1 `local_9f523983-4db7-477b-928b-9b9eeb4255e6` ("UI copy session brief") · S2 `local_00e071ee-8387-418d-9507-5b8d114878e8` ("UI copy session") · S3 = הסשן הזה · S4–S6: שלושת "סשן סטנדביי UI Copy" (`local_afba9e6c…` · `local_417fcf21…` · `local_fa31b79c…` — לאמת ב-`list_sessions` לפי הכותרת; המספר שלהם לפי הפרומפט שלהם).
6. **סקריפטי-הראיות מחוץ לסקראצ'פד** (מת עם הסשן, והבא מנחש מחדש): `evidence_m4.spec.js` (תבנית לכל מודול) · `playwright.before/after/smoke.config.js` · סקריפט-ה-`raise` המדויק · הדפוס `git archive <ראש> + junction ל-node_modules **גם בסקראצ'פד עצמו** + vite build + preview 4175`. ⏳ **המיקום = הכרעת-ישי** (`scripts/ui-copy-night/` בריפו — S2 · בית ב-`e2e/` עם מצביע — S1); עד אז: הסקראצ'פד של S3 + העתקה בכל העברה.
7. **שורת "שרת-dev" ב-§0 מתארת עובדה מדודה, לא הנחה** (ר' §0 עכשיו).
8. **מה שכן שימש בכל שלב, ונשאר:** §0 · ציטוטי-ישי ב-§2 · §4 חלוקת-כותבים + E2E נעוצים · §5.

**סטנדביי:** סשן שקיבל את פרומפט-ההדבקה, קרא, ענה על מבחן-הקבלה — ו**לא קיבל הודעת-מקל**: ממתין. אסור לו לכתוב, לקמט, או להריץ סוכנים. מותר לו לקרוא ולהתכונן (למשל להכין את רשימת-הקבצים של המודול הבא). כשהמקל מגיע — צעד 1 של הנכנס מחדש (הראש זז מאז שקרא).

**שרשרת:** כל סשן מוסיף בראש הקובץ שורת **"ממשיך מ־: סשן N (ראש `<hash>`, HH:MM)"** מתחת לקודמת — כך רואים את כל הלילה בחמש שורות.

---

## §6 · 🌙 איך הלילה מתנהל — מה שסוכם עם ישי, ואיפה כל חלק רשום

**זה הסיכום שהיה רק בצ'אט (הודעת עצירה 1, 08/09 23:1X) — נכתב כאן בהוראתו: *"דברים שהיו רק בשיחה חשוב שיהיו במסמך… כל מה שסיכמנו ואיך הולך להיות הלילה והתוכנית"*.**

**חמישה עקרונות-הפעלה, כולם הכרעות-ישי:**
1. **שתי עצירות בלילה כולו — ✅ עצירה 1 עברה; נותרה 🛑 עצירה 2 (סוף שלב 2).** מעבר לה: דיווח קצר בצ'אט אחרי כל מודול **בלי להמתין** (צילום לפני/אחרי + ≤10 שורות), ועצירה רק על סתירה בין הכרעות שלו · רגרסיה אדומה שלא מתיישרת · דבר שמשתמשת תראה ואין לו תקדים. *(הבריף §מודל-העצירות · ישי 22:2X "אתה לא צריך עוד אישור ממני".)*
2. **קובץ-בוקר אחד** — `docs/plans/ui-copy-night-report.md` (שלב 9–10): הוורדיקט של 2+2 ראשון, ואז כל הלפני/אחרי לפי מודול. זה מה שישי קורא בבוקר, לא הצ'אט.
3. **תקציב: 50 סוכנים ללילה, Sonnet לכתיבה, Opus לביקורת (שלב 10).** הסשן מחליט כמה ומנמק לפני כל גל. **6 נוצלו בשלב 0.**
4. **שרשרת-סשנים בסטנדביי, מקל = קובץ** (§5). ישי פותח סשנים מראש עם פרומפט-הסטנדביי (למטה); רק אחד כותב.
5. **מודול = קומיט אחד** רק אחרי רגרסיה ירוקה + `npm run smoke`; מודול שנקטע — `git restore`, לא חצי.

**התוכנית — כפי שהוצגה לישי ואושרה (23:1X; "אני מאשר" 23:2X):**

| שלב | מה | סוכנים (Sonnet אלא אם צוין) | זמן משוער | קומיט |
|---|---|---|---|---|
| ✅ 0 | המנגנון + המדריך | 6 (מילון · בדיקות-בסיס · 2+2 כותבים) | 22:00–23:20 (בפועל) | `bc9c524` |
| **2** | רכיבים משותפים + **לשון-נקבה בכל המערכת** (74–100 מחרוזות; 22 קובצי-`נסה שוב`) → רגרסיה מלאה | ~6 כותבים בחלוקת-קבצים בלעדית | ~75 דק' | 1 → 🛑 **עצירה 2** |
| 3 | מ7 מסך-הבית (35 מחרוזות) | 2 | ~30 דק' | 1 |
| 4 | מ6 פרויקטים (300 + `lib`; איחוד "ממוין:" — 2 מתאחדים, 3 נשארים באותה תבנית) | 4 | ~75 דק' | 1 |
| 5 | מ8 כספים (304; מעבר-H2 פר-משפט) | 4 | ~75 דק' | 1 |
| 6 | מ4 דיילות (269) | 3 | ~60 דק' | 1 |
| 7 | מ2 + מ3 (378) | 4 | ~60 דק' | 1 |
| 8 | מ5 + מ9 + מ1 (273; מעבר ידני על מסכי מ1) | 4 | ~60 דק' | 1 |
| 9 | מסמך-שלם · **מלל-ההטמעה** (מ-`candidates.md`) · מסך-הדגל · קובץ-הבוקר | 2 | ~60 דק' | 1 |
| 10 | עין טרייה 2+2 (**Opus**) + מיזוג — כלל-ההחלטה 2+2/3+3 בתוכנית §4 שלב 10 | 4–6 | ~45 דק' | 1 |
| | **סה"כ** | **~35–41 מתוך 50** (רזרבה ~10) | סיום ~07:00–08:00 | |

**הזמנים הם הערכה** — מה שמכתיב אותם הוא הרגרסיה המלאה אחרי כל מודול (CI-parity ≈ 3.5 דק' + smoke ≈ 1 דק' + תיקוני-בדיקות), לא הכתיבה. **חריגה מהתקציב ⇒ אומרים לישי לפני, לא אחרי.** *(`docs/guides/01_estimation_log.md` §1 — גורמי-הסיכון המדודים לכל שורה.)*

**פרומפט-הסטנדביי** (מה שישי מדביק בכל סשן-המתנה; ההודעה שמעירה אותו היא הודעת-המקל של §5):
```
אתה סשן-סטנדביי מספר S<N> בלילה שכתוב-הטקסטים של REG-IN (08/09/2026). המקל אינו אצלך עד שתקבל הודעת-מקל
(מסשן אחר או מישי) עם האש של ראש-הענף. עד אז: 🚫 לא כותב, לא מקמט, לא משגר סוכנים.
עכשיו: קרא במלואם, שורה-שורה ולא בחיפוש — docs/plans/ui-copy-session-brief.md (הבלוק שבין ה-```
וכל מה שהוא מפנה אליו) · docs/plans/ui-copy-handoff.md (כולו; §5 = איך המקל עובר, §6 = התוכנית) ·
docs/plans/ui-copy-styleguide.md (🔒). טען את הסקיל הגלובלי ui-copy-and-onboarding-levels; בסתירה —
הבריף גובר. ההודעה הראשונה שלך: התשובות לעשר שאלות-הבריף + ארבע השאלות ב-§3 של המסירה, מהדיסק —
ואז "ממתין למקל". כשהמקל יגיע: git fetch · pull --ff-only · אמת HEAD == ההאש שבהודעה · קרא שוב את
§0 של המסירה (הוא זז מאז שקראת) · והמשך מהשלב שכתוב שם.
```

</div>
