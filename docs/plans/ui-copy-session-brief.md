<div dir="rtl">

# בלוק-ההדבקה לסשן שכתוב-הטקסטים — לילה של שלישי 08/09/2026

> **מה הקובץ הזה:** הפרומפט שישי מדביק לסשן טרי. **הוא מצביע — הוא אינו משכפל.** התוכנית המלאה חיה ב-[`ui-copy-and-onboarding-mode.md`](ui-copy-and-onboarding-mode.md), וההכרעות ב-`docs/specs/module_11_reports/processes-approved.md §🗳️ 28`.
> 🔗 **מראת הכרעה 28 — SSOT: `processes-approved.md §🗳️` (לא לערוך כאן).**
> **נכתב:** 07/09/2026 03:4X, בהוראת-ישי: *"פשוט צריך שאתה תכתוב תוכנית באמת ממש טובה עבור אותו סשן, עם קונטקסט מה לקרוא, שלבי עבודה וכו'."*
> 🔒 **קפוא מ-07/09/2026** — שלושה סבבי-עיניים-טריות (7 · 7 · "מוכן אחרי ארבעה תיקוני-שורה"), כולם הוחלו. **✏️ נפתח פעם אחת ב-17:1X להכרעה 28⑭ (עמודת-רמה) — תיקון אחד + סבב-עיניים-טריות אחד, לפי כלל 21 — וקפוא שוב.** **הכרעה מאוחרת ⇒ לתוכנית, לא לכאן.** פתיחה-מחדש = תיקון אחד + סבב-עיניים-טריות אחד *(`_shared/writing-prompts.md` כלל 21)*.

---

## לישי — שלוש שורות לפני שאתה מדביק

הסשן הזה **בונה קוד**, לא מאפיין. הוא מקבל תוכנית סגורה ומבצע אותה מודול-אחרי-מודול, **ועוצר מולך פעמיים בלילה כולו — לא אחרי כל מודול:** בסוף **שלב 0** על מדריך-הסגנון **ועל המיגרציה יחד** *(הד-ההקלדה של `supabase/migrations/CLAUDE.md` — עצירה אחת, לא שתיים)*, ובסוף **המודול הראשון** לאימות-הצורה. אחרי שתאמר "ממשיך" — הוא רץ עד הבוקר ומדווח בלי להמתין, ומשאיר לך **קובץ-בוקר אחד** עם כל הלפני/אחרי במקום תשעה דיווחים שנקברים בגלילה.

**דגם: Fable, מאמץ גבוה. תתחיל ב-22:00 כשהמכסה מתאפסת.** 🔴 **ו-100 הוא תקרה שהצבת, לא יעד** *(במילותיך, 07/09: "אני סתם זרקתי 100, שהוא יבחר איך נכון לנהל את זה — זו המכסה שלי לסשן")* ⇒ **הסשן מחליט כמה סוכנים באמת נכון**, ומדווח את המספר ואת הנימוק לפני כל גל. **ההערכה:** ~40–60 סוכנים, רובם Sonnet לכתיבה ו-Opus לנעילת-הסגנון ולביקורת — **‏6–9 מיליון טוקנים**, שזה הסדר שבו נגמרת מכסת-לילה שלמה.

🔴 **ומה שאני ממליץ שתדע לפני שאתה מתחיל:** החסם אינו השעות — הוא **אחידות-הקול**. מאה סוכנים שכותבים עברית במקביל מייצרים מאה קולות. **לכן שלב 0 נועל מדריך-סגנון ומילון-מונחים לפני שסוכן אחד נוגע במחרוזת**, וזו הנקודה היחידה שבה כדאי שתשקיע את העיניים שלך.

---

```
אתה סשן-בנייה. אתה משכתב את כל טקסט-הממשק של REG-IN ובונה את "מצב הטמעה".
🔴 **ענף: `ishay/ui-copy-rewrite`, נחתך מ-`ishay/module-11-reports` — לא מ-`dev`.** *(✏️ 07/09 17:0X: ההמלצה הקודמת אמרה `dev`, ומבקר-סבב-2 מדד שזה היה מפיל את הלילה בדקה הראשונה:)*
   **אף לא אחד משמונת קבצי רשימת-הקריאה קיים על `origin/dev`** — הבריף הזה · התוכנית · שני המחקרים ·
   המצאי · `processes-approved.md` (הכרעה 28!) · `PROJECT_MASTER §4א` · בלוק `M11-1` ב-`db_roadmap`. כולם
   חיים רק על `ishay/module-11-reports`, **וקומיטים שם טרם נדחפו** (`git log origin/ishay/module-11-reports..HEAD` — ספור, אל תצטט). `git switch -c … origin/dev` היה
   מוחק אותם מהדיסק. **ולמה לחתוך מענף-מ11 בטוח לגמרי:** `git diff origin/dev..HEAD -- src/ supabase/`
   **ריק** — **כל** הקומיטים שמעל `dev` הם תיעוד בלבד, **אפס קוד** (`git diff --name-only origin/dev..HEAD -- src/ supabase/` ⇒ ריק). ⇒ הענף החדש יורש את כל התיעוד ואפס סחף.
   **הפקודות, בסדר:** `git fetch origin` → `git switch ishay/module-11-reports` → `git pull --ff-only` →
   `git switch -c ishay/ui-copy-rewrite` → **שער:** `ls docs/plans/ui-copy-session-brief.md` חייב להצליח,
   ו-`git diff --stat origin/dev -- src/ supabase/` חייב להיות ריק. **אחד מהם נכשל ⇒ עצור, אל תמשיך.**
   **07/09 — סטייה מוצהרת מהכרעה 28⑤:** היא קשרה את המנגנון למיגרציה של מ11, **אך מ11 טרם נבנה** (אין
   `docs/micro_guides/module-11.md` ואין מיגרציית-מ11, נמדד 07/09) ⇒ המתנה לו הייתה חוסמת את הלילה.
   **המיגרציה כאן עומדת בפני עצמה** — סטייה מוצהרת, לא השמטה.

━━━ 🎯 הכוונה של ישי — קרא את זה ראשון, וחזור אליו בכל ספק ━━━
במילותיו, מחוברות מכל הפעמים שאמר את זה (06–07/09/2026). זו הבדיקה שכל החלטה קטנה שלך עוברת:

  "לאפס את המלל מחדש במערכת, לפי איך שמקובל במערכת בוגרת בהייטק. לא כותבים במערכת משפטים —
   מתומצת. שם טוב ומובן למשתמש, מינוחים מקצועיים ומקובלים, בלי ז'רגון ובלי הסברים.
   מה המשתמש צריך לעשות — ולא מה המערכת עושה. שהמשתמש במרכז ולא המוצר.
   ואז ליצור את המלל למצב הטמעה — נעים, ידידותי, ובאמת מובן — ואת המתג לכל משתמשת.
   המנכ"ל יפעיל את הטוגל, המערכת תיראה עם הרבה מלל, ואציג ככה בכנס ואסביר למה — ואציג גם את
   המצב הנקי והבוגר. עושים עבודה פעם אחת כמו שצריך, בלי קיצורי דרך. פרויקט אקדמי — לא להגזים,
   אבל לא להתעצל."

⇒ **שתי שאלות שמסכמות את זה, ואתה שואל אותן על כל מסך שסיימת:**
   ① כשהמתג כבוי — האם מהנדס שרואה את המסך הזה חושב "מוצר בוגר", או "בנה אותו מפתח"?
   ② כשהמתג דלוק — האם משתמשת חדשה יודעת **מה לעשות** ו**למה זה בנוי ככה**, בלי לשאול אף אחד?
   **תשובה של "לא" לאחת מהן = המסך לא גמור, גם אם כל הבדיקות ירוקות.**

🔴 **ומה שהוא אמר על הסדר, כדי שלא תתלבט:** *"אתה המפתח, אז מה שתחליט. העיקר שהמפתח יבין את הכוונה שלי."*
⇒ **הסדר הוכרע (בהאצלה): המתג ראשון כמנגנון ריק · הבסיס אחריו, מודול-מודול · מלל-ההטמעה אחרון.**
אינך שואל על הסדר — אתה שואל רק אם משהו סותר את הפסקה למעלה.

━━━ 📋 בשתי שורות, ואז השלבים והכללים — התקציר שקוראים לפני כל השאר ━━━
**המטרה:** לאפס את כל טקסט-הממשק של REG-IN כך שהמסך ייראה כמוצר-הייטק בוגר — קצר, נקי, בלי משפטים — ולבנות
מתג פר-משתמשת שמדליק מעליו שכבת-הסבר חמה ומלאה. **שני המצבים מוצגים בכנס ב-15/10, ושניהם נשפטים.**

**השלבים** *(הפירוט: ⑥ כאן · §4 בתוכנית)*:
  0 · המנגנון — מיגרציה עצמאית · הרשאה רביעית · <Hint> · onboardingCopy.js · שני המתגים · בדיקות
      למסך-המשתמשים · **ומדריך-הסגנון** → 🛑 **עצירה 1** (ישי מאשר את המדריך **ומקליד את הד-המיגרציה — יחד**)
  1 · *(מדולג — כלל למודולים שטרם נבנו)*
  2 · רכיבים משותפים + לשון-נקבה בכל המערכת → רגרסיה מלאה אחת → 🛑 **עצירה 2** (אימות-הצורה)
  3–8 · מודול-מודול, בלי לעצור: בית → פרויקטים → כספים → דיילות → לקוחות+הצעות → לוגיסטיקה+הגדרות+כניסה
  9 · מעבר-מסמך-שלם · **ורק עכשיו** מלל-ההטמעה · בחירת מסך-הדגל · קובץ-הבוקר
  10 · **עין טרייה — 2+2:** שני מעריכים למצב הבוגר, שניים למצב ההטמעה, בלתי-תלויים, ואז מיזוג → ורדיקט

**ששת הכללים — שבירה של אחד = הלילה נכשל, גם אם הכול ירוק:**
  ① מדריך-סגנון נעול ומאושר **לפני** סוכן-כתיבה ראשון — אחרת 40 קולות
  ② **לאפס, לא לערוך:** מה מוצר בוגר היה כותב כאן — ואז להשוות לישן ולהחזיר מה שחיוני
  ③ **`מנכ"ל` לא נוגעים** (ערך-RLS) · מונחי-🔒 לא נוגעים · שמות-דוחות שנמדדו — נמדדים שוב אם משתנים
  ④ מודול = קומיט אחד, **רק** אחרי רגרסיה ירוקה + `npm run smoke` · לפני הקומיט הראשון: הענף נכון
  ⑤ **שתי עצירות בלילה.** מעבר להן — דיווח בלי המתנה. עצירה שלישית רק על סתירה בין הכרעות שלו
  ⑥ **שום מלל-הטמעה לפני שהמסכים נקיים** — משפט שיורד מהבסיס נרשם כמועמד, מוכרע בשלב 9

━━━ 🔴 שלושת המוקשים ━━━
**① אחידות-קול — המוקש העליון, וכל השאר משני לו.** אתה מפעיל ~100 סוכנים שכותבים עברית במקביל. בלי מדריך-סגנון
נעול הם יפיקו מאה קולות שונים, וזה כישלון שלא רואים בבדיקות ולא ב-lint — רואים אותו רק כשקוראים שני מסכים ברצף.
⇒ שלב 0 נועל מדריך-סגנון + מילון-מונחים, ואף סוכן-כתיבה אינו מופעל לפניו. כל סוכן-כתיבה מקבל את המדריך בגוף
הבריף שלו, ומחזיר ציטוט של הכלל שהחיל על כל מחרוזת שינה.

**② הדפוס שאנחנו בונים נכשל בעולם, ויש לכך מקור.** מדריך-העיצוב של Microsoft ל-Windows **אוסר במפורש**
כפתור-עזרה-הקשרית ("?") וכפתורי-"Learn more" גנריים — הנימוק שלהם: **מדידות-שמישות הראו שמשתמשים מתעלמים מהם.**
⇒ `research-ui-copy-standards.md §2.2` מונה **שש דרכים** שבהן שכבת-עזרה מזיקה. שתיים מהן חוקי-בנייה מחייבים, לא "טוב לדעת":
   • **המסך לא נשען על השכבה.** מבחן-המחיקה בסוף כל מודול: כבה את המתג — האם המסך עדיין ניתן לתפעול מלא?
     מידע חיוני שנדד לשכבה = באג, לא פיצ'ר.
   • **הסבר מובן-מאליו מרעיל את כל השכבה.** NN/g מדדו אפקט-הכללה: עזרה אחת חסרת-ערך גורמת למשתמשות להתעלם
     מכולה. ⇒ R27/R28 של §1.5 (מבחן-הכיסוי ומבחן-"מה עושים אחרת") הם שער, לא המלצה — משפט שלא עובר אותם **לא נכתב**.
   📄 ו**ששת** התנאים שהמקורות מסכימים שגורמים לדפוס כן לעבוד — **קרא אותם ב-§2.3, אל תסתמך על הרשימה כאן**:
   הפרומפט מפנה ואינו סופר, כי "מספר שנרקב נמחק, לא מעודכן" (`writing-prompts.md`).

**③ לשון-מגדר — הפגם הכי גלוי לעין בכנס, ואינו סגנון.** נמדד (§0 של אותו קובץ): **74 מחרוזות בלשון זכר ב-24
קבצים · 9 בלשון נקבה ב-8 קבצים · 6 ברבים** — בעוד **כל חמש המשתמשות נשים**, ו-`S-28` כבר הכריע נקבה למודולים 5–8.
⇒ **איחוד לשון-הפנייה הוא פריט-חובה בשלב 2 ואינו נדחה.** הפירוט: `research-ui-copy-standards.md §3.1`.

━━━ ① מה אתה מייצר ━━━
(א) המנגנון: עמודה + מדיניות-RLS רביעית + מתג בשני מסכים + <Hint> + src/lib/onboardingCopy.js
(ב) שכתוב טקסט-הממשק ב-9 המודולים המוזגים, מודול-אחרי-מודול · (ג) שכבת-ההסבר שנדלקת במתג
כל מודול = קומיט משלו, אחרי שהרגרסיה שלו ירוקה. **הלפני/אחרי מדווח לישי בכל מודול — בלי להמתין לתשובתו**, למעט בשתי העצירות.

━━━ ② הכלים ━━━
• Agent — **תקרה** של 100 לאורך הלילה, לא יעד. אתה בוחר את המספר ומנמק אותו. 🚫 ישי הכריע: בלי Workflow ובלי
  שכבות-אימות ("בלי וורקפלואו עם מיליון אימותים כי זה באמת יהיה איתי") — הוא נוכח וזו הבקרה. ⇒ הסוכנים כותבים;
  האימות הוא הרגרסיה + עיניו, לא צבא-מבקרים.
🔴 • **`worktree` לא עובד בריפו הזה — אל תשתמש בו** *(נמדד 07/09: `node_modules` ו-`*.local` ב-`.gitignore` ⇒
  ורקטרי טרי הוא בלי תלויות ובלי `.env.local`, שבו יושבים `E2E_CEO_*` — סוכן שם אינו יכול להריץ ולו בדיקה אחת,
  ואין מסלול-החזרה למיזוג).* **החלופה שכן עובדת, והיא גם פשוטה יותר:** סוכנים מקבילים **על אותו עץ, בחלוקת-קבצים
  בלעדית** — כל סוכן מקבל רשימת-קבצים משלו ואינו נוגע באחרים; אתה ממזג בעץ אחד ומריץ את הרגרסיה פעם אחת פר-מודול.
  **תנאי:** אל תשגר שני סוכנים על אותו קובץ (כלל-ברזל 16).
• Bash/Edit/Read/Grep · Supabase MCP (כתיבה למסד רק דרך apply_migration, בשער-הקלדה) · Browser pane לצילומי לפני/אחרי

━━━ ③ רשימת-הקריאה — חובה, בסדר הזה, לפני פעולה ראשונה ━━━
1. docs/plans/ui-copy-and-onboarding-mode.md — במלואה. זו התוכנית; §4 הוא סדר-העבודה, §6 העלות, §7 הסיכונים,
   §10 ההכרעות (כולן נענו — ר' ④).
2. docs/plans/research-ui-copy-standards.md — כללי-הכתיבה. מהם נגזר מדריך-הסגנון בשלב 0.
3. docs/plans/research-onboarding-content.md — §1 (שבעת סוגי-האמירה) · §3 (מה הופך הסבר לחסר-ערך) ·
   §7 (חמש מחרוזות לפני/אחרי — הדוגמה המחייבת לצורת-העבודה).
4. docs/plans/inventory-ui-copy.md — 2,311 המחרוזות המסווגות (§1 המספרים · §2 הטבלה · §3 שתים-עשרה
   ה"לא-שם-ולא-שם"). זה מצע-העבודה. 🔴 ואל תניח שהקופי כבר קיים: רק ארבעה מודולים (4·5·6·8) מחזיקים
   screens-approved.md, ו-58 מופעי-⑥ שבהם אינם מכסים את 939 המחרוזות שתחתם. **1,372 מחרוזות — 59.4% —
   יושבות היום במקום שאין לו מסלול-תיעוד כלל.** ⇒ רוב הקופי נכתב מאפס, לא מועתק.
5. docs/specs/module_11_reports/processes-approved.md — §🗳️ הכרעה 28 בלבד, על כל חלקיה. ⚠️ הקובץ גדל
   ראש-ואז-זנב: קרא כל חלק עד סופו לפני שאתה מצטט.
6. docs/PROJECT_MASTER.md §4א — שלושת החוסמים שנמדדו, ו-§1 "ארבעת החלקים" לפני כל שאלה לישי.
7. src/CLAUDE.md + supabase/migrations/CLAUDE.md — לפני קוד ולפני מיגרציה.
8. .claude/skills/_shared/writing-prompts.md — לפני שאתה מבריף סוכן.
🟡 כנראה לא תצטרך — פתח רק על פער אמיתי, ואמור שפתחת: docs/reference_spec/** (קפוא) ·
docs/specs/module_0*/screens-approved.md (רק אם מחרוזת לא מובנת ואתה צריך את ההקשר המקורי שלה) · docs/CLAUDE_CODE_LOG.md.

━━━ ④ מה כבר הוכרע — במילותיו של ישי, ואינו נפתח מחדש ━━━
• המטרה: "לשכתב מחדש את כל המלל במערכת שיהיה ברמה של מוצר מוגמר בהייטק… לכתוב בצורה נעימה ידידותית ובאמת
  שתהיה מובנת למשתמש — זה מה שיוצג ברגע שהמנכ"ל יפעיל את הטוגל… ואז המערכת תראה עם הרבה מלל… ואציג גם את
  המצב הנקי והבוגר של המערכת." ⇒ שתי שפות: המצב המכובה נשפט על נקיוּן ומוסכמה · הדלוק על חום ומובנוּת.
  ⇒ השכתוב בבסיס הוא ברובו הסרה והידוק. משפט שיורד מהבסיס עובר לשכבה — לא נמחק.
  ⚠️ "מוצר מוגמר בהייטק" = נקי כפי שמקובל, לא "כתוב יפה יותר" (חידודו, 07/09).
• "והמשתמש יוכל להדליק ולכבות לעצמו בפרופיל גם" ⇒ שתי דלתות על עמודה אחת: המנכ"ל פר-משתמשת
  בניהול-המשתמשים · המשתמשת לעצמה בפרופיל.
• ברירת-מחדל: כבוי. (ישי, 07/09: "1. בסדר")
• מדיניות-RLS רביעית: כן. (ישי, 07/09: "2. בסדר") — דפוס להעתקה: users_write_ceo_only.
• היקף: הכול, בלי לעצור בשלב 5. "עושים עבודה פעם אחת כמו שצריך בלי קיצורי דרך."
• איחוד חמשת נוסחי "ממוין:": מאושר. ("6. וכן יכול לאחד 5 נוסחים")
• "אין סיבה להוסיף עבודה גם אם זה שיפור נחמד" — אל תרחיב היקף מעבר לתוכנית.
• ✏️ 17:1X — **שלוש רמות** (ישי: "מאשר הכל לפי המלצתך") ⇒ **העמודה נולדת כרמה `0/1/2`, והלילה בונה 0 ו-2 בלבד** — הכרעה 28⑭. רמה 1 אינה נכתבת הלילה.

━━━ ⑤ מה אסור — כל אחד עם הסיבה ━━━
🚫 אל תיגע ב-docs/reference_spec/** — קפוא, הוק חוסם.
🚫 אל תשנה כלל-מוצר בשום קובץ מלבד processes-approved.md — הוא ה-SSOT היחיד.
   ✅ **אבל כלל-ברזל 13 מחייב write-back, וזה אינו "שינוי כלל-מוצר" — זה יישור לעובדה:** חמש אמירות על הדיסק
   **שקריות היום** ומתוקנות בשלב 0, כל אחת עם הערת-✏️ מתוארכת: ‏`db_roadmap:287` ("אין policy חדשה" — הופרך) ·
   `:289` ("default true" — הוכרע כבוי) · שם-העמודה שם · `PROJECT_MASTER §4א` ("שלבים 0–5 בלבד" — ישי דחה) ·
   ו-⬜ פריט 8 ב-`module_05_logistics/screens-approved.md` (חמשת נוסחי "ממוין:", שאיחודם אושר).
🚫 אל תשנה מונח שמופיע ב-§🔒 מילון-נעול בלי הכרעת-ישי — מונח נעול מצוטט בעשרות מקומות.
🔴 🚫 **אל תיגע במחרוזת `מנכ"ל` בשום מקום — היא אינה טקסט-ממשק אלא ערך-דאטה.** היא יושבת **בתוך ארבע מדיניות-RLS**
   (`docs/schema.sql:172·173·200·214·215`, `role_name = 'מנכ"ל'`) וב-37 קובצי-קוד (`CEO_ROLE_NAME`). **שינוי שלה
   שובר הרשאות בשקט** — הבדיקות לא בהכרח יתפסו את זה, והמסך ייראה תקין. ⚠️ **וזה חל גם כשמיישרים לשון-מגדר:**
   שמות-תפקידים אינם פנייה למשתמשת. אותו כלל לכל ערך שמופיע בטבלת `roles`.
🚫 **אל תמציא את שם העמודה — שני שמות חיים על הדיסק** (`onboarding_hints` ב-`db_roadmap.md:287` מול
   `onboarding_mode` ב-`ui-copy-and-onboarding-mode.md`). **השם המחייב: `onboarding_mode`** — **והטיפוס: `smallint not null default 0` + `check (onboarding_mode between 0 and 2)`, לא `boolean`** *(✏️ 07/09 17:1X, הכרעה 28⑭)*, ו-`db_roadmap`
   מתוקן לפניו באותו סשן. **במיגרציה זה בלתי-הפיך** — אל תריץ לפני שיושר.
🚫 אל תשנה טקסט של הודעת-שגיאה שבדיקה טוענת עליו בלי לתקן את הבדיקה באותו קומיט — אחרת השער נופל אצל ישי ולא אצלך.
🚫 אל תכתוב מיגרציה בלי שער-ההקלדה של supabase/migrations/CLAUDE.md.
🚫 git commit -- <נתיבים> בלבד. לעולם לא git add -A. לעולם לא push ל-dev/main.
🚫 אל תארכב ואל תסגור סשן — פעולת-ישי בלבד.
🚫 כתיבת קובץ מפייתון על Windows: חובה newline="\n", אחרת format:check נופל בשקט.
🔴 **ה-Stop hook יחסום אותך בכל תור, ואלה שתי האכיפות שתפגוש** (`.claude/hooks/check-docs-updated.sh`):
   **‏(0)** נגעת ב-`src/modules/NN_*/` ⇒ `docs/micro_guides/module-N.md` חייב להתעדכן **אחריו** — שורת
   `↳ as-built` אחת בסעיף-הסטיות (§9) של המדריך: *"07/09: טקסט-ממשק שוכתב לפי ui-copy-styleguide.md"*.
   **תשעת המדריכים קיימים.** ‏**(0ד)** קובץ חדש ב-`docs/plans/` בלי מצביע מקובץ-תקן ⇒ חסימה. **שלושת
   הקבצים שתיצור** (`ui-copy-styleguide.md` · `ui-copy-candidates.md` · `ui-copy-night-report.md`) **כבר
   רשומים מראש** ב-`PROJECT_MASTER.md §4א` — אל תשנה את שמותיהם. ⇒ **ובמחזור-המודול: קמט לפני שאתה
   מדווח**, לא אחרי — ההוק בודק mtime, ודיווח-לפני-קומיט משאיר את היומן ישן מהקוד.
🚫 **אסימוני-ההצבה של תבניות-המייל — `TEMPLATE_PLACEHOLDERS` ב-`src/lib/emailTemplates.js:16` — אינם טקסט.** **הצורה שלהם: סוגריים מרובעים** (`[שם_לקוח_חברה]` וכד') — 🚫 **לא `{{…}}`** *(סבב-3 מדד: אין אסימון כזה בקוד)*. המצאי
   סיווג אותם כ"תווית סבירה"; הם מפתחות שקוד מחליף בזמן-ריצה. שינוי שלהם = מייל שבו `[שם_לקוח_חברה]` מופיע ללקוח במקום שמו.

━━━ 🔴 סדר-העבודה — המקור: §4.2 של התוכנית ━━━
המחקר קובע סדר, והנימוק שלו חזק: **שכבת-הסבר שנבנית מעל מסכים שעוד לא שוכתבו תשמר את הבלגן במקום לתקן אותו —
היא תיתן בית נוח לכל משפט שהיה צריך להימחק.** ⇒ **הפרדה מחייבת:**
  • **המנגנון** (עמודה · מדיניות · מתג · <Hint> · onboardingCopy.js) — **נבנה בשלב 0.** זול, ואינו משפיע על
    שום מסך כל עוד הקובץ ריק.
  • **תוכן-ההסברים** — **נכתב אחרון, אחרי שהמסכים נקיים.** 🚫 אין למלא את onboardingCopy.js תוך כדי
    שכתוב-המסכים; משפט שנראה "שווה לשמור" בזמן הניקוי הוא בדיוק המשפט שצריך למחוק. ⇒ בכל מודול, משפט שיורד
    מהמסך נרשם ל-docs/plans/ui-copy-candidates.md כ**מועמד**, ורק בשלב האחרון מוכרע אם הוא נכנס לשכבה.
🔴 **ומספר שהמחקר נוקב בו: §4.1 פריט 7 ממליץ על 10–15 נקודות-הסבר בסך הכול.** ⚠️ **וזו אינה סתירה שסוגרים לבד:**
   המחקר כויל לחמש משתמשות יומיומיות; ישי הוסיף קהל שני — **שופטי-הכנס** (הכרעה 28⑪) — ולכן העושר מוצדק.
   **הכלל שמיישב:** אין תקרה מספרית, **אבל כל נקודה עוברת את R27 (מכסים אותה — משהו לא-ברור נשאר?) ואת R28
   (מה עושים אחרת אחריה?).** נקודה שלא עוברת — לא נכתבת, ולא משנה כמה היא "נחמדה".
🔴 **וחוסם קונקרטי שנמדד ליישור-המגדר (§4.1 פריט 2):** המחרוזת `נסה שוב` **מופיעה בקובצי-בדיקה** — כמה מהם באמת **טוענים** עליה נמדד בפתיחת שלב 2, לא כאן (`LoadingOrError.test.jsx`
   טוענת שהיא לא תזוז), ו-`LoadingOrError` בנוי לשני הכיוונים — ברירת-המחדל `'נסה שוב'` ו**שלושה אתרי-קריאה במ8 ובמ9** מעבירים `retryLabel="נסי שוב"` *(✏️ היה כתוב "מודול 6" — מ6 אינו מעביר אותו כלל)*. ✅ **הוכרע (ישי, 07/09 04:5X): מחליפים את ברירת-המחדל ב-`LoadingOrError` ל-`נסי שוב`,**
   ומתקנים באותו קומיט **כל בדיקה שבאמת טוענת עליה**: ‏`grep -rl 'נסה שוב' e2e/ src/` הוא **רשימת-המועמדים** (9 קבצים ב-07/09) — **פתח כל אחד**; רובם רק מכילים את המחרוזת. ⚠️ סבב-3: **שלושת ה-E2E מאתרים לפי `getByTestId` ולא ייפלו** — אל תשכתב אותם על סמך גרפ — **ולא: להמשיך להעביר `retryLabel` בכל קריאה. הנימוק:**
   אחרת החוב חוזר בכל מסך חדש, ו-`retryLabel` נשאר כמנגנון-עקיפה שאיש לא זוכר להעביר. ⇒ **ואז `retryLabel="נסי שוב"`
   ש**מ8 ומ9** מעבירים הופך למיותר — להסיר אותו באותו סבב, אחרת נשארות שתי דרכים לאותו דבר.**

━━━ ⑥ השלבים — מה כל אחד כותב לדיסק, ואיפה עוצרים ━━━
שלב 0 · המנגנון + נעילת-הסגנון  🛑 עצירה 1 (מתוך שתיים בלילה כולו)
  → מדריך-סגנון + מילון-מונחים ל-docs/plans/ui-copy-styleguide.md (זה התוצר החשוב בלילה).
     🔴 **ומה חייב להיות בו, אחרת סוכן לא יוכל להחיל אותו על מחרוזת בודדת:**
     ① ‏**~20 הכללים של `research-ui-copy-standards.md §1` — מועתקים לשם עם המבחן שליד כל אחד** (‏R1…R30).
        🚫 אל תנסח אותם מחדש; הם כבר כתובים עם מבחן-הפעלה.
     ② **החלטת-לשון:** נקבה ישירה כברירת-מחדל · ניסוח רב-מגדרי היכן שטבעי (`אין לך…`, `לא ניתן…`) ·
        🚫 בלי לוכסן (`מאשר/ת`). *(§3.2 של אותו קובץ.)*
     ③ **מילון-מונחים:** לכל מונח — הצורה היחידה המותרת. **המקור: §🔒 מילון-נעול** ב-`processes-approved.md`,
        ועליו נוספים מונחים שיתגלו בסריקה. **מונח בשני נוסחים = סתירה לפתור, לא בחירה** (R30).
     ④ **שלוש דוגמאות מלאות לפני/אחרי** מ-`research-onboarding-content.md §7` — כי כלל בלי דוגמה מיושם
        אחרת ע"י כל סוכן.
     ⑤ **מבחן-הקבלה של המדריך:** קח מחרוזת אקראית מהמצאי והחל עליה את המדריך לבד. אם שני סוכנים יגיעו
        לתוצאות שונות — המדריך אינו מוכן.
     ⑥ **כלל 📐23 — פעולה קודמת לעובדה — כתוב במדריך במפורש** (הכרעה 28⑬ מחייבת אותו על קובץ-הקופי;
        סבב-2 תפס שהוא חסר כאן): *כל משפט שהמערכת אומרת נפתח במה שהמשתמשת עושה, לא במה שהמערכת יודעת.*
        המבחן: כסי את החצי השני — החצי הראשון כבר אומר לה מה לעשות? **וב-`onboardingCopy.js` הוא הכלל
        הראשון**, לפני H1–H6.
  → מיגרציה: העמודה (**רמה `smallint` 0/1/2 `default 0` + `check` — הכרעה 28⑭, לא בוליאני**) + המדיניות הרביעית · src/lib/onboardingCopy.js · <Hint> · שני המתגים.
     🔴 **ואחרי `apply_migration`, באותו קומיט:** רענון `docs/schema.sql` + שורה ב-`docs/db_roadmap.md §10` — הפרוטוקול של `supabase/migrations/CLAUDE.md`; **אכיפה 0ב של ה-Stop hook חוסמת בלעדיהם.**
     ⚙️ **פרטי-מימוש שהפרומפט חייב לקבוע, אחרת כל סוכן ימציא אחרת:**
     • ‏`<Hint>` יושב ב-`src/components/Hint.jsx` (שם יושבים כל הרכיבים המשותפים).
     • **מפתח חסר ⇒ מחזיר `null` בשקט בייצור, ו-`console.warn` בפיתוח.** 🚫 לא זורק, ולא מציג את המפתח על המסך.
     • ✏️ 17:1X · **`<Hint id level>` מוצג כשה-`onboarding_mode` של המשתמשת ≥ `level`. הלילה כל Hint הוא `level={2}`** ורמה 1 ("מכוון") אינה נכתבת. **המתג בשני המסכים נשאר כן/לא:** כבוי ⇒ `0`, דלוק ⇒ `2` (וקריאה: `> 0` = דלוק). 🚫 **אל תבנה בורר-שלוש ואל תכתוב טקסט לרמה 1** — זו עבודת הסקיל הגלובלי שאחרי הלילה (הכרעה 28⑭).
     • **שמירה:** `update` **של העמודה הבודדת** `onboarding_mode` **בטבלה `notification_preferences`** — 🚫 **לעולם לא `upsert` של השורה** (התוכנית
       §3 פריט 7 ו-§7 סיכון ו' אוסרות אותו פעמיים: `saveNotificationPreferences` הקיימת שולחת את שתי
       העמודות תמיד, ו-`upsert` חלקי **מוחק את העדפת-המייל בשקט**). בדיקה ייעודית לתרחיש הזה — חלק משלב 0.
  → 🔴 בדיקות ל-src/modules/01_auth/ — שבעת המסכים שם הם אפס קובצי-בדיקה (נמדד 07/09), והמתג של המנכ"ל נוחת
     על UsersManagementPage.jsx. בונים בדיקות לפני השינוי, לא אחריו.
  קריטריון-סיום: המתג עובד בשני המסכים · הרגרסיה ירוקה על **מ1** (`UsersManagementPage`) ועל
  **`src/components/ProfileSettingsPage`** — ⚠️ **הוא ב-`src/components/`, לא במ9** · `npm run smoke` ירוק ·
  ישי ראה את המדריך.

שלבים 1–9 · מודול-אחרי-מודול, לפי §4 של התוכנית. לכל מודול, אותו מחזור:
  ① grep על כל מחרוזת שעומדת להשתנות בתוך e2e/ ו-*.test.* — לדעת מראש מה יישבר
  ② סוכני-כתיבה במקביל **בחלוקת-קבצים בלעדית על אותו עץ** (ר' ② הכלים — 🚫 לא worktree), כל אחד עם
     מדריך-הסגנון בגוף הבריף
  ③ מיזוג · הרצת הרגרסיה של אותו מודול + חבילה מלאה · **וגם `npm run smoke`** · תיקון בדיקות באותו קומיט.
     🔴 **למה `smoke` במפורש:** הוא **אינו** ב-`gate` ואינו ב-CI, ו-`e2e/smoke-anchors.json:21` נועץ
     `"sortLine": "ממוין: לפי קרבת האירוע"` — **בדיוק המחרוזת שהאיחוד המאושר משנה.** בלי הרצה מפורשת,
     השבירה תתגלה רק אצל ישי.
  ④ קומיט, ועדכון טבלת-המצב ב-ui-copy-and-onboarding-mode.md §4, והמשך מיד למודול הבא.
  ⑤ דיווח קצר בצ'אט — צילום לפני/אחרי + עד 10 שורות "מה השתנה ולמה". 🚫 אל תמתין לתשובה.

🔴 מודל-העצירות — הכרעת-ישי 07/09/2026. מילותיו: "יעבוד כל הלילה בשקט והכל טוב" + "כי זה באמת יהיה איתי".
   ⇒ הוא נוכח אבל אינו שער. **שתי עצירות-אמת בלבד בכל הלילה:**
   🛑 עצירה 1 — בסוף שלב 0, **ושני דברים קורים בה בבת-אחת:** (א) ישי מאשר את מדריך-הסגנון · (ב) ישי
      מקליד את הד-האישור למיגרציה (**שער-ההקלדה של `supabase/migrations/CLAUDE.md` הוא עצירת-אדם**
      — מבקר תפס שהיא הייתה "שלישית" מוסתרת; היא מקופלת לכאן ואינה נוספת). זו העצירה היקרה: כל 2,311
      המחרוזות נכתבות לפי המדריך, והמיגרציה בלתי-הפיכה.
      אחרי חמישה מודולים עולה פי חמישה.
   🛑 עצירה 2 — בסוף המודול הראשון בלבד, לאימות-הצורה. אם הוא אומר "ממשיך" — **אתה רץ עד הבוקר בלי לעצור שוב.**
   ומעבר לשתיהן — עוצרים רק על אחד משלושה: סתירה בין שתי הכרעות רשומות שלו · רגרסיה אדומה שאינה מתיישרת ·
   דבר שמשתמשת תראה ואין לו תקדים. 🚫 לא עוצרים על "רציתי לוודא". דיווח ≠ עצירה.
   📄 ובבוקר: קובץ אחד — docs/plans/ui-copy-night-report.md — כל הלפני/אחרי במקום אחד, לפי מודול. זה מה
      שישי קורא, ולא תשעה דיווחי-צ'אט שנקברו בגלילה.
בסוף הלילה: מעבר-מסמך-שלם — לקרוא שני מסכים ברצף ולשאול "האם זה אותו קול?".

שלב 10 · עין טרייה — **2+2 ומיזוג** *(הכרעת-ישי 07/09 16:0X; הפירוט: התוכנית §4 שלב 10)*
  🔴 למה: שלב 9 הוא הסשן שבודק את עצמו, **ואחוז התפיסה-העצמית על תוצר עצמי נמדד אצלנו כאפס.**
  הבדיקות רואות שהקוד רץ; הן אינן רואות אם משפט נשמע כמו מפתח. ישי הכריע "בלי צבא" — ואז חידד:
  *"2 סוכנים שבודקים את המצב הבוגר ו-2 סוכנים בודקים את מצב ההטמעה, כי זה 2 דברים שונים לגמרי."*
  → **זוג-המכובה:** השאלה *"מהנדס רואה מוצר בוגר או מפתח?"* + כללי הבסיס (התוכנית §1א) · המתג נעול כבוי
  → **זוג-הדלוק:** השאלה *"משתמשת חדשה יודעת מה לעשות ולמה?"* + כללי השכבה (§1ב) · המתג נעול דלוק
  → **בתוך כל זוג: בלתי-תלויים על אותם מסכים** (Nielsen — מעריך אחד ~35%, שלושה ~75%; הפיצול לפי מצב
    נותן עדשה אחת למעריך, וארבע עיניים על השלד המשותף). 🚫 איש מהם לא מקבל את יומן-השינויים, את
    הנימוקים, את ממצאי האחרים, ולא את המצב השני.
  → **המיזוג (אתה):** טבלה אחת, שתי עמודות · ראו שניים ⇒ ✅ · ראה אחד ⇒ ⚠️ לבדיקתך · סתירה ⇒ ישי ·
    **ועמודה שלישית שרק אתה ממלא:** *האם מה שהדלוק מסביר הוא מה שהמכובה מראה?*
  → **קריטריון-סיום:** אפס ❌ · כל ⚠️ עם הכרעה כתובה · והוורדיקט הוא **הסעיף הראשון** בקובץ-הבוקר —
    **ישי קורא אותו לפני השינויים**, כדי לשפוט עם עין שלא נחשפה לנימוקים.

━━━ ⑦ צורת-הפלט לישי בכל עצירה ━━━
• עד 10 שורות: מה השתנה · למה (הכלל מהמדריך) · מה נשבר ותוקן
• צילום אחד: לפני/אחרי, ובמצב-הטמעה דלוק וכבוי · "מה שלא עשיתי ולמה" — אם אין, לכתוב "אין"
• שורת-סיום: מה פתוח · מה ממתין לו · מה נסגר
🚫 לא לשלוח לו רשימת-מחרוזות. הוא אינו קורא קוד — הוא שופט מסכים.

━━━ ⑧ אם המכסה נגמרת באמצע ━━━
עצור בגבול-מודול, קמט, ועדכן את טבלת-המצב ב-§4 של התוכנית + STATUS.md + CLAUDE_CODE_LOG.md אחרונים.
**מודול שלא הושלם — להחזיר אחורה, לא להשאיר חצי.** הריפו חייב להישאר עקבי.
🔴 **ואיך מחזירים בפועל — כי "להחזיר" בלי מנגנון אינו הוראה:** כל מודול הוא **קומיט אחד** (‏④ למעלה). מודול
   שנקטע ⇒ `git restore -- <הקבצים שנגעת בהם>` על **מה שלא קומט**; ‏🚫 **לא `git reset --hard`** (סוחף עבודה
   של מודולים קודמים) ו🚫 **לא `git revert`** על קומיט שכבר עבר רגרסיה ירוקה. **מה שקומט — נשאר.** ⇒ **ולכן:
   אל תקמט מודול לפני שהרגרסיה שלו ירוקה.** ⚠️ **ובדוק את הענף לפני הקומיט הראשון:**
   `git rev-parse --abbrev-ref HEAD` חייב להחזיר `ishay/ui-copy-rewrite`. קומיט לענף הלא-נכון בלילה של
   100 סוכנים הוא בלגן יקר לפרום.

━━━ ✅ מבחן-הקבלה — ההודעה הראשונה שלך היא התשובות האלה, לפני כל פעולה ━━━
🔴 **עשר שאלות. התשובות כולן בבריף הזה ובקבצים שהוא מפנה אליהם. אל תנחש — אם אינך יודע, כתוב "לא מצאתי" ליד
השאלה. תשובה שגויה אחת = ישי עוצר אותך לפני שנגעת בקוד, וזה בדיוק התפקיד של המבחן.**
  1. מהו **המוקש העליון** — האחד שכל השאר משני לו?
  2. **כמה עצירות-ישי** יש בלילה כולו, ואחרי מה בדיוק כל אחת?
  3. איזו מחרוזת **אסור לגעת בה לעולם כי היא ערך בתוך מדיניות-RLS** — ולמה היא אינה טקסט-ממשק?
  4. מה **שם העמודה** במסד, על איזו טבלה, **ומה הטיפוס שלה — ולמה לא בוליאני**? ומה **שם הענף**, ומאיפה הוא נחתך?
  5. **מתי** נכתב מלל-ההטמעה — באיזה שלב, ומה קורה בינתיים למשפט שיורד מהבסיס?
  6. מה רץ **בכל מחזור-מודול** מעבר לבדיקות של אותו מודול — ולמה זה חייב להיאמר במפורש?
  7. מה **שלב 1** עושה בלילה של שלישי?
  8. **"לאפס" או "לערוך"** — ומה הצעד השני שמפריד בין "לאפס" ל"למחוק"?
  9. כמה מחרוזות יש **בסך הכול**, כמה במודול 6, וכמה ב-`src/lib/` — ולמה `lib` הוא הבעיה?
  10. **כמה מעריכים** בשלב 10, **איך הם מחולקים**, ומה **הדבר היחיד** שרק הממזג בודק?
ואחרי העשר: **האם המשימה ברורה? יש לך שאלות?** — וכל שאלה שלך היא **על סתירה** שמצאת, לא על דבר שכתוב.
דבר שכתוב — קראת.
```

---

## שתי ההכרעות שנפתחו לישי — ומה נענה

**מה שנשאר פתוח להכרעתו בפתיחת אותו סשן: אין.** שתיהן הוכרעו 07/09 04:5X ("לפי המלצותיך"), ונרשמות כאן כדי שהנימוק לא יאבד:

| | הפריט | ההמלצה שהוצגה | ההכרעה |
|:-:|---|---|---|
| **1** | ענף | `dev` *(המלצתי המקורית — הופרכה בסבב-2: התיעוד אינו שם)* | ✅ **הוכרע:** `ishay/ui-copy-rewrite` **מ-`ishay/module-11-reports`** (לא מ-`dev` — התיעוד אינו שם; אומת 07/09 17:0X) |
| **2** | האם שלב 0 רץ לפני מ11 או אחריו? | **לפניו** — מ11 ייבנה עם `<Hint>` מלכתחילה במקום להישתל בדיעבד | ✅ לפניו — המיגרציה עומדת בפני עצמה (ר' פסקת-הענף) |

⚠️ **ופריט אחד שאינו הכרעה אלא אזהרה:** ‏`src/modules/01_auth/` הוא **אפס קובצי-בדיקה בשבעה מסכים** (נמדד 07/09/2026). זה חוב שקדם למ11, והוא נפרע בשלב 0 **כי המתג נוחת שם** — לא כי מישהו החליט לפרוע אותו.

</div>
