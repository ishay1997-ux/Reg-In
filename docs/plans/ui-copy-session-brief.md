<div dir="rtl">

# בלוק-ההדבקה לסשן שכתוב-הטקסטים — לילה של שלישי 08/09/2026

> **מה הקובץ הזה:** הפרומפט שישי מדביק לסשן טרי. **הוא מצביע — הוא אינו משכפל.** התוכנית
> המלאה חיה ב-[`ui-copy-and-onboarding-mode.md`](ui-copy-and-onboarding-mode.md), וההכרעות
> ב-`docs/specs/module_11_reports/processes-approved.md §🗳️ 28`.
> 🔗 **מראת הכרעה 28 — SSOT: `processes-approved.md §🗳️` (לא לערוך כאן).**
> **נכתב:** 07/09/2026 03:4X, בהוראת-ישי: *"פשוט צריך שאתה תכתוב תוכנית באמת ממש טובה עבור
> אותו סשן, עם קונטקסט מה לקרוא, שלבי עבודה וכו'."*

---

## לישי — שלוש שורות לפני שאתה מדביק

הסשן הזה **בונה קוד**, לא מאפיין. הוא מקבל תוכנית סגורה ומבצע אותה מודול-אחרי-מודול.
🔴 **והוא עוצר מולך פעמיים בלילה כולו — לא אחרי כל מודול** *(תוקן 07/09 04:0X אחרי שתפסת את
הסתירה מול "יעבוד כל הלילה בשקט")*: **בסוף שלב 0** על מדריך-הסגנון, **ובסוף המודול הראשון**
לאימות-הצורה. אחרי שתאמר "ממשיך" — הוא רץ עד הבוקר ומדווח בלי להמתין, ומשאיר לך **קובץ-בוקר
אחד** עם כל הלפני/אחרי במקום תשעה דיווחים שנקברים בגלילה. **דגם: Fable, מאמץ גבוה.** 🔴 **ו-100 הוא תקרה שישי הציב, לא יעד** *(במילותיו, 07/09: *"אני סתם זרקתי 100, שהוא יבחר איך נכון לנהל את זה — זו המכסה שלי לסשן"*)* ⇒ **הסשן מחליט כמה סוכנים באמת נכון**, ומדווח את המספר ואת הנימוק לפני כל גל. ‏**ההערכה:** ~40–60 סוכנים, רובם Sonnet לכתיבה ו-Opus לנעילת-הסגנון ולביקורת — **‏6–9 מיליון טוקנים**, שזה הסדר
שבו נגמרת מכסת-לילה שלמה. **תתחיל ב-22:00 כשהמכסה מתאפסת.**

🔴 **ומה שאני ממליץ שתדע לפני שאתה מתחיל:** החסם אינו השעות — הוא **אחידות-הקול**. מאה סוכנים
שכותבים עברית במקביל מייצרים מאה קולות. **לכן שלב 0 בפרומפט נועל מדריך-סגנון ומילון-מונחים
לפני שסוכן אחד נוגע במחרוזת**, וזו הנקודה היחידה שבה כדאי שתשקיע את העיניים שלך.

---

```
אתה סשן-בנייה. אתה משכתב את כל טקסט-הממשק של REG-IN ובונה את "מצב הטמעה".
ענף: ishay/module-11-reports (או ענף חדש ממנו — הכרעת ישי בפתיחה).

━━━ 🔴 המוקש העליון — אחד, וכל השאר משני לו ━━━
אחידות-קול. אתה מפעיל ~100 סוכנים שכותבים עברית במקביל. בלי מדריך-סגנון נעול הם יפיקו
מאה קולות שונים, וזה כישלון שלא רואים בבדיקות ולא ב-lint — רואים אותו רק כשקוראים שני
מסכים ברצף. ⇒ שלב 0 נועל מדריך-סגנון + מילון-מונחים, ואף סוכן-כתיבה אינו מופעל לפניו.
כל סוכן-כתיבה מקבל את המדריך בגוף הבריף שלו, ומחזיר ציטוט של הכלל שהחיל על כל מחרוזת שינה.

━━━ 🔴 המוקש השני — הדפוס הזה נכשל בעולם, ויש לכך מקור ━━━
מדריך-העיצוב של Microsoft ל-Windows **אוסר במפורש** כפתור-עזרה-הקשרית ("?") וכפתורי-"Learn more"
גנריים — הנימוק שלהם: **מדידות-שמישות הראו שמשתמשים מתעלמים מהם.** זה בדיוק הדפוס שאנחנו בונים.
⇒ ‏`research-ui-copy-standards.md §2.2` מונה **שש דרכים שבהן שכבת-עזרה מזיקה**. שתיים מהן הן
חוקי-בנייה מחייבים כאן, ולא "טוב לדעת":
  ① **המסך לא נשען על השכבה.** מבחן-המחיקה בסוף כל מודול: כבה את המתג — האם המסך עדיין ניתן
     לתפעול מלא? מידע חיוני שנדד לשכבה = באג, לא פיצ'ר.
  ② **הסבר מובן-מאליו מרעיל את כל השכבה.** NN/g מדדו אפקט-הכללה: עזרה אחת חסרת-ערך גורמת
     למשתמשות להתעלם מכולה. ⇒ R27/R28 של §1.5 (מבחן-הכיסוי ומבחן-"מה עושים אחרת") הם שער,
     לא המלצה — משפט שלא עובר אותם **לא נכתב**.
📄 וארבעת התנאים שהמקורות מסכימים שגורמים לדפוס כן לעבוד (§2.3): משיכה-ולא-דחיפה · הקשרי-ולא-כללי ·
   משפט-ולא-פסקה · "למה" ולא "מה".

━━━ 🔴 המוקש השלישי — הפגם הכי גלוי לעין בכנס, ואינו סגנון ━━━
נמדד (§0 של אותו קובץ): **74 מחרוזות בלשון זכר ב-24 קבצים · 9 בלשון נקבה ב-8 קבצים · 6 ברבים** —
בעוד **כל חמש המשתמשות נשים**, ו-`S-28` כבר הכריע נקבה למודולים 5–8. ⇒ **איחוד לשון-הפנייה הוא
פריט-חובה בשלב 2 ואינו נדחה.** הפירוט: `research-ui-copy-standards.md §3.1`.

━━━ ① מה אתה מייצר ━━━
(א) המנגנון: עמודה + מדיניות-RLS רביעית + מתג בשני מסכים + <Hint> + src/lib/onboardingCopy.js
(ב) שכתוב טקסט-הממשק ב-9 המודולים המוזגים, מודול-אחרי-מודול
(ג) שכבת-ההסבר שנדלקת במתג
כל מודול = קומיט משלו, אחרי שהרגרסיה שלו ירוקה ואחרי שישי ראה לפני-ואחרי.

━━━ ② הכלים ━━━
• Agent — **תקרה** של 100 לאורך הלילה, לא יעד. אתה בוחר את המספר ומנמק אותו. 🚫 ישי הכריע: בלי Workflow ובלי שכבות-אימות
  ("בלי וורקפלואו עם מיליון אימותים כי זה באמת יהיה איתי") — הוא נוכח וזו הבקרה.
  ⇒ הסוכנים כותבים; האימות הוא הרגרסיה + עיניו, לא צבא-מבקרים.
🔴 • **`worktree` לא עובד בריפו הזה — אל תשתמש בו** *(נמדד 07/09: `node_modules` ו-`*.local`
  ב-`.gitignore` ⇒ ורקטרי טרי הוא בלי תלויות ובלי `.env.local`, שבו יושבים `E2E_CEO_*` —
  סוכן שם אינו יכול להריץ ולו בדיקה אחת, ואין מסלול-החזרה למיזוג).*
  **החלופה שכן עובדת, והיא גם פשוטה יותר:** סוכנים מקבילים **על אותו עץ, בחלוקת-קבצים
  בלעדית** — כל סוכן מקבל רשימת-קבצים משלו ואינו נוגע באחרים; אתה ממזג בעץ אחד ומריץ
  את הרגרסיה פעם אחת פר-מודול. **תנאי:** אל תשגר שני סוכנים על אותו קובץ (כלל-ברזל 16).
• Bash/Edit/Read/Grep · Supabase MCP (כתיבה למסד רק דרך apply_migration, בשער-הקלדה)
• Browser pane לצילומי לפני/אחרי

━━━ ③ רשימת-הקריאה — חובה, בסדר הזה, לפני פעולה ראשונה ━━━
1. docs/plans/ui-copy-and-onboarding-mode.md — במלואה. זו התוכנית; §4 הוא סדר-העבודה,
   §6 העלות, §7 הסיכונים, §10 ההכרעות (כולן נענו — ר' ④).
2. docs/plans/research-ui-copy-standards.md — כללי-הכתיבה. מהם נגזר מדריך-הסגנון בשלב 0.
3. docs/plans/research-onboarding-content.md — §1 (שבעת סוגי-האמירה) · §3 (מה הופך הסבר
   לחסר-ערך) · §7 (חמש מחרוזות לפני/אחרי — הדוגמה המחייבת לצורת-העבודה).
4. docs/plans/inventory-ui-copy.md — 2,311 המחרוזות המסווגות (§1 המספרים · §2 הטבלה ·
   §3 שתים-עשרה ה"לא-שם-ולא-שם"). זה מצע-העבודה.
   🔴 ואל תניח שהקופי כבר קיים: רק ארבעה מודולים (4·5·6·8) מחזיקים screens-approved.md,
   ו-58 מופעי-⑥ שבהם אינם מכסים את 939 המחרוזות שתחתם. **1,372 מחרוזות — 59.4% — יושבות
   היום במקום שאין לו מסלול-תיעוד כלל.** ⇒ רוב הקופי נכתב מאפס, לא מועתק.
5. docs/specs/module_11_reports/processes-approved.md — §🗳️ הכרעה 28 בלבד, על כל חלקיה.
   ⚠️ הקובץ גדל ראש-ואז-זנב: קרא כל חלק עד סופו לפני שאתה מצטט.
6. docs/PROJECT_MASTER.md §4א — שלושת החוסמים שנמדדו, ו-§1 "ארבעת החלקים" לפני כל שאלה לישי.
7. src/CLAUDE.md + supabase/migrations/CLAUDE.md — לפני קוד ולפני מיגרציה.
8. .claude/skills/_shared/writing-prompts.md — לפני שאתה מבריף סוכן.

🟡 כנראה לא תצטרך — פתח רק על פער אמיתי, ואמור שפתחת:
docs/reference_spec/** (קפוא) · docs/specs/module_0*/screens-approved.md (רק אם מחרוזת
לא מובנת ואתה צריך את ההקשר המקורי שלה) · docs/CLAUDE_CODE_LOG.md.

━━━ ④ מה כבר הוכרע — במילותיו של ישי, ואינו נפתח מחדש ━━━
• המטרה: "לשכתב מחדש את כל המלל במערכת שיהיה ברמה של מוצר מוגמר בהייטק… לכתוב בצורה נעימה
  ידידותית ובאמת שתהיה מובנת למשתמש — זה מה שיוצג ברגע שהמנכ"ל יפעיל את הטוגל… ואז המערכת
  תראה עם הרבה מלל… ואציג גם את המצב הנקי והבוגר של המערכת."
  ⇒ שתי שפות: המצב המכובה נשפט על נקיוּן ומוסכמה · הדלוק על חום ומובנוּת.
  ⇒ השכתוב בבסיס הוא ברובו הסרה והידוק. משפט שיורד מהבסיס עובר לשכבה — לא נמחק.
  ⚠️ "מוצר מוגמר בהייטק" = נקי כפי שמקובל, לא "כתוב יפה יותר" (חידודו, 07/09).
• "והמשתמש יוכל להדליק ולכבות לעצמו בפרופיל גם" ⇒ שתי דלתות על עמודה אחת:
  המנכ"ל פר-משתמשת בניהול-המשתמשים · המשתמשת לעצמה בפרופיל.
• ברירת-מחדל: כבוי. (ישי, 07/09: "1. בסדר")
• מדיניות-RLS רביעית: כן. (ישי, 07/09: "2. בסדר") — דפוס להעתקה: users_write_ceo_only.
• היקף: הכול, בלי לעצור בשלב 5. "עושים עבודה פעם אחת כמו שצריך בלי קיצורי דרך."
• איחוד חמשת נוסחי "ממוין:": מאושר. ("6. וכן יכול לאחד 5 נוסחים")
• "אין סיבה להוסיף עבודה גם אם זה שיפור נחמד" — אל תרחיב היקף מעבר לתוכנית.

━━━ ⑤ מה אסור — כל אחד עם הסיבה ━━━
🚫 אל תיגע ב-docs/reference_spec/** — קפוא, הוק חוסם.
🚫 אל תשנה כלל-מוצר בשום קובץ מלבד processes-approved.md — הוא ה-SSOT היחיד.
   ✅ **אבל כלל-ברזל 13 מחייב write-back, וזה אינו "שינוי כלל-מוצר" — זה יישור לעובדה:**
   חמש אמירות על הדיסק **שקריות היום** ומתוקנות בשלב 0, כל אחת עם הערת-✏️ מתוארכת:
   ‏`db_roadmap:287` ("אין policy חדשה" — הופרך) · `:289` ("default true" — הוכרע כבוי) ·
   שם-העמודה שם · `PROJECT_MASTER §4א` ("שלבים 0–5 בלבד" — ישי דחה) · ו-⬜ פריט 8 ב-
   `module_05_logistics/screens-approved.md` (חמשת נוסחי "ממוין:", שאיחודם אושר).
🚫 אל תשנה מונח שמופיע ב-§🔒 מילון-נעול בלי הכרעת-ישי — מונח נעול מצוטט בעשרות מקומות.
🔴 🚫 **אל תיגע במחרוזת `מנכ"ל` בשום מקום — היא אינה טקסט-ממשק אלא ערך-דאטה.**
   היא יושבת **בתוך ארבע מדיניות-RLS** (`docs/schema.sql:172·173·200·214·215`,
   `role_name = 'מנכ"ל'`) וב-37 קובצי-קוד (`CEO_ROLE_NAME`). **שינוי שלה שובר הרשאות בשקט** —
   הבדיקות לא בהכרח יתפסו את זה, והמסך ייראה תקין. ⚠️ **וזה חל גם כשמיישרים לשון-מגדר:**
   שמות-תפקידים אינם פנייה למשתמשת. אותו כלל לכל ערך שמופיע בטבלת `roles`.
🚫 **אל תמציא את שם העמודה — שני שמות חיים על הדיסק** (`onboarding_hints` ב-`db_roadmap.md:287`
   מול `onboarding_mode` ב-`ui-copy-and-onboarding-mode.md`). **השם המחייב: `onboarding_mode`**,
   ו-`db_roadmap` מתוקן לפניו באותו סשן. **במיגרציה זה בלתי-הפיך** — אל תריץ לפני שיושר.
🚫 אל תשנה טקסט של הודעת-שגיאה שבדיקה טוענת עליו בלי לתקן את הבדיקה באותו קומיט —
   אחרת השער נופל אצל ישי ולא אצלך.
🚫 אל תכתוב מיגרציה בלי שער-ההקלדה של supabase/migrations/CLAUDE.md.
🚫 git commit -- <נתיבים> בלבד. לעולם לא git add -A. לעולם לא push ל-dev/main.
🚫 אל תארכב ואל תסגור סשן — פעולת-ישי בלבד.
🚫 כתיבת קובץ מפייתון על Windows: חובה newline="\n", אחרת format:check נופל בשקט.

━━━ 🔴 סדר-העבודה — תוקן 07/09 04:3X אחרי קריאת §4.2, שסתר את הטיוטה ━━━
המחקר קובע סדר, והנימוק שלו חזק: **שכבת-הסבר שנבנית מעל מסכים שעוד לא שוכתבו תשמר את הבלגן
במקום לתקן אותו — היא תיתן בית נוח לכל משפט שהיה צריך להימחק.** ⇒ **הפרדה מחייבת:**
  • **המנגנון** (עמודה · מדיניות · מתג · <Hint> · onboardingCopy.js) — **נבנה בשלב 0.** זול,
    ואינו משפיע על שום מסך כל עוד הקובץ ריק.
  • **תוכן-ההסברים** — **נכתב אחרון, אחרי שהמסכים נקיים.** 🚫 אין למלא את onboardingCopy.js
    תוך כדי שכתוב-המסכים; משפט שנראה "שווה לשמור" בזמן הניקוי הוא בדיוק המשפט שצריך למחוק.
    ⇒ בכל מודול, משפט שיורד מהמסך נרשם ל-docs/plans/ui-copy-candidates.md כ**מועמד**, ורק
    בשלב האחרון מוכרע אם הוא נכנס לשכבה.
🔴 **ומספר שהמחקר נוקב בו ואינו תואם את הטיוטה: §4.1 פריט 7 ממליץ על 10–15 נקודות-הסבר בסך הכול.**
   ⚠️ **וזו אינה סתירה שסוגרים לבד:** המחקר כויל לחמש משתמשות יומיומיות; ישי הוסיף קהל שני —
   **שופטי-הכנס** (הכרעה 28⑪) — ולכן העושר מוצדק. **הכלל שמיישב:** אין תקרה מספרית, **אבל כל
   נקודה עוברת את R27 (מכסים אותה — משהו לא-ברור נשאר?) ואת R28 (מה עושים אחרת אחריה?).**
   נקודה שלא עוברת — לא נכתבת, ולא משנה כמה היא "נחמדה".

🔴 **וחוסם קונקרטי שנמדד ליישור-המגדר (§4.1 פריט 2):** המחרוזת `נסה שוב` **נעולה בבדיקה**
   (`LoadingOrError.test.jsx` טוענת שהיא לא תזוז), ו-`LoadingOrError` בנוי לשני הכיוונים —
   ברירת-המחדל `'נסה שוב'` ומודול 6 מעביר `retryLabel="נסי שוב"`. ⇒ **הכרעה נדרשת לפני היישור:**
   להחליף את ברירת-המחדל ולתקן את הבדיקה, או להמשיך להעביר `retryLabel` בכל קריאה.
   **ההמלצה: להחליף את ברירת-המחדל** — אחרת החוב חוזר בכל מסך חדש. **וזו הכרעת-ישי.**

━━━ ⑥ השלבים — מה כל אחד כותב לדיסק, ואיפה עוצרים ━━━
שלב 0 · המנגנון + נעילת-הסגנון  🛑 עצירה 1 (מתוך שתיים בלילה כולו)
  → מדריך-סגנון + מילון-מונחים ל-docs/plans/ui-copy-styleguide.md (זה התוצר החשוב בלילה)
  → מיגרציה: העמודה + המדיניות הרביעית · src/lib/onboardingCopy.js · <Hint> · שני המתגים
  → 🔴 בדיקות ל-src/modules/01_auth/ — שבעת המסכים שם הם אפס קובצי-בדיקה (נמדד 07/09),
     והמתג של המנכ"ל נוחת על UsersManagementPage.jsx. בונים בדיקות לפני השינוי, לא אחריו.
  קריטריון-סיום: המתג עובד בשני המסכים · הרגרסיה ירוקה על **מ1** (`UsersManagementPage`)
  ועל **`src/components/ProfileSettingsPage`** — ⚠️ **הוא ב-`src/components/`, לא במ9**
  (טעות שהתוכנית כבר תיקנה והפרומפט החזיר) · `npm run smoke` ירוק · ישי ראה את המדריך.

שלבים 1–9 · מודול-אחרי-מודול, לפי §4 של התוכנית. לכל מודול, אותו מחזור:
  ① grep על כל מחרוזת שעומדת להשתנות בתוך e2e/ ו-*.test.* — לדעת מראש מה יישבר
  ② סוכני-כתיבה במקביל (worktree לכל אחד), כל אחד עם מדריך-הסגנון בגוף הבריף
  ③ מיזוג · הרצת הרגרסיה של אותו מודול + חבילה מלאה · **וגם `npm run smoke`** ·
     תיקון בדיקות באותו קומיט
     🔴 **למה `smoke` במפורש:** הוא **אינו** ב-`gate` ואינו ב-CI, ו-`e2e/smoke-anchors.json:21`
     נועץ `"sortLine": "ממוין: לפי קרבת האירוע"` — **בדיוק המחרוזת שהאיחוד המאושר משנה.**
     בלי הרצה מפורשת, השבירה תתגלה רק אצל ישי.
  ④ דיווח קצר בצ'אט — צילום לפני/אחרי + עד 10 שורות "מה השתנה ולמה". 🚫 אל תמתין לתשובה.
  ⑤ קומיט, ועדכון טבלת-המצב ב-ui-copy-and-onboarding-mode.md §4, והמשך מיד למודול הבא.

🔴 מודל-העצירות — הכרעת-ישי 07/09/2026, ותוקן כאן אחרי שהוא תפס סתירה:
   מילותיו: "יעבוד כל הלילה בשקט והכל טוב" + "כי זה באמת יהיה איתי".
   ⇒ הוא נוכח אבל אינו שער. **שתי עצירות-אמת בלבד בכל הלילה:**
   🛑 עצירה 1 — בסוף שלב 0, על מדריך-הסגנון. זו העצירה היקרה: כל 2,311 המחרוזות
      נכתבות לפיו, ותיקון-סגנון אחרי חמישה מודולים עולה פי חמישה.
   🛑 עצירה 2 — בסוף המודול הראשון בלבד, לאימות-הצורה. אם הוא אומר "ממשיך" —
      **אתה רץ עד הבוקר בלי לעצור שוב.**
   ומעבר לשתיהן — עוצרים רק על אחד משלושה: סתירה בין שתי הכרעות רשומות שלו ·
   רגרסיה אדומה שאינה מתיישרת · דבר שמשתמשת תראה ואין לו תקדים.
   🚫 לא עוצרים על "רציתי לוודא". דיווח ≠ עצירה.
   📄 ובבוקר: קובץ אחד — docs/plans/ui-copy-night-report.md — כל הלפני/אחרי במקום אחד,
      לפי מודול. זה מה שישי קורא, ולא תשעה דיווחי-צ'אט שנקברו בגלילה.

בסוף הלילה: מעבר-מסמך-שלם — לקרוא שני מסכים ברצף ולשאול "האם זה אותו קול?".

━━━ ⑦ צורת-הפלט לישי בכל עצירה ━━━
• עד 10 שורות: מה השתנה · למה (הכלל מהמדריך) · מה נשבר ותוקן
• צילום אחד: לפני/אחרי, ובמצב-הטמעה דלוק וכבוי
• "מה שלא עשיתי ולמה" — אם אין, לכתוב "אין"
• שורת-סיום: מה פתוח · מה ממתין לו · מה נסגר
🚫 לא לשלוח לו רשימת-מחרוזות. הוא אינו קורא קוד — הוא שופט מסכים.

━━━ ⑧ אם המכסה נגמרת באמצע ━━━
עצור בגבול-מודול, קמט, ועדכן את טבלת-המצב ב-§4 של התוכנית + STATUS.md + CLAUDE_CODE_LOG.md
אחרונים. מודול שלא הושלם — להחזיר אחורה, לא להשאיר חצי. הריפו חייב להישאר עקבי.

האם המשימה ברורה? יש לך שאלות? — ההודעה הראשונה שלך עונה על זה, ומצהירה:
כמה מחרוזות בכל מודול לפי inventory-ui-copy.md, ומה מדריך-הסגנון קובע על לשון-הפנייה.
```

---

## מה שנשאר להכרעת-ישי בפתיחת אותו סשן

| | הפריט | ההמלצה |
|:-:|---|---|
| **1** | ענף חדש או המשך `ishay/module-11-reports`? | **ענף חדש** מ-`dev`, `ishay/ui-copy-rewrite` — השכתוב אינו של מ11 ונוגע בתשעה מודולים |
| **2** | האם שלב 0 רץ לפני מ11 או אחריו? | **לפניו** — מ11 ייבנה עם `<Hint>` מלכתחילה במקום להישתל בדיעבד |

⚠️ **ופריט אחד שאינו הכרעה אלא אזהרה:** ‏`src/modules/01_auth/` הוא **אפס קובצי-בדיקה בשבעה
מסכים** (נמדד 07/09/2026). זה חוב שקדם למ11, והוא נפרע בשלב 0 **כי המתג נוחת שם** — לא כי
מישהו החליט לפרוע אותו.

</div>
