<div dir="rtl">

# מועמדים לשכבת-ההסבר — משפטים שירדו מהבסיס וממתינים להכרעה בשלב 9

> **מה הקובץ הזה:** רשם-המועמדים של לילה-הטקסטים (`ui-copy-session-brief.md` ⑥ · `PROJECT_MASTER §4א`). כל משפט שיורד
> מהבסיס בשלבים 2–8 נרשם כאן **כמועמד** — לא נכתב ל-`src/lib/onboardingCopy.js` (כלל ⑥ של הבריף: שום מלל-הטמעה לפני שהמסכים נקיים).
> **ההכרעה נופלת בשלב 9 בלבד**, לפי שני השערים: R27 (מכסים אותו — משהו לא-ברור נשאר?) · R28 (מה עושים אחרת אחריו?).
> **השאלה כאן:** *לאיזו רמה* (חזרה לבסיס · רמה 2 · נעלם). ⚠️ אותו משפט יכול להופיע גם ב-`ui-copy-structural-findings.md` — שם השאלה אחרת (*איזה שינוי-מסך היה מייתר אותו*), וזו אינה כפילות.
> **נוצר:** 08/09/2026 23:3X, סשן 2 של הלילה, פתיחת שלב 2. **מי כותב:** הסשן הממזג בלבד, מדיווחי הכותבים (המדריך §8).
> **סוגי-האמירה** (`research-onboarding-content.md` §1): ① ייעוד · ② צעד-ראשון · ③ מונח · ④ מקור-מספר · ⑤ נימוק-סידור · ⑥ תוצאת-לחיצה (לעולם לא לשכבה) · ⑦ המשך.

## שלב 2 · רכיבים משותפים + לשון-נקבה

| # | מסך · קובץ | המשפט שירד (לפני) | הבסיס אחריו | למה ירד | סוג | הכרעת שלב 9 |
|---|---|---|---|---|---|---|
| C-01 | כרטיס-לקוח — טופס אנשי-קשר (`CustomerFormDialog.jsx`, `NO_PRIMARY_MSG`) | `אי אפשר למחוק את איש הקשר הראשי. סמן קודם אחר כראשי.` | **נשאר בזכר בכוונה** — קשור-בייט ל-`raise exception` של ה-RPC `replace_customer_contacts` (מיגרציה `20260902141451`), ונוסח-ישי (`02_customers/CLAUDE.md`) | שינוי = מיגרציה בשער-ההקלדה של ישי — לא בלילה | — | ⏳ **שלב 7:** הצעה למיגרציה + לקוח יחד: `אי-אפשר למחוק את איש הקשר הראשי. סמני קודם איש קשר אחר כראשי.` (הדוגמה המאושרת בתוכנית §4 שלב 7) |
| C-02 | חלון-הסגירה (`ClosingWindowDialog.jsx`, `FEE_STORAGE_NOTE`) | `נשמר במסד: הסכום הסופי וההערה בלבד — שלושת הרכיבים שלמעלה נגזרים-מחדש לתצוגה בכל פתיחה, ולא נשמרים כעמודות נפרדות.` | טרם ירד — עדיין חי (שלב 5) | המדריך §4 דוגמה 2 כבר הכריע: רמה 0 נמחק · רמה 2 `closing.frozenAmount` | ④ | ⏳ שלב 5 מבצע את דוגמה 2 של המדריך כלשונה |
| C-03 | חלון-הסגירה (`ClosingWindowDialog.jsx`, `CANCEL_ACTIONS_NOTE`) | `"ויתור" = פעולה מפורשת (סכום 0 + הערת-חובה), לא מחיקה שקטה. "סגור ללא תשלום" = מסלול חוב-אבוד…` | טרם ירד (שלב 5) | הגדרת שני מונחים בפסקה קבועה — חומר-③ | ③ | ⏳ שלב 5 |
| C-04 | הפרופיל — לשונית "העדפות והתראות" (`ProfileSettingsPage.jsx`) | `ההתראות עצמן יישלחו כשמנוע ההתראות יעלה (מודול 10)` ⇒ בסיס: `ההתראות יתחילו להישלח בפועל כשמנוע ההתראות יופעל` | "מודול 10" הוא ז'רגון-בנאים (B8) | הנימוק *מתי* הן יישלחו — לשכבה | ⑦ | ⏳ שלב 9: האם רמה 2 צריכה משפט על מנוע-ההתראות, או שהבסיס מספיק |

## שלב 4 · מ6 פרויקטים

| # | מסך · קובץ | המשפט שירד (לפני) | הבסיס אחריו | למה ירד | סוג | הכרעת שלב 9 |
|---|---|---|---|---|---|---|
| C-05 | כרטיס-פרויקט — "אורחים מוערכים" (`ProjectCardPage.jsx`) | `מההצעה — לא מספר סופי` | `מהצעת המחיר` | "לא סופי" חוזר על "מוערכים" (R27); *איפה המספר הסופי* — לא נענה על המסך | ⑦ | ⏳ הצעה לרמה 2: *"המספר הסופי מוזן בלשונית 'סגירת אירוע', אחרי האירוע"* |
| C-06 | לשונית צוות — היסטוריית-סבבים (`lib/projectTeam.js` `RAW_STATUS_NOTE`) | `כאן מוצג הסטטוס הגולמי כפי שנרשם — לא התווית הנגזרת שלמעלה` | נמחק; הכותרת ⇒ `סטטוס מקורי` | "גולמי/נגזר" = ז'רגון-בנאים (B8); הגדרת-מונח | ③ | ⏳ |
| C-07 | לשונית צוות — סרגל-הכלים (`lib/projectTeam.js` `SCOPE_NOTE`) | `שינוי הכמות נרשם כשינוי-תכולה עם סיבה; ההצעה נשארת קפואה. אין חסימת-זמן…` | נמחק | חוזר על מה שהדיאלוג אומר בזמן-הפעולה (R27) | ③/⑤ | ⏳ |
| C-08 | סגירת אירוע — שדה "שעות ביצוע בפועל" (`ClosingTab.jsx`) | `קובע את ברירת-המחדל בעמודת "שעות בפועל"…` | נמחק (השדה ממלא מראש לעיניה) | R28 — אינה פועלת אחרת | ⑤ | ⏳ |
| C-09 | סגירת אירוע — טבלת-האיכות (`ClosingTab.jsx`) | פסקת "לא לשלוח שוב" (פותח שדה-סיבה; תקף ללקוח הזה בלבד) | נמחק | המשפט הראשון מובן-מאליו חי (R25/R28); ההגדרה (③) לשכבה | ③ | ⏳ הסייג *"ללקוח הזה בלבד"* — לבדוק R26 בשלב 9 |
| C-10 | ביטול פרויקט — אזור הסיבה (`CancelProjectDialog.jsx`) | `התאריך, השעה ומי ביצעה את הביטול — נחתמים אוטומטית עם האישור, ואינם ניתנים לעריכה.` | נמחק | R28 | ④ | ⏳ |
| C-11 | לשונית לוגיסטיקה (`LogisticsTab.jsx`) | מניית-הסדר `טרם החל → הוזמן → מוכן` | `הסטטוסים כאן לקריאה בלבד — מעדכנת אותם מנהלת הלוגיסטיקה, במסך שלה.` | הגדרת-מונחים (③) | ③ | ⏳ |
| C-12 | שינוי-תכולה — סיכום-כסף (`ScopeChangeDialog.jsx`) | `ההנחה זהה לזו שבהצעה — X% הנחת-לקוח ועוד Y% הנחה ידנית.` | `מע"מ לפי השיעור שהוקפא באישור ההצעה.` | פירוק-הנוסחה ⇒ שכבה; הסייג (מע"מ קפוא) נשאר | ④ | ⏳ |
| C-13 | עריכת פרטי האירוע — שדה מיקום (`EditProjectDetailsDialog.jsx`) | `…והנקודה על המפה נקבעת מחדש.` | `שינוי מיקום אינו מבטל אישורים — הדיילות מקבלות עדכון.` | מנגנון-גיאוקוד (R10) | ④ | ⏳ |

</div>
