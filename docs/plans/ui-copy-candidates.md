<div dir="rtl">

# מועמדים לשכבת-ההסבר — משפטים שירדו מהבסיס וממתינים להכרעה בשלב 9

> **מה הקובץ הזה:** רשם-המועמדים של לילה-הטקסטים (`ui-copy-session-brief.md` ⑥ · `PROJECT_MASTER §4א`). כל משפט שיורד
> מהבסיס בשלבים 2–8 נרשם כאן **כמועמד** — לא נכתב ל-`src/lib/onboardingCopy.js` (כלל ⑥ של הבריף: שום מלל-הטמעה לפני שהמסכים נקיים).
> **ההכרעה נפלה בשלב 9 (09/09/2026 06:1X, S4 — עמודת "הכרעת שלב 9" בכל שורה): 24 מפתחות ב-`onboardingCopy.js` (+3 תוספות ⇒ 27 · הנוסחים: `stage9-layer-proposal.md` של W13) · 7 נעלמו · 4 נשארו בבסיס.** השערים שהופעלו: R27 (מכסים אותו — משהו לא-ברור נשאר?) · R28 (מה עושים אחרת אחריו?).
> **השאלה כאן:** *לאיזו רמה* (חזרה לבסיס · רמה 2 · נעלם). ⚠️ אותו משפט יכול להופיע גם ב-`ui-copy-structural-findings.md` — שם השאלה אחרת (*איזה שינוי-מסך היה מייתר אותו*), וזו אינה כפילות.
> **נוצר:** 08/09/2026 23:3X, סשן 2 של הלילה, פתיחת שלב 2. **מי כותב:** הסשן הממזג בלבד, מדיווחי הכותבים (המדריך §8).
> **סוגי-האמירה** (`research-onboarding-content.md` §1): ① ייעוד · ② צעד-ראשון · ③ מונח · ④ מקור-מספר · ⑤ נימוק-סידור · ⑥ תוצאת-לחיצה (לעולם לא לשכבה) · ⑦ המשך.

## שלב 2 · רכיבים משותפים + לשון-נקבה

| # | מסך · קובץ | המשפט שירד (לפני) | הבסיס אחריו | למה ירד | סוג | הכרעת שלב 9 |
|---|---|---|---|---|---|---|
| C-01 | כרטיס-לקוח — טופס אנשי-קשר (`CustomerFormDialog.jsx`, `NO_PRIMARY_MSG`) | `אי אפשר למחוק את איש הקשר הראשי. סמן קודם אחר כראשי.` | **נשאר בזכר בכוונה** — קשור-בייט ל-`raise exception` של ה-RPC `replace_customer_contacts` (מיגרציה `20260902141451`), ונוסח-ישי (`02_customers/CLAUDE.md`) | שינוי = מיגרציה בשער-ההקלדה של ישי — לא בלילה | — | ✅ **בסיס** — נשאר בזכר עד מיגרציה (נוסח-ישי, קשור-בייט ל-`raise`); פריט-בוקר |
| C-02 | חלון-הסגירה (`ClosingWindowDialog.jsx`, `FEE_STORAGE_NOTE`) | `נשמר במסד: הסכום הסופי וההערה בלבד — שלושת הרכיבים שלמעלה נגזרים-מחדש לתצוגה בכל פתיחה, ולא נשמרים כעמודות נפרדות.` | טרם ירד — עדיין חי (שלב 5) | המדריך §4 דוגמה 2 כבר הכריע: רמה 0 נמחק · רמה 2 `closing.frozenAmount` | ④ | ✅ **רמה 2** `closing.frozenAmount` — מדריך §4 דוגמה 2 מילה-במילה |
| C-03 | חלון-הסגירה (`ClosingWindowDialog.jsx`, `CANCEL_ACTIONS_NOTE`) | `"ויתור" = פעולה מפורשת (סכום 0 + הערת-חובה), לא מחיקה שקטה. "סגור ללא תשלום" = מסלול חוב-אבוד…` | טרם ירד (שלב 5) | הגדרת שני מונחים בפסקה קבועה — חומר-③ | ③ | ✅ **רמה 2** `closing.terms` — הגדרת ויתור · סגור ללא תשלום · חוב-אבוד (עם C-16) |
| C-04 | הפרופיל — לשונית "העדפות והתראות" (`ProfileSettingsPage.jsx`) | `ההתראות עצמן יישלחו כשמנוע ההתראות יעלה (מודול 10)` ⇒ בסיס: `ההתראות יתחילו להישלח בפועל כשמנוע ההתראות יופעל` | "מודול 10" הוא ז'רגון-בנאים (B8) | הנימוק *מתי* הן יישלחו — לשכבה | ⑦ | ✅ **נעלם** — R27: הבסיס כבר אומר שההתראות יתחילו כשהמנוע יופעל; "מתי" אינו משנה מה היא עושה |

## שלב 4 · מ6 פרויקטים

| # | מסך · קובץ | המשפט שירד (לפני) | הבסיס אחריו | למה ירד | סוג | הכרעת שלב 9 |
|---|---|---|---|---|---|---|
| C-05 | כרטיס-פרויקט — "אורחים מוערכים" (`ProjectCardPage.jsx`) | `מההצעה — לא מספר סופי` | `מהצעת המחיר` | "לא סופי" חוזר על "מוערכים" (R27); *איפה המספר הסופי* — לא נענה על המסך | ⑦ | ✅ **רמה 2** `project.guestsEstimate` — איפה ומתי מוזן המספר הסופי, ומה הוא כן ולא משנה |
| C-06 | לשונית צוות — היסטוריית-סבבים (`lib/projectTeam.js` `RAW_STATUS_NOTE`) | `כאן מוצג הסטטוס הגולמי כפי שנרשם — לא התווית הנגזרת שלמעלה` | נמחק; הכותרת ⇒ `סטטוס מקורי` | "גולמי/נגזר" = ז'רגון-בנאים (B8); הגדרת-מונח | ③ | ✅ **רמה 2** `team.rawStatus` — למה ההיסטוריה מראה סטטוס אחר מהטבלה (בלי "גולמי/נגזר") |
| C-07 | לשונית צוות — סרגל-הכלים (`lib/projectTeam.js` `SCOPE_NOTE`) | `שינוי הכמות נרשם כשינוי-תכולה עם סיבה; ההצעה נשארת קפואה. אין חסימת-זמן…` | נמחק | חוזר על מה שהדיאלוג אומר בזמן-הפעולה (R27) | ③/⑤ | ✅ **נעלם** — R27: הדיאלוג אומר זאת בזמן-הפעולה |
| C-08 | סגירת אירוע — שדה "שעות ביצוע בפועל" (`ClosingTab.jsx`) | `קובע את ברירת-המחדל בעמודת "שעות בפועל"…` | נמחק (השדה ממלא מראש לעיניה) | R28 — אינה פועלת אחרת | ⑤ | ✅ **נעלם** — R28: השדה ממלא מראש לעיניה; אינה פועלת אחרת |
| C-09 | סגירת אירוע — טבלת-האיכות (`ClosingTab.jsx`) | פסקת "לא לשלוח שוב" (פותח שדה-סיבה; תקף ללקוח הזה בלבד) | נמחק | המשפט הראשון מובן-מאליו חי (R25/R28); ההגדרה (③) לשכבה | ③ | ✅ **רמה 2** `closing.doNotSendAgain` — היקף הסימון (לקוח זה בלבד), חוסם ולא מנקד; R27 דק — מסומן למחיקה ראשונה אם המעריכים יראו חזרה |
| C-10 | ביטול פרויקט — אזור הסיבה (`CancelProjectDialog.jsx`) | `התאריך, השעה ומי ביצעה את הביטול — נחתמים אוטומטית עם האישור, ואינם ניתנים לעריכה.` | נמחק | R28 | ④ | ✅ **נעלם** — R28 |
| C-11 | לשונית לוגיסטיקה (`LogisticsTab.jsx`) | מניית-הסדר `טרם החל → הוזמן → מוכן` | `הסטטוסים כאן לקריאה בלבד — מעדכנת אותם מנהלת הלוגיסטיקה, במסך שלה.` | הגדרת-מונחים (③) | ③ | ✅ **רמה 2** `project.logisticsStatuses` — שלושת המצבים ומה נספר באריח |
| C-12 | שינוי-תכולה — סיכום-כסף (`ScopeChangeDialog.jsx`) | `ההנחה זהה לזו שבהצעה — X% הנחת-לקוח ועוד Y% הנחה ידנית.` | `מע"מ לפי השיעור שהוקפא באישור ההצעה.` | פירוק-הנוסחה ⇒ שכבה; הסייג (מע"מ קפוא) נשאר | ④ | ✅ **רמה 2** `scope.discountFormula` — חיבור ולא שרשור, ההקפאה באישור |
| C-13 | עריכת פרטי האירוע — שדה מיקום (`EditProjectDetailsDialog.jsx`) | `…והנקודה על המפה נקבעת מחדש.` | `שינוי מיקום אינו מבטל אישורים — הדיילות מקבלות עדכון.` | מנגנון-גיאוקוד (R10) | ④ | ✅ **נעלם** — R10: מנגנון-גיאוקוד; מה שמעניין אותה כבר בבסיס |

## שלב 7 · מ2 לקוחות + מ3 הצעות מחיר

| # | מסך · קובץ | המשפט שירד (לפני) | הבסיס אחריו | למה ירד | סוג | הכרעת שלב 9 |
|---|---|---|---|---|---|---|
| C-28 | כרטיס-לקוח — כותרות-המשנה של רשימות הפרויקטים (`CustomerDetailsPage.jsx` `ProjectsSection`) | `תאריך האירוע טרם עבר · הקרוב ראשון` · `תאריך האירוע עבר, או שהפרויקט בוטל · האחרון ראשון · המבוטלים בסוף` | *(נשארו — לא ירדו)* | נימוק-סידור ⑤ שיושב קבוע על המסך; המדריך §2ב: ⑤ הוא הלב של השכבה, לא של הבסיס | ⑤ | ✅ **בסיס** — תת-כותרות-הרשימה הן התשובה של הרשימה (סדר = מבנה); אין מפתח |
| C-29 | רשימת-לקוחות — תא שביעות-הרצון (`CustomersPage.jsx` `SatisfactionCell`) | — | כוכבים + `טעון בירור` בלי מונה-משובים | ④ — הסייג "מ-N משובים" קיים בכרטיס-הלקוח (`StatTile`) אבל לא בתא הרשימה הדחוס | ④ | ✅ **רמה 2** `customers.satisfactionAverage` — ברמת-הרשימה, מעל הטבלה |
| C-30 | בונה-ההצעה — חלון "עדכן ושלח" בלי שינוי (`lib/quotes.js` `NO_CHANGES_SEND_CONFIRM`) | `לא בוצע שינוי בהצעה. היא תישלח שוב כפי שהיא, והתוקף לא יתאפס. להמשיך לשליחה?` | *(נשאר — הכרעת-ישי 01/08/2026, ⑥ "התוקף לא יתאפס" חייב להישאר גלוי)* | 3 משפטים (R13) שמערבבים ⑥ עם הקשר | ⑥/① | ✅ **בסיס** — הכרעת-ישי 01/08/2026 עומדת; לא פוצל |

## שלב 6 · מ4 דיילות + Smart Match

| # | מסך · קובץ | המשפט שירד (לפני) | הבסיס אחריו | למה ירד | סוג | הכרעת שלב 9 |
|---|---|---|---|---|---|---|
| C-19 | מבט-על — כיתוב-המיון (`OverviewTab.jsx`) | *(לא ירד — הכיתוב נשאר, מאוחד עם מ6)* | `ממוין: חסרים תחילה, ובתוכם לפי קרבת האירוע` | "חסר" אינו מוגדר על המסך; התוצאה המפתיעה (רחוק-וחסר מעל קרוב-ושלם) אינה מוסברת | ③+⑤ | ✅ **רמה 2** `projects.sort` — מפתח אחד לשני המסכים (מ6 + מ4 מבט-על), מדריך §4 דוגמה 1 מילה-במילה |
| C-20 | מאגר — עמודת "אירועים · רבעון אחרון" (`RepositoryTab.jsx` · `lib/hostesses.js` `QUARTER_WINDOW_DAYS`) | — | המספר בעמודה | חלון 90 יום מתגלגל, רק `אושרה סופית` על אירועים שהתקיימו, בלי מבוטלים, פרויקט נספר פעם אחת — שתי קוראות יחשבו אחרת | ④ | ✅ **רמה 2** `hostesses.quarterEvents` — בלי המספר ("רבעון מתגלגל"); העברת `QUARTER_WINDOW_DAYS` לפרמטר נשארת הכרעת-ישי |
| C-21 | מאגר — גלולת `לא ענתה ל-N האחרונים` (`RepositoryTab.jsx`) | — | הגלולה עם N מהפרמטר | "לא ענתה" = עדיין `pending` (כולל פג-תוקף), נספר על השורה המכריעה בלבד | ③/④ | ✅ **רמה 2** `hostesses.noAnswerPill` |
| C-22 | מאגר — חלון-ההשבתה, אפשרות "שחררי מהאירועים" (`RepositoryTab.jsx`) | `…וזה נספר כשחרור-שלנו — לא לרעתה.` | `כל שיבוץ יסומן כ"שוחררה", הדיילת תקבל מייל-ביטול על כל אירוע, והאירוע חוזר לחסר-איוש.` | ⑥ (המייל, התוצאה) נשאר; הנימוק *למה זו הבחירה הבטוחה לדיילת* ⇒ שכבה | ⑤ | ✅ **רמה 2** `hostesses.releaseWhy` — בדיאלוג-ההשבתה, אחרי הכותרת |
| C-23 | טופס הוספת דיילת — תיאור-הדיאלוג (`HostessFormDialog.jsx`) | `הכרטיס נפתח רק אחרי שדיברת עם הדיילת בטלפון — היא לא רואה את הטופס הזה ולא ממלאת אותו בעצמה.` | `בשמירה, הדיילת נכנסת מיידית למאגר בסטטוס "פעילה"` | ② צעד-ראשון/תהליך; ⑥ (נכנסת מיידית) נשאר בבסיס | ② | ✅ **רמה 2** `hostesses.addFormWhen` — רק מה שמדיד (אין הרשמה עצמית); "אחרי שיחת טלפון" לא נכתב — אין לו מקור |
| C-24 | כרטיס-דיילת — "העדפות של לקוחות" (`HostessViewCard.jsx`) | `שכבה 2 של Smart Match` + `⚠️ עדיין לא זמין — הטבלה נכתבת ע״י מודול 6…` | כותרת `העדפות של לקוחות` · ריק: `טרם נרשמו העדפות` | ז'רגון-בנאים (B8); ההגדרה ⇒ שכבה | ③ | ✅ **רמה 2** `hostess.clientPreferences` — הנוסח המוכרע; תלוי ב-S-13 (אם הסעיף יוסתר — למפתח אין בית) |
| C-25 | Smart Match — הפסקה מתחת ל"שלחי מייל תיאום" (`SmartMatchPage.jsx`) | `אין מספר שהמערכת מציעה. שולחים בסבבים… ושליחה נדיבה מדי שוחקת את שיעור-ההיענות.` | `כל זימון עודף שייסגר ישלח לדיילת הודעה ש'המשרה כבר אוישה'.` | המדריך §4 דוגמה 3 כלשונה — ⑥ בבסיס, הנימוק לשכבה | ⑤ | ✅ **רמה 2** `smartMatch.roundsWhy` — מדריך §4 דוגמה 3 מילה-במילה |
| C-26 | Smart Match — מצב-ריק של המועמדות (`SmartMatchPage.jsx`) | `כולן נפסלו בשער: לא פעילות · משובצות באותו תאריך · הצהירו אי-זמינות · רחוקות מ-N ק"מ · או מסומנות "לא-לשלוח" אצל הלקוח הזה.` | `בדקי במאגר הדיילות אם יש דיילת שהסינון האוטומטי לא כלל — אף אחת לא עברה אותו לאירוע הזה כרגע.` | כשל-5 של המחקר (חמישה חוקים בנשימה אחת); המרחק הוא פרמטר | ⑤ | ✅ **רמה 2** `smartMatch.emptyGateRules` — חמשת החוקים + תנאי-הרכב, המרחק "שנקבע בהגדרות" |
| C-27 | Smart Match — צ'יפ "דיילת חדשה" (`SmartMatchPage.jsx`) | `title="פחות מ-3 מענים — מדורגת לפי ממוצע החברה"` | הצ'יפ בלבד, בלי `title` | H4 (הסבר בריחוף) + `3` הוא `params.minAnswersForScore` | ③/④ | ✅ **רמה 2** `smartMatch.newHostessRanking` — פעם אחת למסך (לא פר-כרטיס), הסף "מוגדר בהגדרות" |

## שלב 5 · מ8 כספים

| # | מסך · קובץ | המשפט שירד (לפני) | הבסיס אחריו | למה ירד | סוג | הכרעת שלב 9 |
|---|---|---|---|---|---|---|
| C-14 | דוח-שכר — פאנל-הבדיקה (`SalaryReportDialog.jsx`) | `שעות בפועל × התעריף הקפוא, בתוספת בונוס אישי ונסיעות` | `…וטרם שולמו.` | הנוסחה (④) — הסייג נשאר, הנוסחה לשכבה | ④ | ✅ **רמה 2** `salary.formula` — הנוסחה + התעריף המוקפא |
| C-15 | דוח-שכר — פסקת "אין תצוגה מקדימה" | `ואי-אפשר לחשב אותן בלי לכתוב אותן` | `…רק ברגע ההפקה.` | נימוק-מנגנון (⑤); משפט-⑥ על הבלתי-הפיכות נשאר במלואו | ⑤ | ✅ **נעלם** — נימוק-מנגנון; ה-⑥ נשאר בבסיס |
| C-16 | חלון-הסגירה — פוטר דמי-הביטול (`ClosingWindowDialog.jsx` `CANCEL_ACTIONS_NOTE`) | הגדרות `ויתור`/`סגור ללא תשלום` | נמחק — הכפתורים נושאים את המונחים הנעולים, והתוצאות חוזרות במלואן בדיאלוגי-האישור | ③ (= C-03, בוצע) | ✅ **רמה 2** `closing.terms` (= C-03) |
| C-17 | חלון-הסגירה — סיווג "אחר" | `הביטול אינו בהכרח באשמת הלקוח, ולכן` | 2 משפטים במקום 3 | ניואנס עסקי, לא נדרש למילוי השדה | ⑤ | ✅ **נעלם** — R28: ניואנס שאינו משנה את מילוי השדה |
| C-18 | חלון-הסגירה — בלוק רווח-פרויקט-מבוטל | `הרווח כאן נגזר מדמי-הביטול בניכוי פיצוי-הצוות ועלות-הסחורה, ולא מהכנסות-האירוע` | העובדה הקצרה בלבד | נוסחת-הגזירה (⑤) | ⑤ | ✅ **רמה 2** `closing.cancelledProfit` — הנוסחה Q-3, ולמה שלילי הוא תקין |

## שלב 8 · מ5 לוגיסטיקה + מ9 הגדרות + מ1 כניסה/משתמשים/קטלוג

| # | מסך · קובץ | המשפט שירד (לפני) | הבסיס אחריו | למה ירד | סוג | הכרעת שלב 9 |
|---|---|---|---|---|---|---|
| C-31 | לוגיסטיקה — מקרא-הענבר (`LogisticsPage.jsx` `AmberLegend`) | `⏱ שורה בענבר — פריט פיזי טרם הוזמן, והאירוע בתוך {N} ימי עסקים — או: משלוח שתאריכו המובטח עבר וטרם הגיע. סימון מיידע בלבד: אפשר להתעלם ממנו, ואף פקד אינו ננעל. הקמת אתר רישום (01WEB) אינה נספרת — הסף נגזר מזמן ייצור של דפוס.` (254 תווים) | `⏱ שורה בענבר = דורש תשומת-לב. שום דבר לא ננעל.` — הדוגמה המוכרעת של התוכנית §4 שלב 8 | שני הטריגרים, סף-`{N}` וחריג-01WEB הם נימוק-סידור (⑤) והגדרת-מונח (③); H2 עומד — כל טריגר עדיין מוסבר פר-שורה (`amberGlyphTitle` · `lateArrivalReason`) | ③+⑤ | ✅ **רמה 2** `logistics.amberWhy` — שני הטריגרים + חריג אתר-הרישום; הסף "שנקבע בהגדרות" (אין ספרה) |
| C-32 | קטלוג-מחירים — דיאלוג המדרגות (`PriceTiersDialog.jsx`) | `המדרגה שכמות-המינימום שלה היא הגבוהה ביותר מבין אלה שאינן עולות על הכמות בהצעה — היא שקובעת את המחיר.` | `ללא מדרגות, ההצעה מתומחרת לפי מחיר הבסיס` | כלל-החישוב הוא הגדרת-המונח "מדרגה"; רק מיעוט צריך אותו כדי לתפעל את הדיאלוג (R25) | ③ | ✅ **רמה 2** `prices.tierRule` |
| C-33 | פרמטרים — כותרת-הקבוצה "תמחור ותזמון" (`ParamsTab.jsx` `GROUP_LEADS.pricing_timing`) | `הערכים שמנוע התמחור וההצעות קורא.` | `שינוי כאן משפיע על הצעות חדשות בלבד — הצעה שכבר אושרה שומרת את הערכים שהוקפאו בה.` | "מנוע התמחור" = ז'רגון-בנאים (R10/B8); המשפט מגדיר *מה הקבוצה* ולא מה עושים בה (H1) | ③ | ✅ **רמה 2** `params.pricingTimingGroup` — בלי "מנוע" (טיוטת-הכותב נדחתה: R10); מוצג רק בקבוצה `pricing_timing` |
| C-34 | פרמטרים — `קבוע_ריסון_m` (`paramsRegistry.js`, הינט) | *(נשאר בבסיס)* הפסוקית `…מונע קפיצה מדירוג אחד או שניים` | ללא שינוי | פסוקית-נימוק (⑤) בתוך הינט של יחידת-משפט אחת (R26 · R13) — נרשם לפי "ספק ⇒ רשום" | ⑤ | ✅ **בסיס** — יחידת-משפט אחת (R26/R13); אין מפתח |

**נבדק ולא ירד — לידיעת שלב 9:** ‏W12d מדד **0** הינטים שמצטטים ערך של פרמטר אחר כקבוע (המוקש של דוגמה 3); דליפת-שם-הפרמטר הגולמי ב-`תבנית_תזכורת_משמרת` (*"טריגר: שעות_תזכורת_לדיילת"*) תוקנה בבסיס, לא הועברה.

</div>
