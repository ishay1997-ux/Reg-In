<div dir="rtl">

# תוכנית-העבודה המתגלגלת — חלון דו-שבועי

> **בעלים: סשן-המנהל** (הוקמה 01/08/2026 בהכרעת ישי; הכללים כאן הם חלק מהסקיל
> `work-manager`). ישי קורא — המנהל כותב, תחת כללי-הזירה (קריאה-מחדש לפני כתיבה,
> קומיט ב-pathspec).
>
> **שלושת חוקי-הקובץ:**
> 1. **אינדקס, לא עותק** — שורה מצביעה על צעד במדריך-המיקרו; ה"איך" חי שם בלבד.
>    פירוט שנכתב לכאן במקום לשם = ממצא לתיקון.
> 2. **מבחן-קיבולת** — מקדמים שורה רק אם היא תתחיל בפועל בתוך החלון בקצב הנוכחי.
>    חלון של 5 שורות אמיתיות הוא חלון מלא; 🟡 היא התור הגלוי; חלון מנופח = שקר-תחזית.
> 3. **מבחן-הסידור** — הנזק מחמיר עם נתוני-אמת / תאריך קשיח קודם; לא "חשיבות" מופשטת.
>
> **מסלולים:** ▶️ בבנייה/מוכן-לבנייה · 💬 דורש הכרעת-ישי · 👤 משימה של ישי עצמו ·
> 🟡 בתור, לא קודמה (מבחן-הקיבולת) · ✅ נסגר — יוצא מהקובץ בסגירת-הסשן.
>
> **מתי החלון מתרענן (הלולאה הדינמית):** בכל אחד מארבעת הטריגרים — פריט נסגר · פריט
> חדש נכנס (רעיון-ישי, ממצא-ביקורת, חוב-§6 שהבשיל) · מדידה שסותרת את הסדר (תלות
> שהתגלתה, באג שמחמיר עם נתוני-אמת) · סגירת-מודול (מד-הקצב) — המנהל עושה שלושה
> דברים: מוציא סגורים ומקדם לפי מבחן-הקיבולת · **מריץ מחדש את מבחן-הסידור על כל
> החלון, לא רק על השורה החדשה** — הסדר הוא השערה שנבדקת, לא החלטה שנחקקה ·
> **ובכל גבול-מודול (סגירה/פתיחה): סריקת רשם-החובות** — `grep '🚧 מN'` על
> `PROJECT_MASTER §6` למודול שנפתח, וסריקה אילו חובות של מודולים אחרים הבשילו
> להיכנס לחלון. הזיכרון הוא של הרשם; האחריות להריץ את הסריקה ברגע הנכון — של המנהל ·
> וכל שינוי-סדר **מוצע לישי עם נימוק-שורה, לעולם לא בשקט**. התפנה ישי (💬 ריק) או
> בנאי (▶️ ריק) — המנהל יוזם קידום, לא מחכה שיבקשו.
>
> **פקודת-הבחירה של ישי — "בחר בין 2 ל-5 משימות לבחירתך":** המנהל בוחר לפי שני
> המבחנים (סידור+קיבולת), ומחזיר: מה נבחר + נימוק-שורה לכל אחת · מה מהן רץ במקביל
> למה · והפרומפטים מוכנים-להדבקה עם המלצת-מודל. פחות משתיים זמינות-באמת? אומרים את
> זה ("יש רק אחת שיכולה להתחיל — הנה למה"), לא ממציאים שורות כדי למלא מכסה.

## החלון — עודכן 01/08/2026 15:34 (מנהל-2)

**נסגר היום (יצא מהחלון):** צעדים 4.3b · 4.4 · **4.5 שער-פזה-4 חתום ע"י ישי ~11:00** · 5.1 ·
5.2+5.3 · 5.4 (אודיט, פסק-דין [NO]) · שלושת פריטי סבב-התיקון · **פער החיבור שמירה⇄שליחה**
(`384af62`) · **תווית "שמור ושלח" + חיווי טרם-נשלחה** (`9f28336`) · **שומר-הפיצול נצפה נכשל**
(`8568e35`). הראיות המלאות: `CLAUDE_CODE_LOG.md` רשומות 01/08.

| # | משימה (מצביע) | מסלול | במקביל? | מודל+מאמץ (§⑨) | פרומפט | הערכה |
|---|---|---|---|---|---|---|
| 13 | **השוואת-שינוי לפני "עדכן ושלח"** — אין שינוי ⇒ לא שומרים + חלון-אישור; **התוקף לא מתאפס** (הכרעת-ישי 15:32, מגובה בבדיקת-שוק ובכלל F4 שבקוד). מצביע: `quotes.js` `deriveQuoteExpiry` | ▶️ בבנייה | טורי | Sonnet · high | ✅ אושר אחרי שש-שכבות | ~45 דק' |
| 14 | **סבב-פיקסצ'רים מורחב** — **הרשימה השמית של שמונת הכשלים + שני הדגלים נחתו לדיסק 01/08 16:02**; המצביע: `micro_guides/module-3.md` §9, רשומת 16:02. **תיקון למה שנרשם כאן קודם: זה 7 מאישור-#6 + אחד שאינו קשור** (`quote-email:54` — תלות-סדר תוך-קובץ, דורש אבחון נפרד; היה מסווג לפי מיקום ולא לפי סיבה). ועוד: `server-messages:145` נושאת כשל שני מוסתר (תווית `עדכון ההצעה`→`עדכן ושלח`, `9f28336`) שיתגלה **אחרי** תיקון-הפיקסצ'ר — לתקן באותו סבב · `auth:23` מדידה כפולה · חילוץ משפט-הבאנר ל-`lib/email.js` · מסלול שליחה-חוזרת על #22 · שורת-תקרת-`getSentQuoteIds` | ▶️ הבא בתור | טורי אחרי 13 | Sonnet · high | ייכתב בנחיתת 13 | ~שעה-שעה וחצי **+ אבחון #4** |
| 15 | **שער ירוק מלא** — `npm run gate` + E2E נקי; שלושה מספרים (רשומים·רצו·דולגו) | ▶️ | אחרי 14 | — | — | ~15 דק' |
| 16 | **אודיט-סגירה חוזר (`module-close`)** — **סשן טרי** (דרישת המדריך: מי שבנה נוטה לאשר את עצמו). האודיט של 06:59 מכסה מצב שקדם לכל עבודת אחה"צ ⇒ ראיותיו התיישנו. אחריו: **המנהל מאמת בעצמו את הטענות היקרות** (ספירות · שומרים-שנצפו-נכשלים · שני כיווני-הרשאות) | 👤 ישי פותח סשן | אחרי 15 | Sonnet · high | 🧩 ייכתב בסגירת-המשמרת | ~שעה |
| 17 | **PR ומיזוג** | 👤 **ישי בלבד** (כלל ברזל 10) | אחרון | — | 🧩 | — |
| 18 | **משימת-מנהל: הנחתת שש המנגנונים המאומצים ל-`prompts.md`** — אומצו 01/08 ~11:30 (‏ADOPT, לא הצעות) עם חלון-ביצוע "בסגירת סבב-התיקון" **שחלף בלי שבוצע**; נמדד 16:1X בקריאה מלאה של `prompts.md`+`concurrency.md`: **אפס מהשישה קיימים**. הפירוט: יומן-הפספוסים רשומה 7. **מבוצע בסגירת-המשמרת** (מעבר-עריכה יחיד, עם ארבע שאלות-הכניסה) | 🟡 משימת-מנהל | לא נוגע בקוד | — | — | ~20 דק' |
| 19 | **משימת-מנהל: להוכיח את ענף "חותמת ישנה" ב-`check:context`** — מנהל-3 מסר בגלוי שרק ענף "חותמת חסרה" נבדק בפועל; ענף-הגיל מעולם לא ירה. מבחן בן דקה (לזייף חותמת ישנה ב-`docs/current_manager.md` ולראות שההוק מתריע). **הועבר לכאן ממסמך-המסירה שנמחק — זה הבית הקבוע שלו** | 🟡 משימת-מנהל | לא נוגע בקוד | — | — | ~5 דק' |
| 20 | **משימת-מנהל: אימות ממצה של `docs/delete/work_manager_situations_draft.md` מול הסקיל** — מנהל-3 בדק **נקודתית ולא ממצה** שכל הכרעות-הטיוטה נחתו בסקיל, והעביר את הקובץ ל-`docs/delete/`. **המחיקה של ישי** ⇒ עד אז הטיוטה חיה ובטוח לקרוא ממנה. לרוץ **לפני** שממליצים לישי למחוק. **הועבר לכאן ממסמך-המסירה שנמחק** | 🟡 משימת-מנהל | לא נוגע בקוד | — | — | ~20 דק' |
| 12 | ~~**אזהרת-דומיין (בדיקת-MX) לפני שליחה**~~ ❌ **בוטלה 01/08 15:33 בהכרעת-ישי** ("אם זה יותר משעה — אפשר לוותר"; הערכה >שעה + נוגעת במנוע המשותף). **עברה לרשם-החובות למודול 8.** הראיה שהולידה אותה נשמרת: `email_log` #9 = `status:sent` לדומיין שאינו קיים | ❌ | — | — | — | — |
| 9 | ~~**מיני-סבב §7 למודול 4**~~ ✅ **הוכרע 01/08 11:50** — שלוש הכרעות (דו-תפקידי "לא קורה" · חצי-משמרת "לא קורה" · תצוגת-עלות הואצלה). *(השורה נותרה פתוחה בטעות עד 15:34 — פספוס-רענון של המנהל.)* | ✅ | — | — | — | — |

**מה לא קודם לחלון, במודע (מבחן-הקיבולת):** הכנת סבב-§7 למודול 4 — **תוכן אמיתי קיים**
(`docs/module4_smart_match_research.md`, שישה סבבי-הכרעות) אך הכנה ראויה דורשת קריאה
לעומק בראש רענן ⇒ **הועבר למנהל הבא** · תסריט-הדמו של מודול 12 · סבב-כריית-תמלילים ·
תוקף-כשדה-שמור (~2-3 שעות + מיגרציה על נתונים חיים — **נדחה במדידה**: התיקון בשורה 13 מסיר
את התרחיש בפועל, ואפס מופעים מדודים לתרחיש שנותר).

## 🔄 בלוק-מסירה — מנהל-2 → מנהל-3 (01/08/2026 15:43, מסירה מתוכננת: מכסת-ישי 9% + הקשר-מנהל 60%)

**⚠️ קרא קודם את החלון למעלה (שורות 13–17) — הוא מעודכן ומדויק. כאן רק מה שאינו נגזר מהקבצים.**

- **מצב-הבנייה:** שורה 13 **נחתה ואומתה** (`33703d2`, 410/410 יחידה שוחזרו אצל המנהל, נוסח-החלון
  אושר ע"י ישי 15:41 "המשפט בסדר"). **שורות 14–15 לא התחילו במכוון** — עצרתי את הבנאי מסיבת-מכסה,
  לא טכנית. הכל מפורט בשורה 14; סשן ירים אותה משם בלי צורך בי.
- **בנאי-התיקונים** `local_6005de04-...` ("הודעת שגיאה בהנחות חורגות"): **פנוי, עץ נקי, הכל דחוף.**
  הונחה לא לפתוח עבודה חדשה ולנתב את ישי למנהל. **הוא בנאי חזק** — תיקן אותי פעמיים במדידה
  (טענת-E2E שגויה שלי · מלכודת רינדור-הייצור) ודיווח נגד עצמו שלוש פעמים. **תן לו מקום לחלוק עליך.**
- **⛔ 8 כשלי-E2E קדם-קיימים אינם רגרסיה** — כולם מאישור-בטעות של הצעה #6 (ישי בלבל בין
  "שליחה במייל" ל"אישור הצעה"). שוחזרו פעמיים בבסיס-מוסתר. **שלושה מהם אובדן-כיסוי** ולא מספר
  שזז ⇒ פתרון ביירוט, **לא** בהצבעה על #22 (‏`getLastSuccessfulSend` תחזיר את כתובתו הפרטית של ישי).
- **דלתת-דמו של היום:** `quotes#22` (מדיטק, `in_progress`, **עם שורת-`email_log` אמיתית**) ·
  `projects#8` (מאישור-#6) · שתי שורות ב-`email_log` (#9 לכתובת-דמו, #10 לישי). #22 **נשארת** בהכרעת-ישי.
- **מכסה — הנתון שקובע הכל:** ישי על **9% שבועי, איפוס יום שלישי 22:00**. הדדליין 19/09 רחוק.
  **אל תריץ עבודה כבדה עד האיפוס אלא אם ישי אומר במפורש.** האודיט (שורה 16) הוא היקר ביותר.
- **ישי חתם היום:** שער-פזה-4 · תיבת-DoD של ה-PDF · "שמור ושלח" · חיווי טרם-נשלחה · שלוש
  הכרעות מיני-סבב · נוסח חלון-האישור. **נשאר שלו: אודיט (לפתוח סשן) · PR.**
- **710:** מנהל-2 שלהם (`local_96b43d17-...`) — שלושה סבבי-החלפה היום, שלוש התכנסויות עצמאיות.
  שאלת-הפילטר שלהם **נסגרה** (ישי אישר שהשצ"ל הגיע). אין חוב פתוח מולם.
- **מוניטור:** חמוש בסשן שלי ו**ימות איתו** — חמש חדש (`references/watching.md`) עם base מלא.
- **הפספוס הדומיננטי של המשמרת — קרא לפני שאתה מתחיל:** יומן-הפספוסים רשומה 5. שלושה מנגנונים
  שכבר קיימים בסקיל לא הורצו (שתי שאלות-הסיום · רענון-התוכנית · אימות-לפני-טענה), וכולם נתפסו
  ע"י מישהו אחר. **לא חסר כלל — חסרה הרצה.** יחס תפיסה-עצמית בזמן-אמת: **0**.

## יומן-החלון (שורה לכל שינוי-חלון, חדש למעלה)

- 01/08/2026 22:05 — **מנהל-4 קיבל משמרת. החלון לא שונה — שתי שורות נוספו, אפס סדר הוזז.**
  שורות 19–20 הן שני החובות שמנהל-3 מסר בגלוי במסמך-המסירה; הועברו לכאן **לפני** מחיקת המסמך,
  כי קובץ זמני אינו בית לחוב. **מבחן-הסידור לא הורץ מחדש** — הוא רץ בסגירת-משמרת-3 והמדידה שלי
  לא סתרה אותו; ריצה מחדש נדחית לרגע שבו משהו באמת יזוז (איפוס-מכסה / הודעת-בנאי).
  🔢 **תיקון-מדידה ליחס-הצמיחה (יומן-פספוסים 8), נמדד עכשיו:** הרשומה מדדה ראיות 178→**196**
  (+10%) — אבל הקומיט `ab3edc6` הוסיף 45 שורות ליומן **אחרי** אותה מדידה. הספירה בפועל:
  כללים 607→**1,079** (+78%) · ראיות 178→**241** (+35%) ⇒ **~2.2:1 לרעת הכללים, לא ~8:1.**
  הכיוון נשאר כפי שנרשם, סדר-הגודל לא. **מבוצע בכתב לסקיל בסגירת-משמרת-4** (שער עריכה-אחת);
  נרשם כאן כדי שהמספר לא ימות אם המשמרת תיקטע.

- 01/08/2026 16:02 — **מנהל-3 קיבל משמרת; שורה 14 תוקנה על מדידה.** רשימת שמונת הכשלים חיה **רק**
  בסשן-הבנאי (הדיסק רשם מספר בלי שמות) ⇒ הונחתה ל-`micro_guides/module-3.md` §9. **שני ממצאים
  חדשים:** (א) אחד משמונת הכשלים **אינו** מאישור-#6 — סיווג-לפי-מיקום שנתפס בערעור-המנהל ואושר
  ע"י הבנאי מול ריצותיו; (ב) כשל שני מוסתר ב-`server-messages:145` שיתגלה רק אחרי תיקון-הפיקסצ'ר.
  **נמדד גם:** ‏`quotes#22` = `in_progress` עם 2 שורות-`email_log` ⇒ פיקסצ'ר-אמת קיים, אך בטוח
  **רק** ל-`getSentQuoteIds`; ‏`getLastSuccessfulSend` היה חושף כתובת פרטית. הסדר לא שונה.

- 01/08/2026 06:15 — **מבט-המסמך של המנהל על PDF-5.1 (שני עמודים, בעין):** המספרים
  והפריסה מדויקים (6,319 ₪ מילולית, מפל-ההנחות נכון) — אבל **שני פגמי-BiDi
  גלויים-ללקוח**: (א) שדה-"הנדון" עם תוכן מעורב עברית-אנגלית מתערבל (סוגריים
  שבורים — צריך בידוד-BiDi על השדה); (ב) בעמוד-התנאים הנקודה יושבת בתחילת-שורה
  במקום בסופה. משפחת ה-RTL-pairing, תקרית רביעית. **ממתין להכרעת-ישי בבוקר** (זה
  מסמך-לקוח = מוצר-נראה): המלצה — תיקון-קטן בסבב-5.2/5.3 (בידוד-BiDi + פיסוק-תנאים).
- 01/08/2026 05:10 — שורה 2 (הדבקת-710) נסגרה: ישי הדביק; איחוד-הארכיון בצד-710 בידי
  המנהל שם. אפס משימות-ישי פתוחות; החלון כולו טורי מאחורי 4.3b (רץ).

- 01/08/2026 04:50 — **שורה 9 (דחיסת-היומן) נסגרה ויצאה**: נרטיב 889→471 (יעד-הביניים;
  גוש-31/07 יידחס כשיתיישן — נרשם בהערת-החוב שביומן עצמו). שני קומיטים מאומתים-מנהל
  (`64d7971` יומן · `b13164a` כלל-insert-first ל-src/CLAUDE.md). כיול ראשון נרשם:
  הערכה 40-60 דק' — עמד.

- 01/08/2026 04:05 — הקובץ הוקם; חלון ראשון נבנה ממצב-מודול-3 כפי שנמדד (4.1–4.3
  סגורים, 4.3b הבא). המצב נקרא מ-`micro_guides/module-3.md` §Status באותו תור.

</div>
