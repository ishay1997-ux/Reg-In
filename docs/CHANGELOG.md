<div dir="rtl">

# REG-IN — יומן שינויים (CHANGELOG)

> **למה הקובץ הזה קיים:** כשמסיימים מודול או משנים את מסד הנתונים, כותבים פה שורה. ככה כל אחד מהשותפים יודע מה השני סיים, בלי לזכור בעל-פה ובלי לשאול. **חובה לעדכן כאן בכל סיום מודול ובכל שינוי בטבלאות ה-DB.**
>
> 📍 **יומן משותף ל-ישי ועמית — סטטוס עבודה משותף.**  
> (יומן סטטוס פנימי של Claude: [CLAUDE_CODE_LOG.md](CLAUDE_CODE_LOG.md) — החלטות טכניות, סנאפשוטים, tech-debt · לוח מצב מודולים חי: [../STATUS.md](../STATUS.md))

---

> 🔑 **פרטי התחברות למשתמשי הבדיקה (5 התפקידים):** אצל ישי, בערוץ פרטי בלבד — **לעולם לא בקובץ הזה ולא בשום קובץ ב-repo.**

## איך ממלאים (כללים)

- **סיימת מודול / חלק ממנו?** הוסף שורה תחת "עדכוני קוד".
- **שינית טבלה ב-Supabase?** (הוספת/מחקת עמודה, שינית constraint) → הוסף שורה תחת "שינויי מסד נתונים (DB)" + **שלח הודעה מיידית לשותף**, כי זה משפיע עליו מיד.
- כל שורה: **תאריך · מי · מה · על מי זה משפיע**. פורמט התאריך: `DD/MM/YYYY HH:MM` (מ-07/07/2026; רשומות עבר בפורמט ישן לא משוכתבות).
- **הכי חדש למעלה** — הוסף שורה חדשה מיד מתחת לשורת-הכותרת של הטבלה (יש שם מרקר `_(שורה חדשה כאן)_`).

---

## 🗄️ שינויי מסד נתונים (DB) — הכי קריטי לתאם!

| תאריך | מי | מה השתנה | משפיע על |
|-------|-----|----------|----------|
| _(שורה חדשה כאן — הכי חדש למעלה)_ | | | |
| 03/07 | ישי (Claude) | **נעילת חשבון אחרי 5 ניסיונות כושלים (מודול 1 — החליף את דרישת ה-CAPTCHA).** migrations `module1_login_attempts_lockout` + `module1_reset_login_attempts_revoke_anon`: טבלה `login_attempts` (RLS-on בלי policies) + 3 פונקציות `SECURITY DEFINER` (`check_login_lock`/`register_failed_login`/`reset_login_attempts`, `search_path=''`), נעילה ל-15 דק'; `reset` ל-authenticated בלבד (revoke ל-anon). נבדק חי: 5 כשלונות נועלים, ה-4 הראשונים לא | זרימת ההתחברות (`LoginPage.jsx`) — שאר המודולים/הטבלאות לא מושפעים |
| 02/07 | ישי (Claude) | **הקשחת `current_user_role_id()`** (migration `harden_current_user_role_id`): קיבוע `search_path=''` + שמות סכמה מלאים (`public.users`, `auth.email()`); הסרת EXECUTE מ-`anon`/PUBLIC — נשאר ל-`authenticated` (נדרש כי ה-policies קוראות לפונקציה בהקשר authenticated). ניקה 2 מ-3 אזהרות advisor על הפונקציה | כל ה-RLS (הפונקציה load-bearing) — אומת: ה-CEO עדיין נטען אחרי השינוי |
| 02/07 | ישי | `modules` תוקן ל-**9 שורות** (הוסרה "מסך הבית" — הוכרע כנגיש-לכולם, לא מודול-RBAC). `permissions` **מלא: 45 שורות** (5×9) לפי המטריצה ב-PROJECT_MASTER §3 | כל המערכת — כל מי שיעבוד מול `modules`/`permissions` |
| 02/07 | ישי | `users.phone` נוספה (עמודה אמיתית, nullable). `role_name`='מנכ"ל' ו-`module_name`='דו"חות' תוקנו ממרכאות עבריות (״) למרכאות רגילות (") | כל שאילתה/policy עתידית שמשווה טקסט מול השמות האלה |
| 02/07 | ישי | **RLS מופעל בפועל** על `roles`,`modules`,`permissions`,`users` + פונקציית עזר `current_user_role_id()` (עם בדיקת `status='active'`). כל 15 הטבלאות היו RLS-on-בלי-policies (חסימה גורפת) — תוקן רק ל-4 אלה; **שאר 11 הטבלאות עדיין חסומות לגמרי**, מישהו שיתחיל לבנות מודול אחר יגלה שהכל ריק עד שיוסיף policy | מודול 2 ואילך — **קרא לפני שאתה מתחיל טבלה חדשה!** |
| 02/07 | ישי | **מיגרציית `users.status`: `frozen` → `inactive`.** רץ בסדר `drop constraint → update … set status='inactive' where status='frozen' → add constraint check (status in ('active','inactive'))`. ה-constraint בפועל: `CHECK (status = ANY (ARRAY['active','inactive']))`. **הערך `'frozen'` לא קיים יותר** | כל שאילתה/policy/קוד שמשווה מול `users.status` — עדכן מ-`frozen` ל-`inactive` |
| 02/07 | ישי | **policy חדשה `users_update_self`** (UPDATE, permissive, מתווספת ב-OR ל-`users_write_ceo_only`): משתמש מעדכן את השורה של עצמו בלבד (`phone`/`full_name`), עם הקפאת `role_id` (דרך `current_user_role_id()`) ו-`status='active'` — מונע הסלמת-הרשאות עצמית. **תיקן באג**: לפני כן עדכון טלפון בפרופיל של משתמש לא-מנכ"ל נחסם בשקט (0 שורות) והציג הצלחה כוזבת | כל מודול שנותן למשתמש לערוך את השורה שלו ב-`users` |
| 02/07 | ישי | **משתמשי בדיקה: כיסוי מלא ל-5 התפקידים.** נוצרו חשבונות Supabase Auth אמיתיים (auth.users+identities) עבור `projects.test@regin.co.il` (role 2, לא היה קיים בכלל), `finance.test@regin.co.il` ו-`recruit.test@regin.co.il` (היו שורות DB בלי חשבון התחברות בפועל) — כולם `status='active'`. אופסה סיסמת `logistics.test@regin.co.il` (לא ידועה). הוזנו מספרי טלפון פיקטיביים ל-5 המשתמשים (כולל מנכ"ל) | בדיקות ידניות של מטריצת ההרשאות/Sidebar/ProtectedRoute לפי תפקיד — פרטי התחברות אצל ישי, לא ב-repo |

---

## 💻 עדכוני קוד (סיום מודולים)

| תאריך | מי | מה הושלם | מי שתלוי בזה יכול להתחיל |
|-------|-----|----------|--------------------------|
| _(שורה חדשה כאן — הכי חדש למעלה)_ | | | |
| 07/07/2026 14:15 | ישי (Claude) | **סנכרון תיאור ה-hook ברוטינת `regin-pr-gate` (יישום ראשון של הפרוטוקול הדו-צדדי).** תיקון ה-Stop hook (12:55) הפך את התיאור בפרומפט של pr-gate למיושן ("stale relative to other changed files"). עודכן ל**מודע-לסשן** — גם בקנוני `docs/claude_routines.md` **וגם** בקובץ החי `~/.claude/scheduled-tasks/regin-pr-gate/SKILL.md` במחשב של ישי (הצד של ישי לא דורף). הצד של אמית יתעדכן דרך ה-merge + סנכרון מקומי | — |
| 07/07/2026 13:52 | ישי (Claude) | **הבהרת רוטינות (תיעוד בלבד): מפת "מתי מריצים מה" + פרוטוקול עדכון דו-צדדי.** `docs/claude_routines.md`: ‏(1) נוספה בסעיף 1 מפה "🗺️ מתי מריצים מה" — טבלה לפי-רגע + **רצף לפני PR מחייב** (docs-sync → e2e → pr-gate, כי pr-gate עושה `git add -A` ומקמט הכל, אז docs-sync חייב לפניו). ‏(2) סעיף 4 עודכן: הקובץ הקנוני **קבוע** (גיבוי/שחזור/מול-דריפט — לא נמחק אחרי אונבורדינג), ופרוטוקול עדכון דו-צדדי — בסשן של ישי, Claude מעדכן גם את ה-SKILL.md החיים במחשב של ישי וגם את הקנוני; אמית מסנכרן את שלו. `amit/05` §④ מצביע למפה. **הוכרע: לא מאחדים רוטינות** (פיצול מוצדק — עלות/תזמון/בטיחות) | שני המפתחים — הרצף לפני PR מחייב מעכשיו; אמית מקבל עם ה-merge | (hooks בלבד, אין קוד אפליקציה/DB/settings.json).** הבעיה: `check-docs-updated.sh` בדק `git status` גלובלי, אז סשן קריאה-בלבד נחסם שוב-ושוב כשסשן מקביל "מלכלך" את העץ (הלולאה מ-07/07). התיקון: `protect-frozen-files.sh` רושם "סימון עריכה" פר-session_id ב-`.git/regin-session-mutations/` (mtime=זמן העריכה האחרונה של הסשן; מחריג עריכות LOG/STATUS למניעת self-reference); `check-docs-updated.sh` מדלג לגמרי על סשן בלי סימון (קריאה-בלבד), ומשווה אכיפת-תיעוד מול זמן-העריכה-של-הסשן במקום מול העץ הגלובלי. הודעת ה-block תויגה "🤖 בדיקה אוטומטית". גם: CLAUDE.md כלל 16 (הבהרת מודעות-לסשן) + הערת `Get-Date` (PowerShell) מול `date` (Bash) בעובדות-הסביבה. אומת ב-6 בדיקות (כולל self-reference). מגבלה: אכיפת מדריך-מיקרו עדיין גלובלית (נדיר) | שני המפתחים — מעכשיו אפשר לפתוח שיחת קריאה-בלבד ליד שיחה כותבת בלי לולאת חסימה | ‏(1) **CLAUDE.md — 2 כללי ברזל חדשים:** כלל 16 (מקביליות: שיחה כותבת אחת בכל רגע; מקביל = קריאה/תכנון בלבד — ה-Stop hook מבוסס-mtime על יומנים משותפים) + כלל 17 (פרומפט מסירה: כל צעד 👤 מקבל הוראות + בלוק 🧩 מוכן להדבקה בתוסף הכרום). גם: הבהרת כלל 2 (Plan Mode בפתיחה/סגירה/§7 — לא בבנייה שוטפת), שורת Supabase MCP בעובדות הסביבה, ופורמט תאריך חדש `DD/MM/YYYY HH:MM` ליומנים (השורה הזו — הראשונה בפורמט). ‏(2) **בלוקי 🧩 נוספו** ליד צעדים ידניים ב-ishay/01 (PR+merge), amit/02/04, roadmap §5, פרוטוקול ה-DB; פרומפטי הסגירה (⑥3) בכל 11 מדריכי המודול דורשים עכשיו הדפסת 🧩. ‏(3) **מדריך חדש `amit/04c_working_with_claude.md`** — פרומפטים, חיסכון בטוקנים, מצבי עבודה, "איזה כלי למה" (Code/כרום/Cowork), קיצורי מקלדת. ‏(4) **module-1.md הושלם לטמפלט הנוכחי** (טבלת יכולות, טבלת מודל+מאמץ, ראיות as-run) — מוכן להרצה חוזרת של פרומפט הסגירה; פרומפט ⑥ ב-ishay/01 רוענן בהתאם. ‏(5) claude_routines.md — סעיף מקביליות; שני הטמפלטים — תאריך+שעה + חובת 🧩 | שני המפתחים — כללי 16–17 חלים מעכשיו; ישי יכול להדביק את פרומפט הסגירה המעודכן (ishay/01 ⑥) |
| 07/07 | ישי (Claude) | **סגירת "החלק של Claude" בשלב ishay/01 + הקשחת ה-E2E לרשת איטית.** ריצת E2E ראשונה נפלה 2/8 (וגם ברי-ראן): אבחון מול לוגי Supabase הוכיח שהשרת ענה 200 מהר לכל בקשה — הכשל בצד הבדיקות: ‏(א) בדיקת המטריצה עשתה `reload()` לפני שה-PATCH חזר (ה-preflight נרשם בשרת, ה-PATCH בוטל באוויר) ‏(ב) זרימות login/שגיאה = עד 8 קריאות-רשת עוקבות שחרגו מ-10s ברשת איטית (רשת שונה מאתמול). **תוקן:** ‏`clickCellAndAwaitWrite` ב-`permissions.spec.js` (המתנה מפורשת לתשובת ה-PATCH — גם בקליקי השחזור, כדי לא להשאיר side-effect ב-DB המשותף) · ‏timeout ‏30s לזרימות login/שגיאה · ‏test-timeout גלובלי ‏60s ב-`playwright.config.js`. אחרי ההקשחה: **E2E ‏8/8 + ‏`npm run verify` ירוקים**. אימות DB חי (MCP): ‏`modules`=9, `permissions`=45, `roles`=5 ✓. ⚠️ הערת היסטוריה: קבצי ה-E2E נסחפו לקומיט ה-docs ‏`ec408d4` (‏`git add -A` של סשן מקביל שרץ באותו זמן) — התוכן שנבדק ירוק = בדיוק התוכן שקומט; מתועד כאן במקום amend על קומיט שכבר נדחף | ישי — הכול קומט ונדחף; נותר רק לפתוח PR (‏base: `dev` ← ‏compare: `ishay/module-1-permissions`) ולמזג |
| 07/07 | ישי (Claude) | **טמפלט הסגירה — הוראות ה-PR המודפסות תוקנו (פער שישי חשף):** האודיט לא דוחף קוד, אך ההוראות הניחו ענף דחוף. נוסף שלב 0 — push דרך רוטינת `regin-pr-gate` (Run now) או קלוד-בסשן; ושלב אחרי-merge — הרצת `regin-docs-sync` (כלל 13(ד)). ‏verify+E2E לא נוספו כצעדי-אדם — האודיט כבר מריץ אותם | כל סגירת מודול מעכשיו (חל אוטומטית — הפרומפטים מפנים לטמפלט) |
| 07/07 | ישי (Claude) | **מדריך אונבורדינג חדש לעמית — כיוונון Claude Code חזק (`docs/guides/amit/04b_claude_code_power_setup.md`).** תיעוד בלבד (אין קוד/DB). מסביר לעמית אילו MCP/סקילים/פלאגינים/קונקטורים להתקין ומה **לא**, כדי שה-Claude שלו יהיה ממוקד לסטאק. עוגן במציאות מאומתת: **`.mcp.json` כבר מגורסן בשורש** ומספק Supabase MCP מתארח (HTTP+OAuth, בלי סוד בקוד) — עמית רק מאשר+מתחבר בהרצה ראשונה; פלאגינים דרך `/plugin` (לאמת שמות בתוך המוצר); קונקטורים לא רלוונטיים ל-CLI; רשימת "רעש לנתק" (Booking/Gmail/Calendar…). נכנס בין 04 ל-05 בלי לשנות שמות קבצים; `00_track.md` + תנאי-הכניסה של 05 עודכנו | עמית — לבצע את 04b אחרי שלב 04 ולפני הרוטינות (05); לאחר מכן ה-Supabase MCP מחובר וה-advisors ברוטינות ירוצו |
| 07/07 | ישי | **2 משתמשים אמיתיים נוספו למערכת (דאטה, לא סכמה):** עמית מילר (`amitmillertati@gmail.com`) וטל רודגולד (`talrodgold@gmail.com`), שניהם בתפקיד **מנכ"ל**, `status='active'`. לעמית נוצר חשבון Auth ‏07/07 (התחברות ראשונה אומתה בפועל); לטל חשבון Auth קיים מ-03/07 — **מעולם לא התחבר**, הסיסמה היא זו שהוגדרה ב-03/07 (אם אינה ידועה — מחיקה ויצירה מחדש ב-Authentication→Users). סיסמאות אצל ישי בלבד, לא ב-repo | שני המפתחים — לעמית יש עכשיו כניסה אמיתית למערכת (הרשאות מנכ"ל) לקראת מודול 2 |
| 07/07 | ישי (Claude) | **תיקון UI בסייד-בר (מודול 1, ליטוש אחרי-אודיט):** פריטי הניווט היו ממורכזים (`justify-center`) — הוחלף ליישור-התחלה (ימין ב-RTL) במצב מורחב, כך שכל האייקונים על קו אנכי אחיד כמקובל; מרכוז נשאר רק במצב מכווץ. `Sidebar.jsx` בלבד, 3 קישורים. אומת חי (login + צילומי מורחב/מכווץ) + lint נקי. **לא בקומיט עדיין** — נכנס ל-PR של מודול 1 כ-commit נפרד. **תיקון הנחיה (07/07):** גם קבצי ה-docs של פתיחת מודול 2 נכנסים לאותו PR כ-commit שני — עמית על מחשב אחר; docs שלא עוברים דרך `dev` לא יגיעו אליו (הוראות מדויקות: ishay/01 ④3) | ישי — לפני ה-PR: שני קומיטים לפי ishay/01 ④3 |
| 07/07 | ישי (Claude) | **מודול 2 (לקוחות) — פתיחה: בלופרינט נוצר + 2 שאלות §7 נסגרו + שדרוג טמפלטים.** ‏(1) `docs/micro_guides/module-2.md` נוצר לפי טמפלט הפתיחה (ישי הכריע במקום עמית; עבר סבב אימות אדוורסרי — 18 ממצאים תוקנו). כולל SQL מלא ל-RLS הראשון על טבלה עסקית (תבנית §7.21, ‏`module_name='לקוחות'`) + טבלת 14 תרחישי הבדיקה שנדחו ממודול 1. ‏(2) **הוכרעו:** §7.3 (תוויות `customer_type` לפי האפיון) + §7.11 (בלי מיזוג; ח"פ קנוני; חיפוש סלחני; חסימת כפילות + הצעת שחזור מארכיון) — ר' PROJECT_MASTER. ‏(3) הכרעות נוספות: שליחת שיווק = Storage ציבורי+mailto (אמיתי במודול 10) · כרטיס לקוח מלא עם empty states. ‏(4) **שני הטמפלטים שודרגו** (משוב ישי): תקציר יכולות בעברית, הצהרת השתלבות, רישום חובות חוצה-מודולים ב-PROJECT_MASTER §6 (+חובת קריאה בפתיחה), סגנון שאלות הכרעה, טבלת מודל+מאמץ. **בלי שינוי קוד/DB** — המיגרציה תיכתב בצעד 1.1 של המדריך | עמית — אחרי merge של מודול 1: התחל ישר מפרומפט 2 (המשך-בנייה) במדריך amit/06 ⑥; **אל תריץ שוב את פרומפט הפתיחה** |
| 06/07 | ישי (Claude) | **מודול 1 נסגר — verdict [YES] — ממתין ל-PR+merge של ישי.** אודיט הסגירה: `npm run verify` ירוק · `npm run test:e2e` ‏8/8 · אימות DB חי מול MCP (‏roles=5, modules=9, permissions=45, ‏7 policies, ‏4 פונקציות SECURITY DEFINER) · gitleaks על כל 39 הקומיטים בהיסטוריה — **אין ממצאים** (ה-CI צפוי ירוק). **§7.24 הוכרע:** סיסמאות משתמשי הבדיקה שנחשפו בהיסטוריה **לא מוחלפות** — סיכון מקובל ומתועד (פרויקט אקדמי). תיקון הערת snapshot ב-`docs/schema.sql` ‏(10→9 מודולים). ⚠️ **המיזוג יבוצע ע"י ישי לבד בלי סקירת עמית** — עמית עדיין בשלב התקנת הכלים (amit/01), לפי הכלל במדריך ishay/01 ④5 | עמית — אחרי ה-merge: `dev` מוכן, מודול 2 יכול להתחיל עם תבנית ה-RLS מ-§7.21 |
| 06/07 | ישי (Claude) | **מערכת מדריכי-מיקרו machine-first (שדרוג כל מערך התיעוד-לעבודה).** ‏(1) שני הטמפלטים ב-`docs/templates/` נכתבו מחדש: התוצר (מדריך מיקרו) כתוב **לקלוד** — אנגלית מלאה, 9 סעיפים, כותרת-מצב חיה, נקודות עצירה מתויגות 🤖/👤 (קלוד מאמת טכני לבד; ישי/עמית מאשרים בסופי פזות/הכרעות §7/מיגרציות), פרוטוקול עדכון-עצמי מחייב. ‏(2) `docs/micro_guides/module-1.md` נדרס ונכתב מחדש כ-as-built באנגלית — הפיילוט של הפורמט (כולל תיקון נספח ה-QA שהתיישן). ‏(3) כל 11 מדריכי המודול: סעיף ⑥ הפך ל-**3 פרומפטים** (פתיחה/המשך-בנייה/סגירה). ‏(4) CLAUDE.md: כלל ברזל 15 (מדריך מיקרו חי) + צעד 0 בפרוטוקול סוף-סשן. ‏(5) ה-Stop hook הורחב: קוד מודול שהשתנה בלי עדכון מדריך-המיקרו שלו חוסם סיום סשן (נבדק ב-3 תרחישים). ‏(6) roadmap §5/§8, README, claude_routines.md + עותקי SKILL.md עודכנו בהתאם | שני המפתחים — מעכשיו כל מודול נפתח/נבנה/נסגר עם 3 הפרומפטים שבמדריך השלב שלו; אין יותר "פרומפט אחד" |
| 06/07 | ישי (Claude) | **סגירת פינות בתשתית התיעוד וההגנות (לא קוד אפליקציה).** ‏(1) **אבטחה:** בלוק פרטי ההתחברות הוסר מהקובץ הזה (הסיסמאות מוחלפות ב-Supabase; פרטים מישי בערוץ פרטי). ‏(2) **hooks עברו לסקריפטים** ב-`.claude/hooks/` (`check-docs-updated.sh` + חדש: `protect-frozen-files.sh` שחוסם עריכה/מחיקה של C5/C6 הקפואים) — `settings.json` רק מפנה אליהם. ‏(3) **CLAUDE.md:** 2 כללי ברזל חדשים — פרוטוקול שינוי מהותי (כלל 13), SSOT ללוגיקה עסקית + `api.js` פר-מודול (כלל 14). ‏(4) **PROJECT_MASTER:** תוכן עניינים, סטטוס הועבר ל-STATUS בלבד, §7.22–7.23 חדשים, עדכון שורות מיושנות. ‏(5) **מדריכים:** פרומפטי ⑥ פושטו (Claude קורא את הטמפלט בעצמו — בלי הדבקת תוכן ידנית); ishay/01 עודכן למצב אמת. ‏(6) `mockup_descriptions.md` נוקה מעותק-אב ישן וסותר — עכשיו תיאורי מסכים בלבד. ‏(7) 4 קבצי פלטי-סוכנים נמחקו מ-`reference_spec/` | עמית — הכללים החדשים ב-CLAUDE.md וה-hooks יגיעו אליך אוטומטית ב-clone; שום דבר לא חוסם אותך |
| 06/07 | ישי (Claude) | **מודול 1 — סגירה: הכרעת §7.21 (מודל RLS) + תיקוני בקרת קוד P0/P1.** §7.21: הרשאה לכל טבלה עסקית עתידית נקבעת אך ורק לפי מטריצת role→module (בלי בעלות ברמת-רשומה) — תבנית ה-policy המדויקת נכתבה ב-`PROJECT_MASTER.md` §7.21, מוכנה לשכפול במודול 2. תיקונים: `package.json` (`engines: node>=20`), `.env.example` (הערת E2E אופציונלי), `src/components/ErrorBoundary.jsx` חדש (עוטף את `App.jsx` — קריסת רינדור לא-מטופלת כבר לא = מסך לבן), `index.html` (`lang="he" dir="rtl"`, היה `lang="en"` בלי dir), הערת load-bearing ב-migration של `current_user_role_id()`, ו-2 בדיקות E2E חדשות ב-`e2e/permissions.spec.js` (שינוי-תא מלא + הגנת self-lockout על עמודת המנכ"ל). `npm run verify` ו-`npm run test:e2e` (8/8) ירוקים. **בדיקה ידנית שנמצאה כבר ממומשת:** שינוי סיסמה ב-`ProfileSettingsPage.jsx` כבר מחובר ל-`supabase.auth.updateUser` — ממצא קודם בבקרת הקוד היה מיושן | מודול 2 (עמית) — תבנית ה-RLS מוכנה לשכפול על `customers`; שאר המפתחים - ה-Error Boundary וה-RTL הגלובלי חלים על כל מסך עתידי |
| 06/07 | ישי (Claude) | **⚠️ עמית, שים לב — מפת הקבצים בתיעוד השתנתה! נוצרה ערכת מדריכים מלאה** בהשראת ערכת "מלאי 710": `CLAUDE.md`+`STATUS.md`+`README.md` חדשים בשורש (**קרא אותם ראשונים**), `docs/guides/` (מפת דרכים + מסלול אישי לך עם 01_install_tools.md כשלב ראשון, מסלול לישי, ומודולים משותפים), `docs/claude_routines.md` (איך ליצור את 4 הרוטינות בחשבון Claude *שלך* — הן פר-מחשב, `git clone` לא מביא אותן), `docs/code_review_2026-07.md`. **נמחקו** (תוכן מוזג לערכה החדשה): `docs/macro-guide.md`, `docs/WORKFLOW.md`, `docs/README.md`, `CLAUDE_CODE_LOG.md` בשורש. `PROJECT_MASTER.md` §7 קיבל 11 שאלות פתוחות נוספות (11–21). ה-Stop hook עבר מ-`.claude/settings.local.json` (אישי) ל-`.claude/settings.json` (משותף, ב-git — תגיע אליך אוטומטית ב-clone) | עמית — עדכן bookmarks; תתחיל מ-`docs/guides/amit/01_install_tools.md` |
| 04/07 | ישי (Claude) | **ספרינט הקשחה — תשתית פרויקט (לא פיצ'ר-מודול).** שער-איכות `npm run verify` (lint+format+test+build); Prettier + ESLint נקי; Husky pre-commit→lint-staged; **Vitest** (16 בדיקות: `validators`/`permissions`/`ProtectedRoute`) + `isAllowed()` חולץ ל-`src/lib/permissions.js`; **CI** `.github/workflows/ci.yml` (על PR: lint+test+build+gitleaks); **מיגרציות מגורסנות** ב-`supabase/migrations/` (baseline + 6 מיגרציות שנמשכו מה-remote — **ללא שינוי סכימה בפועל**); `vercel.json`+`.env.example` (מוכן, לא-מופעל). ראה `docs/architecture_and_qa_roadmap.md` (סטנדרט+DoD) ו-`supabase/README.md` | שני המפתחים — מכאן: כל PR עובר `npm run verify`; שינויי DB = קובץ מיגרציה חדש (לא עריכת schema.sql); בדיקות `*.test.js` לצד לוגיקה חדשה |
| 04/07 | ישי (Claude) | **⚠️ שינוי שמות-קבצים בתיעוד — עמית, שים לב ללינקים ישנים ששמרת!** תוכן RTL לא נגע, רק שמות: `REG-IN_מדריך_מאקרו.md`→`docs/macro-guide.md`, `REG-IN_מדריך_לעמית_מאוחד.md`→`docs/amit-onboarding.md`, `micro_guides/REG-IN_מדריך_מיקרו_מודול_1.md`→`micro_guides/module-1.md`. `docs/mokap/`→`docs/mockups/` (11 תת-תיקיות + 44 תמונות, שמות אנגליים). כל הקישורים הפנימיים תוקנו | עמית — לעדכן bookmarks/קיצורי-דרך אם יש; מדריכי-מיקרו עתידיים ייקראו `module-2.md` וכו' (לא שם עברי) |
| 04/07 | ישי (Claude) | **E2E אמיתי (Playwright) הוקדם ממודול 12 — לזרימות מודול 1 (Auth/RBAC) בלבד.** `@playwright/test` + Chromium; `playwright.config.js`; `e2e/auth.spec.js`+`e2e/permissions.spec.js` (6 בדיקות, ירוקות). `npm run test:e2e` — **לא** חלק מ-`verify`/CI (איטי במכוון, רוטינה נפרדת `regin-e2e-check`). ⚠️ **דורש `.env.local` מקומי** עם `E2E_CEO_EMAIL/PASSWORD`+`E2E_STAFF_EMAIL/PASSWORD` (ראו `.env.example`) — בלי זה הבדיקות מדלגות על עצמן (לא נכשלות). `regin-pr-gate` שודרגה: verify ירוק על ענף פיצ'ר → commit+push אוטומטי (לעולם לא main/dev) | עמית — אם תרצה להריץ E2E מקומית, תצטרך להוסיף את אותם 4 משתני-סביבה ל-`.env.local` שלך |
| 03/07 | ישי (Claude) | **מודול 1 — באטצ' 2: Sidebar שטוח + admin permission-driven + session hardening.** `Sidebar`: "ניהול מערכת" עבר מ-accordion לקישור **שטוח** (מנווט ל-`/system/users`; ניווט-משנה דרך הטאבים בתוכן). **Topic A**: גישת `/system/*` עברה מ-`role==='מנכ"ל'` קשיח ל-**permission-driven** (`SYSTEM_MODULES` ב-`constants.js`, נאכף ב-`Sidebar`+`App.jsx` ProtectedRoute) — פתר את ה-split-brain. **Topic B**: `supabaseClient` → `sessionStorage` (ניתוק בסגירת לשונית/דפדפן). תיעוד: playbook חירום לשחרור נעילה; החלטות Seed של products/params ננעלו (Topic D, בראש `products_and_params.md`) | מודול 2+ — אותה מוסכמת גישת-מערכת permission-driven; החלטות ה-Seed נעולות ל-Seed במודול 3 |
| 03/07 | ישי (Claude) | **מודול 1 — סגירה: Google Sign-In + נעילת חשבון + UX Sidebar + ביטול CAPTCHA.** `LoginPage`: כפתור **Google OAuth אמיתי** (`signInWithOAuth`) + **נעילת-חשבון** (RPCs `check_login_lock`/`register_failed_login`/`reset_login_attempts`) בזרימת הכניסה. `AuthContext`: **שער הרשאה מרכזי** לזרימת OAuth — חשבון גוגל בלי שורת `users` מנותק (`signOut`) עם `authError`. `Sidebar`: כפתור הכיווץ **הועבר מהתחתית לראש התפריט** (ליד הלוגו). תיעוד: CAPTCHA בוטל רשמית; 2 הטמפלטים עודכנו + `docs/WORKFLOW.md` חדש | מודול 2+ — מודל האבטחה/Auth יציב; אותה מוסכמת שער-הרשאה חלה על מסכים חדשים |
| 02/07 | ישי (Claude) | **מודול 1 — ליטוש Context/Hooks + הערות + ניקוי תיעוד.** `AuthContext`: `loadUser` ב-`useCallback`, Guard Clause ל-`useAuth` (זריקת שגיאה מחוץ ל-Provider), `mountedRef` נגד setState-אחרי-unmount. הערות עברית "why-first" בכל קבצי המודול; תוקן hooks-lint ב-`PermissionsMatrixPage` (useCallback) — המודול כולו lint-נקי. תיעוד: נמחק README boilerplate, `reference_spec/*.md.md`→`.md`+כותרת "קפוא", נוצר `docs/README.md`, מדריך-מאקרו הורחב ל-SSOT (§9–13) | ללא שינוי חוזה — מודול 2+ ממשיכים כרגיל |
| 02/07 | ישי | **מודול 1 — ביטול מסגור "מחיקה", מעבר לסטטוס דו-כיווני.** `UsersManagementPage`: הוסר כל מסגור של "מחיקה" (כפתור/אייקון פח/הודעת אישור) - הוחלף בכפתור מצב יחיד שמשתנה לפי הסטטוס הנוכחי ("השבת משתמש"/`UserX` כשפעיל, "הפעל משתמש מחדש"/`UserCheck` כשלא-פעיל). **הוסר הסינון שהחביא שורות `inactive`** - הטבלה מציגה עכשיו את כל המשתמשים, עם תג סטטוס אמיתי ("פעיל"/"לא פעיל") ושורה מעומעמת (`opacity-60`) למשתמשים לא-פעילים. שינוי סטטוס דו-כיווני לגמרי עכשיו - אין יותר "מסע חד-כיווני". אין שינוי RLS/DB - הפעולה כבר הייתה מותרת למנכ"ל (`users_write_ceo_only` לא מוגבל לעמודות) | מודול 2+ — אותה מוסכמה (סטטוס דו-כיווני, בלי מסגור "מחיקה") מחייבת גם במסכי לקוחות (מודול 2) ודיילות (מודול 4) כשייבנו |
| 02/07 | ישי | **מודול 1 — QA משוב: עריכת שם-עצמי + עריכת משתמש למנכ"ל.** `ProfileSettingsPage`: שדה "שם מלא" הפך לבר-עריכה (היה תצוגה בלבד; מותר גם ב-RLS, `users_update_self` מקפיא רק `role_id`/`status`). `UsersManagementPage`: נוסף כפתור "ערוך" (Pencil) לכל שורה, פותח את אותו Dialog בשני מצבים (הוספה/עריכה) — מנכ"ל יכול לעדכן שם/טלפון/**תפקיד** למשתמש קיים; דוא"ל לא ניתן לעריכה בטופס זה (מפתח RLS+FK). **מניעת self-lockout**: כשמנכ"ל עורך את עצמו, שדה התפקיד ננעל (`disabled`) — אותו עיקרון כמו נעילת עמודת המנכ"ל במטריצה ומניעת מחיקה עצמית | מודול 2+ (ללא שינוי מהשורה למטה) |
| 02/07 | ישי | **מודול 1 — צעד 10 + ליטושים** (השלמה מעל השורה מ-02/07 למטה): מסך **מטריצת הרשאות** (`PermissionsMatrixPage`, `/system/permissions` — 7 מודולים עסקיים מקובצים ל-4 קבוצות-על, עמודת מנכ"ל נעולה, Auto-Save). **הגדרות פרופיל** (`ProfileSettingsPage` — פרטים/אבטחה/התראות, נגיש לכל מחובר). **מחיקה רכה** למשתמשים (`status='inactive'`, אייקון פח אדום, שורות מסוננות מה-UI) — החליף את "הקפאה"; `frozen`→`inactive` עודכן ב-`LoginPage`+`MainLayout`. טוגלי התראות מנוטרלים (`disabled`+"בקרוב"). **הקשחה**: עדכוני `users` בפרונטנד עברו ל-`.select()` כדי לא להציג הצלחה כוזבת על חסימת RLS שקטה | מודול 2+ (ללא שינוי מהשורה למטה) |
| 02/07 | ישי | **מודול 1 — ברובו מוכן** (צעדים 1,2,4,5,6,8,9 מתוך 14 במדריך המאוחד; 3 מסופק מראש). מסך התחברות: שדרוג shadcn/ui + הגנת פרונטנד (חוסם משתמש מוקפא/לא-מורשה). מסך ניהול משתמשים חדש (`UsersManagementPage`, CEO-בלבד, RLS-מוגן). **תשתית ניתוב חדשה**: `react-router-dom` + `MainLayout`+`Sidebar`(מתקפל, RBAC-מסונן מ-DB)+`Topbar`(פרופיל+Logout)+`ProtectedRoute` (הגנת נתיבים גלובלית)+`AuthContext` משותף. `App.jsx` הוגדר מחדש עם כל ה-Routes. **לא בוצע commit — הכל עדיין רק לוקאלית, ממתין לסקירה** | מודול 2+ יכולים להתחיל לבנות מסכים אמיתיים במקום ה-`UnderConstruction` placeholder ב-`/customers` וכו', ברגע שמודול 1 יעבור merge ל-`dev` |
| 30/06 | ישי | מודול 0 — תשתית (Vite, Tailwind, Supabase, RTL) | כולם |
| 30/06 | ישי | מודול 1 (חלקי) — מסך התחברות + Auth + משתמש מנכ"ל | — |

---

## 📌 חובות עתידיים (TODO שצריך לחזור אליהם)

דברים שהשארנו "פתוחים" בכוונה, לחזור אליהם כשנגיע למודול המתאים:

- ✅ ~~מסך התחברות → לנתב למסך הבית~~ — הושלם 02/07: מנתב ל-`/` (WelcomePage placeholder, עד שמודול 7 האמיתי ייבנה).
- **שכחתי סיסמה** → לחבר מייל אמיתי + מסך הזנת קוד. יושלם במודול 10.
- **פרמטרים (`params`)** → להזין Seed (מע"מ 18%, יחס אורחים-לדיילת 1:50, משקולות Smart Match) לפני מודול 3. **טרם בוצע.** ⚠️ יש פערי דאטה פתוחים ב-`reference_spec/products_and_params.md` (מיפוי enum עברית→אנגלית, פרמטרים חסרים, סמנטיקת W3, טעויות) — להכריע עם ישי לפני ה-Seed (מפורט בתוכנית מודול 1).
- ✅ ~~**כניסה עם גוגל**~~ — **בוצע (03/07/2026)**: כפתור Google OAuth אמיתי במסך ההתחברות (הוקדם ממודול 10 כחלק ממודל האבטחה החדש).
- ✅ ~~**CAPTCHA**~~ — **בוטל רשמית (03/07/2026)**, הוחלף ב-Google Sign-In + נעילת חשבון (מודל אבטחה חדש, ראו PROJECT_MASTER §5.1).
- **שדרוג נעילת החשבון ל-Auth Hook** → כרגע האכיפה ברמת אפליקציה/DB (`login_attempts` + RPCs, מתאים ל-Free plan) וניתנת לעקיפה בקריאת API ישירה. לנעילה שרת-אמיתית שאי-אפשר לעקוף — Password Verification Hook (נעול לתוכנית Team). לשקול אם/כשעולים לתוכנית בתשלום.
- ✅ ~~**מסך מטריצת הרשאות**~~ — **נבנה (02/07/2026)**: `PermissionsMatrixPage` ב-`/system/permissions` (ראו שורת "צעד 10 + ליטושים" בעדכוני קוד).
- **RLS על 11 הטבלאות הנותרות** → רק `roles`/`modules`/`permissions`/`users` מוגנות. כל טבלה אחרת חסומה לגמרי (RLS on, 0 policies) — כל מי שבונה מודול חדש יצטרך להוסיף policy לפני שהטבלה שלו תחזיר נתונים.
- ✅ ~~**הקשחת `current_user_role_id()`**~~ — **בוצע (02/07/2026)**: migration `harden_current_user_role_id` — `search_path=''` מקובע + EXECUTE הוסר מ-`anon` (ראו שורת ה-DB מ-02/07).
- **הפעלת Leaked Password Protection** ב-Supabase Auth (בדיקת HaveIBeenPwned) → מודול 10, יחד עם זרימת "שכחתי סיסמה" המלאה.
- ✅ ~~**Split-brain RBAC**~~ — **נפתר (03/07/2026)**: גישת "ניהול מערכת" עברה ל-permission-driven (`SYSTEM_MODULES` נאכף ב-`Sidebar`+`App.jsx`, קורא את מפת `permissions` על מודולים 8-9 במקום `role === CEO` קשיח). שורות ה-Seed של מודולי המערכת הפכו למקור אמת חי — כבר לא דאטה מת. *נותר אופציונלי:* לחשוף את 2 מודולי המערכת גם במטריצת ההרשאות ב-UI, כדי לאפשר הענקת גישת-מערכת לתפקיד אחר דרך המסך במקום SQL.
- **מיפוי מודולים לפי מחרוזת עברית** (`MODULE_META` ב-Sidebar, `GROUPS` במטריצת ההרשאות) — שינוי שם מודול ב-DB ישבור בשקט את התצוגה (`if (!meta) return null`). להעביר למפתח יציב (`module_id`/slug) בפעם הבאה שנוגעים בסכמת `modules`.
- ✅ ~~**Error Boundary ברמת ה-Router**~~ — **בוצע (06/07/2026)**: `src/components/ErrorBoundary.jsx` עוטף את `App.jsx`; קריסת render מציגה הודעה בעברית במקום מסך לבן.
- **מוסכמת "פעיל/לא-פעיל" דו-כיוונית ללא מסגור "מחיקה"** — הוכרעה סופית ב-`UsersManagementPage.jsx` (02/07/2026, לפי בקשת ישי): בלי כפתור/אייקון/הודעת "מחיקה", תג סטטוס אמיתי בטבלה, ושינוי סטטוס הפיך בשני הכיוונים מאותו כפתור. **מחייב באופן זהה** במסך לקוחות (מודול 2, `customers.status`) ובמסך דיילות (מודול 4, `hostesses.status`) כשייבנו במקום ה-`UnderConstruction` הנוכחי — שתי הטבלאות כבר כוללות עמודת `status active/inactive` בסכמה, אין צורך במיגרציה.

---

## דוגמה לשורה טובה (למחוק כשמבינים)

> `05/07 · עמית · מודול 2 (לקוחות) גמור — אפשר לבחור לקוח במודול 3 · משפיע על: מודול 3`

</div>
