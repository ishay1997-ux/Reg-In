<div dir="rtl">

# REG-IN — יומן שינויים (CHANGELOG)

> **למה הקובץ הזה קיים:** כשמסיימים מודול או משנים את מסד הנתונים, כותבים פה שורה. ככה כל אחד מהשותפים יודע מה השני סיים, בלי לזכור בעל-פה ובלי לשאול. **חובה לעדכן כאן בכל סיום מודול ובכל שינוי בטבלאות ה-DB.**
>
> 📍 **יומן משותף ל-ישי ועמית — סטטוס עבודה משותף.**  
> (יומן סטטוס פנימי של Claude: [CLAUDE_CODE_LOG.md](CLAUDE_CODE_LOG.md) — החלטות טכניות, סנאפשוטים, tech-debt · לוח מצב מודולים חי: [../STATUS.md](../STATUS.md))
>
> ✅ אומת-סנכרון: 14/07/2026 22:36 (regin-docs-sync — סבב-סגירת-קרקע + ראיון-PM + מועצת-תמחור, 0 קונפליקטים)

---

> 🔑 **פרטי התחברות למשתמשי הבדיקה (5 התפקידים):** אצל ישי, בערוץ פרטי בלבד — **לעולם לא בקובץ הזה ולא בשום קובץ ב-repo.**

## איך ממלאים (כללים)

- **סיימת מודול / חלק ממנו?** הוסף שורה תחת "עדכוני קוד".
- **שינית טבלה ב-Supabase?** (הוספת/מחקת עמודה, שינית constraint) → הוסף שורה תחת "שינויי מסד נתונים (DB)" + **שלח הודעה מיידית לשותף**, כי זה משפיע עליו מיד.
- כל שורה: **תאריך · מי · מה · על מי זה משפיע**. פורמט התאריך: `DD/MM/YYYY HH:MM` (מ-07/07/2026; רשומות עבר בפורמט ישן לא משוכתבות).
- **הכי חדש למעלה** — הוסף שורה חדשה מיד מתחת לשורת-הכותרת של הטבלה (יש שם מרקר `_(שורה חדשה כאן)_`).
- **קיבוץ הזנב-הישן (חריג ל-append-only; הכרעת-ישי 12/07/2026):** רשומות **עדכוני-הקוד** של מודול שכבר מוזג ל-`dev` (זנב ישן מ-~שבוע, לא הרשומות האחרונות) מותר לאחד ל**שורת-דלי שבועית** `| 📦 שבוע DD/MM–DD/MM | …`, בשמירת כל שורת-📣 קואורדינטיבית **מאוחדת** (מה שעמית עדיין צריך). **טבלת שינויי-ה-DB לא מקובצת** (הכי-קריטית-לתיאום). הקיבוץ = **פעולת סשן-אדם** בלבד (לא `regin-docs-sync` האוטומטית). 2–3 השבועות האחרונים נשארים מפורטים.

---

## 🗄️ שינויי מסד נתונים (DB) — הכי קריטי לתאם!

| תאריך | מי | מה השתנה | משפיע על |
|-------|-----|----------|----------|
| _(שורה חדשה כאן — הכי חדש למעלה)_ | | | |
| 11/07/2026 02:18 | ישי (Claude) | **מיגרציה `20260711013517_module2_customer_contacts.sql` הוחלה+אומתה חי (ריבוי אנשי-קשר, §7.81).** הוחל דרך SQL Editor (הקלדה ישירה נכשלה על RTL/פוקוס → הוכנס דרך Monaco API ואומת-תוכן לפני הרצה). אומת חי (MCP `list_tables`/`execute_sql` + browser): טבלה `customer_contacts` · 7 עמודות · FK→customers · covering-index · trigger `extensions.moddatetime` (עבד, בלי fallback) · RLS + 2 policies · 0 שורות. `docs/schema.sql` עודכן. | 📣 **עמית** — `customer_contacts` חיה ב-DB המשותף (מ2); בוחר-הלקוח/PDF שלך צורכים איש-קשר ראשי בלבד — לא נשבר. |
| 11/07/2026 01:35 | ישי (Claude) | **מיגרציה `20260711013517_module2_customer_contacts.sql` נכתבה (ריבוי אנשי-קשר, אופציה C §7.81) — טרם הוחלה.** טבלת-ילד `customer_contacts` (FK→`customers` on delete/update cascade · RLS §7.21 'לקוחות' · moddatetime · covering index) — אנשי-קשר *נוספים* (הראשי נשאר inline). **ממתינה ל-👤 typed-echo apply** (פרויקט משותף). סטיית-C6 §2.4.1. | 📣 **עמית** — טבלה חדשה `customer_contacts` (מ2); בוחר-הלקוח/PDF של מ3 צורכים את איש-הקשר הראשי בלבד — לא נשברים (🚧 מ3 ב-§6). מגיע ב-merge |
| 10/07/2026 17:34 | ישי (Claude) | **5 הלקוחות הפיקטיביים הוסרו** (`delete from customers` → 0 שורות, אומת חי; הכרעת-ישי — היו סיכון-בלבול לסשן הטרי). `customers` ריקה שוב. קובץ `seed_dev_customers.sql` נשמר (הפיך). | 📣 **עמית** — customers ריקה שוב; אין לקוחות-דמה ב-DB המשותף. |
| 10/07/2026 17:00 | ישי (Claude) | **5 לקוחות פיקטיביים (dev-seed)** נוספו ל-`customers` (customer_id 7–11; קובץ `supabase/seed_dev_customers.sql`, idempotent) — **נתוני-פיתוח בלבד**, ל-UI מודול 2 (Phase 3) ולבוחר-הלקוח של מודול 3. `customers` כבר לא ריקה. **↳ הוסרו 17:34 (ר' השורה שמעליה) — `customers` ריקה שוב.** | 📣 **עמית** — לקוחות-דמה זמינים ב-DB המשותף לקישור הצעות-מחיר (מ3); ריאליסטיים אך לא אמיתיים. **הוסרו 17:34.** |
| 10/07/2026 16:07 | ישי (Claude) | **§7.64 הוכרע — מפתח surrogate ל-`customers` (+ כיוון קנוני לכל המפתחות).** `customer_id`→`bigint` identity PK; ח"פ→`company_number` unique not null; `quotes.customer_id`→`bigint` (FK). מיגרציה `20260710160735_module2_customers_surrogate_key_rls_and_marketing.sql` **הוחלה (MCP, remote version `20260710132720`) 10/07 16:32, אומתה חי** (PK=bigint · company_number unique+not-null+check · 2+4 policies · FK · 11 triggers · 0/0 rows). ✅ advisor `moddatetime`-in-public **תוקן** במיגרציה `20260710164420` (moddatetime→`extensions`; 11 טריגרים תקינים, advisor נקי). כוללת גם חבילת-nod (§7.40א/48/62/73) + RLS ל-customers + bucket שיווק. **סטיית-C6 §2.4.1 מתועדת** (module-2.md §9). | 📣 **עמית** — `products.sku` נשאר PK טבעי + `ON UPDATE CASCADE` (מ3, לא נוגעים עכשיו); **`quotes.customer_id` (טבלתך) שינתה טיפוס ל-`bigint`**. הסבר-הכלל: `guides/amit/07` §②. |
| 07/07/2026 16:37 | ישי (Claude) | **תיקון לינט `auth_rls_initplan` על `users` (מיגרציה `20260707163709_module1_users_rls_initplan_select_wrap`, הוחלה ב-MCP):** קריאות `auth.email()`/`current_user_role_id()` בשתי ה-policies ‏`users_select_self_or_ceo`+`users_update_self` נעטפו ב-`(select …)` — חישוב פעם-לשאילתה במקום פעם-לשורה. **זהות התנהגותית מלאה, אומת חי:** התחזות (עובדת רואה רק עצמה, מנכ"ל הכל, הסלמה→42501) + verify + E2E ‏8/8. ⚠️ **תבנית §7.21 ב-PROJECT_MASTER עודכנה לעטיפה — מחייב את כל ה-policies ממודול 2 והלאה** (טיוטת ה-SQL ב-module-2.md כבר עודכנה בהתאם) | **עמית** — למשוך לפני כתיבת ה-policies של מודול 2; התבנית והבלופרינט כבר עטופים |
| 03/07 | ישי (Claude) | **נעילת חשבון אחרי 5 ניסיונות כושלים (מודול 1 — החליף את דרישת ה-CAPTCHA).** migrations `module1_login_attempts_lockout` + `module1_reset_login_attempts_revoke_anon`: טבלה `login_attempts` (RLS-on בלי policies) + 3 פונקציות `SECURITY DEFINER` (`check_login_lock`/`register_failed_login`/`reset_login_attempts`, `search_path=''`), נעילה ל-15 דק'; `reset` ל-authenticated בלבד (revoke ל-anon). נבדק חי: 5 כשלונות נועלים, ה-4 הראשונים לא | זרימת ההתחברות (`LoginPage.jsx`) — שאר המודולים/הטבלאות לא מושפעים |
| 02/07 | ישי (Claude) | **הקשחת `current_user_role_id()`** (migration `harden_current_user_role_id`): קיבוע `search_path=''` + שמות סכמה מלאים (`public.users`, `auth.email()`); הסרת EXECUTE מ-`anon`/PUBLIC — נשאר ל-`authenticated` (נדרש כי ה-policies קוראות לפונקציה בהקשר authenticated). ניקה 2 מ-3 אזהרות advisor על הפונקציה | כל ה-RLS (הפונקציה load-bearing) — אומת: ה-CEO עדיין נטען אחרי השינוי |
| 02/07 | ישי | `modules` תוקן ל-**9 שורות** (הוסרה "מסך הבית" — הוכרע כנגיש-לכולם, לא מודול-RBAC). `permissions` **מלא: 45 שורות** (5×9) לפי המטריצה ב-PROJECT_MASTER §3 | כל המערכת — כל מי שיעבוד מול `modules`/`permissions` |
| 02/07 | ישי | `users.phone` נוספה (עמודה אמיתית, nullable). `role_name`='מנכ"ל' ו-`module_name`='דו"חות' תוקנו ממרכאות עבריות (״) למרכאות רגילות (") | כל שאילתה/policy עתידית שמשווה טקסט מול השמות האלה |
| 02/07 | ישי | **RLS מופעל בפועל** על `roles`,`modules`,`permissions`,`users` + פונקציית עזר `current_user_role_id()` (עם בדיקת `status='active'`). כל 15 הטבלאות היו RLS-on-בלי-policies (חסימה גורפת) — תוקן רק ל-4 אלה; **שאר 11 הטבלאות עדיין חסומות לגמרי**, מישהו שיתחיל לבנות מודול אחר יגלה שהכל ריק עד שיוסיף policy | מודול 2 ואילך — **קרא לפני שאתה מתחיל טבלה חדשה!** |
| 02/07 | ישי | **מיגרציית `users.status`: `frozen` → `inactive`.** רץ בסדר `drop constraint → update … set status='inactive' where status='frozen' → add constraint check (status in ('active','inactive'))`. ה-constraint בפועל: `CHECK (status = ANY (ARRAY['active','inactive']))`. **הערך `'frozen'` לא קיים יותר** | כל שאילתה/policy/קוד שמשווה מול `users.status` — עדכן מ-`frozen` ל-`inactive` |
| 02/07 | ישי | **policy חדשה `users_update_self`** (UPDATE, permissive, מתווספת ב-OR ל-`users_write_ceo_only`): משתמש מעדכן את השורה של עצמו בלבד (`phone`/`full_name`), עם הקפאת `role_id` (דרך `current_user_role_id()`) ו-`status='active'` — מונע הסלמת-הרשאות עצמית. **תיקן באג**: לפני כן עדכון טלפון בפרופיל של משתמש לא-מנכ"ל נחסם בשקט (0 שורות) והציג הצלחה כוזבת | כל מודול שנותן למשתמש לערוך את השורה שלו ב-`users` |
| 02/07 | ישי | **משתמשי בדיקה: כיסוי מלא ל-5 התפקידים.** נוצרו חשבונות Supabase Auth אמיתיים (auth.users+identities) עבור `projects.test@regin.co.il` (role 2, לא היה קיים בכלל), `finance.test@regin.co.il` ו-`recruit.test@regin.co.il` (היו שורות DB בלי חשבון התחברות בפועל) — כולם `status='active'`. אופסה סיסמת `logistics.test@regin.co.il` (לא ידועה). הוזנו מספרי טלפון פיקטיביים ל-5 המשתמשים (כולל מנכ"ל) | בדיקות ידניות של מטריצת ההרשאות/Sidebar/ProtectedRoute לפי תפקיד — פרטי התחברות אצל ישי, לא ב-repo |

---

## 💻 עדכוני קוד (סיום מודולים)

| תאריך | מי | מה הושלם | מי שתלוי בזה יכול להתחיל |
|-------|-----|----------|--------------------------|
| _(שורה חדשה כאן — הכי חדש למעלה)_ | | | |
| 14/07/2026 23:23 | ישי (Claude) | **§7.85 הוכרע + ראיון-PM + מועצת-תמחור + עדכון פרומפט-הפתיחה (תיעוד בלבד; אין קוד/DB).** ‏(1) **ראיון-PM** → ריידרים **F23–F26** ב-§7.82: שעות-התחלה/סיום ב-`quotes` (estimated_hours נגזר) · טאבים-לפי-סטטוס במסך-הצעות · לקוח-חדש-מתוך-ההצעה · **מודל-תמחור=חבילות** (llm-council 5+3). ‏(2) **§7.85 נסגר** — מבנה-מפתח `quote_services` = `line_id bigint` סינתטי (PK), עקבי §7.64; הוכרע בסבב-סגירת-הקרקע ולא נדחה לבלופרינט (מונה-§7: 🟢32/🟡33). ‏(3) פרומפט-הפתיחה של `amit/07` עודכן — F16–F26 + אינווריאנט-התמחור (`pricing.js` לא מקודד "משמרת") + URL-סקר נסגר (Seed #14). **אפס פריטים פתוחים חוסמי-מ3.** | 📣 **עמית** — מ3 מוכן-לבלופרינט לגמרי; מבנה `quote_services` = `line_id` סינתטי (משפיע על §7.67 שיבוץ-מ4 ו-§7.72 ליניאז'-מ6 — הפניה בעמודה-אחת). אין שינוי קוד/DB בפועל. |
| 14/07/2026 22:36 | ישי (Claude) | **סבב-סגירת-קרקע לפני בלופרינט-מ3 (תיעוד בלבד; אין קוד/DB).** ציד-פערים טרי (סוכן-Explore + אימות-ידני) מעל כל הכרעות-הקדם → **7 ריידרים חדשים F16–F22 בתוך §7.82** (הכרעות-ישי בצ'אט): אכיפת-סיבת-דחייה ב-CHECK · שמירת-עריכה אטומית · סה"כ-מהמדויקים · אין-הפרדת-חובות · יחס-דיילת=ברירת-מחדל-נדרסת+הזנת-כמות-ידנית · שעות↔מק"ט בלי-אכיפה · `required_hostess_count`=סכום-qty-דיילות. **+ בלוק "הבהרות-Seed משלימות" (החלטות 10–13)** ב-`reference_spec/products_and_params.md` (רפאים-#4 · הרכב-20 · מדרגות-שירות/מקפים · מק"טי-תגים קנוניים) · **db_roadmap C-6** (אינדקס פקיעה) · תיקון דריפט-מראה `אחוז_מעמ` ב-`amit/07` · שורת-CHANGELOG-בדיעבד לסשן-20:43 · ‏`validators.js` שוחזר (שיבוש-הדבקה) · ‏`AI_rec` נמחק (נקצר). | 📣 **עמית** — ‏§7.82 גדל ב-7 ריידרים (F16–F22) ומסמך-ה-Seed הנעול קיבל בלוק-הבהרות מחייב (החלטות 10–13); אם תקרא את הכרעות-מ3 — קרא את §7.82 המלא. אין שינוי קוד/DB בפועל. |
| 14/07/2026 20:43 | ישי (Claude) | **בדיקת-מוכנות לפני בלופרינט-מ3 + זיקוק `AI_rec` (תיעוד בלבד; אין קוד/DB).** ‏(1) **§7.83 נסגר** — RLS ל-3 טבלאות-בסיס (`params`/`products`/`price_tiers`): ‏`select` פתוח לכל `authenticated`, כתיבה CEO-בלבד (db_roadmap A-19). ‏(2) **§7.84 נסגר** — תוספת-מסך מאושרת: טאב "מחירים" בניהול-מערכת (קטלוג+מדרגות+2 פרמטרי-תמחור, CEO); עיצוב-רקע ב-`docs/module3_prices_tab_design_notes.md` (חדש). ‏(3) **§7.85 חדש-פתוח** — מבנה-מפתח `quote_services` (PK משולש מול `line_id` סינתטי) → קלט ל-DB Design Challenge של בלופרינט-מ3. ‏(4) §7.69 עודכן ("+נסיעות" נשאר, דדליין-לפני-מ10) · §7.72 קיבל כיוון-מועמד (`project_changes`). ‏(5) **ה-Seed הנעול הורחב 16→20 שורות-params** (#17–20: 4 תבניות-מייל עם תוכן אמיתי של ישי). פרומפט-הפתיחה במדריך `amit/07` חווט לכל החדש. *(שורה זו נרשמה בדיעבד 14/07 22:17 — סשן-ההכרעות דילג עליה.)* | 📣 **עמית** — ‏3 הכרעות-מ3 חדשות ב-§7 (‏83/84/85) + ה-Seed הנעול (`reference_spec/products_and_params.md`) גדל ל-20 שורות; אם תיגע במיגרציית-Seed או ב-RLS של טבלאות-הבסיס — קרא אותם קודם. אין שינוי קוד/DB בפועל. |
| 12/07/2026 08:16 | ישי (Claude) | **אבן-דרך 1: קידום `dev`→`main` (PR #8) + תג `milestone-1` + ניקוי-ענפים (git housekeeping; אין קוד/DB).** ישי קידם dev→main (merge `4b09d2f`); אומת דו-זווית: תוכן main==dev · בלי force · בלי דליפת-מ3 · `gh` MERGED. תויג **`milestone-1`** על `4b09d2f` (מקומי+origin). **ניקוי:** נמחקו 5 ענפי-`agents/*`+worktrees · 4 ענפים-אישיים ממוזגים (`governance-cleanup`/`module-1-post-merge-docs`/`module-2-customers`/`module-2-post-merge-docs`) מקומי+origin · `docs-council-hardening` (תוכנו נמסר ב-PR #4). ענף `ishay/module-3-quotes` נפתח מ-dev טרי. נשארו: `main`/`dev`/`ishay/module-3-quotes`. | 📣 **עמית** — ‏`main` מכיל עכשיו מודולים 1+2 (תג `milestone-1`); הענפים המרוחקים הישנים נמחקו מ-origin (לך ל-`dev` הטרי). שום שינוי-קוד/DB — לא נוגע במודולים שלך. |
| 12/07/2026 05:24 | ישי (Claude) | **סבב-טריאז'-2 למ3 (2 סוכני-אודיט) + write-back — אפס-שאלות-פתוחות למ3 (תיעוד; אין קוד/DB).** 2 סוכני-Explore סרקו כיסוי-אפיון (C5/C6) + מוכנות-תהליך → בלוקר-אחד (מי-מאשר) + ~13 פערים. **הוכרעו (ישי):** §7.49 מתוקן (**מנהלת-פרויקטים מאשרת**, כספים=צפייה; RPC=`SECURITY DEFINER`+בדיקה-פנימית) · §7.1 שם-פרמטר-מע"מ=**`אחוז_מעמ`** (מונע מע"מ-0%-שקט) · **§7.82 חדש** (7 סיבות-דחייה+`rejection_notes` · תוקף מ-`updated_at`+פרמטר · נעילת-DB-לכל-שאינו-in_progress · CHECKs-הנחה 0-100 · `products.unit` CHECK · **מסנני-הצעות** ⭐"פג-בקרוב"+פילוח-דחייה · F8-15). מדריך `amit/07` נוקה (ישי-בונה לא-עמית · ענף `ishay/module-3-quotes` · פרומפט-פתיחה מרענן לכל ·מ3·+§7.82 · §7.57🟢). | 📣 **עמית** — מ3 מוכן-לבלופרינט, אפס-פתוחים; כל הכרעותיו ב-`§7` (`grep '·מ3'`) + §7.82. בסבב-קדם-**מ8** יידונו: §7.79/80 (שיקלול-שביעות-רצון-לקוח) + §7.41-משוב-3-ערכים (לאמת C5:409). |
| 12/07/2026 04:48 | ישי (Claude) | **סבב-הכרעות-קדם למודול 3 — 8 פריטי-§7 נסגרו/הונהנו (תיעוד בלבד; אין קוד/DB).** ‏§7.32 (חסימת-אישור תאריך-עבר) · §7.34↳ (בורר-הצעה מסנן מוצרים לא-`active`) · §7.41 (CHECK-צבע 5-ערכים · line_number max+1 · פג-תוקף→`rejected`+סיבה-אוטו בלי-החייאה · PDF-עברית ב-DoD) · **§7.57 🟢 נסגר** (פרמטר-דו"ח-שכר לא-נזרע, ידני) · §7.70 (`params`: UNIQUE-עכשיו · מסך/טיפוס→מ9 · בלי-היסטוריה) · §7.40ב+§7.62 (UNIQUE-param + `quotes.customer_id` NOT NULL הונהנו למ3) · §7.75 (job-ניקוי login_attempts). **+ שליחת-הצעה במ3 = הפקת-PDF+הורדה/ידני** (אוטו=מ10, עקבי §7.12/36). §7.13 Seed = צעד-1 של מ3. סטטוסי-הצעה(3)/פרויקט(8) אושרו מול הסכמה. | 📣 **עמית** — כל הכרעות-מ3 חיות ב-`PROJECT_MASTER §7` (32/34/41/57/70/40/62/75). כשתיגע במ8: §7.41-משוב(3-ערכים) + §7.57-דו"ח-שכר + §7.79/80-שיקלול-לקוח יידונו בסבב-קדם-מ8. |
| 12/07/2026 03:45 | ישי (Claude) | **קיבוץ זנב-ה-CHANGELOG + P25 בספריית-הפרומפטים (תיעוד/tooling; אין קוד/DB).** ‏(1) **טבלת עדכוני-הקוד** — 53 שורות ≤09/07 (מודול-1/תשתית, כבר-מוזג) אוחדו ל-**4 שורות-דלי שבועיות** (📣 מאוחד נשמר); **טבלת שינויי-ה-DB + רשומות 10–12/07 לא נגעו**. הקובץ 165→116 שו'. תועד חריג-הקיבוץ ב"איך ממלאים" + `docs/CLAUDE.md`. ‏(2) **P25** נוסף ל-`prompt_library.md` — פרומפט-הדבקה "דחיסת-תיעוד/סיכום-יום" (מדוד-gauge→קבץ-זנב-ישן; ידני, אדם-נוכח). *(בחירת-ישי: פרומפט בספרייה במקום רוטינה-5 — אפס ריפל "4→5", מכבד 🧊.)* | 📣 **עמית** — ‏(1) ה-CHANGELOG יקובץ מעת-לעת (זנב-ישן→דלי-שבועי; ה-📣 שלך נשמרים) — קרא את הדליים לסקירה מהירה, פרטים ביומן. ‏(2) פרומפט חדש **P25** לדחיסת-תיעוד (סוף-יום) — ר' `prompt_library.md`. |
| 12/07/2026 03:03 | ישי (Claude) | **תחזוקת `CLAUDE_CODE_LOG.md` (תיעוד/tooling בלבד — אין קוד/DB):** הקובץ תפח ל-437 שו'/187KB/34K-טוקנים (כלי-הקריאה נחנק) → **153 שו'/54KB, נקרא בקריאה-אחת**. snapshot ~55→15 שו' (נכתב-מחדש); זנב-היומן ≤09/07 אוחד ל-4 דלי-על שבועיים (10/07 חצי-מפורט; 11/07-ומעלה מילה-במילה) — נרטיב 315→~75 שו'; "קוצרים-לפני-שמוחקים" (עובדות-נצחיות אומתו ברפרנס לפני דחיסה); סעיפי-הרפרנס לא-נגעו. **מנגנון-מניעה:** 2 מרקרי-תקציב מוטבעים + חידוד-מדיניות (כלל-שבוע+self-check) + **`regin-docs-sync`** (קנוני+SKILL-חי) קיבל צעד מדוד-ודגל (STEP 3) ותיקון-סתירה STEP 6 (סף-ישן 250→נרטיב-180, דגל-לא-דוחס). **+ המרה לאנגלית:** הקובץ כולו הומר לאנגלית (קובץ Claude-facing כמו `micro_guides`; אנגלית ~חוצה את ספירת-הטוקנים → הקריאה-בעמוד-אחד). ריפלים: CLAUDE.md (חלוקת-שפות + פרוטוקול-סוף-סשן) · עוגני-הכותרות + הוראת-שפת-הרשומה ב-`regin-docs-sync`. הכרעות-ישי (Plan Mode→אושר; בקר-תוכנית טרי). | 📣 **עמית** — שני דברים: ‏(1) `regin-docs-sync` הקנוני עודכן (STEP 3 מדידת-יומן · STEP 6 סף · עוגני-כותרות אנגליים · רשומות-הלוג נכתבות **באנגלית**) — סנכרן את עותק-ה-SKILL שלך כשתתחיל. ‏(2) `CLAUDE_CODE_LOG.md` עכשיו **אנגלית** (STATUS/CHANGELOG נשארים עברית). שום שינוי-קוד/DB; לא נוגע במודולים שלך. |
| 12/07/2026 02:55 | ישי (Claude) | **בדיקת-עין-בוחנת לפני פתיחת-מ3 (2 סוכני-ביקורת בהקשר-טרי, בקשת-ישי):** ‏(1) **טמפלט-הפתיחה מול module-2.md — ורדיקט: כן, יפיק מדריך באיכות-מ2** (כל 4 מנגנוני-11/07 נחתו ועקביים); תוקנו 3 פערי-ליטוש שנמצאו: רשימת-Files-to-create ב-Context-Packet · צעד-אודיט-סגירה מחייב בסוף-Phase-5 (+שורת-מודל) · הבהרת-scope ל"never a human wait" (ראיה-בלבד, לא כוונת-בנייה). ‏(2) **module-1.md יושר לפורמט הנוכחי:** טוקני-`🚧 מN` בטבלת-היכולות + כלל-התאמת-§6 · ‏5.4-handoff סומן superseded (מ2 נבנה ע"י ישי) · תיקון סתירת anon-EXECUTE ב-DoD · ‏PR→הערת-post-merge · ‏§8 קיבל (h)+ציטוטי-כללים · ‏"12-scenario"→14. ‏(3) דריפט-צבע תוקן ב-CLAUDE.md כלל-8 (`#14B8A6`→`#0D9488`, אדוות-הכרעת-11/07). | 📣 **עמית** — הטמפלט שייצור את המדריך של כל מודול עתידי שלך אומת-ותוקן; module-1.md (הדוגמה הקנונית) מיושר. שום שינוי-קוד/DB. |
| 12/07/2026 02:22 | ישי (Claude) | **סבב-הכרעות-קדם למודול 3 (P13-בצ'אט, 11–12/07) נרשם ב-§7:** ‏§7.49 המרת-הצעה→פרויקט = **RPC אטומי** (הכל-או-כלום; פרויקט נולד-שלם; האישור נשאר אנושי — כפתור-מייל נדחה למ10+) · ‏§7.50 **נעילת-DB** להצעה מאושרת · ‏§7.51 **vat_rate_snapshot** באישור · ‏§7.52 **שני מספרי-רווח** (צפוי נגזר-חי · סופי נשמר-₪-בסגירה; ‏% לתצוגה) · ‏§7.53 ✅ **נסגר** ("אין דבר כזה אירוע בלי דיילות" — CHECK>0 נשאר) · ‏§7.76 הוכרע-עם-49 · ריידר-§7.59 (צבר-רווח-חודשי) · ‏§7.78 התכנס (מחיר-קופא-באישור, בפועל-בסגירה). ‏**+2 🚧-מ3 ב-§6:** היסטוריית-הצעות בכרטיס-הלקוח (תצוגה+PDF) · מנוע-PDF כפונקציה עצמאית. אדוות: db_roadmap + 4 מדריכים (amit/07·09·10 · shared/06). **מודול 3 = ישי (הכרעת 12/07).** | 📣 **עמית** — ‏§7.49–53 שהיו פתוחים למ3 **הוכרעו ע"י ישי**, ומודול 3 עובר לישי (אתה מסייע בהכרעות). אם תיגע במ3/מ6/מ8: קרא PROJECT_MASTER §7.49–53+76+78 + ‏`grep '🚧 מ3'` ב-§6 — הבלופרינט מיישם, לא שואל מחדש. |
| 11/07/2026 22:42 | ישי (Claude) | **מודול 2 (לקוחות) נסגר — verdict [YES]; DoD typed-echo נחתם (ישי, "לקוחות DoD").** אודיט-סגירה מלא בסשן טרי לפי `create_module_final_test_template.md`: ‏13/13 DoD מגובי-ראיה · RLS אומת-מחדש עצמאית (התחזות-MCP מגולגלת: CEO/blocked/view על customers+customer_contacts; policies ≡ §7.21 מילולית) · שערים: lint 0 · 37/37 · build ✓ · e2e 10/2-skip · advisors מטויאז' (0 ממצא-חדש-לא-מטופל) · אפס דריפט-סכמה · דאטת-בדיקה נוקתה באישור-ישי (customers/contacts/marketing = 0/0/0). מדריך-המיקרו 🔒 Closed — awaiting PR/merge. תיקוני-עובדה: הענף קומט+נדחף לפני-האודיט (`dce7675`=origin); ‏§7.81/db_roadmap עודכנו. PR נפתח אחרי אימות-ויזואלי-סופי של ישי. | 📣 **עמית** — מודול 2 בדרך ל-`dev`: אחרי ה-merge זמינים לך `customers`+`customer_contacts` (RLS חי), `matchesCustomerFilters`/`sortCustomers` ב-`src/lib/customers.js`, ו-api-הלקוחות — בוחר-הלקוח של מ3 בונה עליהם. חובות-מ3 שלך: ‏grep ‏`🚧 מ3` ב-PROJECT_MASTER §6 + ‏db_roadmap ‏C-1 (אינדקס `quotes.customer_id` במיגרציה הראשונה שלך). |
| 11/07/2026 22:42 | ישי (Claude) | **טמפלט-הפתיחה `create_micro_guide_template.md` — 3 הכרעות-ישי (11/07):** ‏(1) 🗣️ הפך ל**"תקציר-חוויה" חובה** לפני כל יחידת-בנייה — הצהרת-הבנה של הזרימה העסקית (מוזמנת-תיקון) · ולידציות · תיאור-מסך/מוקאפ · דגלי-"מהמוקאפ/אפיון-שותק — לאישורך" — **והמתנה לאישור מנהל-המוצר לפני כתיבת קוד** (החליף את ההמשך-בלי-המתנה). ‏(2) **יישור שערי-ויזואל:** ‏🤖 = אימות-עצמי פונקציונלי+ויזואלי **עם סקרינשוטים כראיה**; ‏👤 מלא רק בסוף-פאזה (כולל 🎨) והחלטות-עיצוב. ‏(3) **סעיף 🎤 "ראיון מנהל-מוצר" חדש** לפני שער-אישור-הבלופרינט: מסע-משתמש מלא בעברית פשוטה כהצהרות-הבנה + שאלות-ממוקדות + "על מה לא שאלתי ושווה שתספר לי?" — התשובות נאפות ל-Ledger לפני האישור. + אדוות-כלל-13: ‏CLAUDE.md כלל 1 עודכן בהתאם. | 📣 **עמית** — **שלושת השינויים מחייבים את הבלופרינט והבנייה של מודול 3 שלך:** תתראיין ע"י קלוד לפני אישור-הבלופרינט (🎤), תקבל תקציר-חוויה לאישור לפני כל יחידת-בנייה (🗣️), ותקבל סקרינשוטים כראיית-אימות בלי המתנות-אמצע (🤖). קרא `create_micro_guide_template.md` — סעיף 🎤 + שני הבולטים ב-§5. |
| 11/07/2026 17:25 | ישי (Claude) | **כלל-ברזל 1 הורחב — "טריאז' בקשת-שינוי" (`CLAUDE.md`).** על כל בקשת-שינוי לא-טריוויאלית, לפני בנייה: קלוד עונה **גלוי** על 3 שאלות (אפשרי-עכשיו? · שייך-למקום-אחר? · נחוץ-עכשיו-או-מוקדם?) ונותן ורדיקט (`בנה-עכשיו`/`דחה-ל-X`/`לא-נדרש`) — המלצה, ההכרעה של ישי/עמית. F1: מרחיב כלל 1, לא מנגנון חדש. | 📣 **עמית** — מחייב גם את הסשנים שלך; קרא `CLAUDE.md` כלל 1, תת-פסקה "טריאז' בקשת-שינוי". |
| 11/07/2026 17:07 | ישי (Claude) | **מודול 2 — רדיזיין מסך-שיווק + שערי-UX/ולידציות בטמפלטים (אודיט-ישי).** שיווק (`MarketingPanel`+`api`): מונה-אטום → **רשימת-נמענים** עם צ'קבוקס פר-נמען (וי בדיפולט, ביטול לשליחה-הזו-בלבד) + שם/איש-קשר/אימייל/סוג/הנחה + **תצוגת-חומר** (img/PDF); `getConsentedCustomers()` מחליף `getConsentedCustomerEmails`. טמפלטים: שער "🎨 UX & functional review" בסוף-Phase-3 + הרחבת confirm-intent לוַלידציות (יצירה) · סעיף-אודיט מחייב "§2b UX & Validation Audit" (סגירה). מ2: Usability as-run ✅. lint 0·37/37·build ✓·e2e 10/2-skip·prettier נקי. טרם קומט (נדחף בסוף). | 📣 **עמית** — **שינויי-הטמפלטים מחייבים את מודול 3 שלך:** מעכשיו כל מודול עובר שער-UX בסוף-Phase-3 + שלמות-ולידציות + אודיט-UX בסגירה. קרא `create_micro_guide_template.md` §5 + `create_module_final_test_template.md` §2b. (גם: `--primary`=טורקיז · ToastProvider live-region · catch-all — מסבב-1.) |
| 11/07/2026 16:21 | ישי (Claude) | **מודול 2 — סבב תיקוני-UX/נגישות (אודיט-ישי, לפני 5.4).** 11 תיקונים על-הענף: 🔴 באג אובדן-אנשי-קשר בעריכה (`replaceCustomerContacts` לא-מותנה מחק הכל בכשל-טעינה שקט → guard `contactsLoaded`) · אנשי-קשר "שם + טלפון-או-אימייל" + עיצוב-כרטיסים · יישור-RTL אחיד בכרטיס-הלקוח · catch-all 404 (`NotFound`) · התנתקות-ממסך-חשבון-לא-פעיל · העלאה-שיווקית+שורות-לקוח נגישות-מקלדת · "נסה שוב" בשגיאת-טעינה · token טורקיז `--primary`=#0D9488 · a11y (aria-label/role=alert/toast-live-region). באקלוג-M12 (נגישות-שיטתית/פונט-עברי/דפדפנים) נרשם ב-`architecture_and_qa_roadmap.md`. lint 0·37/37·build ✓·test:e2e 10/2-skip·prettier נקי. לא-מקומט (נכנס ל-5.4). | 📣 **עמית** (משטחים-משותפים): `--primary`=טורקיז (כפתור-ברירת-מחדל יוצא טורקיז — override מיותר) · `ToastProvider` live-region פר-toast · `App.jsx` catch-all + `NotFound`. |
| 11/07/2026 12:53 | ישי (Claude) | **מודול 2 — ליטוש-UX אחרון + קומיט-מודול.** באג-טלפון: אותיות נחסמו (`ן9999999` נשמר; regex תווים-מותרים). טופס קוצר ל-2-עמודות לשדות קצרים. כפילות פילטר-הסכמה הוסרה (הצ'יפ "קהל דיוור" מכסה). **קומיט-מודול-יחיד נוצר בסוף-הסשן** (קוד+תשתית-UI משותפת+דוקס; טרם דחיפה — 5.4 דוחף+PR). lint 0·37/37·build ✓·test:e2e 10/2-skip. | 📣 **עמית** — קומיט-מ2 כולל תשתית-UI משותפת (`ConfirmDialog`+`ToastProvider` ב-`src/components/`) שגם מ1 מאמץ; יגיע ב-merge של מ2. |
| 11/07/2026 12:45 | ישי (Claude) | **מודול 2 — טריאז' המלצות-AI-חיצוני (ביקורתי):** יושמו: הסרת פילטר-הנחה-מינימלי (כפילות עם dropdown-ההנחה) · תווית "אירוע אחרון (רדום?)"→"אירוע אחרון" · חיפוש `max-w-md` · עמודות-משנֶה ל-`text-slate-600`. נדחו-עם-נימוק: "3 עמודות-מודגשות" (שגוי — רק שם-לקוח) · גריד-2×2 (מתנגש עם "קטן יותר") · יישור-מתג (כבר תוקן) · placeholder-0 (מיותר אחרי ההסרה). lint 0·37/37·build ✓·test:e2e 10/2-skip. טרם קומט. | — (מסך מ2 בלבד; ללא משטח של עמית) |
| 11/07/2026 12:39 | ישי (Claude) | **מודול 2 — יישום 3 מתובנות-ה-UX (בחירת-ישי):** (2) טבלה עטופה ב-`overflow-x-auto`+`min-w` (גלילה במובייל). (5) הסכמת-ספירה דקדוקית ב-`MarketingPanel` (`consentedPhrase`). (3) **פאנל-הסינון עוצב מחדש לשורה-קומפקטית אחת** (פקדים צרים גולשים, במקום 2-עמודות רוחב-מלא) — אומת ויזואלית. lint 0·37/37·build ✓·test:e2e 10/2-skip. טרם קומט. | — (מסך מ2 בלבד; ללא משטח של עמית) |
| 11/07/2026 12:27 | ישי (Claude) | **מודול 2 — 2 תיקונים + סקירת-UX ויזואלית.** (1) מסננת שביעות-רצון **הוחזרה** לפאנל (הכרעת-ישי; מיישר בחזרה עם §7.80). (2) ולידציית-טלפון רוככה 9→4 ספרות (מספרים קצרים לגיטימיים). + סקירת-UX חיה (צילומי-מסך דרך Playwright) → תובנות רשומות ב-§9 לטריאז' (כרטיס 5-מדדים-ריקים · פאנל-סינון עדיין גדול · טופס ארוך · טבלה בלי overflow-x · "1 לקוחות" · כפילות-דיוור). lint 0·37/37·build ✓·test:e2e 10/2-skip. טרם קומט. | — (מסך מ2 בלבד; ללא משטח של עמית) |
| 11/07/2026 12:17 | ישי (Claude) | **תשתית-UI משותפת: מערכת התראות (toasts) + הידוק-סינון.** קובץ חדש `src/components/ToastProvider.jsx` (`ToastProvider` ב-`App.jsx` + hook `useToast()`; snackbar מרכז-תחתון, נעלם-מעצמו, RTL/§4). **כל 4 מקומות ה-`window.alert` הומרו** (מ2 `CustomersPage`/`MarketingPanel` · מ1 `UsersManagementPage`) → אפס alert/confirm נייטיב בקוד. + toast-הצלחה לארכוב/שחזור. פאנל-הסינון הוקטן עוד (פקדים p-2, תוויות text-xs). lint 0·37/37·build ✓·test:e2e 10/2-skip. טרם קומט. | — (מ1+מ2 של ישי; ללא משטח של עמית. שינוי מ1 רוכב על PR-מ2) |
| 11/07/2026 12:06 | ישי (Claude) | **מודול 2 — מקבץ-UX (סקירת-ישי):** (1) ולידציית אנשי-קשר נוספים (שם+אימייל-תקין פר-שורה בשימוש). (2) טלפון-לקוח: בדיקת-אורך קלה ≥9 ספרות (חופשי-פורמט נשמר; תופס מספר קטוע). (3) **וידוא-ארכוב הוסר** (פעולה הפיכה — חיכוך מיותר; `ConfirmDialog` נשאר למ1/מחיקות-אמת). (4) פאנל-סינון הודק + מסננת-שביעות-רצון המושבתת הוסרה (חוזרת במ8, `🚧 מ8`; סטייה מ-§7.80). lint 0·37/37·build ✓·test:e2e 10/2-skip. טרם קומט. | — (מסך מ2 בלבד; ללא משטח של עמית) |
| 11/07/2026 11:53 | ישי (Claude) | **תשתית-UI משותפת: `ConfirmDialog` (חלון-וידוא מעוצב במקום `window.confirm`).** קובץ חדש `src/components/ConfirmDialog.jsx` (`ConfirmProvider` ב-`App.jsx` + hook `useConfirm()` אימפרטיבי). חוּוט בשני מקומות-האישור הקיימים לעקביות: **מ2 `CustomersPage` (ארכוב)** + **מ1 `UsersManagementPage` (השבתת-משתמש)**. RTL/§4 טורקיז. 4 מקומות `window.alert` נותרו (רעיון-toast נפרד — נרשם §9). lint 0·test:run 37/37·build ✓·test:e2e 10/2-skip. טרם קומט. | — (מ1+מ2, שניהם של ישי; ללא משטח של עמית. שינוי מ1 רוכב על PR-מ2 — לציין באודיט-הסגירה) |
| 11/07/2026 11:41 | ישי (Claude) | **מודול 2 — UX (סקירת-ישי, אחרי שער-ויזואלי): ניווט-ארכיון + אייקוני-פעולה.** (א) toggle "הצג ארכיון" (הראה פעילים+ארכיון יחד) → **כפתור-יחיד "ארכיון"** שמוביל לארכיון ובחזרה ("חזרה לפעילים"); לעולם לא שתי הרשימות יחד. (ב) אייקוני ארכב/שחזר → **צ'יפים צבועים** (ענבר-ארכב / טורקיז-שחזר; לא פח-אדום — ארכוב הפיך). ה-E2E עודכן. lint 0·build ✓·test:e2e 10/2-skip. טרם קומט. | — (מסך מ2 בלבד; ללא נגיעה במשטח של עמית) |
| 11/07/2026 03:07 | ישי (Claude) | **מודול 2 — Phase 5 steps 5.1+5.2: `e2e/customers.spec.js` (חדש) + שער-איכות.** ‏4 ספי Playwright (guide ⑦): edit-tier CEO — יצירה→ראייה→סינון→ארכוב-הפיך · blocked STAFF/לוגיסטיקה — בלי סרגל + URL חסום · וריאנטים finance/logistics ב-`test.skip` עד creds. **ספ מנקה-עצמו** (`afterAll` מוחק קשיח דרך לקוח supabase-js מחובר-CEO; אומת customers=0/contacts=0). **`test:e2e` 10 passed / 2 skipped** · lint 0 · build ✓ · test:run 37/37. (verify מלא: `format:check` נכשל מקומית על CRLF — רעש-Windows ידוע, CI נקי.) טרם קומט. | 📣 **עמית** — דפוס E2E למודול 2 קיים (יצירה/סינון/ארכוב/חסימת-תפקיד) — רפרנס לספי מ3; הספ מנקה-עצמו כדי לשמור customers=0. מגיע ב-merge |
| 11/07/2026 02:48 | ישי (Claude) | **מודול 2 — Phase 4 (Control & Integration) אומת 🤖 (ללא שינוי קוד/DB — צעדי-אימות בלבד).** 4.1 מטריצת-הרשאות (היברид: שאילתת-מטריצה חיה 3×edit/2×blocked + מסלולי-קוד תלויי-הרשאה + הפניית-דפדפן לא-מחובר→login + ספקי M1 E2E כהוכחה עם-סיסמה) · 4.2 רגרסיית-אבטחה (כלל 9): `test:run` 37/37 · `test:e2e` 8/8 (0 דילוגים, M1 ירוק) · תרחישי-RLS 6/9 עם MCP = בסיס-1.3 (CEO 7/45, לוגיסטיקה 1/0). **👤 שער סוף-Phase-4 ממתין לישי לפני Phase 5.** טרם קומט. | — (אימות פנימי; ללא נגיעה במשטח של עמית) |
| 11/07/2026 02:34 | ישי (Claude) | **מודול 2 — step 3.7: ריבוי אנשי-קשר (Option C) חוּוט בקוד.** `api.js`: `listCustomerContacts`+`replaceCustomerContacts` (replace = delete+insert), `listCustomers` מושך `customer_contacts(contact_name)`. `src/lib/customers.js`: `matchesText` מחפש על פני הראשי + כל אנשי-הקשר הנוספים (+בדיקה). `CustomerDetailsCard`: מקטע "אנשי קשר נוספים". `CustomerFormDialog`: שורות-קשר חוזרות (הוסף/הסר, מפתח-שורה יציב, טעינה בעריכה + שמירה). שער: **lint 0 · 37/37 · build ✓ · prettier-content נקי**. טרם קומט. | 📣 **עמית** — בוחר-הלקוח של מ3 יכול לייבא `matchesCustomerFilters` (מחפש כעת גם אנשי-קשר נוספים) ו/או `listCustomerContacts` מה-api. מגיע ב-merge |
| 11/07/2026 02:34 | ישי (Claude) | **CLAUDE.md — פרוטוקול-DB הובהר (הכרעת-ישי): שער-typed-echo = וידוא-כפול, לא הרצה-ידנית.** Claude מסביר את השפעת-המיגרציה → המשתמש מקליד-שם-לאישור → **Claude מחיל דרך MCP `apply_migration`**. ה-MCP מריץ מיגרציות (לא רק קורא); fallback דפדפן/CLI + SQL-נקי-מהערות רק אם ה-MCP לא זמין. | 📣 **עמית** — כשתחיל מיגרציה: אתה מאשר (מקליד שם), **Claude מחיל דרך MCP** — לא אתה ידנית ב-SQL Editor. |
| 11/07/2026 01:35 | ישי (Claude) | **מודול 2 — Phase-3 הרחבות (סשן תגובת-ביקורת, הכרעות-ישי).** 4 תיקונים (updateCustomer strip status · disabled בכפילות · dedup getConsentedCustomerEmails · dialog key=open-counter) + 3 פילטרים ב-`matchesCustomerFilters`/`CustomersFilterSheet` (קהל-דיוור preset · נוספו-לאחרונה · יש/אין-הנחה) + toggle "הצג ארכיון" (**סטייה ממוסכמת-מ1**) + `MarketingPanel` embedded → דיאלוג-בכפתור-כותרת (**סטייה ממוקאפ 01**) + כרטיס-לקוח **5 מדדים** (רווח-גולמי ירד לטובת מספר-אירועים/אירוע-אחרון/גודל-עסקה — **סטייה מ-C6 §2.4.1**). `deriveCustomerMetrics` 5-shape (בלי grossProfit). שער: **36/36 · lint 0 · build ✓**. טרם קומט (נערם על WIP הלא-מקומט של Phase 3). | 📣 **עמית** — רווח-גולמי ירד מכרטיס-הלקוח (יעדו מ8/מ11); 3 פילטרים נגזרים נרשמו 🚧 מ3/מ6/מ8 ב-§6. מגיע ב-merge |
| 11/07/2026 01:01 | ישי (Claude) | **מודול 2 — Phase 3 (UI) הושלם: 6 הקומפוננטות (3.1–3.6), preview-verified.** קבצים חדשים ב-`src/modules/02_customers/`: `CustomersPage.jsx` (רשימה + חיפוש-אחד §7.11 + מיון-עמודות דרך `sortCustomers` + ארכוב דו-כיווני + מתג-הסכמה) · `CustomerFormDialog.jsx` (הוספה/עריכה + זרימת-כפילות §7.11 פעיל/ארכיון + מחרוזות-אפיון מדויקות + ולידציה דו-שכבתית + פס-ירוק) · `CustomersFilterSheet.jsx` (סוג/הנחה/דיוור + שביעות-רצון מושבת §7.80) · `MarketingPanel.jsx` (העלאה→Storage + mailto מקודד ל-BCC-מאושרים + fallbacks) · `CustomerDetailsCard.jsx` (getCustomer טרי + 3 placeholder-מדדים כולל רווח-גולמי §7.79 + היסטוריה-ריקה). `App.jsx`: `/customers` → `CustomersPage` (במקום UnderConstruction). כל השאילתות דרך `api.js`/`customers.js` (כלל 14, 0× `supabase.from` בקומפוננטות). שער: 32/32 · lint 0 · build ✓ · DoD-grep 0. **טרם קומט; ממתין לשער-👤 ויזואלי (ישי vs §4) לפני Phase 4.** ניקוי: 2 לקוחות-בדיקה + 2 קבצי-Storage שנוצרו באימות נמחקו (customers=0). | 📣 **עמית** — UI מלא ללקוחות מוכן; בוחר-הלקוח של מ3 יכול לייבא את `matchesCustomerFilters` מ-`src/lib/customers.js`. מגיע ב-merge של מודול 2 |
| 10/07/2026 19:52 | ישי (Claude) | **מודול 2 — סבב-P13 ל-Phase-3 הוכרע (ישי) + אודיט-עין-בוחנת (מבקר-הקשר-טרי) → 4 ממצאים תוקנו.** הכרעות: תיבת-חיפוש אחת · מיון-עמודות=כן · החלף/הסר-קובץ=כן, drag-drop=לא · §7.80 בלי-תג · §7.65 בלי-UNIQUE-email (קומפוזיט נדחה כ-no-op) · §7.34 בלי-guard (נטיית-מ3: אזהרה) · יד-חופשית לקלוד על פרטי חיפוש/סינון/מיון. תיקוני-אודיט: **חוב רווח-גולמי נותב `🚧 מ7`→`🚧 מ8`** (מ7=דשבורד-תצוגה; בעל-הנוסחה=מ8, אפיון 5.14; מצבת-ניתוב ב-§6 + תיקון §7.79 וציטוטו) · `getCustomer` חווט ל-3.6 · **דרישת-מסלול-קריאה ב-§6**: policies של quotes/projects חייבות SELECT ברמת-view (אחרת כרטיס-הלקוח מתרוקן בשקט) · הערת-מ8: ממוצע-משוב על אירועי-עבר בלבד (5.7.3). שער-Phase-2 נחתם (ישי). | 📣 **עמית** — שלוש נגיעות במודולים שלך: ‏(1) חוב רווח-הגולמי של כרטיס-הלקוח עבר ממ7 **למ8 שלך** (grep `🚧 מ8` ב-§6 בפתיחתו); ‏(2) בבלופרינט-מ3: ה-policies של `quotes` חייבות SELECT ל-view (דרישה ב-§6); ‏(3) במ8: ממוצע-משוב = אירועי-עבר בלבד. הכול מגיע ב-merge |
| 10/07/2026 19:26 | ישי (Claude) | **מודול 2 — חיזוק-עקיבוּת + בדיקה-מלאה של Phase 2 (בקשת-ישי).** מצביעי-`🚧 מN` גריפ-אבל נוספו להערות-הקוד (api.js: מ6 היסטוריית-פרויקטים, מ10 שיווק · lib/customers.js: מ3/מ7/מ8 מדדים) — כולם מגובים ב-§6 קיים. `supabase/README.md` רוענן: 2 שורות-מיגרציה של מ2 + הערת **פער-מספור מקומי↔remote** (MCP חותם UTC; אומת חי `list_migrations`). בדיקה-מלאה: 32/32 · lint 0 · build ✓ · DoD-grep 0 (אחרי תיקון `\bvat\b` — false-positive על "private"). | 📣 **עמית** — אם תריץ `supabase migration list`: 3 המיגרציות שהוחלו-ב-MCP רשומות ב-remote תחת מספר שונה משם-הקובץ — ר' הטבלה וההערה ב-`supabase/README.md` |
| 10/07/2026 17:42 | ישי (Claude) | **מודול 2, Phase 2 הושלם — step 2.2: שכבת ה-API (`src/modules/02_customers/api.js`).** 9 שאילתות המודול מרוכזות בקובץ אחד (כלל 14): `listCustomers`/`getCustomer`/`getCustomerProjects`/`getConsentedCustomerEmails`/`createCustomer`/`updateCustomer`/`setCustomerStatus`/`uploadMarketingFile`/`getMarketingPublicUrl` + קבועי MIME/גודל. תבניות מודול-1: `.select()` לזיהוי חסימת-RLS שקטה, שימור `code` ל-23505 (§7.11). אימות: lint 0-err · `from('customers')` רק ב-api.js · prettier נקי. (step 2.1 כבר: `customers.js`+validators+32 בדיקות.) **עומד ב-`🔻👤` שער-סוף-Phase-2.** טרם קומט. | 📣 **עמית** — שכבת-ה-api של לקוחות מוכנה; אם תמשיך את המודול או תקשר ממודול 3 — ה-SSOT לשאילתות-לקוחות הוא `src/modules/02_customers/api.js` (וחיפוש §7.11 ב-`src/lib/customers.js`). מגיע ב-merge |
| 10/07/2026 12:23 | ישי (Claude) | **כלל-ברזל 13(א) חוזק (הכרעת-ישי, מניעת-חזרה לדריפט-ה-nod).** "§7 קודם" מפורש עכשיו כ**מילולית-ראשון** — ה-write-back ל-§7 נכתב לפני ה-Ledger/הציטוט; אסור לדחות ל"שלב הביצוע". + בשער-הכרעה מהותי (§7/nod) להריץ `regin-docs-sync` (STEP 2 FORWARD תופס מוכרע-שנשאר-פתוח). F1-נקי (משמעת+רוטינה, לא מנגנון). על ענף `ishay/module-2-customers` (הצרכן-המיידי); ל-dev עם PR-מ2. | 📣 **עמית** — כלל-13(א) הודק (§7-קודם מילולית + docs-sync בשער-הכרעה); מגיע ב-merge של מודול 2 |
| 10/07/2026 11:56 | ישי (Claude) | **PR #5 מוזג ל-`dev` (merge `5ddb002`) + מודול 2 נפתח.** גל-הממשל+אודיט-הנאמנות חיים ב-dev (אומת `gh`/`git fetch`). **ישי מתחיל את מודול 2 במקום עמית** (עמית עסוק; המשך-אפשרי = העברת-כותב על אותו ענף). ענף `ishay/module-2-customers` מ-dev טרי · צעד 0.1 ✅. עצר ב-👤: חבילת-nod של Phase 1 (§7.40(א)/48/62/73 + §7.63). | 📣 **עמית** — מודול 2 בבנייה ע"י ישי; אם תמשיך — אותו ענף `ishay/module-2-customers` + שורת-מסירה (P8/P14), לא ענף מקביל |
| 10/07/2026 10:44 | ישי (Claude) | **אודיט-נאמנות סמנטי למכונת-מודול-2 + תיקוני-ממשל (מועצת-llm-council + 2 מבקרים + מעבר-נאמנות-לאפיון A→B; תיעוד בלבד — אין קוד/DB; 🧊 נפתחה בהכרעת-ישי לגל זה).** ‏`module-2.md`: בלוק **Test-Identities** (5 משתמשים/claims/positive-control) · **טלפון-לקוח→free-form** (ביטול `ISRAELI_PHONE_REGEX` שהומצא נגד-אפיון) · **רווח-גולמי-מצטבר** הוחזר לכרטיס (placeholder+🚧 מ7) · פידבק-ויזואלי-ולידציה + מחרוזות-מדויקות (C5 §5.6.17.4) · פרטי-מוקאפ (חיפוש-כפול/מיון/tag) → confirm-intent · `sortCustomers` נוצר ב-2.1 · storage-42501 fallback→comment-out · typed-echo בשער-ההחלה · impersonation+positive-control ב-1.3 · verify-mailto מקודד+guard-סף · write-back של module-1.md · DoD פוצל audit/post-merge · greps תוקנו. **§7:** נוספו 79 (רווח-גולמי-פר-לקוח) + 80 (מיפוי-tag-שביעות); §7.36 (אטומיות upload↔DB) **עוגן** (לא נוצר-מחדש). **§6:** 🚧 מ7 gross-profit. **טמפלטים+CLAUDE.md:** §8 גורע (a-d+h verbatim, e-g ציטוט), 🗣️ מחוזק (קבצים+מוקאפ-ערבון-מוגבל), typed-echo, DoD-split, גליפי-תצוגה, **Test-Identities כדרישת-Context-Packet (חסינות חוצה-מודולים: כל מודול עם RLS/הרשאות חייב לפתור זהויות-בדיקה + positive-control; דחיית-הפשטה עד צרכן-שני)** · **spec-coverage cross-check למבקר-ההקשר-הטרי (חילוץ רשימת-חובה מהאפיון עיוור-לטיוטה → הצלבת-כיסוי; תופס השמטה-שקטה שה-screen-to-column לבד מפספס)**; כלל-1 הודק (מוקאפ-ערבון-מוגבל). | 📣 **עמית** — `module-2.md` (המודול שלך) שוכתב באודיט-נאמנות: קרא את §9 (רשומת 10/07) + בלוק Test-Identities ב-§2 + 3 פריטי-§7 (79/80/§7.36-anchor). 2 הטמפלטים שאתה מריץ עודכנו (typed-echo, §8-גורע, 🗣️-מחוזק, DoD-split). הכול מגיע ב-merge |
| 10/07/2026 00:12 | ישי (Claude) | **קונסולידציית-מכונת-§7 (תיעוד-פרוצס בלבד; אין קוד/DB/§7/§6).** מנגנון §7/§6/🚧/ripple שוכפל מילולית ב-4 מקומות (הפרת כלל-14 SSOT-על-עצמו; בגל-4 שינוי-מנגנון חייב 4 עדכונים). **הומרו 3 העותקים לציטוט-מספר**, ‏CLAUDE.md 13/15 = SSOT-של-פרוצס יחיד: ‏(Site 2) טמפלט-מיקרו §2/🎁/§8-g · ‏(Site 3) טמפלט-final-test §0/§0b/§0c · ‏(Site 4) `regin-docs-sync` STEP 2 בולט-🚧 (קנוני + SKILL.md זהה-בייט). **נשמרו מילה-במילה:** מחרוזת-פורמט §6 · 🔻🤖 stop-point · grep-verify · כל מכניקת-האודיט (Class/FORWARD/BACKWARD). אפס-ניפוח ל-CLAUDE.md (הסרה, לא הוספה — מכבד 🧊). אומת: ציטוטים פתירים · hook קפוא 14/0 · C5/C6 לא-נגעו. `module-2.md` L45 (site-5) דחוי-🔮. | 📣 **עמית** — 2 הטמפלטים שאתה מריץ (פתיחת-מודול + סגירת-מודול) קוצרו: שחזורי-מנגנון-ה-🚧 הוחלפו בציטוט "iron rule 15/13". **התנהגות זהה** — פשוט מצטטים את CLAUDE.md במקום לחזור עליו. אין מה לעשות; מגיע ב-merge |
| 📦 שבוע 09/07/2026 | ישי (Claude) | **גל-חיסון-התשתית + תעתיקי-אפיון + הקפאה 🧊 (תיעוד בלבד).** טריאז'-ישי A–D (תיקוני-נכונות · הסרת-תאריכים F6 · בולט-🗣️ · הרזיית-snapshot) · תעתיקי `C5/C6_clean_transcript` (דרגה-2, קפואים; המקוריים נמחקו אחרי אימות-PASS) · §7.71–78 (קידום-ריידרים) · מנגנון 🚧 נאכף (Stop-hook 0ג) · רשת-עמית (§7=ישי · typed-echo · בקר-הקשר-טרי) · חותמות-🕓 לסעיפי-הרפרנס. **PR #4** (תעתיקים+הוקס) + **PR #5** (גל-הממשל) מוזגו ל-`dev`. | 📣 **עמית** — מדריכיך (`amit/06/07/08/10`) מנתבים שאלות-§7 **לישי**; האפיון-הקפוא = תעתיקי `reference_spec/C5_clean_transcript.md`+`C6_clean_transcript.md`; קרא `🚧 מN` ב-PROJECT_MASTER §6 לחובותיך. הכול ב-`dev`. |
| 📦 שבוע 08/07/2026 | ישי (Claude) | **מיזוג מודול 1 + אודיט-DB מבני + `db_roadmap`.** מודול 1 מוזג ל-`dev` (**PR #2** = קוד `3ba5c5f` · **PR #3** = דוקס); CI-secret-scan תוקן (`permissions` ב-`ci.yml`). תקרית מיזוג-באמצע-סשן → **STEP 0** (fetch+ancestry) בכלל-ברזל 10 + ברוטינה. אודיט-ערעור-DB מבני → נולד **`db_roadmap.md`** + אצוות §7.61–70 (§7.64🔴 מפתחות-טבעיים · §7.63 בעלות-עמודות · §7.67/68). רשם-§7 שליף-לפי-סוג (token ×70). hooks מחוזקים (חור-כלים PowerShell/DC + `test-protect-frozen` 14/14). | 📣 **עמית** — מודול 1 חי ב-`dev` (Auth/RLS/ניתוב); **`db_roadmap.md`** = מפת-שינויי-ה-DB (קרא בכל בלופרינט); §7.61–70 חדשים (63+64 ממונפים למ3+). |
| 📦 שבוע 06–07/07/2026 | ישי (Claude) | **סגירת מודול 1 + תשתית-התיעוד + אודיטי-§7.** מודול 1 verdict [YES] (gitleaks נקי · E2E 8/8 · DB חי 5/9/45). מערכת מדריכי-מיקרו machine-first (2 טמפלטים 9-סעיפים) + ערכת `docs/guides/**` + **4 רוטינות קנוניות** (`claude_routines.md`). אודיטי-§7.25–60 (הכרעות-כבדות: מע"מ 18% · אשכול-שיבוץ §7.6/14/16/29/33/43/44/45 · PDF-בזמן-אמת · ניווט-שטוח). כללי-ברזל 13–17 עוגנו. Stop-hook מודע-לסשן. E2E הוקשח לרשת-איטית. בלופרינט `module-2.md` נפתח. | 📣 **עמית** — כל התשתית שלך: 3 פרומפטים/מודול (⑥) · `prompt_library.md` · `amit/04b`+`04c`; §7.25–60 מוכרעים (מספרים ב-§7, grep). |
| 📦 שבוע 30/06–05/07/2026 | ישי (Claude) | **הקמה + ספרינט-הקשחה + מודל-אבטחת מודול 1.** מודול 0 (Vite+Tailwind+Supabase+RTL) + מודול 1 גל-ראשון (Auth · מסך-התחברות · מנכ"ל). **ספרינט-הקשחה:** Prettier+ESLint+Husky→lint-staged · Vitest (16 בדיקות) · CI (`ci.yml`) · baseline+6 מיגרציות מגורסנות · Vercel-מוכן · E2E Playwright (הוקדם ממ12). **מודל-אבטחה:** CAPTCHA בוטל → Google Sign-In + נעילת-חשבון + sessionStorage + admin permission-driven. Seed products/params ננעל (הביצוע נדחה למ3). | — (תשתית; פרטי-ה-DB בטבלת-שינויי-ה-DB למעלה) |

---

## 📌 חובות עתידיים (TODO שצריך לחזור אליהם)

> 🗺️ **חובות-DB (מ-08/07/2026):** התצוגה המאוחדת של כל שינויי-הסכמה העתידיים — `docs/db_roadmap.md` (השורות כאן מצוטטות שם ב-Lane A2; ההחלטות עצמן רק ב-PROJECT_MASTER §7).

דברים שהשארנו "פתוחים" בכוונה, לחזור אליהם כשנגיע למודול המתאים:

- ✅ ~~מסך התחברות → לנתב למסך הבית~~ — הושלם 02/07: מנתב ל-`/` (WelcomePage placeholder, עד שמודול 7 האמיתי ייבנה).
- **שכחתי סיסמה** → לחבר מייל אמיתי + מסך הזנת קוד. יושלם במודול 10.
- **פרמטרים (`params`)** → להזין Seed (מע"מ 18%, יחס אורחים-לדיילת 1:50, משקולות Smart Match) לפני מודול 3. **טרם בוצע.** ⚠️ יש פערי דאטה פתוחים ב-`reference_spec/products_and_params.md` (מיפוי enum עברית→אנגלית, פרמטרים חסרים, סמנטיקת W3, טעויות) — להכריע עם ישי לפני ה-Seed (מפורט בתוכנית מודול 1).
- ✅ ~~**כניסה עם גוגל**~~ — **בוצע (03/07/2026)**: כפתור Google OAuth אמיתי במסך ההתחברות (הוקדם ממודול 10 כחלק ממודל האבטחה החדש).
- ✅ ~~**CAPTCHA**~~ — **בוטל רשמית (03/07/2026)**, הוחלף ב-Google Sign-In + נעילת חשבון (מודל אבטחה חדש, ראו PROJECT_MASTER §5.1).
- **שדרוג נעילת החשבון ל-Auth Hook** → כרגע האכיפה ברמת אפליקציה/DB (`login_attempts` + RPCs, מתאים ל-Free plan) וניתנת לעקיפה בקריאת API ישירה. לנעילה שרת-אמיתית שאי-אפשר לעקוף — Password Verification Hook (נעול לתוכנית Team). לשקול אם/כשעולים לתוכנית בתשלום.
- ✅ ~~**מסך מטריצת הרשאות**~~ — **נבנה (02/07/2026)**: `PermissionsMatrixPage` ב-`/system/permissions` (ראו שורת "צעד 10 + ליטושים" בעדכוני קוד).
- **RLS על 11 הטבלאות הנותרות** → רק `roles`/`modules`/`permissions`/`users` מוגנות. כל טבלה אחרת חסומה לגמרי (RLS on, 0 policies) — כל מי שבונה מודול חדש יצטרך להוסיף policy לפני שהטבלה שלו תחזיר נתונים.
- ✅ ~~**הקשחת `current_user_role_id()`**~~ — **בוצע (02/07/2026)**: migration `harden_current_user_role_id` — `search_path=''` מקובע + EXECUTE הוסר מ-`anon` (ראו שורת ה-DB מ-02/07).
- **הפעלת Leaked Password Protection** ב-Supabase Auth (בדיקת HaveIBeenPwned) → מודול 10, יחד עם זרימת "שכחתי סיסמה" המלאה.
- ✅ ~~**Split-brain RBAC**~~ — **נפתר (03/07/2026)**: גישת "ניהול מערכת" עברה ל-permission-driven (`SYSTEM_MODULES` נאכף ב-`Sidebar`+`App.jsx`, קורא את מפת `permissions` על מודולים 8-9 במקום `role === CEO` קשיח). שורות ה-Seed של מודולי המערכת הפכו למקור אמת חי — כבר לא דאטה מת. *נותר אופציונלי:* לחשוף את 2 מודולי המערכת גם במטריצת ההרשאות ב-UI, כדי לאפשר הענקת גישת-מערכת לתפקיד אחר דרך המסך במקום SQL.
- **מיפוי מודולים לפי מחרוזת עברית** (`MODULE_META` ב-Sidebar, `GROUPS` במטריצת ההרשאות) — שינוי שם מודול ב-DB ישבור בשקט את התצוגה (`if (!meta) return null`). להעביר למפתח יציב (`module_id`/slug) בפעם הבאה שנוגעים בסכמת `modules`.
- ✅ ~~**Error Boundary ברמת ה-Router**~~ — **בוצע (06/07/2026)**: `src/components/ErrorBoundary.jsx` עוטף את `App.jsx`; קריסת render מציגה הודעה בעברית במקום מסך לבן.
- **מוסכמת "פעיל/לא-פעיל" דו-כיוונית ללא מסגור "מחיקה"** — הוכרעה סופית ב-`UsersManagementPage.jsx` (02/07/2026, לפי בקשת ישי): בלי כפתור/אייקון/הודעת "מחיקה", תג סטטוס אמיתי בטבלה, ושינוי סטטוס הפיך בשני הכיוונים מאותו כפתור. **מחייב באופן זהה** במסך לקוחות (מודול 2, `customers.status`) ובמסך דיילות (מודול 4, `hostesses.status`) כשייבנו במקום ה-`UnderConstruction` הנוכחי — שתי הטבלאות כבר כוללות עמודת `status active/inactive` בסכמה, אין צורך במיגרציה.

---

## דוגמה לשורה טובה (למחוק כשמבינים)

> `05/07 · עמית · מודול 2 (לקוחות) גמור — אפשר לבחור לקוח במודול 3 · משפיע על: מודול 3`

</div>
