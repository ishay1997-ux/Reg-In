<div dir="rtl">

# REG-IN — יומן שינויים (CHANGELOG)

> **למה הקובץ הזה קיים:** כשמסיימים מודול או משנים את מסד הנתונים, כותבים פה שורה. ככה כל אחד מהשותפים יודע מה השני סיים, בלי לזכור בעל-פה ובלי לשאול. **חובה לעדכן כאן בכל סיום מודול ובכל שינוי בטבלאות ה-DB.**
>
> 📍 **יומן משותף ל-ישי ועמית — סטטוס עבודה משותף.**  
> (יומן סטטוס פנימי של Claude: [CLAUDE_CODE_LOG.md](CLAUDE_CODE_LOG.md) — החלטות טכניות, סנאפשוטים, tech-debt · לוח מצב מודולים חי: [../STATUS.md](../STATUS.md))
>
> ✅ אומת-סנכרון: 10/07/2026 11:18 (regin-docs-sync — אחרי גל-הנאמנות, 0 קונפליקטים)

---

> 🔑 **פרטי התחברות למשתמשי הבדיקה (5 התפקידים):** אצל ישי, בערוץ פרטי בלבד — **לעולם לא בקובץ הזה ולא בשום קובץ ב-repo.**

## איך ממלאים (כללים)

- **סיימת מודול / חלק ממנו?** הוסף שורה תחת "עדכוני קוד".
- **שינית טבלה ב-Supabase?** (הוספת/מחקת עמודה, שינית constraint) → הוסף שורה תחת "שינויי מסד נתונים (DB)" + **שלח הודעה מיידית לשותף**, כי זה משפיע עליו מיד.
- כל שורה: **תאריך · מי · מה · על מי זה משפיע**. פורמט התאריך: `DD/MM/YYYY HH:MM` (מ-07/07/2026; רשומות עבר בפורמט ישן לא משוכתבות).
- **הכי חדש למעלה** — הוסף שורה חדשה מיד מתחת לשורת-הכותרת של הטבלה (יש שם מרקר `_(שורה חדשה כאן)_`).

---

## 🗄️ שינויי מסד נתונים (DB) — הכי קריטי לתאם!

| תאריך | מי | מה השתנה | משפיע על |
|-------|-----|----------|----------|
| _(שורה חדשה כאן — הכי חדש למעלה)_ | | | |
| 07/07/2026 16:37 | ישי (Claude) | **תיקון לינט `auth_rls_initplan` על `users` (מיגרציה `20260707163709_module1_users_rls_initplan_select_wrap`, הוחלה ב-MCP):** קריאות `auth.email()`/`current_user_role_id()` בשתי ה-policies ‏`users_select_self_or_ceo`+`users_update_self` נעטפו ב-`(select …)` — חישוב פעם-לשאילתה במקום פעם-לשורה. **זהות התנהגותית מלאה, אומת חי:** התחזות (עובדת רואה רק עצמה, מנכ"ל הכל, הסלמה→42501) + verify + E2E ‏8/8. ⚠️ **תבנית §7.21 ב-PROJECT_MASTER עודכנה לעטיפה — מחייב את כל ה-policies ממודול 2 והלאה** (טיוטת ה-SQL ב-module-2.md כבר עודכנה בהתאם) | **עמית** — למשוך לפני כתיבת ה-policies של מודול 2; התבנית והבלופרינט כבר עטופים |
| 03/07 | ישי (Claude) | **נעילת חשבון אחרי 5 ניסיונות כושלים (מודול 1 — החליף את דרישת ה-CAPTCHA).** migrations `module1_login_attempts_lockout` + `module1_reset_login_attempts_revoke_anon`: טבלה `login_attempts` (RLS-on בלי policies) + 3 פונקציות `SECURITY DEFINER` (`check_login_lock`/`register_failed_login`/`reset_login_attempts`, `search_path=''`), נעילה ל-15 דק'; `reset` ל-authenticated בלבד (revoke ל-anon). נבדק חי: 5 כשלונות נועלים, ה-4 הראשונים לא | זרימת ההתחברות (`LoginPage.jsx`) — שאר המודולים/הטבלאות לא מושפעים |
| 02/07 | ישי (Claude) | **הקשחת `current_user_role_id()`** (migration `harden_current_user_role_id`): קיבוע `search_path=''` + שמות סכמה מלאים (`public.users`, `auth.email()`); הסרת EXECUTE מ-`anon`/PUBLIC — נשאר ל-`authenticated` (נדרש כי ה-policies קוראות לפונקציה בהקשר authenticated). ניקה 2 מ-3 אזהרות advisor על הפונקציה | כל ה-RLS (הפונקציה load-bearing) — אומת: ה-CEO עדיין נטען אחרי השינוי |
| 02/07 | ישי | `modules` תוקן ל-**9 שורות** (הוסרה "מסך הבית" — הוכרע כנגיש-לכולם, לא מודול-RBAC). `permissions` **מלא: 45 שורות** (5×9) לפי המטריצה ב-PROJECT_MASTER §3 | כל המערכת — כל מי שיעבוד מול `modules`/`permissions` |
| 02/07 | ישי | `users.phone` נוספה (עמודה אמיתית, nullable). `role_name`='מנכ"ל' ו-`module_name`='דו"חות' תוקנו ממרכאות עבריות (״) למרכאות רגילות (") | כל שאילתה/policy עתידית שמשווה טקסט מול השמות האלה |
| 02/07 | ישי | **RLS מופעל בפועל** על `roles`,`modules`,`permissions`,`users` + פונקציית עזר `current_user_role_id()` (עם בדיקת `status='active'`). כל 15 הטבלאות היו RLS-on-בלי-policies (חסימה גורפת) — תוקן רק ל-4 אלה; **שאר 11 הטבלאות עדיין חסומות לגמרי**, מישהו שיתחיל לבנות מודול אחר יגלה שהכל ריק עד שיוסיף policy | מודול 2 ואילך — **קרא לפני שאתה מתחיל טבלה חדשה!** |
| 02/07 | ישי | **מיגרציית `users.status`: `frozen` → `inactive`.** רץ בסדר `drop constraint → update … set status='inactive' where status='frozen' → add constraint check (status in ('active','inactive'))`. ה-constraint בפועל: `CHECK (status = ANY (ARRAY['active','inactive']))`. **הערך `'frozen'` לא קיים יותר** | כל שאילתה/policy/קוד שמשווה מול `users.status` — עדכן מ-`frozen` ל-`inactive` |
| 02/07 | ישי | **policy חדשה `users_update_self`** (UPDATE, permissive, מתווספת ב-OR ל-`users_write_ceo_only`): משתמש מעדכן את השורה של עצמו בלבד (`phone`/`full_name`), עם הקפאת `role_id` (דרך `current_user_role_id()`) ו-`status='active'` — מונע הסלמת-הרשאות עצמית. **תיקן באג**: לפני כן עדכון טלפון בפרופיל של משתמש לא-מנכ"ל נחסם בשקט (0 שורות) והציג הצלחה כוזבת | כל מודול שנותן למשתמש לערוך את השורה שלו ב-`users` |
| 02/07 | ישי | **משתמשי בדיקה: כיסוי מלא ל-5 התפקידים.** נוצרו חשבונות Supabase Auth אמיתיים (auth.users+identities) עבור `projects.test@regin.co.il` (role 2, לא היה קיים בכלל), `finance.test@regin.co.il` ו-`recruit.test@regin.co.il` (היו שורות DB בלי חשבון התחברות בפועל) — כולם `status='active'`. אופסה סיסמת `logistics.test@regin.co.il` (לא ידועה). הוזנו מספרי טלפון פיקטיביים ל-5 המשתמשים (כולל מנכ"ל) | בדיקות ידניות של מטריצת ההרשאות/Sidebar/ProtectedRoute לפי תפקיד — פרטי התחברות אצל ישי, לא ב-repo |

---

## 💻 עדכוני קוד (סיום מודולים)

| תאריך | מי | מה הושלם | מי שתלוי בזה יכול להתחיל |
|-------|-----|----------|--------------------------|
| _(שורה חדשה כאן — הכי חדש למעלה)_ | | | |
| 10/07/2026 10:44 | ישי (Claude) | **אודיט-נאמנות סמנטי למכונת-מודול-2 + תיקוני-ממשל (מועצת-llm-council + 2 מבקרים + מעבר-נאמנות-לאפיון A→B; תיעוד בלבד — אין קוד/DB; 🧊 נפתחה בהכרעת-ישי לגל זה).** ‏`module-2.md`: בלוק **Test-Identities** (5 משתמשים/claims/positive-control) · **טלפון-לקוח→free-form** (ביטול `ISRAELI_PHONE_REGEX` שהומצא נגד-אפיון) · **רווח-גולמי-מצטבר** הוחזר לכרטיס (placeholder+🚧 מ7) · פידבק-ויזואלי-ולידציה + מחרוזות-מדויקות (C5 §5.6.17.4) · פרטי-מוקאפ (חיפוש-כפול/מיון/tag) → confirm-intent · `sortCustomers` נוצר ב-2.1 · storage-42501 fallback→comment-out · typed-echo בשער-ההחלה · impersonation+positive-control ב-1.3 · verify-mailto מקודד+guard-סף · write-back של module-1.md · DoD פוצל audit/post-merge · greps תוקנו. **§7:** נוספו 79 (רווח-גולמי-פר-לקוח) + 80 (מיפוי-tag-שביעות); §7.36 (אטומיות upload↔DB) **עוגן** (לא נוצר-מחדש). **§6:** 🚧 מ7 gross-profit. **טמפלטים+CLAUDE.md:** §8 גורע (a-d+h verbatim, e-g ציטוט), 🗣️ מחוזק (קבצים+מוקאפ-ערבון-מוגבל), typed-echo, DoD-split, גליפי-תצוגה, **Test-Identities כדרישת-Context-Packet (חסינות חוצה-מודולים: כל מודול עם RLS/הרשאות חייב לפתור זהויות-בדיקה + positive-control; דחיית-הפשטה עד צרכן-שני)**; כלל-1 הודק (מוקאפ-ערבון-מוגבל). | 📣 **עמית** — `module-2.md` (המודול שלך) שוכתב באודיט-נאמנות: קרא את §9 (רשומת 10/07) + בלוק Test-Identities ב-§2 + 3 פריטי-§7 (79/80/§7.36-anchor). 2 הטמפלטים שאתה מריץ עודכנו (typed-echo, §8-גורע, 🗣️-מחוזק, DoD-split). הכול מגיע ב-merge |
| 10/07/2026 00:12 | ישי (Claude) | **קונסולידציית-מכונת-§7 (תיעוד-פרוצס בלבד; אין קוד/DB/§7/§6).** מנגנון §7/§6/🚧/ripple שוכפל מילולית ב-4 מקומות (הפרת כלל-14 SSOT-על-עצמו; בגל-4 שינוי-מנגנון חייב 4 עדכונים). **הומרו 3 העותקים לציטוט-מספר**, ‏CLAUDE.md 13/15 = SSOT-של-פרוצס יחיד: ‏(Site 2) טמפלט-מיקרו §2/🎁/§8-g · ‏(Site 3) טמפלט-final-test §0/§0b/§0c · ‏(Site 4) `regin-docs-sync` STEP 2 בולט-🚧 (קנוני + SKILL.md זהה-בייט). **נשמרו מילה-במילה:** מחרוזת-פורמט §6 · 🔻🤖 stop-point · grep-verify · כל מכניקת-האודיט (Class/FORWARD/BACKWARD). אפס-ניפוח ל-CLAUDE.md (הסרה, לא הוספה — מכבד 🧊). אומת: ציטוטים פתירים · hook קפוא 14/0 · C5/C6 לא-נגעו. `module-2.md` L45 (site-5) דחוי-🔮. | 📣 **עמית** — 2 הטמפלטים שאתה מריץ (פתיחת-מודול + סגירת-מודול) קוצרו: שחזורי-מנגנון-ה-🚧 הוחלפו בציטוט "iron rule 15/13". **התנהגות זהה** — פשוט מצטטים את CLAUDE.md במקום לחזור עליו. אין מה לעשות; מגיע ב-merge |
| 09/07/2026 23:16 | ישי (Claude) | **גל-חיסון — סשן-סיום: טריאז'-ישי A–D + הקפאת-תשתית 🧊 (תיעוד בלבד; אין קוד/DB).** ‏(A) **תיקוני-נכונות:** ripple חסר של רשת-עמית תוקן — `shared/module_06` (4× "הכרעה משותפת"→§7=ישי), `amit/06`+`amit/07` (שורות-מגן §7=ישי); `PROJECT_MASTER §2` "15 טבלאות"→**16** + שורת `login_attempts`; `ishay/00_track` (1.5→✅ מוזג); `ishay/03_module_05` (סתירת-סדר 5↔6 → טבלת `projects` קיימת-בסכמה); `amit/06`/`amit/07` (live-state/סתירת "אמת↔מוכרע" → מגן חסר-מצב). ‏(B) **F6 אחיד** — תאריכים-מוטבעים הוסרו מכללי CLAUDE.md 2/10/DB/סוף-סשן (קו-אדום 10 — הוראות-git מילה-במילה). ‏(C) **הסבר-בנייה בעברית** — בולט 🗣️ בטמפלט-המיקרו (יחידת-בנייה משמעותית · מה/מה-בו/מטרה). ‏(D) **הרזיית snapshot** (LOG+STATUS, חסר-תאריך). **🧊 הקפאת-תשתית** נרשמה. אומת: hook קפוא 14/0 + grep-post-apply. | 📣 **עמית** — כל 4 מדריכיך עם פרומפט-פתיחה (מ2/מ3/מ7/מ11 = `amit/06/07/08/10`) עודכנו: שאלות-§7 מנותבות **לישי** (לא טריאז' עצמאי, שורת-מגן אחידה); `amit/06` פרומפט-פתיחה = מגן חסר-מצב ("אם module-2.md קיים → דלג לפרומפט 2"). הכול מגיע ב-merge |
| 09/07/2026 12:37 | ישי (Claude) | **גל-חיסון: קידום-ריידרים §7.71–78 + מנגנון חוב-חוצה-מודולים נאכף (🚧) + רשת-עמית (תיעוד/hooks בלבד; אין קוד-אפליקציה/DB).** ‏(א) **8 ריידרי-↳ פתוחים קודמו לפריטי-§7 מלאים (71–78)** — `↳פתוח` הוסר מ-8 פריטי-האם, כל אחד נושא מצביע; כל ציטוטי-הריידר (db_roadmap/מדריכים/STATUS/טמפלטים) הוסבו למספרים החדשים (אומת: 78 טוקנים רציפים, 0 שיורי-↳פתוח). ‏(ב) **מנגנון 🚧 (חוב חוצה-מודולים) נאכף** — האימוג'י 🕗 (שהתנגש ויזואלית עם 🕓, ו-⏳/🧩 כבר תפוסים) הוחלף ב-🚧; טוקן `🚧 מN` במדריך-מיקרו מחייב שורה תואמת ב-`PROJECT_MASTER §6` (שליף ב-grep); ה-Stop hook (אכיפה-0ג חדשה) חוסם 🚧-בלי-§6 (נבדק); 2 הטמפלטים + `regin-docs-sync` (canonical≡live בייט) + CLAUDE.md כלל 15 עודכנו; §6 רוטרופט ל-6 שורות-חוב. ‏(ג) **רשת-עמית (מועצת-Q3):** ניתוב §7=ישי-בלבד (כלל 1) · typed-echo בהחלת-מיגרציה + חתימת-DoD · בקר-הקשר-טרי לבלופרינטים · שאלה-חוזרת⇒תיקון-מקור. | 📣 **עמית** — §7.73 (חותמות-זמן, מ2) ו-§7.74/75/76/78 (מ3) קודמו ממספרי-ריידר; **מנגנון 🚧 חדש למדריכי-המיקרו שלך** (כל יכולת-נדחית = `🚧 מN` + שורת-§6, אחרת ה-hook חוסם); ניתוב §7=ישי. הכול מגיע ב-merge |
| 08/07/2026 23:55 | ישי (Claude) | **גל 2 חיסון-תשתית — רשם-§7 שליף-לפי-סוג (תיעוד בלבד).** לבקשת ישי (הבחנה: §7=רשם-כל-ההכרעות, `db_roadmap`=אגרגטור-DB-בלבד): כל 70 פריטי §7 קיבלו **שורת-סטטוס בת-בדיקה-מכונה** בתחילתם — סמן 🟢סגור/🟡פתוח/🔵להנהון/⚪ממתין + code-span `סטטוס·סוג·מודול` (סוגים: DB/מסך/לוגיקה/אוטומציה/הרשאות/תהליך). **התוצאה:** ‏grep יחיד שולף "כל הפתוחים מסוג אוטומציה" או "של מודול N" — בלי קובץ-אגרגטור חדש. נוסף: כותרת-אמנסטiה (נרטיב-קפוא/מטא-חי) + **איסור-ריידרים-חדשים** (עבודה חדשה = §7.71+, לא ↳). ‏(באג-הטמעה נתפס-ותוקן: §1 נתפס בטעות; אומת 0-לפני-§7/70-ב-§7.) | שני המפתחים — מעכשיו כל פריט-§7 נפתח בשורת-סטטוס; הרוטינה תתחזק אותה (בהמשך-הגל) |
| 08/07/2026 23:26 | ישי (Claude) | **גל 1 חיסון-תשתית (מועצת-llm-council) הושלם — hooks + דריפט + כלל-ראיות (‏hooks+תיעוד; אין קוד-אפליקציה/DB).** ‏(א) **מעבר-ענף נקי** — `ishay/module-1-post-merge-docs` מוזג (PR #3) → ענף-טרי `ishay/docs-council-hardening` מ-dev (8 קבצים עברו נקי, dev==HEAD). ‏(ב) **דריפט §7.12** (הוכרע 07/07; 3 מקומות אמרו "פתוח") — תוקן ב-module-2.md לדג'ר+backlog + פלייסהולדר amit/07. ‏(ג) **`protect-frozen-files.sh` מחוזק** (2 ממצאי-מבקר): באג ה-Bash-branch (בדק את כל-הקלט → חסימות-שווא) צומצם לשדה-הפקודה בלבד; חור-הכלים נסגר — PowerShell + Desktop-Commander (write/edit/move) עברו קודם מתחת לרדאר (גם הגנה וגם רישום-marker). עקרון fail-open + `cp`/`copy` נוספו. ‏(ד) **`test-protect-frozen.sh` חדש** — 14 בדיקות (כולל אימות שהבאג-לשעבר עובר + החור נסגר), **14/14 ירוק**. ‏(ה) **CLAUDE.md — משמעת-ראיות לכתיבת-נרטיב** (הלקח מתקרית-ה-PR): טענה על מצב-חיצוני מצטטת שורת-ראיה מילולית; עדות-אדם גוברת על git מקומי; write-back מורחב לדיווחי-אירוע | 📣 **עמית** — הגנת-ה-hooks עכשיו מכסה גם כלי-כתיבה של MCP; הכול מגיע ב-merge. **פעולה של ישי:** הדבקת matcher ל-settings.json (בדיווח) |
| 08/07/2026 19:14 | ישי (Claude) | **שורת-CHANGELOG שבורה קדם-קיימת תוקנה** (`08/07/2026 17:55` הכילה 2 רשומות ממוזגות — פוצלה ל-17:55+17:16, תוכן משוחזר-git ללא שינוי, אומת נגדית מול STATUS.md שכבר ציטט "17:16" כתאריך-הרשומה השנייה) | — |
| 08/07/2026 19:10 | ישי (Claude) | **תיקון-תהליך: `regin-docs-sync` (+ CLAUDE.md כלל 10) מקבלים בדיקת מיזוג-באמצע-סשן.** תקרית אמיתית: PR #3 מוזג ל-`dev` בזמן שהסשן המשיך לעבוד על אותו ענף (מוזג ב-GitHub, בלתי-נראה לגיט מקומי שלא עשה fetch) — הרוטינה המשיכה "לתקן" דריפט אמיתי אבל ערמה commits לא-מקומטים על ענף שכבר מת. **תוקן:** ‏STEP 0 חדש ב-`regin-docs-sync` (קנוני+חי) — `git fetch` + `git merge-base --is-ancestor HEAD origin/dev` **לפני** שסומכים על מצב-git מקומי; אם כבר מוזג — מדווח בקול, לא ממשיך לצבור commits על הענף. + שורה מקבילה בכלל-ברזל 10 (כל commit נוסף אחרי push). אומת: הפקודה מזהה נכון את המצב הנוכחי | שני המפתחים — כל סשן עתידי שממשיך לעבוד אחרי push יבדוק מיזוג לפני commit נוסף |
| 08/07/2026 18:35 | ישי (Claude) | **🎉 מודול 1 (משתמשים והרשאות) מוזג ל-`dev`.** ‏PR [#2](https://github.com/ishay1997-ux/Reg-In/pull/2) — merge commit רגיל, 54 קומיטים. quality-gate + gitleaks ירוקים (אחרי תיקון-ה-403). זהו סגירת המודול הראשון בפרויקט: תשתית ה-Auth+RLS+ניתוב שכל המודולים הבאים נשענים עליה חיה עכשיו ב-`dev` | 📣 **עמית** — `dev` מוכן, החסם היחיד למודול 2 הוסר. הודעת-מסירה מלאה נשלחת בנפרד (מה לקרוא, תבנית §7.21, טמפלט-הצעת-המחיר) |
| 08/07/2026 18:30 | ישי (Claude) | **תיקון CI: הרשאות ל-gitleaks על PR (`.github/workflows/ci.yml`).** ה-job `secret-scan` נפל ב-PR #2 עם `403 Resource not accessible by integration` — ל-`GITHUB_TOKEN` חסרה `pull-requests:read` לקריאת `GET /pulls/{n}/commits`. **לא היה דלף-סוד** (gitleaks נפל *לפני* הסריקה). התיקון: בלוק `permissions: {contents:read, pull-requests:read}` ל-job. באג-תשתית שהיה חוזר בכל PR — נסגר לכולם | כל PR עתידי (גם של עמית) — ה-secret-scan יעבוד; אין השפעה על קוד/DB |
| 08/07/2026 18:05 | ישי (Claude) | **טמפלט-הצעת-מחיר של ישי אורגן (תיעוד/רפרנס בלבד).** קובץ ה-docx בשם עברי הוסב ל-`docs/mockups/quote-screen/quote_template_draft.docx` (כלל 11) ונבדק: **טיוטה תקפה** — מפל-התמחור (ביניים→הנחת-לקוח%→הנחה-נוספת%→מע"מ 18%→סה"כ) תואם `applied_customer_discount`+`manual_discount` ו-§7.1/§7.26; מבנה-שדות ממופה ל-`quotes`/`quote_services`/`customers`. ההפניה חווטה (המשימה שהמתינה ב-§7.12): PROJECT_MASTER §7.12 · mockup_descriptions · amit/07 | 📣 **עמית** — מודול 3: יש עכשיו רפרנס-מבנה מוכן ל-PDF ההצעה (`quote_template_draft.docx`); לא לבנות טמפלט מאפס — לתרגם אותו ל-renderer בזמן-אמת (§7.12 = בלי אחסון). מיפוי מלא ב-§7.12 |
| 08/07/2026 17:55 | ישי (Claude) | **אודיט-סגירה חוזר למודול 1 → verdict YES (שוב) + תיקון טסט-E2E flaky (test-only).** האודיט המלא הורץ מול ה-DoD של מדריך-המיקרו: DoD אומת (ספירות 5/9/45 חי; users=7), RLS אומת חי ב-MCP (כל ה-policies עטופים `(select …)`, כתיבה CEO-בלבד, חסימת-הסלמה ב-`users_update_self`, `schema.sql` ללא drift), advisors נקי מממצאים חדשים. **הטסט `e2e/permissions.spec.js` (כתיבת-תא) התגלה flaky** — נכשל 1/3 בהתנעה קרה בגלל מרוץ בין העדכון-האופטימי ל-`loadData` הכפול של StrictMode; תוקן test-only (`waitForLoadState('networkidle')` לפני הקליק + אימות-תווית דטרמיניסטי). **הקוד המוצרי לא נגע** (זהה לקומיט `6460fc4` הירוק). אומת: `verify` ירוק, סוויטה 8/8, הטסט 3/3 קר. סביבת-הבדיקה הושארה נטו-אפס | מודול 1 בלבד (ישי) — משטח E2E; ה-CI לא מושפע (לא מריץ E2E). לא נוגע במודולים/טבלאות של עמית |
| 08/07/2026 17:16 | ישי (Claude) | ‏(א) הושלמו שאריות הסשן המקביל שנעצר (אזל-שימוש): תיקון מיקום שורת-🗡️ ב-amit/09 §② + אימות 6/6 מדריכים; **קומיט מרוכז `9cccf47` נדחף לענף** (31 קבצים — שלושת גלי-היום; הוחרג: ‏docx בשם עברי ב-mockups, מפר כלל 11 — ממתין לשינוי-שם). ‏(ב) **CLAUDE.md כלל 2 — נוהל-תוכניות**: ‏write-back להערות-על-תוכנית · בקר-תוכנית (סוכן-ביקורת טרי) · רישום תוכנית-פעילה ב-STATUS. ‏(ג) **כלל-המודלים** עודכן: ‏Opus/Fable לחשוב (המלצת-בחירה, לא מנגנון). ‏(ד) **claude_routines.md**: חותמות-🕓 לכל רוטינה (עודכנה/סונכרנה — אומתו mtime מול העותקים החיים) + שלב "✅ אומת-סנכרון" ל-docs-sync (מטביעה על קבצי-הליבה רק בריצה ירוקה; קנוני+עותק-חי עודכנו יחד באותו סשן). ‏(ה) ‏backlog: רוטינת דייג'סט אישי אחרי-pull (אחרי אונבורדינג עמית); דחיפה-אוטומטית-ל-dev נדחתה (כלל 10) | 📣 **עמית**: אחרי ה-merge — לסנכרן את עותקי-ה-SKILL שלו מהקנוני (כולל שלב-החותמת החדש ב-docs-sync); נוהל-התוכניות חל גם על הסשנים שלך |
| 08/07/2026 12:54 | ישי (Claude) | **📣 עמית — גל-המשך מתואם (תיעוד+hook-באנר; רץ אחרי סיום סשן-אודיט-ה-DB, כותב-יחיד).** ‏(א) **באנר-הפתיחה מזהה-מכונה** (`session-start-context.sh`): לפי `git config user.name` — "🫵 המסלול שלך" ראשון (ishay/amit; לא-חד-משמעי ⇒ ניטרלי); זיהוי=תצוגה בלבד, הכרעות="אני X" (כלל 1). אומת ב-3 תרחישים. ‏(ב) **קונבנציית מודול-משותף בטמפלט-הפתיחה** (למודולים 6/12): שתי שורות Active-step + עמודת Owner (ישי/עמית/יחד) + איסור בניית-צעד-של-השני בלי העברת-תפקיד מפורשת. ‏(ג) **סריקת-פזה**: כלל (h) בסעיף-8 של הטמפלט + שורה בפרומפט-ההמשך ⑥2 ב-**11 המדריכים** — בכניסה לפזה אוספים את כל הפתוחים המעוגנים אליה ומכריעים במרוכז (P13) לפני הצעד הראשון. ‏(ד) **P20 סנכרון-טרום-merge** נוסף לספרייה (המשלים של P7) + ‏README יושר ל-P1–P20 + שורת-באנר ב-04c ⑪ + עדכון זיכרון-הסשנים הפרסיסטנטי של Claude | **📣 עמית** — הטמפלט ופרומפטי-ההמשך שתעבוד איתם השתנו (סריקת-פזה, מודול-משותף); ‏P20 רלוונטי לפני כל merge משותף; הכול מגיע אליך ב-merge כולל settings/באנר |
| 08/07/2026 12:29 | ישי (Claude) | **אודיט-ערעור DB מבני + מפת-DB מרוכזת (תיעוד+hook בלבד; אפס קוד-אפליקציה, אפס מיגרציות).** ‏(א) **§7 הורחב ב-10 פריטים (61–70)** מאודיט-תקיפה בהוראת ישי ("לערער על הטבלאות ועל הרפרנס, בעירבון מוגבל"; קריאה מלאה של C5+C6 + סוכן-ערעור עצמאי + MCP חי): ‏61 ‏Storage מאוחד · 62 ‏NOT NULL · **63 בעלות-עמודות מול RLS שורתי** · **64 מדיניות-מפתחות (טבעי/surrogate) 🔴 לפני מ3** · 65 ייחודיות-email · 66 שכר-מינימום · **67 שיוך-שיבוץ-למשמרת** · **68 דוח-שכר-כמסמך** · 69 "+נסיעות" · 70 ‏params-מטופס; ‏+8 רוכבי-↳ לפריטים 12/17/23/25/42/49/50/52. ‏(ב) **`docs/db_roadmap.md` חדש** — התצוגה המאוחדת של כל שינויי-הסכמה העתידיים (ודאי/דיון/היגיינה/Storage/אינדקס-פר-טבלה/מטריצת-RLS/פגמי-רפרנס/פרוטוקול-תחזוקה), מחווט מ-CLAUDE.md+README+מצביעי-התיקיות+שני היומנים. ‏(ג) **שני הטמפלטים חוסנו:** בלוק 🗡️ ‏DB Design Challenge (כולל כלל "לכל מספר-מסך עמודת-מקור") + צ'קליסט-מיגרציה + ‏advisors-בסגירה + כללי (f)/(g) בעדכון-העצמי. ‏(ד) **Stop hook — אכיפה 0ב:** מיגרציה בלי עדכון-db_roadmap = חסימת-סיום; + תיקון `-uall` (קובץ-ראשון-בתיקייה-חדשה חמק מהאכיפה) — נבדק בסנדבוקס, 4 תרחישים ✓. ‏(ה) **קונבנציית 📣** ליידוע המפתח השני (CLAUDE.md + טמפלטים + roadmap §10). ‏(ו) **ספרייה P17–P19** (אודיט-ערעור-DB · סקירת-סשן-אחר · המשכת-תוכנית-שנקטעה) + חיזוק P15. ‏(ז) שורות-🗡️ ב-② של 6 מדריכים + ‏module-2.md (חבילת-ההנהונים של צעד 1.1 → ‏4 הנהונים+שער-כיוון §7.63; שורות Ledger ל-64/65) | 📣 **עמית**: ‏(1) צעד 1.1 של מודול 2 מחכה עכשיו ל-4 הנהונים (§7.40א/48/62/23↳) + כיוון-§7.63 — יישאלו בשער ‏(2) לפני בלופרינט מודול 3: §7.64 חייב הכרעה ‏(3) קרא את `docs/db_roadmap.md` לפני כל מיגרציה |
| 08/07/2026 07:38 | ישי (Claude) | **גל-חיזוק להמשך-הדרך (תוכנית מאושרת A–I; תיעוד+hooks, אפס קוד-אפליקציה/DB).** ‏(א) **טמפלט-פתיחה** — 4 כללים חדשים: הצמדת-שאלה-לצעד (כל ⏳/nod-pending מעוגן לצעד שבו יתפוצץ) · שאלה-חדשה-תוך-בנייה = עצירה+סגנון-השאלות (לעולם לא בחירה שקטה) · הצלבת-בלופרינטים מול המדריך של המפתח השני · תג `⚠️ shared-surface` לצעדים שנוגעים בקבצים משותפים. ‏(ב) **ספרייה P13–P16**: סשן-הכרעות · המשכת-עבודת-השני · תקרית-מיגרציה fix-forward · מטא-פרומפט; ‏P5 קיבל רמז-keep-both; ‏README/STATUS ל-P1–P16. ‏(ג) **רשימות-② בכל 10 המדריכים** יושרו ל-§7.48–60 (ציטוטי-מספרים; ‏ishay/05 קיבל תג 🔗). ‏(ד) **CLAUDE.md**: שוחזרה כותרת "פרוטוקול DB" שאבדה + מפת-כללים (אפס רה-נומרציה) + עוגן-04c בכלל 16. ‏(ה) **אוטומציות**: שער-מיגרציות ב-`protect-frozen-files.sh` (עריכת ‎.sql מקומט = 🧱 חסום; טיוטה חופשית; גם פעלים-הרסניים ב-Bash) · 3 קובצי-מצביע `CLAUDE.md` (‏migrations/docs/src) · סקריפט באנר `session-start-context.sh` (החיבור ב-settings — ידני של ישי, snippet נמסר). ‏(ו) **תחזוקה**: היומן נדחס 292→256 לפי מדיניותו; ל-§7 נקבעה מדיניות-גודל (ארכוב-בסטאבים ב-~200 שורות/סגירת מ-6, המספור נשמר). ‏(ז) תוקן מיזוג-שורות נוסף ב-CHANGELOG (רשומת 22:20 בלעה 4 רשומות-ערב — פוצלו) | שני המפתחים — טמפלטים/ספרייה/מדריכים חכמים יותר מהמודול הבא; עמית מקבל הכול ב-merge |
| 07/07/2026 22:20 | ישי (Claude) | **שדרוג ספריית הפרומפטים P1–P12 + יישור הפניות כלל-13 (תיעוד בלבד).** ‏(א) **P12 חדש** — אודיט-סנכרון עמוק *אינטראקטיבי* בתוך סשן (מריץ את הגדרת regin-docs-sync מהקנוני; קונפליקט עמום = שאלה מיידית אליך במקום ledger ביומן). ‏(ב) **P8** — המלצת Plan-Mode + בלוק העברת-תפקיד-כותב מפורש. ‏(ג) **P9** — נוסף מדריך-המיקרו לרשימת-העדכון. ‏(ד) **P10** — אם ההכרעה מתקבלת בו-בסשן ⇒ הפעלת צ'קליסט כלל 13 מיד. ‏(ה) שתי הפניות "13(ד)" ישנות בטמפלט-הסגירה יושרו למספור החדש (13(ז)); README+STATUS מעודכנים ל-P12 | שני המפתחים — P12 הוא הדרך המומלצת להריץ סנכרון-עמוק כשאתה ליד המקלדת |
| 07/07/2026 22:12 | ישי (Claude) | **חיזוק שני הטמפלטים + סנכרון מדריך-המיקרו של מודול 2 (תיעוד בלבד).** ‏(א) **טמפלט-פתיחה:** כלל write-back — הכרעת-§7 שמתקבלת בפתיחה נרשמת גם ב-§7 עצמו באותו סשן (13(א)) · אחרי שמירת בלופרינט מעדכנים את שורת-STATUS · שכפול-תוכן-הכרעה בלדג'ר רק כמראה מתויגת 🔗. ‏(ב) **טמפלט-סגירה:** הוראות-ה-PR מפנות לרצף המחייב — `regin-docs-sync` לפני `regin-pr-gate`. ‏(ג) **module-2.md:** נוספו שני שערי-הנהון לצעד 1.1 — §7.40(א) ייחודיות + **§7.48 קידוד enable-RLS לכל 11 הטבלאות** (המיגרציה של 1.1 = הבית הטבעי אחרי ההנהון) · הערת §7.34 על צעד 3.4 (ארכוב-עם-התחייבויות = לא-מוגן במכוון ב-M2) · תג 🔗 על בלוק-ה-SQL של §7.21 | **עמית** — הבלופרינט של מודול 2 מעודכן; בצעד 1.1 לוודא שההנהונים התקבלו (או לשאול בשער) |
| 07/07/2026 22:00 | ישי (Claude) | **מדריך 04c הורחב — עבודה עם שתי שיחות במקביל + ה-hooks בשפה פשוטה (תיעוד בלבד).** סעיף ⑥ נכתב מחדש כמדריך מלא: טבלת מותר/אסור במקביל לשיחה כותבת (קריאה/תכנון ✅ · עריכה/רוטינות/dev-שני ❌) · באיזה מצב לפתוח שיחה שנייה (Plan Mode מומלץ; auto-accept לעולם לא) · המילים המדויקות (פתיחת קריאה-בלבד/P8, העברת תפקיד-כותב מפורשת) · מלכודות (git משותף, רוטינה=כותבת, worktree, "מסתבך→צ'אט אחד"). נוסף סעיף ⑪ חדש: הסבר פשוט של שני ה-hooks (שומר-הקפואים+פתק-פר-שיחה · שומר-היומנים מודע-לסשן) ומה הם **לא** אוכפים (CHANGELOG/ריפול-§7 → כלל 13+רוטינה); בדיקת-הקבלה (⑫ כעת) הורחבה בהתאם | עמית — רלוונטי גם לישי; יגיע ב-merge |
| 07/07/2026 21:52 | ישי (Claude) | **שדרוג `regin-docs-sync` לאודיט-סנכרון עמוק + קונבנציית-מראה 🔗 + שכתוב כלל-ברזל 13 (תיעוד+רוטינות בלבד).** ‏(א) הרוטינה שוכתבה (קנוני `docs/claude_routines.md` + ה-SKILL.md החי של ישי, דואל-אפדייט): קריאה מלאה של כל דוק + קוד/DB נושא-החלטות (כולל `products_and_params.md`), **אודיט מצב-החלטות §7/§6 דו-כיווני** (מוכרע-שלא-רופל ↔ פתוח-שכבר-יושם), עץ-הכרעה Class 1/2/3 (מכני=מתקנת · יש-מקור-הכרעה=מסנכרנת · עמום=שואלת/ledger), אנטי-רעש לסטיות-מתועדות, ובדיקת שלמות-טבלאות. ‏(ב) **CLAUDE.md כלל 13** שוכתב לצ'קליסט דו-כיווני (א–ז: §7 קודם · קוד/DB · grep גם על `micro_guides/` · roadmap · יומנים · כיוון-הפוך · רוטינה) + **קונבנציית מראה 🔗** (`מראת §7.N`; שכפול-בלי-תג = ממצא); תויגו 6 בלוקי-מראה קיימים במדריכים (amit/06/07/09, ishay/02/04, shared/06). ‏(ג) טמפלט-הסגירה קיבל צעד 0b — ריפול-§7 מפורש (13(ב)-(ג)). ‏(ד) **3 שורות-טבלה שבורות ב-CHANGELOG תוקנו** (מיזוגי-שורות מ-07/07: ‏16:37/03/07 · ‏16:09/15:45 · ‏13:52/12:55/10:47) — שוחזרו מ-git; כותרות 12:55/10:47 שוחזרו מהפניות-הצלבה (לא היו שלמות מעולם) | שני המפתחים — מעכשיו: שכפול-החלטה במדריך רק עם תג 🔗; ‏`regin-docs-sync` היא אודיט עמוק (להריץ לפני סגירת-מודול/מיזוג); עמית מסנכרן את ה-SKILL.md שלו מהקנוני |
| 07/07/2026 21:20 | ישי (Claude) | **ביקורת-אפיון אדוורסרית → §7.48–60 + תיקון Stop-hook (hooks בלבד, אין קוד-אפליקציה/DB).** ‏(א) ביקורת C5+C6 מול עצמם ומול הסכמה החיה בשש עדשות (3 סוכני-קריאה + אימות MCP): ‏**PROJECT_MASTER §7 הורחב ב-13 פריטים (48–60)** + חידודי ↳ ל-12 פריטים קיימים (15–17, 29, 32–34, 36, 41–42, 44–45); הבולטים — **§7.48** הפעלת ה-RLS על 11 הטבלאות העסקיות לא קיימת בשום מיגרציה (אומת חי שהיא כן פעילה — פער-שחזור בלבד), **§7.49–52** אטומיות-המרה/נעילת-DB-להצעה/מע"מ-snapshot/רווח-נשמר-או-נגזר לקראת מודול 3, **§7.44↳** guard-סטטוס לטריגר האטומי. ‏(ב) **`protect-frozen-files.sh` תוקן:** סימון-עריכה נרשם רק לנתיב בתוך עץ-הריפו — כתיבת קובץ-תוכנית (Plan Mode) או scratchpad כבר לא מסמנת סשן-קריאה ככותב וגורמת ללולאת-block (אומת ב-4 תרחישים + רגרסיית קובץ-קפוא) | שני המפתחים — לפני policies מודול 2: ‏§7.40(א)+§7.48 (שתי מיגרציות-הנהון); עמית (מודול 3): לקרוא §7.49–53 לפני הבלופרינט |
| 07/07/2026 20:37 | ישי (Claude) | **הכרעה 5/5 — אשכול סכמת-השיבוץ (§7.6/14/16/29/33/43/44/45), מודול 4 (תיעוד; השלכות-סכמה עתידיות).** מרחק אווירי בלי Maps API · כתובת+`lat`/`lng` · double-booking (שני "קצרים"+≥שעה) · שחרור-אוטומטי · `invite_token`+פקיעה-משולשת (ערוץ מייל/SMS דחוי) · היעדרות/איחור + **מדיניות ביטול פרויקט 0/50/100%/force-majeure** (`cancelled_at`+`cancellation_reason`) · **טריגר-DB אטומי** ל-`project_status` · `ready` נגזר-והפיך. **§7.47 חדש = רשם תוספות-הסכמה** (address/lat/lng · זמני-אירוע · cancelled_at/reason · invite_token · שדות-נוכחות) לביצוע במיגרציות מודול 4/8. ריפול: PROJECT_MASTER §7, ishay/02, amit/09, shared/06, ishay/05 | **ישי (מודול 4)** — האשכול מוכרע לבנייה; המיגרציות לפי §7.47. **האודיט הושלם: 5/5 הכרעות, 20 פתוחים** |
| 07/07/2026 19:04 | ישי (Claude) | **הכרעות אודיט — §7.5 ניווט + §7.12 PDF + §7.38 חשבונית (תיעוד; תחום מודול 3/8 של עמית).** ‏§7.5 סיידבר שטוח של 9 (4-קבוצות=תצוגה בהרשאות). §7.12 = **הפקת-PDF בזמן-אמת בלי אחסון** (`pdf_url` deprecated; ההצעה המוקפאת=SSOT). §7.38 = **העלאה-ידנית-ושליחה** של חשבונית (המערכת לא מפיקה; דפוס מודול-2). דרישת-בנייה: יצירת פרויקט קוראת שורות קפואות דרך `quote_id`. ריפול: PROJECT_MASTER §7, amit/07, amit/09, shared/module_06 | **עמית** — §7.12/38 מוכרעים לבנייה; טמפלט-PDF של ישי יגיע ל-`mockups/quote-screen/` |
| 07/07/2026 18:40 | ישי (Claude) | **הכרעות אודיט — §7.4 + חבילת הכסף (תיעוד בלבד; הכרעות בעלות השלכות-סכמה עתידיות במודול 3).** ‏**§7.4** ערכי מטריצה=האפיון (as-built), קיבוץ=תצוגה. **חבילת הכסף §7.1/25/26/27/28/42:** מע"מ **18%** · עיגול אגורות-בחישוב/שקל-בתצוגה · הנחות בחיבור, תקרה ≤100%, **CHECK ב-DB** · מדרגות tie-break-בקוד (בלי אילוץ-DB) · **`closing_unit_cost` חדש ל-`quote_services`** (מיגרציה במודול 3) · פקיעת-הצעה/"אירוע הסתיים" = **`pg_cron`** (בר-הפעלה כבר במודול 3; ‏pg_cron זמין-לא-מותקן — אומת ב-MCP). ריפול: ‏PROJECT_MASTER §7, amit/07, ishay/04, ishay/05 | **עמית (מודול 3)** — כל חבילת הכסף כבר מוכרעת; ליישם לפי §7, בלי לשאול שוב. **§7.12 PDF עדיין פתוח** |
| 07/07/2026 18:06 | ישי (Claude) | **אודיט פריטים פתוחים (תיעוד בלבד — אין קוד/DB).** סריקה שיטתית בשלושה מעברים (רישומים קיימים · grep קוד/מסמכים · קריאה מלאה של C5+C6 מול הסכמה בשמונה ממדים + מעבר מנגנוני-ביצוע). תוצאות: ‏(1) **PROJECT_MASTER §7 הורחב ב-22 פריטים חדשים 25–46** (כולם פתוחים; ‏40–41="להנהון") — כסף/עיגול/הנחות, double-booking, snapshot עלות, ייחודיות מפתחות שה-RLS תלוי בהם, קסקדת ביטול, מנגנון מעברי-זמן, נעילה-מול-סטטוס, קישור-זימון בלי token, ועוד. ‏(2) שני חובות תויקו ב-§6 בהכרעת ישי: העדפות התראות (טבלה+UI→מודול 9, שליחה→מודול 10) ובדיקות (cross-browser→לפני מודול 5; UAT→מודול 12 — יושרה התנגשות סימון אבן-דרך-M5/מודול-5 ב-module-1/2.md). ‏(3) שורת initplan מיושנת באזור-הרפרנס של ה-LOG עודכנה ל-✅. ‏(4) advisors אומתו מול קבלות module-1.md §4 — אין ממצא חדש; מיגרציות⇄snapshot מסונכרנים. ‏(5) תזכורת STATUS: 5 ענפי `agents/*`+worktrees ישנים לניקוי אחרי ה-merge | שני המפתחים — לפני policies מודול 2: להנהן על §7.40(א) (ייחודיות roles/modules); לפני מודול 3: חבילת הכסף §7.1+25–28 |
| 07/07/2026 16:09 | ישי (Claude) | **⬆️ עדכון-patch ל-3 חבילות + triage של Supabase advisors (בלי DB, בלי שינוי-לוגיקה).** `@supabase/supabase-js` 2.110.0→2.110.1, `radix-ui` 1.6.1→1.6.2, `vitest` 4.1.9→4.1.10 (`package.json`+`package-lock.json`); lint נקי, 16/16 Vitest ירוקים, 0 פגיעויות. ממצאי ה-advisors מוינו ותועדו במדריך מיקרו מודול 1 §4 "Advisor acceptances": SECURITY DEFINER 0028/0029 = מקובל-בכוונה; קבלת trade-off של lockout-DoS (§3, ישי); leaked-password = חסום-בתוכנית (Pro); perf-WARNs נדחים | שני המפתחים — למשוך את `package-lock.json` המעודכן ב-merge הבא; אין שינוי התנהגותי |
| 07/07/2026 15:45 | ישי (Claude) | **📚 ספריית פרומפטים מצבית חדשה (`docs/guides/prompt_library.md`) + סגירת פערי כיסוי (תיעוד בלבד).** אחרי סקירת כל 21 קבצי המדריכים: המבנה (8 סעיפים, 3 פרומפטי-מודול ב-⑥, 🧩 בצעדי 👤) נשאר כמות שהוא. נוסף: ספרייה של 11 פרומפטים P1–P11 לפי סיטואציה (3 עברו מ-README + 8 חדשים: סטייה-מהתוכנית P4, merge-conflict P5, ‏undo P6, רגרסיה-אחרי-משיכה P7, קריאה-בלבד-מקבילה P8, "עשיתי ידנית" P9, שאלת-§7 P10, "הסבר לי" P11), כולם נפתחים ב"אני [ישי/עמית]". מצביעים עודכנו: README (נשאר P1+קישור), 11 מדריכי-מודול ⑧, ‏04c, ‏roadmap. פערים שנסגרו: טמפלט-הסגירה בודק עכשיו טריגרי-עדכון-רוטינות (claude_routines §4) · CLAUDE.md כלל 1 — שאל-אם-זהות-הדובר-חשובה. הוכרע: אין פרומפט עדכון-רוטינות/טסטים פר-מדריך (מונחה-אירוע/מכוסה בכלל 14) | שני המפתחים — מהיום: סיטואציה מיוחדת? פותחים את הספרייה, לא ממציאים פרומפט |
| 07/07/2026 14:32 | ישי (Claude) | **סקירת 4 הרוטינות — תיקוני דריפט + הקשחות (דו-צדדי: קנוני + SKILL.md חי).** אימות: הפניות `npm run` תואמות ל-package.json; health-pulse/e2e-check תקינות. תוקן: ‏(A) docs-sync בודקת עכשיו גם `README.md` · (D1) הערת `.mcp.json` מגורסן · (D2) דריפט-SKILL.md מפנה לפרוטוקול §4 · (C) pr-gate — הערה ש-`git add -A` מקמט את כל העץ (מניח כלל 16) + דיווח רשימת-הקבצים שקומטו · (B) §3 — תוקן הסבר בדיקת-הקבלה המיושן (ה-hook מדלג על health-pulse) · (D3) §3 — הערת באג התצוגה של `list_scheduled_tasks`. שינויי-פרומפט הוחלו גם בקבצים החיים של ישי | אמית — יקבל את הקנוני עם ה-merge; יסנכרן את החי שלו |
| 07/07/2026 14:15 | ישי (Claude) | **סנכרון תיאור ה-hook ברוטינת `regin-pr-gate` (יישום ראשון של הפרוטוקול הדו-צדדי).** תיקון ה-Stop hook (12:55) הפך את התיאור בפרומפט של pr-gate למיושן ("stale relative to other changed files"). עודכן ל**מודע-לסשן** — גם בקנוני `docs/claude_routines.md` **וגם** בקובץ החי `~/.claude/scheduled-tasks/regin-pr-gate/SKILL.md` במחשב של ישי (הצד של ישי לא דורף). הצד של אמית יתעדכן דרך ה-merge + סנכרון מקומי | — |
| 07/07/2026 13:52 | ישי (Claude) | **הבהרת רוטינות (תיעוד בלבד): מפת "מתי מריצים מה" + פרוטוקול עדכון דו-צדדי.** `docs/claude_routines.md`: ‏(1) נוספה בסעיף 1 מפה "🗺️ מתי מריצים מה" — טבלה לפי-רגע + **רצף לפני PR מחייב** (docs-sync → e2e → pr-gate, כי pr-gate עושה `git add -A` ומקמט הכל, אז docs-sync חייב לפניו). ‏(2) סעיף 4 עודכן: הקובץ הקנוני **קבוע** (גיבוי/שחזור/מול-דריפט — לא נמחק אחרי אונבורדינג), ופרוטוקול עדכון דו-צדדי — בסשן של ישי, Claude מעדכן גם את ה-SKILL.md החיים במחשב של ישי וגם את הקנוני; אמית מסנכרן את שלו. `amit/05` §④ מצביע למפה. **הוכרע: לא מאחדים רוטינות** (פיצול מוצדק — עלות/תזמון/בטיחות) | שני המפתחים — הרצף לפני PR מחייב מעכשיו; אמית מקבל עם ה-merge |
| 07/07/2026 12:55 | ישי (Claude) | **ה-Stop hook הפך מודע-לסשן — שיחת קריאה-בלבד לא נחסמת ליד סשן כותב (hooks בלבד, אין קוד אפליקציה/DB/settings.json).** הבעיה: `check-docs-updated.sh` בדק `git status` גלובלי, אז סשן קריאה-בלבד נחסם שוב-ושוב כשסשן מקביל "מלכלך" את העץ (הלולאה מ-07/07). התיקון: `protect-frozen-files.sh` רושם "סימון עריכה" פר-session_id ב-`.git/regin-session-mutations/` (mtime=זמן העריכה האחרונה של הסשן; מחריג עריכות LOG/STATUS למניעת self-reference); `check-docs-updated.sh` מדלג לגמרי על סשן בלי סימון (קריאה-בלבד), ומשווה אכיפת-תיעוד מול זמן-העריכה-של-הסשן במקום מול העץ הגלובלי. הודעת ה-block תויגה "🤖 בדיקה אוטומטית". גם: CLAUDE.md כלל 16 (הבהרת מודעות-לסשן) + הערת `Get-Date` (PowerShell) מול `date` (Bash) בעובדות-הסביבה. אומת ב-6 בדיקות (כולל self-reference). מגבלה: אכיפת מדריך-מיקרו עדיין גלובלית (נדיר) | שני המפתחים — מעכשיו אפשר לפתוח שיחת קריאה-בלבד ליד שיחה כותבת בלי לולאת חסימה |
| 07/07/2026 10:47 | ישי (Claude) | **כללי ברזל 16–17 (מקביליות + פרומפט-מסירה 🧩) + מדריך 04c + עדכוני טמפלטים.** ‏(1) **CLAUDE.md — 2 כללי ברזל חדשים:** כלל 16 (מקביליות: שיחה כותבת אחת בכל רגע; מקביל = קריאה/תכנון בלבד — ה-Stop hook מבוסס-mtime על יומנים משותפים) + כלל 17 (פרומפט מסירה: כל צעד 👤 מקבל הוראות + בלוק 🧩 מוכן להדבקה בתוסף הכרום). גם: הבהרת כלל 2 (Plan Mode בפתיחה/סגירה/§7 — לא בבנייה שוטפת), שורת Supabase MCP בעובדות הסביבה, ופורמט תאריך חדש `DD/MM/YYYY HH:MM` ליומנים (השורה הזו — הראשונה בפורמט). ‏(2) **בלוקי 🧩 נוספו** ליד צעדים ידניים ב-ishay/01 (PR+merge), amit/02/04, roadmap §5, פרוטוקול ה-DB; פרומפטי הסגירה (⑥3) בכל 11 מדריכי המודול דורשים עכשיו הדפסת 🧩. ‏(3) **מדריך חדש `amit/04c_working_with_claude.md`** — פרומפטים, חיסכון בטוקנים, מצבי עבודה, "איזה כלי למה" (Code/כרום/Cowork), קיצורי מקלדת. ‏(4) **module-1.md הושלם לטמפלט הנוכחי** (טבלת יכולות, טבלת מודל+מאמץ, ראיות as-run) — מוכן להרצה חוזרת של פרומפט הסגירה; פרומפט ⑥ ב-ishay/01 רוענן בהתאם. ‏(5) claude_routines.md — סעיף מקביליות; שני הטמפלטים — תאריך+שעה + חובת 🧩 | שני המפתחים — כללי 16–17 חלים מעכשיו; ישי יכול להדביק את פרומפט הסגירה המעודכן (ishay/01 ⑥) |
| 07/07 | ישי (Claude) | **סגירת "החלק של Claude" בשלב ishay/01 + הקשחת ה-E2E לרשת איטית.** ריצת E2E ראשונה נפלה 2/8 (וגם ברי-ראן): אבחון מול לוגי Supabase הוכיח שהשרת ענה 200 מהר לכל בקשה — הכשל בצד הבדיקות: ‏(א) בדיקת המטריצה עשתה `reload()` לפני שה-PATCH חזר (ה-preflight נרשם בשרת, ה-PATCH בוטל באוויר) ‏(ב) זרימות login/שגיאה = עד 8 קריאות-רשת עוקבות שחרגו מ-10s ברשת איטית (רשת שונה מאתמול). **תוקן:** ‏`clickCellAndAwaitWrite` ב-`permissions.spec.js` (המתנה מפורשת לתשובת ה-PATCH — גם בקליקי השחזור, כדי לא להשאיר side-effect ב-DB המשותף) · ‏timeout ‏30s לזרימות login/שגיאה · ‏test-timeout גלובלי ‏60s ב-`playwright.config.js`. אחרי ההקשחה: **E2E ‏8/8 + ‏`npm run verify` ירוקים**. אימות DB חי (MCP): ‏`modules`=9, `permissions`=45, `roles`=5 ✓. ⚠️ הערת היסטוריה: קבצי ה-E2E נסחפו לקומיט ה-docs ‏`ec408d4` (‏`git add -A` של סשן מקביל שרץ באותו זמן) — התוכן שנבדק ירוק = בדיוק התוכן שקומט; מתועד כאן במקום amend על קומיט שכבר נדחף | ישי — הכול קומט ונדחף; נותר רק לפתוח PR (‏base: `dev` ← ‏compare: `ishay/module-1-permissions`) ולמזג |
| 07/07 | ישי (Claude) | **טמפלט הסגירה — הוראות ה-PR המודפסות תוקנו (פער שישי חשף):** האודיט לא דוחף קוד, אך ההוראות הניחו ענף דחוף. נוסף שלב 0 — push דרך רוטינת `regin-pr-gate` (Run now) או קלוד-בסשן; ושלב אחרי-merge — הרצת `regin-docs-sync` (כלל 13(ד)). ‏verify+E2E לא נוספו כצעדי-אדם — האודיט כבר מריץ אותם | כל סגירת מודול מעכשיו (חל אוטומטית — הפרומפטים מפנים לטמפלט) |
| 07/07 | ישי (Claude) | **מדריך אונבורדינג חדש לעמית — כיוונון Claude Code חזק (`docs/guides/amit/04b_claude_code_power_setup.md`).** תיעוד בלבד (אין קוד/DB). מסביר לעמית אילו MCP/סקילים/פלאגינים/קונקטורים להתקין ומה **לא**, כדי שה-Claude שלו יהיה ממוקד לסטאק. עוגן במציאות מאומתת: **`.mcp.json` כבר מגורסן בשורש** ומספק Supabase MCP מתארח (HTTP+OAuth, בלי סוד בקוד) — עמית רק מאשר+מתחבר בהרצה ראשונה; פלאגינים דרך `/plugin` (לאמת שמות בתוך המוצר); קונקטורים לא רלוונטיים ל-CLI; רשימת "רעש לנתק" (Booking/Gmail/Calendar…). נכנס בין 04 ל-05 בלי לשנות שמות קבצים; `00_track.md` + תנאי-הכניסה של 05 עודכנו | עמית — לבצע את 04b אחרי שלב 04 ולפני הרוטינות (05); לאחר מכן ה-Supabase MCP מחובר וה-advisors ברוטינות ירוצו |
| 07/07 | ישי | **2 משתמשים אמיתיים נוספו למערכת (דאטה, לא סכמה):** עמית מילר (`amitmillertati@gmail.com`) וטל רודגולד (`talrodgold@gmail.com`), שניהם בתפקיד **מנכ"ל**, `status='active'`. לעמית נוצר חשבון Auth ‏07/07 (התחברות ראשונה אומתה בפועל); לטל חשבון Auth קיים מ-03/07 — **מעולם לא התחבר**, הסיסמה היא זו שהוגדרה ב-03/07 (אם אינה ידועה — מחיקה ויצירה מחדש ב-Authentication→Users). סיסמאות אצל ישי בלבד, לא ב-repo | שני המפתחים — לעמית יש עכשיו כניסה אמיתית למערכת (הרשאות מנכ"ל) לקראת מודול 2 |
| 07/07 | ישי (Claude) | **תיקון UI בסייד-בר (מודול 1, ליטוש אחרי-אודיט):** פריטי הניווט היו ממורכזים (`justify-center`) — הוחלף ליישור-התחלה (ימין ב-RTL) במצב מורחב, כך שכל האייקונים על קו אנכי אחיד כמקובל; מרכוז נשאר רק במצב מכווץ. `Sidebar.jsx` בלבד, 3 קישורים. אומת חי (login + צילומי מורחב/מכווץ) + lint נקי. **לא בקומיט עדיין** — נכנס ל-PR של מודול 1 כ-commit נפרד. **תיקון הנחיה (07/07):** גם קבצי ה-docs של פתיחת מודול 2 נכנסים לאותו PR כ-commit שני — עמית על מחשב אחר; docs שלא עוברים דרך `dev` לא יגיעו אליו (הוראות מדויקות: ishay/01 ④3) | ישי — לפני ה-PR: שני קומיטים לפי ishay/01 ④3 |
| 07/07 | ישי (Claude) | **מודול 2 (לקוחות) — פתיחה: בלופרינט נוצר + 2 שאלות §7 נסגרו + שדרוג טמפלטים.** ‏(1) `docs/micro_guides/module-2.md` נוצר לפי טמפלט הפתיחה (ישי הכריע במקום עמית; עבר סבב אימות אדוורסרי — 18 ממצאים תוקנו). כולל SQL מלא ל-RLS הראשון על טבלה עסקית (תבנית §7.21, ‏`module_name='לקוחות'`) + טבלת 14 תרחישי הבדיקה שנדחו ממודול 1. ‏(2) **הוכרעו:** §7.3 (תוויות `customer_type` לפי האפיון) + §7.11 (בלי מיזוג; ח"פ קנוני; חיפוש סלחני; חסימת כפילות + הצעת שחזור מארכיון) — ר' PROJECT_MASTER. ‏(3) הכרעות נוספות: שליחת שיווק = Storage ציבורי+mailto (אמיתי במודול 10) · כרטיס לקוח מלא עם empty states. ‏(4) **שני הטמפלטים שודרגו** (משוב ישי): תקציר יכולות בעברית, הצהרת השתלבות, רישום חובות חוצה-מודולים ב-PROJECT_MASTER §6 (+חובת קריאה בפתיחה), סגנון שאלות הכרעה, טבלת מודל+מאמץ. **בלי שינוי קוד/DB** — המיגרציה תיכתב בצעד 1.1 של המדריך | עמית — אחרי merge של מודול 1: התחל ישר מפרומפט 2 (המשך-בנייה) במדריך amit/06 ⑥; **אל תריץ שוב את פרומפט הפתיחה** |
| 06/07 | ישי (Claude) | **מודול 1 נסגר — verdict [YES] — ממתין ל-PR+merge של ישי.** אודיט הסגירה: `npm run verify` ירוק · `npm run test:e2e` ‏8/8 · אימות DB חי מול MCP (‏roles=5, modules=9, permissions=45, ‏7 policies, ‏4 פונקציות SECURITY DEFINER) · gitleaks על כל 39 הקומיטים בהיסטוריה — **אין ממצאים** (ה-CI צפוי ירוק). **§7.24 הוכרע:** סיסמאות משתמשי הבדיקה שנחשפו בהיסטוריה **לא מוחלפות** — סיכון מקובל ומתועד (פרויקט אקדמי). תיקון הערת snapshot ב-`docs/schema.sql` ‏(10→9 מודולים). ⚠️ **המיזוג יבוצע ע"י ישי לבד בלי סקירת עמית** — עמית עדיין בשלב התקנת הכלים (amit/01), לפי הכלל במדריך ishay/01 ④5 | עמית — אחרי ה-merge: `dev` מוכן, מודול 2 יכול להתחיל עם תבנית ה-RLS מ-§7.21 |
| 06/07 | ישי (Claude) | **מערכת מדריכי-מיקרו machine-first (שדרוג כל מערך התיעוד-לעבודה).** ‏(1) שני הטמפלטים ב-`docs/templates/` נכתבו מחדש: התוצר (מדריך מיקרו) כתוב **לקלוד** — אנגלית מלאה, 9 סעיפים, כותרת-מצב חיה, נקודות עצירה מתויגות 🤖/👤 (קלוד מאמת טכני לבד; ישי/עמית מאשרים בסופי פזות/הכרעות §7/מיגרציות), פרוטוקול עדכון-עצמי מחייב. ‏(2) `docs/micro_guides/module-1.md` נדרס ונכתב מחדש כ-as-built באנגלית — הפיילוט של הפורמט (כולל תיקון נספח ה-QA שהתיישן). ‏(3) כל 11 מדריכי המודול: סעיף ⑥ הפך ל-**3 פרומפטים** (פתיחה/המשך-בנייה/סגירה). ‏(4) CLAUDE.md: כלל ברזל 15 (מדריך מיקרו חי) + צעד 0 בפרוטוקול סוף-סשן. ‏(5) ה-Stop hook הורחב: קוד מודול שהשתנה בלי עדכון מדריך-המיקרו שלו חוסם סיום סשן (נבדק ב-3 תרחישים). ‏(6) roadmap §5/§8, README, claude_routines.md + עותקי SKILL.md עודכנו בהתאם | שני המפתחים — מעכשיו כל מודול נפתח/נבנה/נסגר עם 3 הפרומפטים שבמדריך השלב שלו; אין יותר "פרומפט אחד" |
| 06/07 | ישי (Claude) | **סגירת פינות בתשתית התיעוד וההגנות (לא קוד אפליקציה).** ‏(1) **אבטחה:** בלוק פרטי ההתחברות הוסר מהקובץ הזה (הסיסמאות מוחלפות ב-Supabase; פרטים מישי בערוץ פרטי). ‏(2) **hooks עברו לסקריפטים** ב-`.claude/hooks/` (`check-docs-updated.sh` + חדש: `protect-frozen-files.sh` שחוסם עריכה/מחיקה של C5/C6 הקפואים) — `settings.json` רק מפנה אליהם. ‏(3) **CLAUDE.md:** 2 כללי ברזל חדשים — פרוטוקול שינוי מהותי (כלל 13), SSOT ללוגיקה עסקית + `api.js` פר-מודול (כלל 14). ‏(4) **PROJECT_MASTER:** תוכן עניינים, סטטוס הועבר ל-STATUS בלבד, §7.22–7.23 חדשים, עדכון שורות מיושנות. ‏(5) **מדריכים:** פרומפטי ⑥ פושטו (Claude קורא את הטמפלט בעצמו — בלי הדבקת תוכן ידנית); ishay/01 עודכן למצב אמת. ‏(6) `mockup_descriptions.md` נוקה מעותק-אב ישן וסותר — עכשיו תיאורי מסכים בלבד. ‏(7) 4 קבצי פלטי-סוכנים נמחקו מ-`reference_spec/` | עמית — הכללים החדשים ב-CLAUDE.md וה-hooks יגיעו אליך אוטומטית ב-clone; שום דבר לא חוסם אותך |
| 06/07 | ישי (Claude) | **מודול 1 — סגירה: הכרעת §7.21 (מודל RLS) + תיקוני בקרת קוד P0/P1.** §7.21: הרשאה לכל טבלה עסקית עתידית נקבעת אך ורק לפי מטריצת role→module (בלי בעלות ברמת-רשומה) — תבנית ה-policy המדויקת נכתבה ב-`PROJECT_MASTER.md` §7.21, מוכנה לשכפול במודול 2. תיקונים: `package.json` (`engines: node>=20`), `.env.example` (הערת E2E אופציונלי), `src/components/ErrorBoundary.jsx` חדש (עוטף את `App.jsx` — קריסת רינדור לא-מטופלת כבר לא = מסך לבן), `index.html` (`lang="he" dir="rtl"`, היה `lang="en"` בלי dir), הערת load-bearing ב-migration של `current_user_role_id()`, ו-2 בדיקות E2E חדשות ב-`e2e/permissions.spec.js` (שינוי-תא מלא + הגנת self-lockout על עמודת המנכ"ל). `npm run verify` ו-`npm run test:e2e` (8/8) ירוקים. **בדיקה ידנית שנמצאה כבר ממומשת:** שינוי סיסמה ב-`ProfileSettingsPage.jsx` כבר מחובר ל-`supabase.auth.updateUser` — ממצא קודם בבקרת הקוד היה מיושן | מודול 2 (עמית) — תבנית ה-RLS מוכנה לשכפול על `customers`; שאר המפתחים - ה-Error Boundary וה-RTL הגלובלי חלים על כל מסך עתידי |
| 06/07 | ישי (Claude) | **⚠️ עמית, שים לב — מפת הקבצים בתיעוד השתנתה! נוצרה ערכת מדריכים מלאה** בהשראת ערכת "מלאי 710": `CLAUDE.md`+`STATUS.md`+`README.md` חדשים בשורש (**קרא אותם ראשונים**), `docs/guides/` (מפת דרכים + מסלול אישי לך עם 01_install_tools.md כשלב ראשון, מסלול לישי, ומודולים משותפים), `docs/claude_routines.md` (איך ליצור את 4 הרוטינות בחשבון Claude *שלך* — הן פר-מחשב, `git clone` לא מביא אותן), `docs/code_review_2026-07.md`. **נמחקו** (תוכן מוזג לערכה החדשה): `docs/macro-guide.md`, `docs/WORKFLOW.md`, `docs/README.md`, `CLAUDE_CODE_LOG.md` בשורש. `PROJECT_MASTER.md` §7 קיבל 11 שאלות פתוחות נוספות (11–21). ה-Stop hook עבר מ-`.claude/settings.local.json` (אישי) ל-`.claude/settings.json` (משותף, ב-git — תגיע אליך אוטומטית ב-clone) | עמית — עדכן bookmarks; תתחיל מ-`docs/guides/amit/01_install_tools.md` |
| 04/07 | ישי (Claude) | **ספרינט הקשחה — תשתית פרויקט (לא פיצ'ר-מודול).** שער-איכות `npm run verify` (lint+format+test+build); Prettier + ESLint נקי; Husky pre-commit→lint-staged; **Vitest** (16 בדיקות: `validators`/`permissions`/`ProtectedRoute`) + `isAllowed()` חולץ ל-`src/lib/permissions.js`; **CI** `.github/workflows/ci.yml` (על PR: lint+test+build+gitleaks); **מיגרציות מגורסנות** ב-`supabase/migrations/` (baseline + 6 מיגרציות שנמשכו מה-remote — **ללא שינוי סכימה בפועל**); `vercel.json`+`.env.example` (מוכן, לא-מופעל). ראה `docs/architecture_and_qa_roadmap.md` (סטנדרט+DoD) ו-`supabase/README.md` | שני המפתחים — מכאן: כל PR עובר `npm run verify`; שינויי DB = קובץ מיגרציה חדש (לא עריכת schema.sql); בדיקות `*.test.js` לצד לוגיקה חדשה |
| 04/07 | ישי (Claude) | **⚠️ שינוי שמות-קבצים בתיעוד — עמית, שים לב ללינקים ישנים ששמרת!** תוכן RTL לא נגע, רק שמות: `REG-IN_מדריך_מאקרו.md`→`docs/macro-guide.md`, `REG-IN_מדריך_לעמית_מאוחד.md`→`docs/amit-onboarding.md`, `micro_guides/REG-IN_מדריך_מיקרו_מודול_1.md`→`micro_guides/module-1.md`. `docs/mokap/`→`docs/mockups/` (11 תת-תיקיות + 44 תמונות, שמות אנגליים). כל הקישורים הפנימיים תוקנו | עמית — לעדכן bookmarks/קיצורי-דרך אם יש; מדריכי-מיקרו עתידיים ייקראו `module-2.md` וכו' (לא שם עברי) |
| 04/07 | ישי (Claude) | **E2E אמיתי (Playwright) הוקדם ממודול 12 — לזרימות מודול 1 (Auth/RBAC) בלבד.** `@playwright/test` + Chromium; `playwright.config.js`; `e2e/auth.spec.js`+`e2e/permissions.spec.js` (6 בדיקות, ירוקות). `npm run test:e2e` — **לא** חלק מ-`verify`/CI (איטי במכוון, רוטינה נפרדת `regin-e2e-check`). ⚠️ **דורש `.env.local` מקומי** עם `E2E_CEO_EMAIL/PASSWORD`+`E2E_STAFF_EMAIL/PASSWORD` (ראו `.env.example`) — בלי זה הבדיקות מדלגות על עצמן (לא נכשלות). `regin-pr-gate` שודרגה: verify ירוק על ענף פיצ'ר → commit+push אוטומטי (לעולם לא main/dev) | עמית — אם תרצה להריץ E2E מקומית, תצטרך להוסיף את אותם 4 משתני-סביבה ל-`.env.local` שלך |
| 03/07 | ישי (Claude) | **מודול 1 — באטצ' 2: Sidebar שטוח + admin permission-driven + session hardening.** `Sidebar`: "ניהול מערכת" עבר מ-accordion לקישור **שטוח** (מנווט ל-`/system/users`; ניווט-משנה דרך הטאבים בתוכן). **Topic A**: גישת `/system/*` עברה מ-`role==='מנכ"ל'` קשיח ל-**permission-driven** (`SYSTEM_MODULES` ב-`constants.js`, נאכף ב-`Sidebar`+`App.jsx` ProtectedRoute) — פתר את ה-split-brain. **Topic B**: `supabaseClient` → `sessionStorage` (ניתוק בסגירת לשונית/דפדפן). תיעוד: playbook חירום לשחרור נעילה; החלטות Seed של products/params ננעלו (Topic D, בראש `products_and_params.md`) | מודול 2+ — אותה מוסכמת גישת-מערכת permission-driven; החלטות ה-Seed נעולות ל-Seed במודול 3 |
| 03/07 | ישי (Claude) | **מודול 1 — סגירה: Google Sign-In + נעילת חשבון + UX Sidebar + ביטול CAPTCHA.** `LoginPage`: כפתור **Google OAuth אמיתי** (`signInWithOAuth`) + **נעילת-חשבון** (RPCs `check_login_lock`/`register_failed_login`/`reset_login_attempts`) בזרימת הכניסה. `AuthContext`: **שער הרשאה מרכזי** לזרימת OAuth — חשבון גוגל בלי שורת `users` מנותק (`signOut`) עם `authError`. `Sidebar`: כפתור הכיווץ **הועבר מהתחתית לראש התפריט** (ליד הלוגו). תיעוד: CAPTCHA בוטל רשמית; 2 הטמפלטים עודכנו + `docs/WORKFLOW.md` חדש | מודול 2+ — מודל האבטחה/Auth יציב; אותה מוסכמת שער-הרשאה חלה על מסכים חדשים |
| 02/07 | ישי (Claude) | **מודול 1 — ליטוש Context/Hooks + הערות + ניקוי תיעוד.** `AuthContext`: `loadUser` ב-`useCallback`, Guard Clause ל-`useAuth` (זריקת שגיאה מחוץ ל-Provider), `mountedRef` נגד setState-אחרי-unmount. הערות עברית "why-first" בכל קבצי המודול; תוקן hooks-lint ב-`PermissionsMatrixPage` (useCallback) — המודול כולו lint-נקי. תיעוד: נמחק README boilerplate, `reference_spec/*.md.md`→`.md`+כותרת "קפוא", נוצר `docs/README.md`, מדריך-מאקרו הורחב ל-SSOT (§9–13) | ללא שינוי חוזה — מודול 2+ ממשיכים כרגיל |
| 02/07 | ישי | **מודול 1 — ביטול מסגור "מחיקה", מעבר לסטטוס דו-כיווני.** `UsersManagementPage`: הוסר כל מסגור של "מחיקה" (כפתור/אייקון פח/הודעת אישור) - הוחלף בכפתור מצב יחיד שמשתנה לפי הסטטוס הנוכחי ("השבת משתמש"/`UserX` כשפעיל, "הפעל משתמש מחדש"/`UserCheck` כשלא-פעיל). **הוסר הסינון שהחביא שורות `inactive`** - הטבלה מציגה עכשיו את כל המשתמשים, עם תג סטטוס אמיתי ("פעיל"/"לא פעיל") ושורה מעומעמת (`opacity-60`) למשתמשים לא-פעילים. שינוי סטטוס דו-כיווני לגמרי עכשיו - אין יותר "מסע חד-כיווני". אין שינוי RLS/DB - הפעולה כבר הייתה מותרת למנכ"ל (`users_write_ceo_only` לא מוגבל לעמודות) | מודול 2+ — אותה מוסכמה (סטטוס דו-כיווני, בלי מסגור "מחיקה") מחייבת גם במסכי לקוחות (מודול 2) ודיילות (מודול 4) כשייבנו |
| 02/07 | ישי | **מודול 1 — QA משוב: עריכת שם-עצמי + עריכת משתמש למנכ"ל.** `ProfileSettingsPage`: שדה "שם מלא" הפך לבר-עריכה (היה תצוגה בלבד; מותר גם ב-RLS, `users_update_self` מקפיא רק `role_id`/`status`). `UsersManagementPage`: נוסף כפתור "ערוך" (Pencil) לכל שורה, פותח את אותו Dialog בשני מצבים (הוספה/עריכה) — מנכ"ל יכול לעדכן שם/טלפון/**תפקיד** למשתמש קיים; דוא"ל לא ניתן לעריכה בטופס זה (מפתח RLS+FK). **מניעת self-lockout**: כשמנכ"ל עורך את עצמו, שדה התפקיד ננעל (`disabled`) — אותו עיקרון כמו נעילת עמודת המנכ"ל במטריצה ומניעת מחיקה עצמית | מודול 2+ (ללא שינוי מהשורה למטה) |
| 02/07 | ישי | **מודול 1 — צעד 10 + ליטושים** (השלמה מעל השורה מ-02/07 למטה): מסך **מטריצת הרשאות** (`PermissionsMatrixPage`, `/system/permissions` — 7 מודולים עסקיים מקובצים ל-4 קבוצות-על, עמודת מנכ"ל נעולה, Auto-Save). **הגדרות פרופיל** (`ProfileSettingsPage` — פרטים/אבטחה/התראות, נגיש לכל מחובר). **מחיקה רכה** למשתמשים (`status='inactive'`, אייקון פח אדום, שורות מסוננות מה-UI) — החליף את "הקפאה"; `frozen`→`inactive` עודכן ב-`LoginPage`+`MainLayout`. טוגלי התראות מנוטרלים (`disabled`+"בקרוב"). **הקשחה**: עדכוני `users` בפרונטנד עברו ל-`.select()` כדי לא להציג הצלחה כוזבת על חסימת RLS שקטה | מודול 2+ (ללא שינוי מהשורה למטה) |
| 02/07 | ישי | **מודול 1 — ברובו מוכן** (צעדים 1,2,4,5,6,8,9 מתוך 14 במדריך המאוחד; 3 מסופק מראש). מסך התחברות: שדרוג shadcn/ui + הגנת פרונטנד (חוסם משתמש מוקפא/לא-מורשה). מסך ניהול משתמשים חדש (`UsersManagementPage`, CEO-בלבד, RLS-מוגן). **תשתית ניתוב חדשה**: `react-router-dom` + `MainLayout`+`Sidebar`(מתקפל, RBAC-מסונן מ-DB)+`Topbar`(פרופיל+Logout)+`ProtectedRoute` (הגנת נתיבים גלובלית)+`AuthContext` משותף. `App.jsx` הוגדר מחדש עם כל ה-Routes. **לא בוצע commit — הכל עדיין רק לוקאלית, ממתין לסקירה** | מודול 2+ יכולים להתחיל לבנות מסכים אמיתיים במקום ה-`UnderConstruction` placeholder ב-`/customers` וכו', ברגע שמודול 1 יעבור merge ל-`dev` |
| 30/06 | ישי | מודול 0 — תשתית (Vite, Tailwind, Supabase, RTL) | כולם |
| 30/06 | ישי | מודול 1 (חלקי) — מסך התחברות + Auth + משתמש מנכ"ל | — |

---

## 📌 חובות עתידיים (TODO שצריך לחזור אליהם)

> 🗺️ **חובות-DB (מ-08/07/2026):** התצוגה המאוחדת של כל שינויי-הסכמה העתידיים — `docs/db_roadmap.md` (השורות כאן מצוטטות שם ב-Lane A2; ההחלטות עצמן רק ב-PROJECT_MASTER §7).

דברים שהשארנו "פתוחים" בכוונה, לחזור אליהם כשנגיע למודול המתאים:

- ✅ ~~מסך התחברות → לנתב למסך הבית~~ — הושלם 02/07: מנתב ל-`/` (WelcomePage placeholder, עד שמודול 7 האמיתי ייבנה).
- **שכחתי סיסמה** → לחבר מייל אמיתי + מסך הזנת קוד. יושלם במודול 10.
- **פרמטרים (`params`)** → להזין Seed (מע"מ 18%, יחס אורחים-לדיילת 1:50, משקולות Smart Match) לפני מודול 3. **טרם בוצע.** ⚠️ יש פערי דאטה פתוחים ב-`reference_spec/products_and_params.md` (מיפוי enum עברית→אנגלית, פרמטרים חסרים, סמנטיקת W3, טעויות) — להכריע עם ישי לפני ה-Seed (מפורט בתוכנית מודול 1).
- ✅ ~~**כניסה עם גוגל**~~ — **בוצע (03/07/2026)**: כפתור Google OAuth אמיתי במסך ההתחברות (הוקדם ממודול 10 כחלק ממודל האבטחה החדש).
- ✅ ~~**CAPTCHA**~~ — **בוטל רשמית (03/07/2026)**, הוחלף ב-Google Sign-In + נעילת חשבון (מודל אבטחה חדש, ראו PROJECT_MASTER §5.1).
- **שדרוג נעילת החשבון ל-Auth Hook** → כרגע האכיפה ברמת אפליקציה/DB (`login_attempts` + RPCs, מתאים ל-Free plan) וניתנת לעקיפה בקריאת API ישירה. לנעילה שרת-אמיתית שאי-אפשר לעקוף — Password Verification Hook (נעול לתוכנית Team). לשקול אם/כשעולים לתוכנית בתשלום.
- ✅ ~~**מסך מטריצת הרשאות**~~ — **נבנה (02/07/2026)**: `PermissionsMatrixPage` ב-`/system/permissions` (ראו שורת "צעד 10 + ליטושים" בעדכוני קוד).
- **RLS על 11 הטבלאות הנותרות** → רק `roles`/`modules`/`permissions`/`users` מוגנות. כל טבלה אחרת חסומה לגמרי (RLS on, 0 policies) — כל מי שבונה מודול חדש יצטרך להוסיף policy לפני שהטבלה שלו תחזיר נתונים.
- ✅ ~~**הקשחת `current_user_role_id()`**~~ — **בוצע (02/07/2026)**: migration `harden_current_user_role_id` — `search_path=''` מקובע + EXECUTE הוסר מ-`anon` (ראו שורת ה-DB מ-02/07).
- **הפעלת Leaked Password Protection** ב-Supabase Auth (בדיקת HaveIBeenPwned) → מודול 10, יחד עם זרימת "שכחתי סיסמה" המלאה.
- ✅ ~~**Split-brain RBAC**~~ — **נפתר (03/07/2026)**: גישת "ניהול מערכת" עברה ל-permission-driven (`SYSTEM_MODULES` נאכף ב-`Sidebar`+`App.jsx`, קורא את מפת `permissions` על מודולים 8-9 במקום `role === CEO` קשיח). שורות ה-Seed של מודולי המערכת הפכו למקור אמת חי — כבר לא דאטה מת. *נותר אופציונלי:* לחשוף את 2 מודולי המערכת גם במטריצת ההרשאות ב-UI, כדי לאפשר הענקת גישת-מערכת לתפקיד אחר דרך המסך במקום SQL.
- **מיפוי מודולים לפי מחרוזת עברית** (`MODULE_META` ב-Sidebar, `GROUPS` במטריצת ההרשאות) — שינוי שם מודול ב-DB ישבור בשקט את התצוגה (`if (!meta) return null`). להעביר למפתח יציב (`module_id`/slug) בפעם הבאה שנוגעים בסכמת `modules`.
- ✅ ~~**Error Boundary ברמת ה-Router**~~ — **בוצע (06/07/2026)**: `src/components/ErrorBoundary.jsx` עוטף את `App.jsx`; קריסת render מציגה הודעה בעברית במקום מסך לבן.
- **מוסכמת "פעיל/לא-פעיל" דו-כיוונית ללא מסגור "מחיקה"** — הוכרעה סופית ב-`UsersManagementPage.jsx` (02/07/2026, לפי בקשת ישי): בלי כפתור/אייקון/הודעת "מחיקה", תג סטטוס אמיתי בטבלה, ושינוי סטטוס הפיך בשני הכיוונים מאותו כפתור. **מחייב באופן זהה** במסך לקוחות (מודול 2, `customers.status`) ובמסך דיילות (מודול 4, `hostesses.status`) כשייבנו במקום ה-`UnderConstruction` הנוכחי — שתי הטבלאות כבר כוללות עמודת `status active/inactive` בסכמה, אין צורך במיגרציה.

---

## דוגמה לשורה טובה (למחוק כשמבינים)

> `05/07 · עמית · מודול 2 (לקוחות) גמור — אפשר לבחור לקוח במודול 3 · משפיע על: מודול 3`

</div>
