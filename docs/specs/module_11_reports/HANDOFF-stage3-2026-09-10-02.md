<div dir="rtl">

# מסירה — מודול 11, שלב 3 (`spec.md`)

> **ממשיך מ־:** `archive/HANDOFF-seed-2026-09-10.md` *(אורכב באותו קימוט שבו נכתב הקובץ הזה)*.
> **נכתב:** 10/09/2026 23:1X · **סימון-מקורות:** 🌐 מצוטט מבחוץ · 📏 נמדד היום · 💭 סברה.
> 🔴 **הקובץ הזה נותן התמצאות ומצביע — הוא אינו משכפל תוכן.** מה שנכון גם בעוד שבוע חי בקובץ שלו.

---

## §0 · המקל

**אין סשן בסטנדביי.** הסשן היוצא נסגר; הנכנס הוא הכותב היחיד.
🚫 **ואין כאן טענה על מצב-העץ** — היא הייתה מתיישנת תוך דקות. **מדוד:**
`git status --short` · `git log origin/ishay/dashboard-legend-ucd --oneline -1`.

## §1 · איפה אנחנו — במשפט אחד

**שלב 2 של הדיסקברי נסגר: ‏19 דפי-מוקאפ צוירו, נבדקו מספר-מספר, ואושרו על-ידי ישי
ב-10/09/2026.** ‏**שלב 3 — כתיבת `spec.md` — טרם התחיל, והקובץ אינו קיים.**

⏱️ **השעון:** הכנס **15/10/2026**. **מודול 11 הוא אפס שורות קוד.**

## §2 · הצעד הבא הבטוח

**לכתוב `docs/specs/module_11_reports/spec.md`** — המסמך העברי של ישי, שמסכם את המודול
*(‏`docs/CLAUDE.md`, טבלת מפת-התיקיות: **`spec.md` עברית — המסמך של ישי** · `discovery-log.md`
אנגלית — הראיות)*. **הוא הפריט האחרון של `module-discovery` לפני `module-blueprint`.**

🚫 **מה לא לעשות לפניו:** לא לפתוח בנייה, לא לגעת בזריעה, ולא לצייר מוקאפ נוסף.

## §3 · 📖 רשימת-הקריאה — **לקרוא, לא לגרפ**

> 🔴 *"לקרוא אותם זה לקרוא 100 שורות כל פעם — ולא גרפים"* **(ישי)**. גריפ מאשר מה שכבר ידעת;
> הוא אינו מגלה מה שלא שאלת.

**חובה, בסדר הזה, ולכל מקור מה לקחת ממנו:**

| # | הקובץ | מה לקחת ממנו |
|:-:|---|---|
| 1 | `processes-approved.md` §🗳️ | **39 ההכרעות המתוארכות** — הן גוף ה-`spec` |
| 2 | `processes-approved.md` §📑 + §📑ב | הגדרת כל דוח פר-דף · דרישות-הצגה |
| 3 | `processes-approved.md` §📐 | ‏23 כללי-הדוחות שמחייבים כל משטח |
| 4 | `screens-approved.md` | **טבלת-המצב — ה-SSOT היחיד לספירת-משטחים.** ‏19 🔶 · 3 ⏸️ · 4 ⬜ |
| 5 | `design-contract.md` §⑥ · §⑧ | שלד-הדף · שכבת-ההטמעה (איך היא מצוירת) |
| 6 | `stage2-cards/cards-*.md` §③ | מקור לכל מספר על המסך, פר-דף |
| 7 | `docs/mockups/management-report-screen/drafts/*.html` | **המראה בפועל.** ⚠️ **ו-19 "הדפים" הם ארבעה קבצים** — קובץ ללשונית (`02_tab_executive` · `03_tab_finance` · `04_tab_hostesses` · `05_tab_customers`), וכל אחד מחליף בין דפיו בלשוניות-משנה. **אל תחפש 19 קבצים** |
| 8 | `.claude/skills/module-discovery/template.md` — הסעיף `# 📦 Stage 3` | **מה `spec.md` חייב להכיל** *(אנגלית; פותח ב-"`spec.md` is five things, and no more")* |

**🟡 "כנראה לא תצטרך" — לפתוח רק על פער אמיתי, ולומר שפתחת:**
`data-set.md` *(107KB — מפת-הדאטה)* · `seed-plan.md` · `business-model-anchor.md` ·
`onboarding-layer-contract.md` · `stage2-review/*` *(דוחות-הביקורת; `m11-inclusion-remeasure-2026-09-10.md`
נושא את §10–§11 — המעבר הוויזואלי)*.

## §4 · 🪤 מלכודות — **נבנה בסריקת-מנגנונים, לא מהזיכרון** 📏

| המלכודת | הראיה, נמדדה 10/09/2026 |
|---|---|
| 🔴 **שלושה קבצים גדולים מתקרת כלי-ה-`Read`** | ‏`processes-approved.md` **307,040** · ‏`PROJECT_MASTER_sec7.md` **270,954** · ‏`PROJECT_MASTER.md` **265,512** — כולם מעל **256KB**, והתקדים מתועד ב-`docs/CLAUDE.md`. 🔴 **והאמצעי הוא בדיוק זה שכלל-ברזל 1 שולח אליך** *(השאלות-הפתוחות)*. ⇒ **לקרוא בטווחים** (`offset`/`limit`) או `sed -n`. **מדוד לבד: `wc -c <קובץ>`** |
| 🔴 **`git add -A` חסום — והחוסם הוא גלובלי, לא של הריפו** | ‏`~/.claude/hooks/git-add-explicit.mjs`, רשום ב-`~/.claude/settings.json` כ-`PreToolUse \| Bash`. **חל בכל פרויקט.** ‏⚠️ הניסוח "הוק חדש בריפו" במסירה הקודמת היה **לא-מדויק במיקום** — הכלל עצמו נכון |
| ⚠️ **הוק חוסם צנרת של שער ל-`head`/`tail`/`grep`** | `.claude/hooks/block-shell-dialect-mixup.sh`. ‏`npm run gate \| tail` **לא ירוץ**. ⇒ `npm run gate > /tmp/g.log 2>&1; echo "exit=$?"; tail -30 /tmp/g.log` |
| ⚠️ **שער-אישור על כתיבה** | ‏`~/.claude/hooks/approval-gate.mjs` על `PreToolUse \| Edit\|Write\|NotebookEdit\|MultiEdit` — **גלובלי**. עריכה עשויה להיחסם עד אישור |
| ⚠️ **הוקים של הריפו — ארבעה** | ‏`session-start-context` *(אינפורמטיבי)* · `block-shell-dialect-mixup` · `protect-frozen-files` · `check-docs-updated` *(Stop)*. **המקור המחייב: `.claude/settings.json`** |
| 🔴 **הוק-הסיום חוסם, ואכיפה 0ג חלה על `docs/specs/module_*/*.md`** | ‏`check-docs-updated.sh`: כל `🚧 מN` שתכתוב ב-`spec.md` **חוסם את סיום הסשן** בלי שורה תואמת ב-`PROJECT_MASTER §6`. ➕ **וסדר-כתיבה כפוי בסוף:** ‏`CLAUDE_CODE_LOG.md` ואז `STATUS.md` צריכים להיות בעלי ה-mtime האחרון |
| ⚠️ **שישה הוקים גלובליים, לא שניים** | ‏`~/.claude/settings.json`: ‏`git-add-explicit` · `approval-gate` · **`open-items-check` (Stop — חוסם סיום בלי שורת "מה פתוח"/"אין פתוח")** · `estimation-reminder` · `route-refresh` · `clear-approval-gate` |
| ⚠️ **`block-shell-dialect-mixup` חוסם גם תוכן, לא רק פקודה** | הוא חוסם `Get-Date`/`Select-String`/`Where-Object` וכו' **גם כשהם מחרוזת שאתה כותב לתוך מסמך**. ⇒ **מסמך עברי גדול נכתב בכלי `Write`, לא דרך heredoc ב-Bash** — ההוק עצמו ממליץ כך |
| 🔴 **כתיבת קובץ מפייתון ⇒ CRLF ושער אדום בשקט** | ‏`io.open(...,newline='')` בקריאה **ובכתיבה**; לאמת `tr -cd '\r' < f \| wc -c` ⇒ **0** |
| 🔴 **`check:docs-structure` — בדיקה (א): כותרות-פרק כפולות או מחוץ-לסדר** | **זו מטרתו הראשית**, והיא רצה על כל `docs/specs`. מסמך עברי חדש ורב-פרקים הוא בדיוק הקורבן: ‏`## 3` אחרי `## 4` ⇒ נחסם. **לבדוק בעצמך: `node scripts/check-docs-structure.mjs`** |
| ⚠️ **אותו סקריפט — בדיקה (ב), ספירת-משטחים** | ‏🔴 **וה-SSOT שלו הוא `docs/specs/module_04_hostesses/screens-approved.md` — מודול 4, לא 11** *(`COUNT_SSOT_FILE` בסקריפט)*. הדפוס תופס **רק** מספר שאחריו `מתוך`/`מ-`/`/` ואז **הספרה שמונה**, ובאותה שורה המילה `סגור` או `נסגר`. ⇒ ‏"19 מתוך 26" **לא** ייתפס; ניסוח שמסתיים בספרה ההיא **כן**. 🪤 **וזו מלכודת שהפילה את המסירה הזו פעמיים:** גם השורה ש**מתארת** את הכלל מפעילה אותו, אם היא נושאת דוגמה מספרית. **כתוב את הדפוס במילים, לא בספרות.** **הכלל המעשי ל-`spec.md`: מצביע ל-`screens-approved.md`, לא מספר** |
| ⚠️ **`npm run gate` אינו מה ש-CI מריץ** | ‏`check:docs-structure` אינו רץ אוטומטית בשום מקום. הפירוט: `CLAUDE.md` §עובדות-סביבה |

## §5 · 💬 מה שקיים **רק בשיחה** — וזו כל הסיבה שהקובץ הזה קיים

### מה ישי אמר — מילה-במילה, מתוארך

| הציטוט | סוג | תאריך |
|---|---|---|
| *"מאשר לפי המלצתך, מאשר את המוקאפים"* | 🟢 **הכרעה** — שלב 2 סגור | 10/09/2026 |
| *"רק להבנה איך התמודדת במוקאפים שצריך גם מצב בוגר כמו ההייטק וגם מצב הטמעה עם יותר הסברים לפי ucd?"* | 🔵 **שאלת-הבנה, לא הכרעה** — נענתה; אין ממנה מטלה | 10/09/2026 |
| *"מאשר הכל לפי המלצתך מחכה שתגיד לי שמחכים לי רק לאישור המוקאפים"* | 🟢 **הכרעה** — האצלה רחבה עד אישור-המוקאפים | 10/09/2026 |
| *"אני כן חושב ש16 זה מספר טוב… פשוט נבחר את הכי טובים לנו"* | 🟢 **הכרעה** — תקרת-משטחים **נבנים** | 10/09/2026 |
| *"אני עקבי שאמרתי בלי קיצורי דרך"* | 🟢 **הכרעה-שיטה** | 10/09/2026 |

🔴 **ורעיון שלו אינו הכרעה שלו** — ‏`CLAUDE.md` כלל 1. **הכחול למעלה אינו מטלה.**

### מה קלוד אמר — ואילו פריטים הוגשו **בלי** המלצה

- **עם המלצה:** פיצול המילה "מרוצים" *(אושר)* · יישור-הכסף להכרעה 36 *(בוצע בהאצלה)* ·
  **סיבוב-סשן** *(הומלץ; ישי לא הכריע — ר' §7)*.
- ⚠️ **בלי המלצה, ולכן "מאשר" אינו חל עליהם:** שני חובות-§6 *(גבולות-פקדים · אטום ערך+יחידה)*
  הוצגו כחוב, **לא כהצעה** — הם ממתינים להכרעה נפרדת.

### מה קלוד עשה — **ומה נשקל ונדחה, עם הנימוק**

- **בוצע:** מעבר ויזואלי על **כל 19 הדפים** *(הורחב מ-8 שתוכננו)*; ‏**5 פגמים תוקנו**;
  הכרעת-"מרוצים"; שער ירוק; ‏4 קומיטים נדחפו.
- 🚫 **נשקל ונדחה — ואל תבנה מחדש:**
  - **לאחד את תגית-הכוכבים בטבלה עם חתך-הלקוחות** — נדחה: `scoreTag` הוא **SSOT של מ8**
    (`src/lib/projectFinance.js`), ואיחוד היה משנה מסכים **מחוץ למ11**.
  - **לתקן את "אירועים שהסתיימו" בלשונית-ההנהלה** — נדחה: שם זו **תווית-אריח** (‏236) ולא
    הגדרת-בסיס, והמספר מוכיח שהבסיס נכון. **תיקון היה רעש.**
  - **לשנות את מספרי הגיול (25) או התזרים (23)** — נדחה: **שניהם נכונים**, כל אחד לפי הכלל
    שהוא מצהיר עליו. נוסף **משפט-גשר** בכל דף במקום.

### 🔴 במה קלוד טעה — והנוסח המתוקן

| הטעות | הנוסח המתוקן |
|---|---|
| **הוכרז ש-`330,445 ₪` שגוי ב-14,269** *(נאמר לישי לפני הקומפקט)* | **המספר נכון.** ‏`2,402,325` משתחזר לשקל כ-`finished` בלבד על **294** פרויקטים. **הפגם האמיתי: דף אחד בשני בסיסים** |
| **`n=1,730` "לא משתחזר"** *(קיבלתי 1,723)* | **הצייר צדק.** ‏`approval_withdrawn` **נספר כנוכחות** ("ויתרה" בסולם-האמינות) |
| **"חובות אבודים (2)" נראה שגוי** *(במסד 7 `written_off`)* | **נכון.** רק ל-**2** נשלחה חשבונית אי-פעם; חמש האחרות מבוטלות שלא חויבו |
| **שני חובות-§6 נשמטו מדיווחי "מה פתוח"** אחרי הקומפקט | **הוחזרו** לראש `STATUS.md`. ‏**הרשם (`PROJECT_MASTER §6`) החזיק אותם — נשבר הדיווח, לא המנגנון** |

🔑 **הדפוס שמאחורי שלושת הראשונים, וכדאי שתישא אותו הלאה:** ‏**מדדתי אוכלוסייה אחת ואת
המסך בדקתי מול אחרת.** ‏📏 הסשן היוצא ספר זאת **שמונה פעמים ביום אחד**. **הכלל שעוצר: שחזר
את ההגדרה שהופכת את המספר שעל המסך לנכון — לפני שאתה קורא לו שגוי.**

## §6 · החלטות-מפתח **עם הנימוק** *(לא רק התוצאה)*

- **הכרעה 36 — אוכלוסיית דוחות-הכסף = ארבעת הסטטוסים** *(`finished` · `awaiting_payment` ·
  `awaiting_invoice` · `event_finished`)*. **הנימוק:** הכנסה נרשמת כשהאירוע קרה והוצאה חשבונית,
  לא כשהכסף נכנס; ‏`finished` לבדו הסתיר **233,481 ₪ מ-2026**.
- **הכרעה 30 — ‏16 משטחים: ארבע לשוניות, מבט-על ועוד שלושה דפים בכל אחת.**
  **ששה נדחים עם טריגר-החזרה ואינם יורדים מהאפיון: מ5 · מ10 · מ11 · מ13 · מ18 · מ26**
  *(מ12 נשאר; **מ13 הוא המחליף הראשון**)*. **הנימוק, במילותיו:** *"יותר מזה יגרום לנו
  צריך לעשות דוחות לא רלוונטים… פשוט נבחר את הכי טובים לנו"*.
  ✅ **אושרר 10/09/2026 23:5X:** *"אני רציתי 16 זה מה שהוחלט בשיחה גם לא? ככה נבחר את
  הטובים והמתאימים ביותר"*. ⇒ **לשונית-הכספים = מ7 · מ8 · מ9 · מ12.**
  ⚠️ **ומה שהיה סחוף עד 23:5X ותוקן:** ‏`screens-approved.md` · טבלת-המשטחים ·
  `design-contract §⑥.1` · ובורר-הדוח ב-`03_tab_finance.html` — כולם מנו שבעה דפי-כספים,
  כי הלשונית צוירה **07/09** וההכרעה נכתבה **10/09**. **כולם יושרו.**
- **שכבת-ההטמעה: שני מצבים בלבד (0 נקי · 2 מודרך).** **הנימוק (ישי):** *"רמה 1 היא לא החור…
  המערכת שלי אקדמית"*. ⇒ **מתג בינארי, דף אחד, לא שני עותקים.**
- **עוגני-רמז באות עברית (א·ב·ג) ולא בספרה.** **הנימוק:** 📐14ב כבר תפס ספרות לאינטראקציות.

## §7 · פריטים פתוחים

| | |
|---|---|
| ✅ **הוכרע 10/09 23:5X — אל תפתח מחדש** | **‏16 משטחים, ששה נדחים** (ר' §6). ‏`spec.md` נכתב על **16**. **הסחיפה בארבעה קבצים תוקנה באותו קימוט** |
| ❓ **להכרעת-ישי** | **סיבוב-סשן** — הומלץ ולא הוכרע · **שני חובות-§6:** גבולות-פקדים `slate-200`=**1.23–1.49:1** מול 3:1 *(התיקון המדוד: `slate-500`=4.76:1, החלפת-טוקן)* · **אטום ערך+יחידה** (`Money`/`LtrFieldGroup`) — **המופע הרביעי השנה**, וחייב לחול גם על `design-contract.md` |
| 🖱️ **לחיצות שלו** | PR **#128** · `regin-docs-sync` |
| 🔴 **חוסם-כנס** | **14/10/2026** — `scripts/conference-refresh/refresh-before-conference.sql`, ואחריו 16 מבחני-הקוהרנטיות |
| ⬜ **ותיק** | ענף-הזריעה עם מיגרציית `seed_registry` · דחיסת `CLAUDE_CODE_LOG.md` |
| ✅ **נסגר, ואל תגרור הלאה** | ‏**§7.96 סגור** — ‏`PROJECT_MASTER_sec7.md` מסמן `96. 🟢 סגור·לוגיקה·מ7`. **הוא נגרר כפתוח בראש `STATUS.md` ובמסירות קודמות בטעות; אומת ותוקן 10/09/2026** |

## §8 · ✋ מבחן-הקבלה — **הפורמט של `~/.claude/references/session-handoff-guide.md` §3**

**הסשן הנכנס עונה בהודעה הראשונה, לפני כל פעולה:**

**🅐 מכני** — לפתוח כל נתיב שהקובץ הזה מזכיר; **נתיב מת ⇒ לעצור ולדווח.** ·
`git rev-parse --abbrev-ref HEAD` תואם ל-`ishay/dashboard-legend-ucd`? · אין `[TODO]` שלא מולא.

**🅑 שרידות** — לכל טענה משלוש הקטגוריות *(ההחלטות · נתיבי-הקבצים · החסמים)*:
**"מהקובץ ‏\<שם\>"** או **"מהזיכרון"**. אין אפשרות שלישית.

**🅒 שיפוט** — החסמים ב-§7 עוד קיימים? · מה ש-§5 מסמן "בלי המלצה" עוד מחזיק?

**🅓 ארבע שאלות עובדתיות — התשובות על הדיסק:**
1. כמה בתים ב-`processes-approved.md`, ומה זה עושה לכלי-`Read`?
2. מה שורת-האוכלוסייה של **מ21** אומרת היום, ומה היא אמרה לפני 10/09?
3. איזה קובץ חוסם `git add -A`, ו**באיזו תיקייה הוא יושב** — של הריפו או הגלובלית?
4. כמה משטחים 🔶 מצוירים, וכמה ⏸️ נדחו — **ומאיזה קובץ המספר**?

</div>
