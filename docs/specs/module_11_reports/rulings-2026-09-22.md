<div dir="rtl">

# 🔨 הכרעות-ישי · 22/09/2026 — מ11, חלון-הייצוא

> **למה הקובץ הזה קיים, ולמה בנתיב חדש:** ההכרעה למטה נמסרה בצ'אט ל-סשן-הסגן, ו**ארבעה ניסיונות
> להעבירה לסשן-הבונה דרך ערוץ-ההודעות בין הסשנים נכשלו** *(הודעות מוחזקות-לאישור שפגו)*.
> 🔑 **הכרעה שקיימת רק בהודעה שלא נמסרה — אינה קיימת.** ⇒ נכתבה לריפו, שם היא שורדת בלי תלות בערוץ.
> 🔴 **ונתיב חדש בכוונה:** ‏`PROJECT_MASTER_sec7.md` · `processes-approved.md` · `CLAUDE_CODE_LOG.md`
> נמצאים כולם בשטח שסשן-הבונה עורך **ברגע זה**. כתיבה לשם הייתה יוצרת קונפליקט מכוון בענף חי.
> **קובץ חדש = אפס התנגשות.** ‏📌 **וזו כתיבה-חזרה לפי כלל-ברזל 13, לא נוחוּת.**

---

## ① עשר התוויות החדשות בבוחר-העמודות — **סגור**

**סטטוס קודם:** ‏`HANDOFF-export-dialog-2026-09-17.md` §7 פריט **3ג③** — *"פתוחה: עשר התוויות
המסומנות `[חדש]` — אין להן שם בממשק היום, והנוסח שלי טעון תיקון"*.
**הכרעת-ישי, 22/09/2026 ‏23:1X. מילותיו: *"מאשר"*** — על הטבלה כולה, אחרי שהוצגה לו שורה-שורה
עם המלצה לכל אחת, וארבע מהן סומנו כשינוי.

| העמודה | הטבלה | **התווית הסופית** |
|---|---|---|
| `owner_phone` | `projects` | טלפון מנהלת |
| `positive_feedback_reasons` | `projects` | סיבות משוב חיובי |
| `negative_feedback_reasons` | `projects` | סיבות משוב שלילי |
| `feedback_notes` | `projects` | הערות משוב |
| `cancel_reason` | `projects` | סיבת ביטול |
| `cancelled_by` | `projects` | בוטל על-ידי |
| `cancelled_at` | `projects` | **תאריך ביטול** ✏️ *(הוצע: "בוטל בתאריך")* |
| `created_at` | `projects` | **תאריך פתיחה** ✏️ *(הוצע: "נוצר בתאריך")* |
| `operationally_closed_by` | `projects` | **סגר תפעולית** ✏️ *(הוצע: "נסגר תפעולית על-ידי")* |
| `written_off` | `project_finance` | **חוב אבוד** ✏️ *(הוצע: "נמחק כחוב אבוד")* |

### 🔑 הנימוק שאושר — **נרשם כי ההכרעה היא הנימוק, לא רק התוצאה**

**כותרת-עמודה באקסל היא *שם*, לא *משפט*.** ארבע מהתוויות המוצעות היו צירופי-פועל
*("בוטל בתאריך" · "נוצר בתאריך" · "נסגר תפעולית על-ידי" · "נמחק כחוב אבוד")* — הן קוראות
כמו שורה בדוח, לא ככותרת של עמודה.

📏 **והמדידה שהכריעה את `written_off` — מ-`docs/schema.sql`, לא מהזיכרון:**

```
written_off  boolean  not null default false
```

⇒ **התא יכיל כן/לא.** *"נמחק כחוב אבוד: כן"* קורא כמו שגיאה; *"חוב אבוד: כן"* קורא נכון.
**הפקודה שמחזירה את זה:** `git show origin/dev:docs/schema.sql` ⇐ `grep -E '^\s+written_off\s'`
*(לשם השוואה: `cancelled_at` ו-`created_at` הם `timestamptz`; `cancelled_by` ו-`operationally_closed_by` הם `text`.)*

### ⚠️ סייג אחד שנוסע עם ההכרעה
**‏`created_at` ⇒ "תאריך פתיחה"** כי זה המונח העסקי לפרויקט אצל ישי.
🔴 **אם מתברר ש"תאריך פתיחה" כבר תפוס בממשק למשמעות אחרת — זה ממצא. לעצור ולדווח, לא לפתור לבד.**

### 🔁 מה עוד צריך לקרות עם ההכרעה הזו *(כלל-ברזל 13)*
**הסשן שיגיע לתיאור-העמודות של `ProjectsPage` רושם אותה שם, ומסמן את §7 פריט 3ג③ כסגור
בהפניה לקובץ הזה** — ‏`docs/specs/module_11_reports/rulings-2026-09-22.md`.

---

## 📡 ועל הערוץ — עובדה תפעולית, לא תלונה

**נמדד 22/09/2026: ‏4 הודעות בין-סשנים לא נמסרו** *(2 הוחזקו ופגו · 2 קודם לכן)*, **בנוסף ל-6
כשלים ו-4 הצטלבויות ב-17/09.**
⇒ 🔑 **הדיסק הוא הערוץ.** כל הכרעה שנמסרת בצ'אט נכתבת לקובץ **באותו תור**, לא "אחר-כך".

</div>
