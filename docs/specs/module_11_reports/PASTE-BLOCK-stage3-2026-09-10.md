<div dir="rtl">

# 📋 הבלוק להדבקה — מודול 11, אחרי שלב 2

> ‏**ישי: להעתיק את המסגרת למטה בלבד.** עודכן 11/09/2026 01:1X, אחרי קריאה חוזרת של כל השיחה.
> 🔑 **נפתח בסלאש בכוונה** — ביטוי-טריגר הוא הסתברות, סלאש הוא ודאות *(הכרעת-ישי 09/09)*.
> 📏 **קצר בכוונה** — בלוק ארוך נקרא בדילוג. כל השאר בקובץ-המסירה שהשורה הראשונה מחייבת לקרוא.

---

```
/ishay אני ממשיך את מודול 11 ב-REG-IN. C:\Users\ishay\Reg-In, ענף ishay/dashboard-legend-ucd.

לפני שאתה עונה לי משהו — קרא במלואו:
docs/specs/module_11_reports/HANDOFF-stage3-2026-09-10-02.md
ואז את רשימת-הקריאה שב-§4 שלו, לפי הסדר שכתוב שם.

קרא אותם באמת — 100 שורות בכל פעם, לא גריפ. יש לך תקציב:
עד ~100K הקשר לפני שאתה מתחיל לעבוד זה מצוין אצלי.
כשפספסת דברים, הכיול-מחדש עלה לי 250K. עדיף שתקרא.

🔴 שלוש עובדות שקובעות הכל:
- הכנס ב-15/10/2026. מודול 11 הוא אפס שורות קוד.
- 16 המשטחים מצוירים ונמדדו מספר-מספר. אמרתי עליהם "מאשר את המוקאפים".
- אבל §2 של קובץ-המסירה מראה ששני חסמים עומדים לפני spec.md, ואחד מהם
  הוא שהאישור שנתתי אינו האישור שהפרוטוקול שלי עצמי דורש. תקרא ותחליט מה להביא לי.

הצעד הבא: לסגור את §2, ורק אז לכתוב spec.md.
לא לפתוח בנייה, לא לגעת בזריעה, לא לצייר עוד מוקאפ.

🪤 שלוש מלכודות שיעלו לך מיד — הפירוט ב-§5:
- שלושה קבצי-אפיון גדולים מתקרת כלי-הRead. לקרוא בטווחים, לא בקריאה אחת.
- `git add -A` חסום בהוק גלובלי. לקמט לפי נתיב מפורש בלבד.
- ה-CLAUDE.md שעל הענף מיושן מול dev — כלל 10 שם שונה. אל תצטט אותו בלי להשוות.

איך לעבוד איתי — §7 בקובץ המסירה, ואל תדלג:
הכרע לבד את כל מה שמקצועי. §7 מפריד מה היה הכרעה שלי, מה היה רגש,
ומה הוגש לי בלי המלצה — ו"מאשר" אינו חל על השלישי.

🚫 ומה שאסור לך להכריע לבד: לפסול נכס קיים שלי — דף, מסמך, שיטה או מספר.
לפני פסילה: שחזר את ההגדרה שהופכת את המספר שעל המסך לנכון.
זו המחלקה שהסשן הקודם טעה בה חמש פעמים ביום אחד (§7).

✋ מבחן-קבלה לפני שאתה מתחיל. הפורמט הוא של ~/.claude/references/session-handoff-guide.md §3,
לא המצאה שלי — ענה על ארבעתם בהודעה הראשונה. הם יושבים ב-§11 של קובץ-המסירה.
```

---

## מצב בעת הכתיבה — **לאמת, לא להניח**

> 🚫 **בלי מספרים קשיחים שמתיישנים** — הפקודה שמודדת, לא התוצאה.

| | |
|---|---|
| הענף והדחיפה | `git status --short` · `git log origin/ishay/dashboard-legend-ucd --oneline -1` |
| ספירת-המשטחים | **טבלת `## מצב` ב-`screens-approved.md`** — ה-SSOT היחיד. לא לספור במקום אחר |
| היכן המוקאפים | `ls docs/mockups/management-report-screen/` — **ולהשוות למה שהבלופרינט מחפש** |
| השער | להפנות לקובץ, ואז לקרוא את קוד-היציאה. **לא לצנרר ל-`tail` — הוק חוסם** |
| מבחני-הזריעה | `docs/specs/module_11_reports/seed-coherence-tests.md` |
| 🔴 חוסם-כנס | **14/10/2026** — ר' ראש `STATUS.md` |

**מה שלא נכנס לבלוק, ולמה:** הקיצור הוא התכונה. נכנסו רק **מה שמשנה את הצעד הראשון** —
לאן ללכת · מה לא לשבור · מה השעון · ומה אסור להכריע לבד. **סיכום מה שקרה, נימוקים, ורשימת
פריטים פתוחים שאינם הצעד הראשון — לא נכנסו בכוונה**, הם מסיטים בדיוק את מי שאמור להתחיל.

</div>
