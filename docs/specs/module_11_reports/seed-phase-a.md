<div dir="rtl">

# תוכנית-זריעה — מודול 11, **פזה א'** (`seed-phase-a.md`)

> 🔗 **מראת `processes-approved.md` — לא לערוך כאן.** כל כלל-מוצר להלן הוא שיקוף של הכרעה שנרשמה
> שם (הכרעות 10 · 11 · 13 · 17 · 22 · 24 · 25 · 27 ✏️ 23:0X · רשם המסלולים שנשללו). ה-SSOT היחיד הוא
> `docs/specs/module_11_reports/processes-approved.md`. מה שכאן ואינו מצוטט משם — **הצעה**, וישי מכריע.
>
> **מה הקובץ הזה:** תוכנית-ביצוע לפזה א' של הכרעה 25 — *"סגירה תפעולית+כספית של אירועי-העבר ·
> השלמת נפח · בונוסים · דוח-שכר · שינויי-תכולה נוספים"*. הוא **נכתב כדי שסשן אחר יריץ אותו**.
> **מה הוא אינו:** אינו פזה ב' (דרג-שיבוץ · סיווגי-AI — אלה תלויי-סכימה ונשארים לבלופרינט),
> ואינו מחליף את `seed-plan.md` — שם יושב הניתוח פר-דוח, וכאן רק החלק בר-ההרצה.
>
> 🔴 **הקובץ הזה לא נגע במסד.** כל מספר להלן נמדד ב-`execute_sql` **בקריאה-בלבד** מול
> `yfeovxppnfoafmfbdfvh` ב-**06/09/2026**, ו**מתיישן** — סעיף 1 נותן את ה-SQL כדי למדוד מחדש
> ביום-ההרצה. **שום צעד להלן לא בוצע, ושום דבר כאן אינו מסומן "בוצע".**
>
> **מה נקרא במלואו לפני הכתיבה:** `processes-approved.md` · `seed-plan.md` ·
> `docs/specs/module_07_dashboard/seed-data-spec.md` · `supabase/migrations/20260903180958_seed_registry_and_helpers.sql`
> · `supabase/migrations/CLAUDE.md` · `e2e/CLAUDE.md` · `e2e/smoke-anchors.json` · כותרות
> `scripts/demo-seed.mjs` · `scripts/seed-lib/plan.mjs` · `scripts/reseed-feedback-notes.mjs` ·
> `scripts/enrich-feedback-reasons.mjs` · `scripts/seed-assert.mjs` · `scripts/apply-write-off-cases.mjs`
> · וגופי ה-RPC-ים החיים דרך `pg_get_functiondef` (לא מקובצי-מיגרציה ישנים — הכלל
> ב-`supabase/migrations/CLAUDE.md`).

---

## 0 · שלוש עובדות שמעצבות את כל התוכנית — לקרוא לפני הצעדים

**① הסגירה אינה הפיכה, בניגוד להנחת הכרעה 25.** הכרעה 25 כתובה *"הפיכות דרך `seed_registry`"* —
**וזה נכון ליצירת שורות חדשות בלבד.** נמדד היום ב-`pg_proc`: **אין בכל המסד פונקציה אחת של
ביטול-ארכוב / פתיחה-מחדש** (חיפוש `unarchive`/`reopen`/`revert`/`undo` ⇒ 0 תוצאות), ו-`projects`
נושאת policy-קריאה בלבד (`projects_select_by_permission`, ואין `..._write_...`). ⇒ **פרויקט שעבר
`archive_project` נעול:** `finance_assert_writable` זורקת *"התיק נעול — הפרויקט כבר הועבר לארכיון"*
על כל כתיבה כספית. **הביטול היחיד הוא `seed_reset('seed-full-2026-09-03')` שמוחק את כל 823
הפרויקטים הזרועים** — כלי-הרס, לא כלי-חזרה. ⇒ **כל צעד שסוגר פרויקט קיים מסומן להלן "לא הפיך",
ולכן יש עצירת-ישי אחרי מקבץ-הפיילוט של 10 פרויקטים ולא אחרי 43.**
✏️ 23:0X **מתוקן בהכרעה 27:** אין עוד עצירת-ביניים באמצע. הרצף (צעדים 1–4) רץ במלואו ברציפות,
וההסתייגות שכתובה כאן ממשיכה להתקיים — אבל היא הפכה לדרישת-**אימות** מיד אחרי הרצף (ר' צעד 4א
החדש), לא לשער-עצירה-לאישור באמצעו.

**② את ספטמבר אי-אפשר לסגור היום, ולא בגלל בחירה.** `close_project_operationally` פותחת ב-
*"אם הסטטוס אינו `event_finished` — לא ניתן לסגור"*, והמעבר ל-`event_finished` נעשה **רק** כשתאריך-
האירוע עבר (עבודת-הלילה `module6-event-finished`, ואותו כלל בדיוק מועתק לתוך `seed_backdate_project`).
**נמדד: 24 מ-29 אירועי ספטמבר הלא-מבוטלים מתקיימים ב-07/09–29/09** — כלומר בעתיד ביחס להיום.
בנוסף, `record_payment` זורקת *"תאריך קבלת התשלום אינו יכול להיות בעתיד"*.
⇒ **פזה א' מתפצלת בהכרח לשתי ריצות:** **א1 (עכשיו)** — מה שכבר עבר · **א2 (29–30/09, יומיים לפני
הכנס)** — ספטמבר. ⚠️ **וזו אינה החמרה שהמצאתי:** הכרעה 17 עצמה קובעת *"תאריך-היעד הוא יום-הכנס
(~01/10/2026), לא היום"*.

**③ שינוי-תכולה חייב לקדם את הסגירה, אחרת הוא בלתי-אפשרי.** `apply_scope_change` נופלת עם
*"האירוע כבר נסגר תפעולית ולא ניתן לשנות את תכולתו"* על כל פרויקט ש-`operationally_closed_at`
שלו אינו NULL או שסטטוסו `awaiting_invoice`/`awaiting_payment`/`finished`. ⇒ **צעד 1 קודם לצעד 2,
ולא להפך.** *(וזה גם ההסבר ל-16 השורות הבודדות ב-`project_changes`: הגנרטור סוגר כל אירוע היסטורי
מיד, ולכן לא נותר לו על מה להחיל שינוי-תכולה — כל 16 השורות יושבות על פרויקטים פתוחים של
ספטמבר–אוקטובר.)*

---

## 1 · מדידה עדכנית — 06/09/2026, קריאה-בלבד

> **כל שאילתה כאן רצה היום והמספר שלצידה הוא הפלט שלה.** **מדוד מחדש ביום-ההרצה** — טבלת-היעד
> בסעיף 2 נגזרת מהמספרים האלה, ואם הם זזו, גם היא זזה.

### 1א · הפער של הכרעה 17 — R11 לפי חודש

`R11` = אוכלוסיית-הרווח שהוכרעה בלדג'ר: `finished` **+** מבוטלים שדמי-הביטול שלהם נפתרו.

```sql
with base as (
  select p.project_id, p.final_event_date, p.project_status, pf.cancellation_fee
    from projects p left join project_finance pf on pf.project_id = p.project_id
   where p.final_event_date >= '2026-01-01')
select to_char(final_event_date,'YYYY-MM') ym,
       count(*) filter (where project_status='finished') fin,
       count(*) filter (where project_status='cancelled' and cancellation_fee is not null) canc_resolved,
       count(*) filter (where project_status='finished'
                           or (project_status='cancelled' and cancellation_fee is not null)) r11
  from base group by 1 order by 1;
```

⇒ **2026: 31 · 35 · 38 · 24 · 37 · 31 · 14 · 5 · 0** — **זהה מילה-במילה למה שהכרעה 17 מצטטת.**
המספר לא זז מאז 06/09 20:5X.

### 1ב · מה באמת עומד מאחורי הקריסה — פילוח-סטטוס לפי חודש

```sql
select to_char(final_event_date,'YYYY-MM') ym, count(*) total,
  count(*) filter (where project_status='finished')          finished,
  count(*) filter (where project_status='cancelled')         cancelled,
  count(*) filter (where project_status='awaiting_payment')  awaiting_payment,
  count(*) filter (where project_status='awaiting_invoice')  awaiting_invoice,
  count(*) filter (where project_status='event_finished')    event_finished,
  count(*) filter (where project_status='ready')             ready,
  count(*) filter (where project_status='in_progress')       in_progress,
  count(*) filter (where project_status='not_started')       not_started
from projects where final_event_date >= '2026-07-01' group by 1 order by 1;
```

| חודש | סה"כ | מבוטל | finished | ממתין-תשלום | ממתין-חשבונית | הסתיים-האירוע | מוכן | בתהליך | טרם-החל |
|---|---:|---:|---:|---:|---:|---:|---:|---:|---:|
| 07/2026 | 15 | 0 | 14 | **1** | 0 | 0 | 0 | 0 | 0 |
| 08/2026 | 19 | 1 | 5 | **7** | **5** | **1** | 0 | 0 | 0 |
| 09/2026 | 30 | 1 | **0** | 0 | 0 | **5** | **7** | **17** | 0 |
| 10/2026 | 22 | 1 | 0 | 0 | 0 | 0 | 3 | 15 | 3 |
| 11/2026 | 19 | 1 | 0 | 0 | 0 | 0 | 1 | 13 | 4 |
| 12/2026 | **0** | — | — | — | — | — | — | — | — |

🔑 **הקריאה הנכונה של הטבלה, וזו אינה מה שכתוב בהכרעה 17:**
· **ספטמבר — פער-סגירה בלבד.** 30 אירועים זה **בתוך** היעד 25–35; אף אירוע לא נסגר.
· **אוגוסט — פער-סגירה בעיקר** (13 מ-18 הלא-מבוטלים תקועים) **ופער-נפח משני** (19).
· **יולי — פער-נפח בעיקר** (15 אירועים) ורק אירוע אחד תקוע.
· **דצמבר 2026 ריק לגמרי** — ראה סיכון ⑤.

### 1ג · ארבעת החורים שהכרעה 17 מונה

```sql
select (select count(*) from assignments where personal_bonus > 0)  bonus_rows,
       (select count(*) from salary_reports)                        salary_reports,
       (select count(*) from project_changes)                       project_changes,
       (select count(*) from projects)                              projects_total,
       (select count(*) from projects where project_status='event_finished') stuck_closings,
       (select count(*) from seed_registry)                         registry_rows,
       (select count(*) from seed_registry where entity_type='project') registry_projects;
```
⇒ **בונוסים 0 · דוחות-שכר 0 · שינויי-תכולה 16 · 827 פרויקטים · 6 "הסתיים-האירוע" · מרשם 2,250
שורות מהן 823 פרויקטים** (אצווה יחידה: `seed-full-2026-09-03`).

### 1ד · שאר שורות פרופיל-העסק (הכרעה 17)

```sql
select extract(year from p.final_event_date)::int yr, count(*) projects,
       round(sum(fm.revenue)) revenue, round(sum(fm.gross_profit)) profit,
       round(100.0*sum(fm.gross_profit)/nullif(sum(fm.revenue),0),1) margin_pct
  from projects p join lateral finance_project_money(p.project_id) fm on true
 where p.project_status='finished' group by 1 order by 1;
```
⇒ **2024: 209 · 1,364,672 ₪ · 57.6% | 2025: 286 · 1,955,575 ₪ · 57.9% | 2026: 206 · 1,431,796 ₪ · 59.3%.**

```sql
select (select count(*) from customers)                          customers,
       (select count(*) from hostesses where status='active')    active_hostesses,
       (select count(*) from hostesses)                          hostesses_total,
       (select count(*) from project_finance where written_off)  written_off,
       (select count(*) from projects p left join project_finance pf using(project_id)
         where p.invoice_sent and p.payment_date is null and coalesce(pf.written_off,false)=false) open_invoices;
```
⇒ **61 לקוחות · 50 פעילות מתוך 186 · 7 חובות-אבודים · 11 חשבוניות פתוחות.** ✅ ארבע השורות
האלה **כבר עומדות ביעד** ואין לגעת בהן.

### 1ה · גיל-החוב — הטבלה שמכתיבה את לוח-התאריכים של הסגירה

```sql
select p.project_id, p.final_event_date, p.project_status,
       p.operationally_closed_at::date opc, p.invoice_sent_at::date inv,
       (current_date - p.invoice_sent_at::date)      age_today,
       (date '2026-10-01' - p.invoice_sent_at::date) age_conference,
       c.customer_type
  from projects p join customers c on c.customer_id = p.customer_id
 where p.invoice_sent and p.payment_date is null
 order by p.invoice_sent_at;
```

⇒ **13 שורות** (11 פתוחות + 2 שנסגרו כחוב-אבוד: 823 · 1108). המדרגים:

| מדרג | **היום (06/09)** | **ביום-הכנס (01/10), אם לא נעשה דבר** |
|---|---|---|
| 0–30 יום | **7** | 🔴 **0 — ריק** |
| 31–60 | 🔴 **0 — ריק** | 7 |
| 61–90 | 1 (#1537) | 1 (#1537) |
| 90+ | 3 | 3 |

🔴 **וזה הממצא שהופך את הכיול לחלק מהתוכנית ולא לליטוש:** **המדרגים נגזרים מ-`current_date`
ולכן זזים כל יום.** מדרג שממלאים היום נמצא ביום-הכנס במקום אחר. **הכיול חייב להתכוון לשני
התאריכים בבת-אחת** — לכן צעד 2 קובע תאריכי-חשבונית שפרושים על ~150 יום ולא על נקודה אחת.

### 1ו · הסתירה של דוח 5 — נפתרה, אינה חוסמת

`seed-plan.md` השאיר פתוח *"175 מול 61"*. **נמדד היום, ושתי המדידות נכונות — הן פשוט מודדות דברים
שונים:**

```sql
with dev as (select fm.budget_deviation, (fm.labor_cost - fm.budget_deviation) planned
  from projects p join lateral finance_project_money(p.project_id) fm on true
 where p.project_status='finished' and fm.budget_deviation is not null)
select count(*) n,
  round(percentile_cont(0.5) within group (order by budget_deviation/nullif(planned,0)*100)::numeric,1) median_pct,
  round(percentile_cont(0.9) within group (order by budget_deviation/nullif(planned,0)*100)::numeric,1) p90_pct,
  count(*) filter (where planned>0 and     budget_deviation /planned > 0.15) signed_over_15,
  count(*) filter (where planned>0 and abs(budget_deviation)/planned > 0.15) abs_over_15
from dev;
```
⇒ **701 · חציון ‎-0.1% · p90 = 16.6% · חתום (חריגה-מעלה) = 81 (11.6%) · מוחלט = 175 (25.0%).**
**ההגדרה הנעולה ב-§📑 #5 היא החתומה ⇒ 81 · 11.6%.** ✅ **הפריט נסגר; אינו חוסם את פזה א'.**

### 1ז · מה שאסור לגעת בו — ארבע השורות שמחוץ למרשם

```sql
select p.project_id, p.event_name, p.final_event_date, p.project_status
  from projects p left join seed_registry r
    on r.entity_type='project' and r.entity_id = p.project_id
 where r.entity_id is null order by p.project_id;
```
⇒ **#11 "כנס טכנולוגיה שנתי" (20/10, `ready`) · #12 "כנס משקיעים שנתי" (19/08, `finished`) ·
#15 "ערב השקה — קמפוס צפון" (07/09, `cancelled`, דמי-ביטול לא נפתרו) · #16 "כנס שנתי" (09/10,
`not_started`).**

🔴 **תיקון-עובדה שנמצא תוך-כדי, ושייכתב ב-write-back:** `scripts/demo-seed.mjs` בכותרתו וגם
`seed-data-spec.md §א׳` מדברים על *"פרויקטים 3/7/8/13/14"* כעוגני-הדמו, ועל **"פרויקט 8"**
כפרויקט שמוקאפי מודול 6 מצוירים עליו. **נמדד היום: הפרויקטים 3 · 7 · 8 · 13 · 14 אינם קיימים
במסד** (שאילתה על `project_id <= 20` מחזירה בדיוק את הארבעה שלמעלה). ⇒ **ההוראה "אל תיגע
בפרויקט 8" ריקה — העוגנים החיים הם 11 · 12 · 15 · 16**, והם מוגנים גם מכנית: `seed_backdate_project`
זורקת *"פרויקט % אינו במרשם-הזריעה — לא נוגעים בו"*.

---

## 2 · היעד המספרי — החשבון, בלי לעגל את הפער

### 2א · כמה פרויקטים צריכים להיסגר

| חודש | R11 היום | פרויקטים לסגור | מהם: כבר עברו (א1) | מהם: מחכים לתאריך (א2) | R11 אחרי |
|---|---:|---:|---:|---:|---:|
| **יולי** | 14 | 1 (#1537, `awaiting_payment`) | 1 | 0 | **15** |
| **אוגוסט** | 5 | 13 + יישוב דמי-ביטול #1564 | 14 | 0 | **19** |
| **ספטמבר** | 0 | 29 | 5 *(01–03/09)* | **24** *(06–29/09)* | **29** |
| **סה"כ** | 19 | **43 + 1 ביטול** | **20** | **24** | **63** |

**החשבון:** 43 = 1 (יולי) + 13 (אוגוסט: 7 `awaiting_payment` + 5 `awaiting_invoice` + 1 `event_finished`)
+ 29 (ספטמבר: 5 `event_finished` + 7 `ready` + 17 `in_progress`).
**24 מתוכם חסומים מכנית היום** — עובדה ②.

🔴 **ואיפה החשבון נשבר, כי `record_payment` מסרבת לתאריך עתידי:** פרויקט נכנס ל-R11 רק אחרי
`archive_project`, ולזה נדרש תאריך-תשלום. תנאי-התשלום שהזריעה עצמה מכתיבה (`seed-data-spec.md`
item 13: פרטי 25–45 · הפקה 20–40 · ממשלתי 50–95 · עמותה 30–60) **דוחפים את רוב תשלומי-אוגוסט
אל אחרי 06/09.** נבדק פר-שורה מול טבלת 1ה: **מתוך שבעת פרויקטי `awaiting_payment` של אוגוסט,
רק ל-#1553 (חברה-פרטית, חשבונית 09/08) יש היום תאריך-תשלום סביר** (28 יום). ⇒ **R11 של אוגוסט
מגיע ל-19 רק בריצת א2.** בא1 הוא יגיע ל-**7** (5 + #1553 + יישוב #1564).

**⇒ סדרת-R11 של 2026 בסוף א1:** 31·35·38·24·37·31·**15**·**7**·**5**
**⇒ ובסוף א2, ביום-הכנס:** 31·35·38·24·37·31·**15**·**19**·**29** — **בלי חודש שקורס.**

### 2ב · הנפח — כאן ישי הכריע פעמיים, אחרת

| המקור | מה הוא אומר על יולי–אוגוסט |
|---|---|
| **הכרעה 17** (06/09/2026 20:5X) | *"~25–35 אירועים בחודש **בלי חודש שקורס**"* ⇒ *"להוסיף נפח לחודשים האלה עד ~30 לחודש"* |
| **`seed-data-spec.md §ג׳`** (03/09/2026, בהאצלת-ישי *"תניח הנחות הגיוניות וסגור את זה"*) | **יולי–אוגוסט = "חלש" עסקי** — חופשת-הקיץ הישראלית. **וזה מקודד בקוד:** `scripts/seed-lib/plan.mjs`, `SEASONALITY` ⇒ `7: 0.65` · `8: 0.7` · `9: 1.2` |

**הפער מול המודל של הזריעה עצמה:** יעד-יולי ≈ 38×0.65 ≈ **25**, בפועל **15**. יעד-אוגוסט ≈ 38×0.7 ≈
**27**, בפועל **19**. ⇒ **גם לפי הטבלה שלו-עצמו יולי חסר ~10 אירועים ואוגוסט ~8.**

🔴 **וזו הכרעה שלו, לא שלי** (כלל-הסתירה ②: *"הוא הכריע פעמיים, אחרת"*). **ההמלצה: השלמה חלקית —
יולי +8, אוגוסט +6** ⇒ 23 · 25 · 30. זה סוגר את הפער מול המודל, שומר על שקע-קיץ מתון שהוא סיפור
נכון ואפשר להסביר אותו בכנס במשפט אחד (*"קיץ חלש בעסקי — זו העונתיות של הענף"*), ואינו דורש
לשטח את `SEASONALITY` — כלומר אינו מבטל הכרעה קודמת שלו. **החלופה שלו:** יולי +15, אוגוסט +11
⇒ שלושה חודשים שטוחים על ~30, ואז `SEASONALITY` בקוד סותר את הדאטה.

⚠️ **ולפני שמבצעים — צעד 7 מסביר למה זה עולה יותר מכל שאר התוכנית יחד:** לגנרטור **אין מצב
השלמה**; הוא מתוכנן לריצת-חלון-מלא.

### 2ג · אוקטובר ואילך — "מאויש, לא סגור"

```sql
select p.project_id, p.final_event_date, p.project_status, p.required_hostess_count,
       count(a.*) filter (where a.assignment_status='finally_approved') fa,
       count(a.*) filter (where a.assignment_status='pending') pend
  from projects p left join assignments a on a.project_id = p.project_id
 where p.final_event_date >= '2026-10-01' and p.project_status <> 'cancelled'
 group by 1,2,3,4 order by p.final_event_date;
```
⇒ **אוקטובר 21 אירועים לא-מבוטלים · נובמבר 18.** **5 מהם באפס-משובצות** (#1600 · #1602 · #1603 ·
#1604 · #16) — בדיוק שורת-הגיבור *"אירוע קרוב שטרם אויש"* שהכרעה 17 מבקשת. **55 זימונים `pending`
על אירועים עתידיים** (ספטמבר 11 · אוקטובר 26 · נובמבר 18) — מספיק ל-מ26 גם אחרי שספטמבר ייסגר
(44 יישארו). ✅ **אין כאן פער-זריעה.**

---

## 3 · הצעדים

> **כל צעד:** מה נכתב · המנגנון (עוגן-גריפ) · הפיכות · שער-הקלדה · זמן · שאילתת-אימות.
> **ההרשאה:** כל הפעולות רצות דרך `connectAsCeo()` (`scripts/seed-lib/db.mjs`) — **סשן-מנכ"ל אמיתי**,
> לא Management API. ⚠️ **ההבחנה חשובה:** `assert_module_permission` נשענת על `current_user_role_id()`
> שנגזר מה-JWT; **דרך Management API אין JWT והפונקציות ייפלו** — וזה בדיוק המקום שבו `seed-plan.md`
> כתב *"`generate_salary_report` אינה ניתנת להרצה"*. **התיקון: היא כן ניתנת — דרך סשן-מנכ"ל.**

### צעד 0 · קדם-טיסה — קריאה בלבד · **אין שער** · ~15 דקות

1. להריץ מחדש את כל שאילתות סעיף 1 ולרשום את המספרים החדשים לצד הישנים.
2. `git fetch origin && git log origin/dev..HEAD --oneline` — ריק ⇒ ענף טרי, מתחילים לעבוד;
   לא-ריק ⇒ לבדוק שהענף לא מוזג (כלל ברזל 10, כולל הסייג של 12/08).
3. לוודא שאיש אינו כותב במקביל (כלל 16) — **הזריעה נוגעת בכל המסד**, וסשן-מוקאפים שרץ במקביל
   קורא ממנו.
4. `node scripts/seed-assert.mjs --batch seed-full-2026-09-03` — **צילום-בסיס לפני**, כדי שאפשר
   יהיה להשוות אחרי. *(שלוש טענות אינן שאילתות ומדווחות כתזכורת — כתוב בכותרת הסקריפט.)*
5. **אימות:** הפלט של 1ב מודפס בצ'אט לפני שנוגעים בכלום.
6. ✏️ 23:0X **הגנת-בדיקות-E2E** (`processes-approved.md §🧪`) — לוודא שלא נוצר ליטרל-נעוץ-בדאטה חדש
   ב-`e2e/` מאז 06/09/2026: `grep -rn '₪' e2e/*.js` (הכול חייב להיות תבנית/בדיקת-נוכחות, לא סכום
   פרטני) ו-`grep -rnoE 'toHaveCount\([1-9][0-9]*\)' e2e/*.js` (חמש התוצאות הידועות — ר' 🧪 — חייבות
   להישאר כפי שהן: DOM-בלבד או מיורטות-רשת, לא live-data). נמצא ליטרל חדש ⇒ עוצרים ומשכתבים את
   **הטענה** (התבנית: `seed-build-handoff.md §7א ②`), לא ממשיכים לצעד 1.

---

### צעד 1 · שינויי-תכולה על פרויקטים **פתוחים** — 🔓 הפיך-בתנאי · 👤 שער · ~1 שעה

**מה נכתב:** 8–12 שינויי-תכולה נוספים על פרויקטים של **אוקטובר–נובמבר** שטרם נסגרו תפעולית,
בארבעת הדפוסים הקיימים (עדכון-צפי-אורחים · דיילת נוספת · פריט חדש · צמצום).

**המנגנון:** `apply_scope_change(p_project_id, p_lines, p_reason)` — ה-RPC האמיתי, אותו אחד
שהמסך קורא. **עוגן:** `scripts/demo-seed.mjs` ו-`seed-data-spec.md` item 26 מתעדים 12 שינויים
שכבר הודגמו בו.
**הגבלה שנמדדה היום:** הפונקציה נופלת עם *"האירוע כבר נסגר תפעולית ולא ניתן לשנות את תכולתו"*
על כל פרויקט ב-`awaiting_invoice`/`awaiting_payment`/`finished` או עם `operationally_closed_at`
לא-ריק. ⇒ **מועמדים חוקיים = אוקטובר ונובמבר בלבד** (ספטמבר ייסגר בא2, ולכן אפשר גם עליו —
אבל אז שינוי-התכולה חייב לקדם את א2).
🚫 **לא לגעת ב-#11 · #16** (מחוץ למרשם).

**הפיכות:** **הפיכה-בתנאי.** `project_changes` היא `deny-all` (0 policies) ⇒ אין מחיקה מהאפליקציה.
המחיקה היחידה היא בקסקדה ממחיקת הפרויקט-האב, כלומר דרך `seed_reset` של האצווה כולה.
**מה שכן הפיך במלואו:** שינוי-תכולה נגדי (`target_qty` חזרה לערך המקורי) — שמשאיר שתי שורות
במקום אפס. ⇒ **מומלץ להריץ במקבץ קטן ולבדוק בעין לפני שמרחיבים.**

**שער-הקלדה:** ✔ **נדרש** (הכרעה 25: *"כל כתיבה עוברת שער-הקלדה של ישי"*).

**אימות:**
```sql
select count(*) rows, count(distinct project_id) projects,
       count(*) filter (where delta_qty>0) additions, count(*) filter (where delta_qty<0) reductions,
       round(sum(delta_qty*unit_price_snapshot)) revenue_impact,
       round(sum(delta_qty*unit_cost_snapshot))  cost_impact
  from project_changes;
```
**עובר אם:** `rows` עלה ב-8–12, `projects` עלה ב-6+, ואין שורה על 11/15/16.

🔴 **ומה שהצעד הזה לא יתקן, ואין לנסות:** `logistics.project_change_id` יישאר **0 שורות**.
נמדד היום: המחרוזת `project_change_id` **אינה מופיעה בגוף `apply_scope_change` כלל** (`position(...)`
⇒ 0 על פונקציה בת 15,505 תווים). ⇒ **זו עובדת-קוד, לא פער-זריעה.** `seed-plan.md` §דוח 11 ביקש
"בדיקת-קוד בבנייה" — **הבדיקה בוצעה, וזו התשובה.** דוח 11 ימשיך למנות בדיוק 3 שורות "ללא
מקור-עלות" (סייג ㉗), וזה מאושר.

---

### צעד 2 · **עשרה פרויקטים ראשונים (יולי + תחילת-אוגוסט) — ✏️ 23:0X אינו עוד עצירה, ר' הכרעה 27** · ❌ לא הפיך · 👤 שער · ~1.5 שעות

✏️ 23:0X **מתוקן בהכרעה 27:** מקבץ-10 נשאר כ**סדר-ביצוע** (הוא זה שהתאריכים בטבלת-הכיול למטה
מכוילים סביבו), **אבל אינו עוד שער-עצירה-לאישור-באמצע.** ממשיכים ישירות לצעד 3 ואז לצעד 4 —
אימות מלא רץ **אחרי** שלושתם (צעד 4א), לא רק אחרי העשרה.

**מה נכתב:** מסע-הסגירה המלא על **10 פרויקטים ראשונים מתוך הרצף**: #1537 (יולי) + תשעה מאוגוסט.

**המסלול, בסדר הזה בדיוק** *(נגזר מגופי ה-RPC-ים החיים; כל שלב חוסם את הבא)*:

| # | הפעולה | ה-RPC | מה הוא דורש (נמדד היום) |
|---|---|---|---|
| ① | סגירה תפעולית | `close_project_operationally` | סטטוס `event_finished` · דוח-סיכום לא-ריק · שעות 0.5–24 · אורחים ≥0 · **שורה לכל דיילת `finally_approved`, בדיוק אחת** (*"חסר סימון נוכחות ל-%"* / *"מופיעה פעמיים"*) |
| ② | משוב | `mark_feedback_survey_sent` → `record_feedback` | חובה לפני ארכוב — `archive_project` דורשת `feedback_status in ('completed','no_response')` |
| ③ | חשבונית | `record_invoice_sent` | סטטוס `awaiting_invoice`; **קובעת `invoice_sent_at = now()`** ⇒ חייב תיארוך-אחורה ב-⑤ |
| ④ | תשלום | `record_payment` | **תאריך לא-עתידי**; מעביר את התיק לכיוון ארכוב |
| ⑤ | ארכוב | `archive_project` | תשלום **או** `written_off` · משוב-פתור · `summary_report_url` · ואם הציון < `סף_שביעות_רצון` — סיבה שלילית |
| ⑥ | תיארוך-אחורה | `seed_backdate_project` | **רק על שורה שבמרשם** · מזיז `operationally_closed_at` · `invoice_sent_at` · `archived_at` |

**המנגנון:** אותו רצף שכבר מומש ועובד ב-`scripts/demo-seed.mjs` (חפש `closeAndSettle`) — **אין
לכתוב אותו מחדש.** הצעד = סקריפט-הרצה קצר שקורא לרצף הזה על רשימת מזהים.

**לוח-התאריכים שנקבע כאן — וזה החלק שאסור לאלתר** *(נגזר מטבלת 1ה ומכלל "המדרגים זזים")*:

| הרכיב | הכלל | הנימוק |
|---|---|---|
| `operationally_closed_at` | אירוע + 1–5 ימים (חציון 3) · **לשניים-שלושה מהמקבץ: 9–14 ימים** | היום `>7 ימים` מחזיר **0 שורות מ-712** — תת-הדוח "צווארי-בקבוק" של דוח 7 ריק לגמרי |
| `invoice_sent_at` | סגירה + 1–5 ימים · **לאחד: 10–12 ימים** | היום `>7 ימים` מחזיר **שורה אחת מ-712** |
| `payment_date` | לפי סוג-לקוח (25–45 / 20–40 / 50–95 / 30–60) · **ולעולם לא > `current_date`** | סיפור ② (68 מול 34 יום) מת אם משטחים את זה |
| **מי נשאר פתוח** | #1537 **אינו משולם**; חשבוניתו מוזזת מ-06/07 ל-**15/07** | **53 יום היום ⇒ ממלא את מדרג 31–60 הריק · 78 יום ביום-הכנס ⇒ ממלא את 61–90.** קריאת-`seed_backdate_project` **אחת** |
| חשבוניות אוגוסט החדשות | 5 ה-`awaiting_invoice` (אירועים 16–27/08) מקבלים חשבונית מתוארכת **01–08/09** | 23–30 יום ביום-הכנס ⇒ **ממלא את מדרג 0–30 שאחרת יהיה ריק שם** |

**הפיכות:** ❌ **לא.** `archive_project` נועל את התיק, ואין ביטול-ארכוב (עובדה ①). ✏️ 23:0X
**מקבץ-10 נשאר חלוקה לוגית (כיול-התאריכים סביבו), לא עצירה** — ר' הכרעה 27; האי-הפיכות עצמה
לא השתנתה, רק המענה לה (אימות אחרי הרצף, לא עצירה באמצעו).

**שער-הקלדה:** ✔ **נדרש.**

✏️ 23:0X **אין עוד עצירת-ישי כאן (הוחלף בהכרעה 27).** ממשיכים ישירות לצעד 3. האימות המלא —
כולל לוח-הסטטוס של 1ב לפני/אחרי · מסך-הכספים (שלוש הלשוניות) · מסך-הבית — רץ **בצעד 4א**, אחרי
שרצף-הסגירה כולו (צעדים 2–4) הושלם.

**אימות:**
```sql
-- א. עשרת המזהים עברו ל-finished / awaiting_payment כמתוכנן
select project_id, project_status, operationally_closed_at::date, invoice_sent_at::date, payment_date
  from projects where project_id = any($1) order by project_id;
-- ב. אף חותמת לא נשארה על "היום"
select count(*) from projects
 where project_id = any($1) and operationally_closed_at::date >= current_date - 1;   -- ⇒ 0
-- ג. המדרגים בשני התאריכים
select count(*) filter (where (current_date-invoice_sent_at::date) between 31 and 60) b31_60_today,
       count(*) filter (where (date '2026-10-01'-invoice_sent_at::date) between 0 and 30) b0_30_conf
  from projects p left join project_finance pf using(project_id)
 where p.invoice_sent and p.payment_date is null and coalesce(pf.written_off,false)=false;
```
**עובר אם:** (א) 10 שורות במצב הצפוי · (ב) **0** · (ג) שני המספרים **≥1**.

---

### צעד 3 · השלמת אוגוסט — 4 הנותרים + יישוב דמי-ביטול · ❌ לא הפיך · 👤 שער · ~40 דקות

**מה נכתב:** אותו מסלול של צעד 2 על ארבעת פרויקטי-אוגוסט הנותרים, **ובנוסף** יישוב דמי-הביטול
של **#1564** (אירוע 23/08, `cancel_type='other'`, בוטל 21/08, `cancellation_fee` ריק).

**המנגנון:** `finance_cancellation_fee_proposal` → `resolve_cancellation_fee(p_action=>'bill'|'waive'|'write_off')`
— בדיוק הרצף של `settleCancellation` ב-`scripts/demo-seed.mjs`.
🚫 **לא לגעת ב-#15** — מחוץ למרשם, וגם *"ביטול שדמי-הביטול שלו טרם נפתרו"* הוא שורת-גיבור
לגיטימית שכדאי שתישאר על המסך.

**הפיכות:** ❌ לא · **שער-הקלדה:** ✔

**אימות:**
```sql
select p.project_id, p.project_status, pf.cancellation_fee, pf.final_profit
  from projects p left join project_finance pf using(project_id)
 where p.project_id = 1564;                     -- ⇒ cancellation_fee לא NULL
select count(*) from projects
 where final_event_date between '2026-08-01' and '2026-08-31'
   and project_status in ('event_finished','awaiting_invoice');   -- ⇒ 0
```

---

### צעד 4 · ספטמבר-שעבר — 5 פרויקטים · ❌ לא הפיך · 👤 שער · ~40 דקות

**מה נכתב:** מסע-הסגירה על **#1570–#1574** (אירועים 01–03/09, כולם `event_finished`), ועל **#1575**
(06/09) **רק אם עבודת-הלילה כבר העבירה אותו ל-`event_finished`** — היא רצה 02:00 והתנאי הוא
`final_event_date < current_date`.

⚠️ **הבדל מצעד 2:** לחמשת אלה **אין עדיין סימוני-נוכחות** (`marked = 0`), בעוד פרויקטי-אוגוסט
כבר נסגרו תפעולית. ⇒ **צריך לייצר תוויות-נוכחות**, ורק לפי ההתפלגות הקיימת שנמדדה: הגיעה 3,319 ·
איחרה 360 (קל 183 · בינוני 136 · כבד 41) · לא-הגיעה 208. **ה-CHECK `assignments_attendance_shape`
אוכף את הצורה** (`late` חייב `lateness_level`; `no_show` חייב `no_show_reason`; ו-`no_show` חייב
`actual_hours = 0`).
🚫 **ואין להמציא חותמת-הגעה** — ראה סעיף 4.

**הפיכות:** ❌ לא · **שער-הקלדה:** ✔

**אימות:**
```sql
select count(*) filter (where project_status='finished')       fin,
       count(*) filter (where project_status='event_finished') still_open
  from projects
 where final_event_date between '2026-09-01' and '2026-09-05';   -- ⇒ 5 · 0
```

---

### צעד 4א ✏️ 23:0X · **אימות-מלא אחרי זריעת-הפרופיל (הכרעה 27)** — קריאה-בלבד · אין שער · ~15–20 דקות

> **מחליף את עצירת-הפיילוט-אחרי-10 שהייתה בצעד 2 (הכרעה 26א).** ישי, במילותיו: *"אין צורך
> בפיילוט פשוט בצע ואמת אחר כך שיצא מה שציפת."* הרצף (צעדים 1–4: שינויי-תכולה + שלוש הסגירות
> של יולי-אוגוסט-ספטמבר-שעבר) רץ במלואו קודם; הצעד הזה הוא הבדיקה שרצה **מיד אחריו**, לפני
> בונוסים ודוחות-שכר (צעדים 5–6) — לא בסופם, כדי שתקלה תיתפס לפני שעוד כתיבות בלתי-הפיכות נערמות
> עליה.

**מה נבדק, בסדר הזה בדיוק:**
1. `node scripts/seed-assert.mjs --batch seed-full-2026-09-03`
2. `npm run smoke`
3. `npm run test:e2e`
4. `VITE_SUPABASE_URL= VITE_SUPABASE_ANON_KEY= npx vitest run` *(עוקף את מוקש השער-העיוור ל-Supabase
   שמתועד ב-`CLAUDE.md` — ריצה מקומית של `npm run gate` לבדה אינה רואה את `ProfileSettingsPage.test.jsx`
   וחבריו)*
5. `npm run gate`
6. **13 בדיקות-תקינות-המסד** (`docs/db_health_checks.md`) — הפלט הנדרש הוא `נבדקו N · נכשלו 0`,
   לא רשימה ריקה.

**ומציגים לישי, כמו שהיה מתוכנן לעצירת-הפיילוט הישנה:** לוח-הסטטוס של 1ב לפני/אחרי · מסך-הכספים
(שלוש הלשוניות) · מסך-הבית.

**אם משהו יצא לא כמצופה** *(מבחן נכשל, ספירה לא תואמת, פלט שאינו `0` כשל)* — **ההחלטה על
איפוס-מלא (`seed_reset`) היא של ישי, לא תיקון-שקט בקוד.** הסגירה בלתי-הפיכה (עובדה ①); איפוס
מלא מוחק את כל הפרויקטים הזרועים ומריץ מחדש — שעה-שעתיים, לא "בטל".

**הפיכות:** — (קריאה-בלבד, אין כתיבה). **שער-הקלדה:** לא נדרש.

---

### צעד 5 · בונוסים אישיים · ✅ הפיך · 👤 שער · ~20 דקות

**מה נכתב:** `assignments.personal_bonus` על **25–40 שיבוצים** (~1% מ-3,887 המשמרות בתשלום),
בסכומים 50–250 ₪, ורק על `finally_approved` שהגיעו/איחרו-קל.

**המנגנון:** **UPDATE ישיר דרך PostgREST כמנכ"ל** — לא RPC. **וזה חוקי ונמדד:** ל-`assignments`
יש policy מסוג `ALL` בשם `assignments_write_by_permission`, ששערה הוא `edit` על **'דיילות'**;
למנכ"ל יש. **אין CHECK על `personal_bonus`** (נבדקו כל שבעת ה-CHECK-ים של הטבלה). `scripts/demo-seed.mjs`
כבר משתמש באותו מסלול (`db.update(...)`).

**הפיכות:** ✅ **מלאה** — UPDATE חוזר ל-`0`/NULL. **⚠️ אבל רק עד צעד 6:** `generate_salary_report`
מצלמת את `coalesce(a.personal_bonus,0)` לתוך שורות-הדוח. ⇒ **בונוסים לפני דוחות-שכר, תמיד.**

**שער-הקלדה:** ✔

**אימות:**
```sql
select count(*) rows, round(sum(personal_bonus)) total,
       min(personal_bonus) lo, max(personal_bonus) hi
  from assignments where personal_bonus > 0;
```

---

### צעד 6 · דוחות-שכר — **שרשרת חודשית, לא דוח בודד** · ❌ לא הפיך · 👤 שער · ~30 דקות

🔴 **הצעד הזה משנה תשובה שישי כבר נתן, ולכן הוא מגיע אליו עם המדידה.**
**מה שהוא ענה** (`seed-plan.md` §4, שאלה 3, 06/09 17:1X): *"אוגוסט 2026"* — **דוח אחד.**
**מה שנמדד היום, וטרם היה ידוע כשהוא ענה:** `generate_salary_report` אוספת **כל** שיבוץ
`finally_approved` עם `salary_report_id is null` שתאריכו ≤ סוף-התקופה ושהפרויקט שלו סגור —
**בלי גבול תחתון.**

```sql
select count(*) sweep_lines, count(distinct project_id) sweep_projects,
       round(sum(a.actual_hours*a.hourly_rate_snapshot)) wages
  from assignments a join projects p using(project_id)
 where a.salary_report_id is null and a.assignment_status='finally_approved'
   and a.event_date <= date '2026-08-31'
   and p.project_status in ('awaiting_invoice','awaiting_payment','finished');
```
⇒ **3,887 שורות · 717 פרויקטים · 919,048 ₪ בדוח אחד.** **דוח-שכר חודשי של 919 אלף ₪ ו-3,887
שורות הוא הדבר הראשון שמהנדס בקהל יעצור עליו.**

**ההמלצה: שרשרת של 32 דוחות חודשיים** — `generate_salary_report('2024-01-01')`,
`('2024-02-01')`, … `('2026-08-01')`, **בסדר עולה**. כל דוח קולט רק את שיירי-חודשו, כי הקודמים
כבר תפסו `salary_report_id`. **מה שזה קונה:** דוח 16 ("שכר ונסיעות לפי חודש") מקבל **סדרה
של 32 נקודות במקום נקודה אחת** — וזה בדיוק מה שהדוח שואל; והפרופיל "שלוש שנים במערכת" נשמר גם
בשכר. **עלות:** 32 קריאות, דקות.
➕ **ולסיום מציאותי:** `finalize_salary_report(p_report_id, p_file_url, p_status)` — כל הדוחות
נולדים `pending`; לסמן את 31 הראשונים כנשלחו ולהשאיר את **אוגוסט 2026 `pending`**, כדי שגם
מצב-"טרם נשלח" יופיע על המסך.

**הפיכות:** ❌ **לא.** `salary_reports` נושאת policy-**קריאה בלבד** (`salary_reports_select_by_permission`),
וכך גם `salary_report_lines` ⇒ **אין מחיקה מהאפליקציה.** הסרה תדרוש מיגרציה — **הכרעת-ישי, לא
תיקון-לילה.** `salary_reports_period_key` (UNIQUE על `period`) גם חוסם הפקה חוזרת של אותו חודש.

**שער-הקלדה:** ✔ **וגם הכרעת-ישי על ההיקף** (32 מול 1).

**סדר:** **אחרי צעדים 2–5, לעולם לא לפניהם.** `seed-data-spec.md` item 25: דוח-אוגוסט קודם
נמחק כי *"71 מ-72 משמרות-אוגוסט לא היו בו — הופק לפני הזריעה"*.

**אימות:**
```sql
select sr.report_id, sr.period, sr.send_status, count(l.*) lines, round(sum(l.line_total)) total
  from salary_reports sr left join salary_report_lines l using(report_id)
 group by 1,2,3 order by sr.period;
```
**עובר אם:** 32 שורות · **אף אחת אינה עולה על ~60 אלף ₪** · הסכום הכולל ≈ 919K + נסיעות + בונוסים.

---

### צעד 7 · השלמת-נפח ליולי–אוגוסט — 🟡 **מותנה בהכרעת-ישי (2ב)** · ✅ הפיך · 👤 שער · ~3–4 שעות

**מה נכתב:** ~14 אירועים חדשים (יולי +8 · אוגוסט +6) — הצעה → אישור → שיבוצים → לוגיסטיקה →
מסע-סגירה מלא, **באצווה נפרדת** (למשל `seed-jul-aug-2026`).

🔴 **וכאן חייבת להיאמר בעיה, כי היא מה שהופך את הצעד הזה ליקר מכל השאר יחד: לגנרטור אין מצב
"השלמה".** `buildPlan` (`scripts/seed-lib/plan.mjs`) בונה **חלון מלא** מ-`--from` ועד
`futureUntil`, וגוזר את הנפח מעקומת-צמיחה `baseStart: 12 → baseEnd: 38` **על-פני החלון כולו**.
ריצה עם `--from 2026-07-01` תיתן עקומה שגויה, **וגם תיצור ישויות שלא ביקשנו:** `Math.round(160 * scale)`
לקוחות חדשים ו-`max(5, round(50*scale) - existingActive)` דיילות — כלומר **לפחות 5 דיילות ו-~16
לקוחות** גם ב-`--scale 0.1`. **זה ישבור את שתי שורות-הפרופיל שכבר עומדות ביעד** (61 לקוחות · 50
פעילות).

**⇒ שלוש דרכים, ואני ממליץ על השנייה:**

| # | הדרך | מחיר | הערכה |
|---|---|---|---|
| א | ריצה כמו-שהיא עם `--from 2026-07-01 --scale 0.1` | שוברת את 61/50 | 🚫 |
| **ב** | **דגל צר חדש ב-`demo-seed.mjs`** — `--months 2026-07,2026-08 --reuse-parties` (יעד-נפח מפורש לחודש; **אפס** לקוחות/דיילות חדשים; שאר הצנרת ללא שינוי) | ~3 שעות עבודת-סקריפט · **אינה מיגרציה** ⇒ **שער-הקלדה של ישי אינו נדרש עליה, רק על ההרצה** | ✅ **מומלץ** |
| ג | לא לזרוע נפח כלל | יולי נשאר 15 | חוקי — 2ב היא הכרעתו |

**הפיכות:** ✅ **מלאה** — אצווה נפרדת ⇒ `node scripts/demo-seed.mjs --reset seed-jul-aug-2026`
מוחקת בדיוק אותה, בסדר-FK, בלי לגעת ב-823 של האצווה הראשית.

**מוקשים ידועים לצעד הזה:**
· **`assignments_one_event_per_day`** — אינדקס ייחודי על `(hostess_id, event_date)` כש-`finally_approved`.
  `loadExisting` קורא את `bookedDates` לפני התכנון, אז ריצה יחידה בטוחה; **שתי ריצות במקביל אינן.**
· **`VOLUME.dayCap = 3`** — התקרה של ישי; `dayCounts` כבר מחסיר את `legacyEventDates`.
· **`enforce_quote_in_progress_lock`** — ההצעה נוצרת עם תאריך-מציין ב-2030, מאושרת, ורק אז
  מוזזת. `seed_backdate_quote` עוברת **רק** על הצעה שבמרשם; ההסתייגות בטריגר דורשת **גם** את
  מפתח-הסשן **וגם** רישום במרשם.
· **`updated_at` אינו ניתן לתיארוך-אחורה** (`moddatetime` דורס) — מוצהר בכותרת המיגרציה. ⇒
  אירועים חדשים ייראו "עודכנו היום" בכל מקום שמציג `updated_at`.

**שער-הקלדה:** ✔ על ההרצה.

**אימות:** להריץ מחדש את 1ב ⇒ יולי ≈ 23 · אוגוסט ≈ 25 · ולוודא
`select count(*) from customers` = 61 ו-`... hostesses where status='active'` = 50 **ללא שינוי**.

---

### צעד 8 · **ריצת א2 — 29–30/09/2026** · ❌ לא הפיך · 👤 שער · ~2 שעות

**מה נכתב:** אותו מסע-סגירה על **24 פרויקטי ספטמבר** שתאריכם עבר בינתיים · תשלומי-אוגוסט
שהבשילו (שישה מ-`awaiting_payment`) · `generate_salary_report('2026-09-01')` · וכיול אחרון של
מדרגי-גיל-החוב מול **התאריך שבו הכנס באמת מתקיים**.

**למה זה חייב להיות ריצה נפרדת:** עובדה ② — `close_project_operationally` דורשת `event_finished`,
ו-`record_payment` מסרבת לתאריך עתידי. **אין דרך לעקוף את שתיהן מבלי להזיז תאריכי-אירוע, וזה
היה הורס את הצורה של ספטמבר.**

**מה קורה אם א2 לא רצה — וזה חייב להיאמר לישי עכשיו, לא ב-30/09:** ביום-הכנס עבודת-הלילה כבר
תעביר את כל 24 האירועים ל-`event_finished` ⇒ **המסך יראה 24 "סגירות תקועות"**, בעוד הכרעה 17
מבקשת *"כמה סגירות שנתקעו, לא הרוב"*, ורצועת "מה דורש טיפול" במסך-הבית תתמלא בהן. ⇒ **א2 אינה
"נחמד שיהיה"; היא חצי מהתוכנית.**

**שער-הקלדה:** ✔

**אימות:** 1א ⇒ R11 של ספטמבר = 29 · 1ב ⇒ `event_finished` בספטמבר = 0 · 1ה ⇒ ארבעת המדרגים
מאוכלסים **ביחס לתאריך הכנס**.

---

## 4 · מה אסור — הרשימה הסגורה

> **מקור:** `processes-approved.md` §"מסלולים שנשללו כ'לא-קורה'" (הכרעות-מציאות של ישי) +
> §🗳️ + הלקח של מ7. **כל שורה כאן היא ציטוט או מראה, לא כלל של הקובץ הזה.**

**1. 🚫 חותמות צ'ק-אין / הגעה.** *"12 (צ'ק-אין) מוכרע-החוצה כתהליך שאינו קיים, לא כדחייה"* ·
ההוראה: *"אין לבנות מסלול לזה; אין לשאול שוב. הנוכחות נשארת תווית ידנית בסגירה התפעולית."*
⇒ צעד 4 מייצר `attendance_status` בלבד — **אין עמודת-חותמת ואין להוסיף.** *(D2 בלדג'ר: העמודה
אינה קיימת.)*

**2. 🚫 `recommended_rank` מומצא.** `seed-plan.md` §דוח 14א: *"אין מילוי-לאחור… לא מספר מומצא"*.
**העמודה אינה קיימת בסכימה** — היא פזה ב'. הדוח יציג *"נמדד מ-<תאריך>"*, וזה בכוונה.

**3. 🚫 סיווגי-AI כתובים-מראש — האיסור החמור ביותר.** `seed-plan.md` §סיפור ④:
*"אסור לכתוב ל-`feedback_ai_insights` תוצאות-סיווג בלי שהמודל באמת רץ… כאן זו לא רק דאטה
מיוצרת, זו תשובת-AI מבוימת."* **`feedback_ai_insights`/`feedback_ai_runs` אינן קיימות ⇒ פזה ב'
⇒ מחוץ להיקף הקובץ הזה לחלוטין.**

**4. 🚫 שום דבר שיגרום למודל "לגלות" את הרעש שזרענו.** הלקח של מ7 (`seed-data-spec.md §ט׳`):
*"מודל שיתאמן על עקומת-הצמיחה 'יגלה' את הרעש שזרענו"*. ⇒ לא לכייל תאריך או סכום אחורנית כדי
שמדד יֵצא עגול. **ג'יני 0.4925 · חציוני-התשלום 68.5/46/34/31 · הפארטו 59.7% — כולם נשארים
כפי שהם.**

**5. 🚫 רספונסיביות/מובייל · 🚫 סיווג-מחדש של משוב.** שני "לא קורה" מפורשים של ישי
(06/09 09:4X): *"שניהם לא קורה"*. אין לזרוע דאטה שמניחה אותם.

**6. 🚫 העוגנים שהוזנו-ביד: #11 · #12 · #15 · #16.** אינם במרשם ⇒ `seed_backdate_project`
תזרוק *"אינו במרשם-הזריעה — לא נוגעים בו"*. **#12 הוא גם עוגן חי של `npm run smoke`** —
`smoke-anchors.json` דורש שהוא יימצא באחת משלוש לשוניות-הכספים.
🔴 **ו-`--reset` מסוכן בדיוק כאן:** `seed_reset` מוחקת **את כל האצווה** בקסקדה
(`project_finance` → `projects` → `quotes` → `customers` → `hostesses`). `seed-data-spec.md §ו׳`
מזהיר: *"ה-reset חייב להיות מוגבל לשורות-מסומנות-כזריעה בלבד"* — **וזה אכן מה שהמיגרציה עושה**,
אבל `--reset seed-full-2026-09-03` עדיין מוחק **823 פרויקטים** בקריאה אחת. ⇒ **לעולם לא להריץ
`--reset` על האצווה הראשית כדרך לבטל צעד.**

**7. 🚫 לא להמציא שורות ל-E2E ולא להזריק שורות-בדיקה.** `e2e/CLAUDE.md`: *"כל בדיקה רצה מול
המסד החי ⇒ אסור להזריק שורות-בדיקה למסד"*. הזריעה עצמה היא המנגנון המאושר; **בדיקות אינן.**

---

## 5 · סיכונים — מה יכול להישבר

**① 🔴 אי-הפיכות (הסיכון הגדול ביותר).** אין ביטול-ארכוב, אין ביטול-דוח-שכר, ואין policy-כתיבה
ל-`projects`/`project_finance`/`salary_reports`. ⇒ **43 סגירות + 32 דוחות-שכר הן פעולה חד-כיוונית.**
**ההפחתה:** עצירת-ישי אחרי 10 (צעד 2) · אימות אחרי כל צעד · ואם משהו יצא לא-נכון —
**התיקון הוא מיגרציה, כלומר הכרעת-ישי, לא תיקון בשקט.**

**② 🔴 המדרגים והחלונות זזים עם `current_date`.** גיל-חוב · "פג בקרוב" · "אירוע קרוב" · שני
טריגרי-הענבר — **כולם נגזרים ב-JS מול היום** (`seed-data-spec.md` טענה 13ב: *"יעברו פעם אחת
ואז יהפכו שקריים בשקט בעודם נראים ירוקים"*). ⇒ כל תאריך שנקבע בצעד 2 חייב להיבדק **פעמיים**:
מול היום ומול 01/10.

**③ 🔴 24 אירועי ספטמבר חסומים מכנית היום** (עובדה ②) ⇒ בלי צעד 8, ביום-הכנס יופיעו **24
סגירות תקועות** במקום "כמה".

**④ 🔴 סחיפת-דוח-השכר — 3,887 שורות בקריאה אחת** (צעד 6). נמדד, לא משוער.

**⑤ 🟡 צינור-העבודה קדימה קפוא ב-03/09.** `futureFactor` בגנרטור: חודש-אחד-קדימה 0.9 ·
שניים 0.45 · שלושה **0**. ⇒ **דצמבר 2026 = 0 אירועים · נובמבר 18.** ביום-הכנס, "חודש הבא"
(נובמבר) יראה 18 ו"בעוד חודשיים" (דצמבר) יראה **אפס** — בעוד `SEASONALITY[12] = 1.3` הוא **שיא
השנה** לפי המחקר של `seed-data-spec.md §ג׳` (*"דצמבר הוא שיא הכנסים בישראל"*). **דאטה שסותרת
את הסיפור שהמצגת מספרת.** ⇒ **אם צעד 7 נבנה בכל מקרה, כדאי שאותו דגל יזרע גם 10–14 אירועי
דצמבר `not_started`/`in_progress`** — עלות שולית, וסוגר חור שנראה על המסך.

**⑥ 🟡 E2E — נבדק פר-קובץ, והתמונה טובה יותר ממה שאפשר היה לחשוש.**
· `e2e/smoke-anchors.json` **נוקה ב-03/09** — אין בו ולו ערך אחד שנגזר מדאטה חיה, פרט ל-#12.
· `smoke.spec.js` — קורא מוני-לשונית **בזמן-ריצה** ומאמת אינווריאנט-עצמי; **מטפל במפורש
  ב-`tabCount === 0`.** האסרשן הקשיח היחיד: #12 חייב להימצא באחת משלוש הלשוניות. ✅ **בטוח:**
  `src/modules/08_finance/api.js` ממיין `.order('project_id')` **עולה**, ו-#12 הוא בין המזהים
  הנמוכים ביותר ⇒ הוא נשאר בעמוד הראשון של "הסתיימו" גם אחרי 43 סגירות נוספות.
· `finance.spec.js` S3 — בורר-החודש נפתח על **החודש הקודם**; הבדיקה כבר מכסה את **שני** הענפים
  (`salary-report-preflight` **או** `salary-report-blocked-banner`). ✅ **צעד 6 לא ישבור אותה.**
· `project-closing.spec.js` — עובד על נושא ייעודי `E2E-*` בלבד; **נמדד: אין היום אף פרויקט כזה**
  ⇒ המסעות מדלגים כבר עכשיו, וצעדי-הסגירה אינם משנים זאת.
· `projects.spec.js` — מדלג כשאין פרויקט פעיל; אוקטובר–נובמבר נשארים פעילים ⇒ ✅.
· **ומה שנשאר טעון-בדיקה:** `npm run smoke` ו-`npm run test:e2e` **אינם רצים ב-CI ואינם ב-`gate`**
  (מוקש מתועד ב-`CLAUDE.md`) ⇒ **חייבים להריץ אותם ידנית אחרי צעד 2 ואחרי צעד 6**, אחרת
  *"הכול ייראה ירוק, והראשון שיגלה הוא מי שמריץ את ההדגמה"*.

**⑦ 🟡 טריגרים ואינדקסים שיירו אם משהו מאולתר:**
· `assignments_one_event_per_day` (יחיד על `hostess_id,event_date` ב-`finally_approved`) — רלוונטי
  לצעד 7 בלבד. **מצב-הכשל הוא ריצה שמתה באמצע, לא שאילתה שנכשלת בדיעבד** (`seed-data-spec.md` טענה 6).
· `enforce_quote_in_progress_lock` — הצעה מאושרת נעולה; המעקף דורש **מפתח-סשן וגם רישום במרשם**.
· `assignments_attendance_shape` · `assignments_no_show_zero_hours` — צורת-הנוכחות בצעד 4.
· `salary_reports_period_key` — הפקה חוזרת של אותו חודש נדחית בקול.
· `moddatetime` על `updated_at` — **לא ניתן לתיארוך-אחורה, בשום מסלול.**

**⑧ 🟡 המסכים שישתנו — לומר לישי לפני, לא אחרי:**
**מ8 כספים** (שלוש הלשוניות: `awaiting_invoice` 5→0, `awaiting_payment` 11→~7, "הסתיימו" 701→~744;
ודיאלוג-השכר יעבור ל"החודש כבר הופק") · **מ7 מסך-הבית** (רצועת "מה תקוע" מתרוקנת · כרטיס-הרווח
החודשי מקבל ערך לאוגוסט–ספטמבר) · **מ6 פרויקטים** (לשונית "לסגירה" מתרוקנת) · **מ2 כרטיס-לקוח**
(רצועת-ההכנסות של ~40 לקוחות זזה) · **מ5 לוגיסטיקה** (שינויי-התכולה של צעד 1 מוסיפים שורות).

**⑨ 🟡 שיחה כותבת אחת בכל רגע** (כלל 16). הזריעה נוגעת כמעט בכל טבלה; סשן-מוקאפים שקורא
מהמסד במקביל יראה תמונה שמשתנה תחת ידיו. **לתאם עם ישי לפני צעד 1.**

---

## 6 · סדר-הרצה מוצע

```
א1 — היום
  צעד 0  קדם-טיסה (קריאה בלבד) + הגנת-בדיקות-E2E                  ~15 דק'
  צעד 1  שינויי-תכולה על פתוחים (אוקטובר–נובמבר)                  ~60 דק'   👤
  צעד 2  עשרה פרויקטים ראשונים (יולי + תחילת-אוגוסט)              ~90 דק'   👤
  צעד 3  4 הנותרים באוגוסט + יישוב #1564                          ~40 דק'   👤
  צעד 4  ספטמבר-שעבר (5 פרויקטים)                                 ~40 דק'   👤
  צעד 4א ✏️23:0X אימות-מלא אחרי הרצף כולו (הכרעה 27) — אין עצירה,  ~15–20 דק' 🤖
         בדיקה. || 1ב לפני/אחרי · מסך-כספים · מסך-הבית ·
         seed-assert + smoke + test:e2e + gate + 13 בדיקות-תקינות
  צעד 5  בונוסים אישיים                                            ~20 דק'   👤
  צעד 6  דוחות-שכר — שרשרת חודשית  (אחרון בא1!)                   ~30 דק'   👤
  צעד 7  השלמת-נפח יולי–אוגוסט  (מותנה בהכרעת 2ב)              ~3–4 שעות   👤
  ─────── seed-assert + smoke + test:e2e + gate ───────

א2 — 29–30/09/2026
  צעד 8  24 פרויקטי ספטמבר · תשלומי-אוגוסט שהבשילו ·
         דוח-שכר ספטמבר · כיול-מדרגים אחרון                       ~2 שעות   👤
```

✏️ 23:0X **אין עוד עצירת-👤 קשה באמצע הרצף** (הוחלף בהכרעה 27; ההיסטוריה — עצירה אחרי צעד 2,
בתקדים זריעת מ7 — נשארת מתועדת בצעד 2 עצמו). **התחליף: צעד 4א**, ריצת-אימות מלאה מיד אחרי
שהרצף (צעדים 1–4) נגמר, ולפני שבונוסים/דוחות-שכר (צעדים 5–6) נערמים עליו.
**כל הצעדים מ-4א ואילך הם 🤖** — קלוד מאמת בעצמו בשאילתה ומדווח — **אבל כל צעד-כתיבה עדיין נושא
שער-הקלדה לפני הכתיבה**, לפי הכרעה 25 (זה לא השתנה).

**סגירה, אחרי הצעד האחרון שרץ** *(אותה רשימה כמו צעד 4א — כאן כסגירה סופית של כל הפזה, לא רק
של רצף-הסגירה)*:
`node scripts/seed-assert.mjs --batch seed-full-2026-09-03` · `npm run smoke` · `npm run test:e2e`
· `VITE_SUPABASE_URL= VITE_SUPABASE_ANON_KEY= npx vitest run` (המוקש של השער העיוור) ·
`npm run gate` · ואז **13 בדיקות-תקינות המסד** (`docs/db_health_checks.md`) — הפלט הוא
`נבדקו N · נכשלו 0`, לא רשימה ריקה.

---

## 7 · טבלת-הסיכום

| # | הצעד | המנגנון (עוגן-גריפ) | הפיך? | שער-ישי? | זמן | שאילתת-האימות |
|:-:|---|---|:-:|:-:|---|---|
| 0 | קדם-טיסה | `execute_sql` קריאה · `seed-assert.mjs` | — | ❌ | 15ד | סעיף 1 כולו |
| 1 | שינויי-תכולה על פתוחים | `apply_scope_change` | 🟡 בתנאי | ✔ | 60ד | `count(*) from project_changes` |
| 2 | 🔻 פיילוט 10 פרויקטים | `close_project_operationally` → `record_feedback` → `record_invoice_sent` → `record_payment` → `archive_project` → `seed_backdate_project` | ❌ | ✔ | 90ד | סטטוסים + `operationally_closed_at >= current_date-1` ⇒ 0 |
| 3 | השלמת אוגוסט + ביטול | אותו רצף · `resolve_cancellation_fee` | ❌ | ✔ | 40ד | `event_finished/awaiting_invoice` באוגוסט ⇒ 0 |
| 4 | ספטמבר-שעבר (5) | אותו רצף + תוויות-נוכחות | ❌ | ✔ | 40ד | `finished` ב-01–05/09 ⇒ 5 |
| 4א ✏️23:0X | אימות-מלא אחרי הרצף (הכרעה 27) | `seed-assert` · `smoke` · `test:e2e` · `gate` · 13 בדיקות-תקינות | — | ❌ | 15–20ד | `נבדקו N · נכשלו 0` בכל הבדיקות |
| 5 | בונוסים | UPDATE ישיר (`assignments_write_by_permission`) | ✅ | ✔ | 20ד | `count(*) where personal_bonus>0` |
| 6 | דוחות-שכר (32) | `generate_salary_report` · `finalize_salary_report` | ❌ | ✔ | 30ד | 32 שורות · אף דוח > ~60K ₪ |
| 7 | השלמת-נפח | `demo-seed.mjs` + דגל חדש · אצווה נפרדת | ✅ | ✔ | 3–4ש | 1ב ⇒ יולי ~23 · אוגוסט ~25 · לקוחות 61 |
| 8 | ריצת א2 (29–30/09) | אותו רצף על 24 + `generate_salary_report('2026-09-01')` | ❌ | ✔ | 2ש | R11 ספטמבר = 29 · `event_finished` = 0 |

**10 צעדים (כולל 4א ✏️ 23:0X) · 8 מהם דורשים שער-הקלדה של ישי · 2 הפיכים במלואם · 5 בלתי-הפיכים
· 1 קריאה-בלבד (4א, ללא שער).**

---

## 8 · מה נשאר לישי, ומה הכרעתי לבד

### ⏳ שלושה פריטים שאני לא יכול להכריע

| # | הפריט | העוגן שאפשר לשים עליו אצבע | ההמלצה |
|:-:|---|---|---|
| **⏳א** | **נפח יולי–אוגוסט** — הכרעה 17 (*"~30 לחודש"*) מול טבלת-העונתיות ב-`seed-data-spec.md` (*"יולי–אוגוסט חלש"*, מקודדת `0.65`/`0.7`) | **שתי הכרעות שלך, מתוארכות ומצוטטות ב-2ב** | **יולי +8 · אוגוסט +6** ⇒ 23·25·30. שקע-קיץ מתון שאתה יכול להסביר במשפט אחד, בלי לסתור את מה שהכרעת ב-03/09 |
| **⏳ב** | **דוח-שכר: 32 חודשיים או אחד לאוגוסט?** ענית *"אוגוסט 2026"* לפני שנמדד מה הוא סוחף | **נמדד: דוח בודד = 3,887 שורות · 919,048 ₪** | **שרשרת של 32.** דוח 16 שואל "כמה עלה השכר **לפי חודש**" — בגרסה של דוח-אחד יש לו נקודה אחת |
| **⏳ג** | **דצמבר 2026 ריק** (סיכון ⑤) — `SEASONALITY[12]=1.3` הוא שיא-השנה, והדאטה מראה אפס | `select count(*) from projects where final_event_date >= '2026-12-01'` ⇒ **0** | **לזרוע 10–14 אירועי דצמבר** באותו דגל של צעד 7. אם ⏳א נדחה — הפריט הזה נופל איתו |

### 🔇 מה הכרעתי לבד (בהאצלה — בר-עקיפה)

| סוג | ההכרעה |
|---|---|
| ⚙️ טכני | **פיצול פזה א' ל-א1/א2** — לא בחירה: `close_project_operationally` דורשת `event_finished` ו-`record_payment` מסרבת לעתיד |
| ⚙️ טכני | **גודל מקבץ-הפיילוט = 10** (יולי ×1 + אוגוסט ×9) — לפי תקדים מ7 |
| ⚙️ טכני | **סדר הצעדים:** שינויי-תכולה → סגירות → בונוסים → דוחות-שכר. שלושתם כפויים על-ידי המנגנונים (`apply_scope_change` חסומה אחרי סגירה · הדוח מצלם את הבונוס · הסחיפה תופסת רק פרויקטים סגורים) |
| ⚙️ טכני | **#1537 נשאר לא-משולם וחשבוניתו מוזזת ל-15/07** — הקריאה היחידה שממלאת מדרג-ריק **בשני** התאריכים |
| ✍️ ניסוח | חלוקת הצעדים, שמות-האצוות המוצעים, וניסוח שאילתות-האימות |
| 🔀 סטייה מוצהרת | **הכרעה 25 כתובה *"הפיכות דרך `seed_registry`"* — ונמדד שזה אינו נכון לצעדים 2–4·6.** לא שיניתי את ההכרעה; רשמתי את הפער בעובדה ① ובניתי סביבו עצירת-ביניים |
| 🔀 תיקון-עובדה | **פרויקטים 3/7/8/13/14 אינם קיימים** — ההוראה "אל תיגע בפרויקט 8" ריקה; העוגנים החיים הם 11·12·15·16 (סעיף 1ז). **טעון write-back ל-`seed-data-spec.md §א׳` ולכותרת `demo-seed.mjs`** |
| 🔀 תיקון-עובדה | **`generate_salary_report` כן ניתנת להרצה** — דרך `connectAsCeo()`, לא דרך Management API. `seed-plan.md §דוח 16` אמר את ההפך |
| 🔀 תיקון-עובדה | **`logistics.project_change_id` לעולם NULL** — `apply_scope_change` אינה כותבת אותו כלל (0 מופעים בגוף הפונקציה). זו תשובה סופית ל"בדיקת-הקוד" ש-`seed-plan.md §דוח 11` ביקש |
| 🔀 תיקון-עובדה | **הסתירה 175/61 של דוח 5 נפתרה** — 175 מוחלט · **81 חתום** (11.6%), וההגדרה הנעולה היא החתומה (1ו) |

</div>
