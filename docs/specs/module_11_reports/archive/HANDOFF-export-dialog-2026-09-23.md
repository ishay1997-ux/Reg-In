<div dir="rtl">

# מסירה — מ11 · חלון-הייצוא · לילה 22→23/09/2026

> **ממשיך מ־:** `docs/specs/module_11_reports/archive/HANDOFF-export-dialog-2026-09-17.md`
> 🔑 **היא עדיין תקפה לכל מה שאינה סותרת כאן** — §5 המלכודות · §4 מילותיו של ישי · §2 הגבול.
> **מה שהשתנה מאז נמצא כאן, וכאן בלבד.**

---

## 0 · שורת-המדידה (§4ג)

> 📏 נקרא לפני הכתיבה: כל מה ששני הצדדים כתבו בשיחה — 22:54 → 01:29 ·
> 3 הודעות של ישי (7KB) ו-106 שלי (18KB) · יחד 25KB.
> 246 קריאות-כלי נשמרו כשורות-סימון; פלטיהן (4590KB) לא נטענו במכוון —
> הראיה למה שנעשה היא `git log`, לא נרטיב. 0.5% מהתמליל הוא השיחה עצמה.

🔴 **ושלוש הסתייגויות שחייבות לנסוע עם השורה הזו:**

1. **הסשן הזה לא עבר דחיסה.** השיחה כולה הייתה בחלון בזמן הכתיבה. ⇒ **הרצתי את הכלי וקראתי את שורת-המדידה, אך לא קראתי את התמליל מחדש** — אין לי מה לשחזר. **אני אומר זאת במפורש במקום להצהיר "נקרא".**
2. 🔴 **"3 הודעות של ישי" מטעה, וזה חור חדש בכלי:** רוב ההנחיה הגיעה **מסשן-הסגן דרך ערוץ ההודעות בין-הסשנים**, ו-`read-session.mjs` **אינו מחלץ אותן כבלוקי-ISHAY.** ⇒ **הכרעות-הסגן שבמסמך הזה הועתקו מהצ'אט החי, לא מהחילוץ.**
3. ⚠️ החור הידוע מ-17/09 עומד: הכלי מדלג על הודעות שנכתבות בתוך תור פעיל.

**סימון-מקורות:** 📄 נקרא מקובץ · 📊 נמדד בהרצה · 💭 שיפוט שלי.

---

## 1 · 📖 רשימת-קריאה

**חובה, בסדר הזה:**

0. 📄 **המסמך הזה, עד הסוף.** §3 הוא מבחן-הקבלה.
   `C:\Users\ishay\Reg-In-export-wt\docs\specs\module_11_reports\HANDOFF-export-dialog-2026-09-23.md`
1. 📄 **המסירה הקודמת** — `HANDOFF-export-dialog-2026-09-17.md`. **מה לקחת:** §5 מלכודות (כולן חיות) · §4 מילותיו של ישי · §2 "הגבול".
2. 🔴 📄 **`docs/PROJECT_MASTER.md` §6, שורת החוב של חשיפת `hourly_rate`** — **הפריט החשוב ביותר במסמך הזה.** מצא אותה: `grep -n "report_m16_quality_cost" docs/PROJECT_MASTER.md` ⇒ **ההתאמה היחידה.** ✏️ *(23/09 14:XX: כאן היה "שורה 896" — אחרי מיזוג `dev` היא זזה ל-905. מספר-שורה אינו כתובת.)* ⚠️ **אל תחפש `"hourly_rate ב-RPC"` — בין השניים יש גרשיים-אחוריים במקור, והגריפ מחזיר אפס.** **מה לקחת:** מה נסגר, מה לא, ולמה מיסוך-לקוח נדחה.
   🔴 **ואזהרה שחייבת לנסוע עם זה:** **השורה שמעליה היא שורת-חוב *אחרת* של אותו פריט** (רשימת-המסכים). ✏️ *(23/09 14:XX: היא סומנה שם במפורש — `visible: () => canEdit` הוחלף ברשם, והנתיב לתוכנית תוקן לריפו.)*
3. 📄 `src/lib/exportSensitiveColumns.js` — **כולו.** הערת-הכותרת מסבירה למה רשם ולא דגל.
4. 📄 עשר התוויות שישי אישר. ⚠️ **ב-`origin/dev` בלבד, לא על הענף — זה לא נתיב מת:**
   `git show origin/dev:docs/specs/module_11_reports/rulings-2026-09-22.md`
   **נדרש כשמרחיבים את תיאור-העמודות של `ProjectsPage`.**
5. 📄 התוכנית המלאה. ⚠️ **ב-`origin/dev` בלבד, לא על הענף:**
   `git show origin/dev:docs/plans/2026-09-17-module-11-excel-export.md`
6. 📄 `src/modules/06_projects/ProjectsPage.jsx` — `EXPORT_COLUMNS` והחיווט. **הדפוס למסך הבא.**
7. 📄 `docs/guides/01_estimation_log.md` §2 רשומה **#13** — למה ההערכה ללילה הזה הייתה פי-3 גבוהה מדי.

---

## 2 · מצב נוכחי — פקודות, לא מספרים

| | |
|---|---|
| **ענף** | `ishay/export-build` · worktree `C:\Users\ishay\Reg-In-export-wt` |
| **נדחף?** | `git ls-remote --heads origin ishay/export-build` מול `git rev-parse HEAD` — **חייבים להיות זהים** |
| **כמה קומיטים** | `git rev-list --count origin/dev..HEAD` · הרשימה: `git log --oneline origin/dev..HEAD` |
| **עץ** | `git status --short --untracked-files=no` — **חייב להיות ריק.** `?? .agents/` ו-`?? .codex/` זרים; 🚫 לא לקמט ולא למחוק |
| **שער** | `npm run gate > /tmp/gate.log 2>&1; echo "GATE_EXIT=$?"` — 12 תת-שלבים. האחרון שרץ: `GATE_EXIT=0` |
| **כמה בדיקות** | הפלט של `test:run` בתוך הלוג — **לא מספר מהזיכרון** |
| **`origin/dev`** | `git rev-parse origin/dev` — **זז פעמיים בלילה הזה.** אל תניח |
| 🔴 **כמה אחורה** | `git rev-list --count HEAD..origin/dev` — **בכתיבת המסמך: 43; ב-12:XX כבר 47 — לכן הפקודה, לא הספרה.** ⇒ **הענף פיגר משמעותית מאחורי `dev`**, ומכאן נובעים שלושה דברים: ① שני הקבצים ב-§1 שאינם כאן הם חלק מה-43 · ② המיגרציה `20260917150000_module11_k1_pagination_params.sql` **כבר ב-`dev`** ⇒ ר' §7 פריט 9 · ③ ~~`scripts/check-type-scale.mjs` קיים בענף~~ ✏️ **תוקן 23/09 12:XX בעיניים-טריות: הסקריפט והשורה ב-`package.json` קיימים רק ב-`origin/dev`, לא בענף** (📊 `ls scripts/check-type-scale.mjs` ⇒ אין; `grep -c type-scale package.json` ⇒ 0) — הוא ייכנס לשער **עם** המיזוג. **בדוק בעצמך לפני שאתה מושך** |

---

## 3 · 🔴 מבחן-הקבלה — בהודעה הראשונה

### 🅐 מכני

1. פתח כל נתיב ב-§1. **נתיב מת ⇒ עצור ודווח.** ⚠️ **פריט 5 מת על הענף בכוונה** — הוא ב-`origin/dev`.
2. `git status --short --untracked-files=no` — ריק?
3. `git log --oneline origin/dev..HEAD` — **לא-ריק?**

### 🅑 שרידות — סמן כל טענה "מהקובץ X" או "מהזיכרון"

- **מה נסגר בדליפת-השכר ומה לא?** *(תשובה שאומרת "נסגר" בלי לפצל בין הקובץ למסך — נכשלת.)*
- **למה `visible` לבדו אינו יכול להגן על עמודה שה-RPC מכריז?**
- **מה מונע ממיגרציה עתידית לחשוף שדה-שכר חדש בשקט?**

### 🅒 שיפוט

- שורת-החוב ב-§6 עדיין מתארת את המציאות?
- §7 פריט 2 — **האם ישי כבר הסתכל על החלון?**

### 🅓 ארבע שאלות עובדתיות — כל אחת דורשת שני מקורות

1. **משתמשת עם `view` על 'דיילות' פותחת את דוח מ16. מה היא רואה בקובץ, ומה על המסך — ולמה התשובות שונות?**
   *(צירוף: `exportSensitiveColumns.js` · המיגרציה `20260917005500_module11_j1_rpc_round4.sql:2131-2152` · שורת-החוב ב-§6.)*
2. **בדיקה אחת בסבב הקודם לא יכלה להיכשל. איזו, למה, ומה החליף אותה?**
   *(צירוף: הודעת-הקומיט שהחליף אותה · והבדיקה ב-`exportSensitiveColumns.test.js`. **הרץ את המוטציה בעצמך** — הסר מפתח מהרשם וראה שהיא נופלת ונוקבת בקבצים.)*
3. **עמודת "מדרג" הציגה ערך שאינו קיים. מה בדיוק קרה, ולמה `scrollWidth` הוא חלק מהתשובה?**
   *(צירוף: הערת-הכותרת של `Cell` ב-`ExportPreviewPanel.jsx` · `ReportTable.jsx:37-39`. **הכיוון ההפוך — למה זה לא היה חיתוך — הוא העיקר.**)*
4. **`ProjectsPage` מעביר `windowedVisible` ולא `visible`. מה ההבדל, ואיזו הכרעת-ישי נשברת אם מחליפים?**
   *(צירוף: `ProjectsPage.jsx` סביב `paginate` · והבדיקה שנועלת את שורת-הכמות.)*

### 🅔 הצהרת-קריאה

**מנה אילו קבצים קראת בפועל ואילו רק אימתת שקיימים.**

---

## 4 · 💬 מה שקיים רק בשיחה

### הכרעות-ישי (22/09/2026)

| מילה-במילה | מה זה הכריע |
|---|---|
| *"להגדיל גם את חלון הרשימה"* | 🔴 **הוכרע על בסיס מספר שגוי שלי — ר' "במה טעיתי" ① — ולכן לא בוצע.** חוזר אליו |
| *"אם יש דליפה אמיתי לא מעוניין להכנס לזה בכלל תחשוב על פתרון ותסדר את זה"* | הרשם נבנה |
| *"זה אקדמי, תשחרר… תכתוב לי חוב ונתקדם"* | **המיגרציה נדחתה כחוב** |
| *"תחסוך ממני כאב ראש, בצע לפי המלצתך"* | הסמכות שמתחתיה סשן-הסגן הכריע בשמו |

### הכרעות-ישי, סבב שני (23/09/2026 — עיצוב ומוצר)

| מילה-במילה | מה זה הכריע |
|---|---|
| *"ייצוא לאקסל נשמע קצת מערכת שנות ה-90"* | אייקון-הורדה + *"ייצוא"* בשני המסכים |
| *"האם רעיון של לחיצה ימנית על עמודה פותחת מסנן זה פתרון טוב?"* | **כן — וגם אייקון גלוי.** ‏AG Grid ו-MUI X נותנים קליק-ימני **כקיצור** לצד אייקון, כי לקליק-ימני אין סימן שהוא קיים ואין מקבילה במקלדת |
| *"הסימון של לעלות לראש לא נראה מקצועי"* | שלושת הגליפים ⇐ אייקוני `lucide` |
| *"צריך בתצוגה מקדימה גלילה ימינה שמאלה לראות את העמודות"* | פס מפורש **+ מספר-העמודות בכיתוב**, כי הפס אינו ניתן לאימות מכאן |
| 🔴 *"בכל מסך שילחצו ייצוא ברירת המחדל תהיה מה שבמסך, ואם ירצה יוכל להוסיף עוד"* | **הכלל המרכזי מכאן והלאה.** ‏`defaultSelection()` + `core: true` + "סמני הכול" |
| *"אם זה לא עוזר להם בניתוח באקסל אז מיותר"* | הקריטריון שהוציא את "מה חסר" מברירת-המחדל — **והחריג היחיד** |

### הכרעות-ישי, סבב שלישי (23/09/2026 בוקר — היקף וקצב)

| מילה-במילה | מה זה הכריע |
|---|---|
| *"מבחינתי להמשיך עד שתגיע ל750 בקונטקסט · סגור לי את כל הפינות על הדרך גם העזר עם סוכנים רק אם צריך"* | **תקציב-הקשר ~750K לסשן אחד**, ומנדט לחבר את כל המסכים הנותרים ברצף — סוכנים לביקורת בלבד (יריב אחד לכל מסך, Opus, קריאה-בלבד) |
| *"אשמח שתרוץ לבד עד נקודת עצירה הבאה"* | ארבע ההכרעות הקטנות של `CustomersPage` נלקחו במקומו כ-`הכרעתי, הפיך` (סוכנים · "טעון בירור" · תווית `created_at` · אי-משיכת `dev`) |
| *"תמשיך"* (אחרי `FinancePage`) | הקצב "2–3 מסכים לסשן" (18/09) **הוחלף ליום הזה** במנדט "כל הפינות" — ארבעת המסכים הלא-מוקפאים חוברו בסשן אחד |

### 🔴 במה טעיתי — והנוסח המתוקן

| # | הטענה השגויה | המתוקן | איך נתפסה |
|:-:|---|---|---|
| **①** | *"השורה תגבה מ-26 ל-34px והרשימה תרד מ-~16 ל-~12 שורות"* — **הצגתי זאת לישי כבסיס להכרעה** | 📊 **השורה כבר הייתה 34 ונשארה 34.** 24 + 8 ריפוד נכנסים בגובה הקיים ⇒ **אפס שורות אבדו** | **מדידה שלי, אחרי שהוא כבר הכריע.** ⚠️ שני המספרים היו **נגזרים ולא מדודים** |
| **②** | *"הבדיקה מוודאת שהעמודות מגיעות כ-JSON"* | **טאוטולוגיה** — `JSON.parse` לעולם אינו מייצר פונקציה, ולכן היא **לא יכלה להיכשל** | סשן-הסגן |
| **③** | בדיקות ה-`visible` הראשונות רצו על עמודה שנושאת פונקציה | **צורה שאף דבר בייצור אינו מייצר** ⇒ הן לא בדקו את מה ששבור | סשן-הסגן |

🔑 **התבנית המשותפת לשלושתן: מדדתי את הדבר הנכון בכלי הלא-נכון, או לא מדדתי כלל והצגתי גזירה כמדידה.**

### מה שנשקל ונדחה — עם הנימוק

| מה | למה נדחה |
|---|---|
| **מיסוך-לקוח על המסך** (סינון `payload.columns`) | **אינו מסיר את הגרף** ⇒ תחושת-סגירה שקרית. ומיסוך-הגרף מוחק את תוכן הדוח, שהוא תעריף-מול-דירוג |
| **חסימת מ16 להרשאת `view`** | `reportsCatalog` מגדיר הרשאות **ברמת-לשונית בלבד** ⇒ מנגנון חדש, לא הפעלת דגל |
| **ייצוא `AuthContext`** | `react-refresh/only-export-components` נפל · וייצוא הוק שבר **29 קבצי-בדיקה** שממקים אותו חלקית |
| **`reorderKey(order, key, order[0])` ישיר** | 📊 **שובר כשהמפתח כבר ראשון** — מעיף אותו לסוף הרשימה |

---

## 5 · 🪤 מלכודות — בנוסף לכל §5 של המסירה הקודמת

| # | המלכודת | הראיה |
|:-:|---|---|
| **18** | 🔴 **פורט 5173 מריץ פרויקט אחר לגמרי**, ו-`reuseExistingServer: true` גורם ל-Playwright למחזר אותו **בשקט**. המדידה רצה על האפליקציה הלא-נכונה עד שנבדקה הכותרת. ⇒ **הרם שרת על פורט ייעודי** (`npm run dev -- --port 5199 --strictPort`) **ואמת שהכותרת היא `REG-IN`** לפני שאתה סומך על מדידה חיה. 🔴 **ומה שהאזהרה הזו *אינה* מכסה, ונמצא בביקורת עיניים-טריות:** `npx playwright test` ו-`npm run smoke` **עדיין מכוונים ל-5173 בקוד** (`playwright.config.js:34,42-43` · `package.json` `smoke`), ו-`reuseExistingServer: true` יגרום להם להיצמד לאפליקציה הזרה **בלי להתלונן**. ⇒ **לפני כל ריצת-E2E או smoke — ודא מי על 5173, או כתוב URL מוחלט בספ.** | 📊 נמדד |
| **19** | 🔴 **`useAuth` זורק בלי Provider, ו-`ReportSurface.test.jsx` מרנדר `ExportBar` בלעדיו** — אותה מחלקה שהפילה **122 בדיקות** ב-17/09. ⇒ ב-`ExportBar` הקריאה עטופה ב-`try/catch` ⇒ `null` ⇒ **נופל סגור** | 📄 `ExportBar.jsx` |
| **20** | 🔴 **ערך שלם שנקרא כערך אחר** — `61–90` מוצג כ-`90–61` תחת RTL, ו-`90+` מוצג כ-`+90`. **אין חיתוך**: `scrollWidth === clientWidth` בכל תא. ⇒ `<bdi>` פר-תא. ⚠️ **ו-`dir="ltr"` גורף היה שובר דווקא את העברית** | 📊 נמדד · 📄 `ExportPreviewPanel.jsx` |
| **21** | ⚠️ **`git commit -F <file> -- <paths>`** — הדגל **לפני** ה-`--`, אחרת git קורא את ההודעה כנתיב. וקובץ **חדש** דורש `git add -- <path>` לפני כן | 📊 נכשל פעמיים |
| **22** | ⚠️ **ההוק `block-shell-dialect-mixup` חוסם כל פקודה שמסתיימת בצינור ל-`head`/`tail`/`grep`** — גם קריאת-קובץ תמימה, וגם כשהחלק הראשון הוא `cat` ולא שער. ⇒ `cmd > /tmp/x.txt 2>&1` ואז `cat /tmp/x.txt` | 📊 נחסם פעמיים |
| **23** | 🔴 **פס-גלילה אופקי אינו ניתן לאימות מ-headless.** 📊 נמדד: `canScrollX: true`, 856px מוסתרים, **וגובה-הפס 0 גם עם `::-webkit-scrollbar` מפורש** — Chromium ב-headless מצייר overlay שאינו תופס מקום. ⇒ **אל תצהיר "יש פס". ההבטחה היא מספר-העמודות בכיתוב**, שאינו תלוי בציור | 📊 נמדד |
| **24** | 🔴 **אי-אפשר לייצא שדה שלא נשלף.** המוקאפ של הפרויקטים הראה 30 שדות; `list_projects_overview()` מחזירה **17**. ⇒ **לפני חיבור מסך — פתח את השליפה וספור**, ואל תסגור את הפער בשקט: הרחבתו היא **מיגרציה ⇒ כלל-ברזל 4** | 📊 נמדד |
| **25** | ⚠️ **`\uXXXX` בתוך heredoc של python הרס קובץ פעמיים בסשן הזה** (‏`ExportPreviewPanel` · `ProjectsPage`) — שגיאת-קידוד באמצע כתיבה משאירה קובץ באורך 0. ⇒ **כתוב עברית ישירות, או השתמש בכלי-עריכה.** ‏`git checkout -- <path>` משחזר — אבל רק אם הקובץ כבר מקומט | 📊 קרה פעמיים |
| **26** | 🔴 **מסך שטוען נגזרות *אחרי* הרשימה מוריד קובץ שלם-למראה עם עמודה ריקה.** 📊 נמדד בלקוחות: לחיצה על "ייצוא" מיד כשהטבלה עלתה ⇒ 60 שורות, עמודת-כסף ריקה כולה, אפס שגיאות — כי `null` הוא גם "טרם נטען" וגם "אין הרשאה". ⇒ **כל מסך מעביר `loading` לחלון עד שכל הנגזרות הסתיימו** (`derivedLoading` · `vatState`), **וכשל מעביר `blockedReason`** — לא תא ריק | 📊 נמדד · 📄 `CustomersPage.jsx` · `FinancePage.jsx` |
| **27** | 🔴 **החלון מודאלי — "נסי שוב" שבבאנר של המסך אינו נגיש מתוכו.** הודעת-חסימה שאומרת *"לחצי נסי שוב"* בלי `error` + `onRetry` לחלון משאירה את המשתמשת תקועה. תפס הסוכן-היריב בכספים; היה גם בלקוחות | 📄 `ExportPreviewPanel.jsx:117-125` |
| **28** | 🔴 **"מה שבמסך" כולל תגיות ושורות-משנה.** תגית "דמי ביטול"/"חוב אבוד" ושורת-ההקשר תחת שם-הפרויקט הן נתונים שעל המסך ⇒ ליבה; בלעדיהן שורת-ביטול נקראת בקובץ כהכנסה רגילה. **ותנאי-סינון של המסך נגזר מאותו מקור שהמסך משתמש בו** — `projectsShowControls` נגזר מכל ההיסטוריה בעוד הלשונית גוזרת מהחלון, וחיפוש ישן סינן את הקובץ בשקט | 📊 שני סוכני-יריב, 23/09 |
| **29** | ⛔ **`git merge origin/dev` לענף האישי נחסם על-ידי מסווג-ההרשאות של Claude Code** *("Modify Shared Resources")* — לא הריפו, הכלי. 💭 **הראיה היחידה היא הצ'אט של הסשן הזה** (הודעת-הדחייה של הכלי) — אין לה עקבה בריפו; אם המיזוג עובר אצלך, המלכודת פגה. ⇒ המשיכה מ-`dev` דורשת הרשאה בהגדרות או שישי מושך בעצמו. **המיזוג הצפוי מתנגש ב-4 קבצים** (📊 `git merge-tree --write-tree HEAD origin/dev`: `STATUS.md` · `CLAUDE_CODE_LOG.md` · `module-11.md` · `ProjectsPage.jsx`) | 📊 נמדד |
| **30** | ⚠️ **תא-תאריך ריק יורד כ-"—" ותא-מספר ריק — ריק.** התנהגות המנוע (`cellFor('date')`), נעולה ב-`reportsExport.test.js:178`. **לא לתקן בלי ישי** — זו שאלת-מוצר שרשומה ב-STATUS | 📄 `reportsExport.js` |

---

## 6 · 🎯 הצעד הבא

### ⏭️ מה שמוכן להתחלה

> 🏁 **23/09/2026 12:XX — ארבעת המסכים חוברו (`d0b186a8` · `58a3af26` · `fa0cc358`). הצעד הבא הוא §7 פריט 9 (משיכת `dev` + "כל השורות"), והבלוק ב-§8 נכתב מחדש עבורו.** מה שכתוב מכאן ומטה הוא ההיסטוריה של היום — `CustomersPage` ⇒ `FinancePage` ⇒ `CustomerDetailsPage`.

> ✏️ **23/09/2026 10:0X — `CustomersPage` בוצע.** הפירוט ב-`docs/micro_guides/module-2.md` §9 (השורה המתוארכת). 🔑 **מה שהצעד גילה ולא היה כתוב כאן:** ① `customers` היא 9 עמודות ו-`select('*')` שולף את כולן — **אין פער-RPC במסך הזה** (שאלת "מלא שדות" נענתה ב"לא") · ② 🔴 **פתיחת החלון לפני שהשליפה השנייה (הצעות) הסתיימה הורידה קובץ עם עמודת-כסף ריקה כולה, בלי שגיאה** — `null` הוא גם "טרם נטען" וגם "אין הרשאה". תוקן ב-`derivedLoading` ⇒ החלון בטעינה עד שהנגזרות הגיעו. ⚠️ **מלכודת 26 לסשן הבא:** כל מסך שטוען נגזרות **אחרי** הרשימה (`FinancePage` · `CustomerDetailsPage`) חייב להעביר `loading` לחלון, אחרת אותו קובץ-ריק-בשקט חוזר · ③ לתפקידים בלי 'הצעות מחיר' (PROJECTS · FINANCE) מסלול-ה-RLS על `quotes` מחזיר `[]` ולא שגיאה — נמדד, לא הונח. **המסך הבא:** `FinancePage` (שלושה תיאורי-עמודות, לשונית-לשונית). ✏️ **11:XX — גם `FinancePage` בוצע** (`module-8.md` §10). 🔑 **שני דברים שהסוכן-היריב תפס שם ושחלים על כל מסך הבא:** ① **תגיות ושורות-משנה על המסך הן "מה שבמסך"** — "סוג ביטול"/"חוב אבוד"/"חשבונית זיכוי" חייבות להיות ליבה, אחרת שורת-ביטול נקראת בקובץ כהכנסה רגילה · ② **הודעת-חסימה שמבטיחה "נסי שוב" חייבת כפתור בתוך החלון** (`error` + `onRetry`) — הבאנר של המסך נשאר מאחורי המודאל. **הבא:** `CustomerDetailsPage` (שתי לשוניות). ✏️ **12:XX — בוצע** (`module-2.md` §9). 🏁 **ארבעת המסכים שאינם מוקפאים מחוברים. מה שנשאר בחלון-הייצוא:** `RepositoryTab` (מיגרציה, ישי) · "כל השורות" (אחרי משיכת `dev` — נחסמה למסווג-ההרשאות בסשן הזה) · שלוש שאלות-מוצר קטנות (מקף מול ריק בתא-תאריך · `69.0` מול `69%` · חסימה גורפת על כשל-מע"מ) · ואימות-העיניים של ישי.

**`CustomersPage`** — המסך הבא לפי `PROJECT_MASTER §6`. *(✏️ בוצע — ר' למעלה.)*

📄 **הדפוס המלא נמצא ב-`ProjectsPage.jsx`, והוא כולל עכשיו את שני חצאי הכלל:**
`EXPORT_COLUMNS` עם **`core: true` על מה שהטבלה מראה** · כפתור בשורת-המסננים ·
`<ExportDialog>` שמקבל את **הרשימה המסוננת, לא את עמוד-הדפדוף**.

🔑 **הכלל שישי ניסח 23/09, והוא חל על כל מסך מכאן והלאה:**
> *"בכל מסך שילחצו ייצוא ברירת המחדל תהיה מה שבמסך, ואם ירצה יוכל להוסיף עוד"*

**האכיפה ב-`defaultSelection()` (`src/lib/exportColumns.js`)** — אל תכתוב אותה מחדש במסך.
⚠️ **וכשאף עמודה אינה מצהירה `core` — הכל מסומן, במכוון:** 16 הדוחות של מ11 מקבלים
עמודות כ-JSON מה-RPC, ו-JSON אינו נושא `core`. **ההתנהגות הישנה נשמרת בדיוק שם.**

⚠️ **תנאי-כניסה שנרשם ולא נבדק:** `listCustomers` (`02_customers/api.js:34`)
**אינה משתמשת ב-`fetchAll`** ⇒ חשופה לתקרת-1,000 השקטה. **בדוק לפני שאתה מחבר.**

🔴 **ושאל את עצמך את השאלה שישי שאל:** *"המסך הזה מכיל מלא שדות, לא?"* —
**פתח את ה-RPC/השליפה וספור.** אי-אפשר לייצא שדה שלא נשלף, ואם השליפה מחזירה פחות
ממה שהמוקאפ הראה — **זה ממצא לרשום, לא פער לסגור בשקט.**

### 🚫 מה שאסור להתחיל

- **`RepositoryTab` (מסך-הדיילות)** — **מוקפא עד שחוב-המיגרציה ייסגר.**
- **מיסוך-מסך כלשהו** לדליפת-השכר. ר' §4 "נשקל ונדחה".

---

## 6ב · 🔴 מה מהמסירה הקודמת **התיישן** — קרא לפני שאתה נשען עליה

⚠️ **הכותרת אומרת "היא עדיין תקפה לכל מה שאינה סותרת כאן". זו רשימת-הסתירות, והיא חובה.**

| בַּמסירה הקודמת | מה השתנה |
|---|---|
| 🔴 **§7 פריט 3 + 3א — "כל השורות" חסום, ממתין למיזוג של הסשן המקביל** | **החסם נפתח.** 📊 המיגרציה `20260917150000_module11_k1_pagination_params.sql` **קיימת ב-`origin/dev`**; `grep -rln p_page_size supabase/migrations/` על הענף עדיין מחזיר אפס **רק מפני שהענף מפגר 43 קומיטים.** ⇒ **הפריט אינו חסום עוד — הוא רק לא נמשך.** ר' §7 פריט 9 |
| 🔴 **§5 מלכודת 2 — "קובץ המיגרציה k1 אינו על הענף, אל תצטט אותו כנתיב"** | **נכון לענף, מיושן לגבי `dev`.** ⇒ אחרי משיכה, הנתיב קיים |
| 🔴 **§5 מלכודת 17 — "תא שנחתך אינו נראה חתוך; `31–60` הוצג כ-`0–61`", מאובחן כ**חיתוך** | **האבחנה הוחלפה.** 📊 נמדד: `scrollWidth === clientWidth` בכל תא ⇒ **לא חיתוך אלא היפוך-כיווניות.** ⇒ **מלכודת 20 כאן גוברת על 17.** מי שירדוף אחרי גלישה — ירדוף אחרי משהו שאינו קיים |
| **§7 פריט 3ד — ההערה ב-`ExportDialog.jsx` מנמקת איפוס ב"רמת-דריל" שאינה נגישה** | **עדיין פתוח ולא תוקן** — `src/components/ExportDialog.jsx:96`. תיקון-הערה בלבד |
| **§7 פריט 6 — הסיווג "רשימת-שיא מול תקרה", שיפוט בלי אישור-ישי** | **עדיין פתוח.** ישי |
| **§7 פריט 8 — §7.101 ושלוש שאלות-ההבנה מדוח האודיט** | **עדיין פתוח, ולא נגעתי בו.** ישי |
| **§7 פריט 10 — אימות-חי חלקי** | **חלקית נסגר.** ✅ נעשה הלילה: מדידת-פריסה בארבעה רוחבים · שני מצבי-קצה. ❌ **עדיין לא נעשו, והם ארבעת הפריטים המקוריים:** צילום התצוגה-המקדימה מול הקובץ שירד · ארבעת סוגי-המסננים אחד-אחד · שלוש בדיקות-הקצה · שורת-הכמות מול ספירה במסד באותו רגע |
| **§6/§7 פריט 7 — `QuotesPage` מוקפא** | **לא נפתח ולא נסגר.** הוא אינו ברשימת-המסכים של `PROJECT_MASTER §6:896`. 💭 **כנראה מחוץ להיקף, אך ההקפאה מעולם לא בוטלה במפורש** ⇒ לישי |
| **§1 פריט 11 — התוכנית ב-`~/.claude/plans/breezy-wibbling-sloth.md`** | **אותה תוכנית בדיוק**, גובתה לריפו (📊 אומת: זהה-בייט, 721 שורות). **מכאן והלאה קרא מהריפו.** ⚠️ `PROJECT_MASTER:895` עדיין מצטט את הנתיב הישן |

---

## 7 · פריטים פתוחים

| # | הפריט | מי |
|:-:|---|---|
| **1** | 🔴 **חוב-המיגרציה — `hourly_rate` מגיע להרשאת `view`.** `PROJECT_MASTER §6`. **הקובץ נסגר; המסך והגרף לא.** כלל-ברזל 4. ✏️ **הכרעת-ישי 23/09 12:XX:** *"אם זה תסביך הייתי משחרר מזה לגמרי שיהיה חוב מבחינתי לעוד שבועיים"* ⇒ **נשאר חוב עד ~07/10/2026, לא נוגעים** | **ישי · ~07/10** |
| **2** | ◐ **§7 פריט 2 — אימות-עיניים.** נמדדה גלישה בארבעה רוחבים, גובה ב-768, שני מצבי-קצה, **והוצא באג אמיתי**. 🔴 **אבל ישי עדיין לא הסתכל.** נשלחו לו שלושה מסכים. **אל תסמן ✅** | **ישי** |
| **3** | ✅ **נסגר 23/09** — `50vh` **לא בוצע**, וזו הכרעה מנמקת: הפיצוי ניתן על אובדן-שורות **שנמדד כלא-קיים**, ואי-אפשר לבדוק צפיפות עד שיהיה מסך עם 30 שדות | ✅ |
| **4** | ⏸️ **הגליף `⤒` חלש ודק ליד `▲▼`** — ב-1024px קשה להבחין ביניהם. שאלה ויזואלית | **ישי** |
| **5** | ✅ **נסגר 23/09** — יצאה מברירת-המחדל ונשארה זמינה, לפי הקריטריון שישי נתן. **והוא החריג היחיד.** מקור: ⚠️ **עמודת "מה חסר" היא משפט מחושב.** 💭 **ביקורת עצמית:** התוכנית §7.4 פוסלת את `LogisticsPage` **בדיוק מהנימוק הזה** — *"התאים הם משפטים מחושבים, לא נתונים"*. **שווה לשאול אם היא שייכת לקובץ** | **ישי** |
| **6** | ⚠️ **עובי פס-הגלילה לא אומת על Windows אמיתי** — 📊 נמדד `canScrollX: true` ו-71px מוסתרים ב-`ProjectsPage`, אבל העובי עצמו נמדד ב-Chromium headless | ישי |
| **7** | 🚧 **כלל-ברזל 14:** `RepositoryTab.jsx:404` · `:455` מצהירים את כלל-ההרשאה עצמאית. הרשם אמור להיות המקור היחיד | הסשן הבא |
| **8** | **המסכים הנותרים** — ~~`CustomersPage`~~ ✅ *(✏️ 23/09/2026 10:0X — חובר, נמדד חי, `derivedLoading` נוסף אחרי שקובץ ירד עם עמודת-כסף ריקה; `module-2.md` §9)* · ~~`FinancePage`~~ ✅ *(✏️ 23/09/2026 11:XX — שלושה סטים של ליבה; `module-8.md` §10)* · ~~`CustomerDetailsPage`~~ ✅ *(✏️ 23/09/2026 12:XX — שני סטים; `module-2.md` §9)* · *(`RepositoryTab` מוקפא)* ⇒ **הפריט סגור עד להכרעת-המיגרציה** | ✅ |
| **9** | 🔓 **"כל השורות" — כבר אינו חסום.** המיגרציה ב-`origin/dev`, והענף מפגר (📊 `git rev-list --count HEAD..origin/dev`). 📊 **המבחן:** `git ls-tree -r origin/dev --name-only supabase/migrations/` מול `grep -rln p_page_size supabase/migrations/` אצלך. ⚠️ **ומה שנדרש אחרי משיכה:** `ExportBar` עדיין אינו מעביר `showAll`/`onShowAllChange` ⇒ **התיבה מוצגת ואינה עובדת**. ✏️ **23/09 12:XX — המשיכה עצמה נחסמה למסווג-ההרשאות (מלכודת 29).** ✅ **13:XX — ישי שחרר את ההרשאה; `dev` מוזג (`757ff3c3`, 4 קונפליקטים נפתרו, שער 0 כולל `check:type-scale`), ו"כל השורות" חווט** (`module-11.md` §9 D-44): 📊 מ19 *"8 מתוך 52"* ⇒ *"52 שורות"*, `p_page_size: 52`, קובץ 53 שורות. ✅ **14:XX — היקף מ04/מ06 הוכרע ("מאשר לפי המלצתך") ובוצע:** התיבה גם שם, נוקבת באילו 50 (`CAPPED_LABELS` ב-`exportFetch.js`); 📊 חי: 741 ו-135 שורות בקובץ | ✅ |
| **10** | ⏸️ **שלוש שאלות-מוצר קטנות מהסוכנים-היריבים (23/09):** ① מקף מול ריק בתא-תאריך (מלכודת 30) · ② אחוז יורד `69.0` ולא `69%` · ③ כשל-מע"מ בכספים חוסם את כל הייצוא גם כשהמשתמשת הורידה "כולל מע"מ". ✅ **הוכרע 23/09 12:XX — *"מאשר את הכל לפי המלצתך"* ⇒ שלושתן נשארות כפי שהן** | ✅ |
| **11** | ✅ **`projectsShowControls` בכרטיס-הלקוח** נגזר עד 23/09 מ-`projects.length` (כל ההיסטוריה) בעוד הלשונית גוזרת מהחלון — ולכן חיפוש ישן המשיך לסנן בשקט את "עוד N מחוץ לחלון" (ומהיום גם את הקובץ). תוקן אגב חיבור-הייצוא (מלכודת 28); `module-2.md` §9 | ✅ |

🚫 **אסור בלי ישי:** PR · מיזוג · `apply_migration` · `--force` · ארכוב-סשן.
✅ **`push` של הענף האישי — מותר** (הכרעת-ישי 17/09).
🔴 **ולפני כל קומיט:** `git fetch origin` **ואז, בנפרד,** `git log --oneline origin/dev..HEAD` — **חייב להחזיר שורות.** 🚫 אל תשרשר ב-`&&` עם `merge-base --is-ancestor`.

---

## 8 · הבלוק להדבקה

```
⚠️ פתח את הסשן בתיקייה C:\Users\ishay\Reg-In-export-wt — לא ב-C:\

/module-build המשך את חלון-הייצוא של מ11 — מה שנותר אחרי 23/09 (dev מוזג, "כל השורות" חווט).
⚠️ הבלוק שמתחת נכתב לצעד ①-③ לפני שבוצעו באותו יום; מה שנותר: היקף התיבה למ04/מ06 (הכרעת-ישי),
   RepositoryTab (מוקפא עד ~07/10/2026), ואימות-העיניים של ישי. קרא את §7 לפני שאתה מבצע משהו מהבלוק.

אם משהו כאן סותר את מה שאתה רואה בדיסק — הדיסק מנצח.

קרא ראשון, בנתיב מלא:
C:\Users\ishay\Reg-In-export-wt\docs\specs\module_11_reports\HANDOFF-export-dialog-2026-09-23.md
§0 ההסתייגויות · §1 רשימת-הקריאה · §3 מבחן-הקבלה · §4 שלושת סבבי-ההכרעות · §5 מלכודות 18-30 ·
§6ב מה התיישן · §7 פתוחים (פריטים 9-11 הם הצעד).
וקרא גם את המסירה הקודמת (HANDOFF-export-dialog-2026-09-17.md) — §5 ו-§4 שלה חיים.
ענה על §3 בהודעה הראשונה, כולל 🅑 ו-🅔.

🔴 מצב: ארבעת המסכים שאינם מוקפאים מחוברים לחלון (פרויקטים · לקוחות · כספים · כרטיס-לקוח).
   🚫 RepositoryTab מוקפא — חוב-המיגרציה של hourly_rate (PROJECT_MASTER §6 — grep -n "report_m16_quality_cost", לא מספר-שורה).
   🔴 הכלל של ישי מ-23/09 חל על כל מסך: "ברירת המחדל תהיה מה שבמסך, ואם ירצה יוכל להוסיף עוד".
      האכיפה ב-defaultSelection() — אל תכתוב אותה מחדש. "מה שבמסך" כולל תגיות ושורות-משנה (מלכודת 28).

אמת, אל תניח:
  git branch --show-current                 # חייב: ishay/export-build
  git status --short --untracked-files=no   # חייב: ריק
  git log --oneline origin/dev..HEAD        # חייב: לא-ריק
  git rev-list --left-right --count HEAD...origin/dev   # מספר-הפיגור מ-dev — פקודה, לא ספרה
🚫 אל תיגע ב-Reg-In · Reg-In-copy-wt · Reg-In-dash-wt.

הצעד: ① git merge origin/dev לענף (לא rebase). ⛔ מלכודת 29: מסווג-ההרשאות חסם את זה
   בסשן הקודם — אם נחסם שוב, עצור ואמור לישי; אל תעקוף. המיזוג מתנגש ב-4 קבצים
   (git merge-tree --write-tree HEAD origin/dev) — פתור, הרץ שער מלא, קמט את המיזוג לבדו.
   🔴 הקונפליקט ב-ProjectsPage.jsx יושב על שורת גודל-טקסט (text-[12px] בצד הענף) —
      קח את הצד של dev (text-xs); ProjectsPage אינו ב-type-scale-baseline.json ויפיל את השער.
   ② אחרי המיזוג: check:type-scale נכנס לשער (הסקריפט מגיע עם dev) — אפס text-[NNpx] בקוד חדש.
   ③ "כל השורות" — §7 פריט 9. מה קיים ומה חסר, נמדד 23/09:
      · המיגרציה k1 הוחלה (origin/dev:docs/db_roadmap.md — שורת APPLIED 17/09; הקומיט 6468c5d8),
        וכותרת קובץ-המיגרציה עדיין אומרת "drafted" — אל תסיק ממנה.
      · p_page_size/p_page קיימים ב-5 RPC-ים בלבד: m02 · m19 · m07 · m04 · m06 (כותרת k1).
        🔴 callReport (11_reports/api.js) שולח פרמטרים בשמם, ו-api.test.js נועל בדיוק ארבעה —
        שליחה גורפת תשבור את 11 האחרים. שלח רק לחמשת אלה, ועדכן את הבדיקה.
      · ExportDialog כבר מקבל showAll/onShowAllChange/rowTotal; ExportBar מעביר rowTotal ו-topN
        אבל לא showAll. חסר: state ב-ExportBar · הוספתו ל-requestKey (אחרת אין שליפה מחדש) ·
        העברה דרך fetchExportData אל callReport.
      · 💭 מה "הכול" שולח — limit null (בלי הגבלה) או rowTotal? הכרע במפורש, ובדוק מול המסד.
      · ⏸️ התיבה מוצגת רק כשיש topN (m02 · m07 · m19). התוכנית (origin/dev:docs/plans/
        2026-09-17-module-11-excel-export.md:258-328) מבקשת אותה גם ל-m04/m06 — היקף לישי.
      בדוק חי: מ19 — 📊 52 שורות בבדיקת 17/09 (db_roadmap:533), לא 8; המספר יכול היה לזוז.
🔴 כל מסך שפותח את החלון מעביר loading עד שהנגזרות הגיעו, ו-blockedReason + error/onRetry על
   כשל (מלכודות 26-27) — זה הדפוס בארבעת המסכים, אל תסטה ממנו.

🔴 מדידה חיה: אל תסמוך על פורט 5173 — שם רץ פרויקט אחר, ו-Playwright ממחזר אותו בשקט.
   npm run dev -- --port 5199 --strictPort   ואמת שהכותרת היא REG-IN.
   סקריפטי-המדידה של הסשן הקודם (כניסה מ-.env.local, פתיחת החלון, הורדה, ספירת שורות ב-xlsx
   דרך zipfile) אינם בריפו — כתוב מחדש לפי הדפוס ב-module-2.md §9 / module-8.md §10.

השער: npm run gate > /tmp/gate.log 2>&1; echo "GATE_EXIT=$?"
שער שלא ראית את קוד-היציאה שלו — לא רץ. אמת 12 תת-שלבים בלוג, אל תסיק.
⚠️ ההוק חוסם צינור ל-head/tail/grep — שמור לקובץ ואז cat.

git commit -F <file> -- <נתיבים>    הדגל לפני ה---. קובץ חדש: git add -- <path> קודם.
🚫 לעולם לא git add -A.

🚫 אין PR · אין מיזוג · אין apply_migration · אין --force · אין ארכוב-סשן.
✅ push של הענף האישי מותר.

בסוף: רשומה ב-docs/CLAUDE_CODE_LOG.md ושורה ב-STATUS.md, שניהם אחרונים.
🚫 לא ל-CHANGELOG — הוקפא 23/07/2026.

גריפ רק כדי למצוא איפה, לעולם לא כדי לענות.
```

</div>
