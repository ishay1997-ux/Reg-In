<div dir="rtl">

# 🔑 מסירה — כל מי שנוגע בזריעה של REG-IN מכאן והלאה

> **נכתב 10/09/2026 בסוף סבב-זריעה בן ~6 שעות.** ‏**אתה לא צריך הקשר נקי — אתה צריך לדעת מה
> לקרוא.** הקובץ הזה **מצביע ואינו משכפל**: כל עובדה חיה בקובץ אחר, והקובץ האחר גובר.
> 🔴 **הדבר היחיד שחייב לקרות לפני הכנס:** ‏**14/10/2026** —
> `scripts/conference-refresh/refresh-before-conference.sql`. הסבר בראש `STATUS.md`.

---

## 1 · רשימת-הקריאה — בסדר הזה, ולא לדלג

| # | הקובץ | למה דווקא הוא |
|:-:|---|---|
| **1** | **`seed-coherence-tests.md`** | **14 מבחנים שרצים**, כל אחד עם שאילתה וסף. **זה החוזה.** נגעת בזריעה ⇒ הרצת אותם ⇒ כולם ירוקים. אחרת לא סיימת |
| 2 | `seed-fix-plan-2026-09-09.md` §"מה בוצע" + §"מלכודות" | מה כבר תוקן, ולמה **ב2 בוטל אחרי בדיקה** — כדי שלא "תתקן" התנהגות תקינה |
| 3 | `business-model-anchor.md` | למה המחירון הוא העוגן, ומה התשובה כשישי שואל *"איך נדע שהמספרים מסתדרים"* |
| 4 | `story-evidence-2026-09-09.md` | ארבעת סיפורי-הכנס והמספר של כל אחד |
| 5 | ‏`~/.claude/references/retro-ledger.md`, השורה ב-`n=5` | **מחלקת-הכשל שחזרה חמש פעמים ביום אחד.** ר' §3 למטה |

🟡 **רק על פער אמיתי:** ‏`20260827144459_module8_finance_money_ssot_and_readers.sql` (נוסחת-הרווח,
שורות 14–20) · `20260903180958_seed_registry_and_helpers.sql` (מנגנון-הזריעה) ·
`20260814141052_module6_status_machine_and_cron.sql` (מכונת-המצבים).

## 2 · העיקרון שכל התיקונים נגזרו ממנו

🔑 **נתון שנגזר נשאר קוהרנטי; נתון שנזרע לבדו — לא.**
**הראיה:** מתוך שבעת המבחנים הראשונים עבר **אחד** — הרווחיות — והיא עברה **מפני שהיא נגזרת
מנוסחה**. דירוג, זמן-תגובה, נוכחות ומשוב נזרעו כשדות-עצמאיים וכולם נכשלו.
⇒ **אל תזרע ערך. גזור אותו** ממה שכבר קיים, והוסף רעש מכוון (~10%) — **מתאם מושלם נראה
מזויף בדיוק כמו היעדר-מתאם**.

## 3 · 🔴 מחלקת-הכשל שתפיל אותך — חמש פעמים ביום אחד

**"מסקנה שפוסלת נכס קיים, מראיה נכונה אבל חלקית, כשההקשר שהיה הופך אותה היה זמין."**
בכל חמש הפעמים **ישי** סיפק את ההקשר, לא הסשן:

| מה נקבע | מה היה קיים ולא נבדק |
|---|---|
| "הדיסקברי הוא לופ" | הוא בנה חצי מערכת עובדת |
| "לאמץ את מסמך-האפיון הישן" | האפיון החדש כבר הפריך את מספריו |
| "‏221→304 שגוי, יש 827" | **נכון — לפי שנה.** 827 הוא שלוש שנים |
| "בדקתי את ארבעת הסיפורים" | היו 30 טבלאות; ארבע תקינות לא נבדקו |
| "אין בדיקה רביעית למחירון" | נקראה קטגוריה אחת מתוך שלוש. **יש שישה עוגנים** |

**המנגנון שמונע אותה, ועובד:** ✅ **לפני שאתה פוסל נכס — שחזר את ההקשר שהיה מצדיק אותו,
ורשום מה מצאת.** הוא נבדק באותו יום: בדיאלוג-השכר עלתה מסקנת *"המוקאפ מיושן"*, ההסבר-הנגדי
נבדק — **והתברר ההפוך: הדוח שילם לדיילת שנעדרה.** *(מ8)*

## 4 · 🪤 מלכודות-המסד — כל אחת עלתה כישלון חי

| המלכודת | מה קורה |
|---|---|
| **`execute_sql` בטרנזקציה** | שאילתה שנכשלת **מגלגלת הכול**. ✅ אין מצב-ביניים — אומת פעמיים |
| **CTE רואה תמונה ישנה** | ‏`insert … returning` ואז `join` חזרה לאותה טבלה **לא ימצא** את החדשות. 10 הצעות נוצרו והפרויקטים לא |
| `assignments_attendance_shape` | `no_show` ⇒ סיבה חובה + `lateness` null · `late` ⇒ `lateness` חובה |
| `assignments_no_show_zero_hours` | `no_show` ⇒ `actual_hours = 0` ⇒ החזרה ל-`arrived` **מחייבת שעות חדשות** (נגזרות משעות-האירוע) |
| `assignments_one_event_per_day` | דיילת אחת ליום. אישור-שיבוץ מחייב `not exists` על אותו תאריך |
| **טריגר `sync_assignments_on_project_date_change`** | שינוי `final_event_date` **מסנכרן שיבוצים לבד**. עדכון ידני במקביל ⇒ התנגשות |
| **נעילת-הצעה** | ‏`update/delete` על `quote_services` חסום מחוץ ל-`in_progress`. **`insert` מותר** |
| 🔑 **`regin.seed_bypass`** | **הדרך המוצהרת לעקוף** — ורק כשההצעה **במרשם-הזריעה**. `set local regin.seed_bypass='on'`. **הצעה אמיתית לעולם אינה במרשם ⇒ נתוני-אמת מוגנים** |
| `logistics.quote_service_line_id` | מחיקת שורת-הצעה חסומה אם יש לה לוגיסטיקה |
| `project_changes.performed_by` | ‏FK ל-`users` — לא כל מייל תקף |
| עמודות מחושבות | `quotes.estimated_hours` · `salary_report_lines.line_id` — **לא לספק ערך** |
| `personal_bonus` | ‏`NOT NULL` — אפס, לא ריק |
| `finance_project_money` | **set-returning ⇒ אסור ב-`UPDATE` ישיר.** נדרש `cross join lateral` |
| **טריגר `updated_at`** | דורס ל-`now()`. **אי-אפשר לצייר תאריך עתידי** — ועל זה נשען סקריפט-הרענון |

## 5 · 🗺️ מה תלוי במה — לפני שאתה נוגע

```
רווח = הכנסות − סחורה − עבודה − נסיעות        (מ13)
  הכנסות ← quote_services · project_changes
  סחורה  ← quote_services 🚫 בלי category='hostess' · project_changes
  עבודה  ← assignments (actual_hours × hourly_rate_snapshot + personal_bonus)
  נסיעות ← params · assignments (actual_hours > 0)
```
🔴 **`final_profit` קפוא ואינו מחשב את עצמו.** נגעת ב-`attendance_status` · `personal_bonus` ·
`quote_services` · `project_changes` · `params` ⇒ **חשב מחדש דרך `finance_project_money`**,
לעולם לא ביד. **נמדד 10/09: 481 מתוך 701 פרויקטים סטו בשקט.**
✅ `hourly_rate` **אינו** ברשימה — הרווח נשען על ה-snapshot הקפוא בשיבוץ.

## 6 · גיבוי ומצב

**טבלאות-גיבוי במסד** (לפני כל כתיבה, 09/09): `bak_hostesses_20260909` ·
`bak_assignments_20260909` · `bak_projects_20260909`.
**ענף-גיבוי:** `backup-m11-seed-2026-09-10`.
⚠️ **16 קומיטים לא נדחפו ל-origin** נכון לכתיבת השורה — לאמת ב-`git status` לפני שמסתמכים.
⚠️ **שני סשנים עבדו על אותו checkout** ב-09–10/09; הענף התחלף פעמיים מתחת לסשן פעיל.
**לאמת `git rev-parse --abbrev-ref HEAD` לפני כל קומיט.**

## 6ב · 🎯 איך עוברים עם ישי על 22 הדפים — **הצעד הבא, ואל תתיש אותו**

**ישי, 10/09:** *"לא בא לי שהוא יתיש אותי, ואני רוצה בתכלס שיחליט במקומי אבל יחליט נכון."*
⇒ **22 שאלות = כישלון. גם אם כל אחת נכונה.**

**הפרוטוקול, ושלושת השלבים אינם ניתנים להחלפה:**

**① עבור על כל 22 הדפים לבד**, מול שמונת כללי-הכניסה ב-`m11-report-inclusion-rules.md`.
**רוב ההכרעות מקצועיות ואינן דורשות אותו:** יש לדף משפט-קיום? · חופף לדף אחר בהזזת-מסנן? ·
נבחן בעדשת-הסוג הנכונה? · האוכלוסייה ≥30 שורות? **אלה שלך. הכרע.**

**② 🚫 ואל תשאל אותו את השאלה שאין לו תשובה עליה.** *"האם מנהלת-הכספים באמת פותחת את
הדוח כל שבוע?"* — **אין מנהלת-כספים. אין לקוח.** ישי, 10/09: *"אין לי דרך לברר באמת ולשאול
מישהו, אז הלכתי לאזורים של מה שהגיוני ומקובל בעולם."* ⇒ **שאלה כזו תייצר ניחוש שייראה
כמו עובדה, ותיבנה עליו מערכת.** זו הטעות שנמנעה ב-10/09 רק כי הוא אמר את המשפט הזה בזמן.

**③ הבא לו רק שאלות מסוג אחד: *"מה אתה רוצה להראות בכנס?"*** — **זו ההכרעה היחידה שבאמת
שלו**, כי הוא זה שיעמוד שם ויענה. **צמצם ל-3–4 שאלות, כל אחת עם ראיה ותמונה.**
🔑 **והעוגן שכבר הוכרע ומכריע רוב מקרים בלי לשאול:** ‏**הכרעה 7 — ארבעת סיפורי-הכנס**
(ריכוזיות · עיריות ב-68 יום · "מתרחק" · סיווג "אחר"). **דף שמשרת סיפור — נשאר. דף שאינו
משרת אף אחד — צריך נימוק חזק כדי להישאר.** ‏`story-evidence-2026-09-09.md` נושא את המספר של כל סיפור.

⚠️ **ומה שאסור לך להכריע לבד: להוריד דף.** ‏`m11-report-inclusion-rules.md` אומר זאת בעצמו:
*"כל ורדיקט 'יורד' או 'מתמזג' הוא **הצעה** להכרעת-ישי, לא הכרעה."* **§3 למעלה מסביר למה זו
בדיוק המחלקה שמפילה.**

## 6ג · 🗣️ ארבעה משפטים של ישי ששינו את העבודה — במילותיו

**הסשן הקודם לא הגיע לאף אחת מהתובנות האלה לבד. הן הגיעו ממנו, ותמיד כתיקון.**

| מה הוא אמר | מה זה שינה |
|---|---|
| *"אני רוצה לספר על עסק מצליח בינוני מופסד? על דיילות שמאשרות מהר לאט? …**והכל קשור אחד לשני**, מתאם וקוהרנטיות"* | ⇒ **מבחני-הקוהרנטיות כולם.** עד אז נבדק כל סיפור **לחוד**, וכל אחד עבר. **יחד** הם סתרו: דירוג-5 שנעדרת יותר מדירוג-3. **בדיקה פר-פריט אינה בדיקה** |
| *"חושש שאתה נופל **בהתאמת יתר או מהטיה**… אולי יש דברים נוספים"* | ⇒ הסריקה עברה מארבעה סיפורים ל-**30 טבלאות**. **ארבעה אזורים תקינים אושרו, וממצא שגוי בוטל** (‏"221→304" היה נכון). **חשד בהטיה ⇒ הרחב את הסריקה, אל תעמיק בה** |
| *"**קרא בקוד** את מעבר-הסטטוסים, נראה לי חסרה לך הבנה"* · *"קרא גם על כל דבר שעובר לפני שאתה רוצה להזריע — **לפעמים רק הקריאה הזו תיתן לך הבנה מספיק טובה**"* | ⇒ עצר הזזה גורפת של **29 פרויקטים ו-179 שיבוצים** שהייתה מיותרת — ‏`pg_cron` עושה זאת לבד. וגילה את **`regin.seed_bypass`**, מנגנון-זריעה מוצהר שהסשן לא ידע עליו |
| *"**לא קראת שיש אתר רישום ותגים שרוכים** והכל? זה לא רק דיילות"* | ⇒ *"אין בדיקה רביעית למחירון"* נאמר אחרי קריאת **קטגוריה אחת מתוך שלוש**. יש **שישה** עוגנים, והחזק בהם — **2.16 יחידות-מוצר לאורח** — הוא זה שכדאי להציג בכנס |
| ➕ *"ואם נעשה 57 אחוז מזמינים **אתר רישום**?"* | ⇒ **סגר את החור הכלכלי היחיד.** ‏893,000 ⇒ **1,035,812 ₪** — מכסה חמש משרות |

🔑 **הדפוס, ולכן הכלל:** ‏**חמש פעמים ביום אחד, ההקשר שהיה הופך את המסקנה היה זמין בריפו
או במסד — והסשן לא פתח אותו.** ⇒ **לפני שאתה קובע משהו על נכס קיים: קרא את המנגנון עצמו.**

## 7 · מה פתוח

| | הפריט |
|---|---|
| 🔴 | **14/10/2026 — סקריפט-הרענון.** חוסם-כנס |
| ⬜ | סיור-מסכים: נבדקו **בית · דיילות · כספים**. נותרו לקוחות · הצעות · פרויקטים · לוגיסטיקה · דוחות |
| ⬜ | המעבר על 22 דפי-הדוחות (`m11-report-inclusion-rules.md`) — **הכרעת-ישי, לא סשן** |
| ⬜ | מודול 11 עצמו: **אפס שורות קוד.** לשונית אחת מתוך ארבע מצוירת |
| ⚠️ | האופק: אירוע אחרון **24/01/2027**. ביום-הכנס = 100 יום קדימה |

</div>
