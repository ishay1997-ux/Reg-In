<div dir="rtl">

# סקירת טיוטת-האפיון של מודול הדוחות — 06/09/2026

> **מה נסקר:** `reports-architecture-draft-v3.1.md` (טיוטת-ישי, 05/09/2026, 973 שורות — נכתבה עם גישה מוגבלת לקוד).
> **איך:** קריאה מלאה · אימות מול המסד החי (Supabase, פרויקט `yfeovxppnfoafmfbdfvh`) · מול `docs/schema.sql`, `src/`, רשמי §6/§7, מפת-הדרכים · 9 סוכני-סקירה בעדשות שונות (111 ממצאים גולמיים; 28 מהם עברו מפריך עצמאי, כולם אושרו) · וכל ממצא שמופיע כאן אומת שוב ידנית לפני הכתיבה.
> **מה זה לא:** לא אפיון מאושר ולא הכרעה. **זה קלט לשלב 0 של ה-Discovery של מודול 11** — ההכרעות הן של ישי, בסבבי-ההכרעות.
> **הכלל למי שקורא:** כל מספר כאן נמדד ב-06/09/2026 על דאטה זרועה. הוא מתיישן. מדוד מחדש לפני שאתה מצטט.

## 0. השורה התחתונה

**הרעיון נכון, ההיקף לא נכנס, והקוד-לדוגמה לא יעבוד כמו שהוא.** הטיוטה מבינה נכון מה דוחות אמורים לעשות לחברה כזו (כל דוח פותח ב"שאלת החלטה" — וזה בדיוק מה שהרשם דרש ב-13/08: *"Discovery של מ11 מתחיל מהשאלה"*). היא שגויה בכל מקום שבו נדרשה גישה לקוד: שמות-עמודות, הרשאות, מספר-המודול, מה-כבר-בנוי, וערכי-פרמטרים. ובלוח של ~4 ימים + ~18 ימי חיץ עד 01/10 — ‏20 דוחות + 6 מודלים + שכבת-AI + 6 ווידג'טים במודולים סגורים אינם נכנסים בשום תרחיש.

**ההמלצה במשפט:** שלוש שכבות — ‏5–7 דוחות-בסיס (הצפוי) · ‏3–4 תובנות (ג'יני/לורנץ מול Smart Match, סיכון-אי-הגעה על המודל הקיים, תאריך-פירעון-צפוי) · תבלין-AI אחד לכל היותר — וכל השאר נרשם כ"אחרי הכנס" ב-§6 עם הסיבה.

---

## א. נכון ומתאים? — מה שבור מול המערכת שלנו

### א1. זהות ומיקום (מכני, בלי הכרעה)
| בטיוטה | במציאות | עוגן |
|---|---|---|
| "מודול 09" | **מודול 11.** מודול 9 = הגדרות-מערכת, בנוי ומוזג (`src/modules/09_settings/` — הטיוטה עצמה מצטטת ממנו את `SmartMatchPane.jsx`) | `docs/guides/00_roadmap.md` §2 שורה 11; §7 עץ-הקוד `11_reports/` |
| `src/modules/10_reports/` | `src/modules/11_reports/` | שם |
| `import { supabase } from '@/lib/supabaseClient'` | `'@/supabaseClient'` — הנתיב שבטיוטה **לא קיים** ושובר בנייה | `CLAUDE.md` §עובדות-סביבה, המוקש הראשון |
| קובץ בשורש `docs/` | אפיון-מודול חי ב-`docs/specs/module_11_reports/`; הקובץ הועבר ל-`research/` כקלט | `docs/CLAUDE.md` כלל-המיקום |
| ענף `ishay/module-09-reports` | המדריך מצפה ל-`ishay/module-11-reports` (נחתך 06/09) | `module_11_reports.md` ④ |

### א2. תפקידים והרשאות — הטיוטה בונה מודל משלה
- **5 תפקידים, לא 4.** מנהלת-פרויקטים — שיש לה `view` על דו"חות — **נעדרת לגמרי** מהטיוטה.
- **מנהלת-גיוס ומנהלת-לוגיסטיקה חסומות (`blocked`) על מודול דו"חות** — במטריצה המאושרת (`PROJECT_MASTER §3`) ובמסד החי (נמדד 06/09). ‏`<ProtectedRoute allow='דו"חות'>` שהטיוטה עצמה דורשת ב-§9.6 ידחה אותן **לפני** שלשונית כלשהי תוצג. לשוניות "כוח-אדם" ו"תפעול" שנבנו עבורן — לא ייראו לעולם ע"י מי שנועדו לו.
- **"הסתרה שקטה" אינה אבטחה ואינה הדפוס.** ישי הכריע פעמיים (‏§7.97 במסך-הבית · `FilterPill` במ4): מציגים וממסכים — *"לא זמין בתפקידך"* — לא מעלימים. והאכיפה האמיתית היא ב-DB (כלל ברזל 9).
- **תיאור ה-RLS הפוך:** ‏`salary_reports`/`salary_report_lines` פתוחות דווקא לבעלי 'כספים' (מנהלת-הכספים כן רואה שכר ברמת-שורה), ואילו `assignments` (עם `hourly_rate_snapshot`) גדורה על 'דיילות' — שהיא חסומה בו. ⇒ `fetchReportsData()` שבטיוטה יחזיר למנהלת-הכספים **אפס שיבוצים בלי שגיאה** ⇒ עלות-שכר 0 ⇒ "רווח גולמי 100%". זה הכשל השקט של RLS ש-`src/CLAUDE.md` מזהיר ממנו.

### א3. הקוד ב-§9.2 — לא ירוץ
- **22 מתוך 24 שמות-העמודות ב-`fetchReportsData()` אינם קיימים** (רק `params.param_name/param_value` נכונים). ‏`projects.id/name/event_date/status/closed_at/total_price/total_hours` → בפועל `project_id/event_name/final_event_date/project_status/operationally_closed_at` ואין מחיר/שעות בטבלה; ‏`assignments.shift_start/actual_start/hourly_rate/travel_payment/bonus_amount` → `hourly_rate_snapshot/travel_amount/personal_bonus`; ‏`customers.name/business_type/payment_terms` → `company_name/customer_type`, **ואין תנאי-תשלום פר-לקוח** (פרמטר גלובלי `תנאי_תשלום_ימים`=30); ‏`quote_services.service_name/quantity/unit_price/cost_estimate` → `sku/qty/closing_unit_price/closing_unit_cost`; ‏`hostesses.is_active` → `status`. **§2 של הטיוטה עצמה נכון כמעט כולו — §9.2 סותר אותו.**
- **`finance_project_money` היא פונקציה, לא טבלה** — אי-אפשר "לצרף" אותה ב-`select()`; והיא **חסומה לדפדפן** (`EXECUTE` רק ל-`service_role`; נמדד `has_function_privilege('authenticated', …) = false`). הקוראים המותרים: `get_finance_overview()`, `get_project_finance_detail()`, `get_dashboard_summary()` — כולם `security definer` עם `assert_module_permission`. השדות שהטיוטה מייחסת לה (`total_invoice_amount`, `balance_due`, `collection_status`) לא קיימים.
- **`project_changes` היא deny-all** (RLS דלוק, 0 policies — במכוון); הקריאה היחידה היא `list_project_changes(p_project_id)` פר-פרויקט. דוח 8 רוחבי דורש RPC חדש.
- ~~**תקרת 1,000 שורות של PostgREST — בשקט.**~~ ✏️ **תוקן 06/09/2026 04:0X — ישי העלה את התקרה ל-10,000 בהגדרות-Supabase, ונמדד חי:** קריאת-REST אחת כמנכ"ל החזירה **5,674 מתוך 5,674** שיבוצים. ⇒ הממצא הזה אינו חוסם עוד. ⚠️ **שני סייגים שנשארים:** ‏(א) ההגדרה חיה **בלוח-הבקרה של Supabase, לא ב-Git** — פרויקט/סביבה אחרת חוזרים ל-1,000 בשקט, ו-`src/api/fetchAll.js` עדיין מתעד 1,000 בכותרתו (סחיפת-תיעוד; לרשום ב-`supabase/migrations/CLAUDE.md` או ב-`db_roadmap`) · ‏(ב) הנימוק לסיכומים-בשרת נשאר במלואו — הוא SSOT-לכסף והרשאות (א4), לא נפח.
- **`written_off` יושב ב-`project_finance`, לא ב-`projects`** (יחד עם `final_profit`, `cancellation_fee`, `archived_at`).
- **`manual_discount` הוא אחוז, לא שקלים** — `finance_project_money` מחברת `applied_customer_discount + manual_discount` ומחלקת ב-100. "נרמול הנחות שקליות" בדוח 2 יעוות.

### א4. ארכיטקטורה — הפוכה למדריך ולתקדימים
הטיוטה: שליפה גורפת + חישוב בדפדפן (`selectors.js`, "P&L כפונקציות טהורות"). **המדריך של מודול 11 (⑤):** *"5 דו"חות כ-Views/RPC ב-DB (מהיר, RLS-מוגן)"*. **מודול 7 (§7.97):** סיכום בשרת אחרי שנמדד שהלקוח מקבל ≤1,000 שורות. **מודול 8 (`08_finance/CLAUDE.md`):** *"המסד הוא מחשבון-הכסף היחיד. ה-JS מציג, לא מחשב"*. **והטיוטה עצמה, §1.2:** *"שום מספר אינו מחושב מחדש בצד הלקוח"* — וסותרת את זה ב-§9.2. ⇒ לכל לשונית RPC-סיכום מגודר שמחזיר מספרים גמורים + דגלי-נראות; בדפדפן רק גזירות-תצוגה (כמו `src/lib/projectFinance.js`).

### א5. מספרים על הכרטיסיות — מוצגים כעובדות, לא תואמים למסד
| בטיוטה | נמדד 06/09/2026 | הערה |
|---|---|---|
| "סך דיילות פעילות (186)" | **50** פעילות · 136 לא-פעילות · 186 סה"כ | |
| "התייצבות ללא איחור 98.2%" / "96%" | **85.4%** (3,319 הגיעו · 360 איחרו · 208 לא הגיעו, מתוך 3,887 מסומנות) | איחור 9.3% · אי-הגעה 5.4% |
| "דיילות מצטיינות (48)" | 26 פעילות עם תג 'מצוינת' (128 בכל המאגר) — **והתג הוא פר-לקוח**, לא גלובלי | `customer_hostess_preference` |
| "יתרות חוב ₪105K" | ≈ **₪75.8K** (11 ממתינים-לתשלום) | |
| "11 פרויקטים בפיגור מעל 60 יום" | 11 ממתינים-לתשלום; **3** מהם מעל 60 יום מעבר לשוטף+30 | |
| "היענות 67%" | 552÷827 = 66.7% — **חישוב על כל הפרויקטים כולל מבוטלים ועתידיים.** לפי `feedback_status`: 552 נענו ÷ 712 נשלחו = **77.5%** | ההגדרה חשובה יותר מהמספר |
| "הכנסות ₪5,098,253 · רווח 59.2%" | ‏5.41M (בלי מבוטלים) / 4.75M (finished) · רווח 58–61% לפי אוכלוסייה | קרוב — אבל **"59.2% יעד" אינו יעד שמישהו קבע**, זה הממוצע; יעד מהממוצע צובע חצי מהפרויקטים אדום |
| "DSO = 38" | חציון חשבונית→תשלום 35, ממוצע 36.8 — **וזה לא DSO** (DSO = יתרת-חייבים ÷ מכירות × ימים) | לקרוא לזה "ימים לתשלום" |
| "מדד ג'יני 0.34 (מאוזן)" | **0.45–0.60** לפי הגדרת-האוכלוסייה (0.526 על 50 פעילות/משמרות מאושרות ב-12 חודשים; 0.456 על 106 שעבדו בפועל) | לפי מדרגי-הטיוטה עצמה — "התראת הטיית-כוכבות" ביום הראשון |
| 221/302/304 פרויקטים · 61 לקוחות · 4.13→4.25→4.41 · 7 חובות-אבודים | ✅ **נכונים** | |

### א6. פרמטרים והכרעות שהטיוטה סותרת
| הטיוטה | ההכרעה / הערך החי | איפה |
|---|---|---|
| "להעלות `משקולת_קרבה` מ-0.30 ל-0.40" | הערך החי **0.25** (היענות 0.40 · אמינות 0.35 · קרבה 0.25) | `params`, §7.14/§7.15 |
| "להעלות `שיעור_בונוס_הוגנות_לשבוע` מ-0.02 ל-0.05" | **0.05 נבחן ונדחה** ע"י ישי (08/08/2026) לטובת 0.02 — הטיוטה ממליצה לחזור למה שנפסל | §7.15↳ |
| "גולפוסט 40→30" | 50→40 הוכרע מנומק; 30 בלי מקור | §7.15↳ |
| ₪22.60 כקבוע | פרמטר `סכום_נסיעות_למשמרת`, ערך-בדין, **§7.69 עדיין 🟠** מול רו"ח | §7.69 |
| N≥3 כקבוע | פרמטר קיים `מינימום_תשובות_להצגת_ציון`=3 — לקרוא, לא להעתיק | `params` |
| רווח מחושב-מחדש בדפדפן מרכיבי-עלות | **רווח סופי קפוא ב-₪** ב-`project_finance.final_profit`; ‏% נגזר לתצוגה בלבד | §7.52 |
| סטיית-תקציב = אחוז-שעות "לא מחויבות" + ציוד + נסיעות; ספים 10%/15%/5–15% | **מספר אחד ב-₪, צד-עבודה בלבד**, כבר מוחזר ב-`budget_deviation` | §7.58 |
| CSAT רק על `feedback_score IS NOT NULL`; מבוטלים מוחרגים | ממוצע-משוב על `feedback_status='completed'`; **מבוטלים-שנפתרו נשארים** עם תג | §7.79 · §7.96 |
| "ציון משוב ≥4.5 ירוק"; "ניטרלי 3.5" | **ירוק 4–5 · צהוב 3 · אדום <3**; הציון שלם 1–5, ו-`סף_שביעות_רצון`=3 | §7.80 |
| "תזכורת גבייה" מהדוחות; "הפקת דוח-שכר" מהדוחות | דוחות = קריאה בלבד (C5 §5.7); דוח-השכר **בנוי ומוזג במ8** (`generate_salary_report` + `salaryReport.js` + `SalaryReportDialog`) ומ11 קורא-בלבד | §7.68 · `🚧 מ11 ← מ8` |
| מייל-תקציר אוטומטי ראשון 07:30 | *"דו"ח-מתוזמן = gold-plating… מנגנון אמיתי במ10"*; אין `pg_net`, 3 עבודות-cron כולן SQL-בלבד; `send-email` דורש משתמש מחובר | §7.57 · §7.33 · §7.36 |
| "סיכון-נטישה 0–100" עם 4 משקולות | 'לקוח רדום' כבר מוכרע ומיושם: `isCustomerDormant` + `סף_לקוח_רדום_ימים`=120 (מנכ"ל בלבד) | §7.79, `customerProjects.js` |
| מובייל/טאבלט | *"המערכת מותאמת לסביבת שולחן עבודה בלבד"*; והמעטפת (`mr-60`) בלי נקודות-שבירה | C5:62 |
| תזרים-מזומנים (דוח 7) | ✅ **מתיישב** — §7.93: "מ11 מקבל תזרים-מזומנים במקום רווח-נטו" | §7.93 |

### א7. עיצוב — שפה חדשה במקום המאושרת
- **סגול `#8B5CF6` "AI Sparkle"** כצבע-משמעות: לא בשפה (`src/CLAUDE.md`: *"אין סגול/כתום בשפת-העיצוב"*; ב-§4 סגול = קטגוריית-גרף בלבד). ‏`#10B981`/`#DC2626` שונים מהירוק/אדום המאושרים (`~#22C55E`/`~#EF4444`) ומ-`StatusTag`. משטח כהה `#0F172A` — אין מצב-כהה. אמוג'י בלשוניות — הבית הוא אייקוני lucide. כלל ברזל 8: אין צבעים חדשים בלי ישי.
- **"רווח ≥58% ירוק"** סותר את כלל-המילוי (07/08/2026): *"עובדה טובה אינה צבועה"*.
- **רכיבים שכבר קיימים ומחייבים:** ‏`KpiCard`→`StatTile` (+`Money` לסכומים; **₪ אחרי המספר**, לא לפניו כמו ב-wireframe) · תגים→`StatusTag` · שלד→`LoadingOrError` · מצב-ריק→`PermissionAwareEmpty` (3 מצבים: ריק/אין-הרשאה/תקלה) · טבלה→`ListWindow` (חלון-זמן + 50/עמוד, הכרעת 04/09) · `DrillDownDrawer`→קישור לכרטיס הקיים (פרויקט/לקוח/דיילת).
- **מילים:** "משפך" ברשימת המטאפורות האסורות; Bullet/Gauge/Treemap/Lorenz/Tornado/Cohort/DSO/CSAT — למנהלת-הכספים אין דרך לדעת. שם-הגרף = השאלה שהוא עונה עליה ("כמה מהמשמרות מרוכזות אצל מעטות").
- **"עודכן לאחרונה 10:45" + [רענן]** — הדאטה חי ממילא; חותמת אחת מעל דוחות-חיים ובאנר-AI ממטמון-6-שעות מטעה. `Date.now()` ב-render אסור (`react-hooks/purity`).
- **נגישות (חסר לגמרי):** ‏axe רץ בכל מסך ונכשל על critical/serious — גרף בלי `aria-label`, כפתור-אייקון בלי שם, מסך בלי `<h1>` (הסריקה ממתינה ל-h1 ותיפול). דוחות אינם ב-`e2e/accessibility.spec.js` — להוסיף.

### א8. המודלים — מה סותר את הנתונים או את הקיים
- **RiskScore (מודל 1)** ממציא מודל-אמינות שני לצד `reliabilityScore` הקיים (ריסון בייסיאני m=3, הכרעת-ישי) — שני ציונים שונים לאותה דיילת על שני מסכים. ואריתמטיקה: "דיילת חדשה מתחילה מ-20%" — לפי הנוסחה 40·(0+1)/(0+5) = **8**. רכיב-העייפות "<8 שעות": בנתונים 9 מתוך 499 זוגות-ימים-עוקבים (1.8%) — כמעט לעולם לא נדלק.
- **מודל 2 (תזרים):** "עיריות משלמות ב-10 לחודש" — 1 מתוך 56 תשלומי-`government` נחת ב-10; חציון יום-התשלום 17. "+3 ימים לחשבונית" — החציון 5. **מה שכן עולה מהמסד ותומך ברעיון:** עיריות משלמות באיחור חציוני 68.5 יום מול 34 לחברות ⇒ חציון-פר-לקוח (או פר-סוג) הוא המודל הנכון, בלי כללי-סבב.
- **מודל 5 (עונתיות):** "+45% בספטמבר–אוקטובר/מרץ–אפריל" — השיא בפועל **נובמבר–דצמבר** (33/31, 35/43), אפריל החלש ביותר (8/10). "‏−35% במבחנים" — אין נתון (5 שורות `hostess_unavailability` בכל המאגר). ⚠️ ועקרוני: **עקומת-הנפח בזריעה עוצבה בהכרעת-ישי כ"רעש+מומנטום", לא לפי עונתיות** — מודל שיתאמן עליה "יגלה" את הרעש שזרענו. ‏`@hebcal/core` = devDependency לסקריפט-הזריעה בלבד.
- **מודל 6 / דוח 14.א:** "אימוץ דרג 1" — **אין שום רישום** של הדרג שהאלגוריתם המליץ בשיבוץ (`rankCandidates` מחשב בזיכרון ומשליך); דורש עמודה חדשה + שינוי במ4 (סגור) + **יהיה ריק בכנס** (5,674 השיבוצים זרועים). **ג'יני + עקומת-לורנץ + Time-to-Fill כן חשיבים היום** (5,673 זימונים, 5,338 תגובות, ממוצע 18.1 שעות).
- **דוח 12 (דיוק-זמנים) + "גלאי-איחור בצ'ק-אין":** אין צ'ק-אין ואין חותמת-הגעה במערכת — הנוכחות היא תווית ידנית (`arrived/late[light|medium|heavy]/no_show`) שמנהלת-הפרויקטים מזינה בסגירה. דוח 12 = דוח 13.
- **דוח 15 (שימור):** אין תאריך-עזיבה ואין היסטוריית-סטטוס (`status` ∈ active/inactive; `updated_at` מרוכז ב-3 תאריכים). ותק מ-`created_at` (158 תאריכים) ו"ימים מאז משמרת אחרונה" — כן.
- **דוח 16:** ‏`salary_reports` = **0 שורות** בזריעה · `personal_bonus>0` = **0** ⇒ "דוח-שכר סגור" ופילוח-בונוסים ריקים בכנס. שעות×תעריף (5,674) ונסיעות (4,391) — מלא.
- **דוח 8:** ‏`project_changes` = **16 שורות** על 827 פרויקטים — טבלה כמעט ריקה.
- **מסנן "סוג אירוע/סוג כנס"** בחמישה דוחות — **אין עמודה כזו** בשום טבלה. "אזור צפון/מרכז/דרום" — אין; אפשר לגזור מ-lat/lng (826/827 מאוכלסים). "סגמנט לקוח" — כן: `private_company` 38 · `production_company` 13 · `government` 6 · `nonprofit` 4 (אין 'הייטק').

---

## ב. מה חסר? — דברים שאיש לא חשב עליהם

1. **שבעה חובות `🚧 מ11` ברשם §6 שהטיוטה לא מזכירה**, ושניים מהם הם הכרעות שמ11 חייב לקבל: **בסיס-העלות לרווחיות — `actual_qty` או `planned_qty`** (§7.22 ביקש שמ11 יכריע; הטיוטה מכריעה בשקט על `quote_services.qty`) · שורות-לוגיסטיקה שנולדו משינוי-תכולה נטולות מצביע-מקור (הכרעה ㉗ של מ5) · "מתי השתנה" אינו ניתן לחישוב (מ6) · חוזה-האספקה ממ8 (ה1·ה8·ה17·ה18) · §7.93 · **והוראת-הפתיחה:** *"הצעד הראשון של מ11 אינו לבנות דוח — אלא לעבור מודול-מודול ולשאול מה כל אחד לא שומר"*.
2. **סוד ה-AI.** ‏`VITE_GEMINI_API_KEY` = כל ערך `VITE_` נצרב לחבילת-הדפדפן הציבורית (שורה 1 ב-`.env.example`). התקדים היחיד: `supabase/functions/send-email` עם הסוד ב-`Deno.env` + אימות JWT + שער-מודול. ⇒ Edge Function שני, סוד ש**רק ישי** מגדיר (כלל 17), חשבון עם חיוב, טיפול בכשל/פסק-זמן. אין שום חבילת-AI ב-`package.json`.
3. **פרטיות:** §9.7 מבטיח "בלי מידע מזהה", אבל §5 ודוח 20 שולחים ל-Google **351 הערות-משוב חופשיות** ומייצרים "טיוטת-שימור מותאמת ללקוח". אין הכרעה בריפו על העברת מידע עסקי ל-LLM חיצוני (C5:102 מזכיר חוקי-פרטיות). **שאלה לישי, בסיפור:** *"הערה כמו 'הדיילת רחל איחרה והמנכ"ל שלנו כעס' — מותר שתצא לשרתי Google?"*
4. **שער-הקבלה של ה-Discovery:** לפחות **מספר אחד מחושב-ביד** מהנוסחאות (template §11) — הטיוטה לא נותנת אף אחד. עוגן מוכן: ג'יני 06/09 = 0.526 על 50 פעילות/משמרות מאושרות/12 חודשים.
5. **RPC-סיכום מגודרים** (`assert_module_permission` + דגלי-נראות) — הארכיטקטורה כולה; ומיגרציה אחת עם שער-הקלדה של ישי.
6. **הצהרת-הדאטה לכנס (קריטריון ④):** §1 מציג את הזריעה כ"צמיחה מואצת של החברה". אפיון-הזריעה של מ7 אמר במפורש: על הבמה *"כך המערכת מציגה מגמה"*, לעולם לא *"כך צמחנו"*. נוסח מוכן לשאלה: *"הנתונים מוזנים — בניתי מחולל שמייצר 800 פרויקטים בשלוש שנים; המערכת עצמה לא יודעת שהם מומצאים, וזה בדיוק המבחן"*.
7. **הגדרות שחסרות כדי שדוח יהיה נכון:** היענות = נענו ÷ (נענו+לא-נענו) · "ימים לתשלום" ולא DSO · אוכלוסיית-ג'יני (כל מי שעבדה בחלון, או רק 'פעילה' היום — 69 מ-106 שעבדו ב-12 חודשים מסומנות היום inactive) · "מצוינת" כתג-דיילת = איזה כלל-צבירה על תגי-לקוחות.
8. **מה C5 §5.7 ביקש ונשמט:** שדות דוח-הרווחיות (מזהה · אירוע · לקוח · רווח % · סטיית-תקציב ₪ · ציון-משוב) ודוח-הלקוחות (ח"פ · חברה · איש-קשר · סוג · כמות-פרויקטים · הכנסות · ממוצע-משוב); ייצוא Excel/PDF "בלחיצת כפתור"; ו**ההערה שדוחות תפעוליים חיים במסכי-העבודה** — ואכן: 5.7.1 במסך-הפרויקטים+מסך-הבית, 5.7.2 במ4, 5.7.3 בכרטיס-הלקוח (§7.99), 5.7.5 במ8.

---

## ג. על מה לוותר? — 20 הדוחות, אחד-אחד

| # | הדוח | המלצה | הסיבה במשפט (שאפשר לומר בכנס) |
|---|---|---|---|
| 1 | מגמות צמיחה ורווחיות רב-שנתי | ✅ בסיס | 3 שנות דאטה; רווח מ-`final_profit` הקפוא לפי §7.52 |
| 2 | הנחות מול רווח | ✅ בסיס (סיפור-קורלציה) | 656 הנחות-לקוח · 137 ידניות · מדרגי 0/1–5/6–10/10+ מפרידים היטב (556/471/135/50); רק למחוק "נרמול שקלי" |
| 3 | צווארי-בקבוק (משפך) | 🔀 מיזוג לתזרים | חציון אירוע→סגירה 3 ימים, →חשבונית 5; מסנן ">7 ימים" מחזיר 0 שורות; "משפך" מילה אסורה |
| 4 | פארטו/ריכוזיות לקוחות | 🔀 לתוך דוח-לקוחות אחד (עם 18+19) | C5 §5.7.3 = "לקוחות זהב" ברשימה מדורגת |
| 5 | רווחיות פרויקט תכנון/ביצוע | 🔀 המבט הרוחבי בדוח 1 (לפי לקוח/חודש) | פר-פרויקט כבר במסך-הכספים; המספרים מ-`get_finance_overview` |
| 6 | גיול-חובות | ✅ בסיס | מדרגי 30/60/90 = מוסכמת-עולם; 11 חשבוניות פתוחות בגיל 13–129 יום — כל המדרגים מאוכלסים; `deriveDaysOverdue` קיים |
| 7 | תזרים צפוי | ✅ בסיס | §7.93 הכריע שמ11 בונה תזרים; המודל = פרמטר-גלובלי + חציון-איחור פר-לקוח (עיריות 68.5 מול 34) |
| 8 | תוספות-שטח | ⏳ אחרי הכנס | 16 שורות בלבד; `project_changes` דורש RPC חדש |
| 9 | שעות מתוכנן/בפועל | 🔀 עמודה ב-1/5 | `budget_deviation` כבר מוחזר (§7.58) |
| 10 | קהל מול צוות | ⏳ v2 (אלא אם זה סיפור שלך) | הדאטה קיימת (`estimated_guests`/`actual_guests`); אין "מוזמנים-לדקה-לעמדה" |
| 11 | ציוד מתכלה | ✂️ | אין החלטה חוזרת מאחוריו — מחייבים לפי ההזמנה (הכרעה ⑳) |
| 12 | דיוק-זמנים מול Call Time | ✂️ | אין צ'ק-אין במערכת; רמות-איחור בדוח 13 |
| 13 | אמינות: איחורים/אי-הגעה | ✅ בסיס | 3,887 משמרות מסומנות; N≥3 מהפרמטר הקיים |
| 14 | איכות מול עלות | ⏳ v2 | "תגית-איכות" היא פר-לקוח; צריך כלל-צבירה |
| 14א | Smart Match: ג'יני/לורנץ/Time-to-Fill | ✅ תובנה (חלקי) | חשיב היום מהנתונים (0.526); **"אימוץ דרג 1" ⏳** — עמודה חדשה, ריק בכנס |
| 15 | שימור/עונתיות-מבחנים | ✂️ | אין תאריך-עזיבה, 5 שורות אי-זמינות; ותק/ימים-מאז-משמרת אפשר בתוך 13 |
| 16 | שכר+נסיעות לפי חודש | ✅ בסיס (תפעולי בלבד) | בונוסים=0 ודוחות-שכר=0 בזריעה; ההפקה במ8 |
| 17 | CSAT + סיבות | ✅ בסיס | 4.13→4.25→4.41 · 351 הערות · 398 סיבות-חיוביות · 83 שליליות; חלון-שנתי (מסך-הבית = 90 יום, §7.95) |
| 18 | סיכון-נטישה 0–100 | 🔀 להחליף ב"רדום" הקיים (120 יום) + מגמת-משוב | ציון עם 4 משקולות בלי עוגן; "שלוש עובדות שמנהלת-הלקוחות קוראת לבד" |
| 19 | תדירות הזמנות | 🔀 לתוך דוח-הלקוחות | הקלט של "רדום" |
| 20 | סנטימנט Gemini | ⏳ אחרי הכנס / רק אם זה ה-AI היחיד | הערות זרועות; שאלת-פרטיות; כפילות עם דוח 17 |

**ומעבר לדוחות:**
- **שורת "שאל שאלה עסקית חופשית"** — ✂️ (בדמו חי: או מבריקה או מביכה, בלי שליטה; אין הגדרה איך היא עונה).
- **תקציר-Gemini שבועי** — לכל היותר פיצ'ר אחד, יזום מהמסך, עם גיבוי-מחושב (§9.7.3 של הטיוטה); **המייל האוטומטי → מ10**.
- **6 הווידג'טים במודולים סגורים** — 2 לכל היותר: תג-סיכון בשיבוץ **על `reliabilityScore` הקיים** (לא מודל שני) · תאריך-פירעון-צפוי במסך-הכספים. השאר ⏳ — כל נגיעה במודול מוזג = רגרסיה מלאה שלו (יש לכל אחד E2E משלו).
- **מובייל** ✂️ (C5 דסקטופ; המעטפת לא רספונסיבית) · **הדפסה/PDF** ⏳ (אקסל בלבד — כמו דוח-השכר) · **חותמת-עדכניות + רענן** ✂️ · **מגירת-פירוט** → קישור לכרטיס הקיים.

---

## ד. ישים אצלנו? — הלוח והאומדן

- **הלוח:** מ11 מתחיל 07/09 עם ~4 ימים; אחריו מ10 (~יומיים) ומ12 (~יומיים); **~18 ימי חיץ עד 01/10** — ובהכרעת-ישי (כלל-החיץ) הם **לליטוש לפני הכנס, לא לבליעה במודול**. ‏STATUS מציג למ11 "24/09" — שני תאריכים בשני קבצים; ליישר.
- **הקצב שנמדד:** מ8 = 4 מסכים + דוח-שכר + 6 מיגרציות = 7 ימים מדיסקברי עד מיזוג; מ7 = 4 אריחים + לוח = יום.
- **הטיוטה במלואה:** 20 דוחות + 6 מודלים + 5 לשוניות + AI (Edge Function + סוד + חיוב) + cron + 6 ווידג'טים + Excel/PDF + מובייל + בדיקות + אודיט-סגירה ⇒ **10–14 ימי-עבודה מעבר להקצאה** — לא נכנס גם על חשבון כל החיץ, ומשאיר אפס ליטוש.
- **ההיקף המומלץ** (5–7 דוחות-בסיס על RPC-ים קיימים + ג'יני/לורנץ + 2 ווידג'טים + אקסל): **4 ימים + עד 3–4 ימי חיץ**, כולל דיסקברי-מקוצר ובלופרינט. נשארים ~14 ימי חיץ.
- **תשתית שהטיוטה מניחה ואינה קיימת:** Edge Function ל-AI · `pg_net`/מתזמן-מיילים · עמודת דרג-שיבוץ · עמודת צ'ק-אין · עמודת סוג-אירוע · תנאי-תשלום פר-לקוח · פרמטר יעד-רווח · `@hebcal/core` ב-dependencies.

---

## ה. מה שרד נקי — ושווה לשמור

- **טבלת §2** — כל השמות נכונים חוץ מ-`written_off` (בטבלה אחרת). ‏`positive/negative_feedback_reasons[]` **קיימות** במסד (מיגרציות 04/09) — `docs/schema.sql` הוא שמפגר.
- **"שאלת החלטה" לכל דוח** — זה בדיוק הפורמט שהרשם דרש; שמות-הדוחות בעברית צריכים להיות השאלות האלה.
- **דוח 6 מדרגי-גיול ודוח 2 מדרגי-הנחה** — מוסכמות-עולם עם מקורות, ומאוכלסות בנתונים.
- **דוח 7 תזרים** — מתיישב עם §7.93; **דוח 3 חשיפת-הזמנים** (717/712/711 מאוכלסים) — כחלק ממנו.
- **ג'יני + לורנץ** — רעיון נכון עם דאטה אמיתית; המדרגים = פרמטרים שישי מכריע, לא קבועים.
- **Time-to-Fill** — `invite_sent_at`/`responded_at` מאוכלסים כמעט במלואם.
- **§6.3 drill-down** — תואם C5 §5.7.3 ודפוס "פתח כרטיס פרויקט" הקיים.
- **§9.6 ניתוב** — `<Route path="reports">` + `allow='דו"חות'` + פריט-הסרגל ב-`constants.js` — **מילה-במילה נכון**.
- **ספריות:** ‏`recharts` (הותקנה 05/09 לצורך זה), `write-excel-file` (בשימוש במ8), `@react-pdf/renderer` (במ3) — קיימות.
- **מודל 2 בבסיסו** (חציון-איחור אמפירי פר-לקוח) ו**מודל 4 בבסיסו** (מרווח-ימים מול הקצב האישי) — הרעיונות נכונים; המשקולות והספים לא.

---

## ו. האם נדרש עוד מחקר-עומק?

| נושא | מה נדרש | מקור |
|---|---|---|
| מדרגי-ג'יני לחלוקת-משמרות | **לא מחקר-חוץ** — למדוד אצלנו ולהפוך לפרמטר; המקורות החיצוניים (0.2–0.4 להכנסות-מדינות; מחקר-אחיות 0.24→0.13) אינם על עסק כזה | הכרעת-ישי + פרמטר |
| מדרגי-גיול 30/60/90, DSO | **אין צורך** — מוסכמה ידועה ומצוטטת | ✅ |
| החלקה בייסיאנית / m | **אין צורך** — הוכרע m=3 במ4 | ✅ |
| עונתיות (חגים/מבחנים) | **לא מחקר-חוץ** — אין מספר עולמי לחברה הזאת; לגזור מקדם-חודשי מההיסטוריה שלנו, ולזכור שהזריעה היא "רעש+מומנטום" | הכרעת-ישי אם בכלל |
| ספי-צבע (רווח, סיכון, איחור) | **שאלה לישי בסיפור** ("מאיזה אחוז פרויקט נחשב אצלך חלש?") → פרמטר | הכרעת-ישי |
| תנאי-השימוש בדאטה של Gemini API | **כן, מחקר-חוץ קצר** — רק אם AI נכנס להיקף | 🔎 |
| "עיריות משלמות בסבב חודשי?" / "אירוע אחרי חצות ומשמרת ב-08:00 למחרת?" / "מנהלת-לוגיסטיקה מטאבלט באירוע?" | **מסנן-מציאות** — "קורה אצלך?" ‏"לא קורה" סוגר | ישי |

---

## ז. שאלות להכרעת-ישי (המלצה-תחילה; 4 בסבב הראשון)

1. **היקף.** להמליץ: 5–7 דוחות-בסיס + ג'יני/לורנץ + 2 ווידג'טים + אקסל; 13 האחרים, המודלים המשוקללים ו-AI → §6 "אחרי הכנס". *(משפט לכנס: "בניתי מעט דוחות שעונים על שאלה אמיתית ועובדים מקצה-לקצה, במקום עשרים שחציים היו מסך-הדגמה.")*
2. **מי רואה.** להמליץ: להשאיר את המטריצה (מנכ"ל · כספים · מנהלת-פרויקטים בצפייה); דוחות תפעוליים לגיוס/לוגיסטיקה חיים במסכי-העבודה שלהן. *(אם רוצה לפתוח — הכרעת-מטריצה שלו, המנכ"ל עורך במסך.)*
3. **AI.** להמליץ: תקציר-מנהלים אחד, יזום מהמסך, עם גיבוי-מחושב — **ורק אם** הסוד נכנס ל-Edge Function בזמן; אחרת → מ10. שאלת-הפרטיות על משוב-חופשי — לפני כל שליחה.
4. **בסיס-העלות לרווחיות** (`actual_qty` / `planned_qty`) — ההכרעה שמ11 חייב לפי §7.22. להמליץ: `planned_qty` (מה שהוזמן = מה שחויב, הכרעה ⑳), עם הערה מוצהרת על שורות-שינוי-תכולה.

*(בסבב שני: אוכלוסיית-ג'יני · יעד-רווח כפרמטר · ספי-צבע · כיוון ציר-הזמן בגרפים.)*

---

## ח. ✏️ תיקון-גישה (06/09/2026 04:0X, בעקבות ישי) — "אין דאטה" אינו "לוותר"

ישי: *"אתה חייב להביא לי סיבות אמיתיות למה לוותר על כל דוח ודוח… לא לפחד מההיקף, באנו לעבוד."* צודק — סעיף ג' למעלה חתך לפי "יש/אין עמודה", וזו לא סיבה: עמודה אפשר להוסיף. **המבחן הנכון לכל דוח, שלוש שאלות:** ‏① יש דאטה היום? ‏② אם לא — מה חסר, כמה זה עולה (ימים), והאם זה שינוי-תהליך (הכרעת-מוצר) או רק עמודה? ‏③ **איזו החלטה חוזרת, של מי, הדוח משרת?** — אם ל-③ אין תשובה, זו הסיבה היחידה הלגיטימית לוותר. **הטבלה הזו מחליפה את סעיף ג' כקלט לסבבי-ההכרעות.**

| # | הדוח | ① דאטה היום | ② מה חסר · עלות · סוג | ③ ההחלטה ומי מקבל אותה | המלצה (ברת-עקיפה) |
|---|---|---|---|---|---|
| 1 | מגמות רב-שנתיות | ✅ 3 שנים | — | מנכ"ל: תמחור/סגמנט לשנה הבאה | **בנה** |
| 2 | הנחות ↔ רווח | ✅ 656 הנחות-לקוח · 137 ידניות | לתקן "נרמול" (שניהם אחוזים) — דקות | מנכ"ל/כספים: מדיניות-הנחות | **בנה** (סיפור-קורלציה) |
| 3 | צווארי-בקבוק | ✅ 717/712/711 | סף ">7 ימים" → פרמטר; בזריעה החציונים 3/5 ימים ⇒ הדוח יראה "תקין" | כספים: איפה כסף נתקע | **בנה בתוך 7** (אותם שלושה תאריכים) |
| 4 | פארטו לקוחות | ✅ | — | מנכ"ל: תלות בלקוחות | **בנה בתוך דוח-לקוחות** |
| 5 | רווחיות פר-פרויקט | ✅ `final_profit` · `budget_deviation` | ליישר ל-§7.52/§7.58 — בהגדרה, לא בקוד | כספים: אילו פרויקטים דלפו | **בנה** (טבלה רוחבית + מסנן חריגה) |
| 6 | גיול-חובות | ✅ | — | כספים: את מי לגבות השבוע | **בנה** |
| 7 | תזרים צפוי | ✅ | חציון-פר-לקוח במקום "10 לחודש" (1/56) | כספים/מנכ"ל: מה נכנס החודש | **בנה** (§7.93) |
| 8 | תוספות-שטח | ◐ 16 שורות | RPC רוחבי (½ יום) **+ זריעת עוד שינויי-תכולה** — סקריפט קיים, ~שעה | מנכ"ל: האם תוספות מתומחרות | **הכרעה:** בנה אם זורעים; אחרת v2 |
| 9 | שעות מתוכנן/בפועל | ✅ `budget_deviation` | — | PM: אירועים שחרגו | **בנה כעמודה+מסנן ב-5** (לא דוח נפרד) |
| 10 | קהל ↔ צוות | ✅ `estimated_guests`/`actual_guests`/שעות | — (אין "מוזמנים-לדקה-לעמדה" — למחוק את המדד הזה בלבד) | מנכ"ל/PM: יחס דיילות-לאורח בתמחור | **ערך אמיתי, ~½ יום** — אם זה סיפור של ישי: בנה; אחרת v2 |
| 11 | ציוד מתכלה | ✅ `logistics` 1,771 (planned/actual) | **ההכרעה `actual_qty`/`planned_qty`** (חוב §7.22) — הכרעה, לא קוד | לוגיסטיקה/כספים: רכש | **הכרעה:** יש בחברה החלטת-רכש חוזרת? אם כן — בנה (½ יום); אם "מחייבים לפי הזמנה וזהו" — v2 |
| 12 | דיוק-זמנים (צ'ק-אין) | ❌ אין חותמת-הגעה | **א)** שדה "שעת הגעה" בטאב-הסגירה: עמודה + שדה, ~½ יום, ה-PM מזינה · **ב)** צ'ק-אין עצמי מהטלפון דרך דף-המשמרת הציבורי (תקדים: `PublicConfirmPage`): ~1–2 ימים + RPC ציבורי + הגבלת-קצב (חוב רשום) · **ובשניהם: הזריעה בלי שעות ⇒ ריק בכנס אלא אם נזרע** | גיוס: מי מאחרת ובכמה | **v2** — הערך מעל 13 הוא דיוק-דקות; המחיר הוא **תהליך חדש** (מי לוחץ, מתי). *משפט:* "ה-PM מדווחת קל/בינוני/רב כי זה מה שהיא באמת יודעת בסוף אירוע" |
| 13 | אמינות: איחורים/אי-הגעה | ✅ 3,887 | — | גיוס: את מי לא לשלוח / להזהיר | **בנה** |
| 14 | איכות ↔ עלות | ✅ `rating` · `hourly_rate` · תגי-לקוחות | **הכרעה:** כלל-צבירה לתגים (למשל "מצוינת אצל ≥1", "לא-לשלוח אצל ≥1") — ואז ½ יום | גיוס/מנכ"ל: העלאת-תעריף / הפסקת-שליחה | **בנה קטן** אחרי ההכרעה |
| 14א | Smart Match | ✅ ג'יני · לורנץ · Time-to-Fill (5,338 תגובות) · ❌ אימוץ-דרג | עמודת `recommended_rank` + כתיבה ב-`SmartMatchPage` (~½ יום); **ריקה בכנס** (5,674 זרועים) — זריעת דרגות מלאכותיות = מספר מומצא, לא מומלץ | מנכ"ל/גיוס: כיול משקולות | **בנה** ג'יני/לורנץ/TTF · **העמודה עכשיו**, המדד כשיצטבר שימוש |
| 15 | שימור/מאגר-חי | ✅ ותק (`created_at`, 158 תאריכים) · ימים-מאז-משמרת (`weeksSinceLastWorked` קיים) · נשירה (`inactive`) · ❌ "מתי הפכה ל-inactive" · ❌ מבחנים (5 שורות) | עמודת `inactive_at` — מיגרציה קטנה, ריקה על העבר | גיוס: מתי ומי לגייס | **בנה מחדש כ"מאגר חי"** — ✏️ **טעיתי כשגזרתי**; רק "מבחנים" נשאר בלי דאטה |
| 16 | שכר + נסיעות | ✅ 5,674 שעות×תעריף · 4,391 נסיעות · ❌ בונוסים=0 · ❌ דוחות-שכר=0 | **זריעה:** כמה בונוסים + הפקת דוח-שכר אחד (אוגוסט) דרך מ8 לפני הכנס — ~שעה | כספים/מנכ"ל: עלות-שכר לפי חודש | **בנה** (תפעולי) + זריעה |
| 17 | CSAT + סיבות | ✅ | חלון שנתי (מסך-הבית = 90 יום) | מנכ"ל: מה משמח/מכעיס לקוחות | **בנה** |
| 18 | סיכון-נטישה | ✅ | **הכרעה:** "רדום" הקיים (120 יום) או ציון-משוקלל חדש | כספים/לקוחות: למי להתקשר החודש | **בנה על הקיים + מגמת-משוב**; הציון המשוקלל — הכרעת-ישי |
| 19 | תדירות הזמנות | ✅ | — | אותה החלטה | **בתוך 18** |
| 20 | סנטימנט (LLM) | ✅ 351 הערות | Edge Function + סוד (צעד-ישי) + הכרעת-פרטיות — ~יום | מנכ"ל: נושאים שהתגיות לא תפסו | **הכרעת-AI:** אחד משניים (תקציר או סנטימנט) |

**כך זה נראה במספרים:** ‏13 "בנה" מיד · ‏4 "בנה אחרי הכרעה אחת" (8 · 11 · 14 · 18) · ‏1 "אם זה סיפור שלך" (10) · ‏1 v2 מנומק (12) · ‏1 הכרעת-AI (20). **לא 5 ולא 20 — וההכרעות עוברות בסבבי-ההכרעות של ה-Discovery, 3–4 בסבב, המלצה-תחילה.**

### ח2. תפקידים — האם לגיוס וללוגיסטיקה מגיע "משהו"? (שאלת-ישי, הכרעה שלו)
**מה מוכרע היום:** ‏C5 §5.4 + המטריצה: גיוס ולוגיסטיקה `blocked` על דו"חות; C5 §5.7 מסביר למה — הדוחות התפעוליים שלהן חיים במסכי-העבודה (שיבוץ במ4, לוגיסטיקה במ5). **מה שהטיוטה ראתה נכון:** דוחות 13 · 14 · 14א · 15 משרתים **החלטות של מנהלת-הגיוס** (את מי לא לשלוח, את מי לגייס, האם האלגוריתם הוגן) — לא של המנכ"ל. **המלצה:** לפתוח לה `view` על דו"חות (לחיצה אחת של המנכ"ל במסך-ההרשאות), כשהלשוניות-הכספיות ממוסכות "לא זמין בתפקידך" לפי הרשאת 'כספים' (הדפוס של §7.97); **לוגיסטיקה נשארת חסומה** — דוחות 9/11 הם החלטות של PM/כספים/רכש, ומסך-הלוגיסטיקה כבר נותן לה את ה"מוכנות". *משפט לכנס:* "מנהלת-הגיוס רואה את דוחות-הדיילות כי היא זו שמחליטה את מי לשלוח; היא לא רואה כסף כי היא לא מחליטה עליו."

</div>
