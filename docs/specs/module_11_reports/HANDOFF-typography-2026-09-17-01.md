<div dir="rtl">

# מסירה · מ11 — טיפוגרפיה וניסוח, פזה א'

> **ממשיך מ־:** אין. זו המסירה הראשונה בשרשרת `HANDOFF-typography-*`.
> *(המסירה הקודמת של מ11 — `HANDOFF-build-2026-09-16.md` — סגורה; המודול מוזג לייצור ב-17/09 12:23.)*
> **הכותב:** סשן `reg-in-9a`, 17/09/2026 13:47–18:3X. **הנמען:** סשן טרי שייקח את **פזה א' בלבד**.

---

## 🆕 בלוק 23/09/2026 ערב — פזה ב׳, סשן ראשון *(הבלוק החי. כל מה שמתחתיו — היסטוריה, ✏️ היכן שהתיישן)*

> 📏 נקרא לפני הכתיבה: כל מה ששני הצדדים כתבו בשיחה — 15:20 → 17:41 ·
> 16 הודעות של ישי (9KB) ו-47 שלי (21KB) · יחד 30KB.
> 334 קריאות-כלי נשמרו כשורות-סימון; פלטיהן (11047KB) לא נטענו במכוון —
> הראיה למה שנעשה היא `git log`, לא נרטיב. 0.3% מהתמליל הוא השיחה עצמה.
> ⚠️ **והחילוץ אינו כולל את תשובות-ישי שניתנו בחלונות-כלי** (ארבע תשובות-ההכרעה ושתי דחיות-התוכנית עם הערה) — **הן מועתקות למטה מהקשר-הסשן עצמו, מילה-במילה.**

### א · 📖 רשימת-קריאה לסשן הבא — **לקרוא, לא לגרפ** *(«גריפ רק כדי למצוא איפה, לעולם לא כדי לענות»)*

| # | מה | מה לקחת |
|:-:|---|---|
| 1 | **הבלוק הזה, עד סופו** | המצב · הצעד הבא · מילות ישי · הטעויות · המלכודות · מבחן-הקבלה (§ז) |
| 2 | `docs/plans/2026-09-17-module-11-typography.md` — **§7 "פזה ב׳"** + הבלוקים 🔄 ו-🗣️ שמעליו | שורת-סטטוס לכל שלב (✅/◐/✏️) · ארבע ההכרעות · **הרף "מוצר בוגר"** |
| 3 | `supabase/migrations/20260923180000_module11_l1_report_copy.sql` — **הכותרת (שורות 1–15) + מעבר על ההחלפות של פונקציה אחת** | מה המיגרציה עושה, ואיך היא בודקת את עצמה |
| 4 | `docs/specs/module_11_reports/phase-b-migration/` — `ops.mjs` (**כל הניסוחים החדשים יושבים שם, כנתונים**) · `gen-migration.mjs` · `md5check.mjs` · `tools/` | אם צריך לשנות ניסוח: **עורכים את `ops.mjs` ומריצים את המחולל — לעולם לא את קובץ ה-SQL ידנית** |
| 5 | `supabase/migrations/CLAUDE.md` | הפרוטוקול **שאחרי** ההחלה (schema.sql · db_roadmap · קומיט משותף) |
| 🟡 | `docs/specs/module_11_reports/stage2-review/m11-copy-rules.md` · `docs/plans/ui-copy-styleguide.md` | **רק אם כותבים ניסוח חדש.** הכללים כ1–כ20 · R1–R30 · H1–H6 |

### ב · 🌿 איפה — **פקודות, לא מספרים**

`git -C C:\Users\ishay\Reg-In-copy-wt branch --show-current` ⇒ `ishay/type-copy-phase-b` · `git log --oneline origin/dev..HEAD` ⇒ **חייב להחזיר שורות** (ריק ⇒ מוזג, עצור) · `git status --porcelain --untracked-files=no` ⇒ ריק.
**מה נדחף:** `git ls-remote --heads origin ishay/type-copy-phase-b` ⇐ זהה ל-`git rev-parse HEAD`.
**אין סשן כותב מקביל על הענף הזה.** סשן-הסגן (`local_2ca23fa2…`, "שתי תוכניות ביצוע") **מאפיין** את מחליפי מ3/מ4/מ6 — **הוא לא כותב לענף הזה.** 📡 הודעות בין-סשנים: הדיסק מנצח.

### ג · ✅ מה נעשה *(הראיה: `git log --oneline origin/dev..HEAD` — כל קומיט נושא את השלב בכותרתו)*

| שלב | מה | הראיה |
|---|---|---|
| שארית פזה א׳ | ‏11 הגדלים ב-`QuotesPage.jsx` ⇐ `text-xs`; קו-הבסיס ⇐ 5 (כולם `quotePdf.jsx`, נקודות-דפוס) | `node scripts/check-type-scale.mjs` |
| **8** | הגרף קורא את ה-`drill_key` **של הדאטום** (אותה דלת כמו השורה) — ‏**במ3 שלוש עמודות-השנים היו מתות**. שורת-יכולת מתחת לכל גרף לחיץ; שם-האלמנט לפי סוג-הגרף (עמודה/נקודה/פלח) | חי: מ3 ⇐ `{"kind":"year","year":2025}` · מ9 ⇐ `{"bucket":"d31_60"}` |
| **9** | **פקד-מת ⇐ תווית-עובדה** (הכרעת-ישי 5): `תקופה  נכון להיום` / `כל הזמנים` / `2024–2026`; `לקוח  כל הלקוחות`. שני משפטי "אינו מושפע" נמחקו מהלקוח | `FiltersBar.jsx` · `ReportsPage.jsx` `periodFact` |
| **4** ✅ | **דגם מ02 — אושר.** `src/components/Disclosure.jsx` · **שבב-היקף** (`population.summary`, ועד המיגרציה "מי נכלל בדוח") במקום שורת-האוכלוסייה + שורת-ההגדרות · **כותרת-כנה לרשימת-שיא** (`surface.topN` בקטלוג; גם חלון-הייצוא קורא ממנו) · רמזי מ2. ‏📏 **1,116 ⇐ 215** תווי-הסבר גלויים | צילומים ב-`scratchpad` (לא שרדו) — **לשחזור:** `docs/specs/module_11_reports/phase-b-migration/tools/` |
| **7** ✏️ | **בוטל למ04/מ06** (יוחלפו). הכותרת-הכנה למ02/מ07/מ19 נבנתה בשלב 4 | — |
| **10ב** + רמזי-הדוחות | ‏24 רמזי-מערכת + 26 רמזי-דוחות ⇐ ≤220. **שער חדש:** `npm run check:report-copy` (= `src/lib/onboardingCopy.budget.test.js`, רץ גם בשער) — נופל על רמז ארוך **ועל מפתח-רמז חסר** | `npm run check:report-copy` |
| **5 + 10** ⏳ | **ניסוחי 13 פונקציות-הדוח + מחיקת 2 גרפי-מ20 ואריח-מ22** — **כתובים במיגרציה, לא מוחלים** | ר' §ד |

### ד · 🔴 הצעד הבא הבטוח — **בסדר הזה**

1. **המיגרציה `20260923180000_module11_l1_report_copy` — ישי טרם הקליד את שמה.** ‏**אל תחיל בלי זה**, גם אם "אישר" משהו אחר. אם הוא מקליד בסשן החדש: הסבר קצר בעברית (מה משתנה במסך · מסד משותף עם האתר החי · הפיך) ⇐ `apply_migration` עם תוכן הקובץ **כפי שהוא**.
   ⚠️ **לפני ההחלה — הרץ שוב את בדיקת-ה-md5** (`md5check.mjs` מול `select proname, md5(prosrc) …` — השאילתה בראש `md5check.mjs`): אם גוף אחד השתנה מאז 23/09, ההחלפות עלולות לא למצוא את מטרתן. **המיגרציה תעצור את עצמה במקרה כזה (raise), אבל עדיף לדעת לפני.**
2. **אחרי ההחלה — אימות מהמסד:** לכל 13 הפונקציות `md5(prosrc)` השתנה; קריאה לכל אחת בזהות מאומתת (טכניקת-ה-claim: `## §3 · מה הושלם היום` למטה — ⚠️ **השאילתה שם בוחרת משתמשת עם הרשאה על `'כספים'`; לדוחות-הדיילות והלקוחות החלף ל-`'דיילות'`/`'לקוחות'`** — המודול-הבעלים של כל לשונית, `reportsCatalog.js`) ⇒ `population.summary` קיים · אין "אינו מושפע" ב-`jsonb::text` · מ20 ‏2 גרפים · מ22 ‏3 אריחים · מ15 `meta.period_filter_ignored = true`.
3. **הפרוטוקול של `supabase/migrations/CLAUDE.md`:** `docs/schema.sql` · שורת ה-⏳ ב-`db_roadmap.md §10ב` ⇐ ✅ APPLIED · קומיט משותף.
4. **הסרת הגיבוי המתוארך של מ9** ב-`ReportSurface.jsx` (`datumDrillKey` — הענף `${xKey}_key`) **ובדיקת-היחידה שלו** (`agingPayload` ב-`ReportSurface.test.jsx` — להוסיף `drill_key` לדאטומים ולמחוק את הציפייה ל-`bucket_key`).
5. **אימות-חי של 12 הדוחות** (לא מ3/מ4/מ6): 1024 · 1536 · מצב 0 ומצב 2 · מנכ"ל. הכלים: `tools/shoot.mjs` (`SHOOT_MODE` · `SHOOT_EVAL` · `SHOOT_PRE`) + `tools/ev-m02.js` (מודד תווי-הסבר גלויים וגדלים). **יעד מצב 0: ~150–220 תווי-הסבר גלויים לדוח** *(מ02 נמדד 215)* · גלישה 0 · `fontSize` ≥13px.
6. **אדוות:** `src/modules/11_reports/CLAUDE.md` אינווריאנט 4 (מילה-במילה: *"ברמה 0 הדף שמיש במלואו — שורת-האוכלוסייה, 'אז מה' והגדרות-המדדים הן בסיס"* — **נשאר נכון, אבל היום האוכלוסייה וההגדרות מקופלות בשבב; לרשום זאת שם**) · `onboarding-layer-contract.md §3` (הכרעת-ישי א׳ כבר הפכה אותו; לרשום את המימוש) · `docs/specs/module_11_reports/design-contract.md` (תבנית-ההעתקה למודול הבא — שורת-אוכלוסייה והגדרות כ"בסיס גלוי"; **בלי עדכון המודול הבא ייוולד בדפוס הישן**, אותו נימוק כמו §8 של התוכנית) · **fixtures של בדיקות-יחידה שמצטטות ניסוחים שהמיגרציה מוחקת** — `FinanceTab.test.jsx` (‏`'אינו מושפע ממסנן התקופה'` ‏:136 ו-:476 שבודק אותו) · `ExecutiveTab.test.jsx:174` · `CustomersTab.test.jsx:132` וכותרות-הגרפים ‏:263 ·:268 ·:651 · `KpiTile.test.jsx:26` — **הן ירוקות גם אחרי ההחלה (מטען מדומה), ולכן לא יתריעו — אבל מתעדות עולם שכבר אינו קיים; לעדכן ל-fixture שמשקף את השרת החדש** · רמזי-מ20 שמזכירים "סיבות שליליות" אחרי שגרף-השלילי נמחק (`reports.satisfaction.why` / `reasonsBasis`) — **לקרוא ולוודא שאינם מצביעים על גרף שאינו קיים** (כ15).
7. **שער מלא · `npm run smoke`** *(🪤 ודא מי על 5173 קודם — ר' מלכודת 18 במסירת-הייצוא)* · עדכון התוכנית (✅+SHA) · בלוק חדש **כאן** · הפרומט `~/.claude/plans/typography-phase-b-prompt.md`.
8. ➕ **הצעה שהועלתה לישי ולא הוכרעה:** מעבר על **מסלול-ההצגה של 10 הדקות בכנס** (הסדר שישי יציג) ב-1536, ורשימה של מה שעוד נראה "פרויקט סטודנטים". **לשאול אותו על הסדר — לא להמציא.**

### ה · 💬 מילותיו של ישי — ציטוט, תאריך 23/09/2026, סוג

| שעה | הציטוט | סוג | מה זה קבע |
|---|---|---|---|
| 15:20 | *"אשמח אם אפשר שתספיק כמה שיותר עד שתגיע בין 700 ל750 אלף קונטקסט וכמה שיותר סגור איתי את מה שצריך לפני העבודה ככה תעבוד בלי לעצור"* | 🔨 הכרעה-תפעולית | **לסגור את כל ההכרעות בסבב אחד בהתחלה, ואז לעבוד בלי לעצור** |
| 15:28 | *"לסריקות וכאלא סונאט מספיק לפעם הבאה"* | 🔨 הכרעה | כל סוכן-סריקה — `model: "sonnet"` במפורש (נשמר בזיכרון) |
| ~15:35 | ארבע התשובות: **"קובץ שינוי אחד במסד (מומלץ)"** · **"תחליט אתה"** (על ניסוחים שלא ראה) · **"למחוק כמאושר (מומלץ)"** (גרף "מה מכעיס") · **"חסכוני (מומלץ)"** | 🔨 הכרעות | הניסוחים במסד · הניסוח בידי קלוד · המחיקה · בלי צבא-סוכנים |
| ~15:38 | *"זה שהבנת שאני רוצה מוצר בוגר כמו בהייטק זה הבנת? בלי מלל מיותר או נוסחים מוזרים ולא מתאימים. כמובן ומצב הטמעה אמיתי ומובן."* | 🎯 **היעד** | **הרף לכל מחרוזת** — ר' §ו |
| ~15:40 | *"ואז אני יראה כמו מערכת SAAS אמיתית? המטרה בסוף זה להראות טוב בכנס (אני מציג בעשר דקות את המערכת) … אשמח אם תצליח האמת אבל בלי לוותר על איכות העבודה ובלי קיצורי דרך"* | 🎯 היעד + ❓ | הכנס הוא הקהל; **איכות לפני כמות** |
| 16:41 | דרך סשן-הסגן: *"אני מאשר הכל לפי המלצתך"* — מ3/מ4/מ6 יוחלפו; *"להשאיר גרפים טיפשים שלא אומרים כלום זה לא טוב... אני מעדיף איכות על כמות"* · ✅ ישירות 17:02: *"כן הכרעה שלי"* | 🔨 הכרעה | לא נוגעים במ3/מ4/מ6 |
| 16:44 | *"מעולה הבנת מצויין מה התכוונתי זה נראה הרבה יותר מקצועי. אגב גדלי הטקסט מספיק קריאים?"* | ✅ אישור-דגם + ❓ | **דגם מ02 אושר** (קלוד אמר במפורש שכך הבין, וישי לא תיקן) |
| 17:34 | *"מה נשאר לנו ?עוד הרבה? ביצעת הכל לפי תוכנית?"* | ❓ | ענה: 6/7 · ארבע סטיות (§ח) |
| 17:40 | *"עדכן מסמך המסירה וכתוב את הפרומט הקטן בצאט. תחשוב מה גרם לך להבין מה אני רציתי שתעשה ואיך וכו כדי שסשן חדש ידע בדיוק מה לעשות"* | 🔨 | הבלוק הזה + §ו |

### ו · 🎯 **מה גרם להבין מה ישי רוצה — ואיך לחזור על זה** *(בקשתו המפורשת)*

1. **הוא אמר את המטרה, לא את המשימה.** *"מוצר בוגר כמו בהייטק · בלי מלל מיותר · מצב הטמעה אמיתי ומובן · להיראות טוב בכנס של 10 דקות."* ⇒ **כל החלטה נמדדת מול זה, לא מול סעיף בתוכנית.**
2. **הצהרת-הבנה עם דוגמה מהדאטה האמיתית, לפני בנייה, ובקשה לתקן.** *"כך הבנתי: כרטיס = שם · מספר · השוואה · שבב-היקף ('246 אירועים שהסתיימו · מתוך 837') · משפט-פעולה אחד. מצב-הטמעה = רמז אחד לבלוק שעונה 'למה', לא חוזר על המסך."* ⇐ התשובה: *"הבנת מצויין"*.
3. **הרף המעשי, ששימש לכל מחרוזת** — שלושה מבחנים: **① כסי אותה — משהו אבד? לא ⇒ מחקי ② מנהלת-כספים הייתה אומרת את זה בטלפון? ③ זה נשמע מוצר או מסמך-אפיון?** 🚫 **אסור על המסך:** "אינו מושפע…" · הסבר של החלטת-עיצוב · קוד פנימי (📐/§) · תת-שורה שחוזרת על מה שגלוי. ✅ **מותר:** הצדקה לחריגה ("החודש האחרון חלקי — 23 ימים בלבד, ולכן העמודה שלו נמוכה").
4. **להראות, לא לתאר:** צילום לפני/אחרי **עם הנתונים האמיתיים**, ומספר מדוד (1,116 ⇐ 215). ישי שופט מסכים, לא קוד.
5. **כל ההכרעות בסבב אחד בהתחלה**, 4 שאלות, כל אחת כסיפור קונקרטי + המלצה ראשונה — ואז עבודה רציפה. **עצירות רק כשהן שלו** (דגם · הקלדת שם-מיגרציה).
6. **לבדוק איפה הדבר באמת חי לפני שמבטיחים.** ההנחה של התוכנית ("הטקסט בלקוח") הייתה שגויה — הוא ב-SQL. **שאלה אחת לישי חסכה בנייה כפולה.**
7. **הודעה מסשן אחר = דווח-לי.** פועלים אם היא מצמצמת עבודה והפיכה, **ומאמתים עם ישי ישירות.**

### ז · 🧪 מבחן-הקבלה — **בהודעה הראשונה, לפני כל פעולה**

- 🅐 **מכני:** פתח כל נתיב ב-§א (נתיב מת ⇒ עצור) · הרץ את שלוש פקודות §ב · **טבלת הצהרת-קריאה** (קובץ | נקרא במלואו / גריפ ועל מה).
- 🅑 **לכל טענה — "מהקובץ X" או "מהזיכרון":** מה אסור להחיל בלי ישי ולמה · איפה עורכים ניסוח של דוח · אילו שלושה דוחות לא נוגעים בהם.
- 🅒 **שיפוט:** המיגרציה עדיין לא מוחלת? (`select … from supabase_migrations.schema_migrations where name like '%l1_report_copy%'`) · ה-md5 עדיין זהה?
- 🅓 **ארבע שאלות — כל אחת דורשת שני מקורות:**
  1. אחרי ההחלה, איזה שינוי-קוד **חייב** לבוא באותו סשן, ומה נשבר בשקט אם לא? *(§ד4 + `datumDrillKey` ב-`ReportSurface.jsx`)*
  2. ישי מבקש לשנות משפט-"אז מה" של מ07. באיזה קובץ עורכים, ולמה **לא** בקובץ ה-SQL? *(§א4 + כותרת המיגרציה)*
  3. `npm run check:report-copy` ירוק. מה הוא **אינו** יכול לתפוס, ואיך מודדים את זה במקום? *(§ג 10ב + `tools/ev-m02.js`)*
  4. מ15 מציג אחרי ההחלה `תקופה  12 החודשים האחרונים`. מאיזה שדה בשרת זה נגזר, ובאיזו פונקציה בלקוח? *(`ops.mjs` מ15 + `readScope`/`periodFact` ב-`ReportsPage.jsx`)*

### ח · 🔴 במה קלוד טעה בסשן הזה — והנוסח המתוקן

| הטעות | הנוסח המתוקן | איך נתפסה |
|---|---|---|
| שיגר 3 סוכני-סריקה **בלי לשאול ובלי `model`** ⇒ ירשו Opus | סריקה = `model: "sonnet"` + שורת-עלות לפני | ישי, 15:28 |
| הבטיח ~150K לסוכן-החילוץ | **~324K בפועל** | מדידה בסוף הריצה — דווח לישי |
| הבטיח ~60–80K לסוכן-העיניים-הטריות על המסירה | **~161K בפועל** | מדידה — דווח לישי. ⇒ **סוכן Sonnet שקורא ריפו ומאמת ~20 טענות עולה ~150K; לתמחר כך** |
| אמר לישי "~30 מופעי 'אינו מושפע'" | **~24** בפונקציות שבתחום (סוכן-החילוץ ספר פר-פונקציה) | ספירה |
| `prettier --write` על `.md` מפורש — **עיצב מחדש שני מסמכים שלמים** (`*.md` ו-`docs` ב-`.prettierignore`, אבל נתיב מפורש עוקף) | revert + החלה-מחדש; **לא מריצים prettier על `.md`** | `git diff --stat` |
| "לחיצה על עמודה" גם מעל **גרף-קווים** | שם-האלמנט נגזר מסוג-הגרף; במ3 "לחיצה על שנה בגרף…" | בצילום |
| שורת "לחיצה על שורה…" **מעל** כותרת-הטבלה | הכותרת קודמת | בצילום |

### ט · 🪤 מלכודות חדשות *(נוספות על §8 למטה ועל מסירת-הייצוא §5)*

| # | המלכודת | מה לעשות |
|:-:|---|---|
| 31 | PowerShell 5.1: ‏`Out-File -Encoding utf8NoBOM` אינו קיים ⇒ קובץ-הודעה ריק ⇒ **"Aborting commit due to empty commit message"** | `[IO.File]::WriteAllText($m, $t.Replace("`r`n","`n"))` |
| 32 | PowerShell: **`${fn}` בתוך מחרוזת במירכאות כפולות נבלע** (אינטרפולציה) — שבר סקריפט-node שנכתב דרכו | כתוב קבצי-JS בכלי Write, לא דרך PowerShell |
| 33 | `Remove-Item Env:X` **נחסם** ("system path h:") | `$env:X=""` |
| 34 | רמזי `onboardingCopy.m11.hostesses.js` הם **template literals עם `${ltr('3')}`** — regex של `'`/`"` לא מוצא אותם | `tools/apply-hints.mjs --ltr` |
| 35 | 🔴 **מה שנראה על המסך בדוחות בא מה-SQL**, לא מה-JSX — גם כותרות-גרפים וחלונות-אריחים | לשנות ניסוח ⇒ `ops.mjs` ⇒ מחולל ⇒ **מיגרציה חדשה** (הקודמת append-only) |
| 36 | `lint-staged` מגבה ב-**`git stash` המשותף** בכל קומיט ("Backed up original state in git stash") | אל תניח שה-stash ריק; `git stash list` לפני כל stash משלך, ותחזיר לפי SHA |
| 37 | `vite` ב-worktree: **הטעינה הראשונה ריקה ~30 שניות** (optimizer) — צילום מוקדם = דף לבן | `preview_logs` עד "bundling" נגמר, ואז לצלם |
| 38 | שם-משתמש בדף-ההתחברות: placeholder הוא **`כתובת אימייל`** (לא "כתובת מייל" כמו ב-`evidence-common.mjs` הישן) | `tools/shoot.mjs` כבר מתוקן |

### י · ⏳ פתוח *(לא של הסשן הבא לבד)*

- **הקלדת שם-המיגרציה** — ישי.
- **מיזוג `ishay/type-copy-phase-b` ⇐ `dev`** — ישי, פעם אחת בסוף פזה ב׳ (הכרעת-ישי 23/09).
- **פזה א׳ ב-`dev` ולא ב-`main`** (נמדד 23/09: `merge-base --is-ancestor` של PR #146 מול `origin/main` = לא) ⇒ **באתר החי הכתב עדיין קטן** עד מיזוג ל-`main`.
- מחליפי מ3/מ4/מ6 — סשן-הסגן, לא כאן.

---

> 🔄 **23/09/2026 ~16:4X — מ3 · מ4 · מ6 יוחלפו בדוחות-החלטה חדשים** *(דווח-לי: הכרעת-ישי דרך סשן-הסגן — "אני מאשר הכל לפי המלצתך"; ✅ אושר ישירות בצ'אט: "כן הכרעה שלי")*. **בפזה ב׳:** לא לנסח מחדש, לא למחוק ולא להוסיף מפתחות-הטמעה למ3/מ4/מ6 · דפדוף מ04/מ06 (שלב 7) **בוטל** · המיגרציה אינה נוגעת בהסברים שלהם · שלב 8 שכבר נגע ב-`TrendsSurface` נשאר. הפירוט: התוכנית, הבלוק 🔄 שמעל "🗣️ אושר 23/09/2026".

> 🔄 **עדכון 23/09/2026 — פזה א׳ מוזגה (PR #146) וחלון-הייצוא מוזג (PR #150). מה כאן עדיין תקף ומה לא:**
> ✅ **תקף לפזה ב׳:** §1ב (פריטים 9–12, חובה לפני מחרוזת) · §1ג · §5ב כלל-הניתוב · §6 מילותיו של ישי · §7 הטעויות · §8 המלכודות · §10.
> ✏️ **התיישן:** §1א הנתיב "ענף `ishay/type-scale`" (מת — ענף חדש מ-`dev`) · §2 טבלת-המצב · §4 (משימת פזה א׳, בוצעה) · §5 גבולות מול הסשן המקביל (אין עוד) · §9 נכתב לפזה א׳ · §11 "בלי סוכנים" · §12 בלוק-ההדבקה.
> 📖 **המשימה של פזה ב׳ חיה בתוכנית** — `docs/plans/2026-09-17-module-11-typography.md` §7 "פזה ב׳", ובראשה בלוק-העדכון מ-23/09.
> 🧩 **הפרומט החי:** `~/.claude/plans/typography-phase-b-prompt.md` (לא הבלוק ב-§12).

> 📌 **מסמך-מסירה יחיד — הכרעת-ישי 23/09/2026:** *"שיהיה מסמך מסירה אחד שמתעדכן בסוף סשן"*.
> ⇒ **זה המסמך. בסוף כל סשן מעדכנים אותו במקום** — בלוק מתוארך חדש מתחת לבלוק הזה (מה נעשה · SHA · מה הבא · מלכודות חדשות),
> ומסמנים ✏️ כל שורה שהתיישנה. 🚫 **לא יוצרים `HANDOFF-typography-*` חדש.**
> ארבע מסירות מ11 שאינן קשורות (בנייה · שלב 3 · שתי מסירות-הייצוא) עברו ל-`docs/specs/module_11_reports/archive/`.
> 🌿 **ענף אחד לכל פזה ב׳:** `ishay/type-copy-phase-b` — גם הארכוב הזה, גם כל הטיפוגרפיה. **מיזוג ל-`dev` פעם אחת, בסוף** (הכרעת-ישי 23/09).

---

## §0 · שורת-המדידה (‏§4ג של מדריך-המסירה)

> 📏 נקרא לפני הכתיבה: כל מה ששני הצדדים כתבו בשיחה — 13:47 → 18:25 ·
> 21 הודעות של ישי (18KB) ו-110 שלי (62KB) · יחד 80KB.
> 331 קריאות-כלי נשמרו כשורות-סימון; פלטיהן (11039KB) לא נטענו במכוון —
> הראיה למה שנעשה היא `git log`, לא נרטיב. 0.7% מהתמליל הוא השיחה עצמה.

✅ **80KB < 400KB ⇒ נקראה השיחה כולה, שני הצדדים, מהשורה הראשונה.** לא הושמט דבר.
⚠️ **והמגבלה הכנה:** השורה הזו מוכיחה שהחילוץ רץ, **לא** שהוא נקרא. מה שמוכיח קריאה הוא §6 ו-§7.

---

## §1 · רשימת-קריאה — **נתיבים מלאים · לקרוא במלואם · לא לחפש**

🔴 **שורש-העבודה שלך: `C:\Users\ishay\Reg-In-copy-wt`** *(worktree, ענף `ishay/type-scale`,
‏`21f249fa`)*. כל נתיב בטבלאות הוא מלא ומוחלט. 🚫 **אל תעבוד ב-`C:\Users\ishay\Reg-In`** —
היא יושבת על `dev`, היא משותפת, ושם בדיוק נולדה תקרית-הענף של היום *(התוכנית §7 שלב 0)*.

⚠️ **תקרת-כלי-הקריאה היא 256KB של *בתים*, לא שורות** *(‏`~/.claude/references/session-handoff-guide.md` §4א;
‏`processes-approved.md` מחזיר בפועל `File content (300KB) exceeds maximum allowed size (256KB)`)*.
📏 **כל הגדלים כאן נמדדו ב-`wc -c` / `wc -l` בסשן הזה, 17/09/2026 19:0X** — לא הועתקו מזיכרון.
🔑 **והכלל שנלווה:** *גריפ רק כדי למצוא **איפה**, לעולם לא כדי **לענות**. בטווחים גדולים ככל
שהכלי מרשה. והגדרה שמספר נשען עליה — עד סופה.*

### 1א · 🔴 חובה לפני השורה הראשונה של קוד — **במלואם, בסדר הזה**

| # | הנתיב המלא | גודל | מה לקחת ממנו |
|:-:|---|:-:|---|
| **1** | `C:\Users\ishay\Reg-In-copy-wt\docs\plans\2026-09-17-module-11-typography.md`<br>🔴 **קרא את זה — זה העותק המגובה.** *(המקור `C:\Users\ishay\.claude\plans\snug-mapping-dragon.md` חי על המחשב בלבד: התיקייה מוחרגת מ-git ואין לה remote. שניהם זהים ב-md5 נכון ל-18/09 — ואם הם ייפרדו, **המקור הוא הסמכות והעותק צריך רענון**)* | 60,098 ב׳ · 573 ש׳ | **התוכנית עצמה.** §2 — הסקאלה וטבלת-המיפוי *(422 מופעים ⇐ 4 מחלקות)* · §6ד — **גבולות-הקבצים מול סשן-האקסל** · §7 שלב 0 — ה-worktree · §7 פזה א' — סדר הביצוע · **§7ב — 9 ההוראות של ישי** · §7ג — התפעול *(פרוסות · אסור `git add -A` · מתי עוצרים)* · §8 — האודיט הכן |
| **2** | `C:\Users\ishay\Reg-In-copy-wt\docs\specs\module_11_reports\HANDOFF-typography-2026-09-17-01.md` | 471 ש׳ · ‏~50KB | **המסמך הזה, כולו.** §4 — הצעד הבטוח · §5 — גבולות-הקבצים · §7 — הטעויות שלי · §8 — טבלת-המלכודות · **§9 — מבחן-הקבלה שאתה עונה עליו בהודעה הראשונה** |
| **3** | `C:\Users\ishay\Reg-In-copy-wt\CLAUDE.md` | 12,431 ב׳ · 124 ש׳ | §2 — האינווריאנטים *(LF בלבד · Expand-Contract · מסד יחיד לפיתוח ולייצור)* · **§3 — פנקס המלכודות השקטות** *(‏CRLF · `ms-`/`ps-` ב-RTL · `shadcn add` שדורס ל-LTR)* · §6 — 17 כללי-הברזל, וכלל 10 (Git) במלואו |
| **4** | `C:\Users\ishay\Reg-In-copy-wt\src\CLAUDE.md` | 7,147 ב׳ · 73 ש׳ | **§2.3 — אין `tailwind.config.js` בריפו, במכוון.** ‏Tailwind v4, הטוקנים יושבים ב-`@theme inline`. **זה מה שהופך את שלב 1 לאפשרי — ואת הטעות בו לגלובלית** |
| **5** | `C:\Users\ishay\Reg-In-copy-wt\src\index.css` | 5,389 ב׳ · 138 ש׳ | **היעד של שלב 1.** ראה איפה יושב `@theme inline` הקיים *(שם כבר יש `--color-*` ו-`--radius-*`, ו-**אפס** `--text-*`)* |
| **6** | `C:\Users\ishay\Reg-In-copy-wt\src\components\StatTile.jsx` | 3,606 ב׳ · 47 ש׳ | **הקובץ הראשון שתיגע בו, ו-14 קבצים מייבאים אותו.** ‏`:33` — `text-xs`/`text-slate-500` בתווית, `text-xl` בערך. **קטן, אבל הרדיוס שלו הכי גדול בכל המערכת** |
| **7** | `C:\Users\ishay\.claude\references\session-handoff-guide.md` | 42,625 ב׳ · 476 ש׳ | §3 — מבחן-הקבלה · §4א — **איך נבנית רשימת-קריאה וטבלת-מלכודות** *(מסריקה, לא מזיכרון)* · §4ג — `read-session.mjs`. 🔴 **קרא אותו לפני שאתה כותב את המסירה הבאה, לא אחרי** — זו בדיוק הטעות שהמדריך עצמו מודד |
| **8** | `C:\Users\ishay\Reg-In-copy-wt\docs\CODE_MAP.md` | 17,968 ב׳ · 134 ש׳ | **המפה — ופזה א׳ נוגעת ב-~70 קבצים בכל המודולים, לא רק בדוחות** *(87% מ-422 הגדלים הידניים יושבים מחוץ למודול 11 — התוכנית §6ג)*. **זה הקובץ היחיד שנותן לך את השטח לפני שאתה נכנס אליו.** ‏אותר על-ידי הסשן המקביל, והוא צדק — הוא חסר מהרשימה |

📌 **ומה שעליו אל תבזבז קריאה לפני שבדקת:** קבצי `CLAUDE.md` נטענים אוטומטית לפי
התיקייה שאתה עובד בה. בסשן הזה **השורש ו-`docs/CLAUDE.md` הוזרקו לבד בפתיחה** *(נצפה, לא הונח)*.
⇒ **בדוק מה כבר אצלך לפני שאתה מוציא עליהם קריאה.** פריטים 3 ו-4 נשארים ברשימה
כדי שתדע מה חייב להיות אצלך — לא כדי שתפתח אותם פעמיים.

### 1ב · 🔴 חובה לפני שאתה כותב מחרוזת אחת *(פזה ב', לא פזה א')*

| # | הנתיב המלא | גודל | מה לקחת ממנו |
|:-:|---|:-:|---|
| **9** | `C:\Users\ishay\Reg-In-copy-wt\docs\specs\module_11_reports\stage2-review\m11-copy-rules.md` | 59,425 ב׳ | **‏R1–R30 — כללי-הניסוח.** ‏`design-contract.md §④א` אומר עליו מילה-במילה: *"קרא אותו לפני שאתה כותב מחרוזת."* **R3 הוא זה שהופך את חוזה שלושת-החריצים מהבטחה לגדר** |
| **10** | `C:\Users\ishay\Reg-In-copy-wt\docs\plans\ui-copy-styleguide.md` | 49,824 ב׳ · 285 ש׳ | 🔒 **נעול.** חל על כל מחרוזת במערכת, לא רק על הדוחות |
| **11** | `C:\Users\ishay\Reg-In-copy-wt\docs\specs\module_11_reports\onboarding-layer-contract.md` | 19,362 ב׳ · 197 ש׳ | **§2 — `Hint.jsx` מחזיר `null` על מפתח חסר**, כלומר רמז שנמחק נעלם בשקט *(⇒ `check:report-copy` חייב ליפול גם על מפתח חסר, לא רק על חפיפה)* · **§3 — הכרעת-ישי מ-10/09 שהכרעתו מ-17/09 הופכת.** קרא את שתיהן לפני שאתה נוגע במצב 0 |
| **12** | `C:\Users\ishay\Reg-In-copy-wt\docs\specs\module_11_reports\design-contract.md` | 111,103 ב׳ · 1,271 ש׳ | **מתחת לתקרה — קריא במלואו, אבל כבד (~28K טוקנים).** אם אתה מקצץ: `:32` הפלטה · `:349` כיווניות *(רלוונטי במיוחד לגרפים)* · `:449` חוזה-הגרפים · `:1245` שכבת-ההטמעה. 🔴 **§8 באודיט התוכנית מחייב שהקובץ הזה יתעדכן** — אחרת המודול הבא נולד עם אותה בעיה |

### 1ג · ⚠️ **מעל התקרה — בטווחים, לעולם לא במלואם**

**שלושת הראשונים יחזירו שגיאה אם תנסה לקרוא אותם שלמים. הטווחים כאן נמדדו, לא נוחשו:**

| # | הנתיב המלא | גודל | מה לקרוא, בדיוק |
|:-:|---|:-:|---|
| **13** | `C:\Users\ishay\Reg-In-copy-wt\docs\specs\module_11_reports\processes-approved.md` | ❌ **320,695 ב׳** · 1,120 ש׳ | **שורות 1,088–1,120** *(4,876 ב׳)* — הפרק `## 👤 טיפוגרפיה וניסוח`, **שבע ההכרעות המוצריות עם מילותיו של ישי**. ➕ **שורות 571–579** — ‏ת4 *(ייצוא אקסל)* במלואה. 🔴 **ו-ת4ב, זו שהופכת אותה, אינה קיימת בענף שלך** *(נמדד 17/09 19:1X — `grep -c` = **0**)*: היא יושבת בקומיט `de054e30` של סשן-האקסל ותגיע במיזוג. **לקריאה מיידית בלי למזג:** `git show de054e30:docs/specs/module_11_reports/processes-approved.md` *(הקומיט יושב במאגר-האובייקטים המשותף — אומת ב-`git cat-file -t`)*. **קרא עד ה-✅ החותם, לא רק את הפתיחה** *(‏`docs/CLAUDE.md §3`: סעיף גדל ראש-ואז-זנב — ציטוט מהפתיחה מצטט את השאלה, לא את ההכרעה)* |
| **14** | `C:\Users\ishay\Reg-In-copy-wt\docs\db_roadmap.md` | ❌ **363,716 ב׳** · 1,831 ש׳ | **שורות 531–545** — ‏`## 10ב` והשורה העליונה *(`:533`)*: מה מיגרציית K1 עשתה, שני הפגמים שנתפסו לפניה *(‏overload של `CREATE OR REPLACE` · איפוס-ACL שהיה פותח 5 דוחות ל-`anon`)*, וכל שרשרת-הראיות שנמדדה **אחרי** ההחלה |
| **15** | `C:\Users\ishay\Reg-In-copy-wt\STATUS.md` | ❌ **361,958 ב׳** · 1,143 ש׳ | **שורות 1–60** *(12,702 ב׳)* — הלוח החי: שורת "עודכן לאחרונה" *(`:38`)* והמצביעים. 🔴 **ומעבר לזה אל תקרא** — הקובץ הפר את תפקידו *(‏`docs/CLAUDE.md §2`: "מצב פעיל עכשווי בלבד")* וגרר לתוכו נרטיב היסטורי |
| **16** | `C:\Users\ishay\Reg-In-copy-wt\docs\micro_guides\module-11.md` | ⚠️ **180,305 ב׳** · 789 ש׳ | **מתחת לתקרה אך יקר (~45K טוקנים) — אל תקרא שלם.** ‏**שורות 744–760**: ‏`## 9` ובתוכו **`D-42`** *(`:748`)* — מילותיו של ישי, מה נשקל ונדחה, ושמונה הטעויות שלי בנוסח המתוקן. **זו הגרסה הארוכה של §6 ו-§7 כאן** |

🔑 **איך קוראים טווח בלי לשרוף את החלון:** ‏`sed -n '1088,1120p' <path>` דרך Bash, או `Read` עם
`offset`/`limit`. **הגריפ מוצא את השורה; הקריאה עונה.** ומה שהמדריך מוסיף מעל זה: *הגדרה שמספר
נשען עליה — עד סופה*, גם אם זה חורג מהטווח שרשמתי כאן.

### 1ד · 🟡 **כנראה לא תצטרך — פתח רק על פער אמיתי, ואמור שפתחת**

| הנתיב המלא | גודל | מתי כן |
|---|:-:|---|
| `C:\Users\ishay\Reg-In-copy-wt\docs\CLAUDE.md` | 5,169 ב׳ · 66 ש׳ | **לפני הכתיבה-חזרה** — §4 פרוטוקול-האדוות *(כלל-ברזל 13)* · §2 מפת-ה-SSOT |
| `C:\Users\ishay\Reg-In-copy-wt\supabase\migrations\CLAUDE.md` | 8,073 ב׳ · 80 ש׳ | **רק אם תיגע ב-SQL.** 🔴 פזה א' אינה נוגעת, ומיגרציית K1 כבר הוחלה |
| `C:\Users\ishay\Reg-In-copy-wt\supabase\migrations\20260917150000_module11_k1_pagination_params.sql` | 15,212 ב׳ · 206 ש׳ | רק אם עולה ספק במה K1 עשתה. **היא הוחלה ואומתה חי** — §2 כאן |
| `C:\Users\ishay\Reg-In-copy-wt\src\modules\11_reports\CLAUDE.md` | 8,034 ב׳ · 38 ש׳ | 🚫 **שטח סשן-האקסל** — הוא מתקן שם את `:26` המיושן. קרא אם אתה חייב, **אל תערוך** |
| `C:\Users\ishay\Reg-In-copy-wt\e2e\CLAUDE.md` | 6,210 ב׳ · 62 ש׳ | רק כש-E2E נופל *(מוקש המשטח-הצף: `mouse.down()` ➔ המתנה ➔ `mouse.up()`)* |
| `C:\Users\ishay\Reg-In-copy-wt\docs\guides\01_estimation_log.md` | 66,980 ב׳ | כשתכתוב את רשומת-הכיול שבחוב *(§9 כאן)*. **התוכנית §8 מודה במפורש שלא נפתח** |

### 1ה · 🚫 **מה שבמפורש לא לקרוא — ולמה**

| הנתיב | גודל | למה לא |
|---|:-:|---|
| `C:\Users\ishay\.claude\plans\breezy-wibbling-sloth.md` | 341 ש׳ *(נכון ל-17/09)* | **התוכנית של סשן-האקסל. לא שטחך** — §4 כאן מספיק. **והיא משתנה תחתיך:** גדלה 248 ⇒ 341 שורות בתוך שעות היום, וזו בדיוק מחלקת-הכשל שתפסה אותי שלוש פעמים *(§6 כאן)* |
| `C:\Users\ishay\Reg-In-copy-wt\docs\specs\module_11_reports\build-2026-09-16\` | **2.79MB** | ראיות ובריפים מהבנייה המקורית. **לא רלוונטיים לטיפוגרפיה** |
| `C:\Users\ishay\Reg-In-copy-wt\docs\specs\module_11_reports\stage2-review\` *(15 הקבצים האחרים)* | **609KB** | סקירות-שלב-2 שכבר נוקזו להכרעות מאושרות. **החריג היחיד: `m11-copy-rules.md`** — פריט 8 למעלה |

---

## §2 · מצב נוכחי — מה קיים על הדיסק ובמסד ברגע זה

🔴 **אל תצטט מספר מהטבלה הזו — הרץ את הפקודה שלידו.** כל שורה כאן התיישנה בעבר בדיוק
ככה, **והערב אני עצמי אמרתי "28 קומיטים" פעמיים — המספר היה 26**, והסשן המקביל תפס אותי.

| מה | הפקודה שמחזירה את האמת | הערך ב-17/09 03:0X |
|---|---|---|
| הענף | `git branch --show-current` | `ishay/type-scale` |
| קומיטים שלא נדחפו | `git rev-list --count origin/dev..HEAD` | **26** |
| העץ | `git status --porcelain` | ריק |
| גדלים ידניים שנותרו | `node scripts/check-type-scale.mjs` | **14 מחלקות + 5 ב-PDF** — כולם לא שלך |
| המסד | ‏§3 כאן | מיגרציית K1 הוחלה ואומתה |

## ✅ פזה א׳ — סגורה (17/09/2026)

| השלב | מה נעשה |
|---|---|
| **0** | `npm install` ב-worktree *(היה חסר לגמרי)* |
| **1** | הסקאלה ב-`src/index.css` + שער `check:type-scale` **עם קו-בסיס**, ו**הוכח שהוא נופל** |
| **2** | **408 מתוך 422** הוחלפו · צירי-הגרפים · ושלושת השינויים ב-`StatTile` שהסקריפט פסח עליהם |
| **3** | **26 מסכים נצפו** — ב-**1024 · 1536 · 1920**. ממצא אחד *(טבלת-הדיילות)*, תוקן |
| **4** | `design-contract.md` יושר — 36 עוגנים, כולל שלד-ההעתקה למודול הבא |

✅ **הפער הזה נסגר 17/09 03:2X, בעקבות שאלת-ישי *"למה?"*** — והשאלה הייתה במקומה:
הנימוק שלי היה *"השינוי גלובלי ולכן זהה לכולם"* — **והוא נכון לגבי הכתב ולא לגבי מה שנשבר:**
תפקיד אחר רואה **תוכן** אחר, ומצב-ההטמעה **מוסיף טקסט** — וזה בדיוק מה ששובר פריסה. ‏**זו היתה הנחה, לא מדידה.**

| מה נבדק | משטחים | גלישה |
|---|:-:|:-:|
| מנכ"ל · **מצב-הטמעה** *(2–10 רמזים למשטח — הטקסט באמת נוסף)* | 16 | **0** |
| כספים · גיוס · לוגיסטיקה · מצב 0 | 48 | **0** |

🔴 **והסייג שבלעדיו "48" מטעה, והוא העיקר כאן: רק **16** מתוך ה-48 הראו תוכן אמיתי.**
‏**32 חזרו `masked`** — מסך-חסימה, בלי טבלה ובלי כרטיסים. מי כן ראה תוכן: **כספים — 12** בשלוש לשוניות ·
**גיוס — 4** בלשונית הדיילות. *(לוגיסטיקה חסומה על ארבעתן — נכון לפי מודל-ההרשאות, לא ממצא.)*
⇒ **הניסוח הנכון: עוד 16 משטחים עם תוכן אמיתי ו-32 מסכי-חסימה — לא "64 משטחים מלאים".**

⚠️ **ומה שעדיין לא נבדק, והוא היחיד:** **מצב-ההטמעה עבור שלושת התפקידים האחרים.**
‏ההיגיון אומר שאין בו חדש *(מסך ממוסך אינו משתנה עם רמזים)* — **אבל זה היגיון, לא מדידה, ולכן הוא רשום ולא נטען.**
🔴 **והפריט הדחוף ביותר שאינו קוד: 26 הקומיטים קיימים רק על המחשב של ישי.**
`git push -u origin ishay/type-scale` היא **גיבוי, לא מיזוג** — והיא דורשת את הרשאתו המפורשת.

📡 **ועל ערוץ-התקשורת, כי הוא אינו אמין:** הודעות בין סשנים **נכשלו בפועל**
*(מסשן-האקסל ליועץ — 6 פעמים)*, **וההודעות מצטלבו ×4** — כל צד הורה על מה שכבר תוקן.
⇒ 🔑 **הדיסק הוא מקור-האמת, לא הצ׳אט בין הסשנים.** לפני שאתה פועל על הודעה — `git log`.

✏️ **נמחקה כאן טבלת-מצב שנייה, יתומה — 22/09/2026, ע"י סשן-הסגן.**
היא הייתה שריד מגרסה מוקדמת של המסמך *(שורת-טבלה שבורה ואחריה שש שורות)*, ובה **שלוש טענות שקריות:**
*"פזה א׳ ❌ טרם התחילה. אפס שורות קוד שונו. זו כל המשימה שלך"* · *"10 קומיטים"* *(היו 36)* ·
*"`Reg-In` על `dev`"* *(הוא על `ishay/reportsfinance-round-helper`)*.
🔴 **ולמה היא הייתה מסוכנת ולא רק שגויה:** בלוק-ההדבקה מורה *"אם משהו סותר את הדיסק — הדיסק מנצח"*,
⇒ **סשן טרי היה קורא אותה, מאמין לה, ובונה מחדש פזה שכבר מוזגה ל-`dev` ב-PR #146.**
🔑 **טבלת-המצב היחידה שתקפה היא זו של §2 למעלה — וכל שורה בה נושאת את הפקודה שמחזירה את האמת,
במקום את הערך עצמו.** זה בדיוק ההבדל שהציל אותה מלהתיישן, וזה מה שהטבלה הזו לא עשתה.

**הסשן המקביל:** `Reg-In-export-wt` / `ishay/export-build` — חלון ייצוא לאקסל. **שטח שלו, לא נוגעים.**

---

## §3 · מה הושלם היום, עם הראיה

**מיגרציה `20260917150000_module11_k1_pagination_params.sql` — הוחלה 17/09 18:1X.**
‏5 מתוך 16 פונקציות-דוח קיבלו `p_page`/`p_page_size` (ברירות-מחדל 8·8·4·50·50 — **בדיוק מה שהמסך
הציג לפני**), ו-`report_m19_customers_overview` קיבלה `meta.row_total` אמיתי.

**שלוש בדיקות-קבלה, כולן אחרי ההחלה, כולן מהמסד החי:**

| הבדיקה | התוצאה |
|---|---|
| כפילויות (`having count(*) > 1` על `report_m%`) | **אפס שורות** |
| ‏`proacl` על כל חמש | `{postgres=X,service_role=X,authenticated=X}` — **`anon` נעדר** |
| ספירת שורות בזהות מאומתת אמיתית | מ02=8 · מ19=8 · מ07=4 · מ04=50 · מ06=50 — **זהה ללפני** |
| ‏`row_total` חדש | מ19=**52** · מ07=**35** |

🔑 **טכניקת-הבדיקה, כי תצטרך אותה שוב:** ‏`assert_module_permission` נשען על `auth.email()`,
שאינו קיים בחיבור-שירות. ⇒ מזריקים claim לפני החלפת-תפקיד:
```sql
select set_config('request.jwt.claims',
  json_build_object('email', u.email, 'role','authenticated')::text, true)
  from public.users u join public.permissions p on p.role_id=u.role_id
  join public.modules m on m.module_id=p.module_id
 where u.status='active' and m.module_name='כספים' and p.permission_level in ('edit','view') limit 1;
set local role authenticated;   -- רק אחרי ההזרקה
```
**מהמסד: הורץ בפועל פעמיים היום (dry-run ואחרי ההחלה).**

---

## §4 · 🔴 הצעד הבא הבטוח — ומה **לא** לעשות

**המשימה שלך: פזה א' בלבד** — `~/.claude/plans/snug-mapping-dragon.md` §7, "פזה א'".
בשלושה שלבים, ובסדר הזה:

0. 🔴 **`npm install` בתוך ה-worktree — לפני כל עריכה.** אין שם `node_modules` *(נמדד 17/09 19:2X)*,
   ולכן `npm run gate` · `verify` · `test:run` · `build` פשוט לא קיימים. **ר׳ מלכודת 10.**
1. **סקאלה ב-`src/index.css`** בתוך `@theme inline` הקיים + **שער `check:type-scale` עם קו-בסיס**.
   🔴 **ולמה קו-הבסיס קריטי ואינו פרט-מימוש:** הסשן המקביל כותב **עכשיו** קוד חדש (`ExportDialog.jsx` · `QuotesPage.jsx`) **בלי השער**, כי השער יושב בענף שלך בלבד. ⇒ **ביום המיזוג הוא ירוץ לראשונה על קבצים שנכתבו בלעדיו.** בנה את קו-הבסיס כך ש**חריגות קיימות מותרות ורק חריגה חדשה מפילה** — אחרת המיזוג נצבע אדום על קוד של סשן אחר, והשער הראשון שמישהו יבקש לכבות הוא זה שנולד חוסם.
2. **ריפקטור 422 המופעים** (‏`text-[Npx]`) לפי טבלת-המיפוי בתוכנית §2.
3. ✅ **רגרסיה מצולמת — בוצעה 17/09/2026 ב-1024px, הרוחב שהתוכנית §8 סימנה כסיכון.**
   ‏**כל 16 משטחי-הדוחות** *(ארבע הלשוניות, מנכ"ל, מצב 0)* ‏**ו-7 מסכי המודולים האחרים** —
   והשניים הם העיקר, כי **87% מהשינוי יושב שם ו-205 מקומות זזו בלי להופיע ב-`git diff`.**
   📏 **המדידה, והיא לפני/אחרי אמיתי** *(ה-"לפני" נמדד מול שרת ה-`dev` על 5173, לא הונח)*:

   | המסך | גלישה לפני | גלישה אחרי |
   |---|:-:|:-:|
   | בית · לקוחות · הצעות · פרויקטים · לוגיסטיקה · כספים | 0 | **0** |
   | ‏16 משטחי-הדוחות | — | **0** |
   | 🔴 **דיילות** | **65px** | **84px** |

   ⇒ **שבע משמונה נקיים. השמיני היה שבור לפני שנגעתי — והחמרתי אותו ב-19px.**
   ‏🔑 **וזו הסיבה שמדדתי את ה-"לפני" ולא הנחתי:** בלי המספר 65 הייתי מדווח רגרסיה שלא גרמתי,
   או — גרוע מזה — מנסה "לתקן" באמצעות הקטנת הכתב בחזרה, כלומר מבטל את פזה א׳ כדי להסתיר באג ישן.
   ‏📁 הצילומים: `<scratchpad>/ev1024/evidence/` *(16)* · `<scratchpad>/cross1024/evidence/` *(7)* · `<scratchpad>/before1024/` *(השוואה)*.
   ⚠️ **ומה שלא נבדק, מוצהר:** זהות אחת בלבד (מנכ"ל) · מצב 0 בלבד · ודיאלוגים לא נפתחו
   *(`ClosingTab` · `ScopeChangeDialog` · `SalaryReportDialog` — **שלושת הקבצים הצפופים במערכת, והם בתוך מודאלים**)*.
   ✅ **בוצע 17/09 22:5X — שלושת הצפופים נבדקו ב-1024px, וכולם נקיים:**
   ‏`ClosingTab` על פרויקט **1122** *(11 שורות-דיילות, התצוגה הנעולה)* · `ScopeChangeDialog` על **1609** · `SalaryReportDialog` — **גלישת-דף 0 בשלושתם, אפס שגיאות-קונסול.**
   🔴 **ושתי מלכודות שנתפסו בדרך, והן שווות יותר מהצילומים:**
   ① **הלשונית אינה נשלטת מה-URL** — `ProjectCardPage.jsx` *(עוגן: `useState(TAB_KEYS.logistics)`)* מחזיק אותה ב-`useState`. ‏`?tab=closing` **נבלע בשקט**
   והכרטיס הראה את לשונית-ברירת-המחדל. **הפתרון: ללחוץ על הפקד, לא לנווט לפרמטר.**
   ② 🔴 **והחמורה: טענת-הבדיקה שלי עברה על המסך הלא-נכון.** בדקתי *"יש שורות בטבלה"* — וללשונית-הלוגיסטיקה **יש** שורות ⇒ המבחן החזיר ירוק על צילום של מסך אחר לגמרי.
   ‏🔑 **הכלל שנולד, והוא אותה מחלקה כמו "422 ⇐ 14": טענת-בדיקה חייבת לנקוב בשם המסך שהיא מצפה לו,
   לא לשאול "האם משהו שם".** תוקן ל-`aria-selected` שמכיל "סגירת", ואז נפל כמו שצריך.
   ⚠️ **ומה שעדיין לא נבדק:** זהות אחת (מנכ"ל) · מצב 0 בלבד. **ארבעת התפקידים האחרים ומצב 2 פתוחים.**
4. 🔴 **עדכון `design-contract.md` ו-`onboarding-layer-contract.md` — חלק מפזה א׳, לא אחריה.**
   התוכנית §8 אומרת זאת מפורשות: *"אחרת המודול הבא ייוולד עם אותה בעיה — זה חלק מהעבודה, לא אחריה."*
   📏 **נמדד 17/09 20:0X — 48 עוגני-טיפוגרפיה ב-`design-contract.md`:** ‏15 מחלקות `text-*`
   *(ובהן 5 קשיחות: `text-[11px]` ×2 · `text-[12.5px]` ×2 · `text-[13.5px]`)* ‏**ו-33 הצהרות `font-size:` בשלד-ה-HTML של §⑥**
   — ואלה החמורות יותר: **10 מהן מתחת ל-12px** *(11px ×6 · 10.5px ×2 · 11.5px · 10px)*, **והשלד הוא
   תבנית-ההעתקה למודול הבא.** ⇒ **בלי לעדכן אותו, המודול הבא ייוולד עם 10px — בדיוק מה שפזה א׳ מוחקת.**
   ⚠️ **והעוגן הקריתי ביותר: §2.2 שם מצטט את מחלקות `StatTile` מילה-במילה** *(`text-xs` · `text-xl` · `text-[11px]`)*
   — כלומר **השינוי הראשון שלך בשלב 2 סותר את החוזה באותו רגע.** עדכן אותו באותו קומיט.
   ‏*(הסשן המקביל מסר 103 — **מדדתי והמספר הוא 48; המסקנה שלו נכונה ואף חמורה מששיערתי תחילה**)*

🚫 **מה שאסור לך, ואלה גדרות של ישי ולא העדפות:**
- ✏️ **תוקן 17/09/2026 — השורה הזו איחדה שני דברים שונים, וזו היתה קריאה-יתר שלי בכלל-ברזל 10.**
  🔴 **אסור, ולא השתנה: אין PR · אין מיזוג ל-`dev`/`main` · אין `--force`.** מיזוג דורש הרשאה
  **מפורשת, בזמן-אמת, בגוף-ראשון, בצ׳אט של אותה שיחה** — והיא לא ניתנה.
  ✅ **אבל דחיפת הענף האישי עצמה אינה אסורה בשום מקום.** כלל 10 אומר מילה-במילה:
  *"לעולם לא לדחוף ל-`main`/`dev` **ישירות** ולא `--force`. עבודה רק על ענף אישי `ishay/<נושא>`"* —
  ודחיפה של `ishay/type-scale` היא **גיבוי של ענף אישי**, לא נגיעה בענף משותף.
  🔑 **הלקח, והוא הסיבה שהשורה תוקנה ולא נמחקה: ציטטתי כלל מהזיכרון והחמרתי אותו**,
  ואז **הסתמכתי על ההחמרה שלי כאילו היא הכלל** וחסמתי בגללה גיבוי של 31 קומיטים.
  ⚙️ **הפקודה, כשהיא רצה:** `git push -u origin ishay/type-scale` ⇐ אימות `git ls-remote --heads origin ishay/type-scale`.
- **אין `apply_migration` נוספת.** ההרשאה שניתנה הייתה ל-K1 בלבד, והיא מומשה. **מהצ'אט.**
- **אל תיגע בחמשת קבצי-הייצוא ובשני קבצים נוספים** — ר' §5.

---

## §5 · גבולות-קבצים מול הסשן המקביל — **בדוק בפועל, אל תסמוך על הטבלה**

> ✏️ **23/09/2026: היסטורי.** `ishay/export-build` מוזג ל-`dev` (PR #150) והענף נמחק. אין בעלים מקביל לאף קובץ.

| הקובץ | שייך ל־ |
|---|---|
| 🔴 **כל `Export*` ב-`src/components/`** *(היום: `ExportDialog.jsx` · `ExportConfigPanel.jsx` · `ExportPreviewPanel.jsx` · `ExportDialog.test.jsx`)* · `src/modules/11_reports/components/ExportBar.jsx` · `src/lib/reportsExport.js` · `src/modules/11_reports/exportFetch.js` · `src/lib/exportColumns.js` · `src/lib/exportFilters.js` | **סשן-האקסל** |
| `src/modules/03_quotes/QuotesPage.jsx` *(11 גדלים ידניים — **דלג עליו בפזה א'**)* | **סשן-האקסל** |
| `e2e/reports.spec.js` → הבלוק `describe('מודול 11 · ייצוא (ת4)')` | **סשן-האקסל** · השאר שלך |
| `src/modules/11_reports/CLAUDE.md` | **סשן-האקסל** מתקן שם את `:26` המיושן |
| כל השאר | **שלך** |

✏️ **17/09 21:4X — הגבול שונה מרשימה לתבנית, וזה הלקח החשוב יותר מהתיקון:**
סשן-האקסל פיצל את `ExportDialog.jsx` לשלושה, ושני החדשים נחתו **בתוך `src/components/`** — בלב שטח הריפקטור. ‏**רשימת-קבצים מתיישנת ברגע שמישהו יוצר קובץ**; תבנית לא.
⇒ **הגבול הוא עכשיו `Export*`, לא שלושה שמות.**
‏📏 **ומה שבדקתי בעצמי לפני שרשמתי את זה** *(הסשן המקביל מסר את שני הקבצים כעובדה)*: **הם אינם קיימים בענף שלך** — `git ls-tree HEAD src/components/` מחזיר אפס `Export*`, והם יושבים רק על `ishay/export-build`. ⇒ **ריפקטור-422 מעולם לא נגע בהם ולא יכול היה;** השורה כאן היא לקראת המיזוג.

🔴 **ומה שהמדידה של היום לימדה:** שמות הקבצים של הסשן המקביל **כבר השתנו פעם אחת** (‏`reportsColumns.js` ⇐ `exportColumns.js`, ו-`ExportDialog.jsx` עבר ל-`src/components/`). ⇒ **הטבלה הזו היא תצלום, לא חוזה.** לפני שאתה נוגע בקובץ — `git log --oneline -5 ishay/export-build` ו-`ls`.

⚠️ **התנגשות אחת מוצהרת ובלתי-נמנעת: `package.json`.** הוא מוסיף `dnd-kit`, אתה מוסיף `check:type-scale`. **שורה אחת, נפתרת במיזוג.** **מהקובץ: שתי התוכניות מצהירות זאת.**

---

## §5ב · 🔴 כלל-הניתוב — מי מכריע מה

*(מדריך-המסירה §4א-2 כלל 10. נוסף כאן אחרי שנמדד שהוא חסר, ואחרי שעשיתי בדיוק את הטעות שהוא מונע.)*

| סוג הפריט | למי | דוגמה מהלילה הזה |
|---|---|---|
| **מכני** — מדידה סוגרת אותו | **הסשן עצמו.** מדוד, אל תשאל | *"גודל הכפתורים"* — שלחתי לישי כהכרעה עיצובית; **`grep` סגר אותו בדקה** |
| **ספק במכניקה** — דרך-ביצוע, סדר, כלי | **סשן-הייעוץ** *(האצלת-ישי 17/09 17:53)* | סדר הפזות · מזהי-הפרויקטים לצילום · אישור דחיפה |
| 🔴 **מוצרי** — מה יוצא מהמסך, מה מקבל כרטיס, היפוך הכרעה קודמת | **ישי בלבד** | טבלת-הדיילות הגולשת · מה נמחק בפזה ב׳ |

🔑 **ושתי הטעויות המשלימות, ששתיהן נעשו הערב — ולכן השורה הזו קיימת:**
① **להעלות לישי שאלה שמדידה סוגרת** — בזבוז שלו וחסימה על כלום.
② **להכריע במקומו כי "זה קטן"** — הצורה המסוכנת יותר, כי איש לא ידע שהוא לא נשאל.
➕ **ואל תעצור את כל העבודה על פריט אחד:** רשום אותו, עצור **עליו**, והמשך בשאר.

---

## §6 · 💬 מילותיו של ישי — ציטוט, תאריך, וסוג

*(‏§4ג: זה אחד משני הפרקים שרק מי שקרא את השיחה יכול לכתוב. כל הציטוטים 17/09/2026.)*

### 🔨 הכרעות

| הציטוט | מה זה הכריע |
|---|---|
| *"לשים לב שמצב 0 זה מצב מוצר הייטק בוגר זה צריך להיות ממש נקי... לכן יצרתי את מצב הטמעה שכל ההסברים יהיו שם"* (13:47) | הכיוון כולו — מצב 0 נקי, ההסברים עוברים לשכבת-ההטמעה |
| *"תמשיך לחקור ולבדוק הראש שלי זה בלי קיצורי דרך ותיקון עמוק אם צריך"* (14:26) | עומק מלא, לא טלאי — ומכאן נולדה פזה א' חוצת-המערכת |
| *"אני לא מסכים איתך ואין לי כח לפרט אתה כבר נוגס בדברים שקשורים לעבודה לו פשוט תתעדכן בתוכנית שלו"* (15:26) | **גבול-תחומים בין סשנים.** לא לנתח את תוכנית האחות — להתעדכן ממנה |
| *"דבר איתו אין לי גם ככה הבנה בזה תבקש ממנו אישור אם הוא מאשר לך תכיל"* (17:53) | האצלת **מכניקת-ביצוע** לסשן-הייעוץ |
| *"את הכרעות המוצר עשית איתי פה ואנחנו סגורים אבל הוא יעזור לך מבחינת הבנייה עצמה"* (17:53) | **קו-החלוקה:** מוצר ⇐ ישי · מכניקה ⇐ סשן-הייעוץ |
| *"לא עוברים לסשן חדש עד שאתה מאשר את מסמכי המסירה"* (18:2X, דרך סשן-הייעוץ) | **המסמך הזה טעון אישור לפני שאתה מתחיל** |
| *"לא להסתבך ולא להמציא את הגלגל בצע לפי הסטנדרט"* (03:1X) | 🔑 **כשיש סטנדרט — הוא התשובה, ואין מה לשקול.** שלושת תיקוני כרטיס-המדד בוצעו ישירות לפיו |
| *"מסכים לבדוק בגדול 15.6 אינטש לפחות"* (03:0X) | **הרגרסיה חייבת לכלול את המסך האמיתי שלו**, לא רק את רוחב-הסיכון ⇐ 1536 ו-1920 נוספו |
| *"למה אתה צריך אישור ממני — היית מבקש ממנהל מוצר לדחוף?"* (03:3X, דרך סשן-הייעוץ) | 🔴 **דחיפת ענף אישי היא פעולה הנדסית, לא הכרעה מוצרית** — וכלל-ברזל 10 מעולם לא אסר אותה. ר׳ §4 |
| *"תחתום על 2 הקבצים האלא הם הכי חשובים"* (03:4X) | התוכנית והמסירה — **שניהם נקראו במלואם ובוקרו מול הטמפלט לפני הדחיפה** |

### 💡 רעיונות *(לא הכרעות — עברו בדיקה וחזרו עם המלצה)*

| הציטוט | מה נעשה איתו |
|---|---|
| *"הכתב קטן מדי... אני לא יודע מה הגודל הנכון פה באמת תברר באינטרנט אם אתה רוצה"* (13:47) | נבדק מול מקורות חיצוניים ⇒ 14px תווית / 24px ערך. **המקורות בתוכנית §3** |
| *"הגרפים מרגיש לי שחלק מהם ניתן לוותר אני מעדיף איכות על כמות אז פה אני מתייעץ איתך"* (13:47) | נבדקו 23 הגרפים ⇒ **3 נמחקים בלבד**, ולא גזירה רוחבית |

### ❓ שאלות *(שהובילו לממצא)*

| הציטוט | מה זה חשף |
|---|---|
| *"לחיצה על עמודה בגרף יפתח את הפירוט... ממה שאני מכיר בדרך כלל עושים ככה לא?"* | **באג אמיתי:** במ03 עמודות-הגרף מתות לחלוטין (`year_key` מול `drill_key`) |
| *"אולי הבעיה לא רק בדוחות"* | **87% מהגדלים הידניים הם מחוץ למודול 11** — וזה מה שהפך את פזה א' לחוצת-מערכת |

---

## §7 · 🔴 במה **אני** טעיתי — והנוסח המתוקן

*(‏§4ג: הפרק השני שרק קורא יכול לכתוב. תקציר מוחק את אלה לגמרי.)*

| הטעות | הנוסח המתוקן | איך נתפסה |
|---|---|---|
| *"8 מתוך **61** לקוחות"* | ‏61 = כל הלקוחות במערכת. האוכלוסייה המדורגת = **52** | מדידה במסד |
| *"הייצוא צריך לשלוף את מלוא הדאטה בעצמו"* — נאמר לסשן-האקסל | סתר את ת4 **כפי שעמד באותו רגע**. ⚠️ **ת4 עצמו התהפך מאוחר יותר באותו יום** ⇒ הטענה נכונה היום, העיתוי היה שגוי. ר' ת4ב | ביקורת עין-טרייה, ואז סשן-האקסל |
| טיוטת-התוכנית אמרה `default 50` לכל חמשת הפרמטרים | היה מקפיץ את מ19/מ02 מ-8 ל-50 ואת מ07 מ-4 ל-50 — **משנה את המסך**. תוקן לפר-פונקציה | ישי, בקריאת התוכנית |
| שורת-האודיט בתוכנית נשארה *"248 שורות · הסינון-הצולב נשמר"* אחרי שהקובץ השני גדל ל-341 והפך את ההכרעה | ‏**תיקנתי את הנרטיב ולא את הבדיקה** — ישי תפס זאת מילולית. ⇒ **לפני כל ציטוט מקובץ חי: `wc -l` קודם** | ישי |
| ‏`create or replace function` עם חתימה שונה | **אינו מחליף — יוצר עומס-יתר**, ו-`42725` היה מפיל 5 דוחות בייצור | סשן-הייעוץ תפס; **אני אימתתי בשחזור מבודד לפני שקיבלתי** |
| ‏`drop`+`create` בלי שחזור הרשאות | ‏`pg_default_acl` מעניק `anon=EXECUTE` כברירת-מחדל ⇒ היה **פותח 5 דוחות לגישה לא-מזוהה** | **אני**, תוך כדי תיקון הקודם |
| פתחתי קובץ `.sql` בעטיפת `<div dir="rtl">` | מוסכמת **תיעוד**, לא SQL. בקובץ SQL — רק `--` | אני, לפני קימוט |
| **אמרתי שאני מקים worktree ולא הקמתי** | המשכתי בתיקייה המשותפת; סשן-האקסל החליף לי ענף מתחתיי וקומיט שלי נחת אצלו | הקומיט עצמו הדפיס שם-ענף זר |

🔑 **הכלל שנולד מהאחרונה, והוא הכי שימושי לך:** **"אני מקים X" בתוכנית אינו עדות ש-X קיים.**
בדיקה בפועל (`git worktree list`) היא מה שקובע.

---

## §8 · 🪤 מלכודות — **נבנה מסריקה של המנגנונים, לא מזיכרון הסשן**

| # | המלכודת | הראיה (איך נסרקה) | מה לעשות |
|:-:|---|---|---|
| **1** | **הוק `protect-frozen-files.sh` חוסם עריכות-Bash לפי *תוכן*, לא רק לפי נתיב** — ‏`sed` על `docs/schema.sql` נחסם כי הטקסט שנוסף הכיל שם-קובץ-מיגרציה | ‏`ls .claude/hooks/` · נחסמתי פעמיים בפועל | **השתמש בכלי `Edit`**, לא ב-`sed`, בכל מקום שהתוכן מזכיר מיגרציה |
| **2** | **הוק `block-shell-dialect-mixup.sh` חוסם צנרת של שערים** — ‏`npm run gate \| tail` נחסם, כי קוד-היציאה של צינור הוא של `tail` | ‏`ls .claude/hooks/` · נחסמתי בפועל | `npm run gate > /tmp/g.log 2>&1; echo "exit=$?"; tail -30 /tmp/g.log` |
| **3** | 🔴 **‏15 טענות-בדיקה נועלות מחלקות `text-*` — ואף אחת אינה גודל.** ‏**13 צבע** (`slate-700` · `teal-600` · `slate-300` · `amber-700` · `slate-400`) · **1 כיווניות** (`text-left`) · **1 הורשה** (`text-inherit`) | ‏`grep -rn "className.*text-" src/ --include="*.test.*"` ⇒ `Ltr.test.jsx` ×2 · `RatingStars.test.jsx` ×6 · `LogisticsTab.test.jsx` ×2 · `PublicFeedbackPage.test.jsx` ×3 · `ReportTable.test.jsx` · `KpiTile.test.jsx` *(נמדד 17/09 18:4X)* | ✅ **ריפקטור-הגדלים אינו נוגע באף אחת.** ⚠️ **וגם `slate-500`⇒`slate-600` לא — ‏`slate-500` מופיע ב-73 קבצי-מקור וב-*אפס* בדיקות** *(נמדד)*. 🔴 **הסיכון האמיתי הוא שינוי לא-מכוון של אחד מחמשת הצבעים הנעולים** — ובמיוחד `KpiTile.test.jsx` (`text-inherit`), שהוא **בשטח שלך**: הוא נועל את הכלל ש**חץ-ההשוואה אינו צבוע** (📐1). אל תחליף `text-inherit` בגוון |
| **4** | **‏`jscpd` בסף 3% ו-`knip` בשער** — ייצוא חדש בלי צרכן מפיל את `npm run gate` | ‏`cat .jscpd.json` (threshold 3, minTokens 50) · `head knip.jsonc` | סקריפט-שער חדש חייב צרכן, או ערך ב-`ignore` **עם תוכנית-הסרה מתוארכת** |
| **5** | **אין `tailwind.config.js` בריפו — במכוון** | ‏`src/CLAUDE.md §2.3` | הסקאלה נכנסת ל-`@theme inline` ב-`src/index.css`, ולא לקובץ-קונפיג חדש |
| **6** | 🔴 **תיקייה אחת = ענף אחד.** סשן אחר שעושה `checkout` בתיקייה המשותפת מחליף לך את הענף **בלי אזהרה** | קרה בפועל היום; `git worktree list` מראה 6 עצי-עבודה | **עבוד רק ב-`Reg-In-copy-wt`.** לפני קימוט: `git branch --show-current` |
| **7** | **‏`e2e/smoke-anchors.json` נועל מחרוזות-מסך** (8KB) | `wc -c e2e/smoke-anchors.json` | שינוי מחרוזת ⇒ עדכון העוגן **באותו קומיט**, ואז `npm run smoke` |
| **8** | **עבודה מתוזמנת ב-02:00 מזיזה מספרים בלילה** — ‏`n` זז 241⇒242 בין שתי מדידות, ונראה כמו רגרסיה | ‏`docs/micro_guides/module-11.md` §9 D-40 | מספר שזז בין אתמול להיום **אינו בהכרח באג** — בדוק `updated_at` לפני שאתה חוקר |
| **9** | **‏`StatTile` מיובא ב-14 קבצים** — שינוי בו נוגע בכל המודולים | `grep -rl "components/StatTile" src/` ⇒ 14 | הרגרסיה המצולמת אינה אופציונלית |
| **10** | 🔴 **ל-worktree אין `node_modules` — אף שער לא ירוץ שם עד `npm install`** | `ls -d node_modules` ב-`Reg-In-copy-wt` ⇒ **חסר**; `npm run format:check` נפל על `'prettier' is not recognized` *(נמדד 17/09 19:2X)*. ‏`check:docs-structure` דווקא רץ — הוא node טהור בלי תלויות, **ולכן הוא לא יגלה לך את החוסר** | **הפעולה הראשונה שלך, לפני כל עריכה: `npm install` בתוך ה-worktree.** בלעדיו `npm run gate` · `verify` · `test:run` · `build` לא קיימים — ו**"כל פרוסה מסתיימת ירוקה" (הכרעת-ישי, התוכנית §7ג) היא בלתי-אפשרית.** ⚠️ ושבעת הקומיטים שכבר על הענף **לא עברו `prettier`** — הם מסמכי-md בלבד, אך **הרץ `format:check` מיד אחרי ההתקנה** |

| **11** | 🔴 **שרת-תצוגה שרץ אינו בהכרח שלך — והצילום ממנו הוא ראיה שקרית** | ‏`preview_start` החזיר `reused: true` על שרת עם `cwd: C:\Users\ishay\Reg-In` *(ענף `dev`, סשן אחר)*. גם הפעלה בשם אחר נחתה שם, כי **שינוי-התיקייה של הסשן נכנס לתוקף רק בסוף התור.** הדף הגיש `index-hYtgHDsy.css` *(11:44, ‏`--text-xs:.75rem`)* ולא את `index-DuPbO262.css` שלי *(19:22, `.8125rem`)* | 🔴 **שלוש דרכים נוסו ונכשלו — אל תבזבז עליהן שוב:** ① `preview_start` בתור חדש, אחרי שתיקיית-הסשן כבר עברה בפועל ⇒ **עדיין `cwd: C:\Users\ishay\Reg-In`** · ② הוספת הגדרה ל-`launch.json` שלי ⇒ **הכלי לא ראה אותה** *(הוא קורא מהתיקייה הראשית; הקובץ הוחזר) · ③ בנייה עם `--base ./` ו-`navigate` ל-`file://` ⇒ **נפתח כ"תצלום סטטי" מחוץ ללשונית**, ו-`javascript_tool` ממשיך להריץ על הדף הישן *(שתי תיקיות-בנייה זמניות נמחקו)*. ⇒ 🔑 **הכלי נעוץ לשורש שבו הסשן נפתח, ושינוי-תיקייה אינו מזיז אותו. **הפתרון היחיד שנותר: סשן שנפתח מ-`C:\Users\ishay\Reg-In-copy-wt`.**<br>**ובכל מקרה — לפני כל צילום: שאל את הדף איזה CSS הוא טען** — ‏`[...document.querySelectorAll('link[rel=stylesheet]')].map(l=>l.href)` — **והצלב מול `ls dist/assets/`.** 🔑 **מדוד ערך מחושב, אל תסתכל על תמונה:** ‏`getComputedStyle` על אלמנט-בדיקה החזיר `12px` בזמן שהקוד שלי אומר 13 — **וזה מה שחשף את זה.** צילום היה נראה תקין לחלוטין |

---

## §9 · 🔴 מבחן-הקבלה — ענה עליו בהודעה הראשונה, לפני כל פעולה

### 🅐 מכני *(בלי שיפוט)*

0. 🔴 **הצהרת-קריאה — מלא את הטבלה הזו לפני כל פעולה** *(בקשת-ישי 17/09; מדריך-המסירה §3🅐)*:

   | הקובץ *(§1א ו-§1ג)* | נקרא במלואו / גריפ בלבד *(ועל מה חיפשת)* |
   |---|---|
   | 1 התוכנית | |
   | 2 המסמך הזה | |
   | 3–4 קבצי `CLAUDE.md` | |
   | 5 `src/index.css` | |
   | 6 `StatTile.jsx` | |
   | 7 מדריך-המסירה | |
   | 8 `CODE_MAP.md` | |

   🔑 **וההודאה היא הערך, לא הרשימה:** *"לא פתחתי את X"* הוא המידע היחיד כאן שאי-אפשר
   להסיק ממקום אחר. ⚠️ **והמגבלה הכנה: זו הצהרה עצמית ואינה ניתנת לאימות** —
   ולכן היא **משלימה** את 🅓 למטה, לא מחליפה אותן.
1. פתח את שלושת הנתיבים: `~/.claude/plans/snug-mapping-dragon.md` · `C:\Users\ishay\Reg-In-copy-wt\docs\micro_guides\module-11.md` · `src/index.css`. **נתיב מת ⇒ עצור ודווח.**
2. הרץ `git worktree list` ו-`git status --short` ו-`git branch --show-current`. **תואמים ל-§2?**
3. ‏`grep -c "\[TODO\]"` על המסמך הזה. ✏️ **התשובה הנכונה היא 1, לא 0 — והאחד הוא השורה הזו עצמה**
   *(נמדד 17/09 20:2X; הנוסח הקודם אמר "אמור להיות 0" ו**היה נכשל תמיד**, כי המבחן תופס את עצמו)*.
   🔴 **יותר מ-1 ⇒ עצור ודווח.**

### 🅑 שרידות-קומפקט — לכל אחת: *"מהקובץ X"* או *"מהזיכרון"*
1. **ההחלטות:** מה ברירת-המחדל של `p_page_size` בכל אחד מחמשת הדוחות?
2. **נתיבי-הקבצים:** אילו 7 קבצים אסור לך לגעת בהם?
3. **החסמים:** מה אסור לך לעשות עם git, ולמה?

### 🅒 שיפוט
1. **החסמים עוד קיימים?** בדוק ש-`ishay/export-build` לא מוזג ל-`dev` בינתיים.
2. **ההנחה "422 מופעים" עוד מחזיקה?** ⚠️ **שני מספרים, ואל תבלבל ביניהם:**
   ‏`grep -rho "text-\[[0-9.]*px\]" src/ | wc -l` ⇒ **421** *(נמדד 17/09 18:3X)* — אלה הפיקסלים בלבד.
   ‏**422 = 421 + מופע יחיד אחד של `text-[0.8rem]`** *(עוגן: `text-[0.8rem]` ב-`src/components/ui/button.jsx`)*.
   **אם 421 זז — הסשן המקביל נגע בקבצים; בדוק `git log ishay/export-build`.**

### 🅓 ארבע שאלות עובדתיות *(התשובה בדיסק — ענה קר)*

✏️ **נכתבו מחדש 17/09 03:4X.** הנוסח הקודם נכשל במבחן שמדריך-המסירה מחיל *(§4א-2 כלל 11)*:
**שלוש מארבע נענו בגריפ בודד על `db_roadmap.md`** *(נבדק: 55 התאמות למונחים שלהן בקובץ אחד)* ⇒
**היה אפשר לעבור את המבחן בלי לקרוא שורה.** ‏🔑 **כל שאלה כאן מחייבת צירוף שני מקורות.**

1. **השער `check:type-scale` עובר ירוק על 19 מופעים שנותרו. כמה מהם חוב שיש לסגור,
   וכמה אסור לגעת בהם לעולם — ולמה?** *(דורש: קו-הבסיס + §5 גבולות-הקבצים + הערת-הראש של השער)*
2. **הרצת `npm run gate` והוא ירוק. מה בפזה א׳ הוא אינו יכול לבדוק בעקרון,
   ואיזה מסך כבר נשבר בדיוק שם?** *(דורש: §4 צעד 3 + §10 פריט-הדיילות)*
3. **סשן-האקסל מוזג. מה צריך לקרות עכשיו בשלושה מקומות שונים — ומה יקרה
   לשער אם לא תעשה את אחד מהם?** *(דורש: §5 + קו-הבסיס + §8 מלכודה 4)*
4. **מדדת מסך והוא גולש. מה אתה חייב למדוד לפני שאתה מדווח או מתקן,
   ולמה התשובה האחרת מסוכנת יותר מהבאג?** *(דורש: §10 + §7 "במה טעיתי")*


---

## §10 · פריטים פתוחים *(לא שלך — של ישי)*

| הפריט | מצב |
|---|---|
| 🔴 **טבלת-הדיילות גולשת אופקית ב-1024px — וזו הכרעה עיצובית, לא תיקון מכני** | 📏 **נמדד שני הצדדים: 65px לפני השינוי · 84px אחריו.** ⇒ **הבאג קדם לפזה א׳ — היא החמירה אותו ב-19px, לא יצרה אותו.** המסך נושא טבלה רחבה מאוד, והפתרונות האפשריים הם הכרעות-מוצר: גלילה-אופקית מוצהרת במיכל · הסתרת עמודות במסך צר · או צמצום העמודות עצמן. 🚫 **מה שאסור: להקטין את הכתב בחזרה** — זה יבטל את פזה א׳ כדי להסתיר באג שקדם לה. **לא נגעתי** |
| ✅ ~~גודל לשוניות וכפתורים~~ — **נסגר 17/09 22:5X, ולא היה צריך להישלח לישי מלכתחילה** | רשמתי את זה כ"הכרעה עיצובית". **מדידה סגרה אותו בדקה:** ‏`ui/button.jsx` *(עוגן: `inline-flex shrink-0 items-center`)* · `FinancePage.jsx` *(עוגן: `function TabsBar`)* · `Sidebar.jsx` *(עוגן: `rounded-lg px-3 py-2.5 text-sm font-medium`)* · `KpiStrip.jsx` *(עוגן: `const MASKED_NODE`)* — **כולם `text-sm`, כלומר 14px, ואף אחד מהם לא היה מעולם ערך קשיח.** הפער היה **בתוך המסמך**, כי החלתי מיפוי של ערכים-קשיחים על טוקן. **המסמך תוקן ל-14; הקוד לא נוגע.** 🔑 **והכלל שנולד, והוא העיקר: לפני שאתה מסמן פריט כ"הכרעת-ישי" — שאל אם מדידה סוגרת אותו. העמסת שאלה מכנית על המנהל-מוצר היא גם היא כשל.** *(אותר ע"י הסשן המקביל)* |
| ✅ ~~§7.101~~ — **נסגר 17/09/2026: להשאיר** | האצלת-ישי ⇐ הכרעת סשן-הייעוץ *(ולכן הפיכה)*. 🔴 **והנימוק שהיה רשום בפריט נמדד ונמצא שגוי** *(0 אזכורי-תעריף בשלושת מסכי-השיבוץ)*; הנימוק המחייב הוא טבלת-ההרשאות. **נרשם ב-`PROJECT_MASTER_sec7.md` פריט 101, ואומת על-ידי לפני הכתיבה** |
| ✅ ~~שלוש שאלות-ההבנה~~ | **שוחררו על-ידי ישי 17/09** *("זה לא רלוונטי, שישחרר את זה")*. לא חוזרות |
| כיווץ `CLAUDE_CODE_LOG.md` | **2,817 שורות מול יעד ≤150** *(נמדד 17/09 18:3X — גדל היום ב-141 שורות, חלקן שלי)* — סשן ייעודי |
| כיווץ `docs/micro_guides/module-11.md` | **789** שורות *(נמדד 17/09 18:3X)* |
| מי מנקה את `ishay/export-dialog` *(קומיט תיעודי כפול שלי, נחת בטעות)* | **לא מזיק** — תוכן זהה לקומיט שעל הענף הנכון. סשן-האקסל עבר ל-`ishay/export-build` ⇒ הענף הישן נטוש |
| ✅ ~~רשומת-כיול~~ — **נכתבה 17/09 02:5X** | הייתה חוב מוצהר ונסגר. **המסקנה שבה, למי שיתמחר משימה כזו שוב:** ‏**הריפקטור עצמו לקח פחות מ-10% מהזמן** — 408 החלפות בשתי פרוסות ירוקות, אפס בדיקות נשברו. **השאר הלך לתיאום בין סשנים, לשלוש סביבות חסרות ב-worktree, ולאימות שהראיה אמיתית.** ⇒ **אל תתמחר לפי מספר ההחלפות** |

---

## §11 · מה זה יעלה, ומתי לעצור

> ✏️ **23/09/2026:** "בלי סוכנים" למטה נכתב במכסה 81% — התיישן. "איפה עוצרים" — עדיין תקף.

📏 **פזה א' היא לולאה מכנית: 422 החלפות ב-~70 קבצים.** זו בדיוק העבודה שסשן טרי עושה טוב.
🔴 **עצור וכתוב מסירה בנקודה שנשארו בה פריטים מאותו סוג** *("נשארו 38 קבצים, אותה החלפה")* —
**לא בין שלב לשלב.** *(הכרעת-ישי 17/09.)*
🚫 **בלי סוכנים** — המכסה השבועית הייתה ב-81% היום. *(הכרעת-ישי.)*

---

**נכתב לפי `~/.claude/references/session-handoff-guide.md` §3 · §4א · §4ג.**
**כל טענה כאן נושאת את מקורה. טענה בלי מקור — לא נכתבה.**

---

## §12 · 🧩 בלוק-ההדבקה — **מה שישי מעתיק לסשן הבא**

> ⛔ **23/09/2026: הבלוק למטה מת** (הוא מפנה לענף `ishay/type-scale` שמוזג). הפרומט החי: `~/.claude/plans/typography-phase-b-prompt.md`.

*(מדריך-המסירה §4ב. נוסף אחרי שנמדד שהוא חסר — **והוא התוצר היחיד כאן שישי בפועל מחזיק**.)*

```
/module-build

אם משהו כאן סותר את מה שאתה רואה בדיסק — הדיסק מנצח.

קרא ראשון, במלואו:
C:\Users\ishay\Reg-In-copy-wt\docs\specs\module_11_reports\HANDOFF-typography-2026-09-17-01.md
§1 שלו הוא רשימת-הקריאה; §9 הוא מבחן-הקבלה שאתה עונה עליו בהודעה הראשונה,
כולל טבלת הצהרת-הקריאה — לפני כל פעולה.

גריפ רק כדי למצוא איפה, לעולם לא כדי לענות.

איפה לעבוד — אמת, אל תניח:
  cd C:\Users\ishay\Reg-In-copy-wt && git branch --show-current   ⇒ ishay/type-scale
  npm install                                                      ⇒ חובה לפני כל עריכה

המשימה: פזה ב׳ — הניסוחים, הכפילויות, המונים והפקדים המתים. 8 שלבים.
פזה א׳ סגורה ומגובה — אל תיגע בה.

מה לא לשבור: אין PR · אין מיזוג ל-dev/main · אין --force · אין apply_migration.
מה אסור להכריע לבד: כל הכרעה מוצרית — ר׳ §5ב (כלל-הניתוב).
השעון: הכנס 15/10/2026 · הקפאה רוחבית 08/10.
```

🔴 **ומה שהבלוק הזה בכוונה אינו נושא** *(§4ב של המדריך)*: סיכום של מה שקרה · הצדקות ·
ורשימת פריטים פתוחים שאינם הצעד הראשון — **הם מסיטים בדיוק את מי שאמור להתחיל.**
‏🔑 **ואף מספר קשיח אינו בו** — רק פקודות. *(בלילה הזה "4 קומיטים" היה 10 תוך שעה, ו-"28" היה 26.)*

</div>
