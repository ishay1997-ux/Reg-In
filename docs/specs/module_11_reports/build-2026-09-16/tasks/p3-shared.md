# P3-SHARED · Five gaps in the shared report components, fixed ONCE for all sixteen surfaces (found by the customers tab builder and the reviewers, 16/09 09:3X)

**Repo:** `C:\Users\ishay\Reg-In`, branch `ishay/module-11-build`. **Files you own (and ONLY these):** `src/modules/11_reports/components/KpiTile.jsx` · `components/ReportSurface.jsx` (+ `ReportSurface.test.jsx`) · `components/ChartCard.jsx` (+ `.test.jsx`) · `components/ReportTable.jsx` · `src/modules/11_reports/api.js` (+ tests) · a new `components/KpiTile.test.jsx` if none exists. **Never** touch `tabs/**`, `src/lib/**`, `ReportsPage.jsx`, `reportsCatalog.js`. **Never commit.** Report in English; UI strings in Hebrew. Four tab builders' tests under `tabs/*Tab.test.jsx` exist (some still being written) — **every change is additive**: run `npx vitest run src/modules/11_reports` before and after; a tab test that was green must stay green (a tab test that was already red before your change is not yours — list it).

## Read first
`docs/micro_guides/module-11.md` §2ב C6 · C8 (+ amendments: `tiles[].sub`, `columns[].sorted`, `meta.extra_tables`, `drill_key {kind,…}`) · §9 D-14 (drill set מ3 · מ9) · D-22 (the slots) · `src/modules/11_reports/CLAUDE.md` · the four components and `api.js` in full · `design-contract.md` §② (chart principles; the cross-filter and the `aria-live` announcement), §⑥ (the shell skeleton and its order) · `processes-approved.md` §📐 (772–810: 📐1 compare · 📐3 window · 📐6/📐9 cross-filter + live region · 📐7/📐8 table sort/pager · 📐19 whole row clickable) · the live payloads in `<scratchpad>/results/payloads/*.json` (what charts/rows actually look like).

## The five gaps
1. **`tiles[].sub` is never rendered.** `KpiTile.jsx` passes `compare` and `window` to `StatTile.sub` but not `tile.sub` (grep `tile.sub` = 0). All 16 RPCs fill it (e.g. m20 `⁦144⁩ מתוך ⁦163⁩ משובים`, m07 `⁦83⁩ משמרות בתשלום`). Render it as a line under the value, above the compare line (`data-testid="kpi-sub"`), masked ⇒ hidden. Test it.
2. **Rows are clickable only on drill surfaces.** `ReportSurface` passes `onDrill` to `ReportTable` only when `surface.drill`; ruling 19 says the WHOLE row is clickable wherever a door exists — and non-drill surfaces (מ19–מ22, מ14–מ17, מ7, מ8, מ12) return `drill_key {kind:'project'|'customer'|'hostess', id}` as a door. Fix: `onDrill` is passed when `surface.drill` OR any row carries `drill_key`; `DrillCrumbs` stays drill-only. The customers builder worked around it by passing a modified `surface` with `drill: true` — after your fix, that workaround becomes unnecessary (do NOT edit their tab; note it in your report so the orchestrator tells them).
3. **Cross-filter chart → table is not wired.** `ChartCard` already accepts `onSelect` (bars, lines, scatter, the accessible data table) but `ReportSurface` never passes it, so no surface has the 📐6/📐9 behaviour and the `aria-live` region (`ReportTable`'s `announcement`) is always empty. Build it once: selection state in `ReportSurface` keyed by `filterKey` — `chart.filter_key` if the RPC provides it (additive C8 field — document it in your report), else auto-detect: `chart.xKey` is also a key of `columns[]` or of the rows; a click on a datum filters `rows` to `row[filterKey] === datum[chart.xKey]` (compare as strings), the pager counts the filtered rows (📐8), the announcement reads *"מסונן ל<label of the datum>; N שורות"*, a small chip/button *"נקה סינון"* (`data-testid="report-clear-crossfilter"`) clears it, a second click on the same datum clears it, and a filter change (requestKey) resets it. Export exports what is shown — read `design-contract §②.8` and card ת4 (`cards-*.md`, the export section) and apply what they say about a filtered view; say what you applied. Keyboard: the accessible data table's row buttons already call `onSelect` — verify. Measure against the live payloads which surfaces get an auto-detected filter key and list them; where a chart's `xKey` matches no column, nothing happens (no dead cursor: pass `onSelect` only when a filter key exists).
4. **`CHART_CAP = 2` in `api.js` contradicts the approved customers mockup (מ20 draws four charts).** Orchestrator ruling (16/09 09:3X, recorded as §9 D-28 — a conflict question for Ishay's morning table, not a silent change): Ishay's ✅4 (*"מבנה · סדר · אילו אריחים/גרפים/טבלאות ⇐ המוקאפ"*) is newer than spec ⏳10's *"2 לדף"* (measured on the four finance pages only). ⇒ raise `CHART_CAP` to 4 with a why-comment quoting both sources and the date; update its test. The customers tab already derives its "excess charts" count from `normalizeCharts`, so raising the cap must not duplicate a chart there — run their test file to prove it.
5. **Order of the "אז מה" line.** `ReportSurface` renders tiles, then so_what, then charts. The customers builder measured that its approved mockup places "אז מה" ABOVE the tiles. Measure all four approved mockups (`docs/mockups/management-report-screen/approved/02…05_*.html`: find the so-what element and the tiles container per page and compare their document order). If all four agree on one order, make `ReportSurface` follow it (C2: order ⇐ mockup) and record the measurement in a comment; if they disagree, change nothing and report the table (page → order).

## Gates
`npx vitest run src/modules/11_reports > <scratchpad>/results/vt-shared.log 2>&1; echo exit=$?` (before and after) · `npx eslint src/modules/11_reports/components src/modules/11_reports/api.js` (complexity ≤ 20) · `npx prettier --check` same · `npm run dup` (no new clones) · `npm run deadcode` (no new unused exports in your files) · `npm run check:bidi` on your files · CR = 0 via `perl -ne '$n+=tr/\r//; END{print "CR=$n\n"}' <file>`. Then one browser proof: `npx vite --port 5190 --strictPort`, sign in as CEO (identities from `.env.local` `E2E_*`, never printed; `login()` from `e2e/permissions.spec.js`), open מ9 (`/reports?tab=finance&report=aging` — check the URL shape in `ReportsPage.jsx`), click a bar, screenshot the filtered table + the announcement text (`<scratchpad>/results/shared/crossfilter.png`), click a row on מ21 (a door) and screenshot where it lands.

## Output (final message = data, also `<scratchpad>/results/p3-shared.json`)
`{ "changes": [ { "file", "what", "test" } ], "crossfilter_surfaces": [ { "rpc", "filter_key", "how": "explicit|auto" } ], "so_what_order": { "measured": {...}, "changed": bool }, "chart_cap": 4, "tab_tests": { "before": "...", "after": "...", "red_before": [...] }, "workarounds_now_unnecessary": [...], "screenshots": [...], "not_verified": [...], "blind_spot": "..." }`
