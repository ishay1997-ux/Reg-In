# P5-FIX-DOC-2 · second doc round — the write-back of rounds 1–2 and the baseline verdict (written 17/09/2026 01:2X by the orchestrator)

**Repo:** `C:\Users\ishay\Reg-In`, branch `ishay/module-11-build`. Read `p5-fix-doc.md` (round 1) and round 1's report `…\scratchpad\results\p5-fix-doc.json` first — its `not_done[]` is your first input. You own: `docs/guides/modules/module_11_reports.md` (the step guide — two lines) · `docs/specs/module_11_reports/stage2-cards/cards-finance.md` (one stale sentence) · `docs/micro_guides/module-11.md §9` (ONE new row D-37; do not edit D-36) · `docs/PROJECT_MASTER.md §6` (the pre-existing E2E failures, now measured) · `docs/specs/module_11_reports/spec.md §1.4` ONLY if the J2 agent has not already changed a label you must mirror (coordinate by reading its report `…\scratchpad\results\p5-fix-rpc-j2.json` if it exists; if J2 is still running, leave §1.4 alone and say so). **Nothing in `src/`, no migrations, not `docs/schema.sql`, not `docs/db_roadmap.md`, not `STATUS.md` / `CLAUDE_CODE_LOG.md`. Never commit.**

## The items
1. **Step guide, two copies of one number** (round-1 `not_done` #1): `docs/guides/modules/module_11_reports.md:208` says *"74 תוויות-אריח ו-19 כותרות-דף"* ⇒ 79; and `:75` says *"עד כדי 79 תוויות-אריח"* — reword both so the number lives ONCE, in `spec.md §1.4` (whose declared line now cites `npm run check:declared-counts`), and the guide points at it instead of repeating it. Verify with `sed -n '75p;208p'` before and after. `npm run check:declared-counts` and `check:docs-structure` must stay 0.
2. **`cards-finance.md:297`** (the RPC fixer's blind spot): the card still says a below-floor row *"יורדת לסוף הטבלה"* and that the table sorts by % — both overturned (📑ב#5: below-floor projects are out of the ranking; D-34 ⑧: sorted by ₪). Correct the sentence in place with a dated ✏️ note; do not delete history.
3. **Micro-guide §9 — one new row `D-37`, dated 17/09/2026, "fix rounds 1–2 after the evidence step — what changed, what was assumed, what stays a question":** (a) round-1 shell fixes (the 11 items of `…\results\p5-fix-shell.json`, one line each with the file) and round-1 RPC J1 (8 functions: m08 · m09 · m14 · m15 · m16 · m20 · m21 · m22 — what each changed, from `p5-fix-rpc.json`), (b) the J2 items when its report exists (`format:'id'` on every identifier column · the comparison wording harmonised to 📐1/15(ב) *"אשתקד …"* · month-axis labels · *"חלון קבוע"* · מ22's window), each with its source or its `הנחתי` tag, (c) the round-2 shell items from `p5-fix-shell-2.json` when it exists (heuristic removed · the two hint rewrites · the run-bar wording · the customer-filter empty sentence `הנחתי` · loading-state rule), (d) **questions for Ishay's morning table** with one line each: מ8's tile *"פרויקטים שחרגו מהתקציב"* still counts 30 while the ranking now lists 8 (22 below the materiality floor — the tile counts the population, the table ranks; is that the intended reading of card 8.8?) · the export caption bidi finding [1] investigated by the shell fixer and NOT reproducible at 1280×800 (its own fix measured as a regression and was reverted — recorded, not fixed) · `below_materiality` is now `false` on every מ8 row (kept as a contract key; no reader marks it) · מ14 and מ15 print the same thresholds from two variables that happen to be equal today, (e) **the baseline verdict**: the five E2E failures in other modules' specs fail IDENTICALLY on `origin/dev @ 9b5ea534` (two runs each, byte-identical messages — `…\results\baseline` in the workflow output; the agent's blind spot: one long-lived preview across both runs) ⇒ **pre-existing, not introduced by module 11**; overwrite D-36 section (ד)'s "טעון בדיקה" wording? NO — D-36 stays as written; D-37 carries the verdict and points back.
4. **`PROJECT_MASTER.md §6`** — one dated line per pre-existing failure as a cross-module debt owned by the module the spec belongs to, in the register's own shape (`🚧 מN ← מ11 · what · source: …`): `customer-page.spec.js:130` (מ2: projects-tab counter 3 vs 6 rendered rows) · `dashboard.spec.js:82` (מ7: first `<h2>` unchanged after the month step) · `finance.spec.js:141` (מ8: `closing-payment-block` not found) · `prices.spec.js:218` (מ1/params: alert copy *"ערך חוקי: מספר שלם בין 1 ל-150"* vs the test's *"מספר שלם חיובי"* — the test is stale, the screen is right) · `projects.spec.js:235` (מ6: no project without products reachable). Read the register's conventions before writing (`docs/CLAUDE.md` iron rule 13; the Stop hook's enforcement 0ג pairs every `🚧 מN` token in a micro-guide with a live §6 line — if you write a `🚧 מN` token in D-37, its §6 line must exist).

## Gates (exit codes from logs)
`npm run check:docs-structure` · `npm run check:declared-counts` · `npm run check:iron-rules` · `npm run check:context` — all 0 · CR=0 on every file you touched.

## Output — `…\scratchpad\results\p5-fix-doc-2.json`, same shape as round 1, written after EVERY correction.

כל עובדה כאן ניתנת לערעור — אם מדדת אחרת, תקן אותי עם המדידה.
