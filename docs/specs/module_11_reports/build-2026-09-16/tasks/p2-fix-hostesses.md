# Task P2-FIX · Hostesses tab RPCs — fix-forward migration F2 for the verifier's findings, apply it, re-verify each finding specifically

**Repo:** `C:\Users\ishay\Reg-In`, branch `ishay/module-11-build`. You write ONE new migration file + edits to `src/lib/reportsHostesses.js` / `.test.js` only. **You apply the migration through MCP** (waiver, `module-11.md §9 D-9`). Never commit. Report in English.

## Read first
1. `docs/micro_guides/module-11.md` §2ב (C5 · C7 · **C8 with all amendments**: `tiles[].sub`, `drill_key {kind,…}`, `meta.extra_tables`, `label_source`, **the window rule** (calendar closed · rolling half-open), `chart.series[].kind/axis`).
2. `<scratchpad>/results/p2v-hostesses.json` — 11 findings + `tests_pass_but_would_not_catch`. **Each finding gets a specific re-verification in your report.**
3. The applied file `supabase/migrations/20260916052359_module11_f_rpcs_hostesses.sql` (do NOT edit it — it is applied for m14/m15/m16, append-only) and the executive migration `20260916052600_module11_d_rpcs_executive.sql` (money formatting with `to_char … 'FM999,999,999' || ' ₪'` inside `chr(8294)…chr(8297)`; `tiles[].sub`; `drill_key` with `kind`).
4. `src/modules/11_reports/components/ChartCard.jsx` — the `ScatterBody` contract (`series[0]` = X name, `series[1]` = the Y column) and the composed-chart contract (`series[].kind`, `.axis`).
5. `supabase/migrations/CLAUDE.md` (fix-forward; pull live bodies with `pg_get_functiondef` before `create or replace`).

## ⚠️ Transport limit measured by the verifier
A single `apply_migration` call of ~90 KB was truncated and rejected. Keep every `apply_migration` call under ~60 KB: the new migration FILE is one file, but apply it in per-function chunks (`module11_f2_hostesses_m17`, `_m14`, …) and record in the file header that the registry carries N rows for one file (the name-vs-version trap of `supabase/migrations/CLAUDE.md §3`). Also record that the 16/09 05:2X apply of F was itself split into three rows (`module11_f_rpcs_hostesses_m14/_m15/_m16`) and that the m14 body was comment-trimmed in transport — the file is the source of truth; **so in F2 recreate m14 in full from the file's body (with the fixes) so `pg_get_functiondef` matches the repo again.**

## Orchestrator rulings per finding (technical, reversible — each a Hebrew `-- why:` line)
- **F1 (blocker) — `report_m17_fairness` never compiled.** Close the `coalesce(` on the `speed_rows` CTE (`… h.full_name), '[]'::jsonb) j`), create the function, and prove it with a live call. Also uniform `drill_key {kind:'hostess', id}`, `tiles[].sub` where the mockup has sub-rows, money/percent formatting in sentences, `drill: null` always (מ17 is not a drill report — §9 D-14).
- **F2 (blocker) — מ14 red-queue tile window.** The queue tile (and its `detail`, `so_what`, `definitions` C) uses the **frozen rolling 12 months anchored to Israel today**, exactly as `report_m15_reliability` does (📑ב#13), regardless of `p_from`/`p_to`; the label stays *"חלון קפוא · 12 חודשים"*; `meta.notes` says the tile ignores the period filter. m14's other tiles keep the caller window as the card defines them. The `definitions` line prints the same C as m15's `meta.company_average`.
- **F3 (blocker) — מ16 scatter series.** `series` = `[{ key:'hourly_rate', label:'תעריף שעתי' }, { key:'rating', label:'דירוג' }]` with `xKey:'hourly_rate'` and `domain [1,5]` on Y. (The executive tab's מ3 has the mirror-image mistake — not yours; it is being handled.)
- **F4 (major) — מ15 drift.** No code change: the function's today-anchored frozen window is the ruled behaviour; the card's 10/09 digits drift by 📐22. Your report states the live values and that they are the definition applied to today's data.
- **F5 (major) — ruling 37 by-rating no-show rates** ⇒ return `meta.no_show_by_rating: { "months12": { "3": 6.5, "4": 2.8, "5": 0.5 }, "ever": { "3": 14.6, … } }` **and** a small table in `meta.extra_tables` (`title: 'אי-הגעה לפי דירוג'`, columns דירוג · 12 חודשים · אי-פעם) so the tab can render it without deriving.
- **F6 (major) — מ16 rows** ⇒ return **all 50 active hostesses** with `rating` (null for the 17 unrated), sorted per 📐7, plus `meta.default_filter: 'no_rating'` and `meta.no_rating_count: 17`; the tab applies the `#onlyNoRating` chip client-side and the pager counts what is shown (📐8).
- **F7 — `WEEKDAY_NAMES_HE`** ⇒ not yours (the hostesses tab builder adds `export` in `src/lib/dates.js`); keep `label_source: 'WEEKDAY_NAMES_HE'`.
- **Minor — population sentence exclusions (the verifier's blind spot):** *"זימונים שלא נענו"* counts only `pending` invites whose `event_date <= today` (an invite for a past event that was never answered); pending invites for future events are not in the attendance population and are not "excluded". Same rule for *"שיבוצים שנוכחותם טרם סומנה"* (past events only). Say both in a comment.
- **Minor — `meta` shape** ⇒ every function returns `meta.drill_echo` (null when unused) and `meta.customer_filter_ignored` (true for all four).
- **Record fix, no code:** `0.4329` = the register's 06/09 measurement (window `(06/09/2024, 06/09/2025]`, 1,455 shifts) — the verifier explained it; mention it in the header so the closing audit does not reopen it.
- **Tests (`reportsHostesses.test.js`)** ⇒ add: the AMBER boundary (`score == amber` ⇒ not amber), `hostessReliabilityScore` delegates to `responsivenessScore` (spy/mock, iron rule 14), `reliabilityParamOrderNote` attribution (the sentence names 0.87 as אדום and 0.95 as ענבר — assert order), `topDecileShare` on n=106 ⇒ 26.0 %, `medianOf` with `null`/`NaN` inputs, Lorenz first vertex on a 106-point fixture built from the live shift counts (`select` them once, paste as fixture with the date).

## After applying F2
Signed-in Node script (CEO/RECRUIT allowed; FINANCE/STAFF ⇒ 42501; delete it after; never print credentials): m17 returns the C8 shape with Gini 0.4559 · 55.3 % / 16.2 % / 26.0 % · 48/13.5/17.6 · 9.9/20.7 · 92.6 % · Lorenz 107 vertices · adoption tile `value:null` + absence sentence · `drill:null` · m14 queue tile = 6 with `p_to = '2026-08-16'` and with `p_to = null` (frozen) · m16 rows = 50 with 17 null ratings and a two-element `series` · m15 `meta.no_show_by_rating` present with the six numbers · `md5(prosrc)` of the four live bodies equals the F2 file's bodies · `select count(*) from pg_proc where proname like 'report_m1%'` ⇒ 8 (m12·m14·m15·m16·m17·m19… count what exists and report). `npx vitest run src/lib/reportsHostesses.test.js src/lib/reportsExecutive.test.js src/lib/reportsCustomers.test.js` green (executive/customers import `medianOf` from your file) · eslint · prettier · CR = 0 (perl).

## Output (final message = data, also written to `<scratchpad>/results/p2fix-hostesses.json`)
`{ "migration": "...", "applied_as": ["<registry names>"], "per_finding": [ { "id", "fix", "reverified_by", "result" } ], "m17_live": {...}, "tests", "not_verified", "blind_spot", "assumed" }`
