# P5 · ADDENDUM 2 to `p5-close.md` + `p5-close-addendum.md` (written 17/09/2026 by the resuming orchestrator, after the verification steps ②–④ and two fix rounds) — read those two first, then THIS; where they disagree, THIS wins

## What happened since addendum 1 (verify each line yourself — `git log --oneline 300e80dc..HEAD` is the spine)
- 16/09 20:4X `300e80dc` — the handoff stop. 17/09 ~01:3X `ae6318e3` — verification steps ② evidence · ③ copy · ④ E2E re-run, plus fix round 1. A later commit carries fix round 2 (J2 + shell-2 + doc-2) — read its message.
- **Evidence (step ②):** 16 surfaces × identities CEO·FINANCE·RECRUIT·STAFF × onboarding modes 0·2 = 128 PNGs + 12 extras (drill מ3/מ9 levels 1–2, export caption, error envelope + recovery, empty-after-filter), taken by four per-tab Opus agents on ONE shared Playwright runner (`build-2026-09-16/scripts/evidence-common.mjs` + `evidence-run.mjs`) against a frozen `npm run preview` of `300e80dc` on port 5189; mode 2 forced by the `notification_preferences` GET intercept, never a DB write. Reports: `build-2026-09-16/results/p3-evidence.json` (merged; `anomalies[]` 29 with an `orchestrator_review`), the four `p3-evidence-<tab>.json`, the four `evidence-run-<tab>.json` (raw measures per identity/mode), `anomalies-summary.md`. **The PNGs and the contact sheet live OUTSIDE the repo** (34 MB): `C:\Users\ishay\Reg-In-evidence\m11-2026-09-16\evidence\index.html`. Two harness lessons are recorded in §9 D-36: a full-page capture froze מ6's charts at animation frame 0 (a fixed-viewport probe found 717 points + 8 bars — `results/probe-staffing.json`), and an identity with no `notification_preferences` row could not be forced to mode 2 until the runner synthesised one row.
- **Copy (step ③):** two fresh-eyes evaluators (mode 0 · mode 2) over `innerText` dumps of all 16 surfaces + drills + export + error + masked (`Reg-In-evidence/…/copy-0`, `copy-2`) — `results/p3-copy-0-1.json`, `p3-copy-2-1.json`, `copy-summary.md`. Their locked-label findings are Ishay's morning table (spec §1.4 🔵), not changes.
- **E2E (step ④):** `results/p3-e2e-rerun.json` + `e2e-full.log.txt`: 185 passed · 6 failed · 7 skipped on `300e80dc`; smoke green; m4 32 green; two durable cases added (`e2e/reports.spec.js` "סדר-תווים בתא", `e2e/accessibility.spec.js` chart-card wait). Of the 6 failures ONE was module 11 (`reports.spec.js:427`, the מ9 root row drilled instead of opening the project — fixed in round 1, DRILL_INTENT decided per row key) and FIVE are other modules' specs — **measured on `origin/dev @ 9b5ea534` by a worktree agent: all five fail identically there ⇒ pre-existing** (`results/p5-baseline-dev.json`); they are registered in `PROJECT_MASTER §6` by the doc round 2, not fixed by this module.
- **Fix round 1** (`results/p5-fix-shell.json` · `p5-fix-rpc.json` · `p5-fix-doc.json` · `fix-round-summary.md`) and **fix round 2** (`p5-fix-rpc-j2.json` · `p5-fix-shell-2.json` · `p5-fix-doc-2.json`): migrations `20260917005500_module11_j1_rpc_round4.sql` (8 functions) and the J2 file (read its header for the function list) — both applied through MCP per function, md5 file == live claimed by the agents: **the md5 sweep is yours, over ALL 17 functions, against the LATEST file per function (`docs/schema.sql §24`).**

## Pre-seeded findings for `close-findings-module-11.md` (Ishay's ruling 16/09 23:4X: *record, do not fix now*)
1. **`approve_feedback_ai_run` live body ≠ file A in comments only** — live `md5(prosrc)` `e03df921…` (1,239 chars) vs file `20260916043300_module11_a_feedback_ai_tables.sql` `69221f3e…` (1,369 chars): the two Hebrew `--` comment lines inside the body were stripped on apply; logic byte-identical (measured 16/09 23:2X by the orchestrator; re-measure). The 16 report functions were 16/16 md5-equal at that time; J1/J2 changed some since — re-measure all 17.
2. The **questions for Ishay** collected in `docs/micro_guides/module-11.md §9 D-36 and D-37` (morning table) — list them in the report as questions, not as defects.
3. The E2E agent's own blind spot: the new bidi test ends in `test.skip` when no row carries both a bucket and an owner — a skip prints `-` and the suite stays green; the day the aging population empties (01:00 quote-expiry cron) it measures nothing. Judge whether `expect(rowIndex).toBeGreaterThan(-1)` should replace the skip.
4. The evidence agents' `not_verified` lists (in `p3-evidence.json`): keyboard/screen-reader paths were never driven; the Excel file itself was never opened; מ22's "no approved run" branch is unreachable while runs 5 and 6 are approved; מ3's drill level 2 beyond the first month.

## What the audit must do beyond the template (unchanged from addendum 1, plus)
- The whole-DB `rls_enabled_no_policy` triage; the 39 rulings map; the §🔢 acceptance numbers on the SCREEN (aging is now 8·12·8·4·3 as of 16/09 — the 10/09 baseline 10·13·8·1·3 moved with the calendar; §3.1 says the boundary is due-date based — verify the five close on the same 35 invoices and 236,382 ₪); the onboarding-key sweep in both directions; the four conference stories with the screenshot that proves each (from `Reg-In-evidence/m11-2026-09-16/evidence/`).
- **Run the gate yourself on the FINAL tree** (`npm run gate > <log> 2>&1; echo exit=$?`, log-read) — the orchestrator ran it too; two readings beat one.
- Verdict rules: `[NO]` with FIX-READY blockers ⇒ stop; the orchestrator dispatches the fix and re-runs you. `[YES]` ⇒ the orchestrator merges (D-9) — you never merge, push or open a PR.
- Write `<scratchpad>/results/p5-close.json` incrementally, and `docs/micro_guides/close-findings-module-11.md` incrementally — the D-24 lesson.
