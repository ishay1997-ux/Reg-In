# P2-E3 finance · One ruled change to `report_m07_finance_overview`: hostess-pay tile rounds the SUM, not the rows (baseline 19,253 ₪) — plus `meta.row_total` / `columns[].sorted` on m09

**Repo:** `C:\Users\ishay\Reg-In`, branch `ishay/module-11-build`. **Never commit.** Report in English.
**Your write scope:** ONE new migration `supabase/migrations/20260916<HHMMSS>_module11_e3_rpcs_finance_rounding.sql` (append-only; never edit the applied E/E2 files) applied through MCP `apply_migration` (waiver `module-11.md §9 D-9`) · `src/lib/reportsFinance.js` + `.test.js` only if a JS twin of the hostess-pay computation exists there · a temporary signed-in Node script in `<scratchpad>` (deleted after; never print credentials).

## The ruling (orchestrator, 16/09 08:1X — technical, reversible; write it as the migration's `-- why:` header)
The finance fixer (E2, F10a) implemented `hostess_pay_month = sum(round(actual_hours * hourly_rate_snapshot))` per the orchestrator's earlier wording and declared the divergence honestly: **19,257 ₪ for August 2026 vs 19,253 ₪ in the approved mockup AND in `signoff-baseline-2026-09-10.md`** (= `round(19,252.5)`). Three sources agree on the order and the ruling flips to them: (1) the baseline is the acceptance criterion (*"16 המשטחים מציגים את מספרי signoff-baseline"*, plan §5); (2) module 8's salary SSOT (`src/lib/salaryReport.js:32` — `round(hours * rate + bonus + travel, 2)` **per row, agorot kept**) rounds to agorot per row, so the whole-shekel figure that matches what is actually paid is `round(sum(...))`, not `sum(round(...))`; (3) the card says only "₪ without agorot". ⇒ **`round(sum(actual_hours * hourly_rate_snapshot))`** for the tile value, the previous-month compare and the rolling-30 measurement — all three places.

## Steps
1. Read `20260916063400_module11_e2_rpcs_finance_fixes.sql` header + the m07 body (find every `sum(round(` for hostess pay — expect three) and `<scratchpad>/results/p2fix-finance.json` F10.
2. Pull the LIVE body: `select pg_get_functiondef('public.report_m07_finance_overview'::regproc)` — build E3 from it programmatically (Python; replace only those expressions; keep everything else byte-identical; `newline='\n'`), keeping the 5-parameter signature (`p_asof date default null`) — do NOT drop/recreate (no signature change) — `create or replace` is enough; keep `comment on function`, `revoke`, `grant` exactly as E2 wrote them (copy from E2).
3. Also on **m09** (`report_m09_aging`): if the root level returns > 50 rows uncapped with no `meta.row_total`, add `meta.row_total` = the total row count and mark the 📐7 sort column with `columns[].sorted: 'descending'` (both additive; `ReportSurface` already renders them). If m09 already has them — say so, change nothing.
4. Apply (chunks < 60 KB; registry names `module11_e3_finance_m07`, `module11_e3_finance_m09`; list them in the header — multiple rows per file are accepted, §9 D-20).
5. Verify: `md5(prosrc)` == file body for m07 (and m09 if touched) — `<scratchpad>/fn_md5.py` (adapt the glob to `e3`) · signed-in call as FINANCE with `p_from='2026-08-01', p_to='2026-08-31'` (and the default window) ⇒ `hostess_pay_month.value` = **19253** · `compare` still carries `count` = shifts (83) and the previous-month value recomputed by the same order · RECRUIT ⇒ `42501` · save payloads to `<scratchpad>/results/payloads/report_m07_e3_<case>.json`.
6. `src/lib/reportsFinance.js`: if a hostess-pay helper mirrors the SQL, align it and its test; otherwise state "no JS twin". `npx vitest run src/lib/reportsFinance.test.js` green · eslint · prettier · CR = 0 via `perl -ne '$n+=tr/\r//; END{print "CR=$n\n"}' <file>`.

## Output (final message = data, also `<scratchpad>/results/p2-e3-finance.json`)
`{ "e3": { "file", "applied_as": [...], "md5_match" }, "m07_august": { "before": 19257, "after": <value>, "compare": {...} }, "m09": "unchanged"|{...}, "js_twin": "...", "tests", "not_verified", "blind_spot", "assumed" }`
