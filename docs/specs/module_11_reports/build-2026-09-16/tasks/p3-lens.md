# P3-V · Lens verifier for ONE tab of the reports UI — READ-ONLY. You refute, you do not fix.

**Repo:** `C:\Users\ishay\Reg-In`, branch `ishay/module-11-build`. You edit no repo file. Temporary scripts only in `<scratchpad>` (deleted after; credentials from `.env.local` `E2E_*` are never printed). Report in English. Your dispatch message names your **tab** (management · finance · hostesses · customers) and your **lens** (A · B · C).

## Shared reading (all lenses)
`docs/micro_guides/module-11.md` §2ב (C1–C8 + amendments) · §3.3(ג) · §9 D-9…D-24 (the recorded rulings — a finding that contradicts one is a **conflict question**, reported as two sources side by side, never "fix this") · `<scratchpad>/tasks/p3-common.md` + `p3-common-addendum.md` + `p3-tab-<tab>.md` (what the builder was told) · `<scratchpad>/results/p3-<tab>.json` (the builder's claims — you re-measure) · the tab's files (`src/modules/11_reports/tabs/<Tab>Tab.jsx`, `tabs/<tab>/*`, `<Tab>Tab.test.jsx`, `src/lib/onboardingCopy.m11.<tab>.js`) · `src/modules/11_reports/components/ReportSurface.jsx` (what the shell renders vs what the tab adds).

## Lens A — spec fidelity (labels · numbers · population · rulings · structure)
1. **Labels byte-for-byte:** every tile/column/title on screen vs `spec.md §1.4` (your tab's row) — labels come from the RPC payload (C8), so fetch the live payloads (signed-in script) and diff: payload label ≠ §1.4 ⇒ finding against the RPC (not the tab); tab renders something other than the payload label ⇒ finding against the tab. D-19: מ3's 12 drill-level labels are not in §1.4 — take them from the card.
2. **Numbers vs `signoff-baseline-2026-09-10.md`:** with the baseline window applied in the UI (period filter), each baseline number appears on screen formatted per `reportsFormat` (`1,250 ₪`, `12.5 %`); note the ones that legitimately drift (📐22 today-anchored windows: aging, m14 frozen tile, m15 — the guide names them).
3. **Population line (📐2), "אז מה" (📐23), definitions (📐16), period in subtitle (📐17), tiles' compare half (📐1) and window (📐3), ≤ 2 charts, table sorted by the displayed metric (📐7), row clickable where a door/drill exists (ruling 19), doors on every overview tile (ruling 33) — per surface.**
4. **Rulings the tab file lists (§3.3(ג)):** where exactly on screen is each visible? Missing ⇒ finding.
5. **Structure and order ⇐ the approved mockup (C2, Ishay ✅4):** for each surface, list the mockup's element order (tiles → chart(s) → table → footers, and any extra blocks) and the built order; a difference is a finding unless the builder's report lists it under `deviations_from_mockup` with a rule.
6. **Onboarding keys:** every `<Hint id>` in the tab ⇔ an entry in the copy file ⇔ a row in the card's §⑩ table (three-way, both directions); text byte-equal to §⑩ after `<span class="ltr">` ⇒ LRI…PDI conversion; **no data count** in any hint (C3 / §5ג); `guided` only.
Method: the dev server + a signed-in Playwright script (see Lens B §0) for what is on screen; `read_page`-style text dumps are enough for this lens.

## Lens B — RTL · accessibility · states · visual
0. **Server:** `npx vite --port <your port> --strictPort` (ports: management 5181 · finance 5182 · hostesses 5183 · customers 5184 — never 5173, other agents share the machine); Playwright (`@playwright/test` is installed; write a script, not a spec file) logs in with `E2E_CEO`/`E2E_FINANCE`/`E2E_RECRUIT`/`E2E_STAFF` from `.env.local` (copy the `login()` pattern from `e2e/permissions.spec.js`); viewport 1280×800; screenshots to `<scratchpad>/results/lensB-<tab>/`.
1. Per surface, both onboarding modes — the mode is PER USER, stored in `notification_preferences.onboarding_mode` (0 · 2), changed in 'ההגדרות שלי' (module 9; `src/modules/09_settings/api.js` has the setter — call it from your signed-in script for the identity you use, read the original value first and RESTORE it at the end; `AuthContext` loads it once per login, so re-login after changing): `document.documentElement.scrollWidth − clientWidth === 0`; zero console errors; `₪` and `%` and minus sit right of the digits with the digits LTR (zoom screenshots of one money tile, one percent, one negative compare); chart inside `dir="ltr"` with Hebrew title/tooltip outside; tooltip `dir`; table numeric columns aligned end; `aria-sort` only on the sorted column; the `aria-live` region exists before any click; clickable rows: `Tab` reaches them and `Enter` opens the drill (מ3/מ9) or the door.
2. **Five envelope states (📐10):** loading (throttle the RPC via `page.route` delay) · error (`page.route` abort on the RPC ⇒ *"נסי שוב"* ⇒ retry re-calls: count requests) · no permission (a blocked identity per the matrix in `p3-common.md`: FINANCE on דיילות · RECRUIT on כספים/לקוחות · STAFF on all) · empty-after-filter (a customer/period with no rows ⇒ the card's §⑧ text, and the clear-filters door) · blank. Screenshots of each.
3. Masked tabs (`role="tab"`, `aria-selected`, *"לא זמין בתפקידך"*) for STAFF.
4. **The 5-pass of `src/CLAUDE.md §4`** as a table per surface (direction · inventory · consistency · copy · empty input).
5. **Mode 0 deletion test:** with onboarding mode 0, every base element (population, so-what, definitions, tiles, charts, table, export) still renders.

## Lens C — tests and code
1. Read `<Tab>Tab.test.jsx`: for each test, state the wrong implementation that would still pass it (mutation by reasoning — you may not edit). Is there a test per surface for: labels, population, so-what, row click, empty, error + retry, missing params, Hint resolution, export disabled? Mocks: `@/supabaseClient`, `recharts`, `../api` — nothing hits the network.
2. Code: no `supabase.from` in components (C6) · no logical Tailwind utilities (`ms-|me-|ps-|pe-|start-|end-`) · no invented colors · `key` props stable · `transformPayload` returns the C8 shape and never deletes a key · effects have correct deps · cognitive complexity ≤ 20 (`npx eslint <files>`) · `npm run dup` findings in the tab · CR = 0 (`perl -ne '$n+=tr/\r//; END{print "CR=$n\n"}'`) · `npm run check:bidi` on the tab's files.
3. Reconciliation: the builder's `payload_changes_reconciled` vs `<scratchpad>/results/p2vf-<tab>.json` / `p2fix-hostesses.json` / `p2-e3-finance.json` — every listed payload change is reflected in the tab (or provably irrelevant).

## Output (final message = data, also `<scratchpad>/results/p3v-<tab>-<lens>.json`)
`{ "tab", "lens", "findings": [ { "surface", "severity": "blocker|major|minor", "claim_refuted", "evidence": "<file:line | screenshot path | query>", "conflict_question": null|{ "source_a", "source_b" } } ], "checked": [ "<what you measured and found correct>" ], "screenshots": [...], "not_verified": [...], "blind_spot": "..." }`
Empty `findings` is legitimate — say so with what you measured. Never invent a finding to fill the list.
