# P3-F · Copy evaluator by onboarding STATE — fresh eyes, READ-ONLY, no changelog

**Repo:** `C:\Users\ishay\Reg-In`, branch `ishay/module-11-build`. You edit no repo file. You are one of four evaluators; your dispatch message names your **state** (mode 0 or mode 2) and your **persona**. You are deliberately NOT given the list of changes or the builders' reports — you evaluate what is on screen as a first-time reader. Report in English; quote screen text in Hebrew verbatim.

## Read first
`C:\Users\ishay\.claude\skills\ui-copy-and-onboarding-levels\SKILL.md` — its evaluation phase (the last phase, "F"): run ONLY that phase, as an evaluator, not the earlier phases. Then `docs/plans/ui-copy-styleguide.md` and `docs/specs/module_11_reports/stage2-review/m11-copy-rules.md` §1 + §4 (the 20 copy rules — your findings cite a rule number or they are taste, and taste is reported separately as *"taste, no rule"*). `docs/specs/module_11_reports/onboarding-layer-contract.md` §3 (what is BASE vs LAYER: population line, "אז מה", definitions are base at mode 0).

## Personas
- **Mode 0 (two evaluators):** *"A software engineer at a partner company opens `/reports` for the first time. Does this read as a mature product, or as something a programmer built for himself?"* — English words on screen, raw enum values, function names, `undefined`/`null`/`NaN`, `[object Object]`, dates in the wrong format, `₪`/`%` on the wrong side, a sentence where a noun-phrase label belongs (rule: labels are noun phrases), imported metaphors (צנרת / משפך), inconsistent terms for the same thing across the 16 surfaces, empty states that say "אין נתונים" where an error should say *"נסי שוב"*.
- **Mode 2 (two evaluators):** *"A new office employee on her first week, no one to ask. On each surface: does she know what to do next and why, from the hints alone?"* — every hint answers "what is this for / what do I do first"; no hint states a live number; no hint contradicts the base text; hints are not the base text repeated; the "אז מה" line is action-first; a hint that assumes knowledge she cannot have.

## Method
Start the dev server on your own port (`npx vite --port <port> --strictPort`; evaluators use 5185 · 5186 · 5187 · 5188 — never 5173). Sign in as CEO (identities from `.env.local` `E2E_*`, never printed; `login()` pattern in `e2e/permissions.spec.js`). **Set the onboarding mode for your state** through module 9's setter (`src/modules/09_settings/api.js`, `notification_preferences.onboarding_mode`; read the current value first and RESTORE it when done; re-login after changing — `AuthContext` loads it once). Visit all four tabs, all 16 surfaces (the chips row), drill one level on מ3 and מ9, open the export caption, trigger one error state (`page.route` abort) and one masked tab (log in as STAFF). Dump the visible text of each surface (`innerText`) to `<scratchpad>/results/copy-<state>-<n>/<surface>.txt` and screenshot each.

## Output (final message = data, also `<scratchpad>/results/p3-copy-<state>-<n>.json`)
`{ "state": 0|2, "persona": "...", "verdict_table": [ { "surface", "verdict": "mature|mixed|programmer-built" | "knows-what-to-do|partly|lost", "evidence": ["<verbatim screen text>"], "rule": "<rule number or 'taste, no rule'>" } ], "cross_surface_inconsistencies": [ { "term", "surfaces", "variants": [...] } ], "locked_label_findings": [ { "label", "surface", "issue", "rule" } ], "not_verified": [...], "blind_spot": "..." }`
Findings on a label locked by `spec.md §1.4` are **reported for Ishay's morning table**, never proposed as a change. "אין הערות" on a surface is a legitimate verdict.
