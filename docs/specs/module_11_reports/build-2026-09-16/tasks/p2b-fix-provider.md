# Task P2ב-F · Make `classify-feedback` diagnosable, fix the provider call, redeploy, and run the seeding run to completion

**Session type:** fix + operate. **Repo:** `C:\Users\ishay\Reg-In`, branch `ishay/module-11-build`. You may edit ONLY `supabase/functions/classify-feedback/index.ts` and its `README.md`. **Never commit.** You MAY deploy through MCP (`deploy_edge_function`, `verify_jwt: true`) and run the function as CEO — Ishay pre-approved the seeding run (`docs/micro_guides/module-11.md §9 D-9`). **Never print a key or token.** Report in English.

## What happened (read `<scratchpad>/results/p2b-classify.json` and the verifier's findings below before touching anything)
The deployed function works on every gate (401/403/400/404/409 all verified live) and the key is present, but the seeding run **failed three times on the first batch with `gemini responded 500`** — and the function **discards the provider's response body** (`console.error('gemini responded', res.status)` only), so nobody can say why. Three `feedback_ai_runs` rows are `failed` (ok=0, failed=0) — leave them. Leading hypothesis (the builder's own pre-registered risk): `generation_config.temperature` is not a field of the `interactions` API.

## Do, in this order — three attempts per hypothesis, then stop and report (module rule)
1. **Diagnosability first, always kept:** on every non-2xx provider response, read `await res.text()` and (a) `console.error` the status plus the first 600 characters, (b) store the first 600 characters in the run row — add nothing to the schema: put it into the existing `model` column? No — that column means the model id. Instead return it in the function's JSON body under `provider_error` (capped, never echoed further than the CEO's browser) and log it. If the body is empty, say so explicitly. This stays in the code permanently.
2. Redeploy, run `{ action: 'start' }` as CEO once, read the captured provider error text from the logs (`get_logs` / the response). **Now you know the real reason.**
3. Fix according to what the provider said — likely candidates, in order: drop `generation_config.temperature` (use `seed` if the doc lists it) · adjust the `response_format` field names · the model id (use the id the error names as valid, or the one `https://ai.google.dev/gemini-api/docs/models` lists as current for flash) · **if the `interactions` endpoint itself is the problem, fall back to the documented `POST /v1beta/models/<model>:generateContent` with `generationConfig.responseMimeType: 'application/json'` + `responseSchema` (the classic shape — `https://ai.google.dev/api/generate-content`)** — the function already has the code structure; keep one code path, not two. Verify the shape against the doc page you cite, then redeploy and run again.
4. When a run reaches `done` (or `partial` — then `continue` until `done`): measure the outcome exactly as `<scratchpad>/tasks/p2b-deploy-verify.md` step 5 lists (rows, unclassifiable, topic distributions, `free_topic` non-null, a 5-row sample with real Hebrew quotes, privacy of the prompt), then **approve** the run as CEO with `supabase.rpc('approve_feedback_ai_run', { p_run_id })`, prove RECRUIT (view) is refused (42501) and a second approval is refused.
5. `npx -y deno check --node-modules-dir=none supabase/functions/classify-feedback/index.ts` and `npx prettier --check` on the file — both must pass; CR = 0 (`perl -ne '$n+=tr/\r//; END{print "CR=$n\n"}'`).
6. Update the README's "What is not verified" to what is now verified, and record the final provider shape with the doc URL and the date.

## Tools
MCP (ToolSearch `select:mcp__5c4d90c8-bdb0-4e4a-bd64-299d0299d315__deploy_edge_function,mcp__5c4d90c8-bdb0-4e4a-bd64-299d0299d315__execute_sql,mcp__5c4d90c8-bdb0-4e4a-bd64-299d0299d315__get_logs,mcp__5c4d90c8-bdb0-4e4a-bd64-299d0299d315__list_edge_functions`), `WebFetch` and `context7` for the provider docs, a temporary Node script in the scratchpad (delete it after) that signs in as CEO / RECRUIT with `E2E_*` from `.env.local` and calls the function / the RPC.

## Output (final message = data)
`{ "provider_error_seen": "<verbatim, capped>", "root_cause", "fix_applied", "final_shape": { "endpoint", "model_id", "fields", "doc_url", "date" }, "attempts", "run": { "run_id", "status", "sent", "ok", "failed", "continues" }, "outcome": { ... as in p2b-deploy-verify step 5 ... }, "approval": { "approved_at", "approved_by", "view_refused", "double_refused" }, "story_4_true_now": bool, "deno_check", "prettier", "not_verified", "blind_spot", "assumed" }`
