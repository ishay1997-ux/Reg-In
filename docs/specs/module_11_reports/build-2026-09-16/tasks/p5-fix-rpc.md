# P5-FIX-RPC · one fix round on the report RPCs after the evidence step (written 17/09/2026 00:1X by the orchestrator)

**Repo:** `C:\Users\ishay\Reg-In`, branch `ishay/module-11-build` @ `300e80dc`. You own: ONE new migration file `supabase/migrations/20260917<HHMMSS>_module11_j1_rpc_round4.sql` (never edit a committed migration — `protect-frozen-files.sh` blocks it; fix forward) · `docs/schema.sql` §24 (the per-function "live file" pointers + the function bodies you change, refreshed IN PLACE, never regenerated) · `docs/db_roadmap.md` §10ב (one row for the file). **Nothing in `src/`, nothing else in `docs/`. Never commit.** Report in English; SQL strings in Hebrew. A shell fixer and a doc fixer work in parallel on disjoint files.

## Read first
`supabase/migrations/CLAUDE.md` (the DB protocol; the apply gate's typed echo is WAIVED for this module by Ishay — `docs/micro_guides/module-11.md §9 D-9`, verbatim *"על כולם — מיגרציות (~8) · חתימת-DoD · ריצת-הזריעה של הסיווג"*) · `docs/micro_guides/module-11.md §2ב C8` (the payload contract) and `§9 D-20 · D-21 · D-34 ⑤⑧ · D-35` · the latest bodies of the functions you touch: `report_m08_profitability` (live = file I1 `20260916114500_module11_i1_rpc_formats_and_notes.sql`), `report_m15_reliability` and `report_m14_hostess_overview` (live = I2 `20260916194500_module11_i2_rpc_round3.sql`), `report_m16_quality_cost` (live = I1) — confirm each with `docs/schema.sql §24` and `md5(prosrc)` before editing · the findings: `C:\Users\ishay\AppData\Local\Temp\claude\C--Users-ishay-Reg-In\ae84cb05-7ca4-4a75-80cd-6e6b13995904\scratchpad\results\anomalies-summary.md` items **[4] [5] [6] [7] [24]** (each carries migration file:line evidence) · the cards those surfaces come from: `docs/specs/module_11_reports/cards-finance.md` (מ8) and `cards-hostesses.md` (מ14 · מ15 · מ16) — the sections that define the so-what, the materiality floor, and the definitions line.

## The fixes (all in the one file, one `create or replace` per function, byte-faithful copy of the current live body plus the change)
1. **[4] מ8 "אז מה" names a project by % while the table ranks by ₪** (D-34 ⑧ residue). The so-what sentence names the FIRST row of the ranking as displayed (largest ₪ deviation — 📑ב#5), with its ₪ and % figures; keep the sentence's locked shape (*"לפתוח את פרויקט … — …"*).
2. **[5] מ8 declares a materiality floor it does not apply**: projects under 4 planned hours or under 1,000 ₪ planned labour cost sit inside the ranked rows. Exclude them from `rows` (they stay in the population and the footnote counts, exactly as the footnote already says) — read the card's definition of the floor before writing it, and re-check the footnote's numbers after.
3. **[6] מ16 tile prints the median rate with two decimals and no ₪** (`'תעריף מעל החציון (' || round(v_median, 2) …`): whole ₪ everywhere (📐4, D-34 ⑤) — `round(v_median)` with ` ₪`, matching the tile *"תעריף שעתי חציוני 43 ₪"* on the same screen.
4. **[7] מ15 definitions cite `reliabilityScore` and `(הכרעה 38)` to the end user.** Rewrite that one sentence in plain Hebrew (*"אותה נוסחה שמדרגת את הדיילות בשיבוץ"* — keep the meaning, drop the identifier and the ruling number; D-34 ④ is the precedent).
5. **[24] The same three constants print at six decimals on מ14 and four on מ15.** Harmonise to FOUR decimals on מ14 (מ15's precision), and make מ14 say *"דיילת ענבר"* where מ15 does, if that is the secondary difference the finding names — check both definitions strings side by side first.

## Added 00:3X from the copy evaluations (`results\copy-summary.md` · `results\p3-copy-0-1.json` · `p3-copy-2-1.json` — quote the screen text from there)
6. **[C4] מ20 (`report_m20_satisfaction`, live = I2):** the notes line prints a spec token on screen — *"הספירות מוצגות כ"N מתוך M" ולא באחוזים (📐11)."* Remove *"(📐11)"* (כ4: rule identifiers never on screen) and write the counts convention in words (*"כמה מתוך כמה"*), once — the hint above it already says the multi-reason caveat, so the notes line does not repeat it (R27).
7. **[C5] מ22 (`report_m22_notes`, live = I2):** the run bar names the approver by login address (*"אושרה ע"י ishay1997@gmail.com"*) — join `users` on `approved_by` (an email FK → `users(email)`) and show the person's display name (the column `users` uses everywhere else; fall back to the email only if the name is null); the notes line ends with vendor model ids (*"gemini-3.5-flash-lite, gemini-3.8-flash"*) — drop the ids from the on-screen sentence (keep them in `meta` if the export needs them). Also the run bar says *"הריצה"* (one) while the notes say two approved runs are shown together — make the bar say how many runs are shown (מ22 shows every approved run).
8. **[C6] מ21 (`report_m21_drifting`, live = I2) — a suspected wrong value:** the third tile *"נתפסים רק בקצב האישי"* shows **3**, and its compare line reads *"כלל ⁦120⁩ הימים לבדו מוצא ⁦11⁩ לקוחות, ואת אלה הוא מפספס: ⁦11⁩"* — the second number should be the count the 120-day rule MISSES (the personal-rate-only customers, i.e. the tile's own 3), not 11. Re-derive both numbers from the tile's query, fix the sentence, and report the two values you measured.
9. **[C8] מ9 (`report_m09_aging`, live = I2):** the same concept is called *"מדרג"* (8×) and *"דלי / דליי-האיחור"* (4×) on one screen — harmonise to *"מדרג"* (the locked term of spec §1.4 and the crumbs). Same file: the mode-0 chart narrative paragraph (seven lines, one per bucket with live numbers) is a question for Ishay (recorded by the doc fixer), NOT yours — do not touch it.
10. **[C7] NOT yours — recorded as a question:** the year-ago comparison row under the tiles has four wordings across nine surfaces (*"2025 באותו טווח"* · *"לפני שנה, אותו חישוב"* · *"התקופה המקבילה אשתקד"* · *"אשתקד באותו טווח (…)"*). Harmonising touches nine functions and a wording ruling — it goes to the morning table, not into J1.

## Apply and prove (the D-20 / D-35 discipline)
Apply through the Supabase MCP (`apply_migration`, project `yfeovxppnfoafmfbdfvh`) **per function, < 60 KB per call** (the ~90 KB truncation is silent, D-20), naming each chunk `module11_j1_<fn>`; then compare registry NAMES to your chunk list (never the count) and hash: `md5(prosrc)` from `pg_proc` must equal the `$function$` body in your file for every function you touched (`docs/specs/module_11_reports/build-2026-09-16/scripts/fn_md5.py`, glob adapted). Signed-in probe of each changed RPC as CEO (a temporary Node script in your scratchpad, `.env.local` `E2E_*` never printed, deleted after) — 200 and the changed strings/rows present; and one blocked identity ⇒ `42501` on a finance function. Grants: `revoke … from public, anon, authenticated` then `grant … to authenticated`, byte-identical to the current file's tail. `docs/schema.sql §24`: point the changed functions at J1 and refresh their bodies in place; `db_roadmap §10ב`: one row for J1 with what landed. CR count 0 on every file (`perl -ne '$n+=tr/\r//; END{print "CR=$n\n"}'`). Then `npm run check:declared-counts` and `npm run check:docs-structure` (exit codes from logs).

## Output (final message = data, also `<your scratchpad>/results/p5-fix-rpc.json`, written after EVERY function, not at the end)
`{ "file", "functions": [ { "name", "items": ["[n]"], "chunk_name", "registry_row": bool, "md5_file", "md5_live", "equal": bool, "probe": "200 · <what changed on the payload>" } ], "schema_sql_updated": bool, "db_roadmap_row": bool, "not_done": [ { "item", "why" } ], "not_verified": [...], "blind_spot": "<checkable>" }`

כל עובדה כאן ניתנת לערעור — אם מדדת אחרת, תקן אותי עם המדידה.
