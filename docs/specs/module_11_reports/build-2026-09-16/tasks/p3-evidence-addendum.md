# P3-S · Evidence — ADDENDUM for the per-tab split (written 16/09/2026 23:2X by the resuming orchestrator)

Read `docs/specs/module_11_reports/build-2026-09-16/tasks/p3-gate-evidence.md` **§P3-S** first, then THIS. Where they disagree, THIS wins. You are ONE of four concurrent evidence agents; your dispatch names your tab (`exec` · `finance` · `hostesses` · `customers`). Pacing: the handoff's D-33 caps concurrency at 5 — you are 4, so do not spawn sub-agents.

## 1. Corrections to §P3-S (each carries its source)
- **Onboarding mode 2 is forced by the PostgREST intercept, never by module 9's setter and never by a DB write** (`HANDOFF-build-2026-09-16.md §0א` row 5; pattern `e2e/reports.spec.js:128 forceOnboardingMode`; iron rule `e2e/CLAUDE.md §2.1`). Nothing to read or RESTORE. The runner below already does this for both modes (0 and 2) and for every identity.
- **The server is a frozen preview build on port 5189** (`npm run preview -- --port 5189 --strictPort`, serving `dist/` built at 23:1X from HEAD `300e80dc`), not a Vite dev server. Reason, measured 09/08/2026 in `playwright.e2e.config.js`'s header: a dev server's HMR full-reloads tear login sessions under parallel load; a preview server has no file watcher. **It is already running — do not start, stop or restart any server.** Verify with `curl -s -o /dev/null -w "%{http_code}" http://localhost:5189/` ⇒ `200`.
- **Chart cap:** ≤ 2 per surface EXCEPT the customers tab, where `CHART_CAP` was raised to 4 (`docs/micro_guides/module-11.md §9 D-28`, Ishay's ✅4 on the mockup). Report the count; flag > 2 only outside customers, and flag > 4 anywhere.
- **The identity matrix is the expected result, not a bug** (`p3-common.md`, measured 16/09 05:2X): CEO opens all four tabs · FINANCE sees דיילות masked · RECRUIT sees only דיילות · STAFF sees all four masked with the sentence *"אין דוחות זמינים בתפקידך — פני למנכ"ל"*. When an identity requests a tab it cannot open, `ReportsPage.jsx:227-229` falls back to the first tab it CAN open — so a "masked" screenshot shows the fallback surface plus the masked tab pill; that is what the runner records as `state: "masked"`.

## 2. The runner — run it, do not rewrite it
Two files in the orchestrator's scratchpad (READ them once so you know what was measured and how):
- `C:\Users\ishay\AppData\Local\Temp\claude\C--Users-ishay-Reg-In\ae84cb05-7ca4-4a75-80cd-6e6b13995904\scratchpad\evidence-common.mjs` — login (copied from `e2e/permissions.spec.js`), session capture/restore (the app keeps the Supabase session in `sessionStorage`, `src/supabaseClient.js:14`), the onboarding intercept, `openSurface` (waits for one of the five 📐10 terminal states or the masked tab), `measure` (DOM probes scoped to the main table's `thead` — the ChartCard sr-only table trap from §0א D), `shoot` (full-page PNG at 1280×800).
- `…\scratchpad\evidence-run.mjs` — the matrix for one tab: 4 surfaces × identities CEO·FINANCE·RECRUIT·STAFF × modes 0·2 ⇒ 32 PNGs + measurements per surface for every identity, and the extras your dispatch names. It rewrites `evidence-run-<tab>.json` after EVERY surface (D-24: a killed agent loses its report, not its writes).

**Command (Bash tool, from the repo root `C:\Users\ishay\Reg-In`, output to a log — never pipe to head/tail, the dialect hook blocks it):**
```
cd /c/Users/ishay/Reg-In && node "/c/Users/ishay/AppData/Local/Temp/claude/C--Users-ishay-Reg-In/ae84cb05-7ca4-4a75-80cd-6e6b13995904/scratchpad/evidence-run.mjs" --tab <TAB> --extras <EXTRAS> --stagger <SECONDS> --out "/c/Users/ishay/AppData/Local/Temp/claude/C--Users-ishay-Reg-In/ae84cb05-7ca4-4a75-80cd-6e6b13995904/scratchpad/results" > "/c/Users/ishay/AppData/Local/Temp/claude/C--Users-ishay-Reg-In/ae84cb05-7ca4-4a75-80cd-6e6b13995904/scratchpad/results/evidence-run-<TAB>.log" 2>&1; echo "exit=$?"
```
Then read the `.log` and the `.json`. Expected wall time ≈ 4–8 minutes. The self-test (CEO · exec-overview · modes 2 and 0) passed at 23:26: `rendered · rows=8/8 · hints=4 (m2) / 0 (m0) · charts=1 · overflow=0 · console=0`.

Outputs land in `…\scratchpad\results\evidence\<IDENTITY>-m<mode>-<slug>.png` (+ `CEO-m2-<slug>-drill1/2.png`, `-export-caption.png`, `-error.png`, `-error-recovered.png`, `drifting-empty-filter.png` for the extras) and `…\scratchpad\results\evidence-run-<tab>.json`.

**If the runner fails on a surface** (`failures[]` non-empty): read the `-failed.png`, diagnose from the error text, and RE-RUN only that slice with `--identities X --modes N --surfaces slug` (the login state is cached per identity for 40 minutes under `results/auth/` — never open or print those files, they hold a live token). If a login fails 3 times, record it in `not_verified` and move on — do not hammer the auth endpoint (Supabase rate-limits sign-in per IP; the m4 E2E precedent was a login timeout under load).

**You may extend the runner only by ADDING a probe you need** (a new field in `measure`, a new extra) — never change what existing fields measure, and say in your report what you added. Do not edit anything under the repo. Do not write to the database. Do not run `npm run gate`/`test:e2e`/`smoke` (other steps own them).

## 3. What YOU do after the runner (the judgement the runner cannot do)
1. **Per surface, as CEO mode 2 — the 5-pass table of `src/CLAUDE.md §4`** (direction · inventory · consistency · copy · empty input), each pass with ONE line of evidence taken from the PNG and/or the DOM measures (quote the on-screen text you judged). Open the CEO m2 and m0 PNGs of each of your four surfaces with the Read tool. For the other three identities, the `state` in the JSON plus ONE look at each identity's first PNG is enough to confirm rendered/masked — say which PNGs you actually opened.
2. **Anomalies** — anything that is wrong on screen or in the measures, each with evidence (PNG name + what you saw, or the JSON field + value). Check at least: `scroll_overflow_px ≠ 0` · `console_errors > 0` · `hints_m0 ≠ 0` · `hints_m2 = 0` on a surface that has §⑩ keys · chart count over the cap · `rows_page1` vs `pager_total` (page 1 must show `min(total, page size)`) · ₪ / % / minus glyph order and mixed `50 ₪` vs `₪50` on one screen · `[object Object]` · `NaN` · `undefined` · `null` · English leaking into labels · a masked identity seeing tiles/table of a tab it is blocked on · the error envelope saying *"אין נתונים"* · the clear-chip wording must be *"× נקי בחירה"* (D-34 ③) · population sentences must NOT print *"(הכרעה 36)"* (D-34 ④) · per-hour rates whole-₪ (📐4) · mode 0 keeps population line, "אז מה" and definitions (C3 deletion test).
3. **Known, ruled, NOT anomalies** (do not report these): aging (מ7 open-debt tile · מ9) ignores the period filter and says *"נכון להיום"* · מ14's red-queue tile is a frozen 12-month window · מ16 lists all 50 active hostesses, 17 with `rating: null` · מ22 shows 426 approved insights from runs 5 and 6 · the hostesses tab opens on 12 rolling months (ruling ⑧H2, 16/09 17:4X) · מ2's 8 rows are a complete top-list, not a cap.
4. **Contact sheet:** write `…\scratchpad\results\evidence\index-<tab>.html` — a plain HTML list (RTL, `dir="rtl"`) of every PNG you produced with a one-line caption each (identity · mode · surface · state · the one thing worth seeing). Relative `src` paths (the PNGs sit beside it). The orchestrator merges the four into `index.html`.
5. **Report:** write `…\scratchpad\results\p3-evidence-<tab>.json` in the §P3-S output shape (`surfaces[]` with `id`, `identity_matrix`, the measures, `five_pass`; `anomalies[]`; `not_verified`; `blind_spot`) **incrementally — after each surface's judgement, not at the end** — then return it as your structured output. `blind_spot` must be concrete and checkable (a file · a mechanism · a scenario someone can check true-or-false), never a generic hedge.

## 4. Reading floor (open these; say if you opened anything else)
`tasks/p3-gate-evidence.md §P3-S` · this file · `src/CLAUDE.md §4` (the five passes) · `src/modules/11_reports/CLAUDE.md` (invariants and traps) · `docs/specs/module_11_reports/spec.md §1.4` (the locked labels of YOUR tab's four surfaces — the copy pass compares against them) · the two runner files. Probably NOT needed: the cards, the mockups, the micro-guide beyond §9 D-28/D-34 — open only on a real doubt and say so.

כל עובדה כאן ניתנת לערעור — אם מדדת אחרת, תקן אותי עם המדידה.
