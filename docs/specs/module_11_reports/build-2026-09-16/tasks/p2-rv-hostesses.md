# P2-RV hostesses · Finish the interrupted F2 fix-forward: re-verify every finding specifically, finish the tests, fix-forward F3 only if a mechanism is measured broken

**Repo:** `C:\Users\ishay\Reg-In`, branch `ishay/module-11-build`. **Never commit.** Report in English.
**Your write scope:** `src/lib/reportsHostesses.js` + `.test.js` · at most ONE new migration `supabase/migrations/20260916<HHMMSS>_module11_f3_rpcs_hostesses_fixes.sql` (append-only; only if a mechanism is broken — never edit the applied F/F2 files) applied through MCP `apply_migration` (waiver `module-11.md §9 D-9`) · a temporary signed-in Node script in `<scratchpad>` (deleted after; never print credentials).

## State you inherit (measured by the orchestrator 16/09 08:0X — re-check, do not trust)
- The previous fixer (task `<scratchpad>/tasks/p2-fix-hostesses.md` — READ IT: the rulings per finding are there) wrote `20260916065642_module11_f2_rpcs_hostesses_fixes.sql` (107,587 bytes, CR=0) and applied it in **seven registry rows**: `module11_f2_hostesses_m17` · `_m16` · `_m15` · `_m14` · `_m15_fix` (a runtime defect it found in its own m15 change — `FULL JOIN` on a non-hashable condition) · `_m14_winlabel` · `_m17_winlabel`. md5(prosrc) of the four live bodies == the file's `$function$` bodies (4/4, orchestrator's `<scratchpad>/fn_md5.py`). **It was killed by a quota limit right after the winlabel applies** — its last words: *"The month-preset run exposed the same defect one tile over (`87.2%`→`93.9%` still labelled '12 החודשים האחרונים'). Fixing that class in m14 and m17 too."* ⇒ the "winlabel" class (a tile whose value follows the caller's month preset while its label/window still says "12 החודשים האחרונים", or the reverse) was fixed in m14 and m17 — **was m15 also checked? was the fix complete?** You verify with a month preset AND the default window on all four.
- It never: wrote its report · finished the six test additions the task lists · ran eslint/prettier on the lib files · re-verified findings F1–F7 + the minors one by one. That is your job.
- The hostesses TAB builder is building `HostessesTab.jsx` **in parallel against the live payloads** — any F3 change is **ADDITIVE ONLY**; list every payload change under `payload_changes`. The tab builder (not you) adds `export` to `WEEKDAY_NAMES_HE` in `src/lib/dates.js`.

## Read first
1. `<scratchpad>/tasks/p2-fix-hostesses.md` (rulings F1–F7 + minors + the six tests) · `<scratchpad>/results/p2v-hostesses.json` (the 11 findings + `tests_pass_but_would_not_catch`) · `<scratchpad>/results/p2-hostesses.json` (the builder's claims).
2. `docs/micro_guides/module-11.md` §2ב C1–C8 with all amendments (window rule D-17; `meta.extra_tables`; `chart.label_source`; `tiles[].sub`; `drill_key {kind}`; D-14 drill set מ3·מ9 — מ17 has `drill: null`) · §9.
3. The F2 file in full (its header records the registry-split trap, the 0.4329 record fix, and what each function changed) and `src/modules/11_reports/components/ChartCard.jsx` (ScatterBody contract: `series[0]` = X, `series[1]` = Y, `xKey`; composed chart `series[].kind/axis`) · `ReportSurface.jsx` (renders `meta.extra_tables` itself; `columns[].sorted` ⇒ `aria-sort`).
4. `stage2-cards/cards-hostesses.md` מ14 (84) · מ15 (194) · מ16 (293) · מ17 (393): §③ §⑤ §⑧ · `spec.md §1.4` דיילות row · `spec.md §🔢` (Gini 1·2·3·4·10 ⇒ 0.40) · `signoff-baseline-2026-09-10.md` · `processes-approved.md §📐` + §📑ב rows for דיילות (#13 = the frozen 12-month window anchored to Israel today).

## What you verify (each with a live signed-in call — CEO or RECRUIT allowed; FINANCE and STAFF ⇒ `42501`)
Per finding of `p2v-hostesses.json` (F1…F7 + minors): `id · fix present in live prosrc (position(...) > 0) · live payload evidence · result PASS/FAIL`. Specifically:
- **F1** m17 compiles and returns the C8 shape: Gini `0.4559` · `55.3 % / 16.2 % / 26.0 %` · `48 / 13.5 / 17.6` · `9.9 / 20.7` · `92.6 %` · Lorenz 107 vertices · adoption tile `value: null` + the absence sentence (never `0%`) · `drill: null` · `drill_key {kind:'hostess', id}` · `tiles[].sub`.
- **F2** m14 red-queue tile = the frozen rolling-12-months value with `p_to='2026-08-16'`, with `p_to=null`, AND with a month preset — same value, label *"חלון קפוא · 12 חודשים"*, `meta.notes` says it ignores the period; the other m14 tiles follow the caller window as the card defines them.
- **Winlabel class:** for each of m14 · m15 · m17, every tile's `window`/label agrees with the window its value was computed in (call with the default window and with `p_from='2026-08-01', p_to='2026-08-31'`; compare tile by tile — a value that moves must not carry a "12 חודשים" label, and a value that stays must not carry the month label).
- **F3** m16 `series` = `[{key:'hourly_rate',…},{key:'rating',…}]`, `xKey:'hourly_rate'`, Y domain `[1,5]`.
- **F4** m15 live values stated (definition applied to today's data; card digits drift by 📐22 — no fix).
- **F5** m15 `meta.no_show_by_rating` = `{ months12: {3,4,5}, ever: {3,4,5} }` with six numbers AND `meta.extra_tables[0]` = *'אי-הגעה לפי דירוג'* (columns דירוג · 12 חודשים · אי-פעם) — derive the six from your own query and compare.
- **F6** m16 rows = 50 (17 with `rating: null`), sorted per 📐7, `meta.default_filter: 'no_rating'`, `meta.no_rating_count: 17`.
- **Minors:** population sentence exclusions count only past-event pending invites / unmarked past shifts; `meta.drill_echo` + `meta.customer_filter_ignored: true` on all four; m15's `chart.label_source: 'WEEKDAY_NAMES_HE'` with `dow` 0–6.
- Cross-cutting: LRI…PDI + thousands separators in server-built sentences (scan for 4+ digit runs, excluding years); `tiles[].compare` on every tile or `sub` explaining the absence; `comment on function` on all four with the declared population; `select count(*) from pg_proc where proname like 'report_m1%'` ⇒ report the count.

## Tests (the six the task lists — add the ones missing; mutation-probe each by editing and restoring)
AMBER boundary (`score == amber` ⇒ not amber) · `hostessReliabilityScore` delegates to `responsivenessScore` (spy) · `reliabilityParamOrderNote` attribution (0.87 אדום · 0.95 ענבר, order asserted) · `topDecileShare` on n=106 ⇒ 26.0 % · `medianOf` with `null`/`NaN` · Lorenz first vertex on a 106-point fixture from the live shift counts (paste as fixture with the date). Then `npx vitest run src/lib/reportsHostesses.test.js src/lib/reportsExecutive.test.js src/lib/reportsCustomers.test.js` green · `npx eslint` + `npx prettier --check` on your two files · CR = 0 via `perl -ne '$n+=tr/\r//; END{print "CR=$n\n"}' <file>`.

## If you write F3
Only for a mechanism measured broken (a wrong number, a missing key, a label that lies) — never for taste. Pull live bodies with `pg_get_functiondef`; chunks < 60 KB; registry names `module11_f3_hostesses_m<NN>`; header lists the rows; after apply `md5(prosrc)` == file body (`<scratchpad>/fn_md5.py`, adapt the glob to include `f3`).

## Output (final message = data, also `<scratchpad>/results/p2fix-hostesses.json`)
`{ "f2_state": { "registry_rows": [...], "md5_match": "4/4" }, "per_finding": [ { "id", "fix_present", "reverified_by", "result" } ], "winlabel_audit": [ { "rpc", "tile", "default_value", "month_value", "label", "verdict" } ], "m17_live": {...}, "f3": null | { "file", "applied_as", "md5_match", "payload_changes": [...] }, "tests": "...", "not_verified": [...], "blind_spot": "...", "assumed": [...] }`
