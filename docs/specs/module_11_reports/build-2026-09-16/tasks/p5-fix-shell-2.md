# P5-FIX-SHELL-2 · second shell/tab round — the leftovers of round 1 and the J2 contract (written 17/09/2026 01:2X by the orchestrator; Ishay's word 00:5X: *"מטפלים הכל מהיסוד בלי קיצורי דרך"*)

**Repo:** `C:\Users\ishay\Reg-In`, branch `ishay/module-11-build` (round 1 is committed; read `git log -1`). You own THIS round's files only: `src/modules/11_reports/components/ReportTable.jsx` (+ its test) · `src/modules/11_reports/components/ReportSurface.jsx` (+ test) · `src/modules/11_reports/components/reportsCopy.js` · `src/modules/11_reports/tabs/customers/AnalysisRunBar.jsx` (+ the customers tab test that covers it) · `src/lib/onboardingCopy.m11.finance.js` · `src/lib/onboardingCopy.m11.customers.js` · `src/modules/11_reports/ReportsPage.jsx` (+ test) only if item 5 needs it. **Never commit.** A parallel RPC agent (J2) edits `supabase/migrations/` + `docs/schema.sql` + `docs/db_roadmap.md`; a parallel doc agent edits `docs/**`. Read round 1's report first: `…\scratchpad\results\p5-fix-shell.json` (its `not_done` and `blind_spot` are your inputs) and `…\scratchpad\results\p5-fix-rpc.json` `not_done`.

## The items
1. **Remove the `_id` key heuristic; keep `format:'id'` only.** Round 1 made `ReportTable` treat a key ending in `_id` as an id (`ReportTable.jsx:48-49`, `ID_KEY_SUFFIX`). J2 declares `'format','id'` on every identifier column in the RPCs (the contract, C8) — two mechanisms for one behaviour is the defect class D-30 removed. Delete the suffix rule and its test case; keep `FORMATTERS.id` and the `format:'id'` path with its tests. ⚠️ Until J2 is applied the live payloads still say `int` — that is expected for a few minutes; the orchestrator re-shoots after both rounds land.
2. **`reports.profitability.sortWhy` (mode-2 hint) now contradicts the table** (`src/lib/onboardingCopy.m11.finance.js:76`): it ends *"… ושורה שמסומנת "מתחת לרצפת-המהותיות" יורדת לסוף הטבלה כי המכנה שלה זעיר מדי"* — after J1 no such row is in the ranking (📑ב#5: below-floor projects are in the population and out of the ranking; the footnote says so). Rewrite the sentence's tail to what the screen does: the ranking sorts by ₪ deviation (📑ב#5 · D-34 ⑧) and below-floor projects are counted in the footnote, not ranked. No live number in the hint (C3).
3. **`reports.satisfaction.*` hint restates the base note** (`src/lib/onboardingCopy.m11.customers.js:59`): after J1 the מ20 notes line already says the counts convention in words; the hint repeats it (R27). Rewrite the hint to carry only what the base does not say (why one feedback can carry more than one reason and what to do first), or drop the duplicated sentence.
4. **The מ22 run bar says "הריצה" (one run) while two approved runs are shown** (`tabs/customers/AnalysisRunBar.jsx:165`: ``מציג את הריצה מ-${day}, אושרה ע"י ${run.approved_by}``). J1 now returns `meta.run.approved_by` as the person's NAME (and `approved_by_email` raw) and the payload carries the count of approved runs shown (read `meta` of the live `report_m22_notes` — `…\scratchpad\results\p5-fix-rpc.json` names the keys; if there is no count key, count from `meta.runs[]` or say so and use what exists). Wording: singular when one run, plural with the count when more (*"מציג 2 ריצות-ניתוח מאושרות, האחרונה מ-16/09/2026 · אושרה ע"י …"*) — Hebrew number agreement per the styleguide; numbers isolated (LRI…PDI via `reportsFormat`). Update the covering test.
5. **State-2 envelope wording when a CUSTOMER filter emptied the table** (round 1 blind spot): `components/reportsCopy.js` locks *"אין נתונים בתקופה שנבחרה"* (ruling 15(ו), for the period). When the emptying filter is the customer selector, the sentence names the wrong filter. Add a second locked sentence for the customer case — *"אין נתונים ללקוח שנבחר"* (`הנחתי`, recorded by the doc agent in D-36) — and pick by which filter is active (customer selected ⇒ customer sentence; else period). Keep *"נקי מסננים"* on both. Tests for both.
6. **Loading/error state with `?customer=` on a surface that ignores the customer** (round 1 blind spot: `readScope` only fires from a loaded payload, so before/without a payload the selector is enabled and a chosen customer is echoed). Make the header/selector rule hold in those states too — the simplest honest form: while no payload has loaded, the header prints the selected period label and NO customer clause, and the selector is enabled (nothing is known yet); on error keep round 1's behaviour. Say what you chose. Test: the aborted-RPC case with `?customer=`.
7. Anything else in round 1's `not_done` that is code and in your file set: `[1]` export caption — round 1 found it NOT reproducible and reverted its own fix; do not retry; leave it.

## Gates (exit codes from logs, never piped)
`npx vitest run src/modules/11_reports src/lib > <your scratchpad>/vt.log 2>&1; echo exit=$?` — the WHOLE module + lib must be green (round 1 left one collateral failure in `tabs/ExecutiveTab.test.jsx:775`, already fixed by the orchestrator — verify it is green) · `npx eslint` + `npx prettier --check` on every file you touched · `npm run dup` · `npm run check:bidi` · CR=0 (perl idiom) · `npm run build > <log> 2>&1; echo exit=$?` (the preview on 5189 serves `dist/` — do not start/stop it) · then the live check with the runner for items 4 and 5: `node "C:\Users\ishay\AppData\Local\Temp\claude\C--Users-ishay-Reg-In\ae84cb05-7ca4-4a75-80cd-6e6b13995904\scratchpad\evidence-run.mjs" --tab customers --identities CEO --modes 2 --surfaces notes,drifting --extras empty --out <your scratchpad>/results-fix2` and look at the PNGs.

## Output — `…\scratchpad\results\p5-fix-shell-2.json`, same shape as round 1 (`fixes[]` with item · files · what_changed · test_added · live_check; `assumed[]` tagged הנחתי with the source checked; `gates`; `not_done[]`; `not_verified[]`; `blind_spot` concrete), written after EVERY fix.

כל עובדה כאן ניתנת לערעור — אם מדדת אחרת, תקן אותי עם המדידה.
