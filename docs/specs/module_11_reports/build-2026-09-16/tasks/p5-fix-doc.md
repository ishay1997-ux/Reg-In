# P5-FIX-DOC · the doc corrections and the morning-table rows after the evidence step (written 17/09/2026 00:1X by the orchestrator)

**Repo:** `C:\Users\ishay\Reg-In`, branch `ishay/module-11-build` @ `300e80dc`. You own `docs/specs/module_11_reports/spec.md` · `docs/micro_guides/module-11.md` (§9 only — one new row) · `docs/specs/module_11_reports/cards-*.md` only where an item below names a card · `docs/PROJECT_MASTER.md §6` only if a new cross-module debt appears. **Nothing in `src/`, no migrations, not `docs/schema.sql`, not `docs/db_roadmap.md` (the RPC fixer's), not `STATUS.md` / `CLAUDE_CODE_LOG.md` (the orchestrator's). Never commit.** Report in English; document text in the language of the document.

## Read first
`docs/CLAUDE.md` (iron rule 13 — where a ruling ripples; `check:docs-structure` heading rules) · `scripts/check-declared-counts.mjs` (which numbers in which five files are compared to what — you will move some of them) · `docs/specs/module_11_reports/spec.md §1.4` in full · `docs/micro_guides/module-11.md §9` (its row shape: `| 🆕 **D-NN** | finding | what was done |`; last row today is D-35) · the findings: `C:\Users\ishay\AppData\Local\Temp\claude\C--Users-ishay-Reg-In\ae84cb05-7ca4-4a75-80cd-6e6b13995904\scratchpad\results\anomalies-summary.md` — all 29 items; you act on **[12] [13] [22]** and you RECORD **[10] [14]+[21] [15] [18] [20] [23] [25]** and the two orchestrator items below.

## The corrections
1. **[12] [13] [22] `spec.md §1.4` is short against the screen** — the table the copy pass compares against lacks מ3's four tile labels, carries מ9's drill-level labels instead of its root labels (*"יתרת-חוב פתוחה · מעל 60 יום · מדרג 90+ · ימים לתשלום (חציון)"* + the side tile *"שוטף — עוד לא באיחור"*), and truncates *"צפוי להיכנס ב-30 יום"* to *"צפוי להיכנס ב-30"*. The SCREEN is the authority (labels arrive from the payload, C8/invariant 3; the same direction as the 16/09 *"נתח 5"* repair). Take the exact strings from the evidence: `results\evidence-run-<tab>.json` → `surfaces.<slug>.identities.CEO.m2.measure` has `h1`, and the tile labels are visible in `results\evidence\CEO-m2-<slug>.png` — or better, from the live payloads the tab agents saved (`results\payloads*` if present) — never retype from memory. Then **every declared count that changes** (the "74" and any other number `check:declared-counts` reads from the five files) is updated with its source line, and `npm run check:declared-counts > <log> 2>&1; echo exit=$?` is 0. If a label is a product question rather than a truncation (מ9's root vs drill labels may be a mockup-vs-card difference), record it as a question instead of changing the table, and say so.
2. **The morning-table row — micro-guide `§9 D-36`** (one row, dated 17/09/2026, "evidence step ② findings recorded, not decided"): the questions for Ishay, each in one line with its evidence pointer: [10] three charts on מ3 vs the ≤2 cap (D-28 raised it to 4 for the customers mockup — does the raise apply to הנהלה?) · [14]+[21] at drill levels the population sentence still describes the root (מ3 · מ9) · [15] on מ12 a cross-filter selection paints chart 1 and the table but not chart 2 · [18] מ19's four tile labels are sentences (already 🔵 open in §1.4 — point, do not duplicate) · [20] two totals on one screen — the pager's *"1–50 מתוך 50"* against the row-cap line *"מוצגות 50 מתוך 736 שורות"* (D-25 wording) · [23] מ8's fifth tile alone on a second row · [25] two מ15 column labels contain the *"·"* that the export caption uses as its separator. **Plus two orchestrator items:** (a) `approve_feedback_ai_run`: the live body differs from file A **in comments only** — the two Hebrew `--` comment lines inside the body were stripped on apply (live `md5(prosrc)` = `e03df921…`, file A = `69221f3e…`, 1,239 vs 1,369 chars, logic byte-identical; measured 16/09 23:2X) — **Ishay's ruling 16/09 23:4X: record for the closing audit's `close-findings`, do not fix now**; the 16 report functions ARE md5-equal to their live files (16/16). (b) The evidence harness: a full-page Playwright capture restarts the recharts animation and can freeze a chart at frame 0 (מ6's scatter looked empty; a fixed-viewport probe found 717 symbols + 8 bars) — durable lesson: chart evidence = element screenshots or animations disabled; and an identity with no `notification_preferences` row cannot be forced to mode 2 by mapping an empty array — fixed in the runner by synthesising one row (still no DB write).
2ב. **Add to the D-36 row — from the copy evaluations** (`results\copy-summary.md` has both; the full JSON is `results\p3-copy-0-1.json` / `p3-copy-2-1.json`; every quote from there, verbatim): the year-ago comparison row has FOUR wordings across nine surfaces (list the four variants and the surfaces — a wording ruling, then one migration) · מ9's mode-0 chart narrative (seven lines, one per bucket, live numbers) — the mode-0 evaluator says the base is doing the guided layer's job (R27/R13, onboarding-layer-contract §3), the card asked for it — Ishay's call · date formats on the month axes differ per tab (*"2025-09"* on מ14 · *"09/2025"*-style on finance · *"ינואר 2026"* on customers — R30) · *"חלון קפוא"* on מ14 reuses a glossary word that means a price frozen at closing (ui-copy-styleguide §3ב/§3ד) · the no-tabs sentence *"אין דוחות זמינים בתפקידך — פני למנכ"ל"* addresses a role name (styleguide §1 says role names are data; its own counter-example is *"פנה למנכ"ל"*) — a locked sentence in the spec, so a question · the page-level hint on a drilled surface (מ3 · מ9) is the root's hint and describes a screen that is no longer there (H1) — per-level hint keys would be a contract change · *"דו"חות"* (module name, h1, sidebar) vs *"דוחות/דוח"* in body text · the six מ19/מ22/מ21 locked-label findings (the evaluators point at the 🔵 open item in §1.4 — do not duplicate, just pointer + the two labels not yet listed there: *"גודל "אחר" מול הקטגוריות"* (מ22) and *"נתפסים רק בקצב האישי"* (מ21)). **And from the E2E re-run:** five failures in OTHER modules' specs (`customer-page.spec.js:130` projects-tab counter 3 vs 6 rows · `dashboard.spec.js:82` · `finance.spec.js:141` · `prices.spec.js:218` copy drift *"ערך חוקי: מספר שלם בין 1 ל-150"* vs the test's *"מספר שלם חיובי"* · `projects.spec.js:235`) — deterministic on this tree, a baseline agent is measuring them on `origin/dev` in parallel; write them as **"טעון בדיקה — baseline on dev pending"** with the file:line, and the orchestrator fills the verdict.
3. **C8 note:** in `docs/micro_guides/module-11.md §2ב C8` add one line: `columns[].format` accepts `'id'` (identifiers: digits, no grouping, LTR-isolated) — and the shell also treats a key ending in `_id` as an id (`הנחתי`, shell fixer 17/09). Keep the section's heading structure (`check:docs-structure`).

## Gates (exit codes from logs)
`npm run check:docs-structure` · `npm run check:declared-counts` · `npm run check:iron-rules` · `npm run check:context` — all 0 · `perl -ne '$n+=tr/\r//; END{print "CR=$n\n"}'` = 0 on every file you touched.

## Output (final message = data, also `<your scratchpad>/results/p5-fix-doc.json`, written after EVERY correction)
`{ "files": [...], "corrections": [ { "item", "file:line", "from", "to", "count_changed": "<from ⇒ to, where>" } ], "recorded": [ "[n] …" ], "questions_for_ishay": [ { "item", "one_line", "evidence" } ], "gates": {...}, "not_done": [...], "not_verified": [...], "blind_spot": "<checkable>" }`

כל עובדה כאן ניתנת לערעור — אם מדדת אחרת, תקן אותי עם המדידה.
