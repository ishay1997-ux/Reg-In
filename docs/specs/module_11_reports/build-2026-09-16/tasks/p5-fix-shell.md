# P5-FIX-SHELL · one fix round on the shared shell after the evidence step (written 17/09/2026 00:1X by the orchestrator)

**Repo:** `C:\Users\ishay\Reg-In`, branch `ishay/module-11-build` @ `300e80dc`. You own the SHELL: `src/modules/11_reports/ReportsPage.jsx` · `src/modules/11_reports/components/**` · `src/modules/11_reports/api.js` · `src/lib/reportsExport.js` · `src/lib/reportsFormat.js` and their tests. **Never** touch `tabs/**`, migrations, docs, or `src/lib/**` beyond those two files. **Never commit** (the orchestrator commits by pathspec). Report in English; UI strings in Hebrew. A parallel RPC fixer edits only `supabase/migrations/` + `docs/schema.sql` + `docs/db_roadmap.md`; a parallel doc fixer edits only `docs/**`. No overlap by construction — if you believe you need a file outside your set, STOP and report it instead.

## Read first
`docs/specs/module_11_reports/build-2026-09-16/tasks/p3-common-addendum.md §2` (the shell's slots — what tabs rely on) · `src/modules/11_reports/CLAUDE.md` · `src/CLAUDE.md §3–§4` · the evidence findings you fix: `C:\Users\ishay\AppData\Local\Temp\claude\C--Users-ishay-Reg-In\ae84cb05-7ca4-4a75-80cd-6e6b13995904\scratchpad\results\anomalies-summary.md` — items **[1] [2] [3] [8] [9] [19] [26] [27] [28]** (each carries file:line evidence; the PNGs are beside it under `results\evidence\`). Then the code each item names.

## The fixes (each = code + a unit test that fails before and passes after + a why-first Hebrew comment per `src/CLAUDE.md`)
1. **[1] Export caption file name breaks across the bidi boundary** (`.xlsx` lands at the left of line 2). Render the file name as ONE isolated LTR run (`textLtr`/LRI…PDI from `reportsFormat.js`, or a `<bdi dir="ltr">` span — pick the one the existing textLtr fix uses) in `ExportBar.jsx` / `exportCaption()`. The locked prefix *"יירד:"* stays. Verify on the DOM that the isolate characters wrap the whole name.
2. **[2] Identifiers formatted as quantities** (`1,907` for a quote number in מ4; project numbers `1,416` in מ8). Add `FORMATTERS.id` in `reportsFormat.js` — plain digits, no grouping, LTR-isolated — and make `ReportTable` use it for a column that declares `format: 'id'` **or whose `key` ends in `_id`** (`quote_id` · `project_id` · `customer_id` · `hostess_id`). Tag the key rule `הנחתי` in your report (the contract C8 gains `format:'id'` for future RPCs — the doc fixer writes that). Do not touch the so-what prose (RPC).
3. **[3] + [8] + [28] The header line states a period or a customer the surface ignores.** Read the flags the RPCs already send: `meta.period_filter_ignored` (מ3 · מ4 · מ6 · מ22 · aging) and `meta.customer_filter_ignored` (all four hostesses surfaces; `grep -rn customer_filter_ignored src/` is 0 today). In `ReportsPage.jsx` (the line under the h1, `reports-window-label`, ~lines 240–262 + `formatWindowLabel`): when the LOADED payload says the period is ignored, show the payload's own window label (`payload.window.label`, e.g. *"כל הזמנים"* / *"נכון להיום"*) instead of the selected period, and mark the period pills as not applying to this surface (disabled with a `title`, or hidden — say which and why); when the customer filter is ignored, do not echo the customer's name in the label and disable the customer selector on that surface with the reason. The URL state must keep working for the surfaces that DO use the filters; `ReportsPage.test.jsx:646-648` asserts the customer echo — adjust that test to the new rule, do not delete it.
4. **[9] Empty-after-filter with an active customer filter shows a bare header row and no sentence** (מ21, `ReportSurface.jsx:~510`; the RPC keeps population/tiles unscoped, which is the RPC facet — not yours). Shell facet: when a customer filter is active and `rows.length === 0`, render the 📐10 state-2 `EmptyAfterFilter` envelope (with its clear-filters action) regardless of `population.n`. Keep D-34 ②'s rule for the no-filter case.
5. **[19] The export file name repeats the drill token twice** at drill levels (`מגמות-רב-שנתיות_2024-·-כל-הלקוחות_2024.xlsx`). Fix `buildExportFileName` in `reportsExport.js` so the drill label appears once (keep `fileNameSegment()` in `e2e/reports.spec.js` valid — it strips isolates and normalises separators; do not change the separator characters).
6. **[26] The masked-tab lock is a literal 🔒 emoji** (`ReportsPage.jsx:129-133`) — replace with a `lucide-react` `Lock` icon at `size-4`, `aria-hidden`, slate colour (`src/CLAUDE.md §4.3`). Keep the visible *"לא זמין בתפקידך"* text and `aria-disabled` untouched (E2E asserts them).
7. **[27] In the error state the window line collapses to a bare "כל הלקוחות"** because `serverToday` comes from the failed RPC (`ReportsPage.jsx:~247`). Either keep the last known `serverToday` across a failed reload or omit the dated part of the line while the surface is in the error state — say which; the envelope text itself stays exactly as is.

## Added 00:3X from the copy evaluations (`results\copy-summary.md`, `results\p3-copy-0-1.json`, `p3-copy-2-1.json`) and the E2E re-run (`results\p3-e2e-rerun.json`)
8. **[E1] E2E regression — `e2e/reports.spec.js:427` "מ9 בשורש: השורה כולה דלת אל כרטיס-הפרויקט (הכרעה 19)" fails deterministically (twice):** expected `/projects/1040`, got `/reports?…&drill={"id":1040,"kind":"project"}`. Cause measured by the E2E agent: `DRILL_INTENT` (commit `fb7bcd26`, after the spec) is passed by `drillHandlers()` (`ReportSurface.jsx:~271`) whenever the SURFACE has a further level, so `openDoor` (`ReportsPage.jsx:~348`) skips the navigate branch for a root row whose key is a door (`{id, kind:'project'}`). **Ruling 19 stands** (the root row of מ9 is a door to the project card; a chart bar drills — D-34 ①; the bucket-level row drills — level key without `id`). Fix: decide the intent PER ROW KEY, not per surface — a key that is a `ROW_DOOR_KINDS` kind WITH an `id` is a door; a key without `id` (or a level kind) drills. Acceptance: that test passes AND the two drill tests (`:357`, `:393`) and the chart-bar drill still pass — run `npx playwright test --config=playwright.e2e.config.js e2e/reports.spec.js > <log> 2>&1; echo exit=$?` (its own preview on 4173; ~4 min). Do not edit the spec.
9. **[C1] Filter labels end in a colon** (*"תקופה:"* · *"לקוח:"*, `FiltersBar.jsx`) — the approved mockup and `m11-copy-rules.md §2.5` (כ19 · R2) fix them without the colon. Remove the colons; adjust any test that asserts them.
10. **[C2] "1 ימים"** on מ9's table (`FORMATTERS.days` or the days column): the styleguide's plural rule (`docs/plans/ui-copy-styleguide.md`, כ12/UC37) — apply it for 1 (and 2 if the guide says so) in the formatter, with a unit test.
11. **[C9] Hint contradicts base on מ17 (fairness), mode-2 verdict "lost":** the tile/notes say the system HAS started recording the recommended rank (true since step 1.5, `assignments.recommended_rank`), while the hint `reports.fairness.*` says it does NOT record it yet. Fix the hint text in `src/lib/onboardingCopy.m11.hostesses.js` to agree with the base (you own that one file for this item only). **[C10]** the Gini tile explanation differs between מ14 and מ17 (same tile, two hint keys, two wordings — `copy-summary.md` cross_surface item 4): make them one sentence (keep מ17's, which adds "ולכן הוא מוצג תמיד ליד הערך של השנה שעברה"). Run the "every Hint id resolves" tests of the hostesses tab after.

## Gates you run before reporting (exit codes from log files, never piped to head/tail — a hook blocks it)
`npx vitest run src/modules/11_reports src/lib/reports*.test.js src/modules/09_settings > <your scratchpad>/vt.log 2>&1; echo exit=$?` (must stay 100 % green — 469 in the module + the m9 suite) · `npx eslint src/modules/11_reports src/lib/reportsExport.js src/lib/reportsFormat.js` · `npx prettier --check` on the same · `npm run dup` (jscpd 3 %) · `npm run check:bidi` · `perl -ne '$n+=tr/\r//; END{print "CR=$n\n"}' <each file you touched>` = 0. Then a LIVE check of items 1, 3 and 6 with the evidence runner: `cd /c/Users/ishay/Reg-In && npm run build > <your scratchpad>/build.log 2>&1; echo exit=$?` — 🔴 the preview server on port 5189 serves `dist/`, so a rebuild is what puts your change on screen; do not start or stop it — then `node "C:\Users\ishay\AppData\Local\Temp\claude\C--Users-ishay-Reg-In\ae84cb05-7ca4-4a75-80cd-6e6b13995904\scratchpad\evidence-run.mjs" --tab exec --identities CEO,STAFF --modes 2 --surfaces exec-overview,trends --extras drill,export --out <your scratchpad>/results-fix` and look at the PNGs (the export caption block, the masked pills as STAFF, the window label on מ3). Coordinate: the RPC fixer applies migrations to the live DB in parallel — a payload may change under you; that is expected.

## Output (final message = data, also `<your scratchpad>/results/p5-fix-shell.json`, written after EVERY fix, not at the end)
`{ "fixes": [ { "item": "[n]", "files": [...], "what_changed", "test_added", "live_check": "<png or 'not checked: why'>" } ], "assumed": [ { "what", "tag": "הנחתי", "source_checked" } ], "gates": { "vitest": "<totals + exit>", "eslint", "prettier", "dup", "bidi", "cr" }, "not_done": [ { "item", "why" } ], "not_verified": [...], "blind_spot": "<file · mechanism · scenario — checkable>" }`

כל עובדה כאן ניתנת לערעור — אם מדדת אחרת, תקן אותי עם המדידה.
