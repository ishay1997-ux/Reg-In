# P2-AV · Apply one tab's RPC migration, then verify it adversarially (read `p2-verify-lens.md` — its lenses and output shape apply; this file adds the apply step and the live-call method)

**Repo:** `C:\Users\ishay\Reg-In`, branch `ishay/module-11-build`. You edit no source file. **The one write you make is the migration apply** (Ishay's typed-echo waiver, `module-11.md §9 D-9`): after reading the tab's migration file in full, apply it through MCP `apply_migration` (name = the file's basename without the timestamp and `.sql`; `query` = the file content verbatim — read the file with the Read tool and paste it whole; do not retype). If the apply fails with a SQL error, **do not patch** — report the exact error and the line, and stop; the orchestrator dispatches a fix-forward.

## After the apply
1. `select proname, prosecdef, proconfig, proacl::text from pg_proc where proname like 'report_m%'` — the tab's four functions exist, `prosecdef`, `search_path=""`, no `anon`/PUBLIC in `proacl`.
2. **Call each function through a signed-in client** (temporary Node script in the scratchpad, `@supabase/supabase-js`, identities from `.env.local` `E2E_*`; delete it after; never print credentials): as an identity **allowed** on the owning module (per the matrix in `p3-common.md`: הנהלה/כספים ⇐ CEO or FINANCE; דיילות ⇐ CEO or RECRUIT; לקוחות ⇐ CEO or FINANCE) with the baseline window (`p_from`/`p_to` as the card's default; for aging pass the "today" the card uses if the function takes one) — save each payload to `<scratchpad>/results/payloads/<rpc>.json`; and as an identity **blocked** on the owning module (RECRUIT for כספים/לקוחות; FINANCE or STAFF for דיילות) — expect an error `42501`, **not** an empty object.
3. Then run the lenses of `p2-verify-lens.md` against the SAVED payloads plus your own independent queries: C8 keys all present · labels vs `spec.md §1.4` · population `n` vs your count · baseline numbers digit for digit · `so_what` action-first · `definitions` present · `meta.missing_params` behaviour (reason it from the SQL) · the rulings list of the tab file.
4. Drill (מ3 and מ9 only): call with `p_drill` for level 1 and level 2 (the keys the level-0 payload's rows expose as `drill_key`) and check `drill.crumbs`/`levels` and that tiles/rows change.
5. `p_customer_id`: one call with a real customer id — the payload shrinks coherently (or `meta.notes` says the surface is not customer-filterable).

## Output — the `p2-verify-lens.md` JSON plus: `"apply": { "migration", "result", "error": null|"…" }, "live_calls": [ { "rpc", "identity", "http_or_error", "n", "tiles": int, "rows": int } ], "payloads_saved": [...]`
