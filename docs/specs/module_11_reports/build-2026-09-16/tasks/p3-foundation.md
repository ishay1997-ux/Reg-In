# Task P3-F · Module 11 UI foundation — the shell (מ1), the first chart, the export mechanism, the API wrapper, the stubs (steps 3.0 · 3.0ב · 3.0ג, part of 3.5)

**Session type:** build. **Repo:** `C:\Users\ishay\Reg-In`, branch `ishay/module-11-build`. Several agents write to the same tree — write ONLY under the paths listed below. **Never commit.** Report in English; UI strings in Hebrew.

## 🔴 The one top mine
The report picker is a **horizontal chip row above the page** (`flex-wrap: wrap; width: 100%`) — **not** a 220px vertical rail. `design-contract §⑥` carried the rail until 11/09 and Ishay caught it by eye. Copy the skeleton from `design-contract §⑥` (the `html` block, lines ~624–1031) **and cross-check against `approved/02_tab_executive_approved.html`** (`nav.picker`, `section.page#p1..p4`). Before reporting, prove it: the rendered page at 1280px has the four role tabs on top, the chip row beneath them spanning the content width, and **no** second vertical sidebar — a screenshot in your report.

## Read first, in this order (MUST — reading floor before the first line of code)
1. `docs/micro_guides/module-11.md` — **`## 2ב. 🔒 Build conventions`** in full · §2.3 (files touched: the route already exists at `src/App.jsx` `path="reports"` wrapped in `<ProtectedRoute allow='דו"חות'>` around `UnderConstruction`; `Sidebar` needs nothing) · §4 (security & masking) · §6 **steps 3.0 · 3.0ב · 3.0ג · 3.5** in full.
2. `docs/specs/module_11_reports/design-contract.md` — §① palette · §② shared components · §③ RTL · §④א · **§⑤ the 12 chart principles (lines ~448–614) including every `לא אומת` line** · **§⑥ the skeleton + §⑥.1 the four pickers + §⑥.2** · §⑧ onboarding layer.
3. `docs/specs/module_11_reports/processes-approved.md` — **§📐 the 23 report rules (lines 772–810)** · **§🏷️ report names (743–770)** · **§🔒 locked vocabulary (688–700)** · card **ת4 export (571–579)** · card **ת8 permissions (608–617)** · **§📊 chart type per page (665–687)**. Long lines — read by range, to the end of each row.
4. `docs/specs/module_11_reports/spec.md` — §1.2 · §1.4 (the 19 page titles per tab — you render titles, tab builders render tiles) · §✅ · §🚫.4/.5/.7.
5. `docs/specs/module_11_reports/onboarding-layer-contract.md` — §3 (what is base vs hint) · §4ב · §5ב (LRI…PDI).
6. `docs/mockups/management-report-screen/approved/02_tab_executive_approved.html` **in full** (150KB — read by ranges): the live shell (`.side`, `.tabs`, `.picker`, `.filters`, `.stamp`, masked tab), the five envelope states, the chart-card markup. Skim `03_tab_finance_approved.html` for the masked-tab rendering and the `deferred` chips.
7. `docs/specs/module_11_reports/stage2-review/m11-charts-rtl-a11y.md` — before the first `<BarChart>`.
8. Repo patterns to COPY (open each): `src/modules/07_dashboard/DashboardPage.jsx` + `KpiStrip.jsx` (page shape · `PermissionAwareEmpty` · `LoadingOrError` · refresh via reloadTick) · `src/components/PermissionAwareEmpty.jsx` · `src/components/LoadingOrError.jsx` · `src/components/Hint.jsx` + `src/lib/onboardingCopy.js` (the `getOnboardingCopy` lookup and the file's header rules) · `src/contexts/AuthContext.jsx` (`useAuth().permissions[<module>]`) · `src/lib/apiError.js` · `src/modules/08_finance/api.js` (rpc wrapper style) · `src/lib/salaryReport.js` (`writeSalaryReportXlsx` — the `write-excel-file` call with `rightToLeft: true`) · `src/lib/listWindow.js` (URL-state helper and `resolveNext`) · `src/lib/dates.js` (Israel-time formatting) · `src/lib/constants.js` · `src/App.jsx` (the `reports` route) · `src/App.routes.test.jsx` (the structural guard your route must keep passing).
9. `docs/plans/ui-copy-styleguide.md` (approved style guide) + `docs/specs/module_11_reports/stage2-review/m11-copy-rules.md` §1 rules כ1–כ20 — for every NEW string you author (envelope states, export captions, breadcrumbs, filter labels). Locked strings are copied, not authored: masked tab *"לא זמין בתפקידך"* · export empty *"אין שורות לייצא"* · *"אין טבלה לייצוא בדף הזה"* · report 20 *"אין שורות לייצא — טרם אושרה ריצת-ניתוח"* · error *"נסי שוב"*.
10. `~/.claude/references/ishay-visual-taste.md` — controls hug their content · no over-information · real colours.
Probably NOT needed (open only on a real gap, and say so): the four `cards-*.md` (tab builders own them) · `processes-approved.md` outside the sections named.

## Tools
- Files: Read/Grep/Glob/Write/Edit/Bash. `npm run dev` may already be running on 5173 — check with `curl -s -o /dev/null -w "%{http_code}" http://localhost:5173` before starting one (Playwright's config starts/reuses it).
- Browser evidence: **Playwright** — write a TEMPORARY spec `e2e/_tmp_m11_foundation.spec.js` that logs in (copy `login()` from `e2e/permissions.spec.js`; identities from `process.env.E2E_*` — the config loads `.env.local`; **never print them**), visits `/reports` as CEO · FINANCE · RECRUIT · STAFF (=מנהלת לוגיסטיקה), asserts the shell, screenshots to the scratchpad folder `C:\Users\ishay\AppData\Local\Temp\claude\C--Users-ishay-Reg-In\719c2c51-1224-4dc9-87d1-19cd2ad34298\scratchpad\results\foundation\`, collects console errors and `document.documentElement.scrollWidth - clientWidth`. Run it with `npx playwright test e2e/_tmp_m11_foundation.spec.js --config=playwright.config.js`. **Delete the temp spec before you finish** (`e2e/CLAUDE.md` §4.3) and say so.
- 🚫 No DB writes. No `supabase.from` in components — all data through `api.js`.

## What to build (files you may write — and nothing outside them)
`src/modules/11_reports/**` (new) · `src/lib/reportsExport.js` + `.test.js` · `src/lib/reportsFormat.js` + `.test.js` · `src/lib/onboardingCopy.m11.exec.js` · `.m11.finance.js` · `.m11.hostesses.js` · `.m11.customers.js` (stubs) · `src/lib/onboardingCopy.js` (spread the four stubs in) · `src/App.jsx` (replace `UnderConstruction` with `ReportsPage` inside the existing guard) · `docs/specs/module_11_reports/design-contract.md` §⑤ (write back verification results, see step D) · `docs/micro_guides/module-11.md` (ONLY the `לא אומת` count sentence in step 3.0ב, see step D) · `e2e/_tmp_m11_foundation.spec.js` (temporary, deleted).

A. **`src/modules/11_reports/reportsCatalog.js`** — pure data: the four tabs (`הנהלה` · `כספים` · `דיילות` · `לקוחות`), each with its owning permission module (`'כספים'` · `'כספים'` · `'דיילות'` · `'לקוחות'`, ruling 2) and its **built** surfaces in order: id (מ2…מ22), `slug`, `rpc` name (`report_m<NN>_<slug>`, see §2ב C5), short name + question **verbatim from §🏷️**, `drill: true` for the four drill reports (📐13: מ3 · מ9 · מ17 and the deferred מ13 — so three built ones), `chartCap: 2`. Deferred surfaces are **not** in the catalog (they render nowhere).
B. **`src/modules/11_reports/api.js`** — `callReport(rpcName, { from, to, customerId, drill })` → `supabase.rpc(rpcName, { p_from, p_to, p_customer_id, p_drill })` → `toError` on error → returns the jsonb; plus `approveFeedbackAiRun(runId)` calling `approve_feedback_ai_run` and asserting `data.ok === true` (copy `callWriteRpc` style from module 8). Shape-guard: the object must have `population`, `tiles`, `rows`, `meta` keys (throw a Hebrew error otherwise — never render `undefined` as data).
C. **`src/modules/11_reports/ReportsPage.jsx`** (מ1 — the shell): `data-testid="reports-page"` · `<h1>` = the current report's short name, subtitle = its question (ruling 18) · four role tabs with `role="tablist"`/`role="tab"`, `aria-selected` always set, **masked** (not hidden) when `permissions[owning module]` ∉ `['edit','view']`: greyed name + 🔒 + visible *"לא זמין בתפקידך"*, not clickable · **chip row** (`nav.picker`, horizontal, wraps) of the tab's surfaces; the active chip is a pill (ruling 29) · **global filters bar** (`FiltersBar`: date range with presets + customer select — the four pickers of §⑥.1; the customer list comes from an existing customers API — find it, do not query `supabase.from` in the component) · the **five envelope states** (`Envelope` component: loading skeleton · empty · no-permission · error with *"נסי שוב"* · content) — copy the wording from §⑥'s skeleton and the mockup · **URL state** (`?tab=&report=&from=&to=&customer=&drill=`) via `useSearchParams` with a functional setter through `resolveNext` (`src/lib/listWindow.js` trap) · the date stamp line (📐17/📐22) · the tab content area renders the tab component from `tabs/` with props `{ surface, filters, drill, onDrill }`. Default tab = the first the user may open; every page loads at its root (drill state lives in the URL, 📐13④).
D. **`components/ChartCard.jsx`** (3.0ב — the first chart in the repo): `recharts` · `<div dir="ltr">` around `ResponsiveContainer` inside a parent with explicit height · Hebrew title + "אז מה" slot OUTSIDE the LTR wrapper · `role="figure"` + `aria-label` + an `sr-only` `<table>` twin of the data · custom tooltip with `dir="rtl"` · colours from §① (teal `#0F766E` primary; never hue-only distinction — label or pattern) · `accessibilityLayer` on · axis ticks from zero, no truncation (📐5) · reference-line support (📐6) · `onClick` cross-filter hook with keyboard equivalence. **Then verify in a real browser every `לא אומת` item of §⑤** (grep `לא אומת` in `design-contract.md` — there are ~10 lines): render ONE sample chart temporarily inside ReportsPage with static data, load it in Playwright at 1280px, and for each item record ✅/❌ with what you observed. **Write the results back into `design-contract.md` at the line itself** as `✅ אומת 16/09/2026: <what>` or `❌ נמדד 16/09/2026: <what> ⇒ <the workaround you built>` — annotate, never delete the original sentence. Then remove the sample chart. 🔴 `npm run check:declared-counts` compares the micro-guide's stated count of `לא אומת` behaviours against the literal count of lines containing `לא אומת` in `design-contract.md` — if your annotations change that count, update the one sentence in `docs/micro_guides/module-11.md` step 3.0ב accordingly, and run `npm run check:declared-counts` to prove it passes.
E. **`components/KpiTile.jsx`** (📐1 half-comparison: value · uncoloured ▲/▼ · the prior value · the window 📐3 · precision 📐4 via `reportsFormat`) · **`ReportTable.jsx`** (`scope="col"`, `aria-sort`, `aria-live` region 📐9 · numbers right-aligned · **whole row clickable** when a drill/target exists, ruling 19 · pager that counts what is shown 📐8 · sticky header) · **`DrillCrumbs.jsx`** (📐13: breadcrumbs, root first) · **`ReportChips.jsx`** · **`FiltersBar.jsx`** · **`Envelope.jsx`** · **`ExportBar.jsx`** (3.0ג).
F. **`src/lib/reportsExport.js`** (3.0ג): `exportReportRows({ fileName, sheetName, columns, rows })` → `write-excel-file` browser entry with **`rightToLeft: true`** (copy the exact call shape from `salaryReport.js`), typed cells, header row; `exportCaption({ fileName, columns })` returning the **two caption lines** shown under the filter row before the click (expected file name · `#xlsxCols` column names). Empty states: no rows ⇒ button disabled + *"אין שורות לייצא"* · no table ⇒ *"אין טבלה לייצוא בדף הזה"* · report 20 without an approved run ⇒ *"אין שורות לייצא — טרם אושרה ריצת-ניתוח"*. **Unit test** the caption builder and the row→cell mapping (mock `write-excel-file`).
G. **`src/lib/reportsFormat.js`**: `formatMoney` (`1,250 ₪`, no agorot), `formatPercent` (one decimal), `formatGini` (two decimals), `formatDelta` (sign before digits), `isolateLtr(text)` (wraps a run in `\u2066…\u2069`), `formatIsraelDate` — reuse `src/lib/dates.js` / existing formatters where they exist (grep for `₪` formatters first; **do not duplicate an existing helper** — jscpd + iron rule 14). Unit tests including a mixed Hebrew/number sentence.
H. **Tab stubs** `tabs/ExecutiveTab.jsx` · `FinanceTab.jsx` · `HostessesTab.jsx` · `CustomersTab.jsx`: each exports a component that renders `<Envelope state="empty">` with a neutral Hebrew sentence for now and is imported by `ReportsPage` (knip fails on unused files). Also `src/lib/onboardingCopy.m11.<tab>.js` ×4 exporting an empty object with a header comment naming its owner, spread into `onboardingCopy.js` (`...M11_EXEC_COPY` etc.).
I. **Tests:** `ReportsPage.test.jsx` (mock `@/supabaseClient`, `./api`, `recharts`): masking for the four permission maps (CEO all · FINANCE ⇒ הנהלה+כספים+לקוחות · RECRUIT ⇒ דיילות only · STAFF ⇒ all four masked ⇒ the page shows the no-permission envelope) · chip click changes `<h1>` and the subtitle · URL state round-trip · error envelope shows *"נסי שוב"* and retry re-calls the API · `ChartCard.test.jsx` renders the sr-only table. Keep `src/App.routes.test.jsx` green.

## Gates you run yourself before reporting
`npx vitest run src/modules/11_reports src/lib/reportsExport.test.js src/lib/reportsFormat.test.js src/App.routes.test.jsx` · `npx eslint src/modules/11_reports src/lib/reportsExport.js src/lib/reportsFormat.js src/App.jsx` (0 errors; cognitive complexity ≤ 20) · `npx prettier --check` on the same · `npm run dup` (jscpd) · `npm run deadcode` (knip — the stubs must not be dead) · `npm run check:declared-counts` · `grep -rc $'\r' src/modules/11_reports | grep -v ':0'` prints nothing. Paste the summary line of each.

## Output (final message = data)
- `files_written`, `route_change`, `catalog` (the 16 surfaces with slug/rpc/owner/drill).
- `chart_verification`: table of every `לא אומת` item → observed → written back where.
- `screenshots`: paths, per identity, with console-error count and horizontal-overflow value.
- `gates`: the summary lines.
- `strings_authored`: every NEW Hebrew string you wrote, with the copy rule it satisfies (locked strings are listed as "copied").
- `handoff_to_tab_builders`: the props contract of a tab component, the `callReport` contract, how to register copy keys, what a builder must NOT touch.
- `not_verified` · `blind_spot` · `assumed`. Tag claims `אומת-על-ידי` / `דווח-לי` / `הנחתי`.

כל עובדה כאן ניתנת לערעור — אם מדדת אחרת, תקן אותי עם המדידה.
