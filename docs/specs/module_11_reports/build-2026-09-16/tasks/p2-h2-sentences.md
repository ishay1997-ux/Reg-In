# P2-H2 · One cross-tab fix-forward migration for server-built sentences: `n=` inside the isolate · thousands separators · m14/m17 Gini clamp parity

**Repo:** `C:\Users\ishay\Reg-In`, branch `ishay/module-11-build`. **Never commit.** Report in English. **Your write scope:** ONE new migration `supabase/migrations/20260916<HHMMSS>_module11_h2_sentence_isolation.sql` (append-only), applied through MCP `apply_migration` (waiver `module-11.md §9 D-9`), and a temporary signed-in Node script in `<scratchpad>` (deleted after; never print credentials). **Functions you may touch:** `report_m07_finance_overview` · `report_m08_profitability` · `report_m09_aging` · `report_m12_equipment` (finance — nobody else writes them now; note m07 was changed by E3 at 08:5X, pull the LIVE body) · `report_m14_hostess_overview` · `report_m15_reliability` · `report_m17_fairness` (hostesses — F2 is final, no F3). **Do NOT touch** m02/m03/m04/m06 (a D2 agent owns them) or m19–m22 (a G2 agent) — they received the same rules by message.

## The three findings (hostesses re-verifier, `<scratchpad>/results/p2fix-hostesses.json` → `defects_found_outside_f3_gate` and `blind_spot`)
1. **D1 — `n=⁦106⁩` renders as "106=n" in a `dir=rtl` paragraph** (measured in Chromium via `Range.getBoundingClientRect`). Cause: the `=` sits outside the isolate between an L character and a neutral, so it takes the RTL level. **Fix:** `chr(8294) || 'n=' || <value> || chr(8297)` — the `n=` goes INSIDE the isolate. Sites named: m17 `population.label` + `tiles[0].compare.label` ("n=97 vs n=106 today"); m07 · m08 (also "(11)") · m09 · m12 `population.label`. Grep each live body for `'n='` and for `n=` adjacent to `chr(8294)`; fix every occurrence, including `definitions` and `meta.notes`.
2. **D3 — thousands separators inconsistent inside the hostesses tab:** m14 prints "1482 מתוך 1699 משמרות" and "n=1699"; m15 "1482 מתוך 1699" and "n=1683"; m17 uses `1,869`. C5 says numbers are formatted; the finance tab already fixed the same class (`module11_e2c_finance_m12_population_separators`). **Fix:** every 4+ digit COUNT in a server-built sentence (population.label, so_what, definitions, tiles[].sub, compare.label/note, meta.notes) goes through `to_char(x, 'FM999,999,999')` — never years, never ids (project ids print plain, per D-15).
3. **Blind spot — m14's Gini tile vs m17's Gini on a FUTURE window:** m17's fairness CTE clamps `a.event_date <= least(v_to, v_today)` (F2 file ~line 1425); m14's Gini block (~lines 400–408) may not. **Measure first:** call both with `{p_from:'2025-09-16', p_to:'2027-09-16'}` as CEO and compare `tiles[key='gini'].value`. If they differ, add the same clamp to m14 (the overview tile and the page its ruling-33 door opens must quote one number for one window — the identical failure shape as F2's red queue); if equal, change nothing and say so with the two values.

## Method
`docs/micro_guides/module-11.md` §2ב C5/C8 · §9 D-12 (CR idiom) · D-20 (rows per file accepted). Pull each LIVE body with `pg_get_functiondef` (E3 changed m07 at 08:5X — the E2 file is stale for m07); build the file programmatically (Python, `newline='\n'`), keeping signatures, `comment on function`, `revoke`, `grant` byte-identical (copy from the latest file for each function: E3 for m07, E2 for m08/m09/m12, F2 for m14/m15/m17). Apply per function in chunks < 60 KB (`module11_h2_m07`, …); header lists the rows and the three findings with their source. After apply: `md5(prosrc)` == the file's `$function$` body for all seven (`<scratchpad>/fn_md5.py`, adapt the glob to `h2`). Live re-verify as CEO: for each function scan every string field for `n=` followed by chr(8294) (must be 0), for 4+ digit runs outside years/ids (must be 0), and — the real proof — serve a probe page (`dir="rtl"`) with the new population labels and read the visual order via `Range.getBoundingClientRect` exactly as the re-verifier did: the reader must see `n=106`, not `106=n`. Numbers must not change (compare every `tiles[].value` before/after; only strings move) — except m14 Gini on the future window if finding 3 fires. FINANCE ⇒ 200 on the finance four, RECRUIT ⇒ 200 on the hostesses three, STAFF ⇒ `42501` on all seven.

## Output (final message = data, also `<scratchpad>/results/p2-h2-sentences.json`)
`{ "h2": { "file", "applied_as": [...], "md5_match": "7/7" }, "sites_fixed": [ { "rpc", "field", "before", "after" } ], "gini_parity": { "m14_future", "m17_future", "clamp_added": bool }, "values_unchanged": bool, "bidi_probe": "...", "payload_changes": [...], "not_verified", "blind_spot", "assumed" }`
