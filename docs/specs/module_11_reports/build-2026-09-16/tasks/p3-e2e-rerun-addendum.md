# P3-E2E · RE-RUN on the settled tree + two durable cases — ADDENDUM (written 16/09/2026 23:4X by the resuming orchestrator)

Read `docs/specs/module_11_reports/build-2026-09-16/tasks/p3-e2e.md` first (it is the build brief of the spec that already exists and is green — 26 tests in `e2e/reports.spec.js`, accessibility 8/8, smoke green, m4 32 green, measured 18:1X on commit `731969e7`), then THIS. Where they disagree, THIS wins. Source of this step: `HANDOFF-build-2026-09-16.md §0א` row 5 item ④.

## 1. What changed since the brief was written
- `e2e/reports.spec.js` EXISTS (26 tests) — do not rewrite it; you ADD two tests to it and one wait to `e2e/accessibility.spec.js`. **Files you may edit: those two only.** Never commit (the orchestrator commits by pathspec). Prettier + ESLint must stay clean on both (`npx prettier --check e2e/reports.spec.js e2e/accessibility.spec.js` · `npx eslint e2e/reports.spec.js e2e/accessibility.spec.js`) and CR must be 0: `perl -ne '$n+=tr/\r//; END{print "CR=$n\n"}' e2e/reports.spec.js` (the `grep -c $'\r'` idiom is broken in this Git Bash — micro-guide §9 D-12).
- **Onboarding mode 2 in the spec is already forced by the intercept** (`forceOnboardingMode`, `reports.spec.js:128`) — never by module 9's setter, never by a DB write (`e2e/CLAUDE.md §2.1`). The brief's journey 6 wording is superseded; keep the spec as it is.
- **Ports:** the orchestrator's frozen preview runs on **5189 — do not touch it**. `npm run test:e2e` builds and starts its own preview on **4173** (`playwright.e2e.config.js`, `reuseExistingServer:false`) — free now. `npm run smoke` wants a dev server on **5173** (`scripts/smoke.mjs`) — start `npm run dev` yourself in the background for that step and stop it after (record the PID; `npx kill-port` is not installed — use `Stop-Process` via PowerShell or `taskkill //PID <pid> //F` in Bash).
- **Traps that read as report bugs (handoff §6):** three active pg_cron jobs move the report populations overnight (01:00 · 01:30 · 02:00 — `module3-quote-expiry`, `module1-login-attempts-cleanup`, `module6-event-finished`); `trg_recompute_project_status` fires on every write to `assignments`/`logistics`/`projects`. **The reports spec writes nothing**; `smoke.spec.js` may. A number that moved is not a regression the code caused — say which population moved before calling anything a defect.
- `npm run test:e2e` `--grep-invert`s the smoke group — "E2E green" is not "smoke green"; run smoke separately.

## 2. The two durable cases to ADD (both in `e2e/reports.spec.js`, CEO, each its own `test`, fixtures chosen at runtime — `e2e/CLAUDE.md §2.2`)
**(a) Per-character bidi order on מ9 (aging, `finance`/`aging`) — the מדרג cell and the 📐18 contact/owner column.** Capture the real RPC payload with `page.route('**/rest/v1/rpc/report_m09_aging', route => route.fetch() …)` (fulfil unchanged; keep a copy of the JSON). Then, for the first table row: read the cell texts (`textContent`, then strip the isolates `\u2066`/`\u2067`/`\u2068`/`\u2069`) of the bucket/מדרג cell and of the owner/contact cell, and assert **character by character** that each equals the string the payload carries for that row (the row's bucket label and its owner/contact value — find the column keys from the payload's `columns[]`; do not hard-code names). Also assert that a cell containing digits AND Hebrew carries at least one isolate character in its raw `textContent` (`src/modules/11_reports/CLAUDE.md` invariant 5 — without isolation the ₪/digits fall to the wrong side and no gate catches it). If no row has a contact value, pick the first row that has one (runtime choice), and if none exists at all, `test.skip` with the reason — never hard-code a customer.
**(b) A chart-card wait before every axe scan of `/reports`** in `e2e/accessibility.spec.js`: before the scan of each reports route, wait for `[data-testid^="chart-card-"]` (first, visible, 30 s) — every default surface of the four tabs has at least one chart — so the scan runs on the rendered SVG, not on the loading skeleton (the "denominator 0" trap of `e2e/CLAUDE.md §3`). If the file scans routes through a shared helper, add the wait there guarded by the route (only `/reports*`).

## 3. Run (exit codes from the logs, never from memory; never pipe a run into head/tail — a hook blocks it)
1. `npm run test:e2e > <scratchpad>/results/e2e-full.log 2>&1; echo exit=$?` — the whole suite on the frozen 4173 preview (this is the re-run "on the settled tree"; expected ≈ 78 + 26 + 2 tests). Report the totals line and every failing test name.
2. `npm run dev` (background, port 5173) → `npm run smoke > <scratchpad>/results/smoke.log 2>&1; echo exit=$?` → stop the dev server.
3. `npx playwright test --config=playwright.e2e.config.js e2e/hostesses.spec.js e2e/smart-match.spec.js > <scratchpad>/results/e2e-m4.log 2>&1; echo exit=$?` — m4 regression (the `recommended_rank` write of step 1.5).
Flakes: rerun the failing file once, report both results (`e2e/CLAUDE.md §4.2`). Your scratchpad for logs: `C:\Users\ishay\AppData\Local\Temp\claude\C--Users-ishay-Reg-In\ae84cb05-7ca4-4a75-80cd-6e6b13995904\scratchpad\results\`.

## 4. Output
`…\scratchpad\results\p3-e2e-rerun.json` — `{ "files_edited": [...], "tests_added": [ { "file", "name", "what_it_asserts", "result" } ], "runs": { "test_e2e": { "exit", "totals", "failed": [...] }, "smoke": { "exit", "note" }, "m4": { "exit", "totals", "failed": [...] } }, "reruns": [...], "populations_moved": [...], "lint": { "eslint_exit", "prettier_exit", "cr": 0 }, "not_verified": [...], "blind_spot": "..." }` — **written incrementally after every run**, then returned as your structured output. `blind_spot` concrete and checkable.

כל עובדה כאן ניתנת לערעור — אם מדדת אחרת, תקן אותי עם המדידה.
