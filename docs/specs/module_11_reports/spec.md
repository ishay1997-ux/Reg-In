<div dir="rtl">

# מודול 11 · דו"חות מנהלים — האפיון המאושר

> **נכתב 11/09/2026 · תוצר שלב 3 של `module-discovery` · הקורא: הבלופרינט, ואחריו סשן-הבנייה.**
> 🔴 **חמישה דברים ולא יותר** — מצביע · המודול בשלוש שורות · **ארבעת פריטי-החוזה** · מה ייחשב עובד ·
> מה אסור לנחש. **הקובץ אינו משכפל שורה** ממה שהוא מצביע אליו.
> 🔑 **ומדד-האיכות אינו קריאוּת אלא ניתן-להפרכה: אם הבנייה יצאה נכונה — הוא הספיק.**
> **הצורה נלקחה מ-`module_04_hostesses/spec.md` ו-`module_06_projects/spec.md`** — שני האפיונים
> העשירים בריפו — ולא הומצאה כאן.

---

## ① 📚 רשימת-הקריאה של הבלופרינט — **קרא בסדר הזה, ואל תקרא יותר**

**בעברית פשוטה:** הסדר משנה. מי שפותח את המוקאפ לפני ההכרעות רואה מסך ולא מבין למה הוא כזה.
‏**רשימת-קריאה היא רצפה ולא תקרה** — קובץ שחסר לך כדי להחליט, פתח; וזה עצמו ממצא ששווה לדווח.

| # | הקובץ | מה לקחת ממנו |
|:-:|---|---|
| **1** | `processes-approved.md` §🗳️ | **‏39 ההכרעות המתוארכות.** הן גוף האפיון; כל השאר נגזר מהן |
| **2** | `processes-approved.md` §📑 · §📑ב | הגדרת כל דוח, ומיד אחריה **תיקוני-הביקורת הפר-דוחיים — חלק מההגדרה, לא הערה** |
| **3** | `processes-approved.md` §📐 | **‏23 כללי-הדוח החלים על כל משטח.** אינם חוזרים בכל שורה — קרא פעם אחת, הם חלים תמיד |
| **4** | `processes-approved.md` §📊 · §🏷️ · §🔒 | סוג-גרף פר-דף · שמות-הדוחות · המילון-הנעול |
| **5** | `screens-approved.md` | טבלת-המצב — **ה-SSOT היחיד לספירת-משטחים** |
| **6** | `stage2-cards/cards-*.md` | תשעת הסעיפים לכל משטח. **‏§③ *"מקור לכל מספר"* ו-§⑩ *"שכבת-ההטמעה"* הם מה שהבנייה מעתיקה** |
| **7** | `design-contract.md` | פלטה · רכיבים · RTL · חוזה-הגרפים · **§④א כללי-הניסוח** · §⑥ שלד · §⑧ שכבת-ההטמעה |
| **8** | `stage2-review/m11-copy-rules.md` | **‏20 כללי-ניסוח, לכל אחד מבחן.** 🔴 **§4 — *"מה לא שיניתי, ולמה"* — נקרא לפני שמנסחים מחדש** |
| **9** | `onboarding-layer-contract.md` | שני המצבים · חלוקת-הרמות · **§5ב: מספר בתוך רמז חייב המרת-בידוד** |
| **10** | `stage2-review/signoff-baseline-2026-09-10.md` | 🔴 **כל מספר עם ההגדרה שלפיה נמדד**, וגם אלה שיצאו שגויים. **אל תמדוד מחדש בלי לפתוח** |
| **11** | `docs/mockups/management-report-screen/approved/*.html` | **המראה.** ⚠️ ‏**16 משטחים = 4 קבצים** — קובץ ללשונית |
| **12** | `db_roadmap.md` §2 · **M11-2…M11-5** | ארבעת פריטי-ה-DB שהמודול חייב · ו-§6 האינדקס הפר-טבלאי |
| **13** | `PROJECT_MASTER §6` · `PROJECT_MASTER_sec7.md` | **‏10 שורות `🚧 מ11`** · **6 פריטי-§7** *(‏7.22 · 7.58 · 7.83 · 7.93 · 7.95 · 7.97 — כולם 🟢)* |

✏️ **וארבעה קבצים נוספו 11/09 אחרי חזרה-יבשה של ביצוע, שנעצרה בהם.** ‏🔑 **ושלושה מהם קוד** —
הרשימה הייתה כמעט כולה מסמכים, וזו הייתה החולשה:

| # | הקובץ | למה בלעדיו צעד אינו בר-ביצוע |
|:-:|---|---|
| **14** | `supabase/migrations/CLAUDE.md` §3 | **המקום היחיד שמסביר איך מייצרים את `schema.sql` מחדש.** אין סקריפט |
| **15** | `src/modules/04_hostesses/api.js` + `SmartMatchPage.jsx` | מסלול-הזימון ומקור-הדרג. **ת5 עצמו מורה *"למצוא את הכותב לפני שמחליטים"*** |
| **16** | `supabase/functions/send-email/index.ts` | התבנית שמנוע-הסיווג אמור לחקות. ⚠️ **היא ממסר-webhook בלי קריאת-LLM** — מה שחסר גדול ממה שקיים |
| **17** | `src/lib/paramsRegistry.js` + `schema.sql` §12 | ‏`params` **אין בה עמודות תקרה/רצפה** — הן חוקי-מרשם, והמרשם מצהיר שאינן נאכפות |

**🚫 ומה במפורש לא לקרוא** — ארבעה, כולם הוחלפו, **ואף אחד מהם אינו רשם חי**:
‏`docs/delete/management-report-screen-legacy/` *(מוקאפי-C5; הכרעה 8)* · ‏`drafts/` **כשקיים `approved/`** ·
‏`research/reports-architecture-draft-v3.1.md` *(טיוטת-ישי — הכרעות 39 ו-24 כבר עיבדו אותה)* ·
‏`stage0-sweeps/` · `stage1-review/`.
🔴 **ורשם חי לעולם אינו ברשימה הזו:** ‏`§6` · `§7` · `db_roadmap` · `schema.sql` **נקראים תמיד.**

---

## ② המודול בשלוש שורות

**מסך אחד — "דו"חות" — ובו ארבע לשוניות לפי תפקיד, ובכל לשונית מבט-על ועוד שלושה דפי-דוח: 16 משטחים.**
**הוא קריאה-בלבד:** אינו משנה נתון עסקי ואינו מחליף מסך קיים — הוא עונה על שאלות שמסך-התפעול לא נבנה
לענות עליהן *("את מי לגבות השבוע" · "את מי לא לשלוח" · "למי להתקשר החודש")*.
**הקוראת:** מנכ"ל רואה הכול; כל תפקיד אחר רואה את הלשונית שהמודול-הבעלים-על-הדאטה פתוח לו — **ממוסך,
לא מוסתר** *(הכרעה 2)*. **והצרכן השני, שאינו משתמש: שופטי הכנס ב-15/10/2026.**

---

# 🔒 פריט-חוזה 1 · אוצר-המילים הנעול

**בעברית פשוטה:** אותו דבר בשני שמות הוא הבאג שאף בדיקה לא תופסת. כאן נעול **מה שמופיע על המסך**,
מילה-במילה. ‏**SSOT מלא: `processes-approved.md` §🔒 ו-§🏷️** — כאן מה שהבנייה צריכה בהישג-יד.

## 1.1 תוויות-הסטטוס — **נלקחות מהקוד, לא מתורגמות מחדש**

‏`PROJECT_STATUS_LABELS`, `src/lib/projects.js:8–17`:

| הערך במסד | התווית על המסך |
|---|---|
| `event_finished` | **ממתין לסגירה** |
| `awaiting_invoice` | **ממתין לחשבונית** |
| `awaiting_payment` | **ממתין לתשלום** |
| `finished` | **פרויקט הסתיים** |

🔴 **וארבעתם יחד הם אוכלוסיית-הכסף** *(הכרעה 36)*. 🚫 **אין להציג את הערך האנגלי על המסך.**
📊 **נמדד 11/09: שני מוקאפים הציגו את הקודים באנגלית, ואחד תרגם מחדש — תוקנו.**

## 1.2 שמות-הדוחות — **השם בניווט · השאלה ככותרת-משנה** *(הכרעה 18)*

🚫 **אין מספר-דוח בממשק.** ‏**SSOT: §🏷️.** שמונה-עשר השמות + ארבעת מבטי-העל יושבים שם; הבנייה
מעתיקה משם ואינה גוזרת.

## 1.3 הישויות והמפתחות — **ואחד שקל לטעות בו**

🔴 **`assignments` אינו מפתח-זוג.** המפתח הוא **שלישייה** — `(project_id, hostess_id, assignment_number)`;
**‏797 כפילויות קיימות בזוג.** **שלוף מ-`pg_constraint`, אל תניח.**

## 1.4 תוויות-המסך — **מילה-במילה, חולצו מכנית מארבעת המוקאפים המאושרים** *(11/09/2026)*

🔑 **זו הטבלה שמונעת מהבנייה לגזור תווית מחדש.** ‏**‏79 תוויות-אריח + 19 כותרות-דף.**

| הלשונית | כותרות-הדפים | תוויות-האריחים |
|---|---|---|
| **הנהלה** | מבט-על הנהלה · מגמות רב-שנתיות · הנחות ורווחיות · קהל מול צוות | הכנסות מתחילת השנה · שולי-רווח גולמי · אירועים שהסתיימו · נתח 5 · שולי-רווח ללא הנחה · שולי-רווח בהנחה מעל 10% · הפרש בין הקצוות · שיעור אישור הצעות · יחס חציוני: אורחים לדיילת · אירועים מעל הצפי · אירועים ביחס מעל 50 · אירועים עם שני המספרים |
| **כספים** | מבט-על כספים · רווחיות פרויקטים · גיול חובות · תזרים צפוי · תוספות שטח · צריכת ציוד · שכר דיילות לפי חודש | יתרת-חוב פתוחה · צפוי להיכנס ב-30 · פרויקטים שחרגו מהתקציב החודש · שכר דיילות החודש · פרויקטים בתקופה · פרויקטים שחרגו מהתקציב · סך החריגה מעל הסף · שולי-רווח בתקופה · חציון-סטייה · חוב במדרג 61–90 · חלק מסך-החוב הפתוח · החשבונית הישנה ביותר · ימים לתשלום (חציון) · שוטף — עוד לא באיחור · חוזי ב-30 · כסף שעבר מועדו · עיריות — ימים לתשלום (חציון) · תוספות — ברוטו · צמצומים — ברוטו · השפעת-הכנסה נטו · שולי התוספות · עלות ציוד בתקופה · פער הוזמן מול הגיע · עלות ציוד — כל הזמנים · שורות ציוד במחיר מוערך · סך שכר הדיילות · סך הנסיעות · עלות-שעה ממוצעת · סך הבונוסים האישיים |
| **דיילות** | מבט-על דיילות · אמינות והתייצבות · איכות מול עלות · הוגנות השיבוץ | הגעה בזמן · דיילות אדומות · ריכוזיות המשמרות · דיילות פעילות · אירועים עם חוסר · איחור · אי-הגעה (הבריזה) · דיילות מסומנות · דיילות בלי דירוג · תעריף שעתי חציוני · יקרות ומדורגות נמוך · מסומנות "לא לשלוח" · רבע הדיילות העמוסות · אימוץ המלצת Smart Match · זמן-תגובה חציוני לזימון · זמן-תגובה, אחוזון 90 · אחוז היענות לזימון |
| **לקוחות** | מבט-על לקוחות · שביעות רצון · לקוחות מתרחקים · ניתוח הערות | שביעות-רצון מנבאת חזרה · הסיבה השלילית הגדולה אינה קטגוריה · הריכוזיות והנטישה עוד לא נפגשות · קצב-התשלום תלוי בסוג הלקוח · שיעור המרוצים (4–5) · שיעור המענה למשוב · ממוצע הציון (1–5) · משובים שסימנו סיבה שלילית · לקוחות מתרחקים · הכנסת 12 החודשים · נתפסים רק בקצב האישי · הוותיק שברשימה · הערות חופשיות שנכתבו · מהן שייכות למשוב שתויג "אחר" · גודל "אחר" מול הקטגוריות · דגלים אדומים |

⚠️ **ארבע התוויות של מבט-על-לקוחות הן משפטים ולא צירופי-שם** — חורגות מכלל **כ8**, ו-`cards-customers.md`
⑧19.1 · ⑧19.6 מסמנים אותן **🔵 פתוח להכרעת-ישי**. **נשארו כפי שהן; ההכרעה שלו.**

## 1.5 צורת-הפנייה — **ציווי אמיתי, בנקבה**

‏`בחרי` ×22 · `פתחי` ×16 · `סמני` ×11 · `לחצי` ×3 · `הזיני` ×3 **בקוד החי** ⇒ זו הצורה.
‏**`נקי מסננים` · `נקי בחירה` · `נקי חיפוש`** — ‏4/4 אתרי-הקריאה החיים בנקבה.
🚫 **`מתחילי` אינה צורת-ציווי** — הנכונה **`התחילי`**. *(תוקן ב-4 מוקאפים.)*
✅ ✏️ **תוקן 11/09/2026 — וכאן ישבה טענה שגויה שלי.** נכתב כאן *"`מתחילי` מופיע פעמיים בקוד החי"*;
**מדידה חוזרת: אפס מופעים ב-`src/` וב-`e2e/`.** שלושת ההיטים הם **`מתחילים`** — מילה תקינה — ו**כולם
בהערות-קוד**, לא במחרוזות-ממשק *(`quotes.js:629` · `QuoteSummaryPanel.jsx:16` · קובץ-בדיקות)*.
🔑 **השורש, והוא בדיוק המחלקה שהקובץ הזה מתריע עליה:** ‏`grep` על **תחילית** תפס מילה אחרת.
**מדדתי את השחזור שלי של החיפוש במקום את המילה.** ⇒ **אין כאן חוב.**

---

# ➡️ פריט-חוזה 2 · הסדר המוכרע

**בעברית פשוטה:** מה קורה לפני מה. זה מה שמאפשר לגזור מסך במקום להמציא אותו.

**‏(א) הדוחות אינם כותבים דבר — פרט לשני מסלולים, ושניהם גדורים:**
‏① **"הרץ ניתוח" ⇐ ואז "אשר להצגה"** *(ת2)* — **שני שלבים נפרדים**, ודף 20 מציג **רק ריצות מאושרות**.
‏② **מתג מצב-ההטמעה** — פר-משתמשת, ב"העדפות והתראות" של מ9, **לא במסך-הדוחות**.
**‏(ב) `recommended_rank` נכתב פעם אחת, בזמן-הזימון** *(ת5)* — ב-`insertInviteRow` ו-`writeInviteToken`
(`src/modules/04_hostesses/api.js`), **אינו נדרס בשליחה-חוזרת**, וכישלונו **אינו מפיל את הזימון**.
**‏(ג) פרמטר חסר אינו נופל לברירת-מחדל שקטה** — אומרים *"חסר פרמטר מערכת: X"* (דפוס מ7).
**‏(ד) כל דף נטען לשורש**, לא לרמת-קידוח; מצב-הקידוח חי בכתובת-הדף *(📐13 ④)*.

---

# 🔢 פריט-חוזה 3 · המספר בר-הבדיקה

🔴 **חושב ביד מההגדרה, לפני שקיים קוד — ואסור לגזור אותו מבדיקה שהבנייה תכתוב לעצמה.**
בדיקה שכתב מי שכתב את הנוסחה מקודדת את אותה שגיאה ועוברת בירוק.

## 3.1 המקרה — גבול-הדלי

‏`deriveDaysOverdue` (`src/lib/projectFinance.js:153–159`) מודדת מול **מועד-הפירעון**
= `invoice_sent_at` + `תנאי_תשלום_ימים`(30) — **לא מול יום השליחה** — ומחזירה 0 כשהתוצאה שלילית.

| נשלחה | מועד-פירעון | היום | ימי-איחור | **הדלי** |
|---|---|---|:-:|:-:|
| 11/05/2026 | 10/06/2026 | 10/09/2026 | **92** | **90+** |
| 03/06/2026 | 03/07/2026 | 10/09/2026 | **69** | **61–90** |
| 12/08/2026 | 11/09/2026 | 10/09/2026 | **0** *(נקטע)* | **שוטף** |

## 3.2 האגרגט — ממדידה בערוץ עצמאי *(SQL חי, `signoff-baseline-2026-09-10.md`)*

**‏35 חשבוניות פתוחות** · **שוטף 10 · 1–30 13 · 31–60 8 · 61–90 1 · 90+ 3**
**סכומים:** 68,585 · 87,574 · 56,839 · 5,438 · 17,946 ⇒ **236,382 ₪** · **מעל 60 יום = 4 חשבוניות · 23,384 ₪**

🔑 **ולמה דווקא המספר הזה:** ‏**חמש פעמים ביום אחד נמדד כאן דלי-גיול שגוי** — תמיד באותה דרך: מדידת
**שחזור** של ההגדרה במקום ההגדרה. **השורה השלישית בטבלה (‏−1 ⇒ 0 ⇒ "שוטף") היא בדיוק הגבול שנשבר.**
⇒ **הבנייה מוכיחה את ה-RPC מול המספרים האלה, לא מול בדיקה שכתבה בעצמה.**

---

# 🔗 פריט-חוזה 4 · השורה החוצה-מודולית

**כל שם-עמודה כאן אומת ב-11/09/2026**; מה שלא נמצא ב-`schema.sql` אומת **בשאילתה חיה** — ר' §🚫.1.

| מה מ11 קורא | מהיכן | עמודות |
|---|---|---|
| אוכלוסיית-הכסף | `projects` | `final_event_date` · `project_status` · `invoice_sent_at` · `payment_date` |
| רווחיות | `project_finance` | `final_profit` · `gross_profit` · `budget_deviation` · `written_off` |
| שעות ועלות-עבודה | `assignments` | `planned_hours` · `actual_hours` · `hourly_rate_snapshot` · `travel_amount` · `personal_bonus` |
| איוש ואמינות | `assignments` · `projects` | `assignment_status` · `invite_sent_at` · `responded_at` · `required_hostess_count` |
| קהל | `quotes` · `projects` | `estimated_guests` · `actual_guests` |
| משוב | `projects` | `feedback_score` · **`negative_feedback_reasons`** · **`positive_feedback_reasons`** *(מערכים)* |
| ציוד ותוספות | `logistics` · `project_changes` | `planned_qty` · `actual_qty` · `quote_service_line_id` · `delta_qty` · `unit_price_snapshot` |
| שכבת-ההטמעה | `notification_preferences` | `onboarding_mode` |

**ומה מ11 כותב — שלושה בלבד, כולם חדשים:** ‏`feedback_ai_runs` · `feedback_ai_insights` *(טבלאות)* ·
‏`assignments.recommended_rank` *(עמודה)*. **הפירוט המחייב: `db_roadmap.md` M11-2…M11-5.**

---

# ✅ מה ייחשב עובד

**בעברית פשוטה:** אין כאן יעדים מספריים — אין משתמשים אמיתיים, ומדד מומצא הוא בדיוק מה שישי יצטרך
להגן עליו בכנס. במקומו: **מה ייראה עובד, במילים.**

- **ארבע הלשוניות נפתחות לפי הרשאה**, וממוסכות — לא מוסתרות — למי שאין לו.
- **‏16 המשטחים מציגים את מספרי `signoff-baseline`**, וכל מספר מוצא את מקורו ב-§③ של כרטיסו.
- **הקידוח עובד בארבעת דוחות-הדריל** *(מגמות · גיול · שכר · ריכוזיות)*: פירורי-לחם · אריחים ושורת-הפעולה
  שיורדים עם הרמה · ייצוא של הרמה הנוכחית · והמצב בכתובת-הדף.
- **מתג-ההטמעה מהפך את כל 16 המשטחים חי**, בלי רענון, **וברמה 0 הדף שמיש במלואו** *(מבחן-המחיקה)*.
- **דף 20 אינו מציג סיווג שלא אושר** — בלי ריצה מאושרת הוא אומר *"טרם אושרה ריצת-ניתוח"*, לא דוח ריק.
- **ואת ארבעת סיפורי-הכנס אפשר לספר מול המסך** בלי שהמסך יסתור את המשפט.

---

# 🎯 מה הוכרע — ומה נשאר פתוח **בכוונה**

**‏39 הכרעות מתוארכות ב-§🗳️**; עמודי-התווך: **30** *(היקף: 16 משטחים)* · **36** *(אוכלוסיית-הכסף)* ·
**23** *(רף-האיכות והדריל)* · **28** *(שכבת-ההטמעה, שני מצבים)* · **35** *(מקדמי-אמינות 0.87 / 0.95)* ·
**18** *(שמות-הדוחות)* · **19** *(שורה לחיצה)* · **22** *(סוג-הדוח קובע את העדשה)*.

| הפתוח | למה הוא פתוח — **ואינו חור** |
|---|---|
| **⏳1 · דוח 11 ומנהלת-הלוגיסטיקה** | היא `blocked` על מודול 6, ולשונית-הכספים נפתחת לפיו *(הכרעה 2)*. ארבע דרכים, **כולן הכרעת-ישי** — §🗳️ 34 |
| **⏳2 · "זיכרון-תור"** *(מ9 · מ21)* | סימון "טופל" = **מסלול-כתיבה ראשון במודול קריאה-בלבד.** הרחבת-היקף |
| **⏳3 · מיון-עמודות בלחיצה** | חל על כל 22 הדפים; הכרעת-מוצר |
| **`recommended_rank`** | אין נתון עד שהעמודה תיווצר. **הדף מצהיר על ההיעדר** ואינו מציג `0%` — שהיה נקרא "אף פעם לא מאמצים" |
| **`m11-copy-rules §3.2`** | נעילת *"שכר דיילות"* במילון. נמדד 11/09 שהמונח הופיע בשלוש צורות; **ההמלצה שם, ההכרעה שלו** |
| **ספירות-דאטה ברמזי רמה-2** | ‏13 רמזים נושאים מספר; הייצור קבע *"אפס ספרות"* ועומד ב-0/37. **ההמלצה: להסיר. לא בוצע** |
| **ארבע תוויות מבט-על-לקוחות** | משפטים ולא צירופי-שם — חורגות מ-כ8, ומסומנות 🔵 בכרטיס |
| **בלוק *"מה עדיין פתוח"* במ10** | **תכליתו לשאול את ישי**, על מסך-מוצר. ‏`m11-copy-rules §4.4`: **הורדתו היא הכרעתו** |

---

# 🚫 מה אסור לבלופרינט לנחש — **במלואו**

**בעברית פשוטה:** בלי הסעיף הזה לשאר אין ערך. כאן יושבות **עובדות מדודות ששוברות בנייה בשקט** —
בלי שגיאה, בלי בדיקה אדומה, בלי שאיש ישים לב.

## 🔴 .1 `docs/schema.sql` אינו שלם — **ובדיקת-העמודות שהבלופרינט מחויב לה תפסול עמודה קיימת**

‏`module-blueprint/template.md` מורה לאמת כל שם-עמודה מול `docs/schema.sql`. **נמדד 11/09/2026:**

| העמודה | ב-`schema.sql` | במסד החי *(`information_schema`, קריאה-בלבד)* |
|---|:-:|:-:|
| `projects.negative_feedback_reasons` | ❌ **אינה** | ✅ **קיימת · `ARRAY`** |
| `projects.positive_feedback_reasons` | ❌ **אינה** | ✅ **קיימת · `ARRAY`** |
| `projects.positive_feedback_reason` | ❌ **אינה** | ✅ קיימת · `text` |

**המקור:** ‏`20260904230000_feedback_positive_and_negative_reasons.sql` · `20260904233000_feedback_multi_select_reasons.sql`.
**ה-snapshot מצהיר "רוענן 08/09/2026 22:5X"** וקלט את `onboarding_mode` מאותו יום — **אך לא את השלוש
מ-04/09** ⇒ **הריענון היה חלקי.**
🔴 **מה זה שובר:** דוחות 17 ו-20 קוראים **את המערכים**. בנייה שתאמת מול ה-snapshot תמצא "אין עמודה כזו",
**ואז תמציא שם אחר או תוותר על הפיצ'ר** — שתי הדרכים נכשלות בשקט.
✅ **מה לעשות:** ‏`supabase/migrations/` הוא מקור-האמת. **אמת עמודה חשודה בשאילתה חיה**, ורענן את
ה-snapshot בצעד-ה-DB הראשון *(DoD סעיף 3)*.

## 🔴 .2 טבלה עם RLS דלוק ואפס policies מחזירה **אפס שורות עם `error: null`**

**המסך משקר, לא נכשל.** ⇒ שתי הטבלאות החדשות **חייבות policy-קריאה באותה מיגרציה שיוצרת אותן.**

## 🔴 .3 `npm run gate` אינו מה ש-CI מריץ

‏`ci.yml` מריץ את השלבים בנפרד ואינו קורא ל-`gate` ⇒ ‏`check:docs-structure` **אינו רץ אוטומטית בשום מקום.**
➕ **וקובץ-בדיקות שנוגע בלקוח Supabase בזמן-ייבוא עובר מקומית ונופל ב-CI** — לרץ אין `.env.local`.
**השחזור:** `VITE_SUPABASE_URL= VITE_SUPABASE_ANON_KEY= npx vitest run`.

## 🔴 .4 מספר בתוך משפט-רמז אינו שורד את המעבר לייצור

‏`<Hint>` מרנדר **מחרוזת שטוחה** (`src/components/Hint.jsx:56–61`) ⇒ `<span class="ltr">` **נעלם**.
📊 **נמדד: 28 מספרים ב-13 רמזים, כולם מבודדים ב-markup בלבד.** ⇒ **ההמרה היא ל-LRI…PDI**
(`U+2066`…`U+2069`), כפי שכבר נעשה ב-`plainTextToEmailHtml`. **הפירוט: `onboarding-layer-contract §5ב`.**

## 🔴 .5 מפתח-הטמעה שגוי מרנדר `null` בשקט

‏`<Hint>` מחזיר `null` כשאין קופי, עם `console.warn` **בפיתוח בלבד** ⇒ **מעתיקים מפתח מטבלת-⑩, לא
מקלידים.** 📊 **‏70 המפתחות נמדדו 11/09 כתואמים 4/4 בין המוקאפים לכרטיסים.**

## 🔴 .6 המוקאפ נכון ליום שצויר בו — **וספירה נבדקת תמיד**

‏📐22: כל מוקאפ מצהיר את "היום" שלו *(ארבעת קובצי מ11: **10/09/2026**)*, ופער-תאריך אינו ממצא.
⚠️ **הסייג שבלעדיו הכלל מגן על באגים:** **מספר שנגזר מספירה או מסכום נבדק מול המסד תמיד.**
**המבחן:** האם הזזת "היום" קדימה בשבוע משנה אותו? **כן ⇒ תלוי-תאריך · לא ⇒ ספירה, וממצא לכל דבר.**

## .7 שלוש מלכודות-סביבה שעולות מיד

**‏`@/supabaseClient`** ולא `@/lib/supabaseClient` · **כתיבת קובץ מפייתון על Windows מזריקה CRLF בשקט**
ומפילה את `format:check` בעוד `git diff` נראה ריק *(המדידה: ספירת בתי-CR = 0)* · **שלושה קובצי-אפיון
מעל תקרת כלי-הקריאה** *(`processes-approved.md` 307KB · שני קובצי-`PROJECT_MASTER`)* ⇒ קריאה בטווחים.

---

## 📎 נספח — מה שלב 3 מצא ותיקן

> **למה כאן ולא ביומן:** כל שורה היא **קובץ שהבנייה קוראת**, שתוכנו השתנה ב-11/09.

| מה | הראיה |
|---|---|
| **‏20 כללי-הניסוח לא הגיעו לשלוש לשוניות** | ‏`m11-copy-rules.md` הוזכר **אפס פעמים** בארבעת הקבצים שסוכן-ציור קורא. חובר ל-`design-contract §④א`, והמעבר הורץ על 02 · 04 · 05 |
| **תוויות-סטטוס** | קודים באנגלית / תרגום-מחדש ⇒ יושרו ל-`PROJECT_STATUS_LABELS` |
| **`נקה` מול `נקי`** | ‏§4.2 ציטט **הערת-קוד** במקום מחרוזת ⇒ 17 מופעים בזכר. **‏4/4 אתרים חיים בנקבה.** תוקן; §4.2 סומן כמופרך |
| **`cards-customers` §⑩** | אמר לבנייה *"כסף שכבר נכנס"* — **ההפך מהכרעה 36.** יושר לנוסח המסך |
| **`cards-finance` §③** | ספירות של לפני-הזריעה *(11 מול 35)*; ודוגמת-הקידוח *(88 ימים)* הוחלפה בערך החי *(69)* |
| **`db_roadmap`** | בלוק **M11-2…M11-5** נכתב; שני מקדמי-אמינות היו **0.8/0.9** במקום **0.87/0.95** של הכרעה 35 |
| **`schema.sql`** | שלוש עמודות חסרות — ר' §🚫.1 |
| **סיפור-כנס ①** | *"156 משמרות, יש עם 0"* מול **48 · חציון 13.5 · n=106** שעל המסך; ‏156 נמדד על אוכלוסייה שנדחתה |
| **‏9 חסמים משתי עיניים טריות** | ז'רגון-בונים על מסכי-מוצר *(שם-פרמטר · `true` · מספרי-הכרעה · שם-שדה בעמודת מק"ט · סטטוס-בנייה)* · שני רמזים ששכפלו את רמה 0 · משפט שמדבר על המוקאפ · וסתירה בדף-הגיול על מה נמדד מול מה |

</div>
