<div dir="rtl">

# מודול 4 — כרטיסי התהליכים המאושרים

> **מה זה:** שני התהליכים של מודול 4 **כפי שישי אישר אותם ב-05/08/2026**, ברמת הפירוט שממנה
> בונים מסכים ומוקאפים. **זה אינו האפיון המלא** — האפיון (`spec.md`) נכתב בשלב ו ומקפל את זה
> לתוכו יחד עם כרטיסי-המסכים.
>
> 🔴 **למה הקובץ הזה נולד:** ‏06/08/2026, במדידת-מסירה שישי דרש, נמצא שכל **תוכן** התהליכים חי
> רק בצ'אט — ‏`discovery-log.md` תיעד **את ההכרעות ואת הנימוקים**, אבל לא את הזרימה עצמה.
> סשן טרי היה מקבל את "מה הוחלט" בלי "איך זה עובד". **מה שבצ'אט נמחק כשהסשן נסגר.**
>
> **קורא:** ישי (עברית) · והסשן שיבנה את המוקאפים.
> **ה"למה" מאחורי כל הכרעה כאן:** ‏`discovery-log.md` באותה תיקייה.

---

## ⚠️ מה הקובץ הזה **אינו** מכיל — קרא לפני שתסיק שמשהו נשכח

**‏① אין כאן כרטיסי-מסך.** מצבי טעינה/ריק/שגיאה, פריסה, סדר רכיבים, נוסחי-כפתורים —
**כל אלה הם שלב המסכים ועדיין לא נדונו.** היעדרם מכוון.
**‏② אין כאן את נוסחאות ה-Smart Match ואת מספריהן** — הן ב-`docs/module4_smart_match_research.md`
§11, **שהוא ה-SSOT שלהן**. כאן רק מה שנראה על המסך. ⚠️ **וה"למה" שמאחוריהן ב-§3 ו-§5–§6 של
אותו מסמך — חובה לקרוא אותם לפני שדנים במרכיב כלשהו.**
**‏③ אין כאן `spec.md`.** האפיון המלא נכתב בשלב ו, אחרי שכל המסכים אושרו.

## 📐 תשעת משטחי-ה-UI של המודול *(נמנו בשלב א; **אף אחד מהם לא הוסכם עדיין**)*

| # | המשטח | מקור | מוקאפ ישן |
|:-:|---|---|---|
| 1 | **מבט-על שיבוץ** — אירועים הממתינים לאיוש + חיווי כמותי | ‏§5.10 · C5 5.6.10 | `01.png` |
| 2 | **Smart Match לאירוע** | ‏§5.11 · C5 5.6.11 | `02.png` · `03.png` |
| 3 | **מאגר דיילות** — טבלה + סינון | ‏§5.12 · C5 5.6.12 | `04.png` |
| 3א | ↳ **שתי לשוניות**: "מעקב פניות ושיבוצים" / "מאגר דיילות" | C5 5.6.12 | — |
| 3ב | ↳ **פופ-אפ הוספת דיילת** | C5 5.6.12 | `05.png` |
| 3ג | ↳ **חלון עריכת דיילת** | מוקאפ | `06.png` |
| 3ד | ↳ **כרטיס דיילת לצפייה** — *"לחיצה על העין פותחת את הכרטיס המלא באותו מסך"* | C5 5.6.12 | — |
| 4 | **תפריט פעולות נפתח פר-שורה** ב-Smart Match | C5 5.6.11 | — |
| 5 | 🔴 **דף ציבורי לאישור/סירוב משמרת** — הדיילת עונה **בלי להתחבר** | ‏§7.45 | **אין** |

🔴 **משטח 5 הוא זה שטבלה הסופרת "מסכים" מפספסת** — אין לו פריט בסרגל-הצד, אין לו הרשאה,
והוא היחיד שמשתמש חיצוני רואה. **מעולם לא נדון, ואין לו מוקאפ.**

📌 **הצהרה שאושרה בשתיקה ולא תוקנה (מומלץ לאמת מחדש):** הלשונית **"מעקב פניות ושיבוצים"**
מציגה את **הזימונים שאנחנו שלחנו ומה קרה איתם** — **לא** פניות שהגיעו אלינו מבחוץ.
*(קלוד הצהיר, ישי לא תיקן. אין קליטת-מועמדות עצמית במודול.)*

## 0 · מי המשתמשת, ומה היא משיגה

**מנהלת הגיוס והשיבוץ.** לפי מטריצת ההרשאות (`PROJECT_MASTER §3`) היא **חסומה** מלקוחות,
מהצעות-מחיר, מלוגיסטיקה ומכספים — כל עולמה הוא **מאגר הדיילות** ו**איוש האירועים**.

**ההצלחה שלה נמדדת בשלילה:** אירוע לא יוצא לדרך עם חור באיוש, ודיילת לא מופיעה בשני מקומות
באותו יום.

**הרשאות המודול:** מנהלת גיוס ושיבוץ = **עריכה** · מנכ"ל = **עריכה** · מנהלת פרויקטים =
**צפייה** · מנהלת כספים ולקוחות = **חסום** · מנהלת לוגיסטיקה = **חסום**.

📌 **החלטת-ממשק שאושרה:** **פרטי בנק ות"ז לא מוצגים בטבלת המאגר** — רק בתוך כרטיס הדיילת,
ורק למי שיש לה הרשאת עריכה. ⚠️ **וזו הסתרה בממשק בלבד, לא הגנה** — ההגנה ברמת-המסד היא §7.63,
שהוכרע כ**נדחה למודולים 6/8**. **מגבלה מוצהרת, לא חור.**

---

## תהליך א׳ · ניהול מאגר הדיילות — ✅ אושר

**מטרה:** לבנות ולתחזק את רשימת האנשים שאפשר לשלוח לאירוע.
**מי מתחיל:** מנהלת הגיוס, **בלי קשר לאירוע כלשהו** — כשמישהי חדשה הגיעה, כשמישהי עדכנה
פרטים, או כשמישהי הודיעה שהיא לא זמינה.
**סדר גודל (ישי, 05/08):** **עד 50 דיילות במערכת.**

### א1 · הקמת דיילת חדשה

**הצהרה שאושרה:** כרטיס נפתח **רק אחרי שהמנהלת כבר דיברה עם הדיילת.** אין "מועמדת ממתינה
לאישור", ואין טופס שהדיילת ממלאת בעצמה.

**מסלול:** מסך "מאגר דיילות" ← **"הוספת דיילת חדשה"** ← חלון צף.

| השדה | ההתנהגות שאושרה |
|---|---|
| **ת"ז** | בדיקת **ספרת ביקורת תוך כדי הקלדה**. שגוי ⇒ *"מספר תעודת זהות אינו תקין"* מתחת לשדה + **"שמור" חסום** |
| **שם מלא · טלפון** | חובה |
| **אימייל** | כתובת קיימת ⇒ **אזהרה רכה ולא חסימה**: *"כתובת זו כבר רשומה אצל \<שם\> — להמשיך?"* *(§7.65: אין UNIQUE — תיבה משפחתית משותפת היא מקרה לגיטימי, והת"ז כבר מונעת כפילות-אדם)* |
| **עיר + כתובת מלאה** | בשמירה מומרת **פעם אחת** לקואורדינטות. נכשל ⇒ **הדיילת נשמרת בכל מקרה**, מסומנת **"אין קואורדינטות"**, ובחישוב הקרבה מקבלת ציון **ניטרלי — לא אפס** *(§7.55/§11.4: אפס מעניש על תקלת מערכת)* |
| **שכר שעתי** | מתחת לשכר-המינימום ⇒ **חסום**: *"השכר השעתי חייב להיות לפחות \<פרמטר\> ₪ (שכר מינימום)"*. הרף הוא פרמטר שנערך במודול 9 |
| **פרטי בנק** (שם · סניף · חשבון) | בדיקת מבנה |
| **דירוג 1–5** | ⚠️ **נשאר בטופס — אבל העמודה על המסך אינה נקראת "דירוג"**, כי "דירוג" נקרא כפסק-דין של המערכת בעוד שזו **התרשמות המנהלת**. **אינו רכיב בציון ההתאמה.** נשמר כי לדיילת חדשה אין שום היסטוריה, וזה הסיגנל היחיד עליה. 🔴 **והמוקאפים מציגים 4.9/4.7 — העמודה `int`, העשרוניות יורדות** |
| **יש רכב?** | בוליאני. מעל 40 ק"מ בלי רכב = **פסילה** (שער), לא מקדם בציון |

**בשמירה:** הכרטיס נוצר בסטטוס **"פעילה"**, והדיילת נכנסת **מיידית** למאגר המועמדות.

### א2 · עדכון שוטף

עריכת פרטים בכרטיס.
🔴 **מוקש שחייב להיאמר על המסך:** דיילת שכבר משובצת לאירוע עתידי — **התעריף של אותו שיבוץ
אינו משתנה.** הוא הוקפא ברגע השיבוץ (`hourly_rate_snapshot`). **הבטחנו לה תעריף במייל, ומייל
הוא הבטחה.** ההעלאה תחול על השיבוץ הבא.

### א3 · אי-זמינות מוצהרת 🆕 *(תוספת שאושרה 05/08)*

הדיילת מודיעה טלפונית ⇒ המנהלת מוסיפה בכרטיס **טווח תאריכים + הערה קצרה**.
מאותו רגע **Smart Match פשוט לא יציע אותה** לאירועים בטווח — **תנאי חמישי בשער.**

🔑 **זו כל הנקודה:** בלעדיה היא מקבלת ארבעה זימונים, מסרבת לארבעתם, **והמערכת רושמת אותה
כלא-אמינה** — כי שיעור-ההיענות הוא **40% מהציון**. עכשיו היא לא מסרבת; היא פשוט לא ברשימה.

**מי מזין:** המנהלת. **אין אפליקציה לדיילת** — זו הגרסה המוקטנת שנבחרה במכוון.

### א4 · השבתה *(§7.34, הוכרע 05/08)*

**"השבת"** ⇒ אם יש שיבוצים עתידיים פעילים, נפתח חלון **שמונה אותם בשמם ובתאריכם**:

> **נועה משובצת ל-1 אירוע עתידי: כנס לקוחות שנתי · 22/08/2026 · אושרה סופית**
> ▸ **"היא לא תגיע — שחרר אותה מהאירועים"** *(מומלץ)*
> ▸ **"היא תשלים את מה שהתחייבה אליו"** ⚠️

🔴 **שתי האפשרויות מנוסחות לפי כוונה, לא לפי מכניקה** — כי שתיהן לגיטימיות:
- **שחרור** ⇒ רץ תהליך-השחרור הקיים כולל מייל הביטול · הפרויקט חוזר מ"מוכן לביצוע" ל"בתהליך" ·
  ומסך המבט-על מראה שחסרה דיילת.
- **בלי שחרור** ⇒ **השיבוץ עומד במלואו**: היא נספרת באיוש, מקבלת את תזכורת ה-24 שעות, ותשולם.
  **השינוי היחיד: לא תקבל הזמנות חדשות.** *(המקרה השכיח: "אני עוזבת בסוף החודש אבל את הכנס
  ב-22/08 אני עושה".)* **האזהרה נועדה למקרה ההפוך** — אם היא לא מגיעה, האירוע ייראה מאויש
  ואיש לא ישים לב עד יום האירוע.

🚫 **אין מחיקה בשום מקום.** תג-סטטוס אמיתי, **הפיך משני הכיוונים מאותו כפתור**, וההיסטוריה
נשארת מחוברת. *(סטייה מודעת מ-C5 5.6.12 שמצייר אייקון פח-אשפה. ‏TempWorks ו-Ubeya עושים
בדיוק כך — היסטוריית העבודה היא הבסיס לתשלום ולדירוג של אחרות.)*

---

## תהליך ב׳ · שיבוץ דיילות לאירוע — ✅ אושר

**מטרה:** לאייש אירוע בלי חורים ובלי כפילויות.
**מי מתחיל:** מנהלת הגיוס, ממסך המבט-על, בלחיצה על אירוע.
**סדר גודל (ישי, 05/08):** **2–10 דיילות לאירוע, בדרך כלל 4–5.**

### ב1 · מה רץ ברקע לפני שהמסך נצבע — ארבע שכבות

**שכבה 1 · שער** (בינארי, לפני כל חישוב): פעילה · **אין לה שיבוץ פעיל באותו תאריך** ·
לא מסומנת "לא-לשלוח" **אצל לקוח האירוע הזה** · מרחק ≤ **80 ק"מ** · **ולא הצהירה אי-זמינות
בתאריך** 🆕.
**שכבה 2 · נעיצה:** מי שסומנה **"מצוינת" אצל הלקוח הזה** צפה לראש עם תגית גלויה.
⚠️ **ריקה עד מודול 6.**
**שכבה 3 · ציון:** שלושה מרכיבים. ⚠️ **מרכיב אמינות-ההגעה כבוי עד מודול 6** ⇒ המשקלים בפועל
**≈0.62 היענות · 0.38 קרבה**, **והמסך אומר זאת במפורש.**
**שכבה 4 · מיון:** ציון מעוגל לשתי ספרות · שובר-שוויון אקראי-אך-קבוע לאירוע.
🔴 **לעולם לא לפי סדר הקליטה למערכת** — זה יתרון-קבע לוותיקות.

📌 **המספרים המלאים והנוסחאות: `docs/module4_smart_match_research.md` §11.** ‏**ה"למה" שמאחוריהם:
§3 ו-§5–§6 באותו מסמך** — ‏🔴 **וחובה לשאת אותו לאפיון, כי בכנס ישאלו "למה", לא "מה".**

### ב2 · מה המנהלת רואה

**לוח בקרה עליון — ארבעה מונים:** `נדרשות · אושרו סופית · אישרו זמינות · ממתינות`.
**‏8 מועמדות גלויות**, השאר בגלילה.
🔴 **ועל כל כרטיס — לא ציון, אלא צ'יפי-הנמקה:**

> **`עבדה אצל \<לקוח\> 3×`** · **`אישרה 9 מ-10`** · **`2.5 ק"מ`**

**צ'יפים נוספים לפי מצב:** `טרם נצבר מידע` (פחות מ-3 תשובות) · `אין קואורדינטות` ·
**`לא ענתה ל-4 הזימונים האחרונים`** 🆕 · `עבדה לאחרונה לפני X שבועות` · תגית `יש רכב`.

**ארבע זוויות-מיון** *(מעבר לסדר-הציון עצמו)*: עבדה אצל הלקוח הזה · תענה הכי מהר ·
הקרובה ביותר · הזולה ביותר. **ברירת מחדל: "תענה הכי מהר" רק כשהאירוע קרוב מ-72 שעות, אחרת
"קרבה".** 🔴 **שכבות 1–3 רצות זהה בכל זווית — הזווית משנה רק את סדר המיון.**

### ב3 · שליחת זימונים

🔴 **המנהלת מסמנת תיבות ושולחת — בסבבים, ומחליטה בכל סבב מחדש כמה.**
**אין מספר שהמערכת מציעה ואין ברירת-מחדל** *(C5:311 — שיקול דעת אנושי טהור)*.

**המייל:** שם · אירוע · תאריך · שעות · מיקום · **תעריף + נסיעות** · **וקישור אישי**.
**הקישור פג במוקדם מבין שלושה:** המשרה אוישה · **48 שעות מהשליחה** · **24 שעות לפני האירוע**.

⚠️ **מוקש מדוד:** מנוע-המייל המשותף **חוסם היום את מנהלת הגיוס** (מקודד קשיח למודול
"הצעות מחיר"). **חוב מ3 רשום ב-§6 — הצעד הראשון של הבנייה.**

### ב3ב · ששת סטטוסי-השיבוץ — הרשימה הקנונית

*(נכתבה 06/08/2026 אחרי שבקרה חיצונית מצאה שהם פזורים בחמישה מקומות ⇒ סשן המסכים היה משחזר
אותם בניחוש. לכל אחד נדרש תג-סטטוס על המסך.)*

| ערך במסד | מי כותב אותו | מתי |
|---|---|---|
| `pending` | המערכת | ברגע שליחת הזימון |
| `confirmed_available` | **הדיילת** דרך הקישור · **או המנהלת** ידנית | מענה חיובי |
| `declined` | **הדיילת** דרך הקישור · **או המנהלת** *(גם כשדיילת חוזרת בה לפני האישור הסופי)* | מענה שלילי |
| `finally_approved` | **המנהלת** בלבד | אישור סופי — כולל "סוכם בטלפון" במצב 24-השעות |
| `released` | **המערכת** | המשרה התמלאה, או שחרור יזום בצמצום-תקנים |
| `approval_withdrawn` | **המנהלת** | הדיילת ביטלה **אחרי** אישור סופי |

➕ **ו"פג תוקף" אינו סטטוס** — הוא **נגזר**: `pending` **וגם** עברו 48 שעות מ-`invite_sent_at`.

🛑 **פתוח להכרעת ישי — התוויות בעברית.** האפיון הקפוא (C5 5.5.8) נוקב ב-**"אושרה זמנית"**,
והמסמכים שנכתבו בשיחה הזאת כתבו **"אישרה זמינות"** לכל אורכם. **שתי המילים אינן אותו דבר:**
*"אושרה"* מרמז שהמנהלת אישרה; *"אישרה זמינות"* אומר שהדיילת הודיעה שהיא פנויה — **וזה מה
שקורה בפועל.** ⚠️ **הפער לא היה מכוון** — הוא נוצר בכתיבה ולא הוכרע. **צריך להכריע תווית
אחת ולסמן סטייה מ-5.5.8 אם בוחרים בשנייה.**

### ב4 · המענה

**אישרה** ⇒ "אישרה זמינות" · **סירבה** ⇒ "סירבה", **והשורה לא נמחקת** (היסטוריה + קלט לציון) ·
**לא ענתה** ⇒ נשארת "ממתין" **ולא חוסמת דבר**.

🔴 **"פג תוקף" הוא מצב נגזר, לא סטטוס שביעי:** `ממתין` **וגם** עברו 48 שעות מהשליחה ⇒ מוצג
**"פג תוקף"**. **בלי מיגרציה ובלי לגעת בששת הסטטוסים.**
🔑 *"3 ממתינות"* אומר **"תני להן זמן"**; *"3 פג תוקפן"* אומר **"שלחי לעוד שלוש, עכשיו"** —
**אותו מספר, שתי פעולות הפוכות.**

**➕ "שלח שוב"** 🆕 — **כפתור ידני, לא טיימר.** פר-שורה, או "שלח שוב לכל מי שפג תוקפן".
מרענן את הטוקן ואת שעת-השליחה **על אותה שורה** — בלי שורה חדשה, בלי סטטוס חדש, **בלי השפעה
על הציון** (`ממתין` מחוץ למכנה). **אין מגבלה על מספר השליחות.**

🔴 **ואי-מענה נשאר מחוץ לציון — לא לשנות.** *(שתיקה אינה סירוב; והעולם ניסה להעניש
שיעור-קבלה ונסוג — DoorDash · Uber · Instacart.)* **הצ'יפ הוא שמראה למנהלת; הציון לא מעניש.**

**מלכודת שכבר פתורה:** המנהלת עוקפת סירוב אחרי שיחה ⇒ **נוצרת שורה שנייה והישנה נשארת**.
בחישוב ההיענות זה נספר **פעם אחת**, לפי השורה האחרונה — אחרת הדיילת נספרת גם כמסרבת וגם
כמאשרת, **ונענשת על ששינתה את דעתה לטובה.**

### ב5 · אישור סופי

עדכון ל**"אושרה סופית"**.
🔴 **המערכת מונעת אישור סופי לדיילת שכבר משובצת באותו תאריך — אילוץ במסד, לא בדיקה בקוד.**
גם שני מסלולים במקביל ייחסמו. *(הכרעת-ישי: "אסור שזה יקרה".)*

**➕ עודף אישורים = אזהרה, לא חסימה** 🆕. באישור מעבר למכסה:
> *"המכסה מלאה — 6 מתוך 6. לאשר את \<שם\> בכל זאת?"*
> *"ההצעה תומחרה ל-6 דיילות. דיילת נוספת היא עלות שהלקוח לא משלם עליה."*

והמונה נצבע: `אושרו סופית 7 · נדרשות 6 — אחת מעבר לנדרש`.
🔑 **הכלל: חוסמים את מה שאסור שיקרה (שיבוץ כפול = תשלום כפול). מזהירים על מה שעולה כסף
(שביעית היא לפעמים ביטוח לגיטימי).** *(וחסימה ממילא תיעקף בשלוש לחיצות.)*

**➕ "אחראית משמרת"** 🆕 — סימון על **אחת** מהמאושרות, **רק אחרי שיש אישור סופי**.
היא **איש הקשר בשטח** במייל של האחרות. **ביטלה** ⇒ הסימון משתחרר, המנהלת מסמנת אחרת;
**לא סימנה אף אחת** ⇒ איש הקשר חוזר להיות מנהלת הפרויקטים.

### ב6 · "שלח אישור לכל המאושרות"

מייל האישור הסופי עם הפרטים המלאים.
**כשהמכסה מלאה** ⇒ הפרויקט עובר ל**"מוכן לביצוע"** — **בטריגר במסד, לא בקוד המסך.**

**➕ שחרור אוטומטי:** מי שאישרה זמינות ולא נכנסה ⇒ **"שוחררה"**; לחיצה על הקישור שלה תציג
*"המשרה כבר אוישה"*. **"שוחררה" לא נספרת לרעתה בשום צד של הציון** — פעולת מערכת.
🆕 **ומודיעים לה — בהודעה קצרה משלה, לא במייל הביטול הקיים** *(שאומר "המשמרת שלך בוטלה",
וזה לא נכון — היא מעולם לא שובצה)*: *"תודה שהתפנית — המשרה כבר אוישה לאירוע הזה. נשמח לפנות
אלייך בפעם הבאה."*
🔑 **לא נימוס: שיעור-ההיענות הוא 40% מהציון.** מי שתענה "כן" שלוש פעמים ולא תשמע כלום — תפסיק
לענות, **והמערכת תרשום אותה כלא-אמינה.**

### ב7 · תזכורת, ומצב 24-השעות 🆕

תזכורת אוטומטית לפי הפרמטר `שעות_תזכורת_לדיילת` (=24 היום).

🔴 **הממצא:** הקישור פג **24 שעות לפני האירוע** ⇒ זימון שנשלח ב-T-20 **נולד מת.**
**בדיוק כשהיא הכי צריכה למלא חור, נתיב הקישור אינו קיים.**

**⇒ בתוך 24 השעות האחרונות המסך מחליף מצב:**
- הפעולה הראשית בשורה הופכת ל**"אושרה סופית — סוכם בטלפון"** *(לא יכולת חדשה: C5:313 כבר נותן
  למנהלת סמכות לעדכן ידנית — אנחנו מציפים אותה כשהיא הכלי היחיד)*.
- **הטלפון על הכרטיס הופך לפעולה.**
- **מייל האישור הסופי עדיין נשלח** אחרי שסגרה בטלפון.
- **לא הצליחה למלא** ⇒ הפרויקט **נשאר "בתהליך"**. **מסך שמסתיר חור גרוע מהחור.**

🔑 **ריצה מתוזמנת אחת, שני פלטים:** ‏T-24 הוא ממילא רגע מערכתי — **שולח תזכורות למאושרות
ומדליק התראה במבט-על על כל אירוע שעדיין חסר.**

**למה לא לבטל את פקיעת ה-24 שעות:** בשעות האלה המנהלת **חייבת לדעת מיד**, ומייל א-סינכרוני.
**הקישור נכון לזמן שיש בו זמן; הטלפון נכון כשאין.** *(סטייה מודעת מהעולם, שמשאיר הכל בערוץ אחד.)*

### ב8 · ארבעה מצבי כשל

| | מה קורה |
|---|---|
| **הלקוח מקטין** (6→4) | הנעילה נפתחת, חוזר ל"בתהליך". משחררת שתיים ← מייל ביטול ← נועל שוב |
| **הלקוח מגדיל** (6→8) | הנעילה נפתחת, ממשיכה לשבץ |
| **דיילת מבטלת אחרי אישור סופי** | סטטוס **"ביטלה אחרי אישור"**. הפרויקט חוזר ל"בתהליך" **אוטומטית**, המסך מראה את החור, והיא פותחת Smart Match שוב. **בציון: נספר במרכיב האמינות** (לא בהיענות — היא כן אמרה כן), **בערך 0.5** |
| **דיילת חוזרת בה לפני האישור הסופי** | מתקשרת, **המנהלת מעבירה ל"סירבה"**. אין מסלול-קישור, אין סטטוס חדש |
| **הלקוח מבטל את הפרויקט** | כל המאושרות **לא נספרות בשום צד** של האמינות. **הן לא אשמות** |

---

## מה נגזר מכל זה למסד — לרישום בבלופרינט

*(ה-SSOT לשינויי-DB הוא `docs/db_roadmap.md`; מה שנוסף היום נרשם שם. כאן הרשימה כדי שסשן
המוקאפים יידע מה קיים ומה עדיין לא.)*

**חדש מהיום:** טבלת **אי-זמינות** פר-דיילת (טווח תאריכים + הערה) · `assignments.responded_at` ·
סימון **אחראית משמרת** על שיבוץ · **‏`params`: שבעה ערכי-Smart-Match חדשים** (שלושת המשקלים
המעודכנים · שער 80 · גולפוסט 40 · ריסון 3 · חלון 12/24 · מינימום-3-תשובות) — **והשלושה הקיימים
היום שגויים** (`משקולת_1W_דירוג`=0.4 · `2W_קרבה`=0.3 · `3W_מהימנות`=0.3).

**כבר מוכרע וממתין לביצוע:** ת"ז → מפתח-סורוגייט (§7.64) · `hostesses.address/lat/lng` ·
`projects.lat/lng` · `has_car` · `invite_token`/`invite_sent_at` · שדות-נוכחות · `travel_amount` ·
טבלת-העדפה `(לקוח, דיילת, שלושה-מצבים)` · הרחבת ה-CHECK ל-6 סטטוסים · אילוץ-ייחודיות
חלקי `(דיילת, תאריך)` · **‏policies על ארבע הטבלאות — כולל `projects` שהיא deny-all.**

---

## קונבנציית המוקאפים *(אושרה 05/08)*

```
docs/mockups/hostesses-screen/
    approved/      ← המוקאפים החדשים שישי אישר — האמת
    superseded/    ← ששת ה-PNG המקוריים — רפרנס, הוחלפו
```
**"superseded" ולא "old"** — השם צריך לומר **למה** לא להסתמך.
⚠️ **ההעברה ל-`superseded/` נעשית בסוף, בפעולה אחת**, כי `PROJECT_MASTER §5.10–5.12`,
`mockup_descriptions.md` ומסמך-המחקר **מצטטים את הנתיבים הישנים**.

🔴 **הכרעת-ישי:** **כל משטח מקבל מוקאפ HTML חדש** — לא רק מה שהשתנה מהותית. סבב לכל אחד:
**בריף → אישור → HTML → תיקונים עד "זה זה".**

---

# ✅ חמישה פריטים שהיו מסומנים כפתוחים — **ארבעה נסגרו בהכרעת-קלוד, אחד בריכוז**

> **איך הרשימה נולדה:** בקרה חיצונית קראה את הקובץ **קר**, בלי להיות בשיחה, ומצאה ארבעה דברים
> שסשן טרי לא יוכל לענות עליהם. **קלוד אישר את ארבעתם ומצא חמישי.**
> 🔴 **הלקח: סשן אינו יכול לאשר את הקובץ של עצמו** — הקובץ עבר שתי בדיקות עצמיות והחזיק חמישה
> חורים; קורא זר מצא ארבעה מהם בקריאה אחת.
>
> 🔴 **ולקח שני, שנולד מיד אחריו:** קלוד סימן את כולם **"פתוח לישי"** — ‏**וירש את המסגור של
> המבקר במקום לשפוט כל פריט לגופו.** ישי עצר: *"הדברים האלו באמת פתוחים? אם אתה יכול לסגור
> עכשיו — יש אישור."* ‏**ארבעה מהם לא היו שאלות-מוצר אלא הכרעות-ממשק הפיכות**, וכולן בתחום
> שיקול-הדעת של קלוד לפי כלל-העבודה *"אל תשאל על החלטה טכנית הפיכה"*.
> ⚠️ **זו אותה מחלקת-טעות כמו שאלה שכבר נענתה — רק הפוכה: לייצר פתיחוּת שאינה קיימת.**
>
> **כל ההכרעות כאן: הפיכות · מנומקות · ולאישור ויזואלי במוקאפ.** ‏**מילה אחת מישי מבטלת כל אחת.**

| # | ההכרעה | הנימוק |
|:-:|---|---|
| ① | **עמודת הדירוג נקראת "התרשמות"** · בכרטיס: *"התרשמות המנהלת (1–5)"* | הוכרע שהיא **אינה** "דירוג" (זה נקרא כפסק-דין של המערכת בעוד שזו דעת המנהלת, והיא **אינה רכיב בציון**) — **אבל שם חלופי מעולם לא נבחר.** "התרשמות" היא מילה אחת, נקראת מיד כסובייקטיבית, ומשתלבת עם כוכבים |
| ② | **אין מונה חמישי — "ממתינות" מתפצל:** `ממתינות 5 · מתוכן 2 פג תוקפן`, **והחלק של פג-התוקף לחיץ ומוביל ל"שלח שוב לכולן"** | ‏§ב4 קובע ש*"3 ממתינות"* ו*"3 פג תוקפן"* הן **פעולות הפוכות** — לוח שמציג רק את הראשון מסתיר את המספר שההכרעה נבנתה סביבו. **פיצול שומר על לוח סרוק, ומחבר את המונה לפעולה** |
| ③ | ✅ **ששת הסטטוסים — רוכזו לרשימה קנונית** *(§ב3ב)* | לא נדרשה הכרעה, רק ריכוז. היו פזורים בחמישה סעיפים |
| ④ | **הצ'יפ "עבדה לאחרונה" נדלק ב-8 שבועות** | 🔑 **לא העדפה — עקביות פנימית.** מנוף-ההוגנות חסום ב-`LEAST(שבועות, 8)`; **סף אחר היה גורם למסך לספר סיפור שונה מהדירוג** |
| ⑤ | **התווית היא "אישרה זמינות"** — ‏**עם הערת "סטייה מ-C5 5.5.8" מתועדת** | האפיון הקפוא נוקב ב-*"אושרה זמנית"*, **והוא מתאר לא נכון מי מבצע את הפעולה**: הדיילת לוחצת על קישור ומודיעה שהיא פנויה — לא המנהלת שמאשרת. הפער נוצר בכתיבה ולא היה מכוון |

🔴 **⑤ הוא היחיד שנוגע באפיון הקפוא ⇒ הוא הראשון שכדאי לך לבחון כשתראה אותו על מסך.**
*(הסטייה נרשמת בתיעוד החי בלבד — לא נוגעים ב-`reference_spec/`.)*

---

---

# 🧱 טיוטת פרק 12 של האפיון — **"מה אסור לבלופרינט לנחש"**

> **למה כאן ולמה עכשיו:** זהו **הפרק החשוב ביותר באפיון** — כל מה שלא ייכתב בו, שלב הבנייה
> ימלא בשקט. **בניית הרשימה בסוף, מהזיכרון, היא בדיוק איך פריטים נושרים.** ⇒ **נצבר תוך כדי.**
> **סשן המסכים מוסיף לכאן פריט בכל פעם שהוא נתקל במשהו שאסור לנחש.**

**‏① מקור-החילוץ של בדיקת-הכיסוי חייב להיות האפיון הזה, לא C5.** ‏`module-blueprint/template.md`
מריץ מבקר שמחלץ את רשימת-הנדרש **מ-C5/C6 ומהמוקאפים** ⇒ הוא יסמן את שלושת מרכיבי-הציון
החדשים, את אי-הזמינות, את אחראית-המשמרת ואת שער-המרחק כ**"המצאות מעבר לאפיון"**, **וידרוש
את אייקון-המחיקה ש-C5 מצייר.** *(ישי מתאים את הסקיל בעצמו — זה רק חייב להיאמר.)*

**‏② `projects` היא deny-all — ומודול 4 חייב policy-קריאה עליה.** בלעדיה **המסך הראשון של
המודול חוזר ריק לכולם, כולל המנכ"ל, בלי שגיאה.** ‏(`🚧 מ4` ב-§6.)

**‏③ מנוע-המייל המשותף חוסם היום את מנהלת הגיוס** (מקודד קשיח למודול "הצעות מחיר") ⇒
**הצעד הראשון של הבנייה, לפני כל שליחה.**

**‏④ אין לקפל את נוסחאות §11 לתוך האפיון — מכפילים נימוק, לעולם לא מספר.**
‏§11 כבר תוקן פעמיים; עותק שני היה מפספס את שניהם.

**‏⑤ מרכיב-האמינות ונעיצת-"מצוינת" כבויים עד מודול 6** — **והמסך אומר זאת במפורש.**
‏**אסור לממש אותם כאפס שקט.**

**‏⑥ ששת הסטטוסים סגורים.** ‏"פג תוקף" **נגזר** ואינו שביעי. ‏**אסור להוסיף סטטוס.**

**‏⑦ הגנת בנק/ת"ז במודול 4 היא בממשק בלבד** — ‏§7.63 נדחה למ6/מ8. **מגבלה מוצהרת, לא חור
לסתום.**

**‏⑧ דוח השכר אינו מודול 4** — ‏`🚧 מ8 ← מ4` ב-§6 מפרט מה מ4 מספק ומה מ8 בונה.

*(ממתין: מה שיתגלה בשלבי המסכים והמוקאפים.)*

---

## 📌 שלושה דברים נוספים שנרשמים כדי שלא יאבדו *(לא שאלות — הקשר)*

1. **היקף שלב המוקאפים לא נמסר לישי במספרים לפני שהכריע "כל המוקאפים".** ‏**תשעה משטחים ×
   (בריף + סבבי-תיקון) ≈ 25–30 נקודות-עצירה** — הערכה שנמסרה לו בצ'אט **ולא קיבלה תגובה.**
   ⚠️ **לא הוכרע מחדש, ולא נדרש** — נרשם כי הכרעה שהתקבלה בלי המספר ראויה להיבדק שוב אם
   העומס יתחיל להעיק.
2. **הקובץ `docs/module4_smart_match_research.md` עדיין יושב מחוץ לתיקיית המודול.** ‏**נמדד
   06/08: 32 אזכורים ב-12 קבצים** *(שניים מהם אסור לגעת בהם — הארכיון ורשומות-יומן מתוארכות)*.
   **המלצת קלוד: להעביר — אבל בשלב ז, יחד עם שמירת `spec.md`**, כפעולה אחת; העברה עכשיו
   נוגעת בדיוק בקבצים שסשן המסכים עומד לכתוב אליהם.
3. 🔴 **ומה שאסור לעשות עם אותו קובץ: לא לקפל את הנוסחאות שלו לתוך האפיון.** ‏**מכפילים
   *נימוק*, לעולם לא *מספר*.** ‏§11 כבר תוקן פעמיים (‏`m=5`→`3` · מפתח-הדיילת) — עותק שני היה
   מפספס את שניהם, וזה בדיוק מה שקונבנציית ה-🔗 מראה קיימת בשבילה. ⇒ **פרק-האלגוריתם ב-`spec.md`
   נושא את ה"למה" בעברית ומצביע ל-§11 למספרים**, עם תג-מראה ו-SSOT מפורש.

</div>
