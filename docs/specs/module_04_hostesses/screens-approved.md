<div dir="rtl">

# מודול 4 — כרטיסי המסכים המאושרים

> **מה זה:** כרטיס לכל משטח-UI **כפי שישי אישר אותו**, ברמת הפירוט שממנה בונים מוקאפ ואז קוד.
> **מקביל ל-`processes-approved.md`** — שם התהליכים, כאן המסכים. *(נפרד כי זה ייגמר ארוך,
> וקובץ-התהליכים כבר עומד על מעל 450 שורות.)*
>
> **‏🔴 שמונה משטחים, לא תשעה** — משטחים 1 ו-3 הם **שתי לשוניות של עמוד אחד**
> *(נראה ב-`01.png` ו-`04.png`, ומוגדר בתוך `C5 §5.6.12`)*, ולכן `3א` אינו משטח נפרד.
>
## 📐 מבנה-הכרטיס — **מה כותבים ליד המוקאפ, ולמה רק את זה** *(נקבע 06/08/2026)*

> **השאלה של ישי:** *"צריך לפני מוקאפ שתכתוב על המסכים פירוט? או שהמוקאפ והתהליך מספיקים —
> שלא יהיה בלבול מה כפתור ומה מוביל למה?"*
>
> **התשובה: לא *לפני*, אלא *לצידו* — ורק את מה שתמונה אינה יכולה להראות.**
> 🔴 **"לפני" כבר נמדד ונכשל:** שני כרטיסים מילוליים ⇐ *"מודה שלא כל כך הבנתי"*.
> 🔴 **ו"מוקאפ בלבד" נכשל גם הוא, ובאותו יום:** ישי שאל *"זה לא כפתור נכון?"* —
> **התמונה לא יכלה לומר לו.**

**⇒ שישה סעיפים, וזה כל מה שנכתב. כל השאר — המוקאפ אומר טוב יותר.**

| # | הסעיף | למה תמונה לא מספיקה |
|:-:|---|---|
| **1** | 🔴 **מפת-לחיצות: מה לחיץ · מה זה עושה · לאן זה מוביל** *(טבלה, שורה לכל רכיב אינטראקטיבי)* | **זה בדיוק מה שישי שאל.** כפתור, קישור וטקסט-מודגש נראים דומה — **וההבדל בין "מבצע" ל"מנתב" הוא החלטת-מוצר** |
| **2** | **ההחלטה האחת + מי מקבל אותה** | תמונה מראה **מה יש**, לא **בשביל מה** |
| **3** | **מקור לכל מספר** — עמודה · חישוב · **וחלון-הזמן** | תמונה מראה `45`, לא *"אירועים ברבעון האחרון, מחושב מ-`assignments`"* |
| **4** | **מצבים שאינם על המסך** — טעינה · ריק-אמיתי מול ריק-אחרי-סינון · שגיאה · פעולה חסומה | **מוקאפ מצייר את המצב המוצלח בלבד** |
| **5** | **הרשאות** — מי רואה, מי עורך, ומה נעלם למי | לא ניתן לצייר |
| **6** | **הגדרות וסיווגים** — *"דחוף = 72 שעות"* · *"קריטי = חסר בתוך T-24"* | **מילה על המסך אינה מגלה את הכלל שמאחוריה** |

🚫 **ומה שלא נכתב יותר:** פריסה · סדר · צפיפות · צבע · דגשים — **המוקאפ הוא המקור, והוא גובר.**

### ✅ לפני שכרטיס עובר ל-✅ אושר — קריאה מלאה, לא grep *(הכרעת-ישי 07/08/2026, הרחבת-כלל)*

🔴 **חובה לפני כל אישור-סופי של כרטיס:** קריאה מחדש ומלאה של הפרק הרלוונטי ב-`processes-approved.md`
(לא סריקת-מילות-מפתח ולא הסתמכות על זיכרון-השיחה) והצלבה מפורשת מול הכרטיס הנבנה.
**זה לא כלל חדש — זו הכללה של לקח שכבר נכתב פעם אחת, נקודתית מדי:** שורה בסוף פרק "מצב" למעלה
אמרה את זה **רק לגבי 3ב/3ג/3ד** (שלושת המסכים הבאים בתור), כי זה בדיוק מה שהתגלה חסר במסך 3
אחרי שכבר אושר. **אבל מסכים 1/2/4 אושרו *לפני* שהלקח הזה נכתב** — ואף אחד לא חזר אליהם, ואז
נמצאו בהם **תשעה** פערים מאותה מחלקה בדיוק (סבב-ביקורת נפרד, 07/08/2026: ראה `discovery-log.md`).
⇒ **הכלל עצמו חייב לחול על כל כרטיס עתידי בכל מודול, לא רק על השלושה שהיו הבאים בתור כשהוא נכתב.**
עוגן-השיטה: `CLAUDE.md` בשורש, כלל-ברזל 1, "עוגן" — סתירה או פער אינם שאלה אלא סימן שלא נקרא
מספיק; קריאה מלאה היא הצעד הראשון, לא הכרעה חדשה.

### 🏷️ ➕ ושורה שביעית, בכל כרטיס ובכל מוקאפ *(הכרעת-ישי 06/08/2026)*

**‏🌱 מה נשאב מהמסד · 🎭 מה דמו מומצא · 📐 מה נגזר ממודל.**
🔴 **הנימוק, והוא כנסי:** ‏**`23 עברו את הסינון` אינו קישוט — הוא טענה על גודל המאגר**, ובכנס
ישאלו מאיפה הוא. **ודאטת-הדגמה יושבת בתוך מוקאפ מאושר שהוא חלק מהאפיון**, כלומר היא נכנסת
לבנייה כאילו נמדדה. *(הרישום המלא: `discovery_lessons.md §15`.)*
🚫 **ולא משנים את הדמו — מסמנים אותו.** ⚠️ **ומסמנים לפי בדיקה בקובץ ה-seed, לא לפי זיכרון:**
בכרטיס הראשון שנשא את השורה, **שניים מארבעה פרטים שסומנו "מהמסד" לא היו משם.**

🔴 **➕ וכל מוקאפ שמציג זמנים נוקב ב"היום" שלו, והתאריכים שבו חייבים להסתדר מולו.**
*(נקבע 06/08/2026 אחרי תקלה: האירוע הוצג כ-`22/08/2026 · בעוד 16 ימים` — כלומר "היום" הוא
‏**06/08** — **והזימונים היו מתוארכים אפריל.** ‏**והפרט ששבר בגלוי: זימון מ-`28/04` עם
"נותרו 31 שעות", בעוד הקישור חי 48 שעות בלבד.**)*
🔑 **והשורש, כי הוא יחזור: התקלה נולדה במוקאפ אחד והועתקה לשני.** ‏**הכלל "שואבים
מ-`src/` ולא מהמוקאפ הקודם" קיים בדיוק בשביל זה — אבל הוא נכתב על *צבע*, והדאטה
נשארה בלי כלל.**

### 🔴 ומה עם קלוד? — *"בסוף אתה כותב אפיון בעברית לקלוד, אז אולי הוא כן צריך תיאור מילולי"* *(ישי, 06/08)*

**שאלה נכונה, והתשובה נשענת על עובדה מבנית שקל לפספס:**
🔑 **המוקאפים שלנו הם HTML — כלומר קלוד *קורא* אותם.** ‏**‏PNG הוא תמונה; HTML הוא טקסט.**
⇒ **תוויות · סדר · עמודות · מצבים מצוירים · היררכיה — קלוד מחלץ אותם מהמארקאפ במדויק יותר
מכל פרוזה שנכתוב.** *(וזו בדיוק הסיבה שהכרעת "כל משטח מקבל HTML" הייתה נכונה מכפי שנראה אז.)*
⚠️ **ולכן זה תלוי-פורמט: אם מוקאפ יומר ל-PNG, החצי הזה נעלם. המוקאפים נשארים HTML ומקומטים.**

**⇒ ואת ה"סיפור" קלוד כבר מקבל — במקום אחר, וכדי שלא יוכפל:**

| מה קלוד צריך | מאיפה הוא מקבל את זה | למה לא במקום אחר |
|---|---|---|
| **הסיפור: מה קורה, בסדר, ומה נשמר** | **`processes-approved.md`** — כרטיסי התהליכים, מקצה לקצה | **סיפור פר-מסך היה משכפל את סיפור-התהליך**, וכפילות היא בדיוק מה שקונבנציית-המראה 🔗 קיימת למנוע |
| **המראה המדויק** | **המוקאפ (HTML)** | פרוזה תמיד תהיה פחות מדויקת מהמארקאפ |
| **מה שאף אחד מהם לא מראה** | **ששת הסעיפים כאן** | — |
| **מה אסור לנחש** | **פרק 12 של האפיון** | — |

**⇒ המסקנה: תיאור מילולי של המסך אינו נחוץ — לא לישי ולא לקלוד. הוא הדבר היחיד שאין לו קורא.**

### ⚖️ ומה גובר על מה — כי "המוקאפ גובר" לבדו אינו מספיק

**‏🔴 המוקאפ גובר על *מראה*: פריסה · סדר · צבע · נוסח-תווית · מה מודגש.**
**‏🔴 האפיון גובר על *התנהגות ונתונים*: מה קורה בלחיצה · מה נשמר · מאיפה כל מספר · הרשאות ·
מצבים · והגדרות.**
🛑 **וסתירה שאינה נופלת בבירור לאחד מהשניים — עוצרים ושואלים את ישי. לא מכריעים לבד.**
*(בלי השורה הזאת, "המוקאפ גובר" היה מרשה למוקאפ להכריע כלל-מוצר בשקט — וזה בדיוק
מה שקרה עם "התאמה 87%" במוקאפ הישן.)*

## מצב

| # | המשטח | מצב | מוקאפ |
|:-:|---|---|---|
| **3** | **מאגר דיילות** *(לשונית)* | ✅ **מוקאפ אושר 07/08/2026 15:25** (אחרי תיקון: חלון-אישור להשבתה עם שיבוץ עתידי + טווח-תאריכים לאי-זמינות, לפי `processes-approved.md §א3/§א4`) | `approved/03_repository_approved.html` |
| **1** | **מבט-על שיבוץ** *(לשונית)* | ✅ **אושר 06/08/2026** | `approved/01_overview_approved.html` |
| **2** | **Smart Match לאירוע** | ✅ **אושר 06/08/2026** | `approved/02_smartmatch_approved.html` |
| **4** | **תפריט פעולות פר-שורה** | ✅ **אושר 06/08/2026** | `approved/04_rowmenu_approved.html` |
| **3ב** | פופ-אפ הוספת דיילת | ✅ **אושר 07/08/2026** | `approved/06_add_hostess_approved.html` |
| **3ג** | חלון עריכת דיילת | ✅ **אושר 07/08/2026** | `approved/07_edit_hostess_approved.html` |
| **3ד** | כרטיס דיילת לצפייה | ✅ **אושר 07/08/2026** | `approved/08_view_hostess_approved.html` |
| **5** | 🔴 **דף ציבורי לאישור/סירוב משמרת** | ✅ **אושר 07/08/2026** *(המוקאפ הועבר ל-`approved/` ב-08/08 — ר' הערה למטה)* | `approved/05_public_confirm_approved.html` |

**‏✅ 8 מתוך 8 סגורים. כל משטחי מודול 4 אושרו — שלב-הדיסקברי של המסכים הושלם 07/08/2026.**

### ➡️ ממשיך מ־ — **עודכן 08/08/2026: שלב-המסכים סגור, והקובץ הזה אינו עוד נקודת-הכניסה**

✅ **שמונת המשטחים אושרו, ושלב 3 (אינטגרציה) הסתיים.** ‏**נכתב `spec.md`** באותה תיקייה, והוא
**נקודת-הכניסה של סשן-הבלופרינט** — בסעיף ① שלו יושבת רשימת-הקריאה המלאה.
🔴 **הקובץ הזה נשאר מקור-האמת לכרטיסי-המסכים** *(פרקים 2 ו-5 של האפיון)* — ‏`spec.md` **מצביע
אליו ואינו מחליף אותו.** **הצעד הבא במפת-העבודה: בלוק ④ — התאמת `module-blueprint`.**

**‏🆕 ותוספת אחת שנכנסה ל-`spec.md` ולא לכאן, כדי שלא תיעלם:** תגית-הנעיצה
**`מצוינת אצל הלקוח הזה`** (שכבה 2) — היא נדרשת ב-`processes-approved.md §ב1` *("צפה לראש **עם
תגית גלויה**")* ומופיעה במוקאפ, **ולא הופיעה באף כרטיס כאן** (נמדד: `grep "מצוינת"` על הקובץ
הזה = **0**). **נוסחה, מיקומה והתנהגותה נקבעו ב-`spec.md §1.5` בהכרעת-ישי 08/08/2026.**

*(הלקח שנשאר בתוקף לכל מודול הבא: **לפני שמציירים מסך — לקרוא את `processes-approved.md`
במלואו, לא רק את כרטיס-המסך.** נלמד 07/08/2026 בכאב, ומנוסח ככלל-קבע בסעיף 📐 בראש הקובץ.)*

---

# ✅ מסך 4 · תפריט פעולות פר-שורה — **אושר 06/08/2026**

> **🖼️ `docs/mockups/hostesses-screen/approved/04_rowmenu_approved.html`** — מצויר **פתוח על שש שורות
> בסטטוסים שונים**, כי זה הדבר האחד שהמשטח הזה צריך להגיד.
>
> 🔴 **וזה אינו מסך — זהו משטח צף על שורה בטור "דיילות באירוע" של מסך 2.** נספר כמשטח נפרד
> כי `C5 §5.6.11` מונה אותו כמרכיב, **ובעיקר כי התוכן שלו נגזר מהסטטוס ואינו קבוע.**

## ① 🔴 מפת-לחיצות

| הרכיב | מה זה עושה | שולח מייל? |
|---|---|:-:|
| **‏`⋯` בקצה השורה** | **פותח** — ותוכנו נקבע לפי סטטוס השורה | — |
| **"שלח את הקישור שוב"** | 🔴 **מבצע** — מרענן טוקן ו-`invite_sent_at` **על אותה שורה**. בלי שורה חדשה · בלי סטטוס חדש · בלי השפעה על הציון | ✅ |
| **"פתח זימון חדש"** | 🔴 **מבצע** — יוצר **שורה שנייה** (`assignment_number+1`); הישנה נשארת כהיסטוריה | ✅ |
| **"סמן: אישרה זמינות"** | **רושם בלבד** → `confirmed_available` | 🚫 |
| **"סמן: סירבה"** | **רושם בלבד** → `declined` | 🚫 |
| **"אשר סופית ושלח פרטים"** | 🔴 **מבצע** → `finally_approved` — **מוצג רק כששורה כבר "אישרה זמינות"** | ✅ |
| **"אושרה סופית — סוכם בטלפון"** | 🔴 **מבצע** → `finally_approved` — **מוצג רק בשורה `ממתינה`/`פג תוקף`** (אישור-ישיר בטלפון, בלי לחכות ל"אישרה זמינות"; ר' §⑧①) | ✅ |
| **"שחרר"** | 🔴 **מבצע** → `released` | ✅ |
| **"סמן: ביטלה אחרי אישור"** | **רושם בלבד** → `approval_withdrawn` | 🚫 |
| **"אחראית משמרת" / ביטול** | **רושם בלבד** — על אחת המאושרות | 🚫 |
| **מספר הטלפון** | **מחייג** — ובתוך T-24 הוא **פעולה ראשית ולא טקסט** | — |

🚫 **ואין בתפריט מחיקה, בשום סטטוס.**

📌 **תוקן 08/08/2026 (סבב-ביקורת 3.1):** שורת "אושרה סופית — סוכם בטלפון" הייתה קיימת במוקאפ (שלוש פעמים) ובפרוזה (§⑧①) אך חסרה מהטבלה הנעולה — נוספה כדי שהמפה תכסה בפועל את כל מה שלחיץ במוקאפ.

## ② ההחלטה האחת, ומי מקבל אותה

> **"מה קרה עם הזימון הזה — ואיך אני רושמת מידע שהגיע בטלפון ולא דרך הקישור."**

**מנהלת הגיוס.** 🔑 **וזו הסיבה שהמשטח קיים בכלל:** לדיילת **אין אפליקציה**, ולכן לכל מצב
שהקישור אמור לייצר **חייב להיות מסלול-מנהלת מקביל.** ‏**התפריט הוא הערוץ השני, לא קיצור-דרך.**

## ③ 🔴 שני הממצאים של המשטח

**‏① "שלח את הקישור שוב" ו"פתח זימון חדש" הן פעולות הפוכות בשם כמעט זהה.** הראשונה מרעננת
**אותה שורה**; השנייה יוצרת **שורה שנייה** ומשמרת את הסירוב כהיסטוריה. ⚠️ **ואילו שתיהן היו
נקראות "שלח שוב", המנהלת הייתה מוחקת היסטוריית-מענה בלי לדעת** — וההיענות היא **40% מהציון**.
⇒ **הניסוח מפריד ביניהן, וכל אחת מופיעה רק בסטטוס שלה.**

**‏② "שחרר" ו"ביטלה אחרי אישור" נראות זהות על המסך ואינן זהות בציון:**
**שחרור = אנחנו ויתרנו עליה** ⇒ **לא נספרת בשום צד** *(פעולת-מערכת, §7.33)* ·
**ביטלה = היא חזרה בה** ⇒ **נספרת במרכיב האמינות בערך `0.5`**.
⇒ **פריט-תפריט אחד לשתיהן היה מזייף את הציון של דיילת חפה-מפשע.**

## ④ מקור לכל מספר

**"נותרו 31 שעות"** — `invite_sent_at + 48h − now` · **"בעוד 19 שעות"** — `final_event_date − now` ·
**הסטטוס המוצג** — השורה עם `MAX(assignment_number)` פר-`(project_id, hostess_id)`, **לא השורה
האחרונה שנוצרה** · **"פג תוקף"** — נגזר: `pending` **וגם** עברו 48 שעות.

## ⑤ מצבים שאינם על המסך

**פעולה נכשלה** ⇒ **השורה חוזרת למצבה הקודם + הודעה מפורשת.** 🔴 **ולעולם לא "נשמר" בשקט** —
כתיבה שנחסמה ב-RLS מחזירה `{data:null, error:null}` *(‏`src/CLAUDE.md`)*, ותפריט שמדווח הצלחה
על כתיבה שלא קרתה הוא בדיוק המסך שמסתיר חור.
**פעולה חסומה** ⇒ **כפתור ה-`⋯` אינו קיים כלל** למי שאין לה עריכה — **לא כפתור מכובה.**
🔴 **ו"שלח את הקישור שוב" מכובה-ומנומק בשלושה מצבים, לא באחד** *(תוקן 06/08 — ר' §⑧③)*:
**‏① האירוע בתוך T-24** · **② האירוע כבר אויש** · **③ השורה אינה `pending`**.
*(אותו כלל של §11.4: מה שאין לו דאטה נאמר בקול, לא נעלם ולא מעמיד פנים.)*
**טעינה** ⇒ אין מצב-ביניים — התפריט נפתח על שורה שכבר נטענה.

🔧 **עודכן 07/08/2026 — שני מצבים ל"אשר סופית"/"אושרה סופית — סוכם בטלפון", חלים גם כאן
כי הפעולה קיימת בשני המסכים:**
**כפל-תאריך** ⇒ **חסום — אילוץ במסד** (`processes-approved.md §ב5`), אותו כלל-"פעולה נכשלה"
שלמעלה. **עודף-כמות** ⇒ **לא חסום, אזהרה** (*"המכסה מלאה — N מתוך N. לאשר בכל זאת?"*).
🔗 מראה — הפירוט המלא (הנוסח המדויק ב-`§①`, ולמה חסימה מול אזהרה ב-`§④`) בכרטיס **מסך 2**; לא
משוכפל כאן כדי לא ליצור שני מקורות-אמת לאותה הכרעה.

## ⑥ הרשאות

**מנהלת גיוס = עריכה · מנכ"ל = עריכה** ⇒ רואות `⋯`. **מנהלת פרויקטים = צפייה** ⇒ **אין `⋯`.**
**כספים · לקוחות · לוגיסטיקה = חסום** ⇒ אינן מגיעות למסך.

## ⑦ הגדרות שמאחורי מילה על המסך

🔴 **הקישור מת במוקדם מבין שלושה** *(`§ב3` · `PROJECT_MASTER §7.45`)*: **המשרה אוישה** ·
**48 שעות מהשליחה** · **24 שעות לפני האירוע**.
⚠️ **והתג "פג תוקף" מציג רק את השני** — הוא נגזר מ-`pending` **וגם** 48 שעות. **שני האחרים
הורגים את הקישור בלי לשנות שום סטטוס**, ולכן הם מצבי-מסך ולא תגיות.
**"ביטלה אחרי אישור"** = `0.5` במרכיב האמינות · **"שחררה"** = מחוץ לציון לגמרי.

## ⑧ שלוש הכרעות-קלוד, הפיכות — **אושרו ויזואלית 06/08**

**‏① "אושרה סופית" מופיעה גם על שורה שעדיין `pending`** — שיחת טלפון אחת יכולה להסתיים ב"אני
בפנים, סגור", **וחסימת-קפיצה הייתה מכריחה שתי לחיצות על אירוע אחד.**
⚠️ **והפריט נקרא "אושרה סופית — סוכם בטלפון" ונושא ✉, לא "סמן: אושרה סופית"** *(תוקן 06/08
כשישי ביקש לאמת את ההנחה)*: **הוא היחיד משלושת פריטי-הטלפון ששולח מייל** — פרטי האירוע יוצאים
לדיילת **בכל מסלול**. **"סמן" על פעולה ששולחת מייל הוא בדיוק השקר-במשתמע שמפת-הלחיצות
קיימת כדי למנוע**, והוא נכתב **בתוך הכרטיס שהמפה שלו כבר סימנה אותו נכון.**

**‏② לשורת `פג תוקף` אותם ארבעה פריטים כמו ל-`ממתינה` — רק ההדגשה משתנה.** "פג תוקף" אינו
סטטוס שביעי אלא מצב נגזר (48 שעות מהשליחה). 🔑 **ובניסוחו של ישי, שחד יותר משלי:
תפריט אחר היה *יוצר סטטוס חדש בדלת האחורית*.**

**‏③ 🔴 באירוע שכבר אויש, "שלח את הקישור שוב" מכובה — בדיוק כמו ב-T-24.**
*(נמצא בבקרת-היועץ, `4a95ed3`, ואומת מול המקור: **הכרטיס טיפל בשניים משלושת תנאי-הפקיעה**.)*
**מה שהיה קורה בלעדיו:** שורה `ממתינה` באירוע מלא נושאת **קישור מת**, והתפריט מציע לרענן
אותו ⇒ **המייל נשלח, הדיילת לוחצת, ומקבלת "המשרה כבר אוישה"** — *בדיוק התקלה ש-T-24 יצר
ושהכרטיס כבר תיקן, לתנאי אחד מתוך שלושה.*
🔑 **ומה שזה *אינו* מבטל — "עודף-אישורים = אזהרה ולא חסימה" עומד במלואו:** המנהלת עדיין
יכולה לצרף שביעית כגיבוי, **דרך הטלפון** — `אושרה סופית — סוכם בטלפון` חי בתפריט בכל מצב.
⇒ **מה שנסגר הוא ערוץ-הקישור בלבד, לא היכולת.** *(ולכן אין כאן פתיחה-מחדש של §7.45
ולא של הכרעת-העודף — רק המסך מפסיק להציע פעולה שתיכשל.)*

## 🏷️ דאטת-הדגמה מול נגזר-ממודל

**‏🌱 מהמסד החי — יושב ב-`scripts/demo-seed.mjs` (נמדד בקובץ, לא הונח):** ‏`כנס לקוחות שנתי` ·
הלקוח ‏`מדיטק פתרונות בע"מ` · ‏`אקספו תל אביב, ביתן 2` · ‏`18:00–22:00` · **6 דיילות** (`04ST`).
**‏🎭 דמו מומצא:** שמות הדיילות · הטלפונים · שעות השליחה והמענה — **המאגר החי ריק (0 שורות)** ·
⚠️ **וגם התאריך `22/08/2026`: ה-seed שותל `היום + 24 יום`, לא תאריך קבוע** · **וגם `מדיטק בע"מ`**,
שקוצר מ-`מדיטק פתרונות בע"מ`.
**‏📐 נגזר ממודל:** ששת הסטטוסים ומפת-הפעולות · חלון 48 השעות · מצב T-24 · הערך `0.5`.

> 🔴 **והשורה הזאת כבר החזירה ערך בתור שבו נולדה:** הגרסה הראשונה שלה כתבה *"נשאב מהמסד: כנס
> לקוחות שנתי · מדיטק · 22/08/2026 · אקספו ת"א"* — **ובבדיקה בקובץ ה-seed התברר ששניים מארבעת
> הפרטים אינם משם.** *(‏`git grep` על `אלביט` החזיר **אפס** בכל `scripts/`+`src/`+`supabase/` —
> כלומר הוא מומצא לגמרי, בעוד `מדיטק` **כן** שתול. הרישום ב-`discovery_lessons.md §15` שאיחד
> את שניהם כ"מומצאים" מדויק לגבי אחד ושגוי לגבי השני.)*

🛑 **עצירה — מסך 4 מתוך 8.**

---

# ✅ מסך 2 · שיבוץ חכם (Smart Match) — אושר 06/08/2026

> **🖼️ `docs/mockups/hostesses-screen/approved/02_smartmatch_approved.html`** — **הוא חלק מהאפיון.**
> **ישי:** *"המוקאפ מעולה"* · *"נראה מעולה"* ⇒ אושר אחרי שני תיקונים: **פחות צבע** *(רקעי-שורות
> ירדו; צ'יפ שאומר עובדה טובה אינו צבוע)* **וכפתור "שלח שוב" יושר לשאר.**

## ① 🔴 מפת-לחיצות

| הרכיב | מה זה עושה |
|---|---|
| **"← חזרה למבט-על"** | **מנתב** — חזרה למסך 1 |
| **תיבת-סימון** על מועמדת | **בוחרת** — ומעדכנת את המונה בכפתור |
| **"שלח מייל תיאום (N נבחרו)"** | 🔴 **מבצע** — יוצר שורות זימון עם **קישור אישי** לכל אחת |
| **"שלח שוב"** *(פר-שורה)* | 🔴 **מבצע** — מרענן טוקן ו-`invite_sent_at` **על אותה שורה**. בלי שורה חדשה · בלי סטטוס חדש · **בלי השפעה על הציון** |
| **"שלח אישור סופי לכל מי שאישרה זמינות (N)"** | 🔴 **מבצע** — מעביר ל-`finally_approved` ושולח את פרטי האירוע. **מעבר למכסה: לא חוסם, מזהיר** — *"המכסה מלאה — X מתוך X. לאשר בכל זאת?"* + *"ההצעה תומחרה ל-X דיילות"* (כבר במוקאפ, `02_smartmatch_approved.html:174-175`; עוגן: `discovery-log.md` תהליך ב׳ #2, מצוטט מישי) 🔧 *(נוסף 07/08/2026)* |
| **ארבע הזוויות** | **מסדרות בלבד — אינן מסננות.** שכבות 1–3 רצות זהה בכל זווית |
| **"2 פג תוקפן"** בכותרת | **מנווט** — מגלגל לשורות הרלוונטיות |
| **★ אחראית משמרת** | **מבצע** — סימון על **אחת** המאושרות, **רק אחרי שיש אישור סופי** |

## ② ההחלטה האחת
**"למי לשלוח עכשיו — ואת מי לאשר סופית."** ‏*(שתי החלטות במסך אחד, וזה מכוון: הן חולקות את
אותו מידע. הטור הימני = מי כבר בפנים; השמאלי = מי המועמדות.)*

## ③ מקור לכל מספר
‏**4 המונים** — ספירת `assignments` **לפי השורה האחרונה פר-דיילת** (`MAX(assignment_number)`) ·
**`8 מתוך 23`** — 8 גלויות *(הכרעת-ישי)*, ו-23 = **מי שעברה את שכבת-השער** ·
**הצ'יפים** — `עבדה אצל X ‏3×` מ-`assignments ⋈ projects.customer_id` · `אישרה 9 מ-10` = מונה/מכנה
**גולמיים** *(הציון המרוסן עצמו נשאר סמוי)* · **מרחק** = haversine · **`טרם נצבר מידע`** = פחות
מ-3 תשובות בחלון. ➕ **`יש רכב`** — `hostesses.has_car` *(רשום ב-`db_roadmap.md`)* ·
**`אין קואורדינטות`** — גאוקוד נכשל, ר' `hostesses.address/lat/lng` · **`לא ענתה ל-N הזימונים
האחרונים`** — `params.לא_ענתה_ל_N` *(ברירת-מחדל 4)* · **`עבדה לאחרונה לפני X שבועות`** —
`max(projects.final_event_date)` על שיבוצים שהושלמו. *(תוקן 08/08/2026, סבב-ביקורת 3.1 —
ארבעת אלה היו מוצגים במוקאפ בלי מקור רשום כאן.)*

🔧 **עודכן 07/08/2026 — שתי תוספות, עוגן `processes-approved.md §🔟`/`§⑦`:**
**① שתי משפחות-צ'יפים, ולא לערבב:** ✅ **בציון** — `אישרה 9 מ-10` (היענות, `0.40`) ו-`2.5 ק"מ`
(קרבה, `0.25`) בלבד. 🔴 **לא בציון** — `עבדה אצל X` (הקשר-תפעולי) · `יש רכב` (שער, לא מקדם) ·
`לא ענתה ל-N` (מוצג למנהלת, לא מעניש) · `טרם נצבר מידע`/`אין קואורדינטות` (סימוני-חוסר).
✅ **תוקן במוקאפ 07/08/2026** — `02_smartmatch_approved.html` היה מציג את שתי המשפחות באותה
מחלקת-CSS (`chip good`), ומחלקות `chip.score`/`chip.ctx` היו קיימות ב-CSS **ולא בשימוש בפועל**;
כעת `אישרה N מ-M` ומרחק מסומנים `score`, ו-`עבדה אצל X` מסומן `ctx`. **② מתחת לסף:** מי שקיבלה פחות מ-4 זימונים ולא ענתה לאף אחד מציגה
`לא ענתה מעולם (N זימונים)` במקום הצ'יפ הרגיל — אותה תבנית כמו `טרם נצבר מידע`. *(קיים כבר
בכרטיס מסך 3; לא הודגם בדאטת-הדגמה הנוכחית של מסך זה — לא חובה לצייר מחדש כדי לתעד את הכלל.)*

## ④ מצבים — **הוכרע 07/08/2026, בעקבות שאלת-ישי "זה מה שחסר?"**

**טעינה** ⇒ שלד של שמונה כרטיסי-מועמדת — לא "0 מתוך 0" באמצע חישוב שכבות 1–4.
**ריק אמיתי** ⇒ שכבה 1 (השער) פוסלת את כולן — למשל כל הדיילות רחוקות מ-80 ק"מ, כבר משובצות
באותו תאריך, הצהירו אי-זמינות, **אינן פעילות**, או **מסומנות "לא-לשלוח" אצל לקוח האירוע הזה**
🔧 *(עודכן 07/08/2026 — שני תנאי-שער מתוך חמישה חסרו מהרשימה; עוגן:
`processes-approved.md §ב1`, שכבה 1)*. *"אין מועמדות פנויות לאירוע הזה כרגע"* + הפניה למאגר-הדיילות.
🔴 **ואין כאן "ריק אחרי סינון" כמו במסך 3 — ובכוונה:** למשטח הזה **אין מסנן**, רק ארבע
זוויות-מיון שמסדרות-מחדש **את אותן** 23 מועמדות (`①` למעלה: *"ארבע הזוויות מסדרות בלבד — אינן
מסננות"*). ⇒ **מצב-ריק אחד, לא שניים** — זה לא חסר, זה נגזר ישירות מהחלטה שכבר הוכרעה.
**שגיאה** ⇒ בדיוק כמו מסכים 1/3: `hostesses`/`assignments`/`projects` **`deny-all` היום** (נמדד)
⇒ שאילתה שנכשלת מחזירה `{data:null, error:null}`, ומסך ש"נופל" ל-0 מועמדות בשקט **נראה זהה
לריק-אמיתי**. **אסור.** מוצגת הודעת-שגיאה מפורשת + "נסה שוב" — לא רשימה ריקה.
**פעולה חסומה** ⇒ מנהלת-פרויקטים (צפייה-בלבד) רואה את הרשימה המדורגת ואת הצ'יפים, **בלי**
תיבות-הסימון, בלי שני כפתורי-השליחה, ובלי ★ אחראית-משמרת — אותו כלל שכבר נקבע במסכים 3/4:
כפתור חסום **אינו קיים כלל בעבורה**, לא כפתור מכובה.

🔧 **עודכן 07/08/2026 — שתי תוספות:**
**פעולה נכשלה — אישור-סופי לדיילת שכבר משובצת באותו תאריך:** **חסום — אילוץ במסד, לא בדיקה
בקוד** (`processes-approved.md §ב5`: *"אסור שזה יקרה"*). ההודעה: השורה חוזרת למצבה הקודם + הודעה
מפורשת — אותו כלל כללי מסך 4 §⑤, ולא מסלול-כשל חדש. ⚠️ **וזו חסימה, לא אזהרה** — שונה מעודף-אישורים
(למעלה ב-①), ששניהם על אותה פעולה ("אשר סופית") אבל בשני כיוונים הפוכים: כפל-תאריך **חוסם**,
עודף-כמות **מזהיר**.
**נדרשות משתנה תוך-כדי שיבוץ (הלקוח מקטין/מגדיל):** אין מצב-מסך ייעודי — **"נדרשות" הוא פשוט
מספר שמתעדכן**, והמונים למעלה (①) עוקבים אחריו בזמן-אמת. עוגן: `processes-approved.md §ב8`.

## ⑤ הרשאות

**גיוס = עריכה · מנכ"ל = עריכה · פרויקטים = צפייה · כספים/לקוחות/לוגיסטיקה = חסום**
*(אותה מטריצה כמו מסכים 3/4 — אותו מודול, לא הכרעה נפרדת).*
⚠️ **והיום `deny-all` על שלוש הטבלאות שהמסך קורא מהן** — `hostesses` · `assignments` · `projects`
(נמדד; `🚧 מ4` ב-`PROJECT_MASTER §6`) ⇒ המסך חוזר ריק **לכולם, כולל מנכ"ל**, בלי שגיאה — בדיוק
המלכודת שסעיף ④ למעלה קיים כדי למנוע.
🔴 **וטבלת-ההעדפה (שכבה 2, "נעיצה") עדיין לא קיימת בכלל** — היא נכתבת ע"י מודול 6 ותישאר ריקה
עד שהוא ייבנה (כבר רשום כ-`🚧 מ6 ← מ4` ב-§6, `db_roadmap.md`) — לא חובה חדשה, רק צוין כאן כדי
שהקורא של הכרטיס הזה לא יטעה לחשוב ששכבה 2 פעילה מהיום הראשון.

## ⑥ הגדרות שמאחורי מילה על המסך
**"אמינות ההגעה כבוי"** = המרכיב לא מנורמל לאפס אלא **מוסר מהנוסחה**, והמשקלים מתחלקים מחדש
ל-≈0.62/0.38 · **"פג תוקף"** = `pending` **וגם** עברו 48 שעות · **"תענה הכי מהר"** נעשית
ברירת-מחדל **מתחת ל-72 שעות** לאירוע.

🔧 **עודכן 07/08/2026 — שתי תוספות:**
**① שובר-שוויון בתוך שכבה 4 (מיון):** ציון-שווה נשבר **אקראי-אך-קבוע לאירוע** — **לעולם לא לפי
סדר-הקליטה למערכת** (זה יתרון-קבע לוותיקות). עוגן: `processes-approved.md §ב1`, שכבה 4.
**② זווית "תענה הכי מהר" תלוית-דאטה שלא קיימת עדיין:** `assignments.responded_at` **אינו קיים
בסכמה** (פרק 12 §⑨ ב-`processes-approved.md`) ⇒ **הזווית חייבת להיות מוצגת מכובה-ומנומקת, לא
נעלמת ולא ממוינת שגוי**, כל עוד אין מספיק דאטה. ✅ **תוקן במוקאפ 07/08/2026** — עד אז הוצג
ככפתור פעיל לגמרי; כעת `opacity:.5` + תווית "— כבוי" (אותו דפוס-כיבוי כמו "שלח את הקישור שוב"
במסך 4) + שורת-הסבר מתחת לכפתורים שאומרת בקול שאין עדיין נתוני-זמן-תגובה.

## 🏷️ דאטת-הדגמה מול נגזר-ממודל *(נוסף רטרואקטיבית 06/08)*

**‏🌱 מהמסד** *(`scripts/demo-seed.mjs`)*: ‏`כנס לקוחות שנתי` · `מדיטק פתרונות בע"מ` ·
`אקספו תל אביב` · `18:00–22:00` · **6 נדרשות**.
**‏🎭 דמו:** שמות שמונה הדיילות · הטלפונים · הערים · השעות · התאריך `22/08/2026` · `מדיטק בע"מ`.
🔴 **וגם `8 מתוך 23 שעברו את הסינון` — זו טענה על גודל המאגר, לא קישוט.** ‏**המאגר החי ריק
(0 שורות)**; ‏23 הוא **מודל** של "כמה עוברות שער ב-50 דיילות", והוא מה שמצדיק את הכרעת
"מסדרים ולא מסננים" *(`world-sources.md` סבב 2 §③)*. ⚠️ **אם המספר ישתנה — הנימוק לזוויות
משתנה איתו.**
**‏📐 נגזר ממודל:** ארבעת המונים · הצ'יפים ומשפחותיהם · ≈0.62/0.38 · סף 8 השבועות.

## עוגן-עולם — **דו-חלקי, ומהסבב השני של המחקר**
✅ **הציון הסמוי: זו אסכולה מתועדת** *(Sidekicker · LinkedIn · Greenhouse · Instawork)*,
**⚠️ ו-Ubeya — האנלוג הקרוב — עושה ההפך.** ⇒ **בחירה מנומקת, לא חיקוי.**
🔴 **וארבע הזוויות: סטייה מהעולם כולו** — לא נמצא מוצר עם שני סדרי-דירוג מנוקבים בשם.
**ובכוונה סטינו, כי** העולם מסנן בגלל **אלפים**; אצלנו **23 נכנסות למסך** ⇒ **סינון חסר-תוחלת,
סידור-מחדש הוא הכלי.** ‏**הפירוט: `world-sources.md` סבב 2.**

---

# ✅ מסך 1 · מבט-על שיבוץ — אושר 06/08/2026

> **🖼️ המוקאפ המאושר: `docs/mockups/hostesses-screen/approved/01_overview_approved.html`** —
> **הוא חלק מהאפיון, לא איור לצידו.** מה שנראה בו ולא כתוב כאן — **המוקאפ קובע.**
>
> **איך הוא אושר:** ישי — *"נראה מצוין"*, ואז *"נותן לך להחליט על זה"* ⇒ **האצלה.**
> ‏לפי `.claude/skills/_shared/ishay-calibration.md §4` **קלוד הוא הבעלים של התוצאה.**
>
> 🔴 **ושלוש הכרעות-צבע ומבנה שנחתכו מ-`src/`, לא מהמוקאפ הקודם ולא מהעין:**
> ‏**ירוק = "פעיל/אושר"** *(`UsersManagementPage.jsx:360` · `RowAction.jsx` tone=`approve`)* ·
> ‏**אדום = שגיאה** *(גישה-נדחתה · כשל-טעינה)* ⇒ **לעולם לא "סירבה"** ·
> ‏**גופן Geist** *(`src/index.css`)*, לא Segoe UI.
> 🔑 **וזה הכלל שנולד כאן: שפת-העיצוב נקראת מ-`src/`, לא מהמוקאפ הקודם — הוא עצמו קירוב,
> והעתקה ממנו מנציחה דריפט.**

## ① 🔴 מפת-לחיצות

| הרכיב | מה זה עושה |
|---|---|
| **לשונית "מעקב פניות ושיבוצים"** | **מנתב** — מציגה זימונים שנשלחו ומה קרה איתם (זה המסך; אין קליטת-מועמדות עצמית במודול) |
| **לשונית "מאגר דיילות"** | **מנתב** — עוברת למסך 3 (אותו עמוד, לשונית שנייה) |
| **כפתורי-סינון** ("הצג חסרים בלבד" · "דחוף (עד 72 שעות)" · "הכול") | **מסננים בלבד** — אינם כותבים ל-DB |
| **לחיצה על שורה** | **מנתב** — פותח Smart Match של אותו אירוע (`C5 §5.6.10`) |
| **"שלח שוב"** (פר-שורה) | 🔴 **מבצע** — הפעולה היחידה מהמסך הזה שכותבת ל-DB. אפס שיקול-דעת: הקישורים מתו, רענון לא יכול להזיק, ואי-מענה ממילא מחוץ לציון |
| **"שלח שוב למי שפג תוקפן (N)"** — כפתור מרוכז בשורת-הסינון, כבוי כש-N=0 | 🔴 **מבצע** — אותה פעולה בדיוק כמו הפר-שורה, על כל השורות שפג תוקפן בבת אחת (בכל הפרויקטים). **לא מנווט לשום מקום, לא פותח דיאלוג-אישור** — אותו "אפס שיקול-דעת" של הגרסה הבודדת חל כאן במלואו; חלון-סקירה לפני היה סותר את זה. הודעת-אישור גנרית בלבד (למשל "N קישורים נשלחו מחדש") |
| **`מחכות לאישורך`** | **טקסט, לא כפתור** — אישור דורש לבחור מי, וההחלטה נעשית במסך שיש בו המידע (Smart Match) |
| **`שלח לעוד דיילות`** | **לא קיים כאן בכלל** — בחירת-מועמדות שייכת ל-Smart Match בלבד |
| **`לשיבוץ →`** | **מנתב** — למסך השיבוץ של אותו פרויקט. אינו מבצע ואינו שולח מייל |

📌 **תוקן 08/08/2026 (סבב-ביקורת 3.1):** הסעיף הוסב לטבלה ממוספרת (היה פרוזה לא-ממוספרת, ולא נשא מספר-סעיף בכלל), ונוספו ארבע השורות הראשונות (שתי הלשוניות + שלושת כפתורי-הסינון) שהיו קיימות במוקאפ המאושר ולא תועדו כלל. משמעות הלשונית הראשונה אושרה בפירוש עם ישי באותו סבב (הצהרה ישנה שעמדה "אושרה בשתיקה").

🔑 **הרקע ל-`לשיבוץ →`** *(תוקן 06/08/2026 בהכרעת-ישי — קודם היה כפתור "סגירה טלפונית", כלומר מבצע)*:
**ולמה הוא לא יכול לבצע, וזה בדיוק הנימוק שכבר הכריע את `מחכות לאישורך`:** הוא יושב על
**שורת-פרויקט**, וסגירה טלפונית מאשרת **דיילת ספציפית** ⇒ **דורשת לבחור מי** ⇒ **ולמסך הזה
אין את המידע לבחור.** הרשימה והמידע נמצאים במסך השיבוץ, ושם הפעולה קיימת בשמה המלא
*(`אושרה סופית — סוכם בטלפון`, מסך 4)*.
⚠️ **וזו גם הסיבה שהסתירה שרדה אישור:** **הרכיב לא הופיע במפת-הלחיצות כלל** — המפה
שהייתה אמורה לתפוס אותו לא הכילה אותו. 🔑 **מפה חלקית גרועה ממפה שאינה קיימת: היא
נראית כמו בקרה ואינה.**

---

# 📓 הכרטיס המלא (התיעוד שמאחורי המוקאפ)

## ② ההחלטה האחת — **וזו הכרעת-ישי, לא גזירה של קלוד**

> **‏🔗 מקור: `discovery-log.md`, סעיף *"🔴 And what a targeted transcript re-read found —
> Ishay's own words, never recorded"*, פריט 2.** ‏*(דבריו נשלפו מהתמליל 06/08/2026 ונכתבו לשם;
> **הם אינם קיימים בשום מקום אחר.**)* ובמילותיו:
> *"מסך מבט על של המנהלת… יכול להיות תהליך מאחורי זה מבחינתה של **על איזה פרויקט להיכנס
> ראשון**… שזה יהיה עבורה **מסך שמקל עליה בקבלת החלטות**."*

> **⇒ *"על איזה אירוע אני נכנסת לטפל עכשיו — ומה בדיוק חסר בו."***

🔑 **זה מסך טריאז', לא דוח.** ‏**סדר-השורות הוא התשובה**, לא קישוט. כל רכיב שאינו עוזר לדרג
בין אירוע לאירוע — יוצא.

**עוגן-עולם, דו-חלקי:** ‏**זהה למקובל, ומגובה:** ‏`research §2` מודד ש**סקר-הבנצ'מרק של
ASA — ‏120+ מדדים — אינו כולל ולו מדד אחד של איכות-עובד**; כולם **איוש**: אחוזי-איוש ·
זמן-לאיוש · שעות-מחויבות. ⇒ **מסך שה-KPI שלו הוא "איפה החור" הוא בדיוק מה שהענף מודד.**
**ומתאים כאן כי** אצל ישי 2–5 מקומות לאירוע ⇒ **חור אחד הוא שליש מהצוות** *(`§9.5`)*.

## ③ 🔴 שלושה חורים שנמצאו במוקאפ — כל אחד הופך שתי פעולות הפוכות לזהות

| מה במוקאפ | הפער — ולמה הוא חוסם החלטה |
|---|---|
| **ארבעה מונים: `נשלחו · אושרו · נדחו · ממתינות`** *(וכך גם C5 §5.6.10)* | 🔴 **אין תא ל"אישרה זמינות" — המצב שבין הזמנה לאישור.** ⇒ אירוע שבו **6 אישרו זמינות ואיש לא אושר סופית** מציג `אושרו 0` **ונראה זהה** לאירוע שאיש לא ענה בו. **הראשון דורש ממנה ללחוץ "אשר"; השני דורש לשלוח לעוד.** 🔑 **בדיוק אותה מחלקה כמו `ממתינות` מול `פג תוקף` — אותו מספר, שתי פעולות הפוכות** |
| **`ממתינות 3`** | **מתפצל** *(הכרעה ② שנסגרה)*: **`ממתינות 3 · מתוכן 2 פג תוקפן`**, והחלק פג-התוקף **לחיץ** |
| **`חוסרים קריטיים 1` = "פרויקטים ללא דיילות מאושרות"** | **ההגדרה שגויה לטריאז':** אירוע שדורש 6, יש בו 5, **ומחר** — קריטי יותר מאירוע שדורש 2, יש בו 0, **ובעוד חודש.** ⇒ **קריטי = *עדיין חסר בתוך T-24* — בדיוק ההתראה שכבר הוכרעה ב-§ב7**, ולא הגדרה חדשה |

## ④ מה נכנס, ומה משתנה

| הרכיב | ההכרעה | הנימוק |
|---|---|---|
| **מונה חמישי: `אישרו זמינות`** | נכנס | ר' ③ |
| **התראת T-24** על שורה שעדיין חסרה | נכנסת | 🔴 **עודכן 08/08/2026 — `PROJECT_MASTER §7.91` (הכרעת-ישי): ההתראה **נגזרת בזמן-קריאה**, כמו "פג תוקף" — ולא מהריצה המתוזמנת.** *(הנימוק המקורי כאן היה *"אותה ריצה ששולחת תזכורות מדליקה אותה — אפס עלות"*, **והוא נשען על ריצה שהיא של מ10 וטרם נבנתה**; §7.91 הכריע שמ4 לא יהיה תלוי בה.)* **אפס עלות עדיין נכון — מסיבה אחרת: נגזרת, בלי עמודה ובלי מיגרציה** |
| **סדר ברירת-מחדל** | **חסרים תחילה, בתוכם לפי קרבת-האירוע; המאוישים אחריהם** | **המוקאפ אינו מגדיר מיון כלל** — ובמסך טריאז' **הסדר הוא התשובה** |
| **הסף ל"דחוף"** | **‏72 שעות** | **לא בחירה חדשה: זה כבר הסף שבו האלגוריתם מחליף ברירת-מחדל ל"תענה הכי מהר"** (`§11.7`). **סף אחר היה גורם למסך ולדירוג לספר סיפורים שונים** |
| **הכפתור `שלח תזכורות במייל`** | **הופך ל-`שלח שוב למי שפג תוקפן (N)`, וכבוי כש-N=0** | **בנוסחו הנוכחי הוא משכפל עבודה של ג'וב אוטומטי** (תזכורת T-24 היא אוטומטית, §ב7). **הערך האמיתי בשורה הזאת הוא הפעולה שאין לה היום מקום** |
| **`לניהול שיבוץ חכם` ככפתור גלובלי** | **יורד** — הכניסה היא **לחיצה על שורה** | ‏**C5 §5.6.10 קובע זאת מפורשות:** *"לחיצה על פרויקט ספציפי תפתח את חלון השיבוץ החכם לאותו אירוע"*. **כפתור גלובלי בלי אירוע נבחר אינו מוביל לשום מקום** |

## ⑤ מקור לכל מספר

**מס' פרויקט · שם · לקוח · תאריך · מיקום** — `projects` ⋈ `customers` · **`נדרשות`** —
`projects.required_hostess_count` · **חמשת המונים** — ספירת `assignments` **לפי הסטטוס בשורה האחרונה
פר-דיילת** *(`MAX(assignment_number)` — אחרת דיילת שסירבה ואז אושרה נספרת פעמיים; `§11.3`)* ·
**`פג תוקפן`** — **נגזר**: `pending` **וגם** `now > invite_sent_at + 48h` · **`בעוד N ימים`** —
מ-`final_event_date`. ➕ **ושני ה-KPI העליונים** (`חסרים 2 · מתוכם 1 בתוך 24 שעות` · `ממתינים 7 ·
מתוכם 3 פג תוקפם`) **הם צירוף של חמשת המונים והתגית `פג תוקפן` על פני כל השורות** — לא עמודה
נפרדת. *(תוקן 08/08/2026, סבב-ביקורת 3.1 — לא היה כתוב במפורש.)*

## ⑥ מצבים

**ריק אמיתי** ⇒ *"אין כרגע אירועים הממתינים לאיוש"* — **וזו בשורה טובה, לא שגיאה** ⇒ ניסוח חיובי.
**ריק אחרי סינון** ⇒ *"אין אירועים התואמים לסינון"* + *"נקה סינון"*.
🔴 **שגיאה — והיא התרחיש הריאלי היום:** ‏`projects` היא **`deny-all`** (נמדד) ⇒ **המסך יחזור ריק
לכולם, כולל המנכ"ל, עם `error: null`.** ⇒ **אסור שמסך זה יציג "אין אירועים" כשהשאילתה נכשלה.**
*(פרק 12 פריט ② — וזו הסיבה שהוא נרשם.)*

🔧 **עודכן 07/08/2026, ותוקן שוב 08/08/2026 — ר' התיקון המהותי מתחת.** עוגן: `discovery-log.md`
(תהליך ב׳, סעיף 6, מצוטט מישי): המסך **הוצע** להציג פרויקטים בסטטוס `בתהליך` בלבד. ברגע שהמכסה
מתמלאת הפרויקט **אמור** לעבור ל**`מוכן לביצוע`** **ולצאת מהרשימה** — לא
להישאר בה עם תג "הושלם". **וכיוון הפוך, ב8 בתהליכים:** אם הלקוח מקטין את הכמות אחרי שהמכסה
כבר התמלאה, הנעילה נפתחת והפרויקט **חוזר** ל"בתהליך" — כלומר **חוזר להופיע במסך הזה** בלי
פעולה נוספת מהמנהלת מלבד השחרור עצמו (מסך 4, "שחרר"). **אין צורך בעמודת-סטטוס נראית על

> 🔴 **תיקון-מהותי, 08/08/2026 (הכרעת-ישי, לאחר council + אימות מול C5):**
> **‏① רשימת-המסך מציגה גם `טרם החל` וגם `בתהליך`, לא `בתהליך` בלבד.**
> ‏`C5 §5.5.7` (השורה שקבעה "בתהליך בלבד") עוסקת ב**יציאה** מהרשימה, לא בכניסה אליה — ומעולם
> לא נבדק כיוון-הכניסה. **פרויקט נולד `טרם החל` (`C5 §5.5.5`, ומאומת בקוד — ה-RPC של מ3 יוצר
> `project_status='not_started'` במפורש)**, וה-`in_progress` הראשון נכתב **רק אחרי** פעולת-שיבוץ
> או לוגיסטיקה ראשונה. ⇒ **בלי `טרם החל`, כל אירוע חדש בלתי-נראה עד לפעולה הראשונה עליו** —
> בדיוק ההפך ממה שמסך-טריאז' אמור לעשות. סדר-המיון הקיים (חסרים תחילה) פותר את זה לבד.
> **‏② `③ מוכן לביצוע` אינו טריגר של מודול 4. תוקן — ר' `spec.md §12⑱(ד)`.**
המסך** — זה כבר מה ש-⑤①/⑤② קובעים על "חסרים" מול "מאוישים"; זו רק ההבהרה **למה** פרויקט
נעלם או חוזר לרשימה.

## ⑦ הרשאות

זהות למסך המאגר. ⚠️ **ובנוסף — קריאה מ-`projects`**, שאין עליה policy כלל.

## ⑧ פרטים שמקורם המוקאפ — הוכרעו 07/08/2026

**הכרעת-ישי, שלושתם אושרו כלשונם** *(מילותיו: **"מאשר את שלושתם"**)*:

**‏`פרויקטים בשיבוץ 5` כ-KPI** — **יוצא.** *(מונה נוכחות, לא מונה-פעולה; ובמסך טריאז'
שני ה-KPI צריכים לענות "כמה בוער".)* **במקומו: `חסרים 2 · מתוכם 1 בתוך 24 שעות`** ·
**⚠ ו-🔗 בשורה הראשונה** — **אפיון שותק, והוכרע:** ⚠ = חסר בתוך T-24 *(אותו סף כמו ב-KPI —
סימן אחד, משמעות אחת)* · 🔗 = פתיחה בלשונית חדשה *(כדי שרשימת-הטריאז' לא תאבד)*.

## 🏷️ דאטת-הדגמה מול נגזר-ממודל *(נוסף רטרואקטיבית 06/08)*

**‏🌱 מהמסד** *(`scripts/demo-seed.mjs`)*: שמות הלקוחות `מדיטק פתרונות בע"מ` · `עיריית חדרה` ·
שמות האירועים `כנס לקוחות שנתי` · `טקס פרסים עירוני` · והמיקום `היכל התרבות, חדרה`.
**‏🎭 דמו מומצא — וזה החלק שהמוקאפ הזה מלא בו:** 🔴 **`רותי לוי`, שם המנהלת** *(אין לה מקור, ובכנס
ישאלו "מי זו")* · 🔴 **`אלביט מערכות` ו-`השקת מוצר — סדרת אביב`** *(`git grep` על `אלביט` החזיר
**אפס** ב-`scripts/`+`src/`+`supabase/`)* · ⚠️ **`היכל התרבות, ת"א`** — ב-seed הוא **בחדרה**,
והמוקאפ העביר אותו לתל אביב והצמיד ללקוח מומצא · והתאריכים.
**‏📐 נגזר ממודל:** חמשת המונים · `חסרים 2 · מתוכם 1 בתוך 24 שעות` · סף 72 השעות · סדר-הטריאז'.

🛑 **עצירה — מסך 2 מתוך 8.**

---

# ✅ מסך 3 · מאגר דיילות — אושר 06/08/2026

*(ישי: **"אני זורם עם ההחלטות שלך"** — ‏**האצלה, לא הסכמה.** ‏לפי `.claude/skills/_shared/ishay-calibration.md §4`
זה אומר שקלוד הוא הבעלים של התוצאה, **ושמשקל הנימוק גבוה יותר ולא נמוך יותר.**)*

## 🔴 מפת-לחיצות *(נוסף 07/08/2026 15:25 — לאחר שהמוקאפ צויר ואושר, ר' `approved/03_repository_approved.html`. בכוונה בלי מספר-עיגול: מיספור-מחדש של ②–⑦ שלמטה היה שובר הפניות קיימות ברחבי התיקייה; זו תוספת, לא שכתוב)*

| הרכיב | מה זה עושה | לאן זה מוביל |
|---|---|---|
| **שם הדיילת (שורה)** | 🔴 **מנתב** | פותח את כרטיס-הדיילת לצפייה — **מסך 3ד, ⬜ טרם צויר** |
| **עיפרון ✎** | 🔴 **מנתב** — ישירות, בלי לעבור דרך הצפייה | פותח את חלון העריכה — **מסך 3ג, ✅ אושר 07/08/2026** (`approved/07_edit_hostess_approved.html`) |
| **מתג הפעלה/השבתה** | 🔴 **תלוי-מצב, תוקן 07/08/2026 15:25 (ר' §🔍 למטה):** אין שיבוץ עתידי פעיל ⇒ מבצע מיד, אפס שיקול-דעת (כמו "שלח שוב" במסך 1). **יש** שיבוץ עתידי פעיל ⇒ **חייב** לפתוח קודם חלון-אישור ששמו אותם בשם+תאריך, לפי `processes-approved.md §א4` (הוכרע 05/08/2026) — שתי אפשרויות: "שחרר אותה" (מומלץ) / "תשלים את מה שהתחייבה" | משנה `hostesses`/`assignments` לאותה דיילת בלבד |
| **+ הוספת דיילת** | 🔴 **מנתב** | פותח פופ-אפ — **מסך 3ב, ⬜ טרם צויר** |
| **חיפוש חופשי (שם/טלפון)** | מסנן בלבד | אין ניווט |
| **"כל הערים"** | מסנן בלבד | אין ניווט |
| **"פעילות בלבד"** | מסנן בלבד (on כברירת-מחדל) | אין ניווט |
| **"לא ענתה ל-4 האחרונים (N)"** | ✅ **מסנן בלבד — הוכרע ישי 07/08/2026 15:25:** אותה תבנית כמו "הצג חסרים בלבד" במסך 1 — לוחצים, נשארות רק המתאימות, לוחצים שוב וחוזר הכול. **לא** דוח נפרד ולא הודעה קבוצתית | אין ניווט |
| **לשונית "מעקב פניות ושיבוצים"** | מנתב | חוזר למסך 1 (אותו עמוד, לשונית ראשונה) |

## ① ההחלטה האחת

המנהלת מגיעה למסך בשלוש סיטואציות בלבד, **וכולן מתחילות משיחת טלפון:** דיילת חדשה נקלטה ·
דיילת עדכנה פרטים או הודיעה שלא תהיה זמינה · דיילת עוזבת.

> **⇒ *"איזו דיילת אני פותחת עכשיו — ומה מצבה, לפני שאני נוגעת בה."***

**זה מסך של איתור-אדם ופעולה עליו. ההשוואה בין דיילות קורית ב-Smart Match ושם היא מחושבת.**
*(ולפי `PROJECT_MASTER §1`, זו התשובה היחידה שמנצחת את "יותר מדי / פחות מדי מידע".)*

## ② מה יוצא, ולמה

| הרכיב במוקאפ | הוורדיקט | עוגן-עולם *(דו-חלקי)* |
|---|---|---|
| **עמודת ת"ז** | **יוצאת** — מחפשים אדם בשם, לא במספר-זהות; וזו PII שמוצגת ל-50 שורות בבת אחת | ⚠️ **מהידע שלי, לא אומת** — לא נבדק בפועל אם מוצרי-שיבוץ מציגים מזהה לאומי בטבלה. **הוצע לישי לבדוק, לא נדרש** |
| **מסנן "כל טווחי השכר"** | **יוצא** — *"תראי לי מי מרוויחה 75–85"* אינה שאלה שנשאלת כשמישהי על הקו | **זהה למקובל:** עלות היא **זווית-מיון ב-Smart Match** ואינה מסנן-מאגר *(`research §9.6`)* |
| **מסנן "כל הדירוגים"** | **יוצא** — מאותה סיבה | כנ"ל |
| **אייקון 🗑 מחיקה** | **הופך למתג השבתה/הפעלה** | ✅ **וזו אינה סטייה מ-C5:** ‏§5.6.12 מצייר פח-אשפה **אך קובע "מחיקה לוגית"**. **ובעולם: Ubeya — מושבת מאבד עתיד ושומר עבר · TempWorks — "אינה מאפשרת למחוק רשומת עובד באמת"** |

## ③ מה נכנס

| הרכיב | הנימוק | עוגן-עולם |
|---|---|---|
| **עמודת "מצב"** — `פעילה` · `מושבתת` · `לא זמינה DD/MM–DD/MM` | 🔴 **חור במוקאפ: יש מסנן "פעילות בלבד" ואין עמודה שמראה מצב** ⇒ כשהמסנן כבוי, מושבתת נראית זהה לפעילה. **תוקן 07/08/2026 15:25:** התווית היא **טווח**, לא תאריך-סיום יחיד — `processes-approved.md §א3` קובע שהמנהלת מזינה "טווח תאריכים + הערה קצרה", לא רק תאריך-חזרה | **זהה למקובל** — מחזור-חיים גלוי הוא הדפוס אצל Ubeya · TempWorks · ESHYFT *(`world-sources.md`)* |
| **עמודת טלפון** | כל שלוש הסיטואציות הן שיחת טלפון. **המספר לא צריך לחכות לפתיחת כרטיס** | **זהה למקובל** |
| **דיילת שלא דורגה מציגה `—`** ולא `3 ★` | ‏`hostesses.rating` הוא `int not null **default 3**` *(נמדד)* ⇒ **כל דיילת נולדת עם 3 כוכבים שאיש לא נתן לה**, והעמודה נקראת **"התרשמות"** | ✅ **מתועד ומכריע:** ‏`research §6.2` — **Nature 2025: 85% מהדירוגים 1–5 הם 5★** · `§9.2` — **Wonolo ביטלה את דירוג-הכוכבים** כי המזמינים לא מילאו אותו. **ערך-ברירת-מחדל שמוצג כשיפוט הוא בדיוק איך העמודה הזאת מתה** |
| ➕ **מסנן "לא ענתה ל-N הזימונים האחרונים"** *(`params`, ברירת-מחדל 4)* | הדוח שהופך את הצ'יפ לפעולה | 🏆 **סטייה מודעת, והיא נכס-כנס:** **הסוכן חיפש דוח היגיינת-מאגר ולא מצא אותו באף מוצר.** **ובכוונה סטיתי, כי:** בקנה-מידה של אלפי עובדים "מי מת במאגר" חסר משמעות; **אצל ישי 50 דיילות והמנהלת מכירה כל אחת בשם — ולכן "ארבע שלא ענו" היא פעולה, לא סטטיסטיקה** |
| ➕ **מתחת לסף: `לא ענתה מעולם (N זימונים)`** | מי שקיבלה 2 זימונים ולא ענתה **מתה באותה מידה ואינה מקבלת צ'יפ** | אותה תבנית של `טרם נצבר מידע` בציון |
| 🔴 ➕ **חלון-אישור בהשבתת דיילת עם שיבוץ עתידי פעיל** *(נוסף 07/08/2026 15:25 — נעדר מהכרטיס עד עכשיו)* | `processes-approved.md §א4` (הוכרע 05/08/2026): מונה את השיבוצים בשם+תאריך, ונותנת שתי אפשרויות מנוסחות-לפי-כוונה — "שחרר אותה" (מומלץ) / "תשלים את מה שהתחייבה". **בלי שיבוץ עתידי פעיל — המתג מיידי, אין חלון** | 🔗 מראה — ההכרעה עצמה כבר קיימת ב-`processes-approved.md`; זו רק השלמת-הכיסוי לכרטיס המסך |

## ④ מקור לכל מספר שמוצג

**שם** `full_name` · **טלפון** `phone` · **עיר** `city` · **התרשמות** `rating` *(‏`int` ⇒ `4 ★`,
**לעולם לא `4.9`** — המוקאפ מציג עשרוניות, סתירה שאומתה גם ב-`research §11.9`)* ·
**שכר שעתי** `hourly_rate` · **מצב** `status` + טבלת אי-הזמינות ·
**מס' אירועים** — **חישוב, ו-C5 אינו נוקב בחלון** ⇒ **"אירועים ברבעון האחרון", והכותרת אומרת זאת**
*(אותו חלון כמו בכרטיס ה-Smart Match, `§11.5` — **שני חלונות לאותו שם היו גורמים למסך לספר
סיפור שונה מהדירוג**)*.

## ⑤ מצבים

**טעינה** — שלד-טבלה. **ריק אמיתי** *(המצב **היום**: 0 שורות, נמדד)* ⇒ *"עדיין אין דיילות
במאגר"* + הכפתור הראשי בגוף המסך. **ריק אחרי סינון** ⇒ **הודעה אחרת** — *"לא נמצאו דיילות
התואמות לסינון"* + *"נקה סינון"*. 🔑 **שתי ההודעות חייבות להיות שונות, אחרת המנהלת חושבת
שהמאגר נמחק.** **שגיאה** ⇒ *"לא הצלחנו לטעון את המאגר"* + "נסה שוב" — **ולעולם לא רשימה ריקה
בשקט** *(המלכודת של RLS-בלי-policy)*. **פעולה חסומה** ⇒ מנהלת פרויקטים רואה בלי הכפתור
הראשי ובלי אייקוני עריכה/השבתה.

## ⑥ הרשאות

גיוס = **עריכה** · מנכ"ל = **עריכה** · פרויקטים = **צפייה** · כספים/לקוחות/לוגיסטיקה = **חסום**.
⚠️ **והיום `deny-all` — ‏RLS דלוק, אפס policies (נמדד)** ⇒ המסך חוזר ריק לכולם **בלי שגיאה**.
‏(`🚧 מ4` ב-§6 · פרק 12 פריט ②.)

## 🏷️ דאטת-הדגמה מול נגזר-ממודל *(נוסף רטרואקטיבית 06/08)*

**‏🌱 מהמסד:** אין — **המאגר ריק, 0 שורות (נמדד)**, וזה גם המצב שהכרטיס מאפיין ב-§⑤.
**‏🎭 דמו:** כל שורה שתופיע במוקאפ כשייצויר — **ותסומן שם.**
**‏📐 נגזר ממודל:** `rating` הוא `int not null default 3` *(נמדד בסכמה)* ⇒ הכלל שדיילת שלא
דורגה מציגה `—` · חלון "אירועים ברבעון האחרון" · סף `N` בצ'יפ ההיגיינה.

## ⑦ פרטים שמקורם המוקאפ — הוכרעו

**מסנן "כל הערים" — נשאר** *(50 דיילות; כשמחפשים מישהי לאירוע בחיפה זה הסינון היחיד שמשרת
איתור)* · **כותרת המסך: "דיילות"** *(המוקאפ: "מנהלת גיוס – מבט על וחריגים" — כותרת של מסמך,
לא של מסך)* · **חיפוש חופשי על שם + טלפון בלבד** *(לא ת"ז — אינה מוצגת ממילא; לא כתובת —
מייצרת תוצאות מבלבלות)* · **בלי עימוד** *(עד 50 שורות ⇒ גלילה אחת)*.

---

# ✅ משטח 3ב · פופ-אפ הוספת דיילת חדשה — **אושר 07/08/2026**

> **🖼️ `docs/mockups/hostesses-screen/approved/06_add_hostess_approved.html`** — מוקאפ מאושר.
> *(סימון תוקן 08/08/2026 — אותה אדווה-שנעצרה כמו משטח 5: הקובץ כבר היה ב-`approved/`,
> וטבלת-"מצב" כבר אמרה ✅, אבל הכותרת כאן נשארה "טיוטה".)*
> **נפתח מ"מאגר דיילות" (מסך 3), לחיצה על "+ הוספת דיילת".**
> **מקור התוכן:** `processes-approved.md §א1` — נקרא **במלואו** לפני הציור, לפי הכלל שנכתב היום
> ב-📐 מבנה-הכרטיס ("✅ לפני שכרטיס עובר ל-✅ אושר"). **מבנה-הדיאלוג עצמו נשאב מ-`src/` בפועל**
> (`src/modules/02_customers/CustomerFormDialog.jsx`) — כותרת+תת-כותרת, שדה+תווית+שגיאה-מתחתיו,
> כפתור-שמירה מלא-רוחב — לא מומצא, כי זה הדיאלוג האמיתי היחיד שכבר בנוי בקוד ומדגים את השפה.

## ① 🔴 מפת-לחיצות

| הרכיב | מה זה עושה |
|---|---|
| **שדה ת"ז** | קלט + **ולידציה תוך כדי ההקלדה** — היחיד בטופס שאינו ממתין ל-blur *(processes-approved.md §א1)* |
| **שאר שדות-הטקסט/מספר** | קלט בלבד, ולידציה בעזיבת-השדה |
| **צ'יפי-שפות** | **בוחר/מבטל** — רב-ערכי, לא בלעדי |
| **מתג "יש רכב"** | **בוחר** בוליאני |
| **כוכבי-התרשמות** | **בוחר** 1–5 |
| **"שמור דיילת"** | 🔴 **מבצע** — יוצר שורת `hostesses` חדשה בסטטוס **"פעילה"**, נכנסת מיידית למאגר-המועמדות. **חסום** כש-ת"ז שגוי או שדה-חובה (שם/טלפון/שכר-שעתי) ריק/מתחת-למינימום |
| **"ביטול"** | **מנתב** — סוגר בלי לשמור, חוזר למסך 3 |

## ② ההחלטה האחת, ומי מקבל אותה

> **"המנהלת כבר דיברה עם הדיילת בטלפון, ועכשיו היא רק מתעדת את מה שסוכם."**

**מנהלת הגיוס.** 🔑 **ולכן הטופס לא נפתח "ריק וממתין"** — `processes-approved.md §א1`: *"כרטיס
נפתח רק אחרי שהמנהלת כבר דיברה עם הדיילת. אין 'מועמדת ממתינה לאישור', ואין טופס שהדיילת ממלאת
בעצמה."* **אין מסך מקביל לדיילת, אין הזמנה, אין קישור.**

## ③ אין ממצא-קונפליקט למשטח הזה

*(סעיף מותנה — לא הושמט: §א1 עצמו כבר מפרש כל שדה בפירוט, ולא נמצאה סתירה בין המוקאפ הקודם/
אינטואיציה לבין ההכרעה הכתובה, כפי שנמצא בשלושת המשטחים האחרים. אם ייחשף ממצא בעת הבנייה —
מקומו כאן.)*

## ④ מקור לכל שדה ומספר

**ת"ז** — בדיקת ספרת-ביקורת (תקן ישראלי); אלגוריתם ה-src/ טרם נכתב, זו דרישת-מוצר בלבד ·
**שכר-מינימום `₪35`** — `params.שכר_מינימום_שעתי` *(נמדד חי 31/07/2026)*, **הרף עצמו עריך במודול 9**
· **קואורדינטות** — geocoding **פעם אחת** בשמירה *(§א1; אותו מנגנון-lazy שכבר הוחלט ב-Smart Match,
`discovery-log.md` "Second self-caught miss")* · **התרשמות** — `hostesses.rating`, `int not null
default 3` *(עקבי עם מסך 3 §④ — "כל דיילת נולדת עם 3 כוכבים שאיש לא נתן לה")* · **שפות** —
`hostesses.languages text[] not null default '{}'` *(הכרעת-ישי 06/08/2026)*.

## ⑤ מצבים

**טעינה** ⇒ **אין מצב-ביניים** — טופס ריק נפתח מיידית, אין שאילתה מקדימה לפני שהוא מוצג.
**ת"ז שגוי** ⇒ *"מספר תעודת זהות אינו תקין"* מתחת לשדה, **"שמור" חסום**.
**שכר מתחת למינימום** ⇒ *"השכר השעתי חייב להיות לפחות 35 ₪ (שכר מינימום)"*, **"שמור" חסום**.
**אימייל כבר קיים** ⇒ **אזהרה רכה, לא חוסמת**: *"כתובת זו כבר רשומה אצל \<שם\> — להמשיך?"*
*(§7.65: אין UNIQUE — תיבה משפחתית משותפת היא מקרה לגיטימי, והת"ז כבר מונעת כפילות-אדם)*.
**כתובת לא-ניתנת-לגאוקוד** ⇒ 🔴 **לא חוסם** — הדיילת **נשמרת בכל מקרה**, מסומנת "אין קואורדינטות",
ובחישוב הקרבה מקבלת ציון **ניטרלי, לא אפס** *(§7.55/§11.4: אפס מעניש על תקלת-מערכת, לא על הדיילת)*.
**שמירה נכשלה מסיבה אחרת (כתיבה נחסמה)** ⇒ אותו כלל כמו מסך 4 §⑤: **לעולם לא "נשמר" בשקט**,
הודעת-שגיאה מפורשת. **הצלחה** ⇒ פס-הצלחה קצר ואז סגירה — אותו דפוס בדיוק כמו `CustomerFormDialog.jsx`.

## ⑥ הרשאות

**גיוס = עריכה · מנכ"ל = עריכה** ⇒ רואות את הכפתור "+ הוספת דיילת" בכלל. **פרויקטים = צפייה**
⇒ **הכפתור אינו קיים לה כלל** *(אותו כלל שכבר נקבע במסכים 2/3/4: כפתור חסום אינו קיים, לא מכובה)*.
**כספים · לקוחות · לוגיסטיקה = חסום.**
🔑 **ולמה שדות-הבנק ות"ז מותרים כאן בכלל, כשהם אסורים בטבלת-המאגר (מסך 3):** `processes-approved.md
§0` — *"פרטי בנק ות"ז לא מוצגים בטבלת המאגר — רק בתוך כרטיס הדיילת, ורק למי שיש לה הרשאת עריכה."*
**זה בדיוק כרטיס הדיילת, ורק בעלות-עריכה מגיעות לכאן** — התנאי מתקיים אוטומטית, לא נבדק בנפרד.

## ⑦ הגדרות שמאחורי מילה על המסך

**"התרשמות" ולא "דירוג"** — כבר הוכרע *(חמישה-פריטים ①, `processes-approved.md`)*: זו דעת המנהלת,
לא פסק-דין של המערכת, ואינה רכיב בציון ה-Smart Match. **"יש רכב"** — שער מותנה-מרחק (מעל 40 ק"מ
בלי רכב = פסילה), **לא מקדם-ניקוד**. **"שפות" אינו שער** — שדה-מידע רב-ערכי בלבד; לאירוע אחד יש
תפקידים שונים ("אולי סבבה מישהי שתהיה עם המדפסת ולא צריכה את השפה" — מילות-ישי), והמנהלת בוחרת
ידנית מי לזמן לכל תפקיד.

## ⑧ אין הכרעות-קלוד הפיכות למשטח הזה

*(סעיף מותנה — כל שדה והתנהגות בטופס נגזרים ישירות מ-`processes-approved.md §א1`; לא נדרשה שום
הכרעת-ביניים עצמאית כמו שנדרשה במסכים 1/3/4.)*

## 🏷️ דאטת-הדגמה מול נגזר-ממודל

**‏🌱 מהמסד:** אין — `hostesses` ריקה (0 שורות, נמדד).
**‏🎭 דמו מומצא:** כל שדה בדיאלוג (שם · טלפון · אימייל · כתובת · פרטי-בנק). **שני מספרי-הת"ז
(התקין והשגוי) הם המצאה-להדגמה בלבד, לא נבדקו מתמטית מול אלגוריתם ספרת-הביקורת האמיתי** —
זו החלטת-מימוש ל-`src/lib/`, לא למוקאפ. **חמש דוגמאות-השפות** (עברית·אנגלית·רוסית·ערבית·אמהרית)
— **אין `enum` בסכמה** (השדה `text[]` חופשי) ⇒ הצעת-כיווני-שוק, לא רשימה סגורה; הקלט האמיתי בבנייה.
**‏📐 נגזר ממודל:** שכר-מינימום `₪35` (`params.שכר_מינימום_שעתי`, לא מוקלד) · ברירת-מחדל `rating=3`
· מבנה-הדיאלוג עצמו (מ-`CustomerFormDialog.jsx` הקיים).

🛑 **עצירה — משטח 3ב, ממתין לאישורך.**

---

# ✅ משטח 3ג · חלון עריכת דיילת — **אושר 07/08/2026**

> **🖼️ `docs/mockups/hostesses-screen/approved/07_edit_hostess_approved.html`** — מוקאפ מאושר.
> **נפתח מ"מאגר דיילות" (מסך 3), לחיצה על עיפרון-העריכה.**
> **מקור התוכן:** `processes-approved.md §א1` (אותם שדות כמו 3ב) **+ `§א2`** (עדכון שוטף, הקפאת-
> תעריף) **+ `§א3`** (אי-זמינות) — שלושתם נקראו במלואם לפני הציור. **לא §א4 (השבתה)** — ר' ③.

## ① 🔴 מפת-לחיצות

| הרכיב | מה זה עושה |
|---|---|
| **שדה ת"ז** | 🚫 **לא לחיץ — נעול** *(כמו ח"פ ב-`CustomerFormDialog.jsx` במצב-עריכה)* |
| **שאר שדות-הטקסט/מספר** | קלט + ולידציה בעזיבת-השדה — זהה ל-3ב |
| **צ'יפי-שפות · מתג "יש רכב" · כוכבי-התרשמות** | בוחר — זהה ל-3ב |
| **"+ הוסף טווח"** (אי-זמינות) | 🔴 **מוסיף שורה לטופס בזיכרון בלבד — לא כותב ל-DB מיד** *(ר' ⑧①)* |
| **"הסר"** (על שורת-טווח) | מסיר מהטופס בזיכרון, לפני שמירה |
| **"שמור שינויים"** | 🔴 **מבצע** — כותב את כל הטופס **בפעולה אחת**, כולל טווחי-האי-זמינות |
| **"ביטול"** | **מנתב** — סוגר בלי לשמור, חוזר למסך 3 |

## ② ההחלטה האחת, ומי מקבל אותה

> **"מה השתנה אצל הדיילת הזו — ומה אני צריכה לתעד לפני שהיא שוב בתמונה."**

**מנהלת הגיוס.** שלוש הסיטואציות מ-`processes-approved.md §①` (הכרטיס המלא, מסך 3): דיילת
עדכנה פרטים, הודיעה על אי-זמינות, או עוזבת — **וכולן מתחילות משיחת טלפון**, אותו כלל כמו בהוספה.

## ③ 🔴 למה §א4 (השבתה) אינו כאן — הכרעת-קלוד הפיכה, לא השמטה

`next-session-prompt.md §4` הזהיר במפורש: **"אסור לצייר מתג-השבתה אחיד בלי הבדיקה"** *(האם יש
שיבוץ-עתידי-פעיל, לפי §א4)* — **אם** למשטח הזה יש בכלל פעולת-השבתה. **הבדיקה בוצעה, והתשובה:
אין.** מסך 3 (טבלת-המאגר) **כבר** נושא את מתג ההפעלה/השבתה **עם** חלון-האישור המלא של §א4
(אושר ותוקן 07/08/2026). ⇒ **שכפול הפעולה כאן היה יוצר שני מקומות שיכולים לסטות** — בדיוק מה
שכלל-המראה 🔗 קיים למנוע. **התג "פעילה" בכותרת כאן קריא-בלבד**, ומצביע בחזרה למסך 3 לפעולה.
🔑 **עוגן להחלטה, לא ניחוש:** אותו דפוס בדיוק כבר קיים במודול 2 — `src/modules/02_customers/
CLAUDE.md`: *"פח = הסרת שורה מטופס... רשומה קיימת מארכבים ולא מוחקים (ב-`CustomersPage.jsx`)"*
— כלומר גם שם ההשבתה/ארכוב חיים ב**רשימה**, לא בתוך דיאלוג-העריכה. **הפיך:** אם תעדיף מתג גם
כאן — זה שינוי-מוקאפ קטן, לא סתירה למשהו שכבר הוכרע.

## ④ מקור לכל שדה ומספר

זהה ל-3ב §④ לשדות המשותפים. ➕ **שכר שעתי בעריכה** — `hostesses.hourly_rate` (כתיבה חדשה),
**אך שיבוצים קיימים קוראים מ-`assignments.hourly_rate_snapshot`, שאינו נגזר מהעמודה הזו לאחר
השמירה** *(§א2: "הוקפא ברגע השיבוץ… ההעלאה תחול על השיבוץ הבא")* · **טווחי-אי-זמינות** — טבלה
חדשה `(hostess_id, מ-תאריך, עד-תאריך, הערה)`, **עדיין לא קיימת ב-DB** *(רשומה כ"חדש מהיום"
ב-`processes-approved.md`, `🔗` מראה ל-`db_roadmap.md`)*.

## ⑤ מצבים

**ת"ז שגוי / שכר מתחת-למינימום / אימייל כפול / כתובת לא-נגזרת-לקואורדינטות / שמירה נכשלה /
הצלחה** — **זהה למילה למסך 3ב §⑤**, 🔗 מראה, לא משוכפל. **טעינה** ⇒ **יש מצב-ביניים כאן,
בשונה מ-3ב** — הדיאלוג צריך לשלוף את השורה הקיימת לפני שהוא מציג ערכים; שלד-שדות עד שהיא חוזרת.

## ⑥ הרשאות

זהה לכל כרטיסי המודול: **גיוס = עריכה · מנכ"ל = עריכה · פרויקטים = צפייה** ⇒ **עיפרון-העריכה
אינו קיים לה כלל** במסך 3 *(אותו כלל שכבר קבע §⑥ שם — כפתור חסום אינו קיים, לא מכובה)*, ולכן
היא אף פעם לא מגיעה לדיאלוג הזה. **כספים · לקוחות · לוגיסטיקה = חסום.**

## ⑦ הגדרות שמאחורי מילה על המסך

🔗 **מראה** — "התרשמות" ≠ "דירוג" ושער-הרכב-40-ק"מ: זהים למילה ל-3ב §⑦, לא משוכפלים.
➕ **חדש כאן:** **"תעודת הזהות קבועה"** — לא הכרעת-מוצר מפורשת בתהליכים, אלא **עקביות עם §7.64**
(ת"ז → מפתח-סורוגייט) ועם הדפוס הקיים היחיד בקוד (ח"פ-לקוח נעול בעריכה) — ר' ⑧② לנימוק המלא.

## ⑧ שתי הכרעות-קלוד, הפיכות

**‏① השבתה נשארת בלעדית למסך 3, לא משוכפלת כאן** — הנימוק המלא ב-③ למעלה.
**‏② טווחי-אי-זמינות נצברים בזיכרון-הטופס ונכתבים רק עם "שמור שינויים" הכללי, לא מיידית בלחיצת
"+ הוסף טווח"** — `processes-approved.md §א3` **אינו** קובע בין השתיים; **הבחירה נסמכת על תקדים
קיים בקוד**, לא על ניחוש: `CustomerFormDialog.jsx`'s "אנשי קשר נוספים" הוא בדיוק אותה צורה
(רשימה-בתוך-טופס-עריכה) והיא **חוסכת-שינוי**, לא כותבת מיד. כתיבה-מיידית הייתה דורשת מנגנון-
ביטול נפרד ל"הסר" (מחיקה-מה-DB, לא רק מהטופס) — מורכבות שאין לה כיסוי בתהליכים. **הפיך במלואו.**

## 🏷️ דאטת-הדגמה מול נגזר-ממודל

**‏🌱 מהמסד:** אין — `hostesses` ריקה (0 שורות, נמדד); **טבלת-אי-זמינות אינה קיימת ב-DB בכלל
עדיין** (חדש-מהיום ב-`processes-approved.md`, לא נבנתה).
**‏🎭 דמו מומצא:** כל שדה, כולל טווח-האי-זמינות המוצג ("15/08–22/08 · חופשה בחו״ל").
**‏📐 נגזר ממודל:** שכר-מינימום `₪35` · נעילת-שדה-הת"ז (תקדים `CustomerFormDialog.jsx`) ·
תג-הסטטוס הקריא-בלבד (מקור `hostesses.status`, נכתב רק ממסך 3).

🛑 **עצירה — משטח 3ג, מתוך 8. אושר.**

---

# ✅ משטח 3ד · כרטיס דיילת לצפייה — **אושר 07/08/2026**

> **🖼️ `docs/mockups/hostesses-screen/approved/08_view_hostess_approved.html`** — מוקאפ מאושר.
> *(מצביע תוקן 08/08/2026 — הכותרת כבר אמרה ✅ אושר, אבל השורה הזאת עדיין הצביעה ל-`_DRAFT`.)*
> **המשטח האחרון מתוך שמונה.** **נפתח מ"מאגר דיילות" (מסך 3), לחיצה על שם הדיילת.**
> **מקור:** `processes-approved.md:42` (מצטט את `C5 §5.6.12`) + `§0` (הרשאות בנק/ת"ז) —
> **והרחבה ביוזמת-ישי היום** (שיבוצים-קרובים · עבדה-אצל-לפי-לקוח · לא-ענתה-ל-N · עבדה-לאחרונה
> · פאנל-העדפה), **כל חמשתם מעוגנים בשאילתות/נוסחאות שכבר קיימות ומאושרות במסכים 2/3 — ר' ⑧④.**

## ① 🔴 מפת-לחיצות

| הרכיב | מה זה עושה |
|---|---|
| **שם הדיילת** (בשורת מסך 3) | 🔴 **מנתב** — פותח כרטיס זה, **overlay על מסך 3, לא ניווט לעמוד** |
| **עיפרון ✎** (בכותרת הכרטיס) | **מנתב** — סוגר את הצפייה, פותח את מסך 3ג. **קיים רק להרשאת-עריכה** (ר' ⑥) |
| **✕** (בכותרת הכרטיס) | **מנתב** — סוגר, חוזר למסך 3 |
| **שאר התוכן** | 🚫 **לא לחיץ — תצוגה בלבד.** אין קישורים לאירועים/לקוחות מתוך היסטוריית-השיבוצים *(מחוץ להיקף; מסך-הפרויקטים שייך למודול 6)* |

## ② ההחלטה האחת, ומי מקבל אותה

> **"מי הדיילת הזו, ומה כדאי לי לדעת עליה לפני שאני מתקשרת אליה או משבצת אותה."**

**מנהלת הגיוס** *(ומנהלת-הפרויקטים, בגרסה מצומצמת — ר' ⑥)*. זה ההבדל בין טבלת-המאגר (סריקה
מהירה, סינון) לכרטיס הזה (תמונה מלאה על אדם אחד) — אותו יחס בדיוק כמו טבלת-לקוחות מול עמוד-לקוח
במודול 2.

## ③ הכרעת-הצלבה — לא ממצא חדש

`processes-approved.md:42` מצטט את C5: *"לחיצה על **העין** פותחת את הכרטיס המלא."* **אבל
`screens-approved.md` (כרטיס מסך 3, §①) כבר הכריע אחרת** — הטריגר הוא **שם הדיילת**, לא אייקון-עין
נפרד (וכך אכן בנוי המוקאפ המאושר, `03_repository_approved.html:166`). לפי היררכיית-האמת
(`CLAUDE.md` §היררכיית-מקורות) **האפיון המאושר של המודול גובר על C5 במה שהוא מכסה בפועל** — וזה
כבר מכוסה. **לא סתירה לפתור, רק תיעוד שהכרטיס הזה יורש את ההכרעה הקיימת ולא ממציא טריגר משלו.**

## ④ מקור לכל מספר ושדה

**התרשמות** — `hostesses.rating`, `int` (עקבי עם מסכים 2/3) · **שיעור-היענות** — אותה נוסחת
מונה/מכנה כמו הצ'יפ במסך 2 §③, **בלי הגבלה לאירוע ספציפי** · **יש רכב · שפות** — `hostesses.
has_car`/`languages` · **אירועים ברבעון האחרון** — אותו חלון כמו כרטיס מסך 3 §④ *(לא חלון חדש —
`§11.5`: "שני חלונות לאותו שם היו גורמים למסך לספר סיפור שונה")*.
➕ **שיבוצים קרובים** — `assignments ⋈ projects`, סינון לעתידי+פעיל, **אותה שאילתה בדיוק שחלון-
ההשבתה במסך 3 כבר מריץ** (`processes-approved.md §א4`) · **"עבדה אצל X — Nx"** — `assignments ⋈
projects.customer_id`, **אותו JOIN כמו הצ'יפ במסך 2**, מקובץ לפי לקוח במקום מסונן-לאירוע ·
**"לא ענתה ל-N" · "עבדה לאחרונה לפני X שבועות"** — `params.לא_ענתה_ל_N` ו-`max(projects.
final_event_date)`, **אותן שתי נוסחאות שכבר מחושבות למסך 2 §③**, ללא הגבלת-אירוע · **פאנל
העדפת-לקוחות** — טבלה שעדיין לא נכתבת (`🚧 מ6 ← מ4`, `PROJECT_MASTER §6` + `db_roadmap.md`).

## ⑤ מצבים

**טעינה** ⇒ שלד-כרטיס (רצועת-הדגשים + פרטים כקווים אפורים). **אין "ריק אמיתי"** — הכרטיס תמיד
נפתח על דיילת קיימת. **שגיאה** ⇒ בדיוק כמו מסכים 1/2/3: `hostesses`/`assignments`/`projects`
`deny-all` היום (נמדד) ⇒ הודעת-שגיאה מפורשת, לא כרטיס-חצי-מלא בשקט. **שיבוצים-קרובים ריק** ⇒
*"אין לה כרגע שיבוץ פעיל"* — בשורה נייטרלית, לא שגיאה. **היסטוריה ריקה** ⇒ *"דיילת חדשה, עדיין
אין לה היסטוריית שיבוצים"* — עקבי עם `טרם נצבר מידע` בציון (מסך 2).

## ⑥ הרשאות

**גיוס = עריכה · מנכ"ל = עריכה** ⇒ רואות את עיפרון-העריכה **וגם** את סעיף "פרטים עסקיים" (בנק+
ת"ז). **פרויקטים = צפייה** ⇒ 🔴 **הכרעת-קלוד הפיכה:** הכרטיס **נגיש לה** (היא כבר רואה את השם
בטבלת-המאגר, ר' מסך 3 §⑤ — "רואה בלי הכפתור הראשי", לא בלי השורה) — **אבל בלי עיפרון-העריכה
ובלי סעיף "פרטים עסקיים"**, לפי `processes-approved.md §0` המילולי: *"רק בתוך כרטיס הדיילת, ורק
למי שיש לה הרשאת עריכה"* — התנאי השני חל **בתוך** הכרטיס עצמו, לא רק על הגישה אליו. **כספים ·
לקוחות · לוגיסטיקה = חסום** — אינן מגיעות למסך 3 בכלל.

## ⑦ הגדרות שמאחורי מילה על המסך

**"פרטים עסקיים"** מכיל בדיוק בנק+ת"ז — לא "כל מה שלא פרטי-קשר"; זו הפרדה מכוונת שמשקפת את
`§0`. **"עבדה לאחרונה"** — אותו סף/מכפיל של מנוף-ההוגנות (8 שבועות, מסך 2 §🏷️), לא ניסוח חדש.

## ⑧ ארבע הכרעות-קלוד, הפיכות

**‏① ירושת-הטריגר מ-שם ולא מ-עין** — ר' ③ למעלה, לא הכרעה עצמאית.
**‏② הכרטיס נגיש למנהלת-פרויקטים בגרסה מצומצמת, לא חסום כליל** — נימוק ב-⑥. **הפיך:** אם תעדיף
חסימה מלאה לתפקיד-צפייה, זו עריכת-הרשאה בשורה אחת, לא סתירה למשהו שכבר נבנה.
**‏③ ארבע התוספות מהיום (שיבוצים-קרובים · עבדה-אצל-לקוח · לא-ענתה-ל-N · עבדה-לאחרונה) ופאנל-
העדפה** — הוכרעו בשיחה עם ישי היום, **כל אחת מעוגנת בשאילתה/נוסחה שכבר מאושרת במסכים 2/3**
(פירוט מלא ב-④) — לא חישוב חדש, רק ריכוז-מחדש בלי הגבלת-אירוע-בודד.
**‏④ מבנה-הכרטיס (רצועת-הדגשים + פרטים-מקובצים + היסטוריה-כרשימה) שאול מ-`CustomerDetailsPage.
jsx`** — היחיד ב-`src/` שהוא "כרטיס-צפייה מלא לישות". **אבל כ-overlay, לא כעמוד נפרד** — כי
`processes-approved.md` מצטט את C5 במפורש: *"באותו מסך"*, לא ניווט. שאלת "עמוד מול חלון" ש-
`CustomerDetailsPage.jsx`'s own header-comment דנה בה (LOCAL-13, "חלון לא מכיל 30 הצעות") **לא
חלה כאן באותו עוצמה** — למאגר של עד 50 דיילות, היסטוריית-שיבוצים בודדת לא מתקרבת לאותו היקף.

## 🏷️ דאטת-הדגמה מול נגזר-ממודל

**‏🌱 מהמסד:** אין — `hostesses`/`assignments` ריקות (0 שורות, נמדד).
**‏🎭 דמו מומצא:** כל שדה, כולל שלוש שורות ההיסטוריה/שיבוץ-פעיל וצ'יפ "עבדה לאחרונה לפני 3
שבועות". **הצ'יפ "לא ענתה ל-4 האחרונים" לא מוצג בכוונה** — הדיילת בדוגמה ענתה 9 מ-10, מתחת לסף
שמדליק אותו, בדיוק כמו שהוא לא מופיע לה במסך 2.
**‏📐 נגזר ממודל:** כל ארבע הנוסחאות ב-④ · מבנה-הכרטיס מ-`CustomerDetailsPage.jsx`.

🛑 **עצירה — משטח 3ד, מתוך 8. האחרון. אושר.**

---

# ✅ משטח 5 · דף ציבורי לאישור/סירוב משמרת — **אושר 07/08/2026**

> **🖼️ `docs/mockups/hostesses-screen/approved/05_public_confirm_approved.html`** — **מוקאפ מאושר.**
>
> ⚠️ **סימון שתוקן 08/08/2026, ומדובר באדווה-שנעצרה ולא בשינוי-החלטה.** הכותרת כאן אמרה
> *"🔶 טיוטה, ממתין לאישור-ישי"* והקובץ נשא `_DRAFT`, **בעוד שלושה מקורות אחרים אמרו שאושר:**
> טבלת-"מצב" בראש הקובץ (`✅ אושר 07/08/2026`) · `next-session-prompt.md §1` (*"8/8 משטחים
> מאושרים"*) · **וכותרת הקומיט `548eaa8` עצמה** — *"משטח 5 … אופיין **ואושר**"*.
> **הוצג לישי מחדש ב-08/08 והכריע: *"נראלי צריך להעביר לתקייה המתאימה"*** ⇒ הקובץ הועבר.
> 🔑 **והלקח, כי הוא בדיוק הדפוס שהמודול הזה כבר נכווה בו:** ‏**אישור שלא הגיע לכל שלושת
> המקומות (טבלה · כותרת · שם-קובץ) נראה זהה לאי-אישור** — וסשן טרי היה מדלג על המשטח היחיד
> שכותב ל-DB בלי התחברות.
>
> 🔴 **זה לא "מסך תשיעי" — זה המשטח היחיד שאינו חלק מהאפליקציה הפנימית.** אין סרגל-צד, אין
> התחברות, אין תפקיד. מי שרואה אותו הוא **דיילת בטלפון שלה**, שקיבלה קישור במייל (`ב3` ב-
> `processes-approved.md`) ולוחצת עליו. **זה גם הנתיב היחיד במערכת שכותב ל-DB בלי אימות-זהות**
> *(מוקש-בנייה שכבר סומן ב-`PROJECT_MASTER §7.45`)* — ולכן הכרטיס הזה נושא סעיף שאין לאף
> משטח אחר: **③ מודל-האבטחה**, לא רק ① מפת-לחיצות.
>
> **מקור התוכן:** `processes-approved.md §ב3–ב4` (הזרימה כבר אושרה) + `PROJECT_MASTER §7.45`
> (מודל-הטוקן, נסגר 07/07; **מי-כותב-מה נסגר היום, 07/08 — ר' ③**).

## ① 🔴 מפת-לחיצות

| הרכיב | מה זה עושה | כותב ל-`assignments`? |
|---|---|:-:|
| **"אני מגיעה"** | 🔴 **מבצע** — קורא לפונקציית-DB ציבורית (מאמתת את הטוקן) שכותבת `confirmed_available` + `responded_at` **על שורת-השיבוץ התואמת בלבד**, ורק אם הסטטוס עדיין `pending` | ✅ *(שדה-ביניים בלבד — ר' ③)* |
| **"לא אוכל הפעם"** | 🔴 **מבצע** — אותה פונקציה, כותבת `declined` + `responded_at`, באותו תנאי | ✅ *(שדה-ביניים בלבד)* |
| *(שום דבר אחר בעמוד אינו לחיץ)* | אין ניווט, אין התחברות, אין עריכת-פרטים, אין הצגת נתוני-שיבוצים אחרים | — |

🚫 **ואין כפתור "שני" אחרי שלחצה.** ברגע שנרשמה תשובה, שני הכפתורים נעלמים ומוצג מסך-תודה
*(ר' ⑤)*. **חזרה-בה היא שיחת-טלפון למנהלת, לא לחיצה נוספת בקישור** — בדיוק כפי שכבר הוכרע
ב-`processes-approved.md §ב4` ("מלכודת שכבר פתורה"): "המנהלת מעבירה ל-`סירבה`" הוא הנתיב
היחיד לשינוי-דעת, ולכן העמוד הציבורי לא צריך לתמוך בו.

## ② ההחלטה האחת, ומי מקבל אותה

> **"אני יכולה להגיע למשמרת הזו — כן או לא, בלי להתקין שום דבר ובלי סיסמה."**

**הדיילת — לגבי הזמינות שלה בלבד.** 🔑 **וזו בדיוק הסיבה שהמשטח קיים:** לדיילת אין אפליקציה
ואין חשבון *(`0 · מי המשתמשת` ב-`processes-approved.md` — כל שאר המודול בנוי סביב מנהלת-הגיוס
דווקא כי לדיילת אין כניסה למערכת)*. **ההחלטה הסופית-המחייבת** — האם היא אכן משובצת —
**נשארת אצל המנהלת** (מסך 4, `אשר סופית ושלח פרטים`). המשטח הזה הוא רק הערוץ שדרכו מגיע אליה
המידע הגולמי לקבל את ההחלטה הזאת.

## ③ 🔴 מודל-האבטחה — מי כותב, לאילו שורות, ובאילו תנאים

**נסגר היום (ישי, 07/08/2026):** ✅ **מודל-ביניים** — ר' `PROJECT_MASTER §7.45↳`. הקליק כותב
ישירות ל-`assignments`, אבל **רק** לשדה-ביניים; הקביעה המחייבת נשארת ביד אדם. פירוט המנגנון:

**מי כותב:** לא הדפדפן ישירות לטבלה — **פונקציית-DB ייעודית** (`SECURITY DEFINER`, ניתנת
להרצה למשתמש אנונימי `anon`, **באותה משפחה** כמו פונקציות-הנעילה של מודול 1 שכבר קיימות
ומתועדות ב-`db_roadmap.md §0.1`). ‏**‏`assignments` עצמה נשארת `deny-all` לגמרי** — אין
policy שמאפשרת לאף אחד, כולל `anon`, לקרוא או לכתוב ישירות לטבלה. **הפונקציה היא הנתיב היחיד.**

**לאילו שורות:** **שורה אחת בלבד** — זו שה-`invite_token` שבכתובת תואם אליה. הפונקציה
מקבלת **רק** את הטוקן (מהכתובת) ואת הבחירה (`מגיעה`/`לא מגיעה`) — שום פרמטר אחר, ולכן אין
דרך לבקש שורה של דיילת אחרת או שיבוץ אחר.

**לאילו שדות:** **שניים בלבד** — `confirmed_available`/`declined` (השדה עצמו, כפי שכבר קיים
בטבלה) ו-`responded_at`. **לא** `finally_approved`, **לא** שום שדה אחר בשורה — הפונקציה
פיזית אינה יודעת לכתוב לשום עמודה מלבד השתיים האלה.

**באילו תנאים** *(שלושתם נבדקים בתוך הפונקציה, לפני כתיבה — לא ב-JS בדפדפן)*:
1. **הטוקן קיים ותקין** — אחרת: הודעת-שגיאה **גנרית** ("קישור לא תקין"), **בלי לגלות אם הטוקן
   כמעט-נכון או לא-קיים בכלל** *(מונע ניחוש-טוקנים בהדרגה)*.
2. **הטוקן עדיין בתוקף** — לא עברו 48 שעות מ-`invite_sent_at`, ולא נותרו פחות מ-24 שעות
   לאירוע *(שני משני תנאי-הפקיעה של §7.45; השלישי — "המשרה אוישה" — נבדק בתנאי 3)*.
3. **הסטטוס הנוכחי הוא `pending`** — **לא מספיק שהטוקן טרי; הפונקציה בודקת גם את מצב-השורה
   עצמה.** 🔑 **וזה התנאי שמונע את המרוץ הכי מזיק:** אם המנהלת כבר עדכנה ידנית (`אושרה סופית`,
   `שוחררה`, או אישרה זמינות בטלפון) בזמן שהדיילת עדיין מחזיקה את אותו קישור פתוח בטלפון —
   הקליק המאוחר נכשל בשקט-מבוקר ומציג את המסך המתאים (⑤), **ולא דורס את מה שהמנהלת כבר קבעה.**

**מה זה מונע, בפועל:** דיילת (או כל מי שמנחש/מיירט טוקן) לא יכולה לקרוא שם של דיילת אחרת,
לקרוא פרטי-שיבוץ של אירוע אחר, לשנות סטטוס-שיבוץ סופי, או "לדרוס" החלטת-מנהלת. **משטח-הפגיעה
מוגבל לביט אחד, בשורה אחת, בתנאי אחד.**

⚠️ **מה שנשאר מחוץ להיקף-הכתיבה הזו, ונרשם ב-④/db_roadmap:** אורך-הטוקן ואקראיותו (חייב
להיות ארוך ואקראי-קריפטוגרפית — **לא** מספר סידורי; אחרת ניחוש-טוקנים הוא רק שאלה של זמן).
זו החלטת-מימוש (לא-נדרשת-הכרעת-ישי), אבל **חייבת** להירשם כדרישה מפורשת לפני שנכתבת המיגרציה
— ר' `db_roadmap.md`.

## ④ מקור לכל פרט על המסך

**שם הדיילת · שם האירוע · תאריך · שעות · מיקום · תעריף + נסיעות** — **בדיוק אותם שדות
שכבר במייל** (`processes-approved.md §ב3`), נשלפים **מהשורה שהטוקן מצביע עליה** ⋈ `projects`.
**אין מספרים מחושבים בעמוד הזה** *(לא ציון, לא השוואה לדיילות אחרות)* — הוא מציג עובדות
בלבד, לא ניתוח.

## ⑤ מצבים — ושישה מהם, לא רק "הצליח/נכשל"

*(תמונה מצליחה-בלבד היא בדיוק המלכודת שסעיף-המבנה של הקובץ הזה נועד למנוע — ⑥/⑦ שם.)*

| המצב | מתי | מה רואה הדיילת |
|---|---|---|
| **טעינה** | תמיד, לרגע, בזמן אימות הטוקן | שלד-עמוד, בלי הבטחה מוקדמת |
| **ממתין למענה** | טוקן תקין, שורה `pending` | פרטי האירוע + שני הכפתורים |
| **תודה — אישרה** | לחצה "אני מגיעה" (הפעם הזאת או בעבר) | "תודה! רשמנו שאת מגיעה. פרטים סופיים יישלחו בקרוב." — **בלי** הכפתורים |
| **תודה — סירבה** | לחצה "לא אוכל הפעם" | "תודה שעדכנת. נשמח לפנות אלייך בפעם הבאה." |
| 🔴 **המשרה כבר אוישה** | ניסתה לאשר אחרי ששוחררה (§7.33) | **הנוסח כבר הוכרע** ב-`processes-approved.md §ב6`: *"תודה שהתפנית — המשרה כבר אוישה לאירוע הזה. נשמח לפנות אלייך בפעם הבאה."* — 🔗 מראה, לא ניסוח חדש |
| **הקישור אינו בתוקף** | 48 שעות מהשליחה **או** פחות מ-24 שעות לאירוע | "הקישור הזה אינו בתוקף יותר. אם עדיין מתאים לך — התקשרי למנהלת הגיוס." *(לא מפרט **איזה** משני התנאים — לדיילת זה אותו הבדל: מאוחר מדי לקישור)* |
| **קישור לא תקין** | טוקן שגוי/מיושן-שנמחק/מטופל-בזדון | הודעה גנרית זהה לתנאי-1 ב-③ — **בכוונה לא מבחינה** בין "לא קיים" ל"כבר נענה במקום אחר" |
| **שגיאת-רשת/שרת** | הפונקציה נכשלה מסיבה טכנית | "לא הצלחנו לשמור את התשובה. נסי שוב, או התקשרי למנהלת." — **לעולם לא "נשמר" כשלא נשמר** *(אותו כלל שכבר הוכרע במסך 4 §⑤)* |

## ⑥ הרשאות

**אין הרשאות במובן הרגיל — וזה בכוונה, לא פער.** זה הנתיב היחיד במערכת בלי תפקיד ובלי
התחברות (`§7.45`). **מה שממלא את מקום ההרשאה:** הטוקן עצמו (ארוך, אקראי, פר-שיבוץ), פקיעה
משולשת, וההיקף המצומצם של ③ — לא "מי מותר לו", אלא "מה בכלל אפשרי לעשות".

## ⑦ הגדרות שמאחורי מילה על המסך

**"אינו בתוקף"** מכסה **שניים** משלושת תנאי-הפקיעה של §7.45 *(48 שעות · 24 שעות-לפני-אירוע)*
**בניסוח אחד ומכוון** — לדיילת אין ערך במידע *איזה* משניהם קרה. **"המשרה כבר אוישה"** מכסה
את התנאי השלישי **בנפרד**, כי הנוסח שלו כבר קיים ואושר (`§ב6`) ומספר משהו שונה: לא "פספסת
זמן", אלא "תודה, כבר לא צריך". ⚠️ **ואם התנאי השלישי מתרחש בעודה מציגה שורה שכבר "ממתין
למענה" בטלפון שלה** *(פתחה את הקישור, לא לחצה עדיין, המשרה התמלאה בינתיים)* — הקליק שלה
מגיע לתנאי 3 ב-③ ומציג את מסך "כבר אוישה", **לא שגיאה סתמית.**

## 🏷️ דאטת-הדגמה מול נגזר-ממודל

**‏🌱 מהמסד** *(`scripts/demo-seed.mjs`, כמו שאר כרטיסי-המודול)*: `כנס לקוחות שנתי` ·
`מדיטק פתרונות בע"מ` · `אקספו תל אביב, ביתן 2` · `18:00–22:00`.
**‏🎭 דמו מומצא — וזה כל התוכן שהדיילת רואה:** שם הדיילת (`נועה שגיא`, **אין מקור** — המאגר
ריק) · שעת-השליחה והזמן-שנותר. ⚠️ **בכוונה נכתבו יחסית ("נשלח לפני 6 שעות", "נותרו 42 שעות")
ולא כתאריך-קבוע** — הלקח מ-`§📐 מבנה-הכרטיס` למעלה: מוקאפ קודם נתקע על `06/08` בעוד זימון
הוצג כ-`28/04`. **זמן-יחסי אינו יכול להסתור מול "היום" של אף מוקאפ אחר.**
**‏📐 נגזר ממודל:** שלושת תנאי-הפקיעה · שני השדות שהפונקציה כן כותבת · ניסוח "כבר אוישה"
(מ-`§ב6`, מראה לא כפילות).

🛑 **עצירה — משטח 5, ממתין לאישורך.**

</div>
