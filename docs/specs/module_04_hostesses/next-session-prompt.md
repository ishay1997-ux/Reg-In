<div dir="rtl">

# מסמך-מצב — סוף סשן 07/08/2026 17:53

> **קורא:** סשן טרי, בלי גישה לשיחה שהפיקה את המסמך. **הדבק את כל מה שמתחת לקו.**
> מבנה שש-הקטגוריות לפי הכתבת-ישי (08/08/2026) — כל דבר פעם אחת, בלי ערבוב.

---

## 1 · מה נמדד היום — עם הפקודה או הקובץ שהוכיח

- **‏8/8 משטחי מודול 4 מאושרים.** ‏`screens-approved.md` טבלת-"מצב" — כל שמונה נושאים ✅ ומצביעים
  לקובץ ב-`docs/mockups/hostesses-screen/approved/`. שלושת האחרונים נבנו ואושרו היום: 3ב
  (`06_add_hostess_approved.html`) · 3ג (`07_edit_hostess_approved.html`) · 3ד
  (`08_view_hostess_approved.html`).
- **סקירת placeholder על כל 8 המוקאפים** (סוכן, הקשר-טרי): **40 מופעי `<input>`** נבדקו; רק
  שלושת שדות טווח-האי-זמינות ב-3ג היו ריקים בלי placeholder — **תוקנו, ואין מופע נוסף במודול.**
- **חילוץ `StatTile` אומת בשלושה שערים:** `npx eslint <3 קבצים>` ⇒ `exit=0` · `npx vite build`
  ⇒ `exit=0` · `npx vitest run` ⇒ **413 בדיקות עוברות, 12 קבצים.**
- **שלושת ה-`data-testid` שבדיקות ה-E2E נשענות עליהן נשמרו** — `grep` על `e2e/` מצא
  `metric-revenue` · `metric-open` · `metric-avg-deal` ב-`e2e/customer-page.spec.js`, וכולם
  עדיין מוזרמים ל-`StatTile` דרך `testId`.
- **נרטיב `docs/CLAUDE_CODE_LOG.md` = 2010 שורות** מול יעד ~150 (‏`awk` בין `## Session Log`
  ל-`## Reference`, הפקודה שכתובה בראש הקובץ עצמו). **חוב קודם, לא מהיום.**
- **מצביע התוכנית:** ‏`docs/plans/two-weeks-2026-08-06.md` — מצביע כבר היה קיים
  ב-`prompt_module_discovery.md`; **נוסף שני** ב-`docs/PROJECT_MASTER.md` §1. אומת בהרצה ידנית
  של בדיקת-ההוק על חמשת קובצי-התקן.

## 2 · מה רק דווח או הונח — ולא נבדק

- 🔴 **שני שינויי-הקוד לא אומתו ויזואלית בדפדפן.** ‏`QuotesPage.jsx` ו-`CustomerDetailsPage.jsx`
  עברו lint/build/יחידה בלבד — **אף אחד לא ראה את המסכים אחרי השינוי.** ‏**מ3 השתנה גם
  ויזואלית**: גודל-הערך 22px⇒20px, וסדר תווית/ערך התהפך. **טעון אימות עין.**
- **המוקאפים 3ג/3ד לא צולמו** — ה-Browser pane לא היה מוצג, ואומתו דרך `get_page_text` בלבד.
  3ד **כן** צולם אחרי תיקון-הצבע.
- **‏`docs/plans/two-weeks-2026-08-06.md` נערך ע"י סשן מקביל** (‏`git log`/mtime) — **תוכנו לא
  נקרא מחדש בסשן הזה.** טבלת-המצב שבו עשויה להיות מעודכנת מעבר למה שמסמך זה יודע.

## 3 · מה הוכרע על ידי ישי — ומה עוד ממתין לו

**הוכרע:**
- שמונת המוקאפים — כולם אושרו במפורש.
- **כלל-המילוי** נכתב ל-`PROJECT_MASTER §4`: מילוי-צבע שמור לפעולה-ראשית/אזהרה; עובדה ניטרלית
  או חיובית = לבן+מסגרת, **גם אם היא "עובדה טובה"**.
- **חילוץ `StatTile`** — לבצע. **הצורה הקנונית: תווית מעל הערך** (מ2 הוא מודול-הייחוס; מ3 יושר).
- **כלל "קרא `processes-approved.md` במלואו לפני אישור-כרטיס"** — הוכלל מלקח-נקודתי לכלל-קבע
  בסעיף 📐 של `screens-approved.md`.
- **לא לתקן רטרואקטיבית** ציטוטי מספרי-שורה שנכתבו לפני עדכון כלל-1 — קדימה בלבד.
- **מ4 3ב נבנה בסשן הזה** ולא בסשן טרי — הכרעה שגברה על ההמלצה.

**ממתין לו:**
- **השכבה המכנית של `StatTile`** — ~5 שורות ל-`scripts/check-docs-structure.mjs` שיאתרו
  אריח-מדד שנכתב ידנית מחוץ לרכיב. **הומלץ, לא בוצע.** בלעדיה מה שיש הוא משמעת ולא שער.
- **שתי שאלות-השורש** — הוצע לכתוב אותן ל-`src/CLAUDE.md` מעל חמשת-המעברים (האם ברור לקורא-קר ·
  האם זה כבר קיים במערכת). **הוצע, לא בוצע.**
- 🔴 **הכרעה שממתינה בדיוק בצעד הבא: להעביר את `docs/specs/module_04_hostesses/module4_smart_match_research.md` לתיקיית-
  המודול יחד עם שמירת `spec.md`?** ההמלצה הכתובה אומרת "בשלב ז, כפעולה אחת" — **והסשן הבא הוא
  שלב ז.** ‏**32 אזכורים ב-12 קבצים** ⇒ **לא ניקיון, ולא להעביר לבד.** *(הפירוט: §🔦 פריט ② למטה.)*
- 🔴 **והכרעה שנייה, לפני בלוק ④ (לא לפני `spec.md`): האם C5/C6 לבדם מספיקים כעוגן-בקרה
  בלתי-תלוי למודול 4?** ‏`module-blueprint/template.md` דורש עוגן מ-"C5/C6 + המוקאפים המקוריים"
  **כי `docs/specs/` נכתב ע"י קלוד** — ⚠️ **אבל אותו נימוק פוסל גם את שמונת המוקאפים המאושרים
  (קלוד כתב אותם 07/08)**, והישנים מיועדים ל-`superseded/` ומסומנים "חלק מפרטיהם שגויים".
  ⇒ **העוגן מצטמצם ל-C5/C6 בלבד — הנחה שלא נבדקה.** *(תויק כ-`4.7②` בתוכנית-השבועיים.)*
- **שלושת ממצאי-הבלופרינט (`4.7` בתוכנית-השבועיים)** — נקראו ותויקו 07/08, **כולם המלצות בלבד**,
  ומסומנים 🚫 **אל-תמחק עד שגם ④ וגם 6.0 הסתיימו.** שם גם **רשימת-קריאה מומלצת (8 קבצים)**
  למי שיריץ את ④.

## 4 · מה בוטל היום — ואסור שיחזור

- **מילוי-צבע על אריח-מדד** — בוטל בשני המסכים החיים ובכל מוקאפי מ4. **אסור להחזיר** מילוי
  טורקיז/ירוק/ענבר לאריח שמציג עובדה.
- **הגדרה מקומית של אריח-מדד** — `Highlight` נמחק מ-`CustomerDetailsPage.jsx`, והאריחים ה-inline
  נמחקו מ-`QuotesPage.jsx`. **אסור להחזיר הגדרה מקומית** — הכול דרך `@/components/StatTile`.
- **סדר ערך-מעל-תווית** (הצורה שהייתה במ3) — בוטל כצורה קנונית.

## 5 · מה לא הצלחתי להכריע — ואיך אפשר לאמת

**אין הכרעה שנשארה תלויה.** ‏**הפריט היחיד שדורש אימות** הוא §2 שלמעלה: המראה בפועל של שני
המסכים אחרי חילוץ `StatTile`. **איך לאמת:** להיכנס לאפליקציה (‏`npm run dev`, פורט 5173),
לפתוח `/quotes` ואת עמוד-לקוח כלשהו, ולוודא ששני האריחים בכל מסך נראים זהים בצורתם.

## 6 · מה עשיתי שלא התבקש במפורש — ולמה חשבתי שזה נדרש

- **הוספתי "מעבר חמישי" ל-`src/CLAUDE.md`** (שדה-קלט ריק חייב placeholder) — כי ישי שאל "איך
  התפספס", והתשובה הכנה הייתה שאף מעבר קיים לא בדק את זה. תיקון-נקודתי לבד היה משאיר את
  הפער פתוח למסך הבא.
- **שיגרתי סוכן שיסרוק את כל 8 המוקאפים** — כי הוא שאל "איך אני יודע שאין עוד", וזו שאלה
  שדורשת מדידה, לא הרגעה.
- **הוספתי מצביע-תוכנית שני ב-`PROJECT_MASTER.md`** — ההוק חסם, המצביע הקיים כבר עמד בתנאי,
  ומצביע יתיר בקובץ **אחר** היה בטוח יותר מלגעת בתוכנית שסשן מקביל עורך.
- **הוספתי `data-testid` לשני אריחי מ3** (לא היו קודם) — כדי שהאריחים האלה יהיו נגישים
  לבדיקה עתידית כמו אלה של מ2.

---

## ➡️ הצעד הבא היחיד

**מה:** ‏**כתיבת `docs/specs/module_04_hostesses/spec.md`** — הקובץ היחיד שחסר כדי לסגור את
בלוק ③ בתוכנית-השבועיים. **פעולה: כתיבה.**
**הפלט:** קובץ אחד, ותוכנו **חמישה דברים בלבד** (הכרעת-ישי 07/08, ר' `discovery-log.md`):
מצביע לשלושת מקורות-האמת · פסקת-פתיחה · **ארבעת פריטי-החוזה** · "מה ייחשב עובד" · "מה הוכרע
מול C5". 🚫 **בלי 12 פרקים, בלי APA, בלי גוף שלישי** — בוטל 06/08.
🔴 **ואת פרק "מה אסור לבלופרינט לנחש" לא כותבים מחדש** — הוא כבר קיים כטיוטה בת ~84 שורות
בתוך `processes-approved.md`. **מעתיקים.**

**קבצים שידוע שצריך לקרוא — נקודת-פתיחה, לא גבול:**
- `docs/plans/two-weeks-2026-08-06.md` בלוק ③ (‏3.3 · 3.4) — **הגדרת ארבעת פריטי-החוזה**
- `docs/specs/module_04_hostesses/processes-approved.md` — במלואו, וב-**פרק 12** יושבת הטיוטה
- `docs/specs/module_04_hostesses/screens-approved.md` — שמונת הכרטיסים
- `docs/specs/module_04_hostesses/discovery-log.md` — ה"למה" מאחורי ההכרעות
- `docs/CLAUDE.md` — מה `spec.md` אמור להיות לפי הקונבנציה

**קבצים שאיני בטוח לגביהם:** ‏`world-sources.md` *(סומן פעם כ"אמינות-נמוכה, לא שווה את הזמן")*.
➕ **‏`docs/specs/module_04_hostesses/module4_smart_match_research.md` §11 עבר מ"לא בטוח" ל"חובה"** — ר' ההכרעה למטה.

### ✅ הכרעת-ישי 07/08/2026 — הגרסה הצרה אושרה

**`spec.md` כן נכתב, אבל צר:** מצביעים + ארבעת פריטי-החוזה. **אין לשכתב דבר** — שלושת הפריטים
הראשונים הם **ריכוז** של מה שכבר קיים בחמשת הקבצים.
🔴 **והעבודה האמיתית היא פריט 3 בלבד: "לפחות מספר אחד בר-בדיקה."**
**למה דווקא הוא, והעוגן מדוד:** ‏`6,319` הופיע **פעם אחת** באפיון של מודול 3 ⇐ **33 פעמים**
במדריך-הבנייה שלו ⇐ ומשם ל**עוגן-רגרסיה קבוע ב-`e2e/smoke-anchors.json`**. ‏**למודול 4 אין
מספר כזה, ובלי שייווצר כאן — השרשרת לא נוצרת**, למודול הכי עתיר-נוסחאות בפרויקט.
**מה זה אומר בפועל:** לבנות **מקרה Smart Match מלא** — אירוע אחד, מועמדות עם ערכים אמיתיים,
והציון מחושב **ביד** לפי הנוסחאות ב-`module4_smart_match_research.md §11` — כך שסשן-הבנייה
יוכל לאמת את הקוד מול מספר, לא מול פרוזה. ⏱️ **הערכה: חצי סשן.**

🔴 **וחידוד שהוכרע מיד אחרי (שאלת-ישי 07/08): פריט 3 אינו "רשימת בדיקות" — הוא הציפייה שהבדיקה
נבדקת מולה. 🚫 אין לרשום ב-`spec.md` אילו בדיקות לממש.** ‏**הבלופרינט כבר יודע** — פורמט-הצעד
שלו הוא `Goal / Files / What / **Verify**`, כלומר אימות פר-צעד הוא תפקידו; רשימת-בדיקות באפיון
היא גם כפילות וגם דבר שירקב. 🔑 **וההבחנה נושאת משקל:** סשן-בלופרינט **יכול** להמציא בדיקה,
אבל **אינו יכול** להמציא את הציון הנכון — זה דורש לקרוא את הנוסחאות ולחשב. ואם הוא יחשב אותו
בעצמו, הבדיקה תשווה את הקוד **לעצמו** — בדיוק "שומר שאינו שומר".
⇒ **הגבול: האפיון מספק ציפייה אחת מחושבת-ביד · הבלופרינט בונה ממנה את הבדיקה.**

**מה חוסם: אין חוסם.** ההכרעה ניתנה; הצעד הבא מוגדר במלואו.

### 📍 איזה פרומפט-דיסקברי לקרוא — ואיפה בדיוק בתוכו

**קרא: `docs/guides/prompt_module_discovery.md`, ובתוכו `## שלב 3 · אינטגרציה` — הכותרת שאומרת
מפורשות "בסופו נכתב `spec.md`".** ‏זהו **הפרומפט היחיד**; שלבים 1 ו-2 (תהליכים · מסכים) **כבר
בוצעו במלואם** ואין לחזור עליהם.
✅ **נבדק ונסגר 07/08 — החשד של ישי היה נכון.** תוכנית-השבועיים (§3.2) הפנתה ל-
`docs/claude mega plans/regin_workflow_short_updated.md` שורות 194–843 כ"הפרומפט הקיים" —
**ושם יושבת גרסה ישנה במבנה של שבעה שלבים (א–ז)**, בעוד הפרומפט קוצר ב-07/08 ל**שלושה**.
**המצביע ב-§3.2 תוקן.** 🚫 **והמגה-פלאן לא נמחק ולא שוכתב** — הוא נושא גם את שלבים 4–7 של
תוכנית-העל, שבלוקים ④/⑤/⑥ מצטטים.
🔴 **ומוקש-תרגום שנובע מזה, וחשוב לך ישירות:** כל אזכור **"שלב ז"** בריפו — כולל ההמלצה להעביר
את `module4_smart_match_research.md` *"בשלב ז, יחד עם `spec.md`"* — הוא **המספור הישן**.
**בפרומפט הנוכחי זה `שלב 3`, כלומר בדיוק המשימה שלך עכשיו.**

### 🛡️ `spec.md` הוא **שער-בקרה**, לא רק תוצר *(הכרעת-ישי 07/08/2026)*

🔴 **וזו ההוראה שמשנה איך כותבים אותו.** ‏**זו הבקרה האחרונה לפני הבלופרינט**, ומרגע שהוא רץ —
כל מה שנשאר סותר או חסר הופך לקוד. ⇒ **כתיבת `spec.md` כוללת הצלבה פעילה של חמשת הקבצים זה
מול זה, ודיווח סתירות — לא רק ריכוז.** *(העוגן: אותה שיטה בדיוק תפסה **תשעה** פערי-כיסוי
ב-07/08 בין `processes-approved.md` לכרטיסי-המסכים — כולם "הוכרע-כבר-במקום-אחר", אף אחד לא
נראה לעין בקריאה רגילה.)*
✅ **והבקרה הזאת כבר אינה נתונה לפרשנות — היא נכתבה כרשימה סגורה של חמש הצלבות** לתוך
`prompt_module_discovery.md`, בשלב 3, תחת הכותרת **"🛡️ בקרת-הצלבה"** *(הכרעת-ישי 07/08: "זה
נתון לפרשנות, כל אחד יעשה בקרה מסוג אחר וככה דברים יתפספסו")*. **קרא אותה שם — אל תמציא בקרה
משלך.** בקצרה: תווית-מול-תווית · מספר-מול-מספר · **הכרעה-בלי-ביטוי-במסך** (זו שהניבה תשעה
ממצאים) · שם-עמודה מול `schema.sql` · דרישת-מסד מול `db_roadmap` **בגריפ, לא לפי הצהרה**.
🚫 **ומה לא לעשות עם ממצא:** לא לתקן בשקט ולא להכריע לבד — **לדווח לישי, ולתקן באישור** (כלל 1).

### 📋 ארבעת פריטי-החוזה — בקצרה כאן, כדי שלא תלוי בקובץ אחר

*(ההגדרה המלאה: `two-weeks-2026-08-06.md` §3.4. משוכפל כאן **בקצרה בכוונה** — הפניה יחידה
לקובץ-תוכנית שכבר נמדד כמיושן היא נקודת-כשל.)*
**‏① אוצר-מילים נעול** — טבלה אחת: כל סטטוס · כל ישות · **כל תווית על מסך**, מילה-במילה.
**‏② סדר מוכרע** — מי מתחיל · מה נשמר · מה קורה לפני מה.
**‏③ לפחות מספר אחד בר-בדיקה** — **זה העיקר, ר' למעלה.**
**‏④ שורת חוצה-מודולים** — מה מ4 קורא/כותב בטבלאות של מודולים אחרים, **בשם-העמודה המדויק,
מאומת מול `docs/schema.sql`**.

### 🔦 שלושה דברים שישי לא היה מודע להם — נמדדו 07/08 בשאלת-נקודות-עיוורון

**‏① `spec.md` נכתב ב-עברית. לא באנגלית.** ‏`docs/CLAUDE.md` נוקב מילולית:
*"`spec.md` (**עברית — המסמך של ישי**) ו-`discovery-log.md` (**אנגלית — הראיות**)"*.
⚠️ **וזה בדיוק מה שסשן עלול להחמיץ** — ברירת-המחדל בריפו הזה לתוצרים-ל-Claude היא אנגלית
(‏`micro_guides/` · `CLAUDE_CODE_LOG` · הסקילים), ו-`spec.md` הוא **החריג**.

**‏② 🔴 `docs/specs/module_04_hostesses/module4_smart_match_research.md` עדיין יושב מחוץ לתיקיית-המודול — וההמלצה הייתה
להעביר אותו בדיוק בשלב הזה.** ‏`processes-approved.md` (סעיף "שלושה דברים שנרשמים כדי שלא יאבדו",
פריט 2) קובע: *"המלצת קלוד: להעביר — **אבל בשלב ז, יחד עם שמירת `spec.md`, כפעולה אחת**"*, ומודד
**32 אזכורים ב-12 קבצים** *(שניים מהם אסור לגעת בהם — הארכיון ורשומות-יומן מתוארכות)*.
⇒ **הסשן הבא הוא שלב ז.** ‏**זו החלטה שממתינה בדיוק ברגע הזה, ואיש מאיתנו לא הזכיר אותה.**
🚫 **ולא להעביר לבד** — 32 אזכורים אינם פעולת-ניקיון, וזו הכרעת-ישי.

**‏③ ‏`discovery-log.md` אינו נושא רשומות ל-3ב/3ג/3ד.** ‏`grep` על שלושת השמות ⇒ **2 מופעים
בלבד**, ושניהם מצביעי-המשך ולא רשומות. **הנימוקים המלאים של שלושת המסכים חיים בכרטיסים עצמם
(‏`screens-approved.md`, סעיפי ⑧) וב-`docs/CLAUDE_CODE_LOG.md`** — לא במקום שהקונבנציה מייעדת להם.
⚠️ **המשמעות לשלב-הבקרה:** מי שיצליב ויחפש את ה"למה" ב-`discovery-log` **לא ימצא אותו שם** לשלושת
האחרונים — ויסיק בטעות שהם לא נומקו. **לחפש בכרטיסים.**

### 🗺️ ומה בא אחרי `spec.md` — כדי שלא תצטרך לחפש

‏`spec.md` סוגר את **בלוק ③** בתוכנית-השבועיים. אחריו, לפי הטבלה שם:
**‏④ התאמת `module-blueprint`** — 🔨 **חצי-בוצע**: הסקיל כבר מפנה ל-`docs/specs/`, **אבל
ראיון-ה-PM עדיין בתוכו** וצריך לרדת *(יש אפיון מאושר — אין מה לראיין; ‏⚠️ עם סייג מפורש שם:
ההסרה תקפה **רק** כשקיים אפיון מאושר, אחרת הסקיל עוצר ומחזיר)*.
**‏⑤ בלופרינט מודול 4** ⇐ הפלט `docs/micro_guides/module-4.md`, בשער Plan-Mode.
**‏⑥ בניית מודול 4** — הסדר: **DB/RLS ⇒ לוגיקה ב-`src/lib` ⇒ UI ⇒ בקרה**; ובראשו `6.0`
(התאמת `module-build`) ו-`6.0.5` (אימות שהאלגוריתם שנבנה תואם למחקר).
**‏⑦ תחזוקה** — שמונה פריטים **בלי תלות בשאר**; מריצים כשבלוק אחר חסום.

🔴 **ולפני שפועלים לפי הטבלה ההיא — לאמת אותה.** ‏**היא כבר נמצאה מיושנת ביום שלם** (יומן-הסטיות
שלה, 07/08 12:1X: ארבע שורות תוקנו, כולל מיזוג שכבר בוצע ועדיין סומן ⬜). **הדפוס החוזר שנרשם
שם: "לא טעות בתוכן — אדווה שנעצרה."** ⇒ **טבלת-מצב היא הדבר הראשון שנקרא והאחרון שמתעדכן.**

### ✅ שלושה פריטים שהיו פתוחים ונסגרו 07/08 19:2X *(בהוראת-ישי "בצע את 4,5,6")*

**‏④ ✅ השער האדום — היה **באג אמיתי בבודק**, ותוקן.** ‏`scripts/check-docs-structure.mjs`:
הרגקס `^## (\d+)(?=[\s·.)]|$)` **התיר נקודה** אחרי מספר-הפרק (בשביל `## 3. שם`) — ולכן חילץ `3`
גם מ-`## 3.1` וגם מ-`## 3.2` ⇒ "כותרת כפולה §3" על שני תת-סעיפים תקינים. **תוקן ל-`\.(?!\d)`**
*(נקודה מותרת רק כשלא באה אחריה ספרה)*, **ונבדק על שש צורות** לפני הכתיבה: `## 3. שם` · `## 4 ·` ·
`## 12)` · `## 7` נשמרו; `## 3.1`/`## 3.2` הפסיקו להיתפס. **הבודק: `exit=0`, 26 קבצים, אפס ממצאים.**

**‏⑤ ✅ האימות הוויזואלי בוצע — שני המסכים נראו בעיניים.** ‏ספ-Playwright זמני **מחוץ לריפו**
(סקרצ'פד, כדי לא להתערבב בקימוט של הסשן המקביל), creds מ-`.env.local` דרך `process.env`.
**נמדד:** כל **חמשת** האריחים בשני המסכים מחזירים `backgroundColor: rgb(255,255,255)` — לבן טהור,
אפס מילוי. **ונראה בצילום:** תווית-מעל-ערך בשניהם · טורקיז רק על כפתורי-הפעולה · ₪ ו-`—` בצד
הנכון · RTL תקין. **הקבצים הזמניים נמחקו; הריפו נקי.**
➕ **וממצא-עין אחד שנשאר פתוח, קטן:** בעמוד-הלקוח שלושת האריחים **פרושים על רוחב מלא**
ונראים ריקים משמאל, בעוד במסך-ההצעות הם **צמודים לגודל-התוכן**. ⚠️ **אי-אחידות בפריסה, לא
ברכיב** — `StatTile` זהה בשניהם; מה שנבדל הוא ה-`grid`/`flex` העוטף. **לא תוקן** (הכרעת-פריסה).

**‏⑥ ✅ כפילות הפרומפט — אוששה ותוקנה.** ר' §📍 למעלה: המגה-פלאן נושא גרסה בת **שבעה שלבים**,
הפרומפט החי בן **שלושה**. **המצביע ב-§3.2 של תוכנית-השבועיים תוקן**, ומוקש-התרגום
**"שלב ז" = "שלב 3"** נרשם בשני הקבצים.

### ⚠️ מה שנשאר פתוח באמת

1. **הקימוט/הדחיפה** — **סשן מקביל טיפל בזה 07/08 ב-Plan Mode** (הכרעת-ישי). ⚠️ עץ-העבודה
   הכיל גם עבודה שלו וגם עריכות של ישי עצמו ⇒ **קימוט חייב פאת'ספק מפורש** (כלל 10).
   🔴 **ומה שנוסף אחרי שאותו סשן התחיל: `scripts/check-docs-structure.mjs` (תיקון-הבודק) —
   לוודא שהוא נכלל.**
2. **שתי ההכרעות שממתינות לישי** — ר' §3 למעלה (העברת מסמך-המחקר · עוגן-הבקרה למ4).
3. **שלושת ממצאי-הבלופרינט (`4.7`)** — המלצות שטרם הוכרעו, מוגנות-ממחיקה עד ④+6.0.

</div>
