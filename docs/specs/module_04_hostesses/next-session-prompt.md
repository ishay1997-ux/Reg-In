<div dir="rtl">

# פרומפט-פתיחה — סשן המסכים (מודול 4)

> **Continues from:** *(אין — זו המסירה הראשונה בשרשרת. המסירה הבאה תצביע לכאן.)*
> 🔑 **כל מסירה עתידית פותחת בשורה הזאת ומצביעה לקודמתה** — כדי שתהיה **שרשרת** ולא סדרת
> קבצים מנותקים, ושסשן יוכל לרדת אחורה עד למקור של כל הכרעה.

---

## 🧪 מבחן-קבלה — **הסשן הנכנס עונה עליו בהודעה הראשונה, לפני כל פעולה**

> **למה הוא קיים, ובמדידה:** ‏06/08/2026 הפרומפט הזה **"עבד חצי"**. ‏**האבחנה: רשימת-הקריאה
> אמרה אילו קבצים לפתוח, ולא על מה חייבים לדעת לענות.** ⇒ **שתי התפיסות הגדולות אחרי
> הקומפקט — ‏`C5 §5.6.10–5.6.12` המלא, ו-`module4_smart_match_research §1–§9` — הגיעו מישי,
> לא מהפרומפט.** *(המקור לצורת המבחן: `softaworks/agent-toolkit` + תיעוד Claude Code —
> **אומץ, לא הומצא.**)*

**🅐 מכני**
· **כל נתיב שמוזכר כאן — פתח אותו. נתיב מת ⇒ עצור ודווח.**
  *(זו הבדיקה שהייתה תופסת את שורה 35 ברגע ש-`.claude/skills/_shared/ishay-calibration.md` הועבר.)*
· **‏`git status` והענף — תואמים למה שכתוב כאן?**

**🅑 שרידות** — ההחלטות · נתיבי-הקבצים · החסמים: לכל אחד **"מהקובץ ‏\<שם\>"** או **"מהזיכרון"**.

**🅒 שיפוט** — החסמים עוד קיימים? ההנחות שסומנו *"לאישורך"* עוד מחזיקות?

**🅓 🔴 ארבע שאלות עובדתיות — וזה החלק שמתקן את "עבד חצי"**
**הסשן היוצא כותב כאן 4 שאלות שהתשובה להן נמצאת במה שהוא הרגע כתב לדיסק.**
**הנכנס עונה עליהן קר, לפני כל פעולה.**
🔑 **הרשימה מגדירה *מה חייבים לדעת*, לא *מה לפתוח*** ⇒ **אם התשובה אינה בקבצים שמנינו —
קרא עוד, ואמור מה חסר ברשימה.**

**ארבע השאלות לסיבוב הבא** *(עודכנו 06/08 בסוף הסיבוב — התשובות בקבצים שנכתבו בו)*:
1. **אילו צ'יפים בכרטיס-המועמדת הם *הציון עצמו*, ואילו אינם בציון כלל — ומה המקרה החריג
   שכן באלגוריתם אך אינו מרכיב?** *(פרק 12, פריט ⑩.)*
2. 🔴 **איזה חוב חסר נמצא ב-06/08, למה `grep '🚧 מ6'` לא היה מוצא אותו, ומה המרכיב שהיה
   נשאר כבוי לנצח בגללו?** *(פרק 12, פריט ⑪.)*
3. **מה גובר על מה בסתירה בין המוקאפ לאפיון — ומה עושים כשהסתירה אינה נופלת לאף אחד מהם?**
4. **ארבע זוויות-המיון הן סטייה מהעולם כולו — מה הנימוק שמצדיק אותה, ובאיזה קובץ הוא כתוב?**
   **➕ ובאותה נשימה: שדה "שפות" הוכרע 06/08 — למה הוא *אינו* שער בשכבה 1, ומה הנימוק
   של ישי עצמו?** *(אם התשובה שלך היא "כי אנגלית לא חשובה" — קראת לא נכון.)*

---

## 🔄 קו-סיבוב — **מתי לסיים סיבוב**

🔴 **בהקשר של 600K: סיים את הכרטיס הנוכחי · קמט · עצור.**
*(הורד מ-640K להכרעת-ישי 06/08/2026. **הנימוק מדוד ולא תחושה:** צוות Claude Code ממליץ לעבוד
ב-**50–60%** מחלון-ההקשר, ומעל **60%** מוגדר **"אזור אדום — עבודה שתצטרך לעשות מחדש"**.)*
**אל תתחיל כרטיס חדש מעל הקו.**
🚫 **ואל תעשה קומפקט — סשן טרי + מבחן-הקבלה.**
*(ישי מסתכל על המספר; **הסף באחריותך**.)*
📌 **ובסוף כל סיבוב — שורה אחת ל-`docs/guides/discovery_lessons.md`: מה המבחן תפס בפועל.**
**בלי זה ממשיכים לתכנן מסברה; עם זה, אחרי 3–4 סיבובים יש נתונים.**

### 🔴 ④ קמט **אחרי כל כרטיס מאושר** — לא "בסוף כל בלוק"

**"בלוק" אינו מוגדר, וקומפקט אוטומטי יורה בלי אזהרה בסביבות 80%.**
🔑 **עץ-עבודה מקומט = נקודת-שחזור מלאה — וזו כל תוכנית-המגירה:
לא *למנוע* קומפקט, אלא להפוך אותו ל*בלתי-מזיק*.**
*(‏`git commit -- <נתיבים מפורשים>`, ולעולם לא `git add -A`.)*

### 🔴 ⑤ ירה קומפקט אוטומטי באמצע? **אל תמשיך. קודם שתי פעולות, בסדר הזה:**

**‏1. `git status` + `git diff`** — ‏**זה מראה בדיוק מה נערך ומה לא, וזו בדיוק המידה
שהקומפקט מוחק** *(*"הסוכן לא ידע אילו קבצים כבר נערכו"* — כשל מתועד)*.
**‏2. ואז הרץ את מבחן-הקבלה שלמעלה על עצמך.** ‏**זה בדיוק מה שהוא נועד לו.**
🚫 **ורק אחרי שניהם — חזרה לעבודה.** ‏**המשך "כאילו כלום" הוא איך חצי-צעד נמחק בשקט.**

---

> **נכתב 06/08/2026, ומתעדכן בסוף כל סיבוב.** ‏**התהליכים אושרו · שלושה מסכים מתוך שמונה נסגרו.**
> ‏🚫 **ולא מוקאפים** — הם באים אחרי שכל המסכים אושרו *(ישי תיקן במפורש)*.
> **הדבק את הבלוק כמות שהוא.**

## 🗺️ איפה אנחנו, ומה נשאר עד סוף התהליך

| השלב | מצב | השער שסוגר אותו |
|---|---|---|
| **א** · קריאה ומפת-על | ✅ | הוצג לישי |
| **ב** · יישור מפת-התהליכים | ✅ | ארבע הכרעות אושרו |
| **ג** · תהליך-תהליך | ✅ **2/2** | *"הכל מוסכם"* · ושמונה תוספות בתהליך ב׳ |
| **ד+ה** · מסך = מוקאפ | 🟨 **3/8** | 🔴 **השלבים מוזגו 06/08.** **מציירים ראשון** *(HTML, פלטה מ-`src/`)*, ולצידו **שישה סעיפים בלבד** — מה שתמונה אינה יכולה להראות. 🛑 אישור ישי לכל מסך. **✅ נסגרו: מאגר · מבט-על · Smart Match** |
| **ו** · האפיון `spec.md` | ⬜ | **שני תורים:** רשימת-מפתח → ואז **שלוש הכרעות בלבד** (גבולות-היקף · "מה ייחשב עובד" · "מה אסור לנחש"). המבנה: `prompt_module_discovery.md` §6ו |
| **ז** · שמירה וסגירה | ⬜ | טבלת כיסוי · בדיקת-כיסוי הפוכה מול C5/מוקאפים · **והעברת `module4_smart_match_research.md` לתיקייה הזאת** (32 אזכורים ב-12 קבצים, נמדד) + העברת ה-PNG ל-`superseded/` |

🔴 **ושלב 4 של תוכנית-העל בא אחרי כולם — ישי מתאים את `module-blueprint` בעצמו.**
שלושת הממצאים שממתינים לו: `discovery-log.md`, סעיף "📌 Findings for STAGE 4".

```
אנחנו ממשיכים Discovery למודול 4 — שלב המסכים. שני התהליכים כבר אושרו.

🚫 אל תטען module-blueprint / module-build / module-close.
🚫 אל תתחיל מוקאפים. שלב המוקאפים בא רק אחרי שכל המסכים אושרו.
🚫 אל תשגר סוכן או מחקר בלי אישור מפורש של ישי — גם סוכן בודד.

קרא, בסדר הזה:
1. docs/specs/module_04_hostesses/processes-approved.md — התהליכים המאושרים ושמונת
   המשטחים. זה הבסיס. 🔴 וקרא עד הסוף — הטבלה שם מחזיקה חמש הכרעות-ממשק
   הפיכות שנסגרו לפני הקומפקט, ואת הנימוק לכל אחת.
2. .claude/skills/_shared/ishay-calibration.md — איך ישי עובד. לפני שאתה כותב לו.
   (הועבר לשם 06/08/2026 — הוא על ישי, לא על מודול 4.)
3. docs/specs/module_04_hostesses/discovery-log.md — ה"למה" מאחורי כל הכרעה,
   וטבלת הסינון המלאה (18 שורות) של מה שנדחה — כדי שלא תציע מחדש מה שכבר נדחה.
4. docs/specs/module_04_hostesses/world-sources.md — המקורות, מזוקק.
4א. 🔴 docs/specs/module_04_hostesses/screens-approved.md — שלושת הכרטיסים
   שכבר אושרו, ומבנה-הכרטיס בן ששת הסעיפים. חובה לפני כתיבת כרטיס חדש,
   אחרת תסתור אותם.
5. docs/guides/prompt_module_discovery.md — §4 (איך מדברים) · §6ד (מה חייב בכרטיס מסך)
   · §6ו (מבנה האפיון הסופי).
5א. 🔴 docs/guides/discovery_lessons.md — §11 (התהליך המתוקן: 6 תתי-תהליכים ·
   3 כללי-משמעת · 4 שערים) · §12 (שלוש הלולאות האמיתיות, ו-C היא השבורה) ·
   §2 (16 הכשלים עם המניעה לכל אחד + פורמט-המקרה).
   מצביע זה נוסף 06/08 — הקובץ הופיע כאן רק כיעד-כתיבה ולא כיעד-קריאה,
   וזה בדיוק הדפוס שנמדד: קובץ עם 0 אזכורי-קריאה נשבר ברגע הסיבוב.
6. docs/module4_smart_match_research.md — §11.5 (מה המסך מציג ומסתיר) · §11.8 (הצ'יפים)
   · §11.9 (ביקורת-המוקאפ). ⚠️ ואם תדון במרכיב כלשהו — קרא גם §3 ו-§5–§6, שם ה"למה".
   🔴 אל תיקח מ-§11 את "קרא §11 ודי" — זה משפט שנכתב לסשן בנייה, לא לסשן אפיון.
7. ששת ה-PNG ב-docs/mockups/hostesses-screen/ — בעיניים. חמישה מהם לא נקראו מעולם.

🔴 שמונה משטחים, לא תשעה — 1 ו-3 הם שתי לשוניות של עמוד אחד, ולכן "3א" אינו משטח.

✅ שלושה נסגרו: מאגר דיילות · מבט-על שיבוץ · Smart Match.
   המוקאפים המאושרים: docs/mockups/hostesses-screen/approved/
⬜ חמישה נותרו, וזה הסדר המומלץ:
   תפריט פעולות פר-שורה ← פופ-אפ הוספה ← חלון עריכה ← כרטיס צפייה ←
   ודף ציבורי לאישור משמרת (בלי התחברות — מעולם לא נדון, אין לו מוקאפ,
   והוא היחיד שמשתמש חיצוני רואה).

🔴 והתהליך למסך השתנה 06/08 — קרא את זה, אל תירש את הישן:
   מציירים ראשון. HTML, דאטה אמיתית, ופלטה שנשאבת מ-src/ ולא ממוקאפ קודם:
     grep -rhoE "(bg|text|border)-(green|red|amber|slate|teal)-[0-9]{2,3}" src/ | sort | uniq -c | sort -rn
   ולצד המוקאפ — שישה סעיפים בלבד, וזה הכול:
   ① מפת-לחיצות: מה לחיץ · מה זה עושה · לאן זה מוביל (שורה לכל רכיב)
   ② ההחלטה האחת ומי מקבל אותה
   ③ מקור לכל מספר — עמודה או חישוב, וחלון-הזמן
   ④ מצבים שאינם על המסך (טעינה · ריק-אמיתי מול ריק-אחרי-סינון · שגיאה · חסום)
   ⑤ הרשאות
   ⑥ הגדרות שמאחורי מילה על המסך ("דחוף = 72 שעות")
   🚫 ולא כותבים: פריסה · סדר · צפיפות · צבע — המוקאפ גובר על מראה,
      האפיון גובר על התנהגות ונתונים, וסתירה שאינה נופלת לאחד מהם — עוצרים ושואלים.
🛑 עצירה לאישור ישי בכל מסך.

✅ חמישה פריטים שהיו מסומנים כפתוחים — נסגרו. הכרעות-קלוד הפיכות,
   לאישור ויזואלי במוקאפ: עמודת "התרשמות" · פיצול "ממתינות" עם החלק
   פג-התוקף לחיץ · סף 8 שבועות · והתווית "אישרה זמינות" (סטייה מ-5.5.8).
   הטבלה בסוף processes-approved.md. מילה אחת מישי מבטלת כל אחת.

🔴 אסור לפתוח מחדש — הוכרע ורשום:
· נוסחת Smart Match, המשקלים, השער, הגולפוסט, הריסון, החלון (research §11)
· הציון נשאר סמוי; במקומו צ'יפי-הנמקה · 8 מועמדות גלויות
· ארבע זוויות-מיון + ברירת-המחדל · ששת סטטוסי-השיבוץ
· "פג תוקף" מצב נגזר ולא סטטוס שביעי · "שלח שוב" כפתור ולא טיימר
· עודף-אישורים אזהרה ולא חסימה · שיבוץ כפול = אילוץ במסד
· אי-מענה מחוץ לציון · השבתה מותרת ולעולם לא בשקט · אין מחיקה בשום מקום
· מצב 24-השעות · אחראית משמרת · אי-זמינות מוצהרת · דוח השכר אינו מ4
· כל משטח יקבל מוקאפ HTML חדש (הכרעת-ישי) — אבל לא בשלב הזה

🔴 לפני כל שאלה לישי — חפש אותה ב-§7, ב-§6, במסמך המחקר וביומן.
שאלה שכבר יש לה תשובה אינה שאלה, היא קריאה שלא בוצעה. זה קרה פעמיים.
🔴 וכשאתה מסנן חומר בשמו — הצג את הסינון המלא, לא רק את התוצאה.
שאלה שגויה הוא תופס; סינון שגוי הוא לא יכול לתפוס.

עדכן את discovery-log.md תוך כדי. אל תסכם לישי את מה שהוא כבר היה בו.
🔴 קמט אחרי כל כרטיס מאושר — לא "בסוף בלוק". "בלוק" אינו מוגדר, וקומפקט
אוטומטי יורה בלי אזהרה בסביבות 80%. עץ-עבודה מקומט = נקודת-שחזור מלאה.
git commit -- <נתיבים מפורשים> בלבד. לעולם לא git add -A.
ועדכן STATUS.md ו-docs/CLAUDE_CODE_LOG.md אחרונים, רגע לפני הקימוט.

האם המשימה ברורה? ההודעה הראשונה שלך עונה על זה.
```

</div>
