19. 🟡 `פתוח·לוגיקה·מ8` **צומצם למחצית — גזירת שעות בפועל בלבד** *(13/08/2026, ‏Discovery מ6, הכרעת-ישי)*. 🔴 **חצי-הבונוס נסגר בכך שהוא בוטל:** ‏`projects.project_bonus` **נמחקת.** ‏**הנימוק, במילותיו:** *"לא עשינו בהצעת מחיר בונוס לפרויקט, בוא נמחק את זה"* — **ואומת: להצעה שנמדדה שלוש שורות (דיילות · תגים · שרוכים) ואין בה בונוס** ⇒ **לעמודה אין מקור בשום מקום במערכת, ואף שורת-קוד אינה נוגעת בה** *(נמדד: 0 אזכורים ב-`src/`)*. ⇒ **אין מה לחלק, ולכן אין שאלת-חלוקה.** ‏**‏`assignments.personal_bonus` נשאר** — הוא מבטא הכל שבונוס-הפרויקט ביטא **ובנוסף תגמול פרטני**, ו**בעלותו של מודול 8** *(אותו היגיון של `§7.63`/㉘: החלטה כספית שייכת למסך-הכספים, ולא למסך-הסגירה שמתעד עובדות)*. 🚫 **מסך-הסגירה של מ6 אינו נוגע בבונוס.** ⚠️ **סטייה מ-`C6 §2.4.4`** *("בונוס פרויקט כללי — חובה")*, **מנומקת ורשומה.** **הפירוט:** `docs/specs/module_06_projects/processes-approved.md`. **מה שנשאר פתוח כאן:** רק גזירת השעות ⇒ **גזירת שעות בפועל:** איך `project_bonus` מתחלק בין הדיילות (שווה בשווה? לפי שעות?), ואיך "סך שעות האירוע" הופך לשעות פר-דיילת (לכל אחת מלוא משך האירוע, עם override ידני ב-`assignments.actual_hours`?). סטטוס: **פתוח — להכרעה משותפת עם ישי** (מודול 8).
20. 🟡 `פתוח·לוגיקה·מ8` **איחור בתשלום / החזר בביטול:** האפיון לא מגדיר תזכורות או טיפול בלקוח שלא שילם, ולא מדיניות החזר כשפרויקט מבוטל אחרי אישור הצעה. סטטוס: **פתוח — להכרעה משותפת עם ישי** (מודול 8; ייתכן שמחוץ להיקף — להכריע במפורש).
38. 🟢 `סגור·אוטומציה·מ8` **חשבונית-PDF — סתירה פנימית:** ✅ **הוכרע (07/07/2026, ישי — אודיט; תחום מודול 8): העלאה-ידנית-ושליחה. המערכת אינה מפיקה חשבונית.** הזרימה: מנהלת הכספים **מעלה קובץ-חשבונית** שהכינה (בתוכנת הנהלת-החשבונות שלה) → **"שמור ושלח"** → הודעה קופצת ("נשמר בהצלחה, החשבונית נשלחה ללקוח") → המערכת **שומרת** את הקובץ, **שולחת** אותו במייל ללקוח (תבנית §1.8.3), **ומסמנת ✓** ‏`projects.invoice_sent`. זה מיישב את הסתירה: §1.2.2 (המערכת לא מפיקה) + התבנית (מייל עם קובץ מצורף) חיים יחד — הקובץ מגיע מהעלאה, לא מהפקה. **דפוס המימוש = כמו העלאת החומר השיווקי במודול 2** (Storage + שליחה); פרטי-אחסון (עמודת URL לקובץ) יוכרעו בבניית מודול 8. סטטוס: **סגור.**
46. 🟡 `פתוח·לוגיקה·מ8` **מניעת כפל-תשלום בשכר — הלוגיקה בפועל:** ‏C5:472 "קוד הפקה ייחודי למניעת כפילויות" — אך המנגנון האמיתי (חתימת `assignments.salary_report_id` בהפקה + החרגת חתומים בשאילתת החודש הבא) לא כתוב; ומקרה-קצה פתוח: פרויקט שנסגר אחרי שדוח חודשו כבר הופק — השעות נאספות אי-פעם או נופלות בין החודשים (השאילתה סורקת "פרויקטים שהסתיימו בחודש X", ‏C5:471)? סטטוס: **פתוח** (מודול 8).
52. ⚪ `ממתין-לביצוע·לוגיקה+DB·מ6/8` **רווח גולמי סופי — נשמר או נגזר, ובאיזו יחידה:** ‏C5:453 ("שחושב **ונשמר** בעת סגירת הפרויקט") מול C6 §2.4.4 ("נגזר, לא נשמר פיזית") — ואין עמודה בסכמה. נגזר-בקריאה ⇒ עריכת `actual_hours` מאוחרת מזיזה דו"ח רווחיות היסטורי; נשמר ⇒ חסרה עמודה + הגדרת רגע-הקפאה. נלווה: יחידת-התצוגה סותרת — ₪ בסגירה (C5:833) מול % בכל ה-overview והדו"חות (C5:415/801/1008). ✅ **הוכרע (ישי, 11/07/2026, סבב-הכרעות-קדם למ3): שני מספרי-רווח — עיצוב-ישי שמיישב את הסתירה.** ‏**רווח צפוי** = נגזר-חי כל עוד הפרויקט פעיל (הכנסות-ההצעה הקפואות מול עלויות מתוכננות: משובצות×שכר-קפוא×שעות-מתוכננות + רכש) — משתנה עם שיבוצים/שינויי-תכולה, לעולם לא נשמר; ‏**רווח סופי** = מחושב בסגירת-האירוע מהנתונים-בפועל **ונשמר בשקלים**, קופא (C5:453 צדק על הסופי; C6 §2.4.4 צדק על הצפוי — שניהם תיארו מספרים שונים). **יחידת-תצוגה:** נשמרים ₪; ‏% תמיד נגזר לתצוגה (רווח÷הכנסות) — מיישב את סתירת-₪/%. **מודל-השעות (אישר ישי, תואם C5 5.5.7):** נתון-אחד-לאירוע שמתפזר לכל המשובצות; אי-הגעה מסומנת עם סיבה (מדיניות 0/50/100% הקיימת). ביצוע (עמודה+מנגנון-סגירה): מ6/8; מ3 רק מבטיח את צד-ההכנסות הקפוא (§7.50/51). סטטוס: **ממתין-לביצוע** (מודולים 6/8; משלים את §7.28). **↳ קודם ל-§7.78 (09/07/2026).** **↳ 🔴 הובהר (ישי, 14/08/2026 01:17): "בסגירת-האירוע" = הסגירה ה*כספית* (מ8), לא התפעולית (מ6).** ‏**השאלה נשאלה כי במערכת שתי סגירות** — תפעולית (מ6: שעות · אורחים · נוכחות · דוח-סיכום ⇒ `awaiting_invoice`) וכספית (מ8: חשבונית · תשלום · רווחיות ⇒ `finished`) — **והנוסח מ-11/07 נכתב לפני שההבחנה הזאת קיבלה שם.** ⇒ 🔴 **מ6 אינו מקפיא רווח ואינו מחזיק עמודת "רווח סופי"; הוא מקפיא את הקלטים שבבעלותו** *(`assignments.actual_hours` פר-דיילת · שלוש עמודות-הנוכחות · `projects.actual_hours`/`actual_guests` · חותמת-הסגירה התפעולית)*, **ומ8 מקפיא את המספר בחלון שלו.** **הראיה שחייבה את ההבהרה, ושתי הצלעות שלה מדודות:** שני מרכיבי-עלות אינם קריאים ברגע הסגירה של מ6 — ‏`assignments.personal_bonus` **בבעלות מ8 בהכרעת-ישי 13/08** *(§7.19: "🚫 מסך-הסגירה של מ6 אינו נוגע בבונוס")* · ו-`assignments.travel_amount` שנמדד 14/08 כ**עמודה שאף קוד בריפו אינו כותב** *(§7.69 🟠, "הסכום לפני מ10")*. ➕ **ו-`PROJECT_MASTER §5.14` כבר אומר את זה על הנוסחה עצמה:** *"אינה כוללת **נסיעות**… **השמטתן מנפחת את הרווח המדווח**"* ⇒ **מספר שמ6 היה מקפיא היום מחושב עם בונוס=0 ונסיעות=0 — מנופח בדיוק בשיעור שהאזהרה מתארת, ומוקפא לנצח.** ✅ **וכל מה שמ8 צריך קיים ברגע שמ6 מקמט:** ההכנסה קפואה ב-`quote_services` + `project_changes`, השעות והתעריפים קפואים ב-`assignments`, וחותמת-הסגירה התפעולית היא העוגן שמולו מ8 מקפיא. *(נמצא בסשן-בנייה מדומה 14/08/2026 והובא לישי כשאלת-מילה-אחת, כי שתי הכרעות שלו — 11/07 ו-13/08 — נקראו כסותרות עד שהוגדר לאיזו סגירה כוונה הראשונה.)*
57. 🟢 `סגור·לוגיקה·מ3/8` **פרמטר-רפאים `יום_הפקת_דוח_שכר`:** ‏Seed ‏#4 (=5, ‏`products_and_params.md`) מרמז על הפקת דו"ח-שכר *מתוזמנת*, אבל C5:470–472 + §1.8.10 מתארים תהליך *ידני* בלחיצת "ייצא ושלח" — פרמטר בלי מנגנון ובלי נרטיב. להכריע: ידני (לא לזרוע את הפרמטר) או מתוזמן (מנגנון בסגנון §7.42, מודול 10). **✅ הוכרע (ישי, 12/07/2026): ידני — הפרמטר `יום_הפקת_דוח_שכר` לא נזרע ב-Seed** (עקבי עם C5:470-472: "ייצא ושלח" ידני; דו"ח-מתוזמן = gold-plating, לא נבנה. אם ירצו תזמון בעתיד — מנגנון אמיתי במ10). סטטוס: **🟢 סגור** (מ3-Seed: לא-לזרוע).
58. 🟡 `פתוח·לוגיקה·מ8/11` **נוסחת סטיית-התקציב:** ‏"תכנון מול ביצוע" (C5:1008; ‏C6 §2.4.4 "סטיית תקציב") משווה עלות-דיילות מתוכננת **פר-משמרת** (מהקטלוג, תוקפא ב-§7.28) מול עלות בפועל **פר-שעה** (‏`actual_hours × hourly_rate_snapshot`) — בסיסים לא-ברי-השוואה בלי כלל-גישור מוגדר. סטטוס: **פתוח** (מודול 8/11; קשור §7.19).
61. 🟢 `סגור·DB·מ6` ✅ **הוכרע 13/08/2026 (בהאצלת-ישי): שלושה buckets — `marketing` (קיים, ציבורי) · 🆕 `reports` (פרטי, שער=פרויקטים) · 🆕 `finance` (פרטי, שער=כספים; שכר+חשבוניות תחת אותו כלל).** **המספר נגזר ממספר כללי-הגישה השונים** — כהמלצת Supabase המפורשת ([quickstart](https://supabase.com/docs/guides/storage/quickstart)). 🔑 **והממצא שקבע: התבנית כבר בפרודקשן** — נמדד ב-`20260710160735` שלבאקט `marketing` ארבע policies על `storage.objects`, **כולן על `permissions × modules × current_user_role_id()`** ⇒ **זו בדיוק מקבילת-§7.21 לקבצים שהפריט ביקש; משכפלים, לא ממציאים.** 🚫 **ולא bucket יחיד עם תיקיות:** טעות בתנאי-נתיב חושפת שכר, ו-`file_size_limit`/`allowed_mime_types` מוגדרים **ברמת ה-bucket** ([fundamentals](https://supabase.com/docs/guides/storage/buckets/fundamentals)) ואנחנו כבר משתמשים בהם. **מסירה:** `createSignedUrl` מהדפדפן של המחובר (ה-policy היא השער) — ⚠️ **נמדד שאינו קיים באף מקום ב-`src/` היום**; ולדוח שנשלח ללקוח — **להוריד ולצרף, לא לשתול קישור פג-תוקף.** **‏§7.36 (אטומיות) נסגר איתו באילוץ-מסד:** `check (project_status not in (awaiting_invoice,awaiting_payment,finished) or summary_report_url is not null)` ⇒ **פרויקט לא יכול לעבור לגבייה בלי דוח, גם אם ה-UI נשבר.** — **תשתית Supabase Storage מאוחדת:** האפיון מחייב קבצים בארבעה מקומות — דוח-סיכום חובה בסגירת פרויקט (C5:647–648), קובץ דוח-שכר (C6 §2.4.10), חשבונית-מועלית (§7.38), חומר שיווקי (C5:538; באקט `marketing` כבר בבלופרינט מודול 2) — אך אין תוכנית-Storage מרוכזת: אילו buckets, ‏public מול private+‏Signed-URLs (שכר/חשבוניות = רגישים), ותבנית policies ל-`storage.objects` (מקבילת-§7.21 לקבצים). קשור §7.36 (אטומיות העלאה+DB). ~~סטטוס: **פתוח — דיון קצר** (כיוון בבלופרינט מודול 6; הבאקט הראשון כבר במודול 2).~~ ⇒ **סטטוס: 🟢 סגור** *(תוקן 26/08/2026 ב-`regin-docs-sync` — הפריט נשא בראשו `🟢` והכרעה מתוארכת מ-13/08/2026, בעוד נוסח-השאלה המקורי שבזנבו לא עודכן כשההכרעה הוצמדה לפניו. אותה תקלה בדיוק כמו ב-`§7.39`, שתוקנה 14/08/2026.)*
63. 🟢 `סגור·הרשאות·מ6/8` ✅ **הוכרע 13/08/2026 (בהאצלת-ישי): יחידת-ההרשאה היא ה-RPC, לא העמודה.** מנהלת-הכספים כותבת `invoice_sent`/`payment_date`/`feedback_*` דרך **פונקציה ייעודית שדורשת הרשאת "כספים" — בלי `edit` על "פרויקטים"**. 🔑 **הנימוק:** `RLS` ב-Postgres הוא **ברמת-שורה ולא ברמת-עמודה** — עובדה שהמערכת כבר נכוותה בה (`product_costs` פוצלה לטבלה נפרדת בדיוק מכך), **ופיצול-טבלה שני ל-`projects`, הישות המרכזית, היה מסרבל את כל המערכת.** ✅ **והתקדים כבר קיים:** נמדד ש-`projects` מחזיקה **policy יחידה, `SELECT` בלבד**, וכל כתיבה עוברת RPC ייעודי (`approve_quote_and_create_project` · `set_project_coordinates` · `sync_assignments_on_project_date_change`). **נסגר יחד עם §7.77 — אותה תשובה לשתי שאלות.** — **בעלות-עמודות מול RLS שורתי (הרחבת §7.21 לטבלאות רב-מודוליות):** מטריצת-ההרשאות היא *עמודתית* בפועל — מנהלת-הכספים חייבת לכתוב `invoice_sent`/`payment_date`/`feedback_*` על שורות `projects` בלי edit על מודול פרויקטים (C5 §1.4.3, §1.5.10), ‏ו-RLS אינו מבחין בעמודות: policy רחבה תיתן לה לדרוס גם `required_hostess_count`; באותו אופן `hostesses.bank_*`+ת"ז גלויים לכל תפקיד עם קריאת-שורות למרות "גישה חסומה לריכוז שכר" (C5 §1.4.2). ‏§7.21 נשארת נכונה לטבלאות חד-מודוליות; נדרשת הכרעת-המשך לרב-מודוליות: פיצול-ילד (`project_finance`) / כתיבה דרך RPC בבעלות-עמודות / view+‏column-grants לעמודות-PII. **✅ כיוון-הוכרע (ישי, 10/07/2026): נדחה ל-M6/M8** — `customers` חד-מודולי, אז §7.21 חל כמו-שהוא במיגרציית מודול-2 (1.1); הכיוון הרב-מודולי מוכרע כשמ6/8 נתקלים במקרה הקונקרטי (לא להעתיק §7.21 בעיוורון לטבלאות רב-מודוליות עד אז). ~~סטטוס: **פתוח — דיון** (הועבר ממ2 למ6/8; קשור §7.77).~~ ⇒ **סטטוס: 🟢 סגור** *(תוקן 26/08/2026 ב-`regin-docs-sync` — הפריט נשא בראשו `🟢` והכרעה מתוארכת מ-13/08/2026, בעוד נוסח-השאלה המקורי שבזנבו לא עודכן כשההכרעה הוצמדה לפניו. אותה תקלה בדיוק כמו ב-`§7.39`, שתוקנה 14/08/2026.)*
68. 🟡 `פתוח·DB·מ8` **דוח-שכר כמסמך (ולא יומן-הפקה):** ‏`salary_reports` = ‏PK+תאריך-שליחה+URL — בלי חודש-כיסוי, בלי שורות, בלי סכומים; "שחזור" הדוח = שאילתא חיה על `assignments`, ולכן תיקון-שעות מאוחר משנה בדיעבד דוח שכבר נשלח לרו"ח — אין הוכחת-מה-נשלח (C5:471–472). מרחיב את §7.40(ג) (חודש-ייחודי) וסוגר את מקרה-הקצה של §7.46. כיוון: ‏`period` ‏UNIQUE + שורות-snapshot מוקפאות בהפקה (`salary_report_lines`); מינימום: ‏period+total. סטטוס: **פתוח — דיון** (מודול 8).
69. 🟠 `סגור-מנגנון·לוגיקה·מ4/8` **"+ נסיעות" בתבנית-הזימון מול מודל-השכר:** התבנית מבטיחה לדיילת "תעריף … ש"ח לשעה + נסיעות" (C5:1095) אבל נוסחת-השכר (C5:471) = שעות×תעריף+בונוסים בלבד — לנסיעות אין ייצוג. להכריע: עמודת `travel_amount` בשיבוץ / נספג ב-`personal_bonus` בהנחיה מפורשת / תיקון-התבנית (סטייה מתועדת מהאפיון). **✅ עדכון (ישי, 14/07/2026, בהכנת-תוכן-Seed למ3):** ההבטחה "+ נסיעות" **נשארת** בתבנית 5.8.4 כלשונה (ישי כתב-מחדש את התוכן המלא וכלל אותה) — לא מוסרת. נדרש לסגור את מנגנון-התשלום **לפני שמודול 10 מתחיל לשלוח מיילים אמיתיים** — לא לפני. **↳ 📋 רשימת-האדוות לסגירה (נרשמה 29/07/2026 23:35 — קודם לכן לא היה כתוב *אילו* מקומות מתעדכנים, וזה בדיוק סוג החוב השקט): כשהמנגנון ייבחר, שלושה מקומות מתעדכנים** — ‏**(1) §5.12** נוסחת-השכר-החודשי (מוסיפים מחובר `+ נסיעות`, ובאקסל-לרו"ח **עמודה נפרדת** ולא מגולם בשכר) · **(2) §5.14** נוסחת-הרווח-הגולמי (נסיעות הן **הוצאה ישירה** — 6 דיילות × סכום = הוצאה לאירוע; השמטתן מנפחת את הרווח המדווח) · **(3) §7.15↳** צימוד-הקרבה (אם לפי-ק"מ ⟹ קרבה היא גם פרוקסי-עלות וזווית-"הזולה" חייבת עלות-כוללת; אם סכום-קבוע ⟹ הנסיעות מתקזזות והקרבה נשארת מנבא-התנהגות טהור). **המלצת-קלוד למנגנון: סכום קבוע למשמרת** (פרמטר יחיד + `travel_amount` בשיבוץ) — הפשוט משלושת המנגנונים שכבר רשומים כאן, ומייתר לוגיקה בשתי נקודות-תכנון נוספות. ⚠️ **לא לנחש סכום** — החזר-נסיעות בישראל נושא מוסדר; לאמת מול רו"ח/רגולציה, לא מהזיכרון. **✅ הונהן — המנגנון (ישי, 01/08/2026, סבב-§7-מ4 #3): סכום קבוע למשמרת** — פרמטר יחיד ב-`params` + `travel_amount` בשיבוץ. **הסכום עצמו לא נקבע** — מוסדר רגולטורית, מאומת מול רו"ח לפני שמ10 שולח מייל אמיתי (לא מנוחש). **שלוש האדוות בוצעו באותו סשן (01/08):** §5.12 ✓ · §5.14 ✓ · §7.15↳-הצימוד נפתר לענף (ב) — הקרבה נשארת מנבא-התנהגות טהור, זווית-"הזולה" = `תעריף×שעות` בלבד. סטטוס: **🟠 סגור-מנגנון · הסכום לפני מ10** (מודול 4/8).
78. 🟡 `פתוח·לוגיקה+DB·מ3/6/8` **כלל-הקפאה אחד (הכרעת-מסגרת למחיר·עלות·מע"מ·רווח):** את §7.51 (מע"מ) + §7.28 (עלות-רכש) + §7.52 (רווח) מכריעים **יחד ככלל-הקפאה אחד** — מה קופא, באיזה רגע, ומה נשאר נגזר; המצב הנוכחי (מחיר קפוא, עלות תיקפא, מע"מ חי, רווח לא-קיים) הוא חצי-קפוא — הגרוע משני העולמות. **נולד-כ-↳ על §7.52 (08/07/2026, אודיט-ערעור); קודם לפריט מלא 09/07/2026.** **↳ עדכון-התכנסות (11/07/2026, סבב-הכרעות-קדם למ3):** שלושת המרכיבים הוכרעו עקבית — מע"מ קופא-באישור (§7.51) · עלות-רכש קפואה-באישור (§7.28, הכרעת-עבר) · רווח-סופי נשמר-בסגירה (§7.52, מודל שני-המספרים). כלל-ההקפאה המסתמן: **צד-המחיר-והעלות קופא באישור-ההצעה; צד-הביצוע-בפועל קופא בסגירת-האירוע; כל השאר נגזר.** נותר למ6/8 לאשרר את המסגרת בעת-הביצוע. סטטוס: **פתוח — מתכנס** (מודולים 6/8; חלק-המע"מ הוכרע).
79. 🟡 `פתוח·לוגיקה·מ8` **הגדרת "רווח גולמי מצטבר" פר-לקוח (כרטיס-הלקוח):** ‏C6 §2.4.1 מונה אותו כתכונה-נגזרת של לקוח, ודוח ניתוח-הלקוחות (5.7.3) נשען על אותה סכימה — אך אין הגדרת אוכלוסייה/נוסחה: סכימת-רווח על אילו פרויקטים (רק שהסתיימו? כולל פתוחים? מתוכנן מול בפועל?) — אח פר-לקוח של §7.59 (KPI-רווח חודשי). מודול 2 מציג placeholder ("אין נתונים עדיין"); הנוסחה נכתבת ב**מודול 8** (סגירת-תיק וחישוב רווחיות — 5.14, שם נוסחת הרווח-הגולמי; ביחד עם §7.78 שמודולי-הביצוע שלו `מ3/6/8`). **נולד באודיט-נאמנות 10/07/2026.** **תיקון-ניתוב (10/07/2026 ערב, מבקר-הקשר-טרי + אימות-מקור):** היעד המקורי "מודול 7" היה שגוי — מ7 הוא הדשבורד (תצוגת-KPI בלבד); בעל-הנוסחה הוא מ8 (‏5.14). גם הציטוט "C5 §5.6.3 מציג אותו במסך" היה מופרז — ‏5.6.3 מונה רק סך-הכנסות+ממוצע-משוב; מקור-החובה הוא C6 §2.4.1. ה-🚧 נותב-מחדש מ-`מ7` ל-`מ8` ב-§6 ובמדריך-מיקרו-מ2. סטטוס: **פתוח** (מודול 8; placeholder במודול 2).
80. 🟡 `פתוח·מסך+לוגיקה·מ8` **מיפוי ציון-שביעות → תגית-מלל:** מוקאפ 01 מציג תג-מלל ("מצוין") לצד כוכבי-השביעות, אך שום מקור-אפיון לא מגדיר את ספי-ההמרה (מתי "מצוין"/"טוב"/"בינוני"). תלוי ב-§7.37 (שקלול ציון-המשוב). מודול 2 מציג כוכבים inert + "אין נתונים עדיין" ו**אינו** בונה את התג (‏confirm-intent — פרט מוקאפ ללא מקור); הספים נקבעים עם נתוני-המשוב במודול 8. **נולד באודיט-נאמנות 10/07/2026.** **scope-מ2 הונהן (ישי, 10/07/2026, סבב-P13):** אושר — כוכבים דוממים בלבד, בלי תג. סטטוס: **פתוח** (מודול 8; קשור §7.37).
92. 🟢 `סגור·אוטומציה+תהליך·מ8/מ10` **מי שולח את מייל-המשוב ללקוח — ומתי.** ‏**מה שכן מוכרע וכתוב:** מכונת-המצבים *(`§5:245` — `לא נשלח → נשלח → הושלם` \| `לא ענה`)* · המסכים שקולטים את הציון *(`§5.13`/`§5.14`, מודול 8)* · האכיפה *("אין 'העבר לארכיון' ללא ציון משוב")* · **והלינק לשאלון קיים כפרמטר אמיתי** *(`params.קישור_בסיס_סקר_לקוחות`, ‏Google Forms חי, נזרע ב-`20260723112000`)* · והתבנית עצמה קיימת *(`תבנית_מייל_משוב_לקוח`, שלושה placeholders שלכולם מקור)*. 🔴 **ומה שאינו כתוב בשום מקום: מי מבצע את המעבר `לא נשלח → נשלח`.** ‏`grep "משוב_לקוח"` על `PROJECT_MASTER.md` ⇒ **0**; ‏`micro_guides/module-3.md:529` מונה אותה כאחת מ*"5 תבניות המייל שלא נבנו"* וכותב *"האודיט שלהן שייך למודול שצורך אותן"* — **ואף מודול לא לקח בעלות.** 🔑 **ולמה זה נופל בין הכיסאות דווקא כאן, וזה הנימוק שמצדיק פריט משלו:** זה **המייל היחיד שנשלח אחרי שהאירוע נגמר** — אחרי הסגירה התפעולית של מ6 ולפני הסגירה הכספית של מ8 — ולכן **שני המודולים יכולים להניח שזה של השני**, וזו בדיוק הצורה שבה חוב נעלם. **שתי האפשרויות:** ‏**(א)** ‏**מ8 שולח ידנית** מחלון-סגירת-התיק — כפתור לצד שדה-הציון; המשוב ממילא חוסם את הארכוב, אז השליחה יושבת במסך שכבר עוסק בו · **(ב)** ‏**מ10 שולח אוטומטית** ‏X ימים אחרי האירוע, בג'וב המתוזמן, כמו תזכורת ה-T-24. **המלצת-קלוד: (א)** — היא **אינה יוצרת תלות במודול שטרם נבנה**, ו"מתי לשלוח סקר ללקוח" הוא שיקול-דעת אנושי *(אירוע שהשתבש — מנהלת הפרויקטים לא תרצה לשלוח סקר למחרת)*; **(ב) נשארת פתוחה כשדרוג במ10** בלי לשנות דבר במ8. ⚠️ **אינו חוסם דבר היום** — נרשם 08/08/2026 בסבב-תבניות-המייל של בלופרינט מ4, **בהוראת-ישי** ("סבבה כתוב במשהו פתוח"), כדי שלא ייעלם. ~~סטטוס: **🟡 פתוח — להכרעה בפתיחת מ8.**~~
