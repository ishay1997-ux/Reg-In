# Module 8 (כספים) — Reverse-Direction Contract Sweep

Read-only sweep for the module-8 Discovery. Purpose: what module 8 must **supply** to siblings, and
what siblings already **dictate to** module 8. Built from three sources per the task brief:
`docs/specs/module_04_hostesses/{processes-approved,spec}.md` ·
`docs/specs/module_05_logistics/{processes-approved,spec}.md` ·
`docs/specs/module_06_projects/{processes-approved,spec}.md` ·
`docs/PROJECT_MASTER.md` §5 (screen cards 5.2 · 5.3 · 5.13 · 5.14 · 5.17) and its §6 register
(`grep '🚧 מ8'`) · `docs/micro_guides/module-{2,3,4,6}.md` (`grep 'מ8'`).

Every row's anchor is a quoted grep-able string from the cited file (never a line number).

---

## Table A — What מ8 must supply to consumers

| # | Consumer (module/screen) | What it expects from מ8 | Source anchor |
|---|---|---|---|
| A1 | **מ2 — customer card, "ממוצע משוב"** | Average customer feedback score, computed **only over past/finished projects** — not all projects. `deriveCustomerMetrics` today averages everything; מ2 built the empty state and is waiting for מ8 to wire the real value. | `docs/PROJECT_MASTER.md` §6, anchor `"ממוצע-משוב (`projects.feedback_score`"` and `"לסנן לפי סטטוס לפני הממוצע"` |
| A2 | **מ2 — customer card, "רווח גולמי מצטבר"** | Cumulative gross-profit-per-customer metric. מ2 shows a placeholder (`"אין נתונים עדיין"`); the formula itself is owned by מ8 (spec screen 5.14). Explicitly **not** on the customer card anymore (persona decision, account-manager not finance) — its home is the finance screen (מ8) and the management report (מ11). | `docs/micro_guides/module-2.md`, anchor `"cumulative gross-profit metric (C6 §2.4.1 derived attr"` and `"owns the gross-profit formula — spec 5.14"` |
| A3 | **מ2 — customer list, satisfaction stars + satisfaction filter** | A visible/derived filter on customer satisfaction (`feedback_score`), currently built **present-but-inert** — column exists, filter was removed from the UI until מ8 has real data (`"the disabled 'ציון שביעות רצון' filter removed... it was inert until M8"`). | `docs/micro_guides/module-2.md`, anchor `"Satisfaction stars in list + satisfaction filter"` and `"the filter returns in **M8** with real data (🚧 מ8)"` |
| A4 | **מ6 — project card ("תיק-האירוע")** | Read-only display of the feedback that מ8 entered: `feedback_score` / `negative_feedback_reason` / `feedback_notes`, plus `invoice_sent` and `payment_date` — all five columns already exist on `projects`. מ6's own reasoning: *"מנהלת-הפרויקטים היא שתישאל 'למה הלקוח היה לא מרוצה?'"* — if the data lives only in the finance screen she has to go ask the finance manager instead of opening the project. | `docs/specs/module_06_projects/spec.md`, anchor `"מ8 כותב"` (row `"מ8 → מ6"`); `processes-approved.md`, anchor `"כרטיס-הפרויקט מציג את המשוב — קריאה בלבד — ברגע שמ8 הזין אותו"` |
| A5 | **מ6 — project status machine** | Two of the eight status labels are written by מ8, not מ6: `awaiting_payment` → **"ממתין לתשלום"**, and `finished` → **"פרויקט הסתיים"**. | `docs/specs/module_06_projects/spec.md`, anchor `"| `awaiting_payment` | **`ממתין לתשלום`** | **מ8** |"` and `"| `finished` | **`פרויקט הסתיים`** | **מ8** |"` |
| A6 | **מ6 — cancellation flow** | The actual 0%/50%/100% cancellation-compensation **computation** (tiers already decided in מ6's Discovery: `>72h⇒0%` · `24–72h⇒50%` · `<24h⇒100%` · force-majeure⇒always 0%) — מ6 only stamps `cancelled_at` + cancellation type; מ8 derives the money from it, "months later." | `docs/micro_guides/module-6.md`, anchor `"🚧 **מ8** — the 0/50/100 compensation computation itself"`; `docs/specs/module_06_projects/spec.md`, anchor `"ומיפוי הערכים במסד נשאר פתוח"` (§1.6ב compensation ladder) |
| A7 | **מ2 — customer card "Amount" column on the projects tab (surface 8)** | The displayed project amount should include scope-change totals (`quote amount + Σ project_changes`), not just the frozen quote total — found live: `project_changes` = 0 rows today so no screen is lying yet, but the fix is real work once scope-changes exist. Ishay explicitly allowed routing this to either מ8 (owner of the project's financial truth) or מ6. | `docs/PROJECT_MASTER.md` §6, anchor `"עמודת \"סכום\" בלשונית-הפרויקטים של כרטיס-הלקוח (משטח 8) אינה כוללת שינויי-תכולה"` |
| A8 | **מ11 — monthly salary report (accountant export)** | A `salary_reports` table + the full monthly-salary formula + Excel export for the accountant. §5.12 (מ4's screen card) once listed `salary_reports` under מ4's tables — corrected: the table was **removed from מ4's card 05/08/2026** and its ownership assigned to מ8, with four verified reasons (data doesn't exist before מ6 closes; the sole user — finance manager — is `blocked` on "דיילות"; all surrounding machinery — accountant email param, dedupe code, email template #20 — is finance; frozen spec C5 §5.6.12 never mentions a salary report). | `docs/PROJECT_MASTER.md` §5.17, anchor `"salary_reports"` (table list); §6, anchor `"🚧 מ8 ← מ4 — **דוח השכר (`salary_reports`) אינו נבנה במודול 4"` and `"**ההכרעה: מ8.**"` |
| A9 | **מ11 — profitability report ("תכנון מול ביצוע")** | The cost-basis decision for per-line profitability (`actual_qty` vs `planned_qty`) is explicitly declared **not** מ5's call — owned by מ8/מ11. Also inherits מ5's declared, bounded limitation: logistics rows born from a scope-change carry `NULL` in *both* source-pointer columns (`quote_service_line_id`/`project_change_id`) by DB-constraint design (`logistics_origin_exactly_one`), so profitability is **not computable exactly** for those specific rows — flagged so מ8/מ11 don't discover it by surprise. | `docs/specs/module_05_logistics/spec.md`, anchor `"בסיס-העלות לרווחיות: `actual_qty` או `planned_qty` | **מ8/מ11**"`; `processes-approved.md`, anchor `"נרשם כאן כדי שמ8/מ11 לא יגלו את זה בהפתעה"` |
| A10 | **מ7 — home dashboard KPIs** | Two of the four top KPI tiles are fed by מ8-owned data: *"רווח גולמי משוער לחודש"* (estimated monthly gross profit) and *"ממוצע שביעות רצון רבעוני"* (quarterly average satisfaction) — aggregated from `projects`/`quotes`. | `docs/PROJECT_MASTER.md` §5.2, anchor `"רווח גולמי משוער לחודש, פרויקטים פעילים, ממוצע שביעות רצון רבעוני"` |
| A11 | **Shared email engine (built in מ3, reused by מ4/מ8/מ11)** | מ8 must consume `src/lib/email.js` + `send-email` **as-is** and add only what's module-specific (placeholders, subject, recipient) — not build a parallel engine (`jscpd` blocks duplicates in CI). Two of the six seeded `params` mail templates are מ8's: **#13 חשבונית** (invoice) and **#20 דוח-שכר** (salary report). | `docs/PROJECT_MASTER.md` §6, anchor `"#13 חשבונית · #14 ביטול-משמרת · #20 דוח-שכר"` and `"מ4/מ8/מ11 ישלחו מיילים גם הם"` |
| A12 | **מ6 — feedback survey timing decision (already ruled, not open)** | Confirmed **negative** expectation, worth stating explicitly so it isn't rebuilt: מ8 does **not** send the customer-satisfaction survey. מ6 sends it at operational closing (§7.39/㉜) specifically because sending later (at invoice time) both lowers response rate and biases the score downward — a decision מ8's Discovery should not reopen without new evidence. | `docs/specs/module_06_projects/processes-approved.md`, anchor `"מודול 6 שולח, בסגירה התפעולית. מודול 8 אינו שולח כלל"` |

---

## Table B — What siblings dictate to מ8

| # | Dictating module | The dictate (verbatim quote) | Source anchor |
|---|---|---|---|
| B1 | **מ4** | *"§7.63 — הגנת בנק/ת"ז ברמת-המסד... נדחה למודולים 6/8. במ4 ההגנה בממשק בלבד — מגבלה מוצהרת, לא חור לסתום."* מ8 (or מ6) must decide/build DB-level protection for bank details and ID number; מ4 only hides them in the UI. | `docs/specs/module_04_hostesses/spec.md`, anchor `"נדחה למ6/מ8. **מגבלה מוצהרת, לא חור."`; also `processes-approved.md`, anchor `"נדחתה למודולים 6/8"` |
| B2 | **מ4** | *"מ4 מספק `hourly_rate_snapshot` ✅ · `actual_hours` ✅ · `personal_bonus` ✅ · `travel_amount` ⬜ טרם קיים (רשום, §7.69); מ8 בונה את דוח-השכר."* — מ8's salary report is built on exactly these four inputs; `travel_amount` does not exist yet and its ₪-value must still be confirmed with the accountant before מ10 sends a real email. | `docs/specs/module_04_hostesses/spec.md`, anchor `"מ4 → מ8"` (contract table row) |
| B3 | **מ4** | Bank-detail "structure validation" was deliberately **not built** in מ4 — fields are required-only, no format check — with the explicit reasoning that *"מ8 (שכר) הוא שצורך את השדות האלה בפועל, ושם תהיה הצלבה אמיתית מול תשלום"* (מ8 is where a real cross-check against payment will happen, so a fabricated format rule in מ4 would only block valid saves). | `docs/PROJECT_MASTER.md` §6, anchor `"בדיקת מבנה\" על פרטי הבנק לא נבנתה"` and `"מ8 (שכר) הוא שצורך את השדות האלה בפועל"` |
| B4 | **מ6** | *"㉞ שדות-המשוב — פיצול בעלות בשלושה חלקים... הזנת הציון והסיבה (feedback_score · negative_feedback_reason · feedback_notes) — מודול 8."* But the **dictionary** (the five closed values for `negative_feedback_reason`: `איחור דיילות · תפקוד דיילות · איכות תגים · ניהול לקוי · אחר`) is owned by מ6, and the DB column has **no CHECK enforcing it** (`negative_feedback_reason text`, unlike `feedback_status` which does have one) — מ8's entry form must self-enforce this list since the DB won't. | `docs/specs/module_06_projects/spec.md` §1.6ב, anchor `"הבעלות על ה*הזנה* היא של מ8... הבעלות על ה*מילון* היא כאן"` and `"ובמסד אין לה CHECK כלל"` |
| B5 | **מ6** | `feedback_status` is stored **in English**, not Hebrew: `check (feedback_status in ('not_sent','sent','completed','no_response'))`. מ6's own value `'sent'` was initially mis-specified in Hebrew and would have been rejected by the CHECK — the same trap applies to whatever value מ8 writes when the survey response comes back (`'completed'`, presumably, on behalf of the finance manager's manual entry). | `docs/specs/module_06_projects/spec.md`, anchor `"feedback_status הוא אנגלית"` and `"הכתיבה הזאת תיפסל ע\"י ה-CHECK"` |
| B6 | **מ6** | *"㉘ — יחידת-ההרשאה היא ה-RPC, לא העמודה. מנהלת-הכספים כותבת invoice_sent/payment_date/feedback_* דרך פונקציה ייעודית שדורשת הרשאת 'כספים' — בלי `edit` על 'פרויקטים'."* מ8 must NOT write these five `projects` columns directly or through a general `edit`-on-פרויקטים grant — only through a dedicated `SECURITY DEFINER` RPC. | `docs/specs/module_06_projects/processes-approved.md`, anchor `"יחידת-ההרשאה היא ה-RPC, לא העמודה"`; `spec.md`, anchor `"דרך RPC ייעודי שדורש הרשאת 'כספים'"` |
| B7 | **מ6** | *"㉙ — נעילה עמודתית — התפעולי ננעל, ושדות-הכספים נשארים פתוחים למנהלת-הכספים."* After operational close the project card's operational fields become read-only for everyone, but the finance-owned columns must **stay writable** for מ8 — a column-level lock, not a row-level one. | `docs/specs/module_06_projects/processes-approved.md`, anchor `"נעילה עמודתית — התפעולי ננעל, ושדות-הכספים נשארים פתוחים למנהלת-הכספים"` |
| B8 | **מ6** | Storage plan already fixed: **exactly three buckets**, decided by module-count-of-access-rules not file-type. מ8's invoice/salary-report files live in a dedicated **private** `finance` bucket (`reports` is מ6's, `marketing` is מ2's) — מ8 must reuse this plan, not invent a new bucket, and delivery to the client must be a real download-and-attach, never a signed URL embedded in an email (it expires and "looks like a bug"). | `docs/specs/module_06_projects/processes-approved.md`, anchor `"שלושה buckets, לא ארבעה ולא אחד"` and `"finance | דוח-שכר · חשבוניות | פרטי | כספים"` |
| B9 | **מ6** | *"ומה שאינו קורה כאן: הרווח הסופי אינו קופא (הובהר ע"י ישי 14/08/2026 01:17, §7.52). מ6 מקפיא קלטים... מ8 מקפיא את מספר-הרווח בחלון שלו."* מ6 only freezes the *inputs* (per-hostess `actual_hours`, attendance, `projects.actual_hours`/`actual_guests`, the closing timestamp) at operational close — the actual profit **number** is frozen by מ8, inside מ8's own (financial) closing action, not automatically inherited from מ6's. | `docs/specs/module_06_projects/processes-approved.md`, anchor `"מ6 מקפיא **קלטים**... מ8 מקפיא את מספר-הרווח בחלון שלו"` |
| B10 | **מ6** | *"§7.19 — גזירת התשלום מהשעות — מ6 מזין את השעות; החישוב שייך למ8."* Half of this is already resolved: `project_bonus` (a project-level bonus) was **deleted** (מ6 decision ㉟ — Ishay: *"לא עשינו בהצעת מחיר בונוס לפרויקט, בוא נמחק את זה"*), so there is nothing to "distribute" at the project level. `assignments.personal_bonus` (per-hostess) survives and stays owned by מ8 — *"החלטה כספית שייכת למסך-הכספים."* Still open for מ8: how "actual hours per hostess" (which מ6 writes, overridable) turns into pay. | `docs/specs/module_06_projects/spec.md`, anchor `"מ6 מזין את השעות; החישוב שייך למ8"`; `processes-approved.md`, anchor `"`assignments.personal_bonus` נשאר — ובבעלות מודול 8"` |
| B11 | **מ6 (shared infra rule, illustrated on מ6's own case)** | `email_log.entity_type`'s CHECK constraint does not know מ8's values (e.g. `invoice`/`salary_report`) yet, and a journal-write failure there is **silently swallowed** by the Edge Function. The binding rule for any module adding a value: *"מ8/מ11 יוסיפו את הערך שלהם יחד עם המיגרציה שמרחיבה את ה-CHECK, לא לפניה"* — deploy the Edge-Function change **together with**, never before, the migration that widens the CHECK (deploy-first ⇒ mail goes out, log write silently fails, no one is notified). | `docs/micro_guides/module-6.md`, anchor `"מ8/מ11 יוסיפו את הערך שלהם יחד עם המיגרציה שמרחיבה את ה-CHECK, לא לפניה"`; also `docs/micro_guides/module-3.md`, anchor `"add your value **together with** the migration that widens the CHECK, never before"` |
| B12 | **מ5** | *"בסיס-העלות לרווחיות: actual_qty או planned_qty — מ8/מ11."* מ5 explicitly refuses to make this call — it supplies `actual_qty` (goods received) but the decision of which quantity profit should be computed on belongs to whichever module builds the profitability calculation. | `docs/specs/module_05_logistics/spec.md`, anchor `"בסיס-העלות לרווחיות"` |
| B13 | **מ5 (declared limitation, inherited)** | *"㉗ — חוב-דיווח מתוחם... שורות שנולדו משינוי-תכולה נשארות בלי מצביע-מקור, ודו"ח-הרווחיות אינו ניתן לחישוב עבורן. נרשם כאן כדי שמ8/מ11 לא יגלו את זה בהפתעה."* A DB constraint (`logistics_origin_exactly_one`) plus an RPC ordering issue (`apply_scope_change` inserts the `logistics` row before `project_changes` exists) means scope-change-born logistics rows can never carry a source pointer — מ5 declares this **will not be fixed** by מ5. | `docs/specs/module_05_logistics/processes-approved.md`, anchor `"נרשם כאן כדי שמ8/מ11 לא יגלו את זה בהפתעה"` |
| B14 | **מ3 (security note, not yet a ruling)** | `quote_services.closing_unit_cost` (the frozen per-line cost) is readable at `view`-tier under the "הצעות מחיר" module gate — the same class of cost data that `product_costs` protects at `edit`-tier. Flagged as *"לא דליפה חיה היום"* (finance's only `view` role already holds `edit` on 'כספים' anyway) but becomes one the moment a role gets `view` on quotes without `edit` on finance — relevant to how מ8 designs its own permission/RLS model for cost-bearing data. | `docs/PROJECT_MASTER.md` §6, anchor `"עלות-מוקפאת בשורות-הצעה נקראת ברמת-view"` and `"חוסר-עקביות אמיתי"` |
| B15 | **מ6 (open item explicitly not מ6's to decide)** | §7.92 — *"מייל-המשוב ללקוח: הכל קיים חוץ מהשולח. הבעלות פתוחה"* — noted under a `🚧 מ8 · 🚧 מ10` line: who actually triggers/owns sending is still undecided between מ8 and מ10, flagged for מ8's Discovery to resolve rather than assume. | `docs/PROJECT_MASTER.md` §6, anchor `"מייל-המשוב ללקוח: הכל קיים חוץ מהשולח. הבעלות פתוחה"` |
| B16 | **מ6 (candidate, not a ruling)** | A public, tokenized feedback page `/feedback/:token` (same pattern as the live `/shift/:token`) is floated as a strong candidate for מ8 to build, replacing the current Google-Forms-plus-manual-re-entry flow — explicitly marked `🔮` (future candidate) and **not** a decision מ8's Discovery is bound by. | `docs/specs/module_06_projects/processes-approved.md`, anchor `"ומועמד חזק למודול 8, לא נבנה כאן: דף-משוב ציבורי /feedback/:token"` |

---

## Checks run (for the "אין ממצאים" discipline, even though findings exist above)

- Grepped both Hebrew (`מ8·שכר·חשבונית·משוב·רווח·כספים·גבייה·סגירה כספית·בונוס·נסיעות`) and English
  (`salary·invoice·feedback·finance`) vocabularies against `module_04_hostesses/{processes-approved,spec}.md`,
  `module_05_logistics/{processes-approved,spec}.md`, `module_06_projects/{processes-approved,spec}.md` — not
  a bare `מ8` grep, per the task brief's warning that it measurably misses most hits.
- Read every hit's surrounding card/section in full (not just the matched line) in all six files.
- Read `docs/PROJECT_MASTER.md` §5 cards 5.2, 5.3, 5.13, 5.14, 5.17 in full, plus 5.6/5.7 (מ6's own cards,
  for boundary context) and §6 canonical register via `grep '🚧 מ8'` (18 hits, all read in full context).
- Ran `grep 'מ8'` on `docs/micro_guides/module-{2,3,4,6}.md` and cross-checked hit counts against the task
  brief's stated counts (13/10/1/10) — all four matched exactly, and every hit was read in surrounding context.
- Did **not** read `docs/micro_guides/module-8.md` (doesn't exist yet — this is the Discovery that creates
  it) or any `screens-approved.md`/`design-contract.md`/`world-sources.md` files (out of the task's READ list).

## Surprising / worth flagging to the parent session

1. **מ8 does not yet exist as a tracked debt-count anywhere central** — the obligations on it are scattered
   across at least 4 different files (three sibling spec pairs + PROJECT_MASTER §6), and §6 itself required
   at least 3 documented corrections/retargets over time (`🚧 מ7`→`🚧 מ8` for gross-profit, a stale line-number
   anchor for a different bug). The Discovery session should expect this sweep's two tables to be the first
   time all of it has been assembled in one place — worth a deliberate cross-check against §6 again once
   Discovery's own write-back begins, since §6 is the SSOT and this file is only a snapshot.
2. **Two "who decides" items are explicitly still open and un-owned even after this sweep** (not מ8's fault,
   but מ8's Discovery is the natural place to close them): §7.92 (B15, who triggers the feedback-survey send
   between מ8/מ10) and the "אחר" cancellation-reason storage question (mentioned in מ6's spec.md as awaiting
   Ishay, tangential to מ8's compensation calc in A6/B — not table-worthy on its own but worth knowing it sits
   right next to A6).
3. **The DB has real teeth on the boundary already** — `projects_closed_needs_report` (blocks reaching
   `awaiting_invoice`/`awaiting_payment`/`finished` without a report) and the RPC-as-permission-unit pattern
   (B6) are live architectural decisions, not proposals; מ8's Discovery should treat them as given constraints
   to design within, not open questions.
