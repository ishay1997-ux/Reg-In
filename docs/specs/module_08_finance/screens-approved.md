<div dir="rtl">

# מודול 8 — כרטיסי-המסך המאושרים (screens-approved)

> 🔴 **החוק היחיד שהקובץ הזה נכתב תחתיו:** **`processes-approved.md` הוא הקובץ היחיד בתיקיית-האפיון
> הרשאי לקבוע כלל-מוצר.** קובץ זה — משטח-הבקרה של ישי — רשאי לשאת נוסח-של-כלל **רק** תחת התג
> `🔗 מראת <מזהה-הכרעה> — SSOT: processes-approved.md (לא לערוך כאן)`. ניסוח-כלל בלי התג הזה הוא
> פגם. כל מה שמתחת לכאן הוא **תרגום** של הכרעות-קיימות למפת-מסך (מי-לוחצת-מה, מאין-בא-כל-מספר,
> אילו מצבים חסרים) — לא הכרעה חדשה. פערים אמיתיים מסומנים ⬜ ביושר, לא ממולאים בניחוש.

**מקורות שנקראו במלואם:** `processes-approved.md` (384 שורות — הרולינגים ה1–ה29 · כרטיסי-התהליכים
P1–P4 · גל-הביקורת) · `design-contract.md §⑥` (שיוך ארבעת S1–S4 לתקדים-קוד) · `data-set.md` (335
שורות — סט-הנתונים המשותף) · ששת המוקאפים המאושרים במלואם, כולל בלוקי "מה הכרעתי לבד" שבתחתית כל
קובץ.

---

## מצב

| # | משטח | מצב | מוקאפ |
|---|---|---|---|
| S1 | מסך הכספים — מבט-על (3 לשוניות · סינון) | ✅ מאושר 26/08/2026 ("מאשר את כולם הם נראים מעולה") | `docs/mockups/finance-screen/approved/01_finance_overview_approved.html` |
| S2 | חלון סגירת-תיק פרויקט | ✅ מאושר 26/08/2026 ("מאשר את כולם הם נראים מעולה") | `docs/mockups/finance-screen/approved/02_closing_window_approved.html` |
| S3 | הפקת דוח-השכר | ✅ מאושר 26/08/2026 ("מאשר את כולם הם נראים מעולה") | `docs/mockups/finance-screen/approved/03_salary_report_approved.html` |
| S4 | דף-משוב ציבורי `/feedback/:token` | ✅ מאושר 26/08/2026 ("מאשר את כולם הם נראים מעולה") | `docs/mockups/finance-screen/approved/04_feedback_public_approved.html` |
| M1 | מייל-החשבונית ללקוח | ✅ מאושר 26/08/2026 ("מאשר את כולם הם נראים מעולה") | `docs/mockups/finance-screen/approved/05_mail_invoice_approved.html` |
| M2 | מייל דוח-השכר לרו"ח | ✅ מאושר 26/08/2026 ("מאשר את כולם הם נראים מעולה") | `docs/mockups/finance-screen/approved/06_mail_salary_approved.html` |

הרשימה עצמה: 🔗 מראה — SSOT: `processes-approved.md`, טבלת "רשימת-המשטחים המאושרת" (26/08/2026).

---

# S1 — מסך הכספים, מבט-על

## ① מפת-לחיצות

| מה לחיץ | מה הוא עושה | לאן מוביל |
|---|---|---|
| לשונית "ממתין לחשבונית" (מונה `2`) | מחליפה תצוגה ללשונית זו (`role="tab" aria-selected`) | נשארת ב-S1 |
| לשונית "ממתין לתשלום" (מונה `1`, פעילה כברירת-מחדל במוקאפ) | מחליפה תצוגה | נשארת ב-S1 |
| לשונית "פרויקטים שהסתיימו" (מונה `1`) | מחליפה תצוגה | נשארת ב-S1 |
| שדות-תאריך "מ-" / "עד" | מסננים את הטבלה לפי טווח (C5 §5.6.13) | נשארת ב-S1, מרעננת טבלה |
| שדה-טקסט "שם חברה" | מסנן לפי שם-לקוח | נשארת ב-S1 |
| שדה-טקסט "מס' פרויקט" | מסנן לפי מספר-פרויקט | נשארת ב-S1 |
| קישור "נקי סינון" | מאפס את כל שדות-הסינון | נשארת ב-S1, טבלה חוזרת למלאה |
| שורת-פרויקט בלשונית "ממתין לחשבונית"/"ממתין לתשלום" | פותחת חלון-סגירה | **S2** — `aria-label="…, פתיחת חלון סגירת-תיק"` (מדויק, שתי השורות) |
| שורת-פרויקט בלשונית "פרויקטים שהסתיימו" (#13) | פותחת חלון-סגירה, נעול-לעיון (תואם לשתי השורות האחרות) | **S2** — `aria-label="פסטיבל קיץ עירוני, פתיחת חלון סגירת-תיק (נעול-לעיון)"` — ✅ נפתר 26/08/2026 (ממצא-ריכוז #1): C5 §5.6.13 "לחיצה על פרויקט מתוך הרשימה תפתח את חלון סגירת תיק הפרויקט" |
| כפתור "נסי שוב" (מצב②, תקלת-טעינה) | מנסה לטעון מחדש | נשארת ב-S1 |
| קישור "נקי סינון" (מצב③, ריק-אחרי-סינון) | מאפס סינון | נשארת ב-S1 |
| שדה-חיפוש כללי בסרגל-העליון | ⬜ שלד-אפליקציה כללי (חוזה-העיצוב §⑤), לא ספציפי ל-S1 — מטרתו כאן לא הוגדרה | ⬜ |

## ② ההחלטה האחת שהמסך משרת

**"לאיזה פרויקט לפנות עכשיו, ובאיזה שלב-טיפול הוא נמצא"** — S1 הוא לוח-הבקרה שממנו מנהלת-הכספים
בוחרת את הפעולה הבאה שלה (חתימה על חלון-סגירה ב-S2). המסך עצמו לא כותב כלום — כל כתיבה קורית
אחרי הקליק, ב-S2. **מי מכריעה:** מנהלת-הכספים (התפקיד היחיד עם `edit` על 'כספים').

## ③ מקור לכל מספר

| מספר/שדה | מקור | חלון-זמן | תג |
|---|---|---|---|
| מונה-לשונית (2/1/1) | ספירת `projects` לפי `project_status` בקבוצת-הלשונית | חי-עכשיו | 🔗 מראת א17/ה10 |
| "ימי איחור" (5) | `היום − מועד-פירעון`, כשחיובי | נגזר בזמן-תצוגה | 🔗 מראת §7.20א — הגדרת "ימי-איחור" |
| "מועד פירעון" (10/10/2026) | `invoice_sent_at + תנאי-תשלום (שוטף+30)` | נגזר | 🔗 מראת §7.20א/כרטיס-P1. ⬜ **אין היום שורת-פרמטר `תנאי_תשלום_ימים` במסד** (`data-set.md §7`) — "30" כאן הוא מראה לטקסט הכתוב בכרטיס-P1, לא קריאת-פרמטר |
| "סכום לתשלום" | `preVat ההצעה + Σ project_changes` | נגזר בסגירה-הכספית | 🔗 מראת ה2 |
| "כולל מע"מ" | `סכום × (1+18%)` | נגזר לתצוגה | 🔗 מראת א3/§7.51 |
| "רווח סופי" (%, לשונית הסתיים) | `final_profit / הכנסה`, **נגזר לתצוגה בלבד** — הסכום-₪ הנשמר יושב בשורת-משנה | קפוא בארכוב | 🔗 מראת §7.52 ("% תמיד נגזר לתצוגה, ‏₪ נשמר") |
| כוכבים + תג-ציון | `feedback_score` (1–5), ממופה לטון/תווית | חי | 🔗 מראת ה16 |

## ④ מצבים שלא על המסך

**מוצגים:** ① חסימת-הרשאה ("אין לך הרשאה לצפות בנתוני הכספים.") · ② תקלת-טעינה ("לא ניתן לטעון
את הנתונים." + "נסי שוב") · ③ ריק-אחרי-סינון ("אין פרויקט התואם לסינון שבחרת.") · הצלחה/טעון —
שלוש הטבלאות עצמן.
**חסרים, לא צוירו:** ⬜ שלד-טעינה (loading skeleton) · ⬜ "ריק-אמיתי" (לשונית עם אפס פרויקטים
**בלי** סינון פעיל — שונה ממצב③ שהוא ריק-**אחרי**-סינון בלבד).

## ⑤ הרשאות

'כספים' — קריאה+פתיחת-S2 (מטריצת-הרשאות חיה: `view` על 'פרויקטים' + `edit` על 'כספים', 🔗 מראת
א13). מנהלת-פרויקטים: "צפייה בדו"חות רלוונטיים" — 🔴 **"רלוונטיים" לא הוגדר** (🔗 מראת א11/R3-13,
המפורש כ"נשאר לשלב-המסכים... ייסגר עם כרטיס S1/S3" — לא נסגר כאן; ⬜ נשאר פתוח). מצב① (למעלה) הוא
תרגום-המסך של חסימה זו.

## ⑥ הגדרות מאחורי מילים-על-המסך

"ימי איחור" / "שוטף+30" → 🔗 מראת §7.20א. "דמי-ביטול" (התג בלשונית ממתין-לחשבונית) → 🔗 מראת
§7.20ג/ה23/ה24. "טוב"/"בינוני"/"טעון-בירור" (סולם-הציון) → 🔗 מראת ה16. "רווח גולמי" → 🔗 מראת
א7/ה2/ה17.

## ⑦ ולידציות

⬜ **לא מוגדר בקובץ-מקור כלשהו:** מה קורה בקלט לא-תקין בשדות-הסינון (למשל תאריך-התחלה מאוחר
מתאריך-הסיום) — אף מוקאפ ואף כרטיס-תהליך לא מתאר הודעת-שגיאה לשדות-הסינון. הנוסח הנעול היחיד
שכן קיים הוא נוסח-השגיאה הכללי (§④ למעלה): `"לא ניתן לטעון את הנתונים."` + `"נסי שוב"`
(01_finance_overview_approved.html, `class="err"`/`class="btn btn-outline"`).

## ⑧ פרטים שמקורם היחיד הוא המוקאפ — אושר 26/08 בחבילת ה"מאשר את כולם"

מ-`01_finance_overview_approved.html`, בלוק "מה הכרעתי לבד" (8 סעיפים) + מה שאיתרתי בעצמי:

1. **שיוך #14 ללשונית "ממתין לחשבונית", לא "ממתין לתשלום"** — נימוק מצוטט: *"data-set.md §3 קובע
   במפורש 'מופיע מיד בממתין לחשבונית'... processes-approved.md ✅ שאלת-מוצר 1: 'הלשוניות: מבוטל
   ב-1/2 לפי מצב-הפה... לעולם לא ב-3'"*.
2. **אין אריחי-KPI (StatTile) מעל הטבלה** — "השורה ב-design-contract.md §⑥/S1 מסמנת אותם 'אם
   ייבחרו' — רשות, לא חובה".
3. **עמודות-הטבלה שונות פר-לשונית** (לא סט-עמודות אחיד מ-C5 §5.6.13) — "הבריף עצמו ביקש זאת
   במפורש".
4. ⚠️ **"רווח סופי" מוצג ב-% כערך-ראשי, ‏₪ בשורת-משנה** — יש **תיקון-על-גבי-תיקון בתוך הקובץ עצמו**:
   הערת-הקוד המקורית ("מה הכרעתי לבד" #4) טוענת ההפך — *"'רווח סופי' מוצג בשקלים (₪) ולא כאחוז"* —
   אך הערת-קוד מאוחרת יותר, צמודה לתא עצמו, מתקנת זאת בפירוש: `<!-- 🔧 תיקון-בקרה 26/08 (הסשן
   הראשי): §7.52 + C5 §5.6.13 — ברשימות מציגים % (נגזר: 3,650/5,300≈69%); ה-₪ ירד לשורת-משנה.
   הערת-הצייר בנספח (שקרא ₪ כעדיפות) — נדחתה -->`. **ה-HTML בפועל תואם לתיקון (% ראשי)** — פער בין
   שני הנוסחים בתוך אותו קובץ מאושר, מיושב לטובת הקוד+ההערה-המאוחרת, לא לטובת בלוק-הפתיחה. מתועד
   כאן כי הקורא של קובץ זה לא בהכרח יבחין בסתירה הפנימית.
5. **שורות ⑳-ענבר** (#15/#12/#14) — "כל אחת נושאת פריט-פעולה אמיתי... שורת #13 (הסתיים) — רגילה".
6. **אין כפתור-פעולה ראשי בכותרת** — "הפעולה של המסך הזה היא פתיחת שורה... טבלת §⑥/S1 לא מונה
   כפתור-פעולה".
7. **שם-משתמשת גנרי ("מ")** — "לא נמצא שם-פרסונה למנהלת-הכספים באף קובץ... להמציא שם היה בדיוק
   סוג הפרט שאסור להמציא".
8. **ניסוחי-עזר לא-נעולים מסומנים ⬜ בעמוד** — כותרת-חוסר-הרשאה · "אין פרויקט התואם" · תוויות-סינון
   · "נקי סינון" (לשון-נקבה).

## ⑨ אבטחה

אינו רלוונטי — S1 הוא מסך מאובטח-בכניסה-רגילה, לא כותב בלי כניסה.

---

# S2 — חלון סגירת-תיק פרויקט

> המוקאפ מציג **שלוש תצוגות** של אותו דיאלוג: א' (#12, בתהליך) · ב' (#13, נעול-לגמרי) · ג' (#14,
> דמי-ביטול). מפת-הלחיצות/הולידציות למטה ממוזגת משלושתן ומתויגת לפי תצוגה.

## ① מפת-לחיצות

| מה לחיץ | מה הוא עושה | לאן מוביל | תצוגה |
|---|---|---|---|---|
| כפתור `✕` (סגירה) | סוגר את הדיאלוג | חזרה ל-S1 | א'/ב'/ג' |
| "בחרי קובץ" / גרירה ל-dropzone | פתיחת בורר-קבצים | מצרף קובץ-חשבונית (PDF/תמונה, עד 10MB) | א' |
| "שמור ושלח" | שולח מייל-חשבונית (M1) ללקוח, מסמן `invoice_sent`+`invoice_sent_at`, מעביר ל"ממתין לתשלום" | נשאר בדיאלוג/חוזר ל-S1 | א' — **מושבת** בתצוגה זו (ר' ⑦) |
| select "סיבת-הבירור" | בוחרת סיבה מ-5-ערכי-המילון (CHECK חי) | — | א' |
| textarea "הערות המנהלת" | תיעוד-שיחת-הבירור | — | א' — **מוצג `readonly`** במוקאפ (כבר-מולא) — ⬜ לא ברור אם עריך בזרימה חיה בזמן-הזנה ראשונה |
| "שמור סטטוס" | שמירת-ביניים בלי מעבר-סטטוס מלא | נשאר בדיאלוג | א' |
| "העבר לארכיון" | מעביר ל`finished`, מקפיא רווח-סופי, נועל תיק, ממית טוקן-משוב | סוגר דיאלוג | א' — **מושבת** בתצוגה זו (ר' ⑦) |
| "סגירה" (dlg-foot, תצוגה ב') | סוגר דיאלוג בלי פעולה (התיק כבר נעול) | חזרה ל-S1 | ב' |
| "שמור דמי-ביטול" | שומר `cancellation_fee` (הסכום הסופי) + הערה | נשאר בדיאלוג/חוזר ל-S1 | ג' |
| "ויתור על החוב" | מאפס סכום ל-0 + מחייב הערה — פעולה מפורשת | — | ג' |
| "סגור ללא תשלום" | מסלול חוב-אבוד: `written_off=true`, רווח נקפא בכל-זאת | סוגר, תג "הסתיים — לא שולם" | ג' |
| שדה "הסכום שייחתם" | עריכת-הסכום-הסופי (ברירת-מחדל: תת-הסכום המוצע) | — | ג' |
| שדה "הערת-פירוט" | טקסט חופשי נלווה לסכום | — | ג' |
| שדה "תיאור-שירות ידני" (רכיב③) | שורת-דוגמה חופשית — **לא חלק מהעוגן 3,508.00** | — | ג' |

## ② ההחלטה האחת שהמסך משרת

**"מה חסר כדי לסגור את הפרויקט הזה, ומה הצעד הבא"** — S2 הוא מנוע-ההתקדמות של P1 (חשבונית→תשלום)
ו-P3 (הקפאת-רווח וארכוב), ועוד ענף-P1 לדמי-ביטול. **מי מכריעה:** מנהלת-הכספים. **בתצוגה ב' (תיק
נעול) אין החלטה כלל** — המסך הופך לתצוגה-לעיון בלבד, זו עובדה שראוי לציין כי היא שונה מהותית
משתי התצוגות האחרות.

## ③ מקור לכל מספר

| מספר/שדה | מקור | חלון-זמן | תג |
|---|---|---|---|
| ח.פ / איש-קשר / מייל-לחיוב | `customers`/`projects`, נעול-לעיון | חי | — |
| "נסגר תפעולית" | `projects.operationally_closed_at` | חי (מ6) | — |
| ציון-משוב + תג | `feedback_score`/`negative_feedback_reason` | חי | 🔗 מראת ה16/כרטיס-P2 |
| הכנסות (תחשיב-מאזן) | `preVat + Σ project_changes` | נגזר | 🔗 מראת ה2 |
| הוצאות (תחשיב-מאזן) | `hostessActualCost() + בונוסים + נסיעות(פרמטר×משמרות) + סחורה(planned_qty×closing_unit_cost)` | נגזר | 🔗 מראת ה17 |
| רווח גולמי | הכנסות − הוצאות | נגזר, **נקפא בארכוב** | 🔗 מראת א7/§7.52 |
| סטיית-תקציב | `Σ actual_hours×rate_snapshot` מול `planned_hours × Σ rates סופיים` | נגזר | 🔗 מראת ה18 |
| רכיב① פיצוי-צוות | `Σ תעריפים × שעות-מתוכננות × %(מ-סולם-§7.16)` | נגזר | 🔗 מראת ה24 |
| רכיב② סחורה-במחיר-מלא | `Σ qty × closing_unit_price` (מחיר-**לקוח**, לא עלות) | נגזר | 🔗 מראת ה23 |
| רכיב③ שירותים-שבוצעו | הזנה ידנית חופשית, בלי מעקב-סטטוס | — | 🔗 מראת כרטיס-P1 |
| `cancellation_fee` הסופי | ברירת-מחדל = תת-סכום①+②, עריך | נשמר-כפי-שהוא | 🔗 מראת ה28 |

## ④ מצבים שלא על המסך

**מוצגים:** תצוגות-הצלחה/טעון בשלוש הצורות (א'/ב'/ג') · מצבי-חסימה (כפתורים מושבתים עם
gate-note מוסבר).
**חסרים, לא צוירו:** ⬜ טעינה (פתיחת-דיאלוג בזמן שנתוני-הפרויקט נטענים) · ⬜ שגיאת-טעינה (נכשל
לטעון נתוני-פרויקט לדיאלוג) · ⬜ **"חלונית-וידוא" לפני "העבר לארכיון"** — כרטיס-P3 מזכיר "עדכן
ושמור עם חלונית-וידוא" (🔗 מראת א37/כרטיס-P3), אך אף תצוגה כאן לא מציירת אותה · ⬜ הצלחה/כשל של
העלאת-הקובץ עצמה (רק המצב-הבינארי נבחר/לא-נבחר מצויר). מצבי-ריק/סינון אינם רלוונטיים-מושגית
לדיאלוג בודד.

## ⑤ הרשאות

'כספים' בלבד לכתיבה (RPC). ⬜ מה קורה אם משתמשת בלי `edit` על 'כספים' מנסה לפתוח S2 (מ-URL ישיר,
למשל) — לא מצויר, לא מתואר.

## ⑥ הגדרות מאחורי מילים-על-המסך

"בירור טלפוני" → 🔗 מראת כרטיס-P2/ה16. "דמי-ביטול" (שלושת הרכיבים) → 🔗 מראת §7.20ג/ה23/ה24.
"ויתור" מול "סגור ללא תשלום" → 🔗 מראת כרטיס-P3 (שתי פעולות נפרדות, לא נרדפות). "התיק נעול" →
🔗 מראת כרטיס-P3.

🔧 **פורמט תג-ניקוד-המשוב — נפתר 26/08/2026 (ממצא-ריכוז #4):** קנוני = פורמט **S1** ("טוב" בלבד,
בלי מספר בתוך התג). **בבנייה: פורמט-התג כ-S1.** לא נדרש ציור-מחדש של S2 כאן — התג הנוכחי ב-S2
(`"ציון X — תיאור"` / `"X — תיאור"`) הוא ייצוג-מוקאפ בלבד, לא חוזה-פורמט.

## ⑦ ולידציות

| שדה/פעולה | מה חייב להתקיים | מה קורה כשלא | הנוסח המדויק על-המסך |
|---|---|---|---|
| "שמור ושלח" | קובץ-חשבונית נבחר | הכפתור מושבת | `title="יש לבחור קובץ-חשבונית לפני השליחה"` + `"חסום: יש לבחור קובץ — שליחה בלי חשבונית אינה אפשרית (כרטיס-P1)."` |
| "העבר לארכיון" | `payment_date` קיים **וגם** משוב-נפתר | הכפתור מושבת | `"🔒 חסום: חשבונית טרם נשלחה ולא נרשם תאריך-תשלום — שער-הארכוב דורש גם תשלום וגם משוב-פתור (ה10 / כרטיס-P3). המשוב כאן כבר תקין; התשלום עדיין חסר."` |
| "ויתור על החוב" | הערת-חובה | לא מוצג נוסח-שגיאה ספציפי במוקאפ | `title="מאפס את הסכום ל-0 ומחייב הערה — לא מחיקה שקטה"` |
| סיבת-בירור (ציון<3) | בחירה מ-5 הערכים המדויקים ב-CHECK | ⬜ לא מצויר מסך-שגיאה על ערך חורג | הרשימה המוצגת: "איחור דיילות" · "תפקוד דיילות" · "איכות תגים" · "ניהול לקוי" · "אחר" |

## ⑧ פרטים שמקורם היחיד הוא המוקאפ — אושר 26/08 בחבילת ה"מאשר את כולם"

מ-`02_closing_window_approved.html`, בלוק "מה הכרעתי לבד" (11 סעיפים):

1. **רוחב-הדיאלוג `sm:max-w-lg` (512px)** — "לפי טבלת-השיוך §⑥ בחוזה-העיצוב עצמה... לא dlg-wide
   768px, שמשויך שם במפורש ל-S3".
2. **אזור-העלאת-קובץ (dropzone) הורכב מאפס** — "אין רכיב-קוד מדוד בחוזה-העיצוב לתבנית-העלאה".
3. **"שמור ושלח" מוצג DISABLED במכוון** — "כי `invoice_sent=false` בסט-הנתונים... מדגים ישירות
   את הכלל 'שליחה בלי קובץ נחסמת'".
4. **תג-סטטוס אדום חדש (`.tag.danger`) למשוב<3** — "‏StatusTag מתעד שישה טונים בלבד... הרחבתי
   בטוקנים קיימים בלבד (`--red100`/`--red700`), לא צבע חדש".
5. **שם-איש-הקשר "בודק אוטומטי" מוצג כפי-שהוא** — "נאמנות למה שמסך אמיתי יציג... מסך-אמת לא
   מפרשן את עצמו".
6. **אין StatTile/LtrFieldGroup בתצוגה א'** — "הבריף לא ביקש זאת שם, והפרויקט טרם שולם/נגבה".
7. **מתח "actual_hours=0" מוצג רק ברצועת-הדגמה מחוץ לדיאלוג** — "מסך-אמת לא היה מזהיר על עצמו".
8. **"סגור ללא תשלום" מעוצב `btn-outline`, לא מסגרת-אדומה** — "חוב-אבוד מוגדר ב-P3 כמסלול-סגירה
   לגיטימי, לא הרסני".
9. **שורת-השירות-הידנית מסומנת בגלוי "🎭 שורת-דוגמה"**.
10. **אווטאר "מ" בלי שם/תפקיד** — תואם ⑤⑤.⑦ של S1.
11. **"24–72 שעות" נשאר טוקן-LTR יחיד, לא מפוצל** — "טווח סמנטי אחד, לא שני מספרים נפרדים".

## ⑨ אבטחה

אינו רלוונטי — S2 מאובטח-בכניסה-רגילה (RLS + 'כספים'), לא כותב בלי כניסה.

---

# S3 — הפקת דוח-השכר

## ① מפת-לחיצות

| מה לחיץ | מה הוא עושה | לאן מוביל |
|---|---|---|
| כפתור `✕` (סגירה) | סוגר דיאלוג | חזרה למסך-אחורי (לא צויר, ⬜) |
| `.monthpick` (בורר-חודש) | פותח בורר-חודש/שנה | ⬜ **אין תקדים-קוד** — נבנה מאפס (ר' ⑧) |
| "ייצא ושלח" | מפעיל את RPC-ההפקה: יוצר `salary_reports`+שורות-snapshot, חותם `assignments`, מייצר Excel, מעלה ל-bucket, שולח מייל (M2) לרו"ח, רושם `email_log` | נשאר בדיאלוג/סוגר; שורה חדשה בהיסטוריה |
| "ביטול" | סוגר דיאלוג בלי פעולה | חזרה |
| "⬇ הורדה" (שורת-היסטוריה) | מוריד את קובץ ה-xlsx מה-bucket | קובץ מקומי |
| "צפייה בדוח הקיים ↑" (מצב-חסום) | מפנה להיסטוריה (תצוגה ב') | תצוגה ב' באותו עמוד |

## ② ההחלטה האחת שהמסך משרת

**"האם להפיק ולשלוח את דוח-השכר לחודש הנבחר"** — פעולה חד-פעמית, ידנית, פר-חודש (🔗 מראת §7.57).
**מי מכריעה:** מנהלת-הכספים בלבד.

## ③ מקור לכל מספר

| מספר/שדה | מקור | חלון-זמן | תג |
|---|---|---|---|
| שורות-שכר (מקור א) | `actual_hours × hourly_rate_snapshot + personal_bonus`, פר-שיבוץ בפרויקט-שנסגר-תפעולית | חי, `salary_report_id IS NULL` | 🔗 מראת ה7/ה15 |
| שורות-שכר (מקור ב) | `%(סולם-§7.16) × planned_hours × hourly_rate_snapshot`, פרויקט-מבוטל | נגזר | 🔗 מראת ה15/ה24 |
| עמודת-נסיעות | פרמטר `סכום_נסיעות_למשמרת` — **שתי גרסאות מוצגות בכוונה** (0 חי / 22.60 לפי ה20, טרם נזרע) | נחתם בהפקה | 🔗 מראת ה14/ה20, `data-set.md §7` |
| "5 שורות" / "620.60 ₪" | ספירה/סכימה של הטבלה | נגזר | — |
| שורת-היסטוריה | `salary_reports.sent_date`/`report_file_url`, `params.מייל_משרד_רואי_חשבון` | חי בעת-הפקה | 🔗 מראת ה9 |

## ④ מצבים שלא על המסך

**מוצגים:** תצוגה-מקדימה-לפני-שליחה (א') · היסטוריה-עם-שורה (ב') · היסטוריה-ריקה-לפני-הפקה-ראשונה
(ב', תצוגה שנייה מתויגת "לא המצב היום") · חסימת-הפקה-כפולה (ג', עם הפניה לדוח-הקיים).
**חסרים, לא צוירו:** ⬜ טעינה (RPC בזמן-ביצוע — יצירת-הקובץ יכולה לקחת זמן) · ⬜ **כשל-שליחת-מייל**
— כרטיס-P4 מתאר זאת במפורש: *"המייל נכשל — הדוח נשמר, סטטוס 'נכשל' מוצג, שליחה-חוזרת מההיסטוריה"*
— **לא מצויר בשום מקום** (טבלת-ההיסטוריה מציגה רק שורת-הצלחה) · ⬜ "אפס שורות" — כרטיס-P4:
*"אפס שורות — 'אין שעות לתשלום החודש' (תקין)"* — לא מצויר.

## ⑤ הרשאות

'כספים' בלבד (🔗 מראת כרטיס-P4). ‏`salary_reports`: RLS היום `deny-all` (0 policies) — יעד-הבנייה
קריאה ל'כספים' + כתיבה דרך RPC בלבד (🔗 מראת ג1/ה9).

## ⑥ הגדרות מאחורי מילים-על-המסך

"שורות-לא-חתומות" (מודל-השיוך-לחודש) → 🔗 מראת ה15/ה9. bucket `finance` → 🔗 מראת ה13. snapshot →
🔗 מראת §7.68.

## ⑦ ולידציות

| שדה/פעולה | מה חייב להתקיים | מה קורה כשלא | הנוסח המדויק |
|---|---|---|---|
| הפקת-חודש שכבר הופק | `period` (UNIQUE) עדיין לא קיים | חסום | `"דוח לחודש אוגוסט 2026 כבר הופק. נשלח ב-01/09/2026 אל office@cpa-firm.co.il. לא ניתן להפיק פעמיים אותו חודש (ה-UNIQUE על period חוסם גם לחיצה-כפולה, ה9)."` |
| עמודות בונוס/נסיעות בשורות-פיצוי-ביטול | — (אינן חלות) | מוצג `"—"`, **לא** `"0.00 ₪"` | הערת-שוליים: `"בונוס/נסיעות בשורות-פיצוי-ביטול: '—' ולא 0.00 ₪ — נוסחת-ה24 (הפיצוי) אינה כוללת רכיב-בונוס, וה29 קובע במפורש שאין נסיעות למשמרת שבוטלה"` |

## ⑧ פרטים שמקורם היחיד הוא המוקאפ — אושר 26/08 בחבילת ה"מאשר את כולם"

מ-`03_salary_report_approved.html`, בלוק "מה הכרעתי לבד" (8 סעיפים):

1. **בורר-החודש הוא כפתור מותאם, לא `<input type="month">`** — "הרינדור-הילידי תלוי-לוקאל-מערכת
   ועלול להציג טקסט לועזי... אין חצי-ניווט קודם/הבא, כדי לא לפתוח שאלת-כיווניות-RTL שאין לה
   הכרעה".
2. **היסטוריית-ההפקות היא card עצמאי מתחת לדיאלוג, לא בתוך `dlg-body`** — "כרטיס-P4 מנסח זאת
   כתכונה נפרדת".
3. **מצב-הריק של ההיסטוריה מוצג כתצוגה שנייה מתויגת** — "כי ב'היום' המשותף כבר קיים דוח-אוגוסט
   אחד".
4. **"—" ולא "0.00 ₪" בשורות-הביטול** — ר' טבלת-⑦ למעלה.
5. **גרסת-הנסיעות המוצגת = ה20 (22.60), לא הפרמטר-החי (0)** — "לפי הנחיית-המשימה המפורשת... סומן
   בכוכבית + הערת-שוליים".
6. **הרקע מאחורי-הדיאלוג נשאר ריק בכוונה** — "'מסך-ההפקה' עצמו... לא הוזמן לציור".
7. **עמודות "שעות"/"תעריף" בשורת-הסה"כ = "—"** — "סיכום שעות-בפועל עם שעות-מתוכננות לא נותן
   מספר בעל-משמעות".
8. **שם-המנהלת "מיכל אורן" מתויג 🎭** — "אין שם-מנהלת-כספים בסט-הנתונים; זו מוסכמת-מיתוג בלבד".

## ⑨ אבטחה

אינו רלוונטי — S3 מאובטח-בכניסה-רגילה, לא כותב בלי כניסה.

---

# S4 — דף-משוב ציבורי `/feedback/:token`

## ① מפת-לחיצות

| מה לחיץ | מה הוא עושה | לאן מוביל |
|---|---|---|
| 5 כפתורי-כוכב (`aria-label="כוכב 1"`…`"כוכב 5"`) | קובעים דירוג 1–5 | נשאר בטופס |
| textarea "רוצים לספר לנו עוד? (רשות)" | טקסט-חופשי, אופציונלי | נשאר בטופס |
| כפתור "שלח" | שולח את הדירוג+הטקסט ל-RPC הציבורי | מסך-תודה (מצב ב') |

⬜ **לא מצויר:** כפתור-נסיון-חוזר על כשל-רשת (קיים בתקדים `PublicConfirmPage`, הוצא במפורש מהיקף
המוקאפ — ר' ⑧ סעיף 7).

## ② ההחלטה האחת שהמסך משרת

**"האם הלקוח מרוצה, ומה יש לו לומר על כך"** — לכידת פסק-הדין של הלקוח על האירוע. **מי מכריעה:**
הלקוח (הדירוג+הטקסט). **ההחלטה הנגזרת** (האם נדרש בירור-טלפוני) מתקבלת ב-**S2**, לא כאן — עמוד-S4
עצמו לא מציג ניסוח-שני מותנה-ציון (🔗 מראת כרטיס-P2).

## ③ מקור לכל מספר

| מספר/שדה | מקור | חלון-זמן | תג |
|---|---|---|---|
| שם-אירוע/לקוח/תאריך-שעה | `projects`/`customers`, נעול-לעיון | חי | 🌱 (`data-set.md`) |
| דירוג (1–5) | קלט-לקוח | הגשה | נכתב ל-`feedback_score` |
| טקסט חופשי | קלט-לקוח (רשות) | הגשה | נכתב ל-`feedback_notes` |

לא קיימים מספרים-נגזרים על המסך הזה — זהו טופס-קלט, לא תצוגת-נתונים.

## ④ מצבים שלא על המסך

**מוצגים:** א׳ טופס-חי (טרם-הוגש) · ב׳ מסך-תודה (מיד-אחרי-שליחה) · ג׳ "המשוב כבר התקבל, תודה"
(הגשה שנייה) · ד׳ "הקישור אינו בתוקף" (טוקן-מת-בארכוב).
**חסרים, לא צוירו (מוצהר בכוונה):** ⬜ שלד-טעינה בזמן-אימות-הטוקן (קיים בתקדים `PublicConfirmPage`,
`function Skeleton()` — לא צויר כאן) · ⬜ כשל-רשת/"נסי שוב" (הוצא מפורשות מהיקף-הבריף) · ⬜
מצב-ולידציה על שליחה-בלי-דירוג (לא ברור מה קורה בלחיצת "שלח" עם 0 כוכבים).

## ⑤ הרשאות

**אנונימי-בטוקן**, בלי התחברות (🔗 מראת כרטיס-P2). קריאה פנימית: 'כספים' (הזנה-ידנית) + מנהלת-
פרויקטים (קריאה-בלבד בכרטיס, 🔗 מראת ㉞).

## ⑥ הגדרות מאחורי מילים-על-המסך

"בירור טלפוני נדרש" (ציון<3) → מטופל ב-S2, לא כאן — 🔗 מראת כרטיס-P2. מחזור-חיי-הטוקן (הנפקה/מוות)
→ 🔗 מראת P2/P3 (ר' ⑨ למטה).

## ⑦ ולידציות

⬜ **לא מוגדר בשום מקור:** מה קורה בלחיצת "שלח" בלי דירוג נבחר — לא מוקאפ ולא כרטיס-תהליך מתארים
הודעת-שגיאה לכך. שדה-הטקסט מוגדר-מפורש כ**רשות** תמיד (גם לציון נמוך) — 🔗 מראת בלוק "מה הכרעתי
לבד" #6 במוקאפ עצמו: *"'<3 בלי סיבה — השמירה נחסמת' (P2) הוא ולידציה על טופס-ההזנה-הידנית הפנימי
של מנהלת-הכספים... לא על טופס-הלקוח הציבורי"*.

## ⑧ פרטים שמקורם היחיד הוא המוקאפ — אושר 26/08 בחבילת ה"מאשר את כולם"

מ-`04_feedback_public_approved.html`, בלוק "מה הכרעתי לבד" (7 סעיפים):

1. **מבנה-העמוד בלי סרגל-צד/עליון כלל** — "הבריף המפורש אומר 'no sidebar, public single-card
   layout'... טבלת §⑥/S4 המפורטת בחוזה עצמו סותרת את §⑤ הכללי ומכריעה לטובתה".
2. **שיוך מצב→פרויקט נגזר, לא כתוב מפורש ב-data-set.md** — "#15 עדיין פעיל → (א); #12 הושלם
   אך-לא-אורכב → (ג); #13 אורכב → (ד)".
3. **נוסחים חדשים** (עדיין לא נעולים ב-processes-approved.md): קיקר "REG-IN · משוב על האירוע" ·
   שאלה "איך היה האירוע?" · "געו בכוכב כדי לדרג" · פלייסהולדר "לדוגמה: מה אהבתם, ומה כדאי לשפר
   בפעם הבאה" · מסך-תודה "תודה שלקחתם רגע לספר לנו איך היה!". **"המשוב כבר התקבל, תודה" ו"הקישור
   אינו בתוקף" הם ציטוט-מדויק מ-processes-approved.md, לא הומצאו.**
4. **צבע-הכוכבים טורקיז, לא ענבר** — "הדירוג *הוא* הפעולה הראשית כאן... לא לחרוג לצבע שישי".
5. **טוני-מסכי-התוצאה** — ב' ירוק (הצלחה) · ג' טורקיז ("כבר טופל") · ד' ענבר+⏳ ("זהה-בייט לטון
   ולאייקון של `dead` בתקדים").
6. **ציון<3 אינו משנה את מסך-התודה הציבורי** — ר' ⑦ למעלה.
7. **מצב-כשל-רשת הוצא במכוון** — "הבריף ביקש בדיוק ארבעה מצבים (א–ד)".

## ⑨ אבטחה — המשטח היחיד עם דרישת-פרק זה

**זוג-ה-RPC:** (1) **הנפקת-טוקן** — נטבע-עצל ("get-or-create") ברגע-שליחת-מייל-הסקר ע"י **מ6**, לא
ע"י S4 עצמו — 🔗 מראת R4-F11 בגל-הביקורת: *"הטוקן נטבע עצל (RPC get-or-create ברגע-השליחה) — בלי
לגעת ב-close_project_operationally"*. (2) **הגשת-המשוב** — RPC ציבורי שנקרא ע"י S4 עצמו בלחיצת
"שלח" — 🔗 מראת כרטיס-P2: *"הציון והטקסט נכנסים לבד (RPC ציבורי, תבנית /shift/:token)"*.

**הגבלת-קצב:** 🔗 מראת כרטיס-P2, שורת-הרשאות: *"הדף — אנונימי-בטוקן + הגבלת-קצב (תקדים
`/shift/:token`)"*.

**הגשה-אחת:** ⚠️ **לא "פעם-אחת-לנצח" באופן גורף** — 🔗 מראת הכרעת-ה"הוכרע" בעל-הכותרת "קליטת-משוב
אוטומטית (P2)": *"③ טוקן = הגשה-אחת; תיקון מאוחר — ידני ע"י המנהלת"*, **וגם** מדויק יותר בכרטיס-P2
עצמו: *"הגשה אחרי 'לא ענה' אך לפני ארכוב — נקלטת ודורסת ל'הושלם' (דאטה עדיף על דגל)"* — כלומר
הגשה שנייה **אחרי** `completed` נחסמת (מצב ג' על-המסך), אך הגשה **אחרי** `no_response`-ידני (ולפני
ארכוב) עדיין מתקבלת ודורסת. שתי הכללים שונים ולא נרדפים — שניהם מצוטטים כאן במלואם כדי שלא ייקראו
כסתירה.

**מוות-הטוקן בארכוב:** 🔗 מראת כרטיס-P3: *"'העבר לארכיון' → ... → נעילה מוחלטת + המתת טוקן-המשוב"*.
מודגם ב-S4 מצב ד' (#13, אורכב 06/09 🎭) → `"הקישור אינו בתוקף"`.

---

# M1 — מייל-החשבונית ללקוח

## ① מפת-לחיצות

**אין אלמנטים אינטראקטיביים** — זהו רינדור סטטי (passive) של מייל-יוצא, לא מסך-אפליקציה. הצ'יפ
`📎 Invoice_4127.pdf` מוצג כעובדה (מסגרת+לבן, `.chip`), לא מסומן כלחיץ (`cursor:pointer`
נעדר, אין `onclick`).

## ② ההחלטה האחת שהמסך משרת

אינו רלוונטי — M1 הוא **הפלט** של החלטה שכבר התקבלה (לחיצת "שמור ושלח" ב-S2), לא מסך-החלטה
בפני-עצמו.

## ③ מקור לכל מספר

| שדה | מקור | תג |
|---|---|---|
| נמען | `customers.email` (מייל-לחיוב, פרויקט #13) | 🌱 |
| שורת-נושא | ⬜ הומצאה — "אין פונקציית `invoiceEmailSubject` בקוד... הלכתי לפי `quoteEmailSubject` הקיים" | 🎭 מהמוקאפ — לאישור |
| קובץ-מצורף (שם) | ⬜ מומצא-להדגמה — "המערכת אינה מנפיקה/עוקבת מספרי-חשבונית" | 🎭, 🔗 מראת §7.38 |
| תאריך-שליחה | ציר-הזמן המודגם | 🎭 |
| גוף-המייל | `params.תבנית_מייל_חשבונית_מס`, שני placeholders ממולאים (`company_name`, שם-אירוע) | 🌱 |

## ④ מצבים שלא על המסך

אינו רלוונטי כמושג — רינדור-מייל בודד אינו נושא מצבי-טעינה/שגיאה משלו. **מה שכן יכול "להישבר"**
(כשל-שליחה) מתועד ב-UI של S1/S2 ("הסימון לא נרשם 'נשלח'", 🔗 מראת כרטיס-P1), לא ב-M1 עצמו.

## ⑤ הרשאות

הנמען — לקוח חיצוני, מחוץ למודל-ההרשאות של REG-IN. השולח — המערכת, דרך RPC בשער 'כספים'.

## ⑥ הגדרות מאחורי מילים-על-המסך

עוגן-עולם למבנה-הדו-מסמכים (חשבונית-מס ביום-השירות · קבלה ביום-התשלום) → 🔗 מראת כרטיס-P1. אי-
הנפקת-מספרי-חשבונית → 🔗 מראת §7.38.

## ⑦ ולידציות

אינו רלוונטי — רינדור סטטי, לא טופס.

## ⑧ פרטים שמקורם היחיד הוא המוקאפ — אושר 26/08 בחבילת ה"מאשר את כולם"

מ-`05_mail_invoice_approved.html`, בלוק "מה הכרעתי לבד" (7 סעיפים) — כולל **סטייה-מודעת מבריף-
המשימה** המתועדת גם בגוף-הדף עצמו:

1. ⚠️ **#13 צויר במקום #12 שהבריף ביקש** — מצוטט על-גבי הדף עצמו: *"הבריף ביקש 'דאטה-הקאסט של #12'
   וצרופה Invoice_12.pdf. שני אלה לא קיימים ב-data-set.md... data-set.md §6 מגדיר את M1 במפורש
   עבור פרויקט #13"*. סטייה מנומקת מהבריף-שהזמין את המוקאפ, לא מ-processes-approved.md.
2. **באנר-הדראפט-עבר בסגנון-toast "info" (סלייט), לא "warn" (ענבר)** — "סטטוס-מסמך ניטרלי אינו
   אזהרת-עסק ממשית".
3. **אין רכיב-מייל קיים בקוד** — מסגרת `.mail-head`/`.meta`/`.chip` נבנתה כשאילה מ-`.dlg-head
   .meta` (S2) + `.pill` (§2.4).
4. **בידוד-הכיווניות בגוף-המייל בתווי LRI/PDI, לא `.ltr`** — "זו בדיוק ההבחנה התיעודית ב-
   `src/lib/email.js` — לקוח-דואר שמפשיט סגנונות מבטל `<span>` אבל לא תו-יוניקוד".
5. ⚠️ **תצפית על פער אפשרי בקוד הקיים, לא-לתיקון-כאן:** *"הקוד הקיים (`quoteEmailSubject`...)
   בפועל *אינו* עובר שום בידוד — רק `body` עובר `plainTextToEmailHtml`, לא `subject`. ייתכן שזה
   פער קיים במוצר האמיתי; לא בטווח המשימה הזו לתקן"*.
6. **אין שדה "מאת" (From)** — "אין כתובת-שולח בשום מקום בדאטה-סט/params".
7. **סימון `.filled` (קו-מקווקו טורקיז) על placeholders** — "אין לו תקדים קיים".

## ⑨ אבטחה

אינו רלוונטי — M1 אינו כותב בלי כניסה-רגילה; זהו פלט-מייל שיוצא מפעולה מאובטחת ב-S2.

---

# M2 — מייל דוח-השכר לרו"ח

## ① מפת-לחיצות

**אין אלמנטים אינטראקטיביים** — רינדור סטטי, כמו M1. הצ'יפ `📎 08_2026_Payroll_Report.xlsx`
אינו מסומן-לחיץ (🔧 שם-הקובץ תוקן 26/08/2026 — ר' §⑧ סעיף 1, ממצא-ריכוז #5).

## ② ההחלטה האחת שהמסך משרת

אינו רלוונטי — פלט של החלטת "ייצא ושלח" שהתקבלה ב-S3.

## ③ מקור לכל מספר

| שדה | מקור | תג |
|---|---|---|
| נמען | `params.מייל_משרד_רואי_חשבון` | 🌱 |
| שורת-נושא | ⬜ הומצאה מאפס, מסומנת "לאישור" | 🎭 מהמוקאפ — לאישור |
| תאריך | `data-set.md §5`, מהכרטיס המאושר עצמו ("1 בספטמבר") | 🎭 |
| שם-רו"ח בגוף-המייל | ⬜ מומצא-לחלוטין — "אין שדה 'שם רו"ח' בשום מקום במסד" | 🎭 |
| גוף-המייל (השאר) | `params.תבנית_מייל_דוח_שכר` | 🌱 |
| שם-הקובץ המצורף | דפוס `[חודש_שנה]_Payroll_Report.xlsx` | 📐 |

## ④ מצבים שלא על המסך

**מוצג:** תג "✓ נשלח" (מצב-היסטוריה, **אחרי** שליחה מוצלחת). **חסרים, לא צוירו:** ⬜ מצב-טרום-
שליחה/דיאלוג-שליחה-פעיל (M2 מציג רק את מצב-ה"היסטוריה", לא רגע-השליחה עצמו — הכרעה מוצהרת, ר' ⑧
סעיף 7) · ⬜ מצב-כשל-שליחה (כרטיס-P4 מתאר "המייל נכשל — הדוח נשמר, סטטוס 'נכשל' מוצג" — לא מצויר
כאן, בדיוק כמו ב-S3).

## ⑤ הרשאות

הנמען — רו"ח חיצוני, מחוץ למודל-ההרשאות. השולח — המערכת, RPC בשער 'כספים' (🔗 מראת כרטיס-P4).

## ⑥ הגדרות מאחורי מילים-על-המסך

snapshot-בהפקה → 🔗 מראת §7.68. חוזה-3-המצבים של מנוע-המיילים (נשלח/נכשל/שליחה-חוזרת) → 🔗 מראת
כרטיס-P4.

## ⑦ ולידציות

אינו רלוונטי — רינדור סטטי.

## ⑧ פרטים שמקורם היחיד הוא המוקאפ — אושר 26/08 בחבילת ה"מאשר את כולם"

מ-`06_mail_salary_approved.html`, בלוק "מה הכרעתי לבד" (9 סעיפים):

1. 🔴 **דגל מפורש לישי/לסוכן-הבא, מצוטט מילולית (כפי שנכתב במקור):** *"שם-קובץ-הצרופה: המשימה הציעה
   '08_2026_Payroll_Report.xlsx'; החלטתי להציג 'אוגוסט_2026_Payroll_Report.xlsx' במקום, כי זה
   הערך המפורש והיחיד ב-data-set.md §5... אם השם '08_2026' באמת נדרש, זו סטייה מה-data-set שצריך
   להכריע עליה, לא רק לצייר."*
   🔧 **נפתר 26/08/2026 (ממצא-ריכוז #5):** הדפוס `[חודש_שנה]_Payroll_Report.xlsx` מעוגן ב-**C5
   §5.8.10** (תבנית-המייל החיה עצמה); הבחירה הקונקרטית — חודש כ**מספר** — היא ההכרעה שנסגרה כאן.
   שם-הקובץ תוקן ל-`08_2026_Payroll_Report.xlsx` ב-`06_mail_salary_approved.html` ובכרטיס-M2 זה
   (§① ו-§⑧ סעיף זה).
2. **אין שלד-אפליקציה** — "מייל אינו 'מסך' בתוך REG-IN... אימצתי את תבנית-העמוד הממורכזת של S4".
3. **כרטיס-המייל בנוי מ-`.dlg-head`/`.meta`/`.dlg-body`/`.dlg-foot` (תקדים-S2), בלי `scrim`**.
4. **רוחב 512px, לא 768px** — "גוף-המייל הוא פסקאות-טקסט, לא טבלת-שורות".
5. **שורת-נושא הומצאה מאפס, מסומנת "🎭 מהמוקאפ — לאישור"**.
6. **אין שדה "מאת"** — "אין פרמטר-שולח/מייל-מערכת ב-params".
7. **הכרטיס ממוסגר כ"היסטוריה", לא כדיאלוג-שליחה-פעיל** — "לפי המשפט 'היסטוריית-הדוחות מציגה
   כל מה שנשלח' (data-set §5)".
8. **M1 לא היה קיים עדיין כשנבנה M2** — "התיקייה לא הייתה קיימת כלל... מסגרת-המייל כאן נבנתה
   מעקרונות §⑤/§2.5/§2.4, ומוצעת כבסיס-משותף למי שיצייר את M1" — ⚠️ הערה זו מתיישנת בפועל: M1
   כן קיים היום (05_mail_invoice_approved.html) ובנוי מאותם עקרונות (`.mail-head`/`.meta`/`.chip`
   דומים אך לא זהים ל-`.mailshell`/`.mrow`/`.pill` של M2 — ר' ממצא-ריכוז #5).
9. **תגיות-מקור (🌱/🎭/📐) עם `title`-הסבר** — "פורמט שלא נקבע במפורש ב-design-contract".

## ⑨ אבטחה

אינו רלוונטי — M2 אינו כותב בלי כניסה-רגילה.

---

# גיליון-ההצלבה החוצה-משטחים

> שורות = תוויות-סטטוס/מחרוזות-משותפות/רכיבים-חוזרים · עמודות = ששת המשטחים · תא-סוטה = **ממצא**
> (רשום למטה, **לא תוקן בשקט**).

| # | הפריט | S1 | S2 | S3 | S4 | M1 | M2 |
|---|---|---|---|---|---|---|---|
| 1 | באנר "מאושר/דראפט" — אימוג'י+צבע | `🛠` ענבר (`.draftban`) | `🚧` ענבר (`.draftbanner`) | `🚧` **אדום** (`.draftbar`) | `🚧` ענבר (`.draftbar`, sticky) | **בלי אימוג'י**, **סלייט** (`.draft-banner`) | **בלי אימוג'י**, ענבר (`.draftbanner`) |
| 2 | שורת-"מקורות" (🌱/🎭/📐) — נוסח מדויק | "🎭 דמו-מומצא (מסומן בגוף הטבלאות ומפורט למטה)" | "🎭 דמו (אינו במסד)" | "🎭 דמו-מומצא (מסומן)" | "🎭 דמו-מתויג" | "🎭 דמו" | "🎭 דמו" (בפריסת-legend נפרדת) |
| 3 | תג-ניקוד-משוב — פורמט הטקסט | ✅ `"טוב"` (בלי מספר בתוך התג) — קנוני (נפתר 26/08/2026, ממצא #4) | `"ציון 2 — טעון בירור"` (א') / `"4 — טוב"` (ב') — בבנייה: כ-S1 | — | — | — | — |
| 4 | זהות-משתמשת בסרגל-עליון | אווטאר "מ" בלבד, בלי שם | אווטאר "מ" בלבד, בלי שם | **שם+תפקיד**: "מיכל אורן / מנהלת כספים" | אינו רלוונטי (בלי-שלד) | אינו רלוונטי | אינו רלוונטי |
| 5 | אייקוני-סרגל-צד | אימוג'י (🏠👥📄📁👤📦💰📊) | אימוג'י — זהה ל-S1 | ✅ אימוג'י — זהה ל-S1 (נפתר 26/08/2026, ממצא-ריכוז #2; היה: סמלים-גיאומטריים ▦☰▤▣☺▥₪▧) | אינו רלוונטי | אינו רלוונטי | אינו רלוונטי |
| 6 | מצב-טעינה (loading skeleton) מצויר? | לא | לא | לא | לא | אינו רלוונטי | אינו רלוונטי |
| 7 | נוסח-שגיאה "לא ניתן לטעון..." מוצג? | **כן**, מדויק (§2.7 הנעול) | לא | לא | לא | אינו רלוונטי | אינו רלוונטי |
| 8 | מצב-חסימה (gate/blocked) מצויר? | כן (הרשאה) | כן (2 כפתורים, gate-note) | כן (חודש-כפול) | כן (טוקן-מת/כבר-הוגש) | אינו רלוונטי | אינו רלוונטי |
| 9 | "היום" המשותף (15/10/2026) עקבי? | כן | כן | כן | כן | כן | כן |
| 10 | תג-הצלחה "נשלח"/"הסתיים" — פורמט | `.tag.ok`>"הסתיים" (בלי סימן-✓) | `.tag.ok`>"הסתיים" (בלי סימן-✓) | — | — | — | `.tag.ok`>**"✓ נשלח"** (עם סימן-✓ בתוך הטקסט) |

## ממצאים — כל ששת הממצאים נפתרו 26/08/2026 (ר' 🔧 בכל ממצא)

**ממצא 1 — חמור — יעד-הקליק של שורת-פרויקט "הסתיים" ב-S1 סותר את S2 עצמו. ✅ נפתר 26/08/2026**
S1 (`01_finance_overview_approved.html`) קבע ב-`aria-label` על שורת #13 בלשונית "פרויקטים
שהסתיימו": `"פסטיבל קיץ עירוני, פתיחת כרטיס-פרויקט (לקריאה)"` — משתמע מסך-כרטיס נפרד (כנראה
`ProjectCardPage.jsx` של מודול 6). אבל S2 (`02_closing_window_approved.html`) עצמו מציג **בדיוק
את אותו פרויקט #13**, "הסתיים", בתוך **אותו רכיב-דיאלוג S2** (`<div class="dlg" ...><h2
id="t13">פסטיבל קיץ עירוני</h2>`, תצוגה ב' — "תיק סגור... לא ניתן לערוך"). שני קבצים מאושרים
תיארו שני יעדים שונים לאותה לחיצה על אותו פרויקט. אף אחד מהם לא סומן כטעות בבלוק "מה הכרעתי
לבד" של קובץ-המקור שלו — כלומר שני הציירים לא ידעו זה על החלטת-זה.

🔧 **נפתר 26/08/2026 — עוגן: C5 §5.6.13** (נבדק במלואו באותו סשן), שקובע במפורש: *"לחיצה על
פרויקט מתוך הרשימה תפתח את חלון סגירת תיק הפרויקט הרלוונטי לצורך ביצוע פעולות חשבונאיות או צפייה
בנתונים"* — לכל שלוש הלשוניות, בלי חריג ל"הסתיים". הקנוני: הקליק על שורת-הפרויקט בלשונית
"פרויקטים שהסתיימו" פותח את **S2** (חלון סגירת-תיק פרויקט), בדיוק כמו שתי הלשוניות האחרות — במצב
**נעול-לעיון**, תואם לתצוגה ב' שכבר מצוירת ב-`02_closing_window_approved.html` עבור אותו פרויקט
(#13; "צפייה בנתונים" ב-C5 = "נעול-לעיון" ב-S2, לא מסך-כרטיס נפרד). תוקן ה-`aria-label` בשורת #13
ב-`01_finance_overview_approved.html` (ל-"פתיחת חלון סגירת-תיק (נעול-לעיון)"), ומפת-הלחיצות של
S1 §① עודכנה בהתאם.

**ממצא 2 — אמצעי — אייקוני-הסרגל-הצדדי ב-S3 שונים מכל שאר המשטחים. ✅ נפתר 26/08/2026**
S1 ו-S2 (ושלד-`design-contract.md §⑤` עצמו) השתמשו בסט-אימוג'י אחיד: 🏠👥📄📁👤📦💰📊. S3 היה
מחליף את כולם בסמלים-גיאומטריים/טקסטואליים: ▦☰▤▣☺▥₪▧ (ובפרט "כספים" מוצג עם `₪` ולא `💰`) — סטייה
חזותית מלאה מהשלד המשותף שהוגדר במפורש ב-design-contract.md §⑤, בלי הערה מקבילה בבלוק "מה הכרעתי
לבד" של S3 שמסבירה למה.

🔧 **נפתר 26/08/2026:** סרגל-הצד ב-`03_salary_report_approved.html` הוחלף בסט-האימוג'י התקני,
מועתק מילולית מ-`01_finance_overview_approved.html`. שלושת המשטחים (S1/S2/S3) תואמים כעת.

**ממצא 3 — קל — באנר "מאושר" נושא שלוש שפות-עיצוב שונות בשישה קבצים. ✅ נפתר-בהכרזה 26/08/2026**
ר' שורה 1 בגיליון למעלה. S1/S2/S4/M2 בענבר (אזהרה/מידע); S3 באדום (`--red50/--red200/--red700`
— באותה משפחת-צבעים ששמורה, לפי כלל-המילוי §④, לכשל/הרס); M1 בסלייט-ניטרלי. S3 מנמק את הבחירה
באדום בהערת-קוד פנימית ("שלט 'עצור, לא מאושר' על הקובץ עצמו — לא תג-סטטוס בתוך האפליקציה"), ו-M1
מנמק את הסלייט ("סטטוס-מסמך ניטרלי... לא אזהרת-עסק"). שני הנימוקים סבירים בפני-עצמם, אבל
התוצאה החזותית היא שלוש קונבנציות שונות למה שאמור להיות אותה הודעה-אחת חוזרת ("קובץ זה מאושר,
ישי כתב X").

🔧 **נפתר-בהכרזה 26/08/2026:** באנר-האישור הוא רכיב **MOCKUP-META** — "שלט" על הקובץ עצמו שמעיד
על מצב-האישור של המוקאפ כלפי קורא-הקובץ, ולעולם לא ייבנה כחלק מהאפליקציה האמיתית. פטור מהצלבה
בין המשטחים; אין כאן עריכת-קבצים.

**ממצא 4 — קל — פורמט תג-ניקוד-המשוב אינו אחיד (3 צורות שונות ב-2 קבצים). ✅ נפתר 26/08/2026**
ר' שורה 3. S1 (לשונית הסתיים): `"טוב"` בלבד. S2 תצוגה א' (ציון 2): `"ציון 2 — טעון בירור"`. S2
תצוגה ב' (ציון 4): `"4 — טוב"`. שלוש תבניות-טקסט שונות לאותו רכיב-מושגי ("תג ציון-משוב") — אף
אחת מהן לא ננעלה כמחרוזת-חוזה ב-processes-approved.md.

🔧 **נפתר 26/08/2026:** קנוני = פורמט S1 (בלי מספר בתוך התג). נרשם ב-S2 §⑥ כ"בבנייה: פורמט-התג
כ-S1" — S2 לא צויר-מחדש; מקבל בפועל את פורמט-S1 בבנייה.

**ממצא 5 — קל — שם-קובץ-הצרופה של M2 מסומן כפער-פורמט לא-מוכרע, במפורש ע"י כותב-המוקאפ עצמו. ✅ נפתר 26/08/2026**
ר' M1 §⑧ סעיף 8 ו-M2 §⑧ סעיף 1: M2 הציג `אוגוסט_2026_Payroll_Report.xlsx` (עברית-מלאה) בעוד
בריף-המשימה המקורי (שאינו קובץ-אפיון) הציע `08_2026_Payroll_Report.xlsx`. ה-M2 עצמו הדגיש: "אם
השם '08_2026' באמת נדרש, זו סטייה מה-data-set שצריך להכריע עליה" — כלומר כותב-המוקאפ **כבר סימן
את זה כפתוח**, וריכוז זה רק העביר את הדגל הלאה. תואם את דפוס `[חודש_שנה]_Payroll_Report.xlsx`
בפרמטר-התבנית החי (`data-set.md §5`/`§6`), שלא פירט אם `[חודש_שנה]` מיוצג באותיות או במספרים.

🔧 **נפתר 26/08/2026:** הדפוס `[חודש_שנה]_Payroll_Report.xlsx` מעוגן גם ב-**C5 §5.8.10** (תבנית-
המייל החיה); הבחירה הקונקרטית — חודש כמספר — הוכרעה. תוקן ל-`08_2026_Payroll_Report.xlsx` ב-
`06_mail_salary_approved.html` ובכרטיס-M2 (§① ו-§⑧ סעיף 1) של קובץ זה.

**ממצא 6 — קל — M1 ו-M2 בנו שני "שלדי-מייל" מקבילים ולא-זהים. ✅ נפתר-בהכרזה 26/08/2026**
M2 §⑧ סעיף 8 מציין שבזמן ציורו M1 עדיין לא היה קיים, ולכן M2 בנה שלד עצמאי (`.mailshell`/
`.dlg-head`/`.mrow`/`.pill`/`.src`). M1, שנכתב אחריו, בנה שלד דומה-אך-שונה (`.mail-frame`/
`.mail-head`/`.meta`/`.chip`/`.src`) — שמות-מחלקות שונים לאותם רכיבים מושגיים (תווית/ערך, צ'יפ-
צרופה, תג-מקור). שני השלדים מיישמים את אותו עיקרון חזותי (שאול מ-`.dlg-head .meta` של S2) אך אינם
קובץ-CSS משותף אחד.

🔧 **נפתר-בהכרזה 26/08/2026:** שני שלדי-המייל הם **MOCKUP-META** — סימולציה ויזואלית של רינדור-
מייל לצורך ההדגמה בלבד, לא קוד שייבנה כפי-שהוא. הרינדור האמיתי הוא `plainTextToEmailHtml`
(‏`src/lib/email.js`, ה-SSOT של מנוע-המיילים) — הפרש-ה-CSS בין שני קבצי-המוקאפ אינו כפילות-קוד
לתיקון.

</div>
