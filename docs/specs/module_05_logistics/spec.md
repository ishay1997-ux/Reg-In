<div dir="rtl">

# מודול 5 · לוגיסטיקה — האפיון המאושר

> 🛠️ **הורכב 22/08/2026 בשלב 3 של ה-Discovery.** ‏**מסמך-מסירה, לא מסמך-תוכן.**
> 🔴 **ומה שהוא *אינו*, במפורש: הוא אינו מכיל טענה אחת שאינה כבר בקובץ מאושר.** כל שורה
> עובדתית כאן **מצביעה** למקור שאפשר לגרוף. **שורה בלי מקור היא ממצא, לא ניסוח.**
> *(השער נקבע בהכרעת-ישי 22/08/2026 — הרישום המלא: `discovery-log.md`, עוגן-גריפ
> `שער-האפס-טענות`. **והוא רץ פעמיים:** בקר בהקשר-טרי בדק ‏96 טענות אחת-אחת מול מקורן
> *(הספירה עצמה: `docs/CLAUDE_CODE_LOG.md`, רשומת 22/08).*)*

---

## ① 📚 רשימת-הקריאה של הבלופרינט — **קרא בסדר הזה**

| # | הקובץ | מה תיקח ממנו | 🔴 |
|:-:|---|---|---|
| **1** | **`processes-approved.md`** | ‏**הסמכות.** ‏**42 הכרעות `①–㊷`** *(תוקן 25/08/2026 — כאן נכתב 38, והרשם כבר החזיק 41: ‏㊴·㊵·㊶ נוספו אחרי שהשורה נכתבה, ושלושתן של ישי ומשנות-בנייה. ‏㊷ נוסף 25/08. **הרשם הוא הסמכות למניין, לא השורה הזאת**)* · ‏7 כרטיסי-תהליך `🔄א–ז` · עובדות-מסד `🧱①–⑨` · לדג'ר בן 30 דרישות | **קרא במלואו** |
| **2** | **`screens-approved.md`** | שני כרטיסי-המסך, תשעה סעיפים כל אחד — מפת-לחיצות · מקור לכל מספר · הרשאות · ולידציות | **קרא במלואו** |
| **3** | **המוקאפים** — `docs/mockups/logistics-screen/approved/` | ‏`01_overview_approved.html` · `02_dialog_checklist_approved.html`. **‏HTML — קרא אותם, אל תסתמך על תיאור מילולי** | **המראה סופי** |
| **4** | **`design-contract.md`** | פלטה שנמדדה · תגים · RTL · טיפוגרפיה · שלד-HTML | לפי הצורך |
| **5** | **`data-set.md`** | אילו שורות מצוירות · "היום" המשותף · הקצאה פר-משטח | לפי הצורך |
| **6** | **`docs/db_roadmap.md`** — בלוק `Module 5 (logistics)` | ‏`M5-1`…`M5-8`. 🔴 **רשם נקרא תמיד** | **חובה** |
| **7** | **`world-sources.md`** | התשובה בכנס ל*"למה ככה ולא אחרת"* | לפני הצגה |
| **8** | **`PROJECT_MASTER §6`** | רשם-החובות — ‏`🚧 מ5 ← מ6` *(שבעה פריטים)* · `🚧 מ11 ← מ5` *(שניים)* | **חובה** |
| **9** | **`docs/PROJECT_MASTER_sec7.md`** | פריטים `7.22` · `7.31` · `7.41` | **חובה** |
| **10** | **`docs/schema.sql`** | שמות-עמודות בפועל — **לפני שכותבים שאילתה** | **חובה** |
| **11** | 🔴 **`supabase/migrations/`** — **התיקייה, לא קובץ בודד** | **גופי-הפונקציות.** ‏`M5-3` · `M5-4` · `M5-7` הם **עריכות לגוף פונקציה קיימת**, ו-`schema.sql` מצהיר בעצמו שהגופים אינם בו ⇒ **‏3 מתוך 8 עוגני `M5` בלתי-ניתנים לביצוע בלעדיה** | **חובה** |
| **12** | **`src/lib/projectLogistics.js` · `projects.js` · `projectChanges.js`** | **המחרוזות והפונקציות שאסור להמציא מחדש** *(פריט-חוזה 1 מצביע אליהן בשמן)* | **חובה** |
| **13** | **`src/lib/projectLogistics.test.js`** | **הדרך היחידה לדעת *אילו* שלושה מתוך חמשת משפטי-הריק באמת נעולים** | לפני ניסוח-מחדש |
| **14** | **`src/modules/06_projects/`** — ‏`ScopeChangeDialog.jsx` · `LogisticsTab.jsx` · `api.js` | **שלוש האדוות למ6 חיות שם**, ובפרט ה-`min="1"` הקשיח שפריט ④ של ㊳ משנה 🔴 גובר: ה-`min` כבר דינמי — `removable ? 0 : 1` · 27/08/2026 · `ScopeChangeDialog.jsx` (צעד 4.2) | לפני האדוות |

🚫 **ומה לא לקרוא — *בשמו*, ורק אלה:** ‏`docs/mockups/logistics-screen/01.png` ו-`02.png`
*(קדם-Discovery, אפס אמינות — הכרעה ①)*. **המוקאפים המחייבים הם שני קובצי ה-`approved/` בלבד.**

➕ **תוספת 25/08/2026 — שורות שבונה-מדומה מדד שחסרות כאן** *(רץ בפתיחת-הבלופרינט; הרשימה
המקורית כיסתה כללים ומראה ופספסה את יעדי-האדוות)*. **‏15** | **יעדי-האדוות למ6, בשמם:**
‏`docs/micro_guides/module-6.md` *(‏`AR-4` — כולל שורת-השמירה בראשו שאוסרת "לתקן בחזרה"; התיקון
מנומק ב-㊳ ונעשה עם `M5-7`, לא לפניו)* · ‏`docs/specs/module_06_projects/screens-approved.md`
*(הבית השני של מחרוזת-השגיאה שנמחקת)* · ‏`src/modules/06_projects/ScopeChangeDialog.test.jsx` +
`LogisticsTab.test.jsx` *(בדיקות שנוגעות באדוות)* · שני מוקאפי-מ6 המאושרים שהאדוות נוגעות בהם
*(‏`06_dialog_scope_change_approved.html` · `03_tab_logistics_approved.html`)*. **‏16** |
רכיבי-הבית שהכרטיסים מצטטים: ‏`PermissionAwareEmpty.jsx` · `LoadingOrError.jsx` · `StatusTag.jsx` ·
`FilterPill.jsx` · ‏`src/App.jsx` + `App.routes.test.jsx`. **‏17** | ‏`supabase/migrations/CLAUDE.md`
*(פרוטוקול-ה-DB: typed-echo · ‏`pg_get_functiondef` לפני `CREATE OR REPLACE` · איסור דאטת-בדיקה
במיגרציות — הסיבה שסקריפט-הסיד אינו מיגרציה)*.
⚠️ **ו-`discovery-log.md` הוא ראיות ונרטיב — לא הוראות בנייה.**
🔴 **ומה שאסור לי לומר לך, ולכן אינו כתוב כאן: "אל תקרא רשם."** ‏**רשם חי נקרא תמיד** —
‏`db_roadmap` · `§6` · `§7` · `schema.sql`. *(תוקן 22/08/2026 בשער-האפס-טענות: הכותרת אמרה
"ואל תקרא יותר", וזה הוציא בפועל את `§7` ואת `schema.sql`. הסקיל אוסר זאת במפורש, ורושם
שבפעם הקודמת בלופרינט-ציית היה מדלג על 17 דרישות-מסד.)*

## ② המודול בשלוש שורות

**מנהלת הלוגיסטיקה** לוקחת מפרט-מכירה שכבר אושר והופכת אותו לסחורה שנמצאת במקום הנכון ביום האירוע.
**היא מזמינה · מקבלת · מסמנת · כותבת הערה** — ‏🚫 **ואינה מוסיפה, מוחקת או עורכת פריטים**; הרשימה
נגזרת אוטומטית מההצעה המאושרת. כשהפריט האחרון מסומן `מוכן` **והאיוש מלא**, הפרויקט עובר **לבד**
ל`מוכן לביצוע` ונעלם ממסכה.
**‏`M = 2` משטחים:** מבט-על לוגיסטי *(`/logistics`)* · צ'קליסט הפרויקט *(דיאלוג)*.

---

# 🔒 פריט-חוזה 1 · אוצר-המילים הנעול

**מה זה:** מחרוזות שכבר קיימות בקוד **מוזג ובדוק**. ניסוח מחדש שלהן אינו שיפור — הוא סטייה שתישבר
מול בדיקות קיימות.
🔴 **ואיפה כל אחד מהם מוגדר במלואו — כי §⑥ אינו הבית של כולם** *(תוקן 22/08 בשער)*:
**מחרוזות ואוצר-מילים** ⇒ `screens-approved.md §⑥` · **סדר-המיון** ⇒ `§③` · **מצבי-הריק** ⇒ `§④`
וטבלת-הקריאה שבראש הקובץ · **הטונים** ⇒ `design-contract.md §1.4` + `data-set.md`.

| מה | היכן הוא נעול | 🔴 המוקש |
|---|---|---|
| שלושת מצבי-הפריט | ‏`LOGISTICS_STATUS_LABELS` *(`src/lib/projectLogistics.js`)* | ‏`resolveLogisticsTone` **זורקת** על תווית לא-ממופה |
| הטונים | 🔴 **`LOGISTICS_STATUS_TONES` אינו מיוצא** — הדלת היחידה היא `resolveLogisticsTone()` | ‏`טרם החל` muted · `הוזמן` teal · `מוכן` ok |
| סדר-השורות בצ'קליסט | ‏`sortLogisticsRows` | ‏`not_started` → `ordered` → `ready`, ואז `sku`, ואז `serial_number` |
| חמשת משפטי מצב-הריק | ‏`LEGAL_EMPTY_TITLE` · `LEGAL_EMPTY_DETAIL` · `NO_PERMISSION_SENTENCE` · `BROKEN_EMPTY_DETAIL` · `LOAD_FAILURE_DETAIL` | **מנוסחים שונה בכוונה** — ⚠️ **אך רק שלושה נעולים בבדיקה** *(‏`TITLE` · `NO_PERMISSION` · `BROKEN`)*; 🔴 **`LEGAL_EMPTY_DETAIL` ו-`LOAD_FAILURE_DETAIL` אינם מוגנים** |
| מונה-המוכנות | ‏`logisticsMetric()` *(`src/lib/projects.js`)* | **נכתבה למ5, ואפס קוראים בייצור** ⇒ **בשני משטחי מ5 משתמשים בה.** ⚠️ **ומונה שני כבר קיים:** `readyItemsCount` *(`projectLogistics.js`, קורא חי ב-`LogisticsTab`)* — **אין לכתוב שלישי** 🔴 גובר: שני קוראים חיים — `LogisticsPage` · `ChecklistDialog` · 27/08/2026 · `src/modules/05_logistics/` |
| שורת-משנה · ריק-אחרי-סינון · אירוע-שעבר · קרבה | ‏`logisticsTileSub` · `filteredOutSentence` · `eventPassedSentence` · `proximitySentence` | ‏`proximitySentence` מחזירה **`בעוד N ימים`** — המסמכים יושרו אליה, לא הפוך |

🔴 **וההבחנה שנשברת הכי בקלות** *(㉘)*: ‏`טרם החל` הוא **גם** סטטוס-פריט **וגם** סטטוס-פרויקט,
**אותה מחרוזת בדיוק, שתיהן בקוד מוזג.** ⇒ **מופרדים בפריסה, לעולם לא בניסוח:** תג-הפריט **בתוך
שורת-הטבלה**, תג-הפרויקט **בכותרת הדיאלוג / שורת-התור**. **לעולם לא באותה שורה ויזואלית.**

---

# ➡️ פריט-חוזה 2 · הסדר המוכרע

**מה זה:** מי מתחיל כל תהליך, ומה קורה מעצמו. **שבעת הכרטיסים המלאים: `processes-approved.md §4`.**

| התהליך | מי מתחיל | הכרטיס |
|---|---|---|
| השורות נולדות | 🚫 **לא היא** — ה-RPC של מ3, באישור ההצעה | `🔄א` |
| היא מזמינה | **היא** | `🔄ב` |
| הסחורה מגיעה | **היא** | `🔄ג` |
| שינוי-תכולה | 🚫 **לא היא** — מנהלת הפרויקטים | `🔄ד` |
| הסרת פריט | 🚫 **לא היא, ואינה משטח של מ5** *(㊳)* | `🔄ה` |
| סעיף-היציאה | אף אחד — סעיף קבוע | `🔄ו` |
| האירוע מבוטל | 🚫 **לא היא** | `🔄ז` |

🔴 **וארבע העובדות שהסדר נשען עליהן, וכולן נמדדו** *(`processes-approved.md §5`)*:
**‏`🧱③`** רק `item_status='ready'` נספר במוכנות — ‏`ordered` **אינו** נחשב מוכן; **אפס שורות ⇒
נחשב מוכן** *(לא נתקע)*; דורש **גם** איוש 100%.
**‏`🧱④`** 🔴 **כתיבה ללוגיסטיקה של פרויקט `cancelled`/`event_finished` מצליחה בשקט — ‏*והשורה כן
מתעדכנת*.** השומר יושב בתוך `recompute_project_status` ועושה `return`; מה שלא קורה הוא **חישוב
הסטטוס**. ⇒ **אפקט קבוע על שורה שהיא ראיית-חיוב.** המסד **אינו** אוכף את כללי-הסטטוס של מ6.
**‏`🧱⑤`** לטריגר אין `WHEN`/`OF` ⇒ **כל** כתיבה מריצה חישוב מלא + `FOR UPDATE` ⇒ **שמירה
קבוצתית מסתדרת בתור** ⇒ **שמירה פר-שורה, לא טופס עם כפתור אחד.**
**‏`🧱⑥`** **אין policy-כתיבה על `logistics`** ⇒ היום `UPDATE`/`DELETE` מחזירים **0 שורות עם
`error: null`**.
🔴 גובר: policy-הכתיבה חיה — `logistics_write_by_permission` (‏`M5-1`) · 27/08/2026 · מיגרציה `20260826002445`

---

# 🔢 פריט-חוזה 3 · המספרים בני-הבדיקה

**מה זה:** אם המסך הבנוי מציג את אלה — הוא מחווט נכון. **המקור המלא: `data-set.md`.**
🕓 **"היום" של שני המוקאפים: `27/08/2026`, יום חמישי.** ‏🚫 **ולא `21/08`** — זהו תאריך-המדידה.

| מה | הערך | היכן נבדק |
|---|---|---|
| סעיף-היציאה | **2 שורות** — `#105` *(היום)* · `#106` *(יום-העסקים הבא)* | משטח 1, סעיף עליון |
| חלון-היציאה | **`27/08`–`30/08`** — מהיום ועד יום-העסקים הבא בכלל *(㉓)* | `businessDaysUntil` |
| תור-העבודה | **3 שורות** בגלולת ברירת-המחדל | משטח 1 |
| ספירות-הגלולות | **`דורש טיפול` = 3 · `ממתין למשלוח` = 1 · `הכול` = 5** | משטח 1 |
| סימון-הענבר של ⑳ | **`#107` בלבד** — פריט פיזי ב-`טרם החל` **וגם** ‏**8 ימי-עסקים** | משטח 1 |
| הצ'קליסט של `#107` | **4 שורות** — שלושת המצבים על מסך אחד | משטח 2 |
| מוכנות `#107` | **1 מתוך 4** | שני המשטחים |

🔴 **ומה שהמספרים האלה *אינם* מכסים** *(מוצהר ב-`data-set.md`)*: הם **מערך-הדגמה**, ו-**9 מתוך
15 שורות-הלוגיסטיקה בו מומצאות** *(מסומנות 🎭)*. **‏6 שורות בלבד הן 🌱 מהמסד החי.**
⇒ **‏`spec.md` אינו מתיימר שהמערך מדיד — הוא מתיימר שהוא עקבי בין שני המסכים.**

---

# 🔗 פריט-חוזה 4 · חוצה-מודולים

**מה זה:** מה שמ5 חייב לגעת בו **מחוץ לעצמו**. 🔴 **הטבלה מונה שש שורות `(מוזג)`, והן *ארבע* אדוות** — ‏`M5-3` · `M5-4` · לשונית-ההערות · **וחבילת-㊳** *(שלוש שורות: `AR-4` · תצוגת `item_status` · מחיקת מחרוזת-השגיאה — כולן נוחתות יחד עם `M5-7`)*. **וכולן לקוד חי בייצור**
— ‏**לומר זאת בקול בבנייה.**

| היעד | האדווה | האנקור |
|---|---|---|
| **מ3** *(מוזג)* | ‏`approve_quote_and_create_project` ימלא `quote_service_line_id` *(⑫)* | `M5-3` |
| **מ6** *(מוזג)* | ‏`apply_scope_change` יאפס `item_status` בהגדלת-כמות · **ולא ייגע ב-`actual_qty`** *(⑪ · ㉟)* | `M5-4` |
| **מ6** *(מוזג)* | ‏`LogisticsTab.jsx` יציג את עמודת `notes` *(㉒)* | — *(שינוי-UI, אין שורת-רשם)* |
| **מ6** *(מוזג)* | 🔴 **`AR-4` במדריך-המיקרו אומר *"the row is never deleted"* — ו-`M5-7` מוחק** | `M5-7` |
| **מ6** *(מוזג)* | ‏`ScopeChangeDialog.jsx` יציג `item_status` על שורות-לוגיסטיקה — כדי שהיא תדע מתי `0` מותר *(㊳)*. **הדאטה כבר שם** | `🔄ה` |
| **מ6** *(מוזג)* | **מחיקת מחרוזת-השגיאה** *"להסרת פריט לגמרי — פני למנהלת הלוגיסטיקה"* — היא הופכת לשקרית כש-`M5-7` נוחת | `🔄ה` |
| **המסד** *(דאטה)* | 🌱 **סקריפט-`seed` שמזריק את `#105`·`#106`·`#107` ואת שורות-הלוגיסטיקה שלהם** *(הכרעת-ישי 22/08: "פשוט שיהיה את הנתונים במערכת כאשר מייצרים")*. 🔴 **אין לו שורת-רשם בשום מקום** — ‏**וכך גם לשורת-ה-`notes` של ㉒ למעלה** *(שינוי-UI)*. **שני פריטי-בנייה בלי רשם, ושניהם חיים רק בטבלה הזאת** ⇒ **אל תחפש אותם ב-`db_roadmap` או ב-`🚧`** | `processes §🔗` |
| **המסד** | ‏`M5-1` policy-כתיבה · `M5-2` `CHECK actual_qty >= 0` · `M5-5` עמודת-צבע · `M5-6` RPC-הצ'קליסט · `M5-7` RPC-ההסרה · 🆕 **`M5-8` שני תאריכי-ההגעה** *(㊶)* | `docs/db_roadmap.md` |

> 📌 **מצב-הביצוע של הטבלה הזאת — נמדד `26/08/2026`** *(צעד 4.3; תוספת בלבד, אף שורה לא שונתה)*:
> ✅ **נחת ואומת חי:** ‏`M5-3` *(‏16/16 שורות עם מצביע)* · `M5-4` · `M5-7` · `M5-1`·`M5-2`·`M5-5`·`M5-6`·`M5-8` —
> כולן ✅ ב-`db_roadmap.md`. · ✅ **מחיקת מחרוזת-השגיאה — בוצעה**: ‏`להסרת פריט לגמרי` **נעדרת
> מהגוף החי של `apply_scope_change`** *(נמדד ב-`pg_get_functiondef`, לא מקובץ-המיגרציה)*, וחמש
> ההודעות הממוקדות שהחליפו אותה נוכחות. · ✅ **‏`AR-4` — יושב עם הערה מתוארכת בשלושה מקומות**
> ב-`docs/micro_guides/module-6.md` *(שורת-ההכרעה · שומר-הכותרת · בלוק חוזה-הפיילוד)*, ובנוסף
> ב-`docs/specs/module_06_projects/screens-approved.md`. 🔨 **בביצוע כרגע:** שתי שורות ה-UI של מ6 —
> עמודת ה-`notes` *(㉒, צעד 4.1)* ותצוגת ה-`item_status` *(㊳, צעד 4.2)*. **יסומנו רק אחרי אימות
> עצמאי בשער 4.5 — לא על סמך דיווח.**

⚠️ **ומה שמ6 מכתיב למ5 — שבעה פריטים** *(‏`PROJECT_MASTER §6`, עוגן-גריפ `🚧 מ5 ← מ6`; הסריקה
המלאה ב-`processes-approved.md §5`)*. 🔴 **הבולט שבהם: השם `line_id` שברשם מעולם לא נבחר —
העמודה בפועל היא `quote_service_line_id` והיא קיימת. אין ליצור עמודה שנייה.**
✅ **פריטים (1) ו-(7) נצרכו `26/08/2026` בראיה חיה** — הרישום המלא עם המדידות יושב ב-`PROJECT_MASTER §6`
עצמו, מתחת לשורת שבעת-הפריטים.

---

# ✅ מה ייחשב עובד

🚫 **אין כאן יעדי-מספר** — אין משתמשים אמיתיים, ומדד מומצא הוא בדיוק מה שיצטרך הגנה בכנס.

- **מנהלת הלוגיסטיקה פותחת `/logistics` ורואה מיד מה יוצא לפני שהיא שוב במשרד** — ובלי ללחוץ דבר.
- **היא נכנסת לפרויקט, מסמנת `הוזמן`, מקלידה כמות שהגיעה, וכותבת הערה** — **וההערה נראית
  למנהלת-הפרויקטים** *(㉒)*.
- **הפריט האחרון מסומן `מוכן` והפרויקט נעלם ממסכה מעצמו** — עם הודעה שמדווחת, **לא שער שמבקש
  אישור** *(⑬)*.
- 🔴 **וכתיבה שנחסמה אינה מדווחת "נשמר"** — ‏`🧱⑥` הוא בדיוק הכשל שהמודול הזה חייב לא לשחזר.
- **פרויקט שבוטל בזמן שהדיאלוג פתוח — ננעל, והיא רואה למה** *(㉝ · ㊲)*.

---

# 🎯 מה הוכרע — ומה נשאר פתוח **בכוונה**

**‏42 הכרעות `①–㊷`** ברשם שב-`processes-approved.md §3` — ‏**19 של ישי · 20 קלוד-בהאצלה ·
2 "ישי תיקן את קלוד" *(⑩ · ㊳)* · 1 נגזרת *(⑤)*.** *(תוקן 25/08/2026: כאן נכתב "38 · 15 של ישי"
בעוד הרשם החזיק 41 — ‏㊴·㊵·㊶ נפלו אחרי כתיבת השורה; ‏㊷ נוסף בסבב-הבלופרינט. **המניין ברשם גובר.**)*

**פתוח בכוונה, ואינו חוסם בנייה:**

| מה | מי סוגר |
|---|---|
| ⏸️ ‏`⑨` צ'קבוקס "יצא" · ⏸️ `⑤` "ספקים" — **תג `הנחתי`, פתוחים לפתיחה מחדש בלי טקס** | ישי |
| 📐 **שני הטורקיזים** — ‏`--primary` `#0D9488` מול `bg-teal-600` `#009689` | ישי *(כלל 8)* |
| 🧾 **‏㉗ — חוב-דיווח מתוחם:** שורות שנולדו משינוי-תכולה בלי מצביע-מקור | **מוצהר, לא פתוח** |
| 💰 **בסיס-העלות לרווחיות:** `actual_qty` או `planned_qty` | **מ8/מ11** *(`🚧 מ11 ← מ5`)* |
| ~~🔶 **סעיפי ⑧ "לאישורך"**~~ ✅ **נסגר 25/08/2026** — אישור-תוכן מלא לשני הכרטיסים בסבב-הטריאז' של הבלופרינט *(ההיקף: `screens-approved.md`, כותרת)*. נשארו שני נוסחים בשער פזה 3 *(‏O-1·O-4 במדריך)* | ~~ישי, פר-כרטיס~~ ✅ |

✅ **ומה שכבר סגור ואין להעלות מחדש:** ‏`§7.22` *(חוסר אינו חוסם)* · `§7.31` *(אין קסקדה בביטול)* ·
**ובסיס-החיוב ללקוח** — הכרעת-ישי ⑳ של מ6: **סחורה מחויבת לפי מה שהוזמן** *(העוגן: `💰`)*.

---

# 🚫 מה אסור לבלופרינט לנחש

🔴 **הסעיף היקר ביותר בקובץ. כל פריט כאן הוא עובדה שנמדדה, ושכשל בה אינו רועש.**

1. 🔴 **ש"מסך ריק" פירושו "אין נתונים".** ‏`logistics` היא RLS-on בלי policy-כתיבה ⇒ ‏`UPDATE`
   מחזיר **0 שורות עם `error: null`**. ‏**המסך אינו נכשל — הוא משקר.** ⇒ **כל כתיבה חייבת
   `.select()` ובדיקת מספר-שורות** *(`🧱⑥`)*.
   🔴 גובר: policy-הכתיבה חיה (‏`M5-1` נבנתה); משמעת ה-`.select()` נשארת · 27/08/2026 · מיגרציה `20260826002445`
2. 🔴 **שהמסד יעצור כתיבה לפרויקט מבוטל.** הוא לא — **הכתיבה "מצליחה" בשקט** *(`🧱④`)*.
   הנעילה **חייבת להיבנות במסך**.
3. 🔴 **שאפשר לסמוך על שורת-התור לסטטוס-הפרויקט.** התור הוא תצלום-רגע ⇒ **הדיאלוג קורא סטטוס
   מחדש בפתיחה** *(㊲)*.
4. 🔴 **שהסרה מותרת לפי הסטטוס לבדו.** ‏**שני תנאים, תמיד:** ‏`not_started` **וגם**
   `actual_qty = 0` *(㊱)*.
5. 🔴 **ש-`טרם החל` פירושו `actual_qty = 0`.** הגדלת-כמות מחזירה פריט ל`טרם החל` **והכמות
   שהגיעה נשארת** *(㉟)* ⇒ **השדה מציג את ערכו, לעולם לא `0` קשיח.**
6. 🔴 **שאפשר לשמור קבוצתית.** הטריגר בלי `WHEN`/`OF` ⇒ **שמירה פר-שורה** *(`🧱⑤`)*.
7. 🔴 **שאפשר לצרף ל-`customers` בשביל שם-הלקוח.** הוא **דנורמלי על `projects`** —
   הצירוף יחזיר ריק בלי שגיאה למי שחסום על 'לקוחות'.
8. 🔴 **שהשער הוא `'פרויקטים'`.** ‏**`M5-1`/`M5-6` מגודרים ב-`'לוגיסטיקה'`** — שער `'פרויקטים'`
   **נועל את בעלת-המודול מחוץ למודול שלה** *(㉞)*. ⚠️ **ו-`M5-7` הוא ההפך: `'פרויקטים'`,
   כי הוא של מנהלת-הפרויקטים** *(㊳)*.
9. 🔴 **שאפשר להמציא תווית-מצב רביעית.** ‏`item_status` הוא `CHECK` תלת-ערכי, ו-`resolveLogisticsTone`
   **זורקת**.
10. 🔴 **ש-`logisticsMetric` צריכה להיכתב.** **היא כתובה, בדוקה, ואפס קוראים בייצור.**
    🔴 גובר: יש לה שני קוראים חיים — `LogisticsPage` · `ChecklistDialog` · 27/08/2026 · `src/modules/05_logistics/`

</div>
