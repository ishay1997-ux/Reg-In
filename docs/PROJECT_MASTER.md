<div dir="rtl">

# REG-IN — מסמך אב מרכזי (PROJECT MASTER)

> מסמך-על יחיד המאחד את כל הידע על הפרויקט: תשתית, סכמת DB, הרשאות, לוגיקה עסקית ותיאור כל מסך.
> נועד להיקרא בתחילת כל סשן עבודה כדי לא לפספס כלום.

> 🗺️ **ניווט:** מפת כל התיעוד ב-[README בשורש](../README.md). כללי העבודה של Claude ב-[CLAUDE.md](../CLAUDE.md), לוח המצב ב-[STATUS.md](../STATUS.md), מפת הדרכים ב-[guides/00_roadmap.md](guides/00_roadmap.md). המקור הסמכותי המלא לתהליכים/מסכים (פרקים 5–6): `reference_spec/C5_clean_transcript.md` + `reference_spec/C6_clean_transcript.md` (תעתיקים מנוקים-קפואים; החליפו את יצואי-ה-Word המקוריים שנמחקו 09/07/2026 אחרי אימות+PASS).

> ✅ אומת-סנכרון: 10/07/2026 11:18 (regin-docs-sync — אחרי גל-הנאמנות, 0 קונפליקטים)

> ⚠️ **עדכון עקרוני (01/07/2026 — הבהרת ישי):** **המוקאפים הם כלי עזר ראשוני בלבד** — נועדו לשאוב רעיון עיצובי כללי ("איך ישי מדמיין את זה"), **הם אינם מקור אמת מחייב**. **פרקים 5 ו-6 באפיון המאושר הם שקובעים הכל** — גם את התהליכים העסקיים/הלוגיקה וגם את תיאור המסכים במילים (מבנה, שדות, פעולות, אכיפות). שני החלקים באפיון הכרחיים ומחייבים במידה שווה. בכל סתירה בין האפיון לבין מוקאפ — **האפיון קובע**, והמוקאפ הוא רק רפרנס ויזואלי כללי.

---

## תוכן עניינים
1. [סקירה כללית, Stack והיררכיית מקורות אמת](#1-סקירה-כללית-stack-והיררכיית-מקורות-אמת)
2. [סכמת מסד הנתונים (16 טבלאות)](#2-סכמת-מסד-הנתונים-16-טבלאות)
3. [מטריצת ההרשאות](#3-מטריצת-ההרשאות-אפיון-סעיף-54)
4. [שפה עיצובית כללית](#4-שפה-עיצובית-כללית)
5. [תיאור מסכים (5.0 זרימות סטטוס · 5.1–5.17 מסך-מסך)](#5-תיאור-מסכים--לפי-האפיון-הקובע-עם-המוקאפ-כרפרנס-ויזואלי-בלבד)
6. [חובות עתידיים (TODO)](#6-חובות-עתידיים-todo--מתוך-changelog)
7. [סתירות/פערים פתוחים — רישום ההחלטות](#7-️-סתירותפערים-פתוחים--להכרעה-משותפת-עם-ישי)

---

## 1. סקירה כללית, Stack והיררכיית מקורות אמת

**מה זה REG-IN:** מערכת ניהול עסקית מודולרית לחברת אירועים/כנסים (הרשמה, דיילות, לוגיסטיקה), הבנויה מודול-אחר-מודול בעבודת שני מפתחים (ישי ועמית).

**Stack טכנולוגי:**
- React 19 + Vite 8 · JavaScript בלבד (לא TypeScript)
- Tailwind CSS v4 + shadcn/ui + Radix · Lucide (אייקונים) · Geist (פונט)
- Supabase (Auth + Postgres DB)
- ממשק עברי RTL מלא · alias `@/` → `src/`
- לקוח Supabase: `src/supabaseClient.js` → מיובא כ-`@/supabaseClient` (⚠️ לא `@/lib/`)

**מבנה קוד:** כל מודול בתיקייה משלו תחת `src/modules/NN_name/`. משותף → `components/` ו-`lib/`.

**היררכיית מקורות אמת (הגבוה גובר בסתירה):**
1. **`docs/schema.sql`** — סכמת ה-DB. החוזה הטכני הקשיח (שמות עמודות, טיפוסים, constraints). [ראה snapshot מלא](schema.sql) · [שינויים דרך migrations](../supabase/migrations/)
2. **האפיון המאושר, פרקים 5–6** (`docs/REG-IN - דוח 2 מאושר .docx`) — **קובע הכל**: גם התהליכים/הלוגיקה העסקית (נוסחאות, זרימות סטטוס, הרשאות 5.4) וגם תיאור המסכים במילים (5.6.1–5.6.16, 5.7: מטרה, רכיבים, אכיפות). שני הסוגים הכרחיים באותה מידה.
3. **המוקאפים** (`docs/mockups/`) — **רפרנס ויזואלי כללי בלבד**. מטרתם לתת "וייב" עיצובי (צבעים, פריסה כללית), **לא** מפרט מחייב. כשיש סתירה בין מוקאפ לאפיון — האפיון קובע.
4. **מדריכי המודולים** — מתכון לסדר העבודה. הכי נמוך בסולם.

**מצב נוכחי:** מתועד **אך ורק** ב-[STATUS.md](../STATUS.md) (לוח המצב החי) — לא כאן, כדי שלא יהיו שני מקורות אמת לסטטוס.

---

## 2. סכמת מסד הנתונים (16 טבלאות)

### 2.1 ליבת ההרשאות (מודול 1)
| טבלה | עמודות מפתח | הערות |
|------|-------------|-------|
| `roles` | `role_id` (PK, serial), `role_name` | 5 תפקידים |
| `modules` | `module_id` (PK, serial), `module_name` | 9 מודולים מוגנים (מסך הבית לכולם, לא ב-RBAC — ראו §7 פריט 10) |
| `permissions` | PK מורכב (`role_id`,`module_id`), `permission_level ∈ {edit,view,blocked}` | **טבלת הצומת = המטריצה** |
| `users` | `email` (PK), `role_id`→roles, `full_name`, `phone`, `status ∈ {active,inactive}` | המייל = זהות Supabase Auth; `inactive` = מחיקה רכה |
| `login_attempts` | `email` (PK), `failed_count`, `locked_until`, `last_attempt_at` | נעילת-חשבון אחרי כשלי-התחברות (מודול 1); RLS-on/0-policies — גישה רק דרך 3 פונקציות `SECURITY DEFINER` |

**הקשר (לב ה-RLS):** משתמש מזוהה ב-Auth לפי `email` → שורה ב-`users` → `role_id` → חיפוש ב-`permissions(role_id, module)` → `edit`/`view`/`blocked`. ה-DB אוכף, אי אפשר לעקוף מהלקוח.

### 2.2 שאר הטבלאות (דאטה עסקי)
| טבלה | מודול | עמודות עיקריות |
|------|-------|----------------|
| `customers` | 2 | `customer_id`(PK), `customer_type ∈ {private_company,government,production_company,nonprofit}`, `company_name`, `contact_name`, `phone`, `email`, `discount_percent`, `marketing_consent`, `status` |
| `products` | 3 | `sku`(PK), `item_name`, `category ∈ {site,hostess,product}`, `unit`, `base_price`, `cost`, `status`, `image_url` |
| `price_tiers` | 3 | PK(`sku`,`min_qty`), `special_price`, `max_qty` — מחירון מדורג |
| `params` | 9 | `param_id`(PK), `param_name`, `param_value`, `param_type ∈ {pricing_timing,control_alerts,smart_match,templates,integration_tech}` |
| `quotes` | 3 | `quote_id`(PK), `customer_id`, `event_name`, `issue_date`, `estimated_hours`, `recommended_hostess_count`, `estimated_guests`, `estimated_event_date`, `estimated_location` (⚠️ נושא-עומס — Smart Match קורא אותו), `quote_status ∈ {in_progress,approved,rejected}`, `applied_customer_discount`, `manual_discount`, `rejection_reason`, `notes`, `pdf_url` (deprecated — §7.12; גורל: §7.71) |
| `quote_services` | 3 | PK(`quote_id`,`sku`,`line_number`), `qty`, `closing_unit_price`, `color` |
| `projects` | 6 | `project_id`(PK), `quote_id`(unique), `owner_email`, `final_event_date`, `required_hostess_count`, `project_bonus`, `project_status`(8 ערכים), `invoice_sent`, `feedback_status`, `actual_guests`, `actual_hours`, `payment_date`, `feedback_score`(1-5) |
| `hostesses` | 4 | `id_number`(PK), `full_name`, `phone`, `email`, `city`, `hourly_rate`, `rating`(1-5), `status`, `bank_name`, `bank_branch`, `bank_account` |
| `salary_reports` | 4 | `report_id`(PK), `sent_date`, `report_file_url` |
| `assignments` | 4 | PK(`project_id`,`id_number`,`assignment_number`), `assignment_status`(5 ערכים), `hourly_rate_snapshot`, `actual_hours`, `personal_bonus`, `salary_report_id` |
| `logistics` | 5 | PK(`project_id`,`sku`,`serial_number`), `planned_qty`, `actual_qty`, `item_status ∈ {not_started,ordered,ready}` |

> ℹ️ **הערת ניווט (07/07/2026; עודכן 09/07/2026 — נפתר בתעתיקים):** יצואי-ה-Word המקוריים (נמחקו 09/07/2026) סבלו משתי מלכודות-ניווט שכעת **תוקנו בתעתיקים**: ‏(1) ב-C6 טבלת-השדות של **Projects** ישבה פיזית תחת §2.4.2 (Quotes) במקום §2.4.4 — בתעתיק `C6_clean_transcript.md` הועברה למקומה ב-§2.4.4 (fix-b, §9#1). ‏(2) ב-C5 מספור-הכותרות היה `1.6.x` בעוד ההפניות-הפְּנים מצטטות `5.6.x` (למשל C5:956→"5.6.17.3") — בתעתיק `C5_clean_transcript.md` הפרק כולו רונמר `1.x→5.x` (עקבי עם ההפניות וילדי-`5.7.x`). ר' הערות-§9 והפרובננס בתעתיקים.

---

## 3. מטריצת ההרשאות (אפיון סעיף 5.4)

`V` = עריכה (edit) · `👁` = צפייה (view) · `➖` = חסום (blocked)

| תפקיד | לקוחות | הצעות מחיר | פרויקטים | דיילות | לוגיסטיקה | כספים | דו"חות | ניהול הרשאות | הגדרות מערכת |
|-------|:--:|:--:|:--:|:--:|:--:|:--:|:--:|:--:|:--:|
| **מנכ"ל** | V | V | V | V | V | V | V | V | V |
| **מנהלת פרויקטים** | V | V | V | 👁 | 👁 | ➖ | 👁 | ➖ | ➖ |
| **מנהלת כספים ולקוחות** | V | 👁 | 👁 | ➖ | ➖ | V | V | ➖ | ➖ |
| **מנהלת גיוס ושיבוץ** | ➖ | ➖ | 👁 | V | ➖ | ➖ | ➖ | ➖ | ➖ |
| **מנהלת לוגיסטיקה** | ➖ | ➖ | 👁 | ➖ | V | ➖ | ➖ | ➖ | ➖ |

**מפורש באפיון:** המנכ"ל = עריכה להכל והיחיד עם גישה ל"ניהול הרשאות"+"הגדרות מערכת"; 3 רמות בלבד (תואם `check` בסכמה). **9 מודולים בלבד** (לא 10) — **"מסך הבית" הוכרע (01/07/2026, ישי) כנגיש לכולם תמיד ואינו שורה בטבלת `modules`/`permissions`**: הוא קיים כמסך/מודול-קוד (`src/modules/07_dashboard/`) אך אינו חלק ממערכת ה-RBAC, ולכן הוסר מהטבלה כאן. חלק מהתאים הנותרים הוסקו מהיגיון הפרדת התפקידים — אלו ערכי **Seed** התחלתיים שהמנכ"ל עורך דינמית. **הערה:** ערכי המוקאפ שונים בחלק מהתאים — **הוכרע (07/07/2026, §7.4): האפיון (הטבלה שכאן) קובע; ערכי המוקאפ נדחו.**

---

## 4. שפה עיצובית כללית

> ✅ **עדכון (06/07/2026, ישי): טבלת הצבעים והפריסה שלהלן אושרו כשפת העיצוב המחייבת של המערכת.** Claude לא משנה צבעים/פריסה בלי אישור ישי (כלל ברזל 8 ב-`CLAUDE.md`). ההיררכיה נשמרת לגבי **מבנה, שדות ולוגיקה**: כשיש תיאור מסך מפורש באפיון (5.6.x, סעיף 5 להלן) שסותר או מוסיף על המוקאפ — האפיון קובע; אבל הצבעים והסגנון — לפי הטבלה כאן.

**צבעים (משוער מהתמונות, לאימות מול המותג):**
- **פרימרי — טורקיז/teal** `~#14B8A6`: כפתורים ראשיים, אייקון פעיל, טוגלים, כוכבי דירוג, לינקים.
- **הצלחה/אישור — ירוק** `~#22C55E`: מודל "אשר הצעה", סטטוס "שולם", "פעיל".
- **סכנה/דחייה — אדום** `~#EF4444`: מודל "דחה הצעה", מחיקה, חובות, איחורים.
- **אזהרה — כתום/צהוב** `~#F59E0B`: סטטוס "בתהליך", תשלום חלקי.
- **כהה — נייבי** `~#1E293B`: כותרות כרטיסי דו"ח, עמודות גרפים.
- **רקע:** אפור בהיר `#F8FAFC`, כרטיסים לבנים, גבולות `#E2E8F0`.
- **גרפים/לוח שנה:** כחול `#3B82F6`, סגול, כתום, ירוק לקטגוריות.

**פריסה כללית (RTL):**
- סרגל צד קבוע מימין (לבן, לוגו REGIN למעלה), פריט פעיל = רקע טורקיז בהיר + טקסט טורקיז.
- סרגל עליון: כותרת המסך (מימין), חיפוש (מרכז), גלגל שיניים + פעמון + אווטאר (משמאל).
- רכיבים חוזרים: כרטיסי KPI לבנים (פינות מעוגלות, צל רך), טבלאות עם אייקוני פעולה, תגי סטטוס (pill), מודלים מרכזיים, טוגלים טורקיז, stepper אופקי, דירוג בכוכבים.

---

## 5. תיאור מסכים — לפי האפיון (הקובע), עם המוקאפ כרפרנס ויזואלי בלבד

כל מסך מפורט להלן **קודם לפי מה שכתוב באפיון** (מטרה, רכיבים, לוגיקה/אכיפות — ציטוט/פרפרזה נאמנה לטקסט המאושר), **ואז** הפניה למוקאפ כתחושה כללית בלבד.

### 5.0 זרימות סטטוס מרכזיות (מהאפיון — חוצות מספר מסכים)

**(א) הצעת מחיר** (`quotes.quote_status`): `בתהליך → מאושרת` (הופכת לפרויקט) | `→ נדחתה`. פסילה אוטומטית ל"נדחתה" אחרי 30 יום ללא מענה. דחייה ידנית דורשת סיבה מרשימה סגורה. אישור לקוח ממיר מיידית לפרויקט חדש בסטטוס "טרם החל" וגוזר משימות למטה. הצעה מאושרת נעולה — כל שינוי הוא "שינוי תכולה" ברמת הפרויקט.

**(ב) פרויקט** (`projects.project_status`, 8 ערכים): `טרם החל → בתהליך → מוכן לביצוע → אירוע הסתיים → ממתין לחשבונית → ממתין לתשלום → הסתיים`; ובכל שלב פעיל אפשר `בוטל`. מעברים אוטומטיים: פעולה ראשונה (שיבוץ/לוגיסטיקה) → "בתהליך"; 100% בשני המדדים (שיבוץ+לוגיסטיקה) → "מוכן לביצוע"; חלף תאריך האירוע → "אירוע הסתיים"; שינוי תכולה על פרויקט "מוכן לביצוע" מחזיר אוטומטית ל"בתהליך". סגירה תפעולית (שעות+אורחים+דו"ח) → "ממתין לחשבונית"; סימון חשבונית נשלחה → "ממתין לתשלום"; תאריך תשלום+חישוב רווחיות (+ציון משוב חובה) → "הסתיים" (ארכיון).

**(ג) שיבוץ** (`assignments.assignment_status`, 5 ערכים): `ממתין → אושרה זמנית → אושרה סופית`; או `סירבה`; או `שוחררה`. רשומות "סירבה" נשמרות להיסטוריה ולא נספרות במדד ההתקדמות; אין מחיקה. המערכת מונעת אישור סופי לדיילת המשובצת בתאריך אחר.

**(ד) פריט לוגיסטיקה** (`logistics.item_status`): `טרם החל → הוזמן → מוכן`. 100% "מוכן" מעביר את הפרויקט ל"מוכנים"; הוספת פריט מחזירה ל"בתהליך".

**(ה) משוב** (`projects.feedback_status`): `לא נשלח → נשלח → הושלם` | `לא ענה`. ציון ≥3 חיובי (משפיע על KPI); <3 מחייב בירור טלפוני + סיבה.

---

### 5.1 מסך התחברות ואימות · אפיון 5.6.1 · מודול 1
**לפי האפיון:** שער כניסה מאובטח; מאמת זהות, מזהה תפקיד ומנתב למסך הבית תוך הסתרת מודולים לא-מורשים.
**רכיבים:** שדות דוא"ל ארגוני+סיסמה · קישור "שכחת סיסמה?" (שליחת קוד איפוס למייל) · **כפתור "התחברות עם Google"** (OAuth אמיתי מול Supabase) · כפתור "כניסה" · הודעת שגיאה ידידותית בפרטים שגויים.
**אבטחה (מודל מעודכן — במקום CAPTCHA):** (1) **Google Sign-In** — שער ההרשאה (שורת `users` פעילה) נאכף מרכזית ב-`AuthContext`; חשבון גוגל שאינו במערכת מנותק. (2) **נעילת חשבון אחרי 5 ניסיונות כושלים** — מונה `login_attempts` ב-DB, נעילה ל-15 דק' (ראו `schema.sql`), מגובה ב-rate limiting המובנה (פר-IP) של Supabase. (3) **התמדת session**: `sessionStorage` (לא localStorage) — סגירת לשונית/דפדפן מנתקת (בטיחות מחשב משותף בעמדות אירוע), רענון דף שומר על ה-session.
**טבלאות DB:** `users` (זיהוי role_id לפי email); `login_attempts` (מונה נעילה).
**מוקאפ:** אין תיקיית מוקאפ למסך זה — הקוד הקיים (`src/modules/01_auth/LoginPage.jsx`) הוא המימוש בפועל.
> ✅ **סטייה מאושרת מהאפיון (5.6.1) — הוכרעה (03/07/2026, ישי):** דרישת ה-CAPTCHA **בוטלה** לחלוטין (פוגעת ב-UX ובבדיקות אוטומטיות במערכת פנימית סגורה), מוחלפת ב-Google Sign-In + נעילת-חשבון. האפיון הקפוא (`reference_spec/C5`) **לא נערך** — ההחלטה מתועדת כאן ובמדריך מודול 1. ראו סעיף 7, פריט 8.

### 5.2 מסך הבית / Dashboard · אפיון 5.6.2 · מודול "מסך הבית"
**לפי האפיון:** צומת ניווט ראשי + מערכת תומכת החלטה בזמן אמת לכלל התפקידים.
**רכיבים:** 4 כרטיסי KPI (רווח גולמי משוער לחודש, פרויקטים פעילים, ממוצע שביעות רצון רבעוני, הצעות מחיר בהמתנה) · לוח אירועים חודשי דינמי (30 יום קדימה) בצבעי חיווי · תפריט ניווט צדדי לכל המודולים · העמקה בלחיצה על אירוע/מונה הצעות.
**טבלאות DB:** `projects`, `quotes` (אגרגציה ל-KPI ולוח האירועים).
**מוקאפ (רפרנס ויזואלי בלבד):** `mockups/control-screen/01.png`, `02.png` — תואם באופן כללי: 4 כרטיסי KPI + לוח שנה + סרגל צד.

### 5.3 ניהול לקוחות ושיווק · אפיון 5.6.3 · מודול 2
**לפי האפיון:** מאגר לקוחות B2B מרכזי (CRM): שימור ידע היסטורי, מניעת הזנה כפולה ודיוור שיווקי מבוקר.
**רכיבים:** רשימת לקוחות ניתנת לסינון · כרטיס לקוח + היסטוריית פרויקטים · אזור פעולות שיווקיות (העלאה ושליחה למאושרים בלבד) · ולידציית ייחודיות ח"פ (9 ספרות) · מדדים נגזרים (סה"כ הכנסות, ציון משוב ממוצע).
**טבלאות DB:** `customers` (עיקרי); שביעות רצון נגזרת מ-`projects.feedback_score`; קבצי שיווק → Supabase Storage.
**מוקאפ (רפרנס ויזואלי בלבד):** `mockups/customers-screen/01-06.png` (6 תמונות) — רשימה, העלאת קובץ, סינון, הוספה/עריכה.
> ⚠️ תוויות "סוג לקוח" במוקאפ לא תואמות ל-enum בסכמה — פתוח להכרעה (סעיף 7, פריט 3).

### 5.4 הפקת הצעת מחיר ראשונית · אפיון 5.6.4 · מודול 3
**לפי האפיון:** מנוע תמחור חכם ואחיד המחליף חישוב ידני; יוצר/עורך הצעה ומפיק PDF ללקוח. תהליך: הזנת יחס "אורחים לדיילת" → המלצת כמות דיילות אוטומטית (ניתנת לדריסה ידנית) → תמחור דיילות לפי תעריף קטלוגי קבוע (זהות הדיילת לא ידועה בשלב זה) → סיכום פיננסי שקוף (ביניים, הנחה קבועה, הנחה ידנית, מע"מ) → "שמור ושלח" שומר בסטטוס "בתהליך", מפיק PDF, שולח ללקוח.
**רכיבים:** הזנת לקוח+נתוני אירוע (שם, תאריך, מיקום, שעות, אורחים) · המלצת כמות דיילות · קטלוג שירותים דינמי + מחירון מדורג (Price Tiers) · סיכום פיננסי (ביניים/הנחות/מע"מ) · כפתור "שמור ושלח"/"עדכן הצעה".
**נוסחאות (מהאפיון, מאומתות מול הטקסט המקורי):**
- **יחס אורחים-לדיילת:** פרמטר גלובלי — ברירת מחדל **1 דיילת לכל 50 אורחים**. כמות מומלצת = תקרת(אורחים/50), ניתנת לדריסה. דוגמה: 300/50 = 6 דיילות.
- **מע"מ: 18%** קבוע (פרמטר גלובלי). מע"מ = סה"כ-לפני-מע"מ × 0.18.
- **מחירון מדורג:** ל-שרוכים/תגים נשלף `special_price` לפי מדרגת כמות (`min_qty`–`max_qty`).
- **דוגמת הבדיקה (M2):** לקוח קיים (הנחה 5%) + הנחה ידנית 10% = 15%; 300 אורחים, 4 שעות, 300 תגים, 300 שרוכים → 6 דיילות×500=3,000 + 300 תגים×5=1,500 + 300 שרוכים×6=1,800 = 6,300 ביניים → הנחה 15%=-945 → 5,355 לפני מע"מ → מע"מ 18%=964 → **סה"כ סופי 6,319 ₪**.
**טבלאות DB:** `quotes`, `quote_services`, `products`, `price_tiers`, `customers` (הנחה קבועה), `params` (מע"מ, יחס).
**מוקאפ (רפרנס ויזואלי בלבד):** `mockups/quote-screen/01-07.png`.
> ⚠️ בסיכום המוקאפ מוצג מע"מ 17% (לא 18%) — **הוכרע (07/07/2026, §7.1): 18% (האפיון קובע; המוקאפ מיושן).**

### 5.5 ניהול הצעות מחיר · אפיון 5.6.5 · מודול 3
**לפי האפיון:** בקרה על מחזור חיי ההצעות; דחייה, עריכה או אישור סופי והתנעת פרויקט.
**רכיבים:** רשימה לפי סטטוס (בתהליך/מאושרת/נדחתה) · כפתורי אישור סופי/דחייה/עריכה · חלונות אימות לפעולות בלתי-הפיכות + סיבת דחייה · פסילה אוטומטית אחרי 30 יום · אישור → פתיחת פרויקט "טרם החל" + גזירת משימות מטה.
**טבלאות DB:** `quotes`, `projects` (יצירה באישור).
**מוקאפ (רפרנס ויזואלי בלבד):** אותה תיקייה `mockups/quote-screen/` — מודלי אישור (ירוק) ודחייה (אדום).

### 5.6 ניהול ובקרת פרויקטים — מבט על · אפיון 5.6.6 · מודול 6
**לפי האפיון:** סביבת העבודה המרכזית של מנהלת הפרויקטים; מוכנות לביצוע וזיהוי צווארי בקבוק.
**רכיבים:** לשוניות סטטוס עם מונה (טרם החל/בתהליך/מוכן לביצוע/אירוע הסתיים) · רשימת פרויקטים פעילים · חיווי שברים (מדד שיבוץ 7/8, מדד לוגיסטיקה 3/5) · מעבר סטטוס אוטומטי לפי המדדים · איתור "פרויקטים אדומים" קרובים לתאריך.
**טבלאות DB:** `projects`, `assignments`, `logistics` (מדדי השבר).
**מוקאפ (רפרנס ויזואלי בלבד):** `mockups/project-management-screen/01.png`, `02.png` — כולל נוסחת מיון "דחיפות תפעולית" (50% קרבה לתאריך + 30% חוסרי שיבוץ + 20% לוגיסטיקה פתוחה — **זו תוספת מוקאפ, לא מופיעה במפורש באפיון**; לאימות).

### 5.7 כרטיס פרויקט מלא ועריכת תכולה · אפיון 5.6.7 · מודול 6
**לפי האפיון:** ניהול מיקרו של אירוע: עריכת תכולה (בלעדי למנהלת פרויקטים), ביטול, וסגירה תפעולית.
**רכיבים:** אזור מידע קפוא (מההצעה) + הכנסה מתוכננת · לשונית לוגיסטיקה (מתוכנן נעול מול בפועל) · לשונית צוות דיילות (עריכת כמות נדרשת) · לשונית סגירת אירוע (שעות בפועל, אורחים בפועל, העלאת דו"ח — שדות חובה) · אכיפה: אין "שמור ושלח" בלי כל הנתונים; נעילה אחרי סגירה. **שינוי תכולה** על פרויקט "מוכן לביצוע" מחזיר אוטומטית ל"בתהליך" (§5.0(ב)); גבולות ההרשאה של שינוי תכולה (מחירים בדיעבד? רק כמויות?) פתוחים ב-§7.17.
**טבלאות DB:** `projects`, `quotes`+`quote_services` (תכולה קפואה), `assignments`+`hostesses`, `logistics`, `customers`.
**מוקאפ (רפרנס ויזואלי בלבד):** `mockups/project-management-screen/03.png`, `04.png`.

### 5.8 בקרה לוגיסטית — מבט על · אפיון 5.6.8 · מודול 5
**לפי האפיון:** מרכז בקרה של מנהלת הלוגיסטיקה; מציג רק פרויקטים עם משימות פתוחות.
**רכיבים:** סינון מהיר (תאריך / שבועיים קרובים) · רשימת פרויקטים עם משימות פתוחות בלבד · חיווי כמותי (3/5) · חזרה אוטומטית ל"בתהליך" בשינוי תכולה.
**טבלאות DB:** `logistics`, `projects`.
**מוקאפ (רפרנס ויזואלי בלבד):** `mockups/logistics-screen/01.png`.

### 5.9 חלון משימות לוגיסטיות (Checklist) · אפיון 5.6.9 · מודול 5
**לפי האפיון:** ניהול מיקרו של שרשרת האספקה לאירוע ספציפי; עדכון סטטוס פריט בלבד.
**רכיבים:** רשימת פריטים+כמות · כפתורי סטטוס לכל פריט (טרם החל/הוזמן/מוכן) · Pop-up אימות בסיום · **המנהלת אינה מורשית להוסיף/למחוק פריטים — רק לעדכן סטטוס**.
**טבלאות DB:** `logistics`, `products`.
**מוקאפ (רפרנס ויזואלי בלבד):** `mockups/logistics-screen/02.png`.

### 5.10 שיבוץ דיילות חכם — מבט על · אפיון 5.6.10 · מודול 4
**לפי האפיון:** סביבת העבודה של מנהלת הגיוס; סריקת אירועים הממתינים לאיוש לפי דחיפות.
**רכיבים:** סרגל סינון (תאריכים/סטטוסי איוש) · רשימת פרויקטים הדורשים כוח אדם · חיווי כמותי (נשלחו/אושרו/ממתינות/סירבו).
**טבלאות DB:** `assignments`, `projects`.
**מוקאפ (רפרנס ויזואלי בלבד):** `mockups/hostesses-screen/01.png`.

### 5.11 שיבוץ דיילות חכם (Smart Match) · אפיון 5.6.11 · מודול 4
**לפי האפיון:** אופטימיזציית גיוס צוות לפרויקט; DSS המגיש רשימת מועמדות ממוינת.
**רכיבים:** לוח בקרה (נדרש מול משובץ) · רשימת דיילות פנויות ממוינת (מרחק, משמרות ברבעון, דירוג — **ללא הצגת הציון עצמו**) · כפתורי סטטוס (סירבה/אישרה/אושרה סופית) + מייל תיאום · "שלח אישור לכל המאושרות" · סינון אוטומטי דיילות המשובצות באותו תאריך.
**נוסחה (Smart Match Score):** `Match_Score = W₁(דירוג) + W₂(מרחק גיאוגרפי) + W₃(מהימנות/PDR)`, משקולות גלובליות בטווח 0.0–1.0 מטבלת `params` (עריכה במודול 9). **מה שהאפיון כן מגדיר:** דירוג = `hostesses.rating` (1–5); מרחק = "פנייה לממשק חיצוני (Maps API)" בין עיר הדיילת למיקום האירוע; מהימנות = שיעור השתתפות בפועל מתוך שיבוצים מאושרים. **מה שהאפיון לא מגדיר** (איזה Maps API, נרמול מרחק לציון, טווח הציון הסופי) — פתוח ב-§7.14; אסור לממש בלי הכרעה שם.
**טבלאות DB:** `hostesses`, `assignments`, `projects`, `params` (משקולות).
**מוקאפ (רפרנס ויזואלי בלבד):** `mockups/hostesses-screen/02.png`, `03.png`.

### 5.12 מאגר דיילות · אפיון 5.6.12 · מודול 4
**לפי האפיון:** נתוני האב לאלגוריתם השיבוץ; הקמה, עדכון וניהול פרופיל דיילות.
**רכיבים:** לשוניות (מעקב פניות ושיבוצים / מאגר דיילות) · הוספת דיילת (Pop-up) עם ולידציות ת"ז וחשבון בנק · סינון (ערים, דירוגים, טווחי שכר, "פעילות בלבד") · טבלה (שם, ת"ז, דירוג★, עיר, שכר שעתי, מס' אירועים) · **מחיקה לוגית** (status="לא פעילה") במקום מחיקה פיזית.
**נוסחת שכר חודשי (לדו"ח שכר):** לכל דיילת — Σ(שעות ביצוע בפועל בפרויקטים שהסתיימו בחודש) × `hourly_rate_snapshot` (מוקפא בעת השיבוץ), + חלוקת בונוס פרויקט כללי בין הדיילות ששובצו. ייצוא Excel לרו"ח עם קוד ייחודי למניעת כפל תשלום.
**טבלאות DB:** `hostesses`, `assignments`, `salary_reports`.
**מוקאפ (רפרנס ויזואלי בלבד):** `mockups/hostesses-screen/04-06.png`.
> ⚠️ טופס הוספת דיילת במוקאפ כולל "כתובת מגורים" שאינה בסכמה (יש רק `city`) — פתוח להכרעה (סעיף 7, פריט 6).

### 5.13 בקרת כספים ופרויקטים שהסתיימו — מבט על · אפיון 5.6.13 · מודול 8
**לפי האפיון:** סביבת העבודה של מנהלת הכספים; בקרת גבייה וארכיון היסטורי.
**רכיבים:** סינון (תאריך/חברה/מס' פרויקט) · לשוניות (ממתין לחשבונית / ממתין לתשלום / הסתיימו) · עמודות (לקוח, תאריך סיום, רווח סופי %, משוב★, סטטוס).
**טבלאות DB:** `projects`, `customers`, `quotes`.
**מוקאפ (רפרנס ויזואלי בלבד):** `mockups/finance-feedback-screen/01.png`.

### 5.14 חלון סגירת תיק פרויקט וחישוב רווחיות · אפיון 5.6.14 · מודול 8
**לפי האפיון:** חתימה פורמלית של מחזור החיים: חישוב רווח גולמי, גבייה, קליטת משוב ונעילה לארכיון.
**רכיבים:** פרטי חיוב נעולים + "חשבונית נשלחה" + תאריך תשלום · תצוגת רווחיות שקופה (הכנסות מול הוצאות ישירות → רווח גולמי בש"ח) · שדה ציון משוב (1-5) · אכיפה: אין "העבר לארכיון" ללא ציון משוב.
**נוסחת רווח גולמי:** (סה"כ הכנסות נטו מההצעה לפני מע"מ, אחרי הנחות) פחות (סך הוצאות ישירות = Σ(שכר שעתי×שעות בפועל+בונוסים) + עלויות רכש שירותים/מוצרים). לא כולל מיסוי/פנסיה/הוצאות קבועות. **שני רכיבים בנוסחה עדיין פתוחים ב-§7.19** (איך `project_bonus` מתחלק בין הדיילות, ואיך נגזרות "שעות בפועל" פר-דיילת) — אסור לממש את החישוב בלי הכרעה שם.
**טבלאות DB:** `projects`, `assignments` (עלויות שכר), `quote_services`/`logistics` (עלויות רכש).
**מוקאפ (רפרנס ויזואלי בלבד):** `mockups/finance-feedback-screen/02.png`, `03.png`.

### 5.15 ניהול משתמשים + מטריצת הרשאות · אפיון 5.6.15 · מודול 1 (מנכ"ל בלבד)
**לפי האפיון:** כלי בלעדי למנכ"ל לפתיחת משתמשים ומידור לפי תפקיד (RBAC).
> ✅ **גישת "ניהול מערכת" — permission-driven (03/07/2026):** הגישה ל-`/system/*` (וההצגה ב-Sidebar) נאכפת לפי הרשאת המשתמש על מודולי "ניהול הרשאות"/"הגדרות מערכת" (`SYSTEM_MODULES` ב-`src/lib/constants.js`), **לא** לפי `role==='מנכ"ל'` קשיח. פותר את ה-"split-brain RBAC" — שורות ה-Seed של מודולי המערכת הפכו למקור אמת חי. לפי ה-Seed הנוכחי (מנכ"ל=edit, השאר blocked) ההתנהגות זהה לקודם, אך כעת מונעת-דאטה וניתן להאציל גישת-מערכת לתפקיד אחר בלי שינוי קוד. (הערה: מטריצת ההרשאות ב-UI עדיין מציגה 7 מודולים עסקיים בלבד; חשיפת 2 מודולי המערכת במטריצה = שיפור עתידי אופציונלי כדי לאפשר הענקה דרך ה-UI במקום SQL.)
**רכיבים:** טבלת עובדי החברה · מטריצת הרשאות חזותית (5 תפקידים × מודולים) · 3 מצבים לכל תא (V/עין/מינוס) · אכיפה אקטיבית בעת התחברות.
**טבלאות DB:** `users`, `roles`, `permissions` (כתיבה/עדכון), `modules`.
**מוקאפ (רפרנס ויזואלי בלבד):** `mockups/users-screen/01-03.png` (רשימה/הוספה/עריכה), `mockups/permissions-screen/01.png`, `02.png` (מטריצה — מקובצת ל-4 על-מודולים במקום 10 מודולים בודדים).
> ⚠️ טופס משתמשים במוקאפ כולל `username`+`טלפון` שאינם ב-`users` — פתוח (סעיף 7, פריט 2). ערכי המטריצה במוקאפ שונים מהאפיון בחלק מהתאים — **הוכרע (07/07/2026, §7.4): טבלת ההרשאות בסעיף 3 לעיל (מהאפיון) קובעת; ערכי המוקאפ נדחו.**

### 5.16 הגדרות מערכת · אפיון 5.6.16 · מודול 9 (מנכ"ל בלבד)
**לפי האפיון:** שליטה בלוגיקה העסקית ובפרמטרים הגלובליים ללא שינוי קוד (טבלת `params`).
**רכיבים:** משקולות Smart Match (W1-דירוג, W2-מרחק, W3-מהימנות, 0.0–1.0) · פרמטרים פיננסיים (מע"מ, שכר מינימום שעתי, תעריף חיוב לדיילת) · אינטגרציות (מייל רו"ח, לינק סקר שביעות רצון) · תבניות תקשורת (מייל הצעה, זימון משמרת, סיום אירוע).
**טבלאות DB:** `params` (כל ערך = שורה; `param_type` מבדיל בין הקטגוריות).
**מוקאפ (רפרנס ויזואלי בלבד):** `mockups/system-settings-screen/01-04.png` — כולל ברירות מחדל לדוגמה: W1=0.4/W2=0.3/W3=0.3, שכר מינ' 35₪, תעריף 350₪ (**ערכים אלו מהמוקאפ, לא מהאפיון עצמו** — לאימות עם ישי בעת Seed).

### 5.17 מסכי דו"חות (מחולל דו"חות) · אפיון 5.7 · מודול 11
**לפי האפיון:** מודול דו"חות ניהוליים לקריאה בלבד עם ייצוא ל-Excel/PDF; 5 דו"חות.
**רכיבים:** פאנל סינון (טווחי תאריכים, סוג דו"ח, סוג לקוח) · דו"ח פרויקטים פעילים · דו"ח רווחיות וביצועים (תכנון מול ביצוע) · דו"ח ניתוח לקוחות ("לקוחות זהב") · דו"ח שיבוץ דיילות + דו"ח ריכוז שכר חודשי (ייצוא Excel לרו"ח).
**טבלאות DB (קריאה בלבד, לרוב דרך Views):** `projects`, `quotes`, `customers`, `assignments`, `hostesses`, `salary_reports`, `logistics`.
**מוקאפ (רפרנס ויזואלי בלבד):** `mockups/management-report-screen/01-04.png`.

---

## 6. חובות עתידיים (TODO — מתוך CHANGELOG)
> **🚧 מ<N> = חוב חוצה-מודולים הממתין למודול N** — נולד מ-🚧 בטבלת-היכולות של מדריך-מיקרו, ונרשם כאן **באותו סשן** (ה-Stop hook חוסם 🚧-במדריך-בלי-שורת-§6-תואמת). שליף: `grep '🚧 מ<N>' docs/PROJECT_MASTER.md` = כל החובות שחייבים למודול N; כך פתיחת מודול N יודעת לחזור ולהשלים. (‏⏳=החלטה-נדחית · 🕓=חותמת-טריות — סמנים אחרים, לא לבלבל.)
- ✅ ~~**מסך התחברות** → לנתב למסך הבית~~ — **בוצע (02/07/2026)**: מנתב ל-`/` (WelcomePage placeholder עד מודול 7). **CAPTCHA בוטל** (03/07/2026) — הוחלף ב-Google Sign-In + נעילת חשבון (ראו 5.1, 7.8).
- 🚧 מ10 — **שכחתי סיסמה** → מייל אמיתי + מסך קוד. מודול 10.
- **מוצרים / מדרגות-מחיר / פרמטרים (`products`/`price_tiers`/`params`)** → Seed **במודול 3** (לא במודול 1). ✅ **החלטות ה-Seed ננעלו (03/07/2026)** ומתועדות בראש `reference_spec/products_and_params.md` (מיפוי enum לאנגלית, SKU בלי מקף, שירותים לפי base_price בלי tiers, `שכר_מינימום_שעתי=35`, W3=מהימנות ומשקולות 0.4/0.3/0.3, max_qty=NULL, תיקוני טעויות). מע"מ 18% + יחס 1:50 כבר מאושרים.
- **כניסה עם גוגל** → ✅ **בוצע (03/07/2026)** — כפתור Google OAuth אמיתי במסך ההתחברות (הוקדם ממודול 10 כחלק ממודל האבטחה החדש).
- **Seed בוצע:** 5 תפקידים (`roles`) + **9 מודולים** (`modules` — שורת "מסך הבית" הוסרה 02/07/2026 לפי הכרעת §7.10) + `permissions` מלא (45 שורות, 5×9).
- 🚧 מ6 · 🚧 מ3 · 🚧 מ8 · 🚧 מ7 — **השלמות כרטיס לקוח (מודול 2, נרשם 07/07/2026 בפתיחת המודול; 🚧 מ7 נוסף 10/07/2026 באודיט-נאמנות):** הכרטיס נבנה במודול 2 עם empty states — **מודול 6** ממלא את היסטוריית הפרויקטים (policies+דאטה של `projects`) · **מודול 3** מחבר את חישוב "סה"כ הכנסות" (דרך ה-SSOT של התמחור, `src/lib/pricing.js` — לא לשכפל נוסחה) · **מודול 8** מחבר "ממוצע משוב" (`projects.feedback_score`) ומפעיל את כוכבי שביעות-הרצון + סינון-לפי-שביעות-רצון ברשימת הלקוחות · **מודול 7** מחבר את "רווח גולמי מצטבר" פר-לקוח (נוסחת-הרווחיות; §7.79 — במודול 2 placeholder בלבד).
- 🚧 מ10 — **שליחת חומר שיווקי אמיתית (מודול 2 → מודול 10):** במודול 2 "שלח ללקוחות מאושרים" = ‏mailto עם BCC + קישור ציבורי (בלי מעקב). **מודול 10** מחליף בשליחת שרת אמיתית + מעקב — כולל ישות יומן-שליחות (מה נשלח/למי/מתי; אין לה טבלה ב-C6 — ביקורת 07/07 ערב).
- 🚧 מ9 · 🚧 מ10 — **העדפות התראות (נרשם 07/07/2026, אודיט פריטים פתוחים):** מסך הפרופיל (מודול 1) נמסר עם טוגלים של התראות מייל/SMS **מנוטרלים בכוונה** (`src/components/ProfileSettingsPage.jsx`, אזור `NotificationsSection`) — אין עדיין טבלת העדפות. **הכרעת ישי (07/07/2026): פיצול** — **מודול 9** בונה את טבלת ההעדפות + ה-UI ומפעיל את הטוגלים; **מודול 10** מחבר שליחת התראות בפועל לפי ההעדפות. (מקושר מ-`docs/micro_guides/module-1.md` §9.)
- 🚧 מ12 — **בדיקות חוצות-מודולים (נרשם 07/07/2026, יישור סתירת UAT — הכרעת ישי):** בדיקת **cross-browser + מובייל** → **לפני מודול 5**; ‏**UAT פורמלי + E2E מערכת-מלאה** → **מודול 12** (אינטגרציה והגשה). זה מיישר את `docs/micro_guides/module-1.md` §2 מול `docs/architecture_and_qa_roadmap.md` (שקבע מודול 12 מלכתחילה). *שורש הבלבול: סימון אבני-הדרך M1–M5 (‏00_roadmap §4; ‏M5=הגשה, שלב מודול 12) מתנגש עם סימון המודולים M1–M12 — במסמכים חיים לציין "אבן-דרך" במפורש כשמתכוונים אליה.*

---

## 7. ⚠️ סתירות/פערים פתוחים — להכרעה משותפת עם ישי

**כלל ההכרעה:** סכמה > אפיון > מוקאפים. **הפריטים כאן נשארים פתוחים בכוונה ולא נפתרים אוטומטית** — נכריע כל אחד **יחד עם ישי** בעת בניית המודול הרלוונטי.

> **מדיניות-גודל (נקבעה 07/07/2026):** כשהסעיף חוצה ~200 שורות או בסגירת מודול 6 (המוקדם) — דיון-ארכוב עם ישי: פריטים סגורים-שכל-האדוות-שלהם-בוצעו יכווצו לסטאב (מספר + כותרת + תאריך-הכרעה + "פירוט בארכיון") והפירוט המלא יעבור לקובץ-ארכיון ייעודי. **המספור לעולם נשמר** (עשרות ציטוטי `§7.N` תלויים בו), ומראות 🔗 ימשיכו להסתנכרן מהמקור החדש. עד אז — לא נוגעים.

> **🔑 שורת-סטטוס לכל פריט (נוסף 08/07/2026 — שדרוג-רשם, מועצת-llm-council):** כל פריט נפתח בסמן-סטטוס + code-span בר-בדיקה בפורמט `סטטוס·סוג·מודול`.
> **סטטוס:** 🟢 סגור · 🟡 פתוח · 🔵 להנהון (אישור קצר, לא דיון) · ⚪ ממתין-לביצוע. **סוג:** `DB` / `מסך` / `לוגיקה` / `אוטומציה` / `הרשאות` / `תהליך` (מרובה עם `+`). **מודול-ביצוע** או `—`.
> **למה:** ה-code-span הוא ה-**SSOT הקריא-למכונה** — הרוטינה `regin-docs-sync` שולפת ממנו "כל הפתוחים מסוג `אוטומציה`" או "של מודול N" ב-grep יחיד, בלי קובץ-אגרגטור נפרד (‏`db_roadmap.md` נשאר רק ל-DB כי מיגרציות דורשות צ'קליסט-ביצוע ייעודי). הנרטיב + "סטטוס:" בסוף-הפריט = ההקשר האנושי; בסתירה בין ה-code-span לנרטיב — הרוטינה מיישרת (הם חייבים להסכים).
> **אמנסטיית-רטרופיט:** הוספת שורת-הסטטוס לפריטי-עבר היא עדכון-**מטא-דאטה מותר** (לא הפרת "נרטיב-מתוארך-לא-משכתבים") — הנרטיב קפוא, רק המטא-שורה חיה.
> **ריידרים (↳):** מ-08/07 **אין ריידרים חדשים** — עבודה חדשה על פריט סגור = מספר §7 חדש (‏§7.79+) עם "נולד-כ-↳ על §7.N". ‏8 הריידרים-הפתוחים שהיו (12/17/23/25/42/49/50/52) **קודמו לפריטים §7.71–78 ב-09/07/2026** — פריט-האם נושא מצביע "↳ קודם ל-§7.7X", וסימוני-`↳פתוח` הוסרו מהטוקנים. ריידרי-"חידוד" (הבהרות על הכרעות) נשארים בפריטי-האם ואינם מקודמים.


1. 🟢 `סגור·לוגיקה+DB·מ3` **מע"מ:** ✅ **הוכרע (07/07/2026, ישי — חבילת הכסף באודיט הפריטים הפתוחים): 18%.** האפיון (18%, מפורש) גובר על המוקאפ (17%, מיושן — מע"מ ישראל עלה ל-18% ב-2025), ודוגמת-הבדיקה הקובעת (6,319 ₪) מחושבת ב-18%. הערך נכנס ל-Seed של `params` (`param_name='מע"מ'`, ‏`param_value=18`) במודול 3. סטטוס: **סגור.**
2. 🟢 `סגור·DB+מסך·מ1` **טופס משתמשים:** ✅ **הוכרע במלואו (02/07/2026, ישי).** `phone` נוסף כעמודה אמיתית ל-`users` (`alter table ... add column phone text`, מתועד ב-`schema.sql`). `username` — **לא יתווסף כשדה נפרד**: כתובת הדוא"ל משמשת כ-username במערכת (היא כבר המפתח הראשי בטבלת `users` ומזהה ההתחברות מול Supabase Auth). המוקאפ שהציג שדה `username` נפרד אינו מיושם בכוונה. סטטוס: **סגור**.
3. 🟢 `סגור·מסך·מ2` **סוג לקוח:** ✅ **הוכרע (06/07/2026, ישי, פתיחת מודול 2).** תוויות ה-UI = הניסוח מהאפיון הקפוא (C5 §1.5.3), מיפוי 1:1 ל-enum: ‏`private_company`=חברה פרטית · `government`=חברה ממשלתית · `production_company`=חברת הפקה · `nonprofit`=עמותה. תוויות המוקאפ (חברה גדולה/עסקי/ממשלתי) נדחו — האפיון גובר על המוקאפ. המיפוי חי ב-`src/lib/customers.js` (`CUSTOMER_TYPE_LABELS`). סטטוס: **סגור.**
4. 🟢 `סגור·הרשאות·מ1` **ערכי מטריצת ההרשאות:** ✅ **הוכרע (07/07/2026, ישי — אשרור as-built באודיט הפריטים הפתוחים).** ה**ערכים** (edit/view/blocked פר תפקיד×מודול) = **מטריצת האפיון 5.4** (טבלת §3 כאן), כפי שכבר נבנתה ב-Seed (45 שורות, `permissions`; ר' [module-1.md](micro_guides/module-1.md) §9 "seed values from spec §3, not mockup"). ערכי המוקאפ שנבדלו בחלק מהתאים **נדחו** — האפיון גובר על המוקאפ. ה**קיבוץ הוויזואלי** ל-4 קבוצות-על במסך המטריצה הוא **תצוגה בלבד** (אין לו ביטוי ב-`modules`/`permissions` — הטבלה נשארת 9 מודולים שטוחים); שאלת שפת-הקיבוץ/הניווט הרחבה יותר נדונה ב-**§7.5**, ומסך המטריצה יתיישר אליה בלי לפתוח פריט זה מחדש. סטטוס: **סגור.**
5. 🟢 `סגור·מסך·מ1/7` **רשימת המודולים הקנונית ומבנה הניווט:** ✅ **הוכרע (07/07/2026, ישי — אשרור as-built באודיט הפריטים הפתוחים): רשימה שטוחה של 9 מודולים בסיידבר.** (שאלת ה"כמה" נסגרה כבר ב-§7.10: 9, בלי מסך הבית.) **המבנה:** הסיידבר = 9 פריטים **שטוחים** מטבלת `modules` (+ "מסך הבית" קבוע למעלה, + "ניהול מערכת" כקישור), כפי שנבנה במודול 1 ואומת. ה-**4 קבוצות-העל** של המוקאפ חיות **אך ורק כקיבוץ-תצוגה במסך ההרשאות** (עקבי עם §7.4) — אין להן ביטוי בניווט, ב-`modules` או ב-`permissions`. ההיגיון: מבנה-הניווט מיושר למודל-ההרשאות השטוח (9 מודולים); אם בעתיד התפריט ייראה ארוך, אפשר כותרות-קיבוץ ויזואליות בלי שינוי-דאטה. **מקור ה-11 בניווט הדשבורד** (מוקאפ) = 9 מודולים + מסך-הבית + פריט-על ("ניהול מערכת"/CTA) — **לא ישות חדשה**; זו שאלת-תצוגה של מודול 7 שתיסגר בפתיחתו, לא חסם עכשיו. סטטוס: **סגור** (הסיידבר); פרטי-התצוגה של הדשבורד — מודול 7.
6. 🟢 `סגור·DB·מ4` **כתובת דיילת:** ✅ **הוכרע (07/07/2026, ישי — אשכול השיבוץ): להוסיף כתובת מלאה + קואורדינטות.** תיווסף `hostesses.address` (טקסט) לצד `city`, וכן `lat`/`lng` (קואורדינטות מגאוקוד חד-פעמי של הכתובת) — נדרשות לחישוב המרחק ב-Smart Match (§7.14). תוספות-סכמה במיגרציה של מודול 4 (ר' רשם-הסכמה §7.47). סטטוס: **סגור** (הכיוון); ביצוע — מודול 4.
7. 🟡 `פתוח·מסך·מ6` **stepper פרויקט:** התצוגה במוקאפ מפשטת ל-5 שלבים, בעוד `project_status` בסכמה = 8 ערכים. סטטוס: **פתוח — להכרעה משותפת עם ישי** (מודול 6).
8. 🟢 `סגור·הרשאות·מ1` **CAPTCHA במסך התחברות:** ✅ **הוכרע (03/07/2026, ישי): CAPTCHA בוטל לחלוטין.** מוחלף במודל אבטחה חדש — Google Sign-In + נעילת חשבון אחרי 5 ניסיונות כושלים (`login_attempts` ב-`schema.sql`), מגובה ב-rate limiting מובנה. האפיון הקפוא (5.6.1) לא נערך; הסטייה מתועדת ב-5.1 ובמדריך מודול 1. סטטוס: **סגור.** **תת-הכרעה (07/07/2026, ישי — triage של Supabase advisors):** ה-trade-off המובנה של נעילה-לפי-אימייל **התקבל** — `register_failed_login` חשופה ל-`anon` (נדרש: רצה טרום-אימות), ולכן גורם זדוני שיודע אימייל של משתמש יכול לנעול לו את החשבון ל-15 דק' (lockout-DoS). מקובל למערכת פנימית: הפקיעה האוטומטית + עקיפת Google Sign-In תוחמות את הנזק; מיטיגציית נעילה-משולבת-IP נדחתה ל-backlog. הרישום המלא: `docs/micro_guides/module-1.md` §3 (‏§7.8↳) ו-§4 "Advisor acceptances".
9. 🟡 `פתוח·לוגיקה·מ6` **נוסחת "דחיפות תפעולית" (מבט-על פרויקטים):** מופיעה במוקאפ (50% קרבה לתאריך + 30% חוסרי שיבוץ + 20% לוגיסטיקה פתוחה) אך **לא אותרה במפורש בטקסט האפיון**. סטטוס: **פתוח — להכרעה משותפת עם ישי** (מודול 6) — האם זו דרישה אמיתית מהאפיון שפספסנו, או תוספת מוקאפ בלבד.
10. 🟢 `סגור·DB·מ1` **מספר מודולים אמיתי ב-`modules`** — ✅ **הוכרע (01/07/2026, ישי): "מסך הבית נגיש לכולם."** מילון הנתונים באפיון (`docs/reference_spec/C6_clean_transcript.md` §2.4.8) מונה 9 ערכים בלבד עבור `modules` (לקוחות, הצעות מחיר, פרויקטים, דיילות, לוגיסטיקה, כספים, דו"חות, ניהול הרשאות, הגדרות מערכת) — ללא מסך הבית. ההכרעה מאשרת שזו הפרשנות הנכונה: מסך הבית **אינו** שורה בטבלת `modules`/`permissions` — הוא נגיש תמיד לכל משתמש מחובר ללא בדיקת הרשאה, קיים רק כמסך/מודול-קוד (`src/modules/07_dashboard/`). **החלטה מעשית ל-Seed:** `modules` = 9 שורות, `permissions` = 45 שורות (5×9), לא 50. ✅ **בוצע בפועל (02/07/2026):** שורת "מסך הבית" נמחקה מ-`modules` (9 שורות) ו-`permissions` מולא ב-45 שורות — ראו שורת ה-DB ב-CHANGELOG. סטטוס: **סגור.**
11. 🟢 `סגור·לוגיקה·מ2` **מיזוג/כפילות לקוחות:** ✅ **הוכרע (06–07/07/2026, ישי, פתיחת מודול 2).** אין פיצ'ר מיזוג — ח"פ (`customer_id`) הוא המזהה הקנוני החד-ערכי. ‏(א) **מניעה בהוספה:** הזנת ח"פ קיים ופעיל ⇒ שגיאה ידידותית ("חברה זו כבר רשומה במערכת") + קישור מהיר לעריכת הכרטיס הקיים; ח"פ קיים **בארכיון** ⇒ הצעה מפורשת "הלקוח קיים בארכיון — לשחזר?". ‏(ב) **חיפוש סלחני וחד-משמעי:** איתור לקוח לפי שם איש קשר בלבד / שם חברה בלבד / תחילת ח"פ, וכל תוצאה מציגה את שלושת המזהים — הלוגיקה ב-`src/lib/customers.js` לשימוש חוזר בבוחר-הלקוח של מודול 3. ‏(ג) **כפולה היסטורית** (אם תתגלה): ארכוב ידני של הרשומה העודפת, בלי סקריפט תיקון אוטומטי. סטטוס: **סגור.**
12. 🟢 `סגור·לוגיקה·מ3` **אחסון PDF של הצעות מחיר:** ✅ **הוכרע (07/07/2026, ישי — אודיט; תחום מודול 3 של עמית, לידיעתו ב-merge): הפקה-בזמן-אמת מהנתונים, בלי אחסון קבוע.** ה-PDF מיוצר על-הזבוב מנתוני ההצעה (client-side/Edge) בכל הורדה/שליחה; **ההצעה עצמה היא ה-SSOT** (`quotes` + `quote_services` + ההנחות והמחיר/עלות המוקפאים באישור — §7.28). **ארכיון:** ההצעה **נשמרת לצמיתות כדאטה** ואינה נמחקת; ברגע האישור המספרים מוקפאים — ולכן ה-PDF **המדויק** של הגרסה המאושרת ניתן-לשחזור בכל רגע, בלי קובץ שיוצא מסנכרון. **`quotes.pdf_url` מסומן deprecated** (לא נמחק — שינוי-סכמה מיותר). **שדרוג אפשרי (לא כרגע):** אם תידרש ראיה משפטית לעותק-הקובץ-שנשלח — snapshot אחד ל-Storage ברגע האישור בלבד (אפשרות ג׳), בלי מיגרציה כואבת. **דרישת-בנייה נגזרת (רשומה למודול 3/6):** יצירת פרויקט מהצעה מאושרת **קוראת את השורות הקפואות** דרך `projects.quote_id`→`quote_services`, לא מחשבת מחדש — נקודת-אימות מפורשת בבלופרינט. **רפרנס ויזואלי:** טמפלט-הטיוטה של ישי ל-PDF הונח ב-`docs/mockups/quote-screen/quote_template_draft.docx` (הוסב לשם אנגלי 08/07/2026). מבנה: כותרת (תוקף+30 יום · מזהה הצעה · תאריך הפקה) · פרטי לקוח (שם/ח"פ/איש-קשר) · פרטי פרויקט (שם/תאריך-אירוע/מיקום) · טבלת-פריטים (מק"ט · תיאור · כמות · צבע · הערות · מחיר-יחידה · סה"כ-שורה) · **מפל-תמחור:** סה"כ-ביניים → הנחת-לקוח(%) → הנחה-נוספת(%) → סה"כ-לפני-מע"מ → **מע"מ 18%** → סה"כ-לתשלום · תנאים כלליים. **זוג-ההנחות תואם ל-`quotes.applied_customer_discount`+`manual_discount` והמפל ל-§7.1/§7.26** — טיוטה תקפה, לא לזרוק. מודול 3 מתרגם את המבנה ל-renderer-בזמן-אמת (לא מנוע-merge; §7.12 = בלי אחסון). סטטוס: **סגור.** **↳ קודם ל-§7.71 (09/07/2026).**
13. ⚪ `ממתין-לביצוע·DB·מ3` **ביצוע Seed מוצרים/מדרגות/params:** ההחלטות ננעלו (03/07/2026, ראו `reference_spec/products_and_params.md`) אך ה-Seed עצמו טרם הורץ. **חסם ההכרעה הוסר (07/07): §7.1 מע"מ=18% נסגר** (וגם §7.25/26/27 שנוגעים ל-Seed). סטטוס: **ממתין לביצוע** (מודול 3).
14. 🟢 `סגור·לוגיקה·מ4` **Smart Match — סקלת ציון ו-Maps API:** ✅ **הוכרע (07/07/2026, ישי — אשכול השיבוץ): מרחק אווירי, בלי Maps API בתשלום.** "אם זה עולה כסף — מוותרים": המרחק יחושב מתמטית ויציב מקואורדינטות (נוסחת **haversine** או תוסף Postgres **`earthdistance`**), ישירות ב-Postgres או ב-Backend — **ללא שום פנייה ל-Maps API חיצוני בתשלום**. הכתובת (§7.6) מגואקדת פעם אחת ל-`lat`/`lng`; המרחק האווירי לכתובת-האירוע מנורמל לציון בפונקציה לינארית-הפוכה עם תקרה (0 ק"מ→1.0, מעל סף→0.0). המשקולות (0.4/0.3/0.3) והציון הסופי המוסתר נשמרים כבאפיון. שדרוג עתידי למרחק-נסיעה = פונקציה מקומית, בלי שינוי-סכמה. סטטוס: **סגור** (מודול 4).
15. 🟡 `פתוח·לוגיקה·מ4/9` **W3 — יישור טרמינולוגיה:** הוכרע כ"מהימנות" (שיעור השתתפות באירועים; החלטות Seed ‏03/07), אך בניסוחים ישנים מופיע "עומס". יש לוודא ששם הפרמטר ב-`params` ובמסכים אחיד. סטטוס: **פתוח — טכני** (מודול 4/9). **↳ חידוד (07/07 ערב, ביקורת-אפיון): הפריט מהותי, לא רק טרמינולוגי** — לרכיב W3 שלוש הגדרות לא-מתיישבות: C5:294+756 = "כמות משמרות ברבעון" (ספירת-נפח), C5:882 = "מדד מהימנות (כמות משמרות)" (ערבוב), וההגדרה החיה כאן (§5.11: שיעור-השתתפות מתוך שיבוצים מאושרים) — שלישית; גם חלון ה"רבעון" (קלנדרי / 90-יום נגלל) לא הוגדר, והוא מזיז ציוני-שיבוץ. להכריע נוסחה+חלון בבניית מודול 4 (קשור לדיון הרכב-הציון שנדחה ב-§7.16).
16. 🟢 `סגור·לוגיקה+DB·מ4/8` **היעדרות/איחור + מדיניות ביטול פרויקט:** ✅ **הוכרע (07/07/2026, ישי — אשכול השיבוץ; חישוב שכר אוטומטי במודול 8).**
    **(א) נוכחות בסגירת פרויקט:** בשלב הסגירה התפעולית המנהלת מסמנת אם דיילת איחרה/לא הגיעה.
    - **איחור:** בחירה מרשימה — *קצת / בינוני / הרבה* → ירידה בציון המהימנות (שיעור הירידה — לוגיקת ה-AI, חלק מדיון הרכב-הציון הנפרד). **דריסה ידנית של ציון המהימנות אפשרית רק ברמת הציון הכללי** של הדיילת (ע"י מנהלת הגיוס), לא פר-רכיב.
    - **אי-הגעה:** סימון סיבה מבין שלוש — *"חולה"* (ירידה קטנה) · *"אישור מראש"* (בלי ירידה) · *"הבריזה"* (ירידה גדולה).
    - **תשלום ושעות:** דיילת שלא הגיעה **בכלל, מכל סיבה = בלי תשלום** — המערכת לא מוסיפה לה שעות שלא עבדה.
    **(ב) מדיניות ביטול פרויקט ע"י הלקוח:** תיווספנה `projects.cancelled_at` (timestamptz) + `cancellation_reason` enum(`standard`,`force_majeure`). חישוב שכר הדיילות המשובצות אוטומטי (מודול 8) לפי מרחק-הזמן ממועד האירוע:
    1. **ביטול >72 שעות לפני:** ביטול חינם — הלקוח לא מחויב, שכר הדיילות **0%**.
    2. **ביטול 24–72 שעות לפני** (`standard`): ביטול חלקי — הדיילות מקבלות **50%** משעות-המשמרת המתוכננות (פיצוי על שריון-יומן).
    3. **ביטול <24 שעות לפני:** הרגע-האחרון — הלקוח מחויב במלואו, הדיילות מקבלות **100%** עבור המשמרת שבוטלה.
    4. **`force_majeure`** (כוח עליון/מלחמה): פטור מלא לשני הצדדים — **0%** לדיילות וללקוח, ללא תלות בזמן הביטול.
    **קישור:** §7.31 (קסקדת-הביטול התפעולית — לוגיסטיקה+מיילים), §7.19 (גזירת שעות/בונוס), §7.15 (מהימנות). **נדחה במפורש:** *הרכב ציון-המהימנות* (ממה הוא מורכב ומה שיעור כל ירידה) = דיון נפרד, לא נסגר כאן. תלות-סכמה: §7.47. סטטוס: **סגור** (המדיניות); הרכב-הציון — דחוי.
    **↳ חידוד (07/07 ערב, ביקורת-אפיון):** ‏(א) C6 §2.4.2 (שדות projects) כבר מגדיר `cancel_reason` עם 4 ערכים הכוללים "כוח עליון" — במיגרציית `cancellation_reason` ליישר במפורש (החלפת העמודה הקיימת, או שדה-חיוב נפרד לצד סיבת-הטקסט) כדי לא להיוותר עם שתי עמודות-סיבה חופפות. ‏(ב) קלט-הפיצוי "שעות-משמרת מתוכננות" לפרויקט מבוטל אינו שמור פר-שיבוץ — ייגזר מזמני-האירוע החדשים (§7.47) או מ-`estimated_hours` דרך ההצעה; לקבוע בבניית מודול 4/8.
17. 🟡 `פתוח·לוגיקה·מ6` **סמנטיקת "שינוי תכולה":** האם מנהלת הפרויקטים רשאית לשנות מחירי יחידה בדיעבד או רק כמויות? ואיך מחושב הרווח מחדש אחרי שינוי תכולה? סטטוס: **פתוח — להכרעה משותפת עם ישי** (מודול 6). **↳ חידוד (07/07 ערב, ביקורת-אפיון): להכרעה נוסף מודל-הדאטה** — לשינוי-תכולה אין כיום בית: ההצעה נעולה אחרי אישור (C5:231), ‏`logistics` בלי עמודות מחיר/עלות (C6 §2.4.13; ה"סך מחיר בפועל" הנגזר שם לא אומר איזה מחיר), ולכן "חיוב על שינויים" (C5:833) ורווחיות §5.14 לא ניתנים לחישוב משום מקום. כיווני-פתרון: שורות-תוספת מוקפאות ב-`quote_services` עם דגל scope-change, או עמודות מחיר/עלות ב-`logistics` (משליך על §7.28). **↳ קודם ל-§7.72 (09/07/2026).**
18. 🟡 `פתוח·מסך·מ7` **חוקי אדום/צהוב/ירוק בלוח האירועים:** "פרויקט אדום = ב-14 הימים הקרובים שלא סיימו הכנות" — אך מה נחשב "לא מוכן": שיבוץ<100% **או** לוגיסטיקה<100%, או דווקא שניהם? סטטוס: **פתוח — להכרעה משותפת עם ישי** (מודול 7; param `ימי_אזהרה_קדם_אירוע`=14).
19. 🟡 `פתוח·לוגיקה·מ8` **חלוקת בונוס פרויקט + גזירת שעות בפועל:** איך `project_bonus` מתחלק בין הדיילות (שווה בשווה? לפי שעות?), ואיך "סך שעות האירוע" הופך לשעות פר-דיילת (לכל אחת מלוא משך האירוע, עם override ידני ב-`assignments.actual_hours`?). סטטוס: **פתוח — להכרעה משותפת עם ישי** (מודול 8).
20. 🟡 `פתוח·לוגיקה·מ8` **איחור בתשלום / החזר בביטול:** האפיון לא מגדיר תזכורות או טיפול בלקוח שלא שילם, ולא מדיניות החזר כשפרויקט מבוטל אחרי אישור הצעה. סטטוס: **פתוח — להכרעה משותפת עם ישי** (מודול 8; ייתכן שמחוץ להיקף — להכריע במפורש).
21. 🟢 `סגור·הרשאות·מ2` **מודל בעלות-דאטה ל-RLS (11 הטבלאות העסקיות):** ✅ **הוכרע (06/07/2026, ישי).** הגישה לכל טבלה עסקית נקבעת **אך ורק** לפי מטריצת ההרשאות (תפקיד→מודול→`edit`/`view`/`blocked`) — **בלי** בעלות ברמת-רשומה (כלומר אין הגבלה נוספת כמו "רק הבעלים של הפרויקט יכול לערוך אותו"; מי שיש לו `edit` על המודול עורך את כל הרשומות של אותו מודול, ומי שיש לו `view` רואה את כולן). **תבנית ה-policy הסטנדרטית לכל טבלה עסקית עתידית** (למשל `customers`):
    ```sql
    alter table <table_name> enable row level security;
    create policy "<table_name>_select_by_permission" on <table_name> for select to authenticated
      using (exists (
        select 1 from permissions p
        where p.role_id = (select current_user_role_id())
          and p.module_id = (select module_id from modules where module_name = '<שם-המודול-המתאים>')
          and p.permission_level in ('edit', 'view')
      ));
    create policy "<table_name>_write_by_permission" on <table_name> for all to authenticated
      using (exists (
        select 1 from permissions p
        where p.role_id = (select current_user_role_id())
          and p.module_id = (select module_id from modules where module_name = '<שם-המודול-המתאים>')
          and p.permission_level = 'edit'
      ))
      with check (exists (
        select 1 from permissions p
        where p.role_id = (select current_user_role_id())
          and p.module_id = (select module_id from modules where module_name = '<שם-המודול-המתאים>')
          and p.permission_level = 'edit'
      ));
    ```
    **עדכון תבנית (07/07/2026):** קריאות `current_user_role_id()` (וכל `auth.<fn>()` עתידי) בתבנית עטופות ב-`(select …)` — תיקון לינט `auth_rls_initplan` של Supabase (חישוב פעם-לשאילתה במקום פעם-לשורה; זהות התנהגותית מלאה, שינוי ביצועים בלבד). הוחל רטרואקטיבית על 2 ה-policies של `users` במיגרציית `20260707163709`; מחייב כל policy חדשה ממודול 2 והלאה.
    **החמרה עתידית אפשרית (לא כרגע):** אם יידרש בהמשך (למשל "מנהלת פרויקטים רואה רק את הפרויקטים שהיא `owner_email` שלהם") — זו תהיה תוספת `and owner_email = (select auth.email())` ל-policy הספציפית, לא שינוי במודל הכללי. סטטוס: **סגור.** מודול 2 (ואילך) כותב את ה-policies הראשונות לפי התבנית הזו.
22. 🟡 `פתוח·לוגיקה·מ5/8` **לוגיסטיקה — `actual_qty` קטן מ-`planned_qty`:** הסכמה מכילה את שתי העמודות, אך האפיון שותק לגבי המשמעות כשבפועל סופק/הוכן פחות מהמתוכנן (חוסר באירוע? פחת? האם זה חוסם "מוכן לביצוע"? האם משפיע על עלויות הרכש ברווחיות §5.14). סטטוס: **פתוח — להכרעה משותפת עם ישי** (מודול 5 / מודול 8).
23. 🟡 `פתוח·DB·אחרי-מ12` **היסטוריית עריכות (Audit Trail):** האפיון לא דורש תיעוד "מי שינה מה ומתי" על רשומות עסקיות (לקוח שנערך פעמיים — הגרסה הקודמת אובדת; last-write-wins). כרגע זו ההתנהגות המכוונת. אם בעתיד יידרש audit — זו טבלה/מנגנון חדש, לא שינוי לטבלאות הקיימות. סטטוס: **פתוח — מודע ונדחה בכוונה** (לשקול מחדש אחרי מודול 12, לפי צורך אמיתי של המשתמשות). **↳ קודם ל-§7.73 (09/07/2026).**
24. 🟢 `סגור·הרשאות·—` **סיסמאות משתמשי הבדיקה שנחשפו בהיסטוריית git:** ✅ **הוכרע (06/07/2026, ישי).** סיסמאות 5 משתמשי הבדיקה הופיעו בעבר ב-`docs/CHANGELOG.md`; הבלוק הוסר מהקבצים (P2‏#10 בבקרת הקוד) אך נשאר בהיסטוריית git. **ההחלטה: לא מחליפים את הסיסמאות** — זהו פרויקט אקדמי, מערכת פנימית סגורה עם משתמשי-בדיקה בלבד, והריפו פרטי; העלות של רוטציה (עדכון `.env.local` אצל שני המפתחים + סנכרון E2E) אינה מוצדקת מול הסיכון. הסיכון מקובל ומתועד. אם הריפו יהפוך ציבורי או שייכנסו משתמשים אמיתיים — לפתוח מחדש (רוטציה מלאה + מחיקת היסטוריה). השלכה תפעולית: ממצא gitleaks על ההיסטוריה (אם יופיע ב-CI) מטופל ב-`.gitleaksignore` עם הפניה לפריט זה, לא ברוטציה. סטטוס: **סגור.**

> **פריטים 25–46 נוספו 07/07/2026 באודיט פריטים פתוחים** (סריקה שיטתית של C5+C6 הקפואים מול הסכמה, בשלושה מעברים: שתיקות-כלל, פערי-דאטה, ופערי מנגנון-ביצוע). פריטים המסומנים **"להנהון"** = התשובה כנראה מובנת-מאליה ונדרש רק אישור קצר של ישי, לא דיון. **חבילת הכסף הוכרעה (07/07): §7.25, 26, 27, 28, 42 סגורים** (יחד עם §7.1 למעלה).

25. 🟢 `סגור·לוגיקה·מ3` **עיגול ודיוק כסף:** ✅ **הוכרע (07/07/2026, ישי — חבילת הכסף): אגורות (2 ספרות) בחישוב ובאחסון, שקל שלם בתצוגה בלבד.** ה-DB שומר סכומים מדויקים לאגורה (למשל מע"מ 963.90), המסך מציג מעוגל-לשקל (964) — כך משוחזר המוקאפ ויזואלית בלי לאבד אגורות בשכר/רווחיות, ותואם חשבונאות. אומת שדוגמת-הבדיקה 6,319 ₪ יוצאת בשתי הדרכים (‏963.90→6,318.90→תצוגה 6,319), כך שהקריטריון אינו נחצה. **אימות-יישום במודול 3:** לוודא שסכום-הביניים 5,355 משוחזר מפירוט-השורות; אם יידרש עיגול פר-שורה כדי להגיע אליו — לחזור לכאן (לא צפוי). ‏SSOT: `src/lib/pricing.js`. סטטוס: **סגור.** **↳ קודם ל-§7.74 (09/07/2026).**
26. 🟢 `סגור·לוגיקה+DB·מ2` **גבולות הנחות:** ✅ **הוכרע (07/07/2026, ישי — חבילת הכסף): אכיפה מלאה.** צירוף ב**חיבור** (לא כפל — כפי שהאפיון מדגים מפורשות, C5:172–180: 5%+10%=15%); **תקרה משולבת ≤ 100%**, כל הנחה בודדת ≤ 100%, **אי-שליליות** — נאכף **גם ב-`pricing.js` וגם כ-CHECK ב-DB** (הנימוק: בלי CHECK, קריאת-API ישירה או policy עתידית תעקוף כל ולידציית-UI ותפיק סה"כ שלילי). ‏`customers.discount_percent` מקבל ‏`check (>= 0 and <= 100)` — ניתן להוסיף כבר במודול 2 (הטיוטה כבר ב-`module-2.md` step 1.2). סטטוס: **סגור.**
27. 🟢 `סגור·לוגיקה·מ3` **חפיפת מדרגות `price_tiers`:** ✅ **הוכרע (07/07/2026, ישי — חבילת הכסף): פתרון-קוד בלבד, בלי אילוץ-DB.** התובנה המכריעה: ה-PK ‏`(sku, min_qty)` כבר מונע את המקרה הדו-משמעי היחיד (שתי מדרגות עם אותו סף-מינימום); מדרגות עם ספי-מינימום שונים נפתרות תמיד ע"י כלל ה-tie-break. **כלל הבחירה:** "המדרגה עם `min_qty` **הגבוה ביותר** שעדיין ‏≤ הכמות מנצחת" — הבחירה לפי `min_qty` בלבד; **`max_qty` הוא מידע-תצוגה בלבד** ואינו משתתף בבחירה, ולכן חפיפה אינה יכולה לייצר דו-משמעות. אילוץ EXCLUDE/GiST/טריגר **נדחה** כמורכבות מיותרת (הטבלה נטענת רק ב-Seed ע"י המפתחים — אין מסך ניהול-מוצרים בהיקף, C5 §1.2.2). מימוש: פונקציה יחידה ב-`src/lib/pricing.js` + בדיקת-יחידה + שורת-נוהל בקובץ ה-Seed. סטטוס: **סגור.**
28. 🟢 `סגור·DB·מ3` **snapshot עלות רכש:** ✅ **הוכרע (07/07/2026, ישי — חבילת הכסף): הקפאה סימטרית למחיר-המכירה.** בדיוק כמו `closing_unit_price` שכבר מוקפא — עלות-הרכש תוקפא ברגע אישור ההצעה בעמודה חדשה **`closing_unit_cost` ב-`quote_services`**, כדי שרווח גולמי היסטורי לא ינדוד עם שינויי `products.cost`. **הכיוון ננעל עכשיו** (כדי שסכמת מודול 3 תיבנה נכון מלכתחילה); **המיגרציה עצמה מתבצעת בפתיחת מודול 3**, כשהטבלה נכנסת לשימוש. סטטוס: **סגור** (הכיוון); ביצוע — מודול 3.
29. 🟢 `סגור·לוגיקה·מ4` **הגדרת ואכיפת Double-Booking:** ✅ **הוכרע (07/07/2026, ישי — אשכול השיבוץ): לפי סוג-אירוע ופער-שעה.** דיילת **יכולה** להשתבץ פעמיים באותו יום (אירוע בוקר + אירוע ערב) **רק אם**: ‏(א) **שני האירועים "קצרים"** — אם ולו אחד מהם מוגדר "ארוך", זו התנגשות חסומה; **וגם** ‏(ב) יש **לפחות שעה הפרש** בין סוף אירוע אחד לתחילת השני. אחרת = התנגשות חסומה. הבדיקה מתבצעת **ברמת ה-API בזמן השיבוץ** (לא אילוץ-DB). **תלות-סכמה:** דורש זמני התחלה/סיום לאירוע + סיווג "קצר/ארוך" (רשם-סכמה §7.47; קשור §7.30, §7.43). הגדרת הסף "קצר/ארוך" (סף-משך אוטומטי או דגל ידני) נסגרת בבניית מודול 4. סטטוס: **סגור** (הכלל); פרטי-סף — מודול 4. **↳ חידוד (07/07 ערב):** במימוש — פילטר-הזמינות של Smart Match (C5:756 מנסח הדרה-גורפת "משובצות באותו תאריך") מיושם לפי הכלל שהוכרע כאן (קצר+פער-שעה), לא כפשוטו של C5, אחרת ייפסלו דיילות שההכרעה מתירה.
30. 🟡 `פתוח·DB·מ3/4` **אירועים רב-יומיים / חוצי-חצות:** המודל כולו יום-בודד (`final_event_date` יחיד + "שעות"); כנס דו-יומי או משמרת 20:00→02:00 לא מיוצגים. **מיודע (07/07):** תוספת זמני-התחלה/סיום (§7.29/§7.45/§7.47) פותרת שעות-יום, אך **רב-יומי אמיתי עדיין פתוח**. סטטוס: **פתוח** (סכמת מודולים 3/4).
31. 🟡 `פתוח·אוטומציה·מ6` **ביטול פרויקט — קסקדה עסקית:** ‏C5:266 מעביר פרויקט מבוטל לכספים, אך שותק על הילדים: דיילות "אושרה סופית", משימות לוגיסטיקה פתוחות, וזכאות שכר. **צומצם (07/07/2026):** צד ה**תשלום** (0/50/100%/force-majeure) וצד ה**שחרור** נסגרו ב-§7.16(ב)+§7.33. **נותר פתוח כאן:** סגירת/ביטול משימות הלוגיסטיקה ומיילי-הביטול לדיילות (תבנית 1.8.5 מוגבלת לצמצום-תכולה). סטטוס: **פתוח — מצומצם** (מודול 6).
32. 🟡 `פתוח·אוטומציה·מ6` **מעבר אוטומטי ל"אירוע הסתיים" מול אי-מוכנות:** ‏C5:262 הופך סטטוס באופן בלתי-מותנה כשהתאריך עובר — גם לפרויקט "בתהליך" עם איוש חלקי; ומתנגש עם C5:267 (סגירה ידנית ע"י המנהלת). מה הדין לפרויקט שהתהפך-אוטומטית ולא נסגר ידנית. סטטוס: **פתוח** (מודול 6; המנגנון עצמו — §7.42). **↳ חידוד (07/07 ערב):** מסלול-כניסה נוסף לאותו מצב — אישור הצעה שתאריך-האירוע שלה כבר עבר (אין שום ולידציית תאריך-מינימום; ‏`final_event_date` יורש את המשוער, C5:226, והצעה חיה עד 30 יום) ⇒ ה-cron הופך את הפרויקט מיד ל"אירוע הסתיים" בלי שעבר דרך בתהליך/מוכן. לצרף ולידציית-אישור להכרעה (מודול 3).
33. 🟢 `סגור·לוגיקה·מ4` **עודף זימונים — נתיב שחרור:** ✅ **הוכרע (07/07/2026, ישי — אשכול השיבוץ): שחרור אוטומטי כשהמשרה מתמלאת.** ברגע שהמשרה אוישה (למשל 5 דיילות אישרו סופית) — כל שאר ה"זמניות" העודפות משתנות אוטומטית לסטטוס `released`. אם דיילת ששוחררה תנסה לאשר הגעה דרך הקישור — המערכת מציגה "המשרה כבר אוישה". ‏idempotency של הקישור מטופל דרך פקיעת ה-token (§7.45). סטטוס: **סגור** (מודול 4). **↳ חידוד (07/07 ערב):** מנגנון-הכתיבה של השחרור לא נקבע (טריגר-DB כמו §7.44, או קוד-API אטומי עם האישור) — והאישור-הסופי ידני (C5:297), כך שמונה-המאושרות יכול לחצות זמנית את הנדרש; נגזרת-ה-ready (§7.43) חייבת לבדוק ≥ ולא =.
34. 🟡 `פתוח·לוגיקה·מ2/4/9` **השבתת ישות בשימוש:** הפיכת לקוח/דיילת/מוצר ל"לא פעיל" מוגדרת רק קדימה (חסימה מ-flows חדשים) — האפיון שותק על התחייבויות חיות: הצעות/פרויקטים פעילים של לקוח מושבת, שיבוץ "אושרה סופית" של דיילת שהושבתה (C5:781 חוסם רק שיבוץ עתידי), מוצר בצ'קליסט פעיל. סטטוס: **פתוח** (מודולים 2/4/9 — עולה ראשון במודול 2). **↳ חידוד (07/07 ערב):** גם הכיוון ההפוך פתוח — מוצר `inactive`/`out_of_stock` ניתן היום להוספה להצעה *חדשה* (אין guard על `quote_services.sku`→`products.status`); והערך "חסר במלאי" (C6 §2.4.3) חסר-צרכן בכלל — אף תהליך ב-C5 לא קורא אותו. להכריע במודול 3.
35. 🟡 `פתוח·הרשאות·מ9` **‏Off-boarding משתמשת ובעלות פרויקטים:** מסך המשתמשים רק מדליק/מכבה גישה; אין תהליך עזיבת-עובדת, ואין העברת בעלות ל-`projects.owner_email` של משתמשת שהוקפאה (`current_user_role_id()` מחזיר NULL ל-inactive — הפרויקטים שלה "תקועים"). סטטוס: **פתוח** (מודול 9; הבסיס ממודול 1).
36. 🟡 `פתוח·אוטומציה·מ10` **כשל שליחה טרנזקציוני (מייל/PDF):** האפיון מגדיר שליחות חובה (PDF הצעה, זימונים, אישור סופי, תזכורת 24ש', שכר לרו"ח) בלי שום טיפול בכשל — ‏bounce/כשל SMTP/כשל הפקת PDF: ‏retry? חיווי למשתמשת? האם השמירה נשמרת כשהשליחה נפלה? סטטוס: **פתוח** (מודול 10 — שם המיילים נעשים אמיתיים). **↳ חידוד (07/07 ערב):** להרחיב את הפריט גם לאטומיות העלאת-קובץ+כתיבת-DB — סגירת-אירוע מחייבת קובץ (C5:647), וכן העלאה שיווקית (מודול 2) וחשבונית (§7.38): כשל-חצי (Storage הצליח/DB נכשל או להפך) משאיר קובץ-יתום או רשומה עם URL תלוי; לקבוע סדר קנוני + ניקוי-יתומים. עולה ראשון במודול 2.
37. 🟡 `פתוח·לוגיקה·מ7/8` **שקלול ציון המשוב:** הסקר מכיל 4 שאלות דירוג (C5:1135–1138) אך המנהלת מזינה "ציון משוקלל" אחד 1–5 (C5:411) — כלל השקלול לא קיים; וה-KPI "ממוצע שביעות רצון משוקלל (מחושב רבעונית)" לא מגדיר לא את המשקולות ולא את החלון (רבעון קלנדרי / 90-יום נגלל). סטטוס: **פתוח** (מודולים 7/8).
38. 🟢 `סגור·אוטומציה·מ8` **חשבונית-PDF — סתירה פנימית:** ✅ **הוכרע (07/07/2026, ישי — אודיט; תחום מודול 8): העלאה-ידנית-ושליחה. המערכת אינה מפיקה חשבונית.** הזרימה: מנהלת הכספים **מעלה קובץ-חשבונית** שהכינה (בתוכנת הנהלת-החשבונות שלה) → **"שמור ושלח"** → הודעה קופצת ("נשמר בהצלחה, החשבונית נשלחה ללקוח") → המערכת **שומרת** את הקובץ, **שולחת** אותו במייל ללקוח (תבנית §1.8.3), **ומסמנת ✓** ‏`projects.invoice_sent`. זה מיישב את הסתירה: §1.2.2 (המערכת לא מפיקה) + התבנית (מייל עם קובץ מצורף) חיים יחד — הקובץ מגיע מהעלאה, לא מהפקה. **דפוס המימוש = כמו העלאת החומר השיווקי במודול 2** (Storage + שליחה); פרטי-אחסון (עמודת URL לקובץ) יוכרעו בבניית מודול 8. סטטוס: **סגור.**
39. 🟡 `פתוח·אוטומציה·מ6/8` **סקר משוב — כפל שולחים:** ‏§1.8.2 שולח את הסקר בנעילת האירוע (מנהלת פרויקטים), ‏§1.5.10 שולח שוב בשלב החשבונית (כספים) — הלקוח מקבל פעמיים ובעלות התהליך מתחלפת. מי שולח, מתי, פעם אחת. סטטוס: **פתוח** (מודולים 6/8).
40. 🔵 `להנהון·DB·מ2/3/8` **ייחודיות מפתחות תשתית (להנהון + מיגרציה מתואמת):** ‏(א) `roles.role_name` ו-`modules.module_name` **לא UNIQUE** בעוד כל ה-RLS משווה אותם כמחרוזות (`role_name = 'מנכ"ל'`, ותבנית §7.21 לפי `module_name`) — שינוי-שם/כפילות שוברים אבטחה בשקט; ‏**טריגר: לפני ה-policies הראשונות של מודול 2**. ‏(ב) `params.param_name` לא UNIQUE — כל מנועי החישוב שולפים לפי שם; לפני Seed מודול 3. ‏(ג) ל-`salary_reports` אין מזהה-חודש — אין מניעת הרצה כפולה של דוח חודשי; מודול 8. **✅ הונהן חלקית (ישי, 10/07/2026): (א)** UNIQUE ל-`roles.role_name`/`modules.module_name` **אושר** למיגרציית מודול-2 צעד 1.1 (0 כפילויות חי 10/07 ✓, ממתין-לביצוע); **(ב)** `params.param_name` נשאר להנהון (מ3), **(ג)** `salary_reports` (מ8). סטטוס: **פתוח — להנהון** ((א) הונהן; (ב)/(ג) ממתינים).
41. 🔵 `להנהון·לוגיקה·פר-מודול` **חבילת הנהונים טכניים (פר-מודול, בעת הבנייה):** אכיפת שדות חובה-מותנית (rejection/cancel/negative-feedback reasons, ‏actual_* בסגירה — באיזו שכבה) · ‏CHECKs של enum שהסכמה משמיטה (`quote_services.color`, `products.unit`) · מרוצי max+1 במפתחות-משנה ידניים (`line_number`/`serial_number`/`assignment_number`) · תצוגת KPI במכנה-אפס · אשרור מפורש ש-last-write-wins בעריכה מקבילה של הצעה מקובל (C5:226) · הצעה שפגה אוטומטית נרשמת בלי סיבת-דחייה (חור באנליטיקת הפסדים) + אין נתיב החייאה · שדה משוב 3-ערכים (C5:409) מיושם כנגזרת-תצוגה מעל `feedback_status`+`feedback_score` · "משפיע מיידית" על KPI = חישוב-בקריאה (אין push) · **תוספות מביקורת 07/07 ערב:** ‏CHECKs ‏≥0 ש-C6 דורש והסכמה השמיטה (`logistics.actual_qty` — C6 §2.4.13; ‏`assignments.actual_hours`+`personal_bonus` — C6 §2.4.14; שעות/בונוס שליליים = שכר שלילי) · כלל גזירת-הלוגיסטיקה = ‏`category != 'hostess'` (‏C5:369 מנסח "קטגוריות" 'תגים'/'שרוכים' שאינן ב-enum — C6 §2.4.3) · תוויות-עברית קנוניות לסטטוסים לפי C6 ("הסתיים", "נדחתה"; ‏C5:117/227 "פרויקט הסתיים"/"נדחה" = נרטיב, לא ערך) · "בזמן אמת" במסכי-עבודה (C5:733) = קריאה-בטעינה כמו ה-KPI · רענון-הרשאות באמצע-session = בטעינה הבאה (ה-RLS ממילא חי פר-שאילתה) · בדיקת-קבלה לעברית/RTL/פונט-מוטמע ב-PDF/Excel מיוצרים (מודול 3 ואילך). סטטוס: **פתוח — להנהון**.
42. 🟢 `סגור·אוטומציה·מ3` **מנגנון ביצוע למעברים מבוססי-זמן (חוצה-מערכת — הכרעת-תשתית אחת, כמו §7.21 ל-RLS):** ✅ **הוכרע (07/07/2026, ישי — חבילת הכסף): `pg_cron` למעברי-סטטוס + Edge Function עתידי לשליחות.** **מעברי-הסטטוס הטהורים** (פקיעת הצעה אחרי 30 יום, C5:227; מעבר פרויקט ל"אירוע הסתיים" כשעובר התאריך, C5:262 = §7.32) ירוצו כ-job יומי ב-**`pg_cron`** — ‏UPDATE טהור בלי מייל, ‏DB-נייטיב, זול, **ובר-הפעלה כבר במודול 3** (הצרכן הראשון = פקיעת ההצעות) בלי תלות במודול 10. אומת דרך ה-MCP: ‏pg_cron **זמין בפרויקט** (לא מותקן עדיין, לא חסום-תוכנית). כל מה ש**שולח** (תזכורת 24ש' — כבר נעוצה ל-`reminder_sent`) יושב ב-**Edge Function מתוזמנת שנבנית במודול 10**. חישוב-עצל-בטעינת-מסך **נפסל** (הצעה שאיש לא פותח לא הייתה פגה לעולם). **רייד שהתקבל:** להבחין בין הצעה-שפגה-אוטומטית להצעה-שנדחתה-ידנית (היום שתיהן `rejected` — חור באנליטיקת-הפסדים); הבחירה בין ערך-סטטוס נפרד `expired` לבין עמודת-דגל/timestamp **נשארת לשלב הבנייה של מודול 3** (לא נדרש להכריע כאן). סטטוס: **סגור** (המנגנון); פרטי-מימוש — מודול 3. **↳ חידוד (07/07 ערב):** מקור-הזמן של תזכורת-הדיילת כפול — C5:298 "24 שעות" קשיח מול פרמטר `שעות_תזכורת_לדיילת` (C5 §1.8.7; ‏Seed #6=24): בבניית מודול 10 — **הפרמטר קובע**, C5:298 = דוגמה. וגם: ‏C6 (טריגר T3) מתאר בטעות את T3 כשליחת פרטי-אירוע-למאושרות (זה §1.8.6 מונחה-הפעולה) — ניסוח שגוי באפיון הקפוא, לא לממש לפיו. **↳ קודם ל-§7.75 (09/07/2026).**
43. 🟢 `סגור·לוגיקה·מ4/6` **"נעילה" מול "מוכן לביצוע":** ✅ **הוכרע (07/07/2026, ישי — אשכול השיבוץ): `ready` הוא מצב נגזר והפיך, בלי עמודת-נעילה.** ‏`project_status='ready'` = **נגזר** מהתנאי איוש 100% **וגם** לוגיסטיקה 100%; אין עמודת-נעילה נפרדת — "נעילת-האיוש" היא תת-תנאי נגזר, לא שדה. **המצב הפיך:** דיילת מבטלת (כותבת למנהלת בוואטסאפ) → המנהלת דורסת ידנית את סטטוס-השיבוץ שלה → הטריגר האטומי (§7.44) מחזיר את הפרויקט אוטומטית מ-`ready` חזרה ל-`in_progress`. **מקרי-קצה שנרשמו:** לוגיסטיקה יכולה להגיע ל-100% ולחזור אחורה; שינויי-תכולה קורים כל הזמן; שינויים אפשריים עד **~36 שעות** לפני האירוע גם כששני הסטטוסים 100% — המערכת חייבת להתמודד עם כל המצבים (כולל מקרי-קצה שטרם נצפו). התנהגות-ההקשחה ב/אחרי 36ש' נסגרת בבניית מודול 4/6. סטטוס: **סגור** (המודל); סף-36ש' — מודול 4/6.
44. 🟢 `סגור·DB·מ4/5/6` **מי כותב את `project_status` הנגזר:** ✅ **הוכרע (07/07/2026, ישי — אשכול השיבוץ): טריגר DB אטומי.** ‏`project_status` נכתב ע"י **טריגר DB אטומי** על שינויי `assignments`/`logistics` — **לא** חישוב-בקריאה (שאינו מתמיד את השינוי) ו**לא** קוד-אפליקציה מפוזר. מקור-אמת יחיד, אין drift; זה גם המנגנון שמחזיר את הפרויקט אחורה מ-`ready` (§7.43). סטטוס: **סגור** (מודולים 4/5/6). **↳ חידוד (07/07 ערב — חשוב למימוש):** הטריגר חייב guard-סטטוס — חישוב-מחדש רק כש-`project_status ∈ {not_started, in_progress, ready}`; בלעדיו הזנת `actual_hours`/`actual_qty` בסגירה התפעולית (C5:267) תפעיל את הטריגר ותחזיר פרויקט שכבר "אירוע הסתיים"/"ממתין לחשבונית" אחורה ל-in_progress.
45. 🟢 `סגור·אוטומציה·מ4/10` **קישור אישור/סירוב משמרת לדיילת לא-מאומתת:** ✅ **הוכרע (07/07/2026, ישי — אשכול השיבוץ): endpoint ציבורי + token פר-שיבוץ עם פקיעה משולשת.** ‏`assignments.invite_token` (+`invite_sent_at`) נקרא ע"י Edge Function ציבורית שמאמתת ומעדכנת סטטוס. **הטוקן פג לפי המוקדם מבין שלושה:** ‏(1) המשרה אוישה במלואה · (2) חלפו **48 שעות** מרגע שליחת הזימון · (3) **24 שעות לפני** מועד תחילת האירוע. ריצת-מרוץ של שתי דיילות שמאשרות בו-זמנית = **לא-בעיה בפועל** (הכרעת ישי; האישור הסופי ידני ממילא). **נדחה במפורש לדיון בבניית המודול (לא עכשיו):** ערוץ ההודעה (מייל או SMS) + בחירת שירות-שליחה חינמי + האם זרימת-הזימון יושבת במודול 4 או במודול 10 — מקביל ל-§7.42: **מודול 4 מגדיר את ה-token, המעברים וה-endpoint; השליחה בפועל = תשתית מודול 10.** תלות-סכמה: §7.47. סטטוס: **סגור** (המנגנון); ערוץ/שירות — דחוי למודול 4/10. **↳ חידוד (07/07 ערב):** ליישר את סמנטיקת הערך השני של סטטוס-השיבוץ — C5:293 "אושרה זמנית" (פעולת-מנהלת) מול C6 §2.4.14 "אישרה זמינות" (פעולת-דיילת דרך הקישור): להכריע בבניית המודול מי כותב `confirmed_available` — הדיילת ב-endpoint (והמנהלת רק דורסת), או המנהלת.
46. 🟡 `פתוח·לוגיקה·מ8` **מניעת כפל-תשלום בשכר — הלוגיקה בפועל:** ‏C5:472 "קוד הפקה ייחודי למניעת כפילויות" — אך המנגנון האמיתי (חתימת `assignments.salary_report_id` בהפקה + החרגת חתומים בשאילתת החודש הבא) לא כתוב; ומקרה-קצה פתוח: פרויקט שנסגר אחרי שדוח חודשו כבר הופק — השעות נאספות אי-פעם או נופלות בין החודשים (השאילתה סורקת "פרויקטים שהסתיימו בחודש X", ‏C5:471)? סטטוס: **פתוח** (מודול 8).

47. ⚪ `מעקב·DB·מ3/4/8` **📋 רשם תוספות-הסכמה מהכרעות האודיט (07/07/2026) — ריכוז לביצוע במיגרציות עתידיות.** *לא פריט-החלטה — טבלת מעקב לתוספות ה-DB שהוכרעו, כי ישי הדגיש "לתעד היטב כל שינוי DB, בסוף מציגים את הטבלאות החדשות". כל שורה = מיגרציה בעלת-שם עם כותרת-why עברית + עדכון `schema.sql` + שורת CHANGELOG (פרוטוקול DB), במודול המצוין — לא עכשיו.*
    | טבלה | עמודות חדשות | מקור-הכרעה | מודול-ביצוע |
    |---|---|---|---|
    | `hostesses` | `address` (טקסט), `lat`, `lng` (קואורדינטות) | §7.6, §7.14 | 4 |
    | `projects` | זמני התחלה/סיום לאירוע + סיווג "קצר/ארוך" | §7.29, §7.45, §7.43 (קשור §7.30) | 4 |
    | `projects` | `cancelled_at` (timestamptz), `cancellation_reason` enum(`standard`,`force_majeure`) | §7.16(ב) | 4/8 |
    | `assignments` | `invite_token`, `invite_sent_at` (+פקיעה) | §7.45 | 4 |
    | `assignments` | `attendance_status`, `lateness_level`, `no_show_reason` | §7.16(א) | 4/8 |
    | `quote_services` | `closing_unit_cost` (הקפאת עלות) | §7.28 | 3 |
    | `params` / `roles` / `modules` / `salary_reports` | אילוצי-ייחודיות | §7.40 | 2/3/8 |
    סטטוס: **מעקב פתוח** עד שכל השורות בוצעו במיגרציות.
    **התצוגה המבצעית המאוחדת** של רשם זה + כל פריטי-§7 הסכמתיים + חובות-היומנים + היגיינת-DB: ‏`docs/db_roadmap.md` (נוצר 08/07/2026; נקרא לפני כל תכנון מיגרציה — ה-SSOT להחלטות נשאר כאן, ב-§7).

> **פריטים 48–60 נוספו 07/07/2026 ערב בביקורת-אפיון אדוורסרית** (סשן קריאה-בלבד שהפך כותב באישור ישי): C5+C6 נבחנו מול עצמם ומול הסכמה החיה בשש עדשות — כיסוי · עקביות-פנימית · דריפט-סכמה · stress לוגיקה-עסקית · שתיקות · מנגנוני-ביצוע (3 סוכני-קריאה מקבילים + אימות MCP חי; 31 ממצאים, אפס חוסמי-מיידי). באותה ביקורת נוספו **חידודי ↳** לפריטים 15, 16, 17, 29, 32, 33, 34, 36, 41, 42, 44, 45. הדו"ח המלא נמסר לישי בצ'אט (עותק: קובץ-התוכנית של הסשן).

48. ⚪ `ממתין-לביצוע·הרשאות·מ2` **מיגרציית enable-RLS ל-11 הטבלאות העסקיות (פער-שחזור):** אומת חי (MCP, ‏07/07): ‏RLS מופעל בפועל על כל 16 הטבלאות (ה-11 העסקיות = RLS-on/0-policies כמוצהר) — **אבל ההפעלה לא קיימת בשום מיגרציה ולא ב-`schema.sql`** (בוצעה ידנית ב-Studio טרום-עידן-המיגרציות; ‏schema.sql:186–322 מפעיל רק על roles/modules/permissions/users/login_attempts). סביבה שתיבנה משרשרת המיגרציות תקום עם RLS כבוי על 11 טבלאות = פתוחות לגמרי, ומקור-האמת #1 מציג מצב-אבטחה שגוי. נדרש: מיגרציה idempotent + עדכון snapshot. **✅ הונהן (ישי, 10/07/2026):** מאושר למיגרציית מודול-2 צעד 1.1 (idempotent enable-RLS ל-10 הטבלאות שנותרו; customers כבר בבסיס). סטטוס: **ממתין-לביצוע** (החלת מיגרציית 1.1).
49. 🟡 `פתוח·DB·מ3` **אטומיות המרת הצעה→פרויקט:** האישור = עדכון סטטוס ההצעה + יצירת פרויקט + ירושת תאריך + גזירת שורות `logistics` (‏C5:230/597/369) — בלי שום הגדרת טרנזקציה/אידמפוטנטיות. כשל-באמצע או דאבל-קליק ⇒ הצעה מאושרת בלי פרויקט, פרויקט בלי משימות, או שגיאת-unique גולמית למשתמשת (‏UNIQUE על `projects.quote_id` עוצר רק כפילות-פרויקט). ‏§7.42/§7.44 לא מכסים את קסקדת-האישור. כיוון: RPC/פונקציית-DB אחת + טיפול-כשל ידידותי. סטטוס: **פתוח** (מודול 3). **↳ קודם ל-§7.76 (09/07/2026).**
50. 🟡 `פתוח·DB·מ3` **נעילת הצעה מאושרת — שכבת DB:** ‏"ננעלת לחלוטין לעריכה" (C5:231/568) היא כרגע אכיפת-UI בלבד — אין עמודת-נעילה, טריגר או policy שמונעים UPDATE/DELETE על הצעה `approved` או על `quote_services` שלה (FK ב-ON DELETE CASCADE). מי שיש לו edit על מודול ההצעות (§7.21 = רמת-מודול) יכול ב-API ישיר לשנות את השורות הקפואות ש-§7.12 (שחזור PDF) ו-§7.28 (רווחיות) נשענים עליהן — אותו רציונל שבגללו §7.26 דרש CHECK. כיוון: טריגר-חסימה או policy מותנית-סטטוס. סטטוס: **פתוח** (מודול 3). **↳ קודם ל-§7.77 (09/07/2026).**
51. 🟡 `פתוח·DB·מ3` **הקפאת מע"מ באישור הצעה:** ההנחות מוקפאות ב-`quotes` אבל שיעור-המע"מ לא — והפרמטר עריך במודול 9 (C5:883) בעוד §1.3.4 מניח אותו קבוע. שינוי 18→X משנה רטרואקטיבית את "סה"כ סופי" של כל הצעה מאושרת, וה-PDF-המשוחזר (§7.12) יציג ללקוח סכום שונה מזה שאישר. כיוון: ‏`quotes.vat_rate_snapshot` מוקפא ברגע האישור (משלים את סט-ההקפאה של §7.28; מועמד לרשם §7.47 עם ההכרעה). סטטוס: **פתוח** (סכמת מודול 3).
52. 🟡 `פתוח·לוגיקה+DB·מ6/8` **רווח גולמי סופי — נשמר או נגזר, ובאיזו יחידה:** ‏C5:453 ("שחושב **ונשמר** בעת סגירת הפרויקט") מול C6 §2.4.4 ("נגזר, לא נשמר פיזית") — ואין עמודה בסכמה. נגזר-בקריאה ⇒ עריכת `actual_hours` מאוחרת מזיזה דו"ח רווחיות היסטורי; נשמר ⇒ חסרה עמודה + הגדרת רגע-הקפאה. נלווה: יחידת-התצוגה סותרת — ₪ בסגירה (C5:833) מול % בכל ה-overview והדו"חות (C5:415/801/1008). סטטוס: **פתוח** (מודול 6/8; משלים את §7.28). **↳ קודם ל-§7.78 (09/07/2026).**
53. 🟡 `פתוח·DB·מ3/6` **אירוע ללא דיילות אינו ניתן-לייצוג:** ‏CHECK ‏>0 על `quotes.recommended_hostess_count` ו-`projects.required_hostess_count` חוסם הזמנת אתר-רישום/תגים-בלבד — תרחיש לגיטימי לפי הקטלוג (C5:170). להכריע: בהיקף (‏CHECK→‏≥0 + דין מדד-איוש ריק) או מחוץ (ולידציה+הודעה מפורשת). סטטוס: **פתוח** (מודול 3/6).
54. 🔵 `להנהון·DB·מ4` **מניעת שיבוץ-כפול-פעיל באותו פרויקט:** ‏PK ‏`(project_id,id_number,assignment_number)` מתיר בכוונה שורות מרובות לאותה דיילת (היסטוריית סירובים, C5:293) — אבל שום אילוץ לא מונע שתי שורות *פעילות* (או שתי `finally_approved`) לאותה דיילת באותו פרויקט ⇒ כפל-שעות בדו"ח השכר. ‏§7.41 מכסה רק את מרוץ-ה-max+1. כיוון: unique חלקי על סטטוסים פעילים או בדיקת-API (מקביל ל-§7.29). סטטוס: **פתוח — להנהון** (מודול 4).
55. 🟡 `פתוח·DB·מ4` **קואורדינטות צד-האירוע + בחירת שירות גאוקוד + דין-NULL:** ‏haversine (§7.14) דורש שתי נקודות, אבל §7.47 מוסיף `lat`/`lng` לדיילות בלבד — ‏`projects.final_location` (ו-`quotes.estimated_location`) נשארים טקסט חופשי. וגם: שירות הגאוקוד החד-פעמי (§7.6 "מגאוקוד") לא נבחר — "בלי Maps API בתשלום" מחייב חלופה חינמית מוגדרת; ואין דין לכתובת שלא גואקדה (דיילת/אירוע עם NULL — מוחרגת? ציון-מרחק 0? "מרחק לא ידוע"?). סטטוס: **פתוח** (מודול 4; משלים §7.14+§7.47).
56. 🔵 `להנהון·DB·מ3` **אזור-זמן ו-DST:** כל לוגיקת-הזמן — ‏pg_cron "יומי" (§7.42), פקיעת-token משולשת (§7.45), תזכורות — רגישת-TZ ואינה מוגדרת בשום מקום; ‏cron ב-UTC = ‏02:00–03:00 ישראל, וישראל מחליפה DST. להנהון: הכול Asia/Jerusalem, עמודות-זמן חדשות = `timestamptz`, ‏jobs בשעה מקומית קבועה. סטטוס: **פתוח — להנהון** (מודול 3 — ה-job הראשון).
57. 🟡 `פתוח·לוגיקה·מ3/8` **פרמטר-רפאים `יום_הפקת_דוח_שכר`:** ‏Seed ‏#4 (=5, ‏`products_and_params.md`) מרמז על הפקת דו"ח-שכר *מתוזמנת*, אבל C5:470–472 + §1.8.10 מתארים תהליך *ידני* בלחיצת "ייצא ושלח" — פרמטר בלי מנגנון ובלי נרטיב. להכריע: ידני (לא לזרוע את הפרמטר) או מתוזמן (מנגנון בסגנון §7.42, מודול 10). סטטוס: **פתוח** (Seed מודול 3 / מודול 8).
58. 🟡 `פתוח·לוגיקה·מ8/11` **נוסחת סטיית-התקציב:** ‏"תכנון מול ביצוע" (C5:1008; ‏C6 §2.4.4 "סטיית תקציב") משווה עלות-דיילות מתוכננת **פר-משמרת** (מהקטלוג, תוקפא ב-§7.28) מול עלות בפועל **פר-שעה** (‏`actual_hours × hourly_rate_snapshot`) — בסיסים לא-ברי-השוואה בלי כלל-גישור מוגדר. סטטוס: **פתוח** (מודול 8/11; קשור §7.19).
59. 🟡 `פתוח·לוגיקה·מ7` **הגדרת ה-KPI "רווח גולמי משוער לחודש":** ‏C5:141/507 לא מגדיר את האוכלוסייה (פרויקטים עם אירוע-החודש? כל הפתוחים? הסתיימו-וטרם-נסגרו?) ולא את שקלול מתוכנן/בפועל בפרויקט חצי-סגור — אח של §7.37 בפריט נפרד. סטטוס: **פתוח** (מודול 7).
60. 🟡 `פתוח·תהליך·מ10` **מודול 10 חסר אפיון-מודול:** לאוטומציות אין מסך, תהליך או טבלה באפיון — היקפו מורכב מרסיסים: תבניות §1.8, טריגרי T1–T3, וחצאי-ההכרעות §7.36/§7.42/§7.45/§7.57. בפתיחת המודול: הבלופרינט מסנתז את היקפו מהמקורות האלה ומאושר ע"י ישי כסטייה-מתועדת (אין סעיף-אפיון לצטט). סטטוס: **פתוח — מודעות** (פתיחת מודול 10).

> **פריטים 61–70 נוספו 08/07/2026 באודיט-ערעור מבני על ה-DB** (סבב-2 בהוראת ישי: "לערער על הטבלאות עצמן ועל הרפרנס, הכול בעירבון מוגבל"): קריאה מלאה של C5+C6+products_and_params + סוכן-ערעור עצמאי + אימות MCP חי, עם דגש על *עלות-דחייה* — רק מודולים 1–2 קיימים, ושינויי-מבנה היום זולים פי-כמה מאשר אחרי מודולים 3–8. באותו אודיט: נוספו רוכבי-↳ לפריטים 12, 17, 23, 25, 42, 49, 50, 52 *(קודמו לפריטים §7.71–78 ב-09/07/2026 — ר' בלוק-האצווה למטה)* · נוצר **`docs/db_roadmap.md`** (התצוגה המבצעית המאוחדת של כל שינויי-הסכמה העתידיים) · חוסנו שני הטמפלטים (בלוק "DB Design Challenge" + צ'קליסט-מיגרציה) · הורחב ה-Stop hook (מיגרציה בלי עדכון-roadmap = חסימה). ממצאי-הרקע המלאים ורשימת פגמי-הרפרנס: ‏db_roadmap ‏§Lane-B + ‏§9. ‏"להנהון" = אישור קצר של ישי, לא דיון.

61. 🟡 `פתוח·DB·מ6` **תשתית Supabase Storage מאוחדת:** האפיון מחייב קבצים בארבעה מקומות — דוח-סיכום חובה בסגירת פרויקט (C5:647–648), קובץ דוח-שכר (C6 §2.4.10), חשבונית-מועלית (§7.38), חומר שיווקי (C5:538; באקט `marketing` כבר בבלופרינט מודול 2) — אך אין תוכנית-Storage מרוכזת: אילו buckets, ‏public מול private+‏Signed-URLs (שכר/חשבוניות = רגישים), ותבנית policies ל-`storage.objects` (מקבילת-§7.21 לקבצים). קשור §7.36 (אטומיות העלאה+DB). סטטוס: **פתוח — דיון קצר** (כיוון בבלופרינט מודול 6; הבאקט הראשון כבר במודול 2).
62. 🔵 `להנהון·DB·מ2` **הידוק NOT NULL לעמודות-חובה:** ‏`users.role_id` (כל שרשרת ה-RLS נשענת על `current_user_role_id()` — משתמש בלי תפקיד = NULL = כל המסכים ריקים בלי הסבר), ‏`quotes.customer_id` (חובה באפיון, C6:226), ‏`projects.owner_email` (בעלת-פרויקט חובה, C6:246), ‏`projects.quote_id` (פרויקט נולד רק מהצעה מאושרת, C5:230 — ה-nullable היום מתיר "פרויקט-בלי-שם" שאין לו לקוח, שם או מפרט). ביצוע לכל עמודה: בדיקת-אפס-NULLים חיה → ‏`SET NOT NULL`. **✅ הונהן חלקית (ישי, 10/07/2026):** החלק של `users.role_id` **אושר** למיגרציית מודול-2 צעד 1.1 (בדיקת-אפס-NULLים חיה 10/07 = 0 ✓, ממתין-לביצוע); `quotes.customer_id` (מ3) ו-`projects.owner_email`/`quote_id` (מ6) **נשארים להנהון** במודוליהם. סטטוס: **פתוח — להנהון** (החלק של users הונהן; יתרת-העמודות ממתינה למ3/מ6).
63. 🟡 `פתוח·הרשאות·מ6/8` **בעלות-עמודות מול RLS שורתי (הרחבת §7.21 לטבלאות רב-מודוליות):** מטריצת-ההרשאות היא *עמודתית* בפועל — מנהלת-הכספים חייבת לכתוב `invoice_sent`/`payment_date`/`feedback_*` על שורות `projects` בלי edit על מודול פרויקטים (C5 §1.4.3, §1.5.10), ‏ו-RLS אינו מבחין בעמודות: policy רחבה תיתן לה לדרוס גם `required_hostess_count`; באותו אופן `hostesses.bank_*`+ת"ז גלויים לכל תפקיד עם קריאת-שורות למרות "גישה חסומה לריכוז שכר" (C5 §1.4.2). ‏§7.21 נשארת נכונה לטבלאות חד-מודוליות; נדרשת הכרעת-המשך לרב-מודוליות: פיצול-ילד (`project_finance`) / כתיבה דרך RPC בבעלות-עמודות / view+‏column-grants לעמודות-PII. **✅ כיוון-הוכרע (ישי, 10/07/2026): נדחה ל-M6/M8** — `customers` חד-מודולי, אז §7.21 חל כמו-שהוא במיגרציית מודול-2 (1.1); הכיוון הרב-מודולי מוכרע כשמ6/8 נתקלים במקרה הקונקרטי (לא להעתיק §7.21 בעיוורון לטבלאות רב-מודוליות עד אז). סטטוס: **פתוח — דיון** (הועבר ממ2 למ6/8; קשור §7.77).
64. 🟡 `פתוח·DB·מ3` **מדיניות מפתחות — PK טבעי מול surrogate (ההחלטה הכי ממונפת-בזמן ברשימה):** כל הליבה ממופתחת במחרוזות אנושיות/PII: ‏`users.email` (בעוד Supabase Auth מזהה ב-UUID — שינוי אימייל בצד אחד מנתק את `current_user_role_id()` ונועל את המשתמש; ביומנים כבר רשום חוב-הסנכרון), ‏`customers.customer_id` ח"פ (תיקון-הקלדה = עדכון-PK חסום-FK; ושתי יחידות של אותו גוף ממשלתי = אותו ח"פ = בלתי-ניתן-לייצוג), ‏`hostesses.id_number` ת"ז (מוטבע ב-PK של כל שיבוץ — פיזור-PII; תיקון ספרת-ביקורת אחרי אירועים נחסם), ‏`products.sku` (כבר שוּנה פעם בהחלטות ה-Seed). אף FK לא מגדיר ON UPDATE. חלון-הזמן: היום surrogate כמעט חינם; אחרי מודולים 3–8 — מפתחות ב-URLs, ‏joins ו-policies. כיוונים: ‏(א) surrogate-pass עכשיו (bigint/uuid; הטבעי נשאר UNIQUE עסקי) ‏(ב) מינימום: ‏ON UPDATE CASCADE לכל FK-טבעי + סנכרון `auth.users`↔`public.users` ‏(ג) קבלה-פר-טבלה (‏users לבד סביר — 5 פנימיים). סטטוס: **פתוח — דיון** (להכריע לפני בלופרינט מודול 3).
65. 🔵 `להנהון·DB·מ2/4` **ייחודיות אימיילים עסקיים:** ‏`customers.email` ו-`hostesses.email` בלי UNIQUE — האפיון שותק, כפילויות אפשריות היום. ברירת-כיוון: ‏hostesses = ‏UNIQUE (זהות-אדם; יעד קישור-הזימון §7.45), ‏customers = להכרעה (איש-קשר אחד לשתי חברות — לגיטימי?). סטטוס: **פתוח — להנהון** (מודול 2/4).
66. 🟡 `פתוח·DB·מ4` **אכיפת `hourly_rate` ≥ שכר-מינימום:** האפיון דורש "≥ שכר מינימום (פרמטר מערכת)" (C6:288) אבל CHECK אינו יכול לקרוא `params` — נדרשת הכרעת-מנגנון: טריגר-DB מול ולידציית-אפליקציה בלבד, + דין רשומות קיימות כשמעלים את הפרמטר במודול 9. סטטוס: **פתוח — דיון קצר** (מודול 4; קשור 9 ו-§7.70).
67. 🟡 `פתוח·DB·מ4` **שיוך שיבוץ לשורת-שירות/משמרת:** אירוע יכול להכיל גם `04ST` וגם `06ST` (C5:171) — אבל `assignments` מפנה רק ל-`project_id`, בלי שיוך לשורת-השירות: אי-אפשר לומר איזו דיילת באיזו משמרת. חוסם את שעות-פר-דיילת וחלוקת-הבונוס (§7.19), את חלון-הזמן של double-booking קצר+פער-שעה (§7.29 — למשמרת אין שעות משלה) ואת רב-יומי (§7.30). כיוון: ‏FK לשורת `quote_services` או ישות-משמרת ילדת-פרויקט. סטטוס: **פתוח — דיון** (בלופרינט מודול 4; משלים את זמני-האירוע של §7.47).
68. 🟡 `פתוח·DB·מ8` **דוח-שכר כמסמך (ולא יומן-הפקה):** ‏`salary_reports` = ‏PK+תאריך-שליחה+URL — בלי חודש-כיסוי, בלי שורות, בלי סכומים; "שחזור" הדוח = שאילתא חיה על `assignments`, ולכן תיקון-שעות מאוחר משנה בדיעבד דוח שכבר נשלח לרו"ח — אין הוכחת-מה-נשלח (C5:471–472). מרחיב את §7.40(ג) (חודש-ייחודי) וסוגר את מקרה-הקצה של §7.46. כיוון: ‏`period` ‏UNIQUE + שורות-snapshot מוקפאות בהפקה (`salary_report_lines`); מינימום: ‏period+total. סטטוס: **פתוח — דיון** (מודול 8).
69. 🔵 `להנהון·לוגיקה·מ4/8` **"+ נסיעות" בתבנית-הזימון מול מודל-השכר:** התבנית מבטיחה לדיילת "תעריף … ש"ח לשעה + נסיעות" (C5:1095) אבל נוסחת-השכר (C5:471) = שעות×תעריף+בונוסים בלבד — לנסיעות אין ייצוג. להכריע: עמודת `travel_amount` בשיבוץ / נספג ב-`personal_bonus` בהנחיה מפורשת / תיקון-התבנית (סטייה מתועדת מהאפיון). סטטוס: **פתוח — להנהון** (מודול 4/8).
70. 🟡 `פתוח·DB·מ3` **‏`params` מטופס + היסטוריה:** טבלת key-value אחת לא-מטופסת מערבבת שלושה עולמות — כסף/אלגוריתם (מע"מ, משקולות, שכר-מינימום: צריכים ולידציה מספרית ולעיתים היסטוריה), תבניות-HTML שלמות (#10–14), וקונפיג-אינטגרציה — כך שעריכת-תבנית ועריכת-מע"מ עוברות באותו צינור טקסט חופשי בלי שום בדיקה. מינימום: ‏UNIQUE(param_name) (‏§7.40) + ולידציית-טיפוס באפליקציה; מקסימום: פיצול לפי אחריות + היסטוריה-אפקטיבית לפרמטרי-כסף (מקל על §7.51 ו-§7.66). סטטוס: **פתוח — דיון קצר** (‏Seed מודול 3).

> **פריטים 71–78 קודמו 09/07/2026 מריידרי-ה-↳ הפתוחים של אודיט-הערעור (08/07/2026)** — השלמת שדרוג-הרשם (מועצת-llm-council; אושר בתוכנית-הסשן 09/07): כל ריידר-"תוספת" פתוח הפך לפריט מלא הנושא "נולד-כ-↳ על §7.N"; פריט-האם נושא מצביע "↳ קודם ל-§7.7X" והטוקן שלו נוקה מסימון-`↳פתוח`. ריידרי-"חידוד" (הבהרות) נשארו בפריטי-האם. תוכן הריידרים הועבר מילולית — תיקוני-הקשר בלבד.

71. 🔵 `להנהון·DB·מ12` **גורל `quotes.pdf_url` ה-deprecated:** לעמודה שהוכרזה deprecated ב-§7.12 אין תוכנית-סיום — להנהון: ‏DROP של `quotes.pdf_url` באצוות-הניקוי של מודול 12, או נשארת לצמיתות; עד אז אף קוד חדש לא קורא/כותב אותה. **נולד-כ-↳ על §7.12 (08/07/2026, אודיט-ערעור); קודם לפריט מלא 09/07/2026.** סטטוס: **פתוח — להנהון** (מודול 12).

72. 🟡 `פתוח·DB·מ6` **ליניאז' שורות הצעה→לוגיסטיקה (ישות שורת-פרויקט):** בעיית שינוי-התכולה (§7.17) עמוקה מעמודות-חסרות — ‏`logistics` *משכפל* את שורות ההצעה בלי שום ליניאז' שורה-לשורה (נגזר באישור, ‏C5:369, ואז חי חיים נפרדים), כך שגם עם עמודות-מחיר אין דרך להצליב מול השורה הקפואה. כיוון-פתרון שלישי (נוסף לשניים שב-§7.17): **ישות שורת-פרויקט אחת** (השורות הקפואות + תוספות-scope-change מדוגלות + סטטוס-הלוגיסטיקה עליהן) — פירוט ב-`docs/db_roadmap.md` ‏Lane B. חוסם את "חיוב על שינויים" שמסך-הכספים מציג (C5:833) — למספר הזה אין כיום עמודת-מקור. **נולד-כ-↳ על §7.17 (08/07/2026, אודיט-ערעור); קודם לפריט מלא 09/07/2026.** סטטוס: **פתוח** (מודול 6; מוכרע יחד עם §7.17).

73. ⚪ `ממתין-לביצוע·DB·מ2` **חותמות-זמן לכל הטבלאות העסקיות:** נפרד מה-audit המלא (§7.23, שנשאר דחוי) — שכבת-היגיינה זולה **עכשיו**: ‏`created_at`/`updated_at`+טריגר `moddatetime` לכל הטבלאות העסקיות במיגרציה אחת (מודול 2). למה לא לחכות: דאטה שנצבר בלי חותמות = היסטוריה שאבדה לתמיד (אי-אפשר להוסיף רטרואקטיבית), אין מיון "לפי עדכון אחרון", ולעוגן של T2 ("הצעה שלא נענתה 30 יום" — ‏C5:227: ממתי סופרים אחרי עריכה?) אין עמודת-זמן מוגדרת. **נולד-כ-↳ על §7.23 (08/07/2026, אודיט-ערעור, "פיצול-להנהון"); קודם לפריט מלא 09/07/2026.** **✅ הונהן (ישי, 10/07/2026):** אושר למיגרציית מודול-2 צעד 1.1 (`created_at`/`updated_at` + `moddatetime` על 11 הטבלאות העסקיות). סטטוס: **ממתין-לביצוע** (החלת מיגרציית 1.1).

74. ⚪ `ממתין-לביצוע·DB·מ3` **קיבוע `numeric(12,2)` לעמודות-כסף:** עמודות-הכסף היום `numeric` בלי precision (שום עיגול לא נאכף באחסון) — קיבוע `numeric(12,2)` במיגרציות-התמחור של מודול 3, יחד עם ה-CHECKs של §7.26; אוכף באחסון את הכרעת-האגורות של §7.25. **נולד-כ-↳ על §7.25 (08/07/2026, אודיט-ערעור, "רוכב-ביצוע"); קודם לפריט מלא 09/07/2026.** סטטוס: **ממתין לביצוע** (מיגרציות-התמחור של מודול 3).

75. 🔵 `להנהון·אוטומציה·מ3` **‏job ניקוי `login_attempts`:** עם התקנת pg_cron (§7.42) — גם job ניקוי שורות עבשות ב-`login_attempts` (נעילות שפגו מזמן נשארות היום לנצח; ניקוי > ‏30 יום מ-`last_attempt_at`). **נולד-כ-↳ על §7.42 (08/07/2026, אודיט-ערעור, "רוכב-תחזוקה"); קודם לפריט מלא 09/07/2026.** סטטוס: **פתוח — להנהון** (מודול 3, עם התקנת ה-pg_cron; קשור מודול 10).

76. 🟡 `פתוח·DB·מ3` **‏snapshot-זהות בהמרת הצעה→פרויקט:** ‏RPC-ההמרה (§7.49) יעתיק גם **snapshot-זהות** (לפחות שם-האירוע) אל `projects` — היום `event_name` והלקוח חיים רק על ההצעה וכל מסך-פרויקטים נזקק ל-join משולש; ההעתקה באפס סיכון-drift (ההצעה קפואה מרגע האישור) ומשלימה את §7.62 (‏quote_id ‏NOT NULL) בסגירת מחלקת "פרויקט-בלי-שם". **נולד-כ-↳ על §7.49 (08/07/2026, אודיט-ערעור); קודם לפריט מלא 09/07/2026.** סטטוס: **פתוח** (מודול 3; מוכרע יחד עם §7.49).

77. 🟡 `פתוח·DB+הרשאות·מ6/8` **נעילת כרטיס-פרויקט אחרי סגירה/ארכוב (נעילה עמודתית):** מנגנון-הנעילה של §7.50 נדרש גם ל**כרטיס-פרויקט אחרי סגירה תפעולית** ("נועלת את כרטיס הפרויקט", ‏C5:648) ולארכיון ("נועלת את התיק באופן מוחלט", ‏C5:836) — אבל שם הנעילה *עמודתית*: השדות התפעוליים ננעלים בעוד שדות-הכספים (`invoice_sent`/`payment_date`/`feedback_*`) חייבים להישאר פתוחים למנהלת-הכספים — מתחבר ישירות ל-§7.63 (בעלות-עמודות). **נולד-כ-↳ על §7.50 (08/07/2026, אודיט-ערעור); קודם לפריט מלא 09/07/2026.** סטטוס: **פתוח** (מודולים 6/8).

78. 🟡 `פתוח·לוגיקה+DB·מ3/6/8` **כלל-הקפאה אחד (הכרעת-מסגרת למחיר·עלות·מע"מ·רווח):** את §7.51 (מע"מ) + §7.28 (עלות-רכש) + §7.52 (רווח) מכריעים **יחד ככלל-הקפאה אחד** — מה קופא, באיזה רגע, ומה נשאר נגזר; המצב הנוכחי (מחיר קפוא, עלות תיקפא, מע"מ חי, רווח לא-קיים) הוא חצי-קפוא — הגרוע משני העולמות. **נולד-כ-↳ על §7.52 (08/07/2026, אודיט-ערעור); קודם לפריט מלא 09/07/2026.** סטטוס: **פתוח** (חלק-המע"מ במודול 3; השאר במודולים 6/8).

> **פריטים 79–80 נוספו 10/07/2026 באודיט-נאמנות-לאפיון של מודול 2** (מעבר-רוחב מול C5 §5.5.3/5.6.3 + C6 + 6 המוקאפים, בעקבות מועצת-llm-council + 2 מבקרים אדוורסריים; אושר בתוכנית-הסשן 10/07 — הכרעת ישי ④א): מדדים/פרטים שמדריך-המיקרו של מודול 2 השמיט או שמקורם מוקאפ-שותק, שנרשמים כפתוחים כדי שלא ילכו לאיבוד. אטומיות ההעלאה↔DB **לא** נפתחה כפריט חדש — היא כבר **§7.36** (עוגנה ב-Decisions Ledger של module-2).

79. 🟡 `פתוח·לוגיקה·מ7` **הגדרת "רווח גולמי מצטבר" פר-לקוח (כרטיס-הלקוח):** ‏C6 §2.4.1 מונה אותו כתכונה-נגזרת של לקוח, ו-C5 §5.6.3 מציג אותו במסך — אך אין הגדרת אוכלוסייה/נוסחה: סכימת-רווח על אילו פרויקטים (רק שהסתיימו? כולל פתוחים? מתוכנן מול בפועל?) — אח פר-לקוח של §7.59 (KPI-רווח חודשי). מודול 2 מציג placeholder ("אין נתונים עדיין"); הנוסחה נכתבת במודול 7 (רווחיות), יחד עם §7.78 (כלל-ההקפאה מחיר·עלות·רווח). **נולד באודיט-נאמנות 10/07/2026 — מסך C5 §5.6.3 מציג את המדד, אך מדריך-המיקרו השמיטו לגמרי (בלי placeholder ובלי 🚧).** סטטוס: **פתוח** (מודול 7; placeholder במודול 2).
80. 🟡 `פתוח·מסך+לוגיקה·מ8` **מיפוי ציון-שביעות → תגית-מלל:** מוקאפ 01 מציג תג-מלל ("מצוין") לצד כוכבי-השביעות, אך שום מקור-אפיון לא מגדיר את ספי-ההמרה (מתי "מצוין"/"טוב"/"בינוני"). תלוי ב-§7.37 (שקלול ציון-המשוב). מודול 2 מציג כוכבים inert + "אין נתונים עדיין" ו**אינו** בונה את התג (‏confirm-intent — פרט מוקאפ ללא מקור); הספים נקבעים עם נתוני-המשוב במודול 8. **נולד באודיט-נאמנות 10/07/2026.** סטטוס: **פתוח** (מודול 8; קשור §7.37).

---
*נבנה מקריאה ישירה של `docs/schema.sql`, האפיון המאושר (פרקים 5–6, כולל אימות ציטוטים מול הטקסט המקורי), ו-44 קבצי המוקאפ ב-`docs/mockups/` (כרפרנס ויזואלי בלבד).*

</div>
