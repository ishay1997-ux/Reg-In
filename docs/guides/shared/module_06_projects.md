<div dir="rtl">

# 🔗 שלב — מודול 6: פרויקטים (המחבר) 🤝 משותף

> מקומו בהיררכיית האמת: `schema.sql` › אפיון › מוקאפים › מדריך זה. בסתירה — המדריך מפסיד.
> צעדים שבוצעו מסומנים ✅ — המדריך כתוב כאילו כלום לא קיים.
> **מודול משותף** — ישי ועמית בונים אותו יחד. זו נקודת האינטגרציה הגדולה באמצע הפרויקט.

## ① מה נשיג בשלב הזה

הישות המרכזית שמחברת הצעות מחיר, שיבוץ דיילות ולוגיסטיקה: מכונת מצבים ל-8 סטטוסים (`טרם החל → בתהליך → מוכן לביצוע → אירוע הסתיים → ממתין לחשבונית → ממתין לתשלום → הסתיים / בוטל`), חישוב אחוזי שיבוץ/לוגיסטיקה, וטיפול בשינויי-תכולה. מסך 5.6-5.7 באפיון.
> 🔗 **מראת §7.12+43+44+16+31** (שתי ההערות הבאות) — SSOT: ‏PROJECT_MASTER §7 (לא לערוך כאן ידנית; מסונכרן ע"י `regin-docs-sync`)
> ℹ️ **דרישת-אימות (§7.12, 07/07/2026):** יצירת פרויקט מהצעה מאושרת **קוראת את השורות הקפואות** דרך `projects.quote_id`→`quote_services` (מחיר+עלות מוקפאים, §7.28) — **לא מחשבת מחדש**. נקודת-בדיקה מפורשת בבלופרינט.
> ℹ️ **מכונת-הסטטוס הוכרעה (07/07/2026, ישי — §7.43/44):** `project_status` נכתב ב-**טריגר DB אטומי** על שינויי `assignments`/`logistics` (לא חישוב-בקריאה); `ready` = מצב **נגזר והפיך** (איוש 100% **וגם** לוגיסטיקה 100%) — דיילת שבוטלה + דריסה ידנית של המנהלת → הטריגר מחזיר אוטומטית ל-`in_progress`. שינויים אפשריים עד ~36ש' לפני האירוע. **קסקדת-ביטול (§7.16↔§7.31):** צד השכר/שחרור נסגר (§7.16/§7.33); כאן במודול 6 — סגירת לוגיסטיקה + מיילי-ביטול לדיילות.

## ② תנאי כניסה

- [ ] מודולים 3 (הצעות, עמית), 4 (דיילות, ישי), 5 (לוגיסטיקה, ישי) מוזגו ל-`dev`.
- 🛑 **שאלות פתוחות שחייבות הכרעה** (מ-[PROJECT_MASTER §7](../../PROJECT_MASTER.md)) — **הכרעת §7 = ישי בלבד** (כלל 1), בהתייעצות עם עמית כמודול-משותף; נרשמת בשמו (כלל 13):
  - **§7.17** — סמנטיקת "שינוי תכולה": מחירי-יחידה בדיעבד או רק כמויות? איך מחושב הרווח מחדש? **↳ הורחב (07/07 ערב):** גם מודל-הדאטה — ההצעה נעולה ו-`logistics` בלי עמודות-מחיר, אז ל"חיוב על שינויים" (C5:833) אין היום בית בסכמה; ההכרעה כוללת איפה זה נשמר.
  - **§7.52 (חדש)** — רווח גולמי סופי: נשמר-בסגירה (C5) או נגזר-תמיד (C6)? ובאיזו יחידה (₪ מול %)? חוסם את לשונית-הסגירה ואת דו"ח-הרווחיות.
  - מצביעים משותפים עם מודול 3: **§7.49** (אטומיות ההמרה — הצד-היוצר כאן) · **§7.53** (אירוע-בלי-דיילות — מדד-איוש ריק).
  - **§7.7** — stepper 5 שלבים (מוקאפ) מול 8 סטטוסים (סכמה) — הסכמה קובעת (8), אבל להכריע איך זה מוצג ב-UI.
- 🗡️ **חדשות מאודיט-הערעור המבני (08/07/2026)** — (רשימה: ‏[PROJECT_MASTER §7](../../PROJECT_MASTER.md); המפה: ‏`docs/db_roadmap.md`): ‏**§7.62** ‏NOT NULL ל-`projects.quote_id`+`owner_email` (להנהון — סוגר את מחלקת "פרויקט-בלי-שם") · ‏**§7.63** בעלות-עמודות: שדות-הכספים על `projects` נכתבים ע"י מודול 8 בלי edit על מודול פרויקטים — ההכרעה מעצבת את ה-policies שנכתבות כאן · רוכבים (קודמו לפריטים מלאים 09/07/2026): ‏**§7.76** ‏RPC-ההמרה מעתיק snapshot-זהות (שם-אירוע) · ‏**§7.77** נעילת-כרטיס אחרי סגירה/ארכיון — עמודתית (תפעולי נעול, כספי פתוח) · ‏**§7.72** כיוון-פתרון שלישי לשינוי-תכולה: ישות שורת-פרויקט אחת עם ליניאז' · ‏**§7.61** תוכנית-Storage (העלאת דוח-הסיכום החובה — כאן).

## ③ חלוקת עבודה

| ישי | עמית | Claude |
|---|---|---|
| מכריע §7.17/§7.7 (בהתייעצות עם עמית, נרשם בשמו — כלל 1); בונה לוגיקת שיבוץ/סטטוס | בונה מסך תכולה/כרטיס-פרויקט; מתאם עם ישי על ה-DB + מנתב שאלות-§7 לישי | בונה בלופרינט משותף, מיישם |

## ④ החלק שלכם

1. **ישי מכריע** ב-§7.17 ו-§7.7 (הכרעת §7 = ישי, כלל 1) — בהתייעצות עם עמית (מודול משותף); ההכרעה נרשמת בשמו (כלל 13).
2. `git checkout -b shared/module-6-projects` (ענף משותף — תאמו commits בין השניים).
3. הדבק פרומפט, אשרו בלופרינט, בנו יחד — מחלקים את הצעדים בין UI (עמית) ל-לוגיקה/DB (ישי) לפי מה שהבלופרינט יציע.

## ⑤ החלק של Claude

בלופרינט: מכונת המצבים כ-DB constraint/trigger (לא רק בקוד React), חישוב אחוזי שיבוץ (`מאושרות סופית / נדרש`) ולוגיסטיקה (`מוכן / סך פריטים`), auto-advance כששני האחוזים ב-100%, reversion ל"בתהליך" בשינוי תכולה. **מציית לשפת העיצוב ולטבלת הצבעים ב-PROJECT_MASTER §4.** **מתחזק את `docs/micro_guides/module-6.md` חי לאורך כל הבנייה** (ה-Stop hook אוכף) — חשוב במיוחד במודול משותף: המדריך הוא נקודת הסנכרון בין שני המפתחים.

## ⑥ 📋 שלושת הפרומפטים להדבקה

> 💡 **בחירת מודל לפני הדבקה:** פרומפט 1 (פתיחה) ופרומפט 3 (סגירה) — **Opus, מאמץ גבוה** (חשיבה: בלופרינט, RLS, אודיט). פרומפט 2 (המשך-בנייה) — לפי טבלת **Model & effort** שבראש סעיף 5 של מדריך המיקרו (ברירת מחדל: Sonnet לפזות בנייה, Opus לפזות DB/RLS ובקרה).

**1) פתיחת מודול (פעם אחת):**
```
אנחנו בפרויקט REG-IN. קרא את CLAUDE.md, STATUS.md, docs/guides/shared/module_06_projects.md
ואת docs/PROJECT_MASTER.md (סעיפים 5.6-5.7, §7 פריטים 7, 17).
אנחנו (ישי ועמית) פותחים את מודול 6 — פרויקטים, על ענף shared/module-6-projects.

הכרעת ישי ב-§7.17 (שינוי תכולה): ___
הכרעת ישי ב-§7.7 (תצוגת stepper): ___

קרא בעצמך את התבנית docs/templates/create_micro_guide_template.md ובצע אותה כלשונה עם:
MODULE_NUMBER=6 · MODULE_NAME=פרויקטים · RELEVANT_SECTIONS=§5.6-5.7, §7.7/17 · BRANCH_NAME=shared/module-6-projects

הצג את הבלופרינט לאישורנו; רק אחרי שנאשר — שמור אותו כ-docs/micro_guides/module-6.md.
בבלופרינט: תייג כל צעד "ישי" (לוגיקה/DB) או "עמית" (UI) — זה מודול משותף.
```

**2) המשך בנייה (בכל סשן עבודה, עד שכל הצעדים ✅):**
```
אנחנו בפרויקט REG-IN. קרא את CLAUDE.md, STATUS.md ואת docs/micro_guides/module-6.md.
אני [ישי/עמית]. המשך מ"הצעד הפעיל" שבכותרת המצב בצעדים המתויגים לי.
אמת בעצמך כל נקודת עצירה 🤖 (הרץ את פקודת האימות והצג ראיה), ועצור לאישור בכל נקודת 👤 ובסוף כל פזה.
נכנסת לפזה חדשה? אסוף קודם את כל השאלות הפתוחות/ממתינות-להנהון המעוגנות אליה בלדג'ר (סעיף 3)
והצג לנו אותן לסגירה במרוכז (סגנון P13) לפני הצעד הראשון של הפזה.
עדכן את מדריך המיקרו תוך כדי העבודה (סעיף 8 שלו) — הוא נקודת הסנכרון בינינו.
בסוף הסשן: עדכן יומנים לפי הפרוטוקול ב-CLAUDE.md והסבר לי בעברית פשוטה איפה עצרנו ומה הבא.
```

**3) סגירת מודול (פעם אחת, כשכל הצעדים ✅):**
```
אנחנו בפרויקט REG-IN. קרא את CLAUDE.md, STATUS.md ואת docs/micro_guides/module-6.md.
קרא בעצמך את התבנית docs/templates/create_module_final_test_template.md ובצע אותה כלשונה עם:
MODULE_NUMBER=6 · MODULE_NAME=פרויקטים · BRANCH_NAME=shared/module-6-projects
בסוף: בצע את סעיף ההתמדה (עדכון מדריך המיקרו + היומנים, תאריך+שעה) והדפס לנו את הוראות ה-PR
בעברית + פרומפט מסירה 🧩 לתוסף הכרום (כלל ברזל 17) לפתיחת ה-PR בפועל.
```

## ⑦ בדיקת קבלה

- [ ] הצעה מאושרת יוצרת פרויקט בסטטוס "טרם החל".
- [ ] שיבוץ מלא + לוגיסטיקה מלאה → סטטוס מתקדם אוטומטית ל"מוכן לביצוע".
- [ ] שינוי תכולה על פרויקט "מוכן" מחזיר אותו ל"בתהליך".
- [ ] שני המפתחים בדקו יחד את התסריט המלא: הצעה → פרויקט → שיבוץ+לוגיסטיקה → סטטוס.
- [ ] `npm run verify` ירוק.

## ⑧ אם משהו השתבש

- סטטוס לא מתקדם אוטומטית → בדוק שהאחוזים מחושבים מה-DB (View) ולא ממטמון בצד לקוח.
- תקוע? → [ספריית הפרומפטים המצביים](../prompt_library.md) — חילוץ, סטייה מהתוכנית, undo, קונפליקט ועוד.

</div>
