<div dir="rtl">

# ⏱️ יומן-כיול — REG-IN

> 🔴 **השיטה אינה כאן. היא בקובץ הקנוני הגלובלי: `~/.claude/references/estimation-and-agents.md`**
> — הסכם-העבודה, מתי סוכן ובאיזה מודל, איך מתדרכים, חמשת המנגנונים נגד התאמת-יתר, ושיטת המדידה.
> **הקובץ הזה מחזיק רק את מה שאינו עובר בין פרויקטים: גורמי-הסיכון של הקודבייס הזה, והרשומות.**
> **המקבילה ב-gedood-710: `docs/17_estimation_log.md`** — אותו מבנה, אוכלוסייה אחרת; **אין להעתיק שורות משם.**
>
> **נוצר:** 08/09/2026 23:5X, בהוראת-ישי (*"תיצור ל-Reg-In את המקבילה… יומן-כיול מקומי, ספציפי לפרויקט הזה בלבד"*).
> **למה כאן ולא ב-`docs/` השורש:** מסמכי-ישי בעברית חיים ב-`docs/guides/` (`docs/CLAUDE.md`, מפת-התיקיות), ו-`00_roadmap.md` הוא הממוספר היחיד שם — זה הבא בתור.
> **מי קורא:** קלוד, לפני כל הערכת-זמן/סוכנים בפרויקט הזה (הקובץ הגלובלי מפנה לכאן) · ישי, כשהוא רוצה לדעת למה הערכה החטיאה.
> **מקרא-מעמד** (מהקובץ הגלובלי): 📊 `n=K` נמדד · ⬜ השערה · ⚠️ בוטל.

**אבן-הדרך של הפרויקט הזה, ואין לשנותה בין רשומות:**
**"מרגע 'ממשיך' של ישי (או קומיט-הפתיחה של הצעד) ועד קומיט ירוק על הענף האישי + הדיווח לישי."**
*(למה לא "עד מיזוג": ה-PR והמיזוג הם פעולת-ישי בזמן שלו — מדידה עד שם מודדת אותו, לא את העבודה.)*

---

## §1 · טבלת הכיול — גורמי-סיכון של הקודבייס הזה

**זו הטבלה שנקראת לפני מתן הערכה.** נגזרת מ-§2 ומתעדכנת. 🔴 **תקרה: 8 שורות** — שורה חדשה מחליפה ישנה או מביאה ראיה חזקה יותר.

| גורם | תוספת | מעמד · העוגן |
|---|---|---|
| **נגיעה ב-`src/lib/**`** ⇒ חבילת-יחידה **מלאה**, לא תת-קבוצה (כלל 14: SSOT; 548 שורות-בדיקה כבולות ב-32 קבצים) | +5 דק׳ ריצה (CI-parity ≈ 3.5 דק׳ ל-2,399 בדיקות) **+ תיקון כל בדיקה שנשברה באותו קומיט** | 📊 `n=1` (08/09: שינוי-צורה של `getNotificationPreferences` שבר 2 בדיקות מיד) · `ui-copy-and-onboarding-mode.md` §7ד |
| **שינוי מחרוזת-ממשק** ⇒ `grep` ב-`e2e/` + `*.test.*` לפני, ותיקון-בדיקה באותו קומיט (1,716 שורות-טענה נושאות מחרוזת עברית; `smoke-anchors.json` נועץ כיתובים) | +2–8 שורות-בדיקה לכל מסך שנגעו בו; +`npm run smoke` (≈55 ש׳) שאינו ב-gate ולא ב-CI | 📊 `n=1` (08/09 שלב 0) · המדידה: `ui-copy-and-onboarding-mode.md` §7א |
| **מיגרציה על המסד החי היחיד** ⇒ הד-הקלדה (המתנה לישי) + `apply_migration` + אימות מול `pg_catalog` + `schema.sql` §-הטבלה + `db_roadmap` M-שורה + §10ב | **+25 דק׳ למיגרציה של עמודה אחת**, בלי ההמתנה לישי (08/09: 22:05 כתיבה → 22:28 אומת, ההד נפטר) | 📊 `n=1` · `supabase/migrations/CLAUDE.md` (ארבעה צעדים אחרי החלה) |
| **מסך בלי קובץ-בדיקות** (מ1: 7 מסכים, אפס `*.test.*` עד 08/09) ⇒ בדיקות-בסיס **לפני** הנגיעה | +10 דק׳ קיר · ~240K טוקנים לסוכן-Sonnet (7 בדיקות; Radix Select ב-jsdom דרש 2 פוליפילים) | 📊 `n=1` (08/09, `UsersManagementPage.test.jsx`) |
| **סקירת פלט של סוכן** — לא הבנייה, הבדיקה שלו (אחוז-התפיסה-העצמית על תוצר עצמי = 0; ההוכחה: הבריף של 07/09 עבר 4 סבבי-עין-טרייה שמצאו 7·7·4·6 חוסמים) | ~½ ממה שהסוכן חסך; **ופרומפט/תוכנית שמוסרים לסשן אחר ⇒ סוכן-עין-טרייה אחד ≈ 150K** | 📊 `n=4` סבבים על ארטיפקט אחד · `_shared/writing-prompts.md` כלל 21 |
| **נגיעה במודול סגור ומוזג** ⇒ ה-Stop hook: שורת `↳ as-built` במדריך-המיקרו **אחרי** הקוד + LOG + STATUS + (מיגרציה ⇒ `db_roadmap`) | **+20–25 דק׳ תיעוד לכל צעד** — 08/09: ~25 מתוך ~80 דק׳ של שלב 0 הלכו לתיעוד-חובה, לא לקוד | 📊 `n=1` · `.claude/hooks/check-docs-updated.sh` אכיפות 0/0ב |
| **ראיות ממסך מחובר** (צילום/מדידה) ⇒ ספק-ראיות זמני ב-`e2e/` עם יירוט-רשת, ומחיקתו **לפני** שער או **אחריו** — לא באמצע | +10 דק׳; מחיקה באמצע ריצת-gate = ריצה נופלת על ENOENT ורצה שוב (+5 דק׳) | 📊 `n=1` (08/09) · `e2e/CLAUDE.md` §"ספק-הראיות" |
| **סיבוב-סשן** (~60% מהחלון) ⇒ מסמך-מסירה + מבחן-קבלה לנכנס + הנכנס קורא את רשימת-הקריאה (בריף+תוכנית+מחקרים ≈ 5,000 שורות) לפני פעולה ראשונה | **+~40 דק׳ קיר לכל סיבוב** (כתיבה ~15 · קריאה של הנכנס ~25) | 📊 `n=1` (08/09 23:2X–23:4X, הכתיבה; קריאת סשן 1 בפתיחה: 22:0X–22:1X) · `session-handoff-guide.md` |

**מלכודות ספציפיות שכבר עלו ביוקר, ואינן "זמן" אלא "נכשל בשקט"** *(כולן מתועדות בבתיהן — כאן רק כדי שההערכה תזכור אותן)*:
- 🔴 **RLS מחזיר 0 שורות עם `error: null`** — כפתור-שמירה שמציג "נשמר" ולא שינה כלום. `npm run gate` לא תופס. ⇒ `assertRowsAffected` על כל כתיבה (`src/CLAUDE.md`).
- 🔴 **`npm run gate` עיוור לקובץ-בדיקות בלי `vi.mock('@/supabaseClient')`** — ירוק מקומית (יש `.env.local`), נופל ב-CI. ⇒ השחזור: `VITE_SUPABASE_URL= VITE_SUPABASE_ANON_KEY= npx vitest run` (`CLAUDE.md` השורש).
- 🔴 **קוד-יציאה של משימת-רקע ≠ קוד-היציאה של השער** — `cmd > log; echo exit=$? >> log` מדווח את המעטפת; ‏08/09: "gate exit 0" בהודעה, `gate exit=1` בלוג. ⇒ קוראים את שורת-`exit=` **בתוך** הלוג.
- 🔴 **כתיבת קובץ מפייתון על Windows בלי `newline='\n'`** ⇒ CRLF ⇒ `format:check` נופל בשקט על 11 קבצים (27/08). ⇒ ספירת-CR חייבת 0 לפני קומיט.
- 🔴 **מספר שחי בפרמטר/מתג נכתב כקבוע** — "62% מהדירוג" נכתב פעמיים (מחקר: 40%; מדריך: 62%), ושניהם שגויים באותו אופן: `משקולת_היענות` ב-`params`, ותלוי במתג-האמינות (`smartMatch.js`). ⇒ `ui-copy-styleguide.md` דוגמה 3.
- 🔴 **`check:docs-structure` יכול להפיל את `gate` על קובץ שאינך נוגע בו** (08/09: `cards-finance.md` של אפיון-מ11) — השלב אינו ב-CI; מבחינים בין "השער שלי אדום" ל"ממצא של סשן אחר" לפני שמתקנים.

---

## §2 · רשומות — חדש לישן

**נפתחת עם ההערכה, נסגרת עם התוצאה. רק פערים מעל 30% נרשמים. כל רשומה מתויגת `REG-IN`.**
**שני מספרים בכל רשומה:** זמן-עבודה של קלוד (בר-השוואה) · זמן עד שזה אצל ישי (מה שהוא מרגיש).
**לכל סוכן:** `שומש כפי שהוא` / `נדרש תיקון` / `נזרק ובוצע מחדש`.

### #9 · לילה-הטקסטים, שלב 10 — עין טרייה 2+2, מיזוג-הוורדיקט, תיקוני-בסיס מאומתים, 5 מפתחות חסרים, קובץ-הבוקר — ✅ נסגרה
- **תוכנית / בריף:** התוכנית §4 שלב 10 · הבריף §שלב 10 · `stage10-evaluator-shared.md` + `stage10-off/on.md` (סקראצ'פד S4)
- **היקף:** 4 מעריכי-Opus על 55 צילומים (2 מתו על מכסת-8:00 אחרי שכתבו, 2 שוגרו מחדש 11:5X) · כותב-Opus נוסף (W14) ל-5 מפתחות שהמעריכים מצאו חסרים + 3 תיקוני-נוסח · תיקוני-בסיס שאומתו בקוד (טבלת-הפרויקטים · סטטוס-משתמשת · מקרא-ההרשאות · עשרוניות בשכר-המינימום) · השתלה של 5 `<Hint>` · חבילה מלאה + עשן + E2E ממוקד + gate · טבלת-הוורדיקט + §0 של קובץ-הבוקר · תיעוד
- **הערכה שנרשמה מראש (09/09/2026 12:0X, נאמרה לישי כצפי-סיום 13:15–13:30):** ⏱️ **70–85 דק' קיר** · סוכנים ~15–25 (מדוד: כותב-Opus 30 · מעריך-Opus 15–28) · השתלה+תיקונים ~10 · שערים ~25 במקביל לתיעוד ~20 · טוקנים: W14 ~350K · 2 מעריכים ~2×230K ≈ 0.8M
- **⚠️ חיזוי בר-הפרכה שנרשם מראש:** ‏(א) שני המעריכים החדשים יאשרו ≥2 מהממצאים של עמיתיהם (projects-off בזכר · הגדרת-חסר במסך-הדיילות) — 'ראו שניים ⇒ ✅' · (ב) לפחות ממצא אחד של OFF יתברר כטקסט **מאושר-באפיון** (§⑧-3 של מ5) ⇒ הכרעת-ישי ולא תיקון · (ג) הסיכון היחיד לחריגה: מכסת-Opus — נפילה שנייה ⇒ שלב 10 נסגר על 2+2 הקיימים ובלי המפתחות החדשים
- **זמן-עבודה בפועל:** ⏱️ **~50 דק'** (12:0X → 12:5X) — סוכנים ~25 במקביל · תיקוני-בסיס + השתלות ~35 (יותר מהצפוי: 7 מפתחות ולא 5, ו-4 תיקוני-בסיס שהמעריכים מצאו) · שערים ~25 · תיעוד ~15
- **זמן עד שזה אצל ישי:** ער — הוורדיקט בראש קובץ-הבוקר + הדיווח בצ'אט
- **הפער והסיבה האחת הגדולה:** מהיר ב-20–35 דק' מהטווח (70–85 ⇒ ~50): התיעוד והוורדיקט נכתבו בזמן שהסוכנים רצו, והשערים רצו סדרתית בלי כשלי-עומס. החיזויים: (א) **אומת** — OFF-1 אישר את projects-off (⚠️) ו-ON-2 את הגדרת-החסר במסך-הדיילות (❌) · (ב) **אומת** — פסקת-החלון (§⑧-3) ותוויות-טופס-הדיילת (⑥) הן טקסט מאושר ⇒ הכרעת-ישי · (ג) **לא התממש** — המכסה החזיקה
- **מה היה הופך את ההערכה לנכונה:** שורת-§1 מועמדת: **עין-טרייה מוצלחת מייצרת עבודה** — 4 מעריכים ⇒ ~10 תיקוני-בסיס + 7 מפתחות ⇒ +30 דק' שלא הופיעו בשום תוכנית-שלב-10 (התוכנית מנתה "מיזוג" בלבד)
- **פלט הסוכנים:** E-OFF-1 (231K) · E-OFF-2 (250K, לפני הנפילה) · E-ON-1 (205K) · E-ON-2 (206K) — כולם שומשו כפי שהם · W14/Opus (352K) — שומש כפי שהוא + תיקון-R7 אחד של S4 ("על-ידינו" ⇒ "שהמשרד שחרר"); שני מעריכים שמתו ב-07:0X בלי לכתוב — נזרקו (~150K משוערים). **סה"כ שלב 10 ≈ 1.4M** (הערכה 0.8M ✗ — הנפילה ושני המפתחות הנוספים)

### #8 · לילה-הטקסטים, שלב 9 — מלל-ההטמעה (רמה 2) + השתלת `<Hint>` + מעבר-מסמך-שלם + קובץ-הבוקר — ✅ נסגרה
- **תוכנית / בריף:** `ui-copy-and-onboarding-mode.md` §4 שלב 9 · הבריף ⑥ (שלב 9: "ורק עכשיו מלל-ההטמעה") · המסירה §4 · `stage9-writer.md` (סקראצ'פד של S4)
- **היקף:** 34 מועמדים (C-01…C-34) ⇒ הכרעה לכל אחד (בסיס / רמה 2 / נעלם) — צפי ~22 מפתחות ב-`onboardingCopy.js` (כולל 4 נוסחים שכבר הוכרעו במדריך §4/§7) · השתלת `<Hint>` ב-~14 מסכים ב-8 מודולים (כל `useAuth` בבדיקות-יחידה חייב mock/provider — 0 קבצי-בדיקה מדמים היום את `Hint`) · מעבר-מסמך-שלם על ~19 מסכים בשני המצבים (צילומים דרך ספקי-הראיות של S3) · בחירת מסך-הדגל · יצירת `ui-copy-night-report.md` (סעיף-הוורדיקט ריק לשלב 10) · שערים: `gate` · `smoke` · `test:e2e` מלא · CI-parity · אפס מיגרציה · **3 סוכנים:** כותב-שכבה אחד (Opus — קול אחד ל-22 משפטים) + 2 משתילי-Sonnet בחלוקת-קבצים בלעדית; הסשן מכריע על המועמדים, ממזג ומריץ
- **הערכה שנרשמה מראש (09/09/2026 05:4X):** ⏱️ **95–120 דק' קיר** · כותב-Opus ~20 · הכרעות-הסשן על 34 מועמדים ~15 · משתילים ~20 (במקביל) · שערים ~30 (E2E מלא ≈ 10 · CI-parity 3.5 · gate 6 · smoke 1.5 · צילומי 19 מסכים ×2 ≈ 6) · מעבר-מסמך + קובץ-הבוקר ~20 · תיעוד-חובה ~15 (§1 #6: **8 מדריכי-מיקרו** ⇒ 8 שורות `as-built`) · טוקנים: Opus ~400K + 2×300K ≈ 1.0M
- **⚠️ חיזוי בר-הפרכה שנרשם מראש:** ‏(א) לפחות 3 קובצי-בדיקה של מסכים יישברו על `useAuth` מחוץ ל-provider אחרי השתלת `<Hint>` — ויתוקנו ב-`vi.mock('@/components/Hint')` · (ב) מתוך 34 המועמדים: ≥8 יוכרעו "נעלם" (R27/R28) ו-≤24 יקבלו מפתח · (ג) `smoke` ו-`prices.spec.js:218` יישארו כפי שהם (ירוק · אדום קדם-ללילה) · (ד) 0 משפטי-שכבה עם מספר-פרמטר קבוע (הכלל של דוגמה 3) — ייבדק ב-grep על ספרות בקובץ-הקופי
- **זמן-עבודה בפועל:** ⏱️ **~80 דק'** (05:4X → 07:0X; הקומיט ב-12:0X אחרי נפילת-מכסה של 5 שעות — לא נספר) — כותב-Opus ~30 (הערכה 20) · הכרעות + קובץ-הקופי ~10 · משתילים ~15 במקביל · שערים + צילומים ~35 · תיעוד ~15
- **זמן עד שזה אצל ישי:** ער — הדיווח + קובץ-הבוקר בקומיט
- **הפער והסיבה האחת הגדולה:** מהיר ב-15–40 דק' מהטווח (95–120 ⇒ ~80): הכותב לקח 30 ולא 20, אבל השערים רצו במקביל למעריכים ולתיעוד, והתיעוד הוכן מראש כתבניות בזמן ההמתנה. החיזויים: (א) **אומת בחלקו** — 4 קובצי-בדיקה נשברו (חזיתי ≥3), אבל **לא על `useAuth` מחוץ ל-provider** (הוקשח מראש) אלא על **קריסת-ייבוא** `supabaseUrl is required` דרך `AuthContext` — תוקן ב-`vi.mock('@/contexts/AuthContext')`, לא ב-mock ל-Hint · (ב) **אומת** — 7 נעלמו (חזיתי ≥8: קרוב), 24 מפתחות (+3 תוספות מהכותב) ≤ 24 ✓ · (ג) **אומת** — עשן ירוק, `prices.spec.js:218` אדום קדם-ללילה · (ד) **אומת** — 0 ספרות ב-27 הנוסחים (grep)
- **מה היה הופך את ההערכה לנכונה:** שורת-§1 מועמדת: **ייבוא רכיב-משותף חדש לתוך מסך = בדיקת-המסך יורשת את שרשרת-הייבוא שלו** — `Hint` ⇒ `AuthContext` ⇒ `supabaseClient` ⇒ קריסה בסביבת-CI-parity בכל קובץ-בדיקה שלא דימה את `AuthContext`; לחזות לפי `grep -L "vi.mock('@/contexts/AuthContext'"` על בדיקות-המסכים המושתלים, לא לפי "useAuth זורק"
- **פלט הסוכנים:** W13/Opus (390K) — שומש כפי שהוא (27 נוסחים, 0 שינויי-מילה; 3 תוספות אומצו) · P1/Sonnet (216K) — שומש כפי שהוא + 4 mock-תיקוני-בדיקה שדיווח · P2/Sonnet (266K) — שומש כפי שהוא + mock אחד ל-`supabaseClient` ב-`LogisticsPage.test.jsx` שדיווח + שער נוסף על `smartMatch.angles` (רשימה לא-ריקה) שאומץ. **סה"כ ~872K** (הערכה 1.0M)

### #7 · לילה-הטקסטים, שלב 8 — מ5 לוגיסטיקה + מ9 הגדרות + מ1 כניסה/משתמשים/קטלוג, איפוס מלא — ✅ נסגרה
- **תוכנית / בריף:** `ui-copy-and-onboarding-mode.md` §4 שלב 8 · המסירה §4 · `stage8-shared.md` + `w12a–d.md` (סקראצ'פד של סשן 3)
- **היקף:** 22 קובצי-מודול + 5 קובצי-`lib` (~650 שורות-קוד עם עברית; `paramsRegistry.js` לבדו 181 — 44 hints) · ~420 שורות-אסרציה כבולות (`LogisticsPage.test` 56 · `UsersManagementPage.test` 52 · `ChecklistDialog.test` 37) · 7 E2E (`logistics` · `settings` · `auth` · `permissions` · `prices` (נופל קדם-ללילה) · `load-failure-guards` · `server-messages`) · **5 מחרוזות קשורות-`raise`** (כולן במ5) · **מ1 בלי בדיקות-יחידה** מלבד `UsersManagementPage` ⇒ מעבר ידני על 4 מסכים · `sortLine` של העשן במ5 · אפס מיגרציה · **4 כותבי-Sonnet במקביל** (מ5 · מ1 · מ9 מסכים · מרשם-הפרמטרים), הסשן ממזג
- **הערכה שנרשמה מראש (09/09/2026 04:1X):** ⏱️ **55–75 דק' קיר** · כותבים ~25 (המרשם והצ'קליסט הכבדים) · מיזוג + שערים ברקע ~15 · **מעבר ידני על מ1 בדפדפן ~10** (§1: מסך בלי בדיקות) · תיעוד ~10 (שלושה מדריכים) · טוקנים: ~4×360K ≈ 1.45M
- **⚠️ חיזוי בר-הפרכה שנרשם מראש:** ‏(א) המרשם (W12d) יפיק ≥15 מועמדים-לשכבה — יותר מכל שלב אחר · (ב) `LogisticsPage.test.jsx` (56 שורות) ידרוש ≥4 תיקוני-אסרציה על מקרא-הענבר ומצבי-הריק · (ג) `smoke` יישאר ירוק (ה-`sortLine` נקוב כ-do-not-touch) ו-`prices.spec.js:218` יישאר אדום כפי שהיה (קדם-ללילה) — ולא יתוקן
- **זמן-עבודה בפועל:** ⏱️ **~80 דק'** — הכותבים 10–33 דק' במקביל (04:1X→04:5X, סשן 3) · **העברת-מקל באמצע השלב** (סשן 3 נמדד ב-800K: כתיבת המסירה + הקבלה של S4 ≈ 35 דק') · מיזוג + שערים + E2E ≈ 35 דק' · מעבר ידני ≈ 10 · תיעוד ≈ 15
- **זמן עד שזה אצל ישי:** ער מ-05:2X — ראה את הכרעת כפתור-הכניסה לפני הקומיט; הדיווח המלא + צילומים בקומיט
- **הפער והסיבה האחת הגדולה:** **+5–25 דק' מעל הטווח (55–75 ⇒ ~80)** — הסיבה: **סיבוב-סשן באמצע השלב** (§1 #8: ≈ +40 דק'), שלא היה בהערכה כי ההערכה נכתבה כשהסשן חשב שהוא מתחת לסף. החיזויים: (א) **הופרך** — המרשם הפיק 2 מועמדים (C-33 · C-34), לא ≥15: רוב ההינטים הם ③ שנשאר בבסיס בשורה אחת (הבריף המשותף קבע זאת מראש) · (ב) **הופרך-חלקית** — `LogisticsPage.test.jsx` דרש בלוק אחד (3 בדיקות של מקרא-הענבר), לא ≥4 נפרדים · (ג) **אומת** — `smoke` ירוק, `prices.spec.js:218` אדום כפי שהיה. **ולא נחזה:** כותב שינה תווית שהיא חוזה-הכניסה של 24 קובצי-E2E ודיווח "0 פגיעות" — נתפס ע"י הממזג (S3), לא ע"י הכותב
- **מה היה הופך את ההערכה לנכונה:** שורת-§1 מועמדת: *לוקטור-הכניסה של E2E הוא חוזה-מחרוזת שאף כותב לא רואה בקבצים שלו* — כל שינוי-תווית של כפתור-ראשי מחייב grep ב-`e2e/` **ע"י הממזג**, לא רק ע"י הכותב (הכותב מדד רק את קבציו) · ו-§1 #8 (סיבוב) חל גם כשההערכה נכתבת — להוסיף +40 לכל שלב שמתחיל מעל ~650K
- **פלט הסוכנים:** W12a (362K) — שומש כפי שהוא + תיקון-E2E שדיווח · W12b (367K) — **נדרש תיקון** (כפתור-הכניסה הוחזר ל-`התחברות` — שאלת-מדריך; S-05 התאום) · W12c (334K) — שומש כפי שהוא · W12d (274K) — שומש כפי שהוא. **סה"כ ~1.34M** (הערכה 1.45M ✓)

### #6 · לילה-הטקסטים, שלב 7 — מ2 לקוחות + מ3 הצעות מחיר, איפוס מלא — ✅ נסגרה
- **תוכנית / בריף:** `ui-copy-and-onboarding-mode.md` §4 שלב 7 · המסירה §4 · `stage7-shared.md` + `w11a–d.md` (סקראצ'פד של סשן 3)
- **היקף:** 17 קובצי-מודול + 8 קובצי-`lib` (~1,000 שורות-קוד עם עברית; `CustomerDetailsPage` 133 · `CustomersPage` 115 · `QuotesPage` 105) · ~300 שורות-אסרציה כבולות (`quotes.test` 92 · `customers.test` 43) · 6 E2E (`customers` · `customer-page` (נופל קדם-ללילה) · `quotes` · `quote-approval` · `quote-document` · `quote-email`) · **5 מחרוזות קשורות-`raise`** (נמדד בסקריפט המדויק) · **שני קהלים** — חמש המשתמשות ולקוח בקצה (PDF · תבניות-מייל ⇒ ניטרלי-מגדר) · אפס מיגרציה (C-01 נשאר) · **4 כותבי-Sonnet במקביל** (מ2 מסכים+lib · טופס-לקוח+api+בונה-ההצעה · רשימת-הצעות+דיאלוגים+lib/quotes · תוצרי-לקוח: PDF+מייל), הסשן ממזג
- **הערכה שנרשמה מראש (09/09/2026 03:4X):** ⏱️ **50–70 דק' קיר** · כותבים ~25 (lib/quotes ו-CustomerDetailsPage הכבדים) · מיזוג + שערים במקביל ברקע ~15 (§1 #5: מקבילי ⇒ 15 ולא 25; E2E ≈ 6 קבצים, ~8 דק') · תיעוד ~10 (שני מדריכים) · טוקנים: ~4×370K ≈ 1.5M (הכיול מ-#5: ~370K לכותב)
- **⚠️ חיזוי בר-הפרכה שנרשם מראש:** ‏(א) לפחות כותב אחד ינסה לשכתב אחת מחמש המחרוזות קשורות-ה-`raise` למרות שהן נקובות בשמן — ייתפס בגרפ-המיזוג מול המיגרציות · (ב) `lib/quotes.test.js` (92 שורות) ידרוש ≥5 תיקוני-אסרציה · (ג) `quote-email.spec.js` יישאר ירוק (ה-`dialog.accept` הגורף) ו-`customer-page.spec.js:130` יישאר אדום כפי שהיה — ולא יתוקן
- **זמן-עבודה בפועל:** ⏱️ **~40 דק' קיר** (שיגור 03:45 → קומיט ~04:1X; הכותבים 13–20 דק' במקביל · מיזוג + שערים ברקע ~20 (ה-E2E של 8 קבצים הוא הארוך) · תיעוד ~10)
- **זמן עד שזה אצל ישי:** ישן — דיווח + צילומים בקומיט
- **הפער והסיבה האחת הגדולה:** בטווח (50–70 ⇒ ~40, מהיר ב-20%): שערים במקביל, וה-E2E (8 קבצים, 5.3 דק') היה הפריט הארוך. החיזויים: (א) **הופרך** — אף כותב לא נגע בחמש המחרוזות קשורות-ה-`raise` (רשימה נקובה-בשם עובדת, כמו ב-#3) · (ב) **הופרך** — `quotes.test.js` 0 תיקונים (הכותבים בחרו נוסחים שהאסרציות `toContain` סובלות) · (ג) E2E: 60 עברו · 2 נפלו · 0 מדולגות
- **מה היה הופך את ההערכה לנכונה:** שורת-§1 מועמדת: *רשימת do-not-touch נקובה-בשם ⇒ 0 פגיעות* (שלוש פעמים ברצף: #3 · #5 · #6) — להפסיק לחזות אותה כסיכון; ו*תיאום-בין-כותבים*: שינוי-תווית של כותב אחד שמצוטט במשפט של כותב אחר (`שלחי במייל`) — נתפס רק במיזוג ⇒ +5 דק' grep-רוחבי על כל תווית-כפתור ששונתה
- **פלט הסוכנים:** W11a (333K) — שומש כפי שהוא · W11b (372K) — שומש כפי שהוא · W11c (369K) — נדרש תיקון קטן (משפט שציטט תווית ישנה של W11d) · W11d (333K) — שומש כפי שהוא. **סה"כ ~1.41M** (הערכה 1.5M ✓)

### #5 · לילה-הטקסטים, שלב 6 — מ4 דיילות + Smart Match, איפוס מלא — ✅ נסגרה
- **תוכנית / בריף:** `ui-copy-and-onboarding-mode.md` §4 שלב 6 · המסירה §4 · `stage6-shared.md` + `w10a–c.md` (סקראצ'פד של סשן 3)
- **היקף:** 10 קובצי-מודול + 6 קובצי-`lib` (~551 שורות-קוד עם עברית; `SmartMatchPage` לבדו 136) · 4 מחרוזות-④ · ~250 שורות-אסרציה כבולות (`smartMatch.test` 57 · `shiftEmails.test` 53 · `hostesses.test` 38 · `api.test` 30) · 3 E2E (`hostesses` 20 · `smart-match` 12 · `public-confirm` 6) · מחרוזת קשורה-`raise` אחת · דף ציבורי לדיילת (נקבה) · אפס מיגרציה · **3 כותבי-Sonnet במקביל** (מבט-על+מאגר+api+lib/hostesses · כרטיס+טופס+lib/shift* · Smart Match+lib/smartMatch*+sortAngles), הסשן (Fable) ממזג
- **הערכה שנרשמה מראש (09/09/2026 01:4X):** ⏱️ **60–80 דק' קיר** · כותבים ~25 (Smart Match הוא הכבד: מצב-ריק עם חמישה חוקים, פסקת-הסבבים, באנר-האמינות) · מיזוג + CI-parity + `smoke` על שרת משלי + 3 E2E ~25 (§1: `lib` ⇒ חבילה מלאה; E2E ≈ 38 בדיקות) · תיעוד ~10 (מדריך אחד) · טוקנים: ~3×350K ≈ 1.05M (הכיול מ-#4)
- **⚠️ חיזוי בר-הפרכה שנרשם מראש:** ‏(א) `SmartMatchPage` יוריד ≥6 משפטים למועמדים, ולפחות אחד מהם ייתפס במיזוג כ-⑥ (מייל שיוצא לדיילת) שחייב לחזור לבסיס · (ב) `smartMatch.test.js` (57 שורות) ידרוש ≤3 תיקוני-אסרציה — רוב מחרוזות ה-`lib` הן שגיאות-פרמטר, לא קופי-מסך · (ג) שלושת ה-E2E יישארו ירוקים בלי עריכה (מאתרים לפי `data-testid` ותוויות שכבר תוקנו בשלב 2)
- **זמן-עבודה בפועל:** ⏱️ **~40 דק' קיר** (שיגור 03:00 → קומיט ~03:4X; הכותבים 22–25 דק' במקביל · מיזוג + CI-parity + build + עשן-על-שרת-משלי + E2E ~15 (הכול ברקע ובמקביל) · תיעוד ~10). *(ההכנות — בריפים · before-tree · רשומה זו — נעשו 01:4X–01:5X לפני הפסקת-ישי ואינן נספרות כאן.)*
- **זמן עד שזה אצל ישי:** ישן — דיווח + צילומים בקומיט
- **הפער והסיבה האחת הגדולה:** מהיר מהטווח (60–80 ⇒ ~40): **הרצת השערים במקביל ברקע** (חבילה מלאה + build + עשן + צילומים בו-זמנית) במקום בטור, ומודול שכבר עבר לשון-נקבה בשלב 2 ⇒ איפוס ≈ וידוא. החיזויים: (א) **הופרך** — Smart Match הוריד 3 משפטים (לא ≥6) ואף אחד לא נדרש לחזור · (ב) **אושר** — `smartMatch.test.js` 0 תיקונים · (ג) **הופרך-חלקית** — E2E היה נשאר ירוק, אבל אסרציה שלילית אחת על הנוסח הישן נעשתה ריקה מתוכן ותוקנה ל-testid
- **מה היה הופך את ההערכה לנכונה:** שורת-§1: *שערים במקביל ברקע* ⇒ מיזוג+שערים ≈ 15 דק' ולא 25; ו*אסרציה שלילית על טקסט* — לחפש גם `not.toContainText` כשמסירים מחרוזת (היא לא נופלת, היא מתרוקנת)
- **פלט הסוכנים:** W10a (374K) — שומש כפי שהוא · W10b (374K) — נדרש תיקון קטן (מקף כפול בהינט) · W10c (359K) — נדרש תיקון (אופס⇒איפוס ×4 בהערות, אינפיניטיב במצב-הריק). **סה"כ ~1.1M** (הערכה 1.05M ✓)

### #4 · לילה-הטקסטים, שלב 5 — מ8 כספים, איפוס מלא — ✅ נסגרה
- **תוכנית / בריף:** `ui-copy-and-onboarding-mode.md` §4 שלב 5 · `stage5-shared.md` + `w9a–c.md` (סקראצ'פד)
- **היקף:** 10 קובצי-מקור (~1,900 שורות-עברית; `ClosingWindowDialog` לבדו 465) · 257 שורות-בדיקה כבולות · 2 E2E (`finance` · `public-feedback`) · מחרוזת קשורה-`raise` אחת · דף ציבורי ללקוח (מגדר-ניטרלי) · **3 כותבי-Sonnet במקביל** (מבט-על+ציבורי · חלון-הסגירה · דוח-שכר), **במקביל למיזוג שלב 4**
- **הערכה שנרשמה מראש (09/09/2026 00:5X):** ⏱️ **60–80 דק' קיר** · כותבים ~30 (חלון-הסגירה הוא הכבד: מעבר-H2 פר-משפט) · מיזוג + שערים ~20 · תיעוד ~10 · טוקנים: ~3×350K ≈ 1.05M
- **⚠️ חיזוי בר-הפרכה שנרשם מראש:** ‏(א) חלון-הסגירה יוריד 8–15 משפטים לשכבה, ולפחות אחד מהם ייתפס במיזוג כ-⑥ שחייב לחזור לבסיס · (ב) `ClosingWindowDialog.test.jsx` (76 שורות) ידרוש ≥15 תיקוני-אסרציה · (ג) `e2e/finance.spec.js` יישאר ירוק בלי עריכה (data-testid)
- **זמן-עבודה בפועל:** ⏱️ **~75 דק' קיר** (שיגור 01:2X → קומיט ~02:4X; הכותבים 14–19 דק'; המיזוג התחיל רק אחרי קומיט שלב 4 (~02:0X) — ~35 דק' מיזוג+שערים; תיעוד ~10)
- **זמן עד שזה אצל ישי:** צילומים + דיווח עם הקומיט
- **הפער והסיבה האחת הגדולה:** בטווח (60–80 ⇒ ~75) — אבל רק בגלל ההמתנה לקומיט שלב 4; זמן-הכותבים היה 19 דק' (הערכה 30). החיזויים: (א) הופרך — 4 משפטים ירדו (לא 8–15) ואף אחד לא נדרש לחזור; (ב) הופרך — בדיקת-חלון-הסגירה נדרשה ל-1 תיקון (לא 15); (ג) אושר
- **מה היה הופך את ההערכה לנכונה:** אותה שורת-§1 כמו #3: מודול עם טבלאות-⑥ ולשון-נקבה ⇒ איפוס = וידוא; ולהעריך את המיזוג לפי *מתי הוא יכול להתחיל* (תור-הקומיטים), לא רק לפי גודלו
- **פלט הסוכנים:** W9a (325K) — שומש + תיקון קטן (סיומת פנימית) · W9b (379K) — שומש כפי שהוא · W9c (366K) — שומש + יישור כפתור-הפתיחה. **סה"כ ~1.07M** (הערכה 1.05M ✓)

### #3 · לילה-הטקסטים, שלב 4 — מ6 פרויקטים, איפוס מלא — ✅ נסגרה
- **תוכנית / בריף:** `ui-copy-and-onboarding-mode.md` §4 שלב 4 · `stage4-shared.md` + `w8a–w8d.md` (סקראצ'פד) · המסירה §4
- **היקף:** 18 קובצי-מקור (~1,900 שורות-עברית; `ScopeChangeDialog` 244 · `ClosingTab` 193) · 248 שורות-בדיקה כבולות · 2 E2E (`projects` · `project-closing`) · **19 מחרוזות קשורות-`raise` שאסור לגעת בהן** · אפס מיגרציה · **4 כותבי-Sonnet במקביל** (מבט-על+כרטיס · צוות · לוגיסטיקה+תכולה · סגירה+ביטול), **במקביל לכותב של שלב 3** (קבצים זרים)
- **הערכה שנרשמה מראש (09/09/2026 00:2X):** ⏱️ **70–90 דק' קיר** · כותבים ~30 במקביל · מיזוג (grep-רוחבי · יישור בין 4 קולות · E2E) ~25 · חבילה מלאה + `smoke` + 2 E2E ~15 · תיעוד ~10 (מדריך-מ6 + סגירת `⬜ §⑧ פריט 8` של מ5 + write-back) · טוקנים: ~4×300K = 1.2M
- **⚠️ חיזוי בר-הפרכה שנרשם מראש:** ‏(א) לפחות כותב אחד יגע במחרוזת קשורה-`raise` למרות הרשימה — ייתפס במיזוג בגרפ מול המיגרציות · (ב) ארבעת הכותבים יפיקו ≥2 נוסחים שונים לאותו רעיון חוצה-לשוניות (למשל מצב-ריק של לשונית) — R30 במיזוג · (ג) `project-closing.spec.js` יידרש לעדכון של 2–4 מחרוזות
- **זמן-עבודה בפועל:** ⏱️ **~75 דק' קיר** (שיגור 00:4X → קומיט ~02:0X; הכותבים 15–32 דק' כל אחד, האחרון (תכולה) 32 · מיזוג + שערים ~30 · תיעוד ~10)
- **זמן עד שזה אצל ישי:** צילומים + דיווח עם הקומיט
- **הפער והסיבה האחת הגדולה:** בתוך הטווח (70–90 ⇒ ~75). החיזויים: (א) הופרך — אף כותב לא נגע במחרוזת קשורה-`raise` (הרשימה המפורשת בבריף עבדה); (ב) אושר — קבוע כפול (`SCOPE_BLOCKED_TITLE`) ומטאפורה (`ציר הפעיל`) הגיעו בשני נוסחים ואוחדו במיזוג; (ג) הופרך — E2E לא נדרש לעדכון כלל
- **מה היה הופך את ההערכה לנכונה:** הייתה נכונה. שורת-§1 מועמדת: *מודול שנבנה עם טבלאות-⑥ ולשון-נקבה (מ5/מ6/מ8)* ⇒ איפוס ≈ וידוא + 10–20 משפטי-עזר; להעריך לפי מספר משפטי-העזר הקבועים (מצאי §2 סיווג ②/③), לא לפי סך-המחרוזות
- **פלט הסוכנים:** W8a (381K) — שומש כפי שהוא (+תיקון-מיפוי שדווח) · W8b (347K) — נדרש תיקון (קבוע כפול, `ציר הפעיל`) · W8c (436K) — שומש כפי שהוא (כולל תיקון-עובדה) · W8d (413K) — שומש כפי שהוא. **סה"כ ~1.58M** (הערכה 1.2M — הכותבים קראו ~300 שורות-אפיון כל אחד)

### #2 · לילה-הטקסטים, שלב 3 — מ7 מסך-הבית, איפוס מלא — ✅ נסגרה
- **תוכנית / בריף:** `ui-copy-and-onboarding-mode.md` §4 שלב 3 · הבריף לכותב `w7-module7.md` (סקראצ'פד) · המסירה §4
- **היקף:** 6 קובצי-מודול + `src/lib/dashboard.js` (~35 מחרוזות-מודול + חלקו של `lib`) · בדיקות כבולות: `DashboardPage.test.jsx` (~115 שורות-עברית) · `lib/dashboard.test.js` (~170) · `e2e/dashboard.spec.js` · אפס מיגרציה · **כותב-Sonnet אחד** + הסשן ממזג
- **הערכה שנרשמה מראש (09/09/2026 00:2X):** ⏱️ **45–60 דק' קיר** · כותב ~20 · מיזוג + חבילה מלאה (נגיעה ב-`lib` ⇒ §1: +5) + `smoke` + `e2e/dashboard.spec.js` ~15 · תיעוד ~10 (מדריך אחד, לא תשעה) · טוקנים: ~300K לכותב (הכיול מ-#1)
- **⚠️ חיזוי בר-הפרכה שנרשם מראש:** ‏(א) `lib/dashboard.test.js` ידרוש ≥10 תיקוני-אסרציה (הטבלה של אריחי-ה-KPI) · (ב) `e2e/dashboard.spec.js` יישאר ירוק בלי עריכה (נועץ רק בכותרת וב-`MASKED`) · (ג) הכותב ינסה לכתוב משפט-שכבה או Hint למרות האיסור — ייתפס במיזוג
- **זמן-עבודה בפועל:** ⏱️ **~35 דק' קיר** (שיגור 00:4X → קומיט 01:2X; הכותב 22 דק'; מיזוג+שערים ~10 — במקביל לכותבי-מ6; תיעוד ~5)
- **זמן עד שזה אצל ישי:** דיווח עם הקומיט (~35 דק')
- **הפער והסיבה האחת הגדולה:** מהיר מהטווח (45–60 ⇒ ~35): המסך כבר עמד במדריך כמעט כולו (הצוות שבנה אותו 03/09 עבר מעבר-ניסוח) — הכותב **וידא** יותר משכתב; החיזוי (א) הופרך: `lib/dashboard.test.js` לא נגע כלל, 0 תיקונים
- **מה היה הופך את ההערכה לנכונה:** שורת-§1: *מודול שנבנה אחרי 03/09 עם מעבר-ניסוח* ⇒ איפוס = וידוא, ½ מהזמן; להעריך לפי תאריך-הבנייה, לא לפי מספר-המחרוזות
- **פלט הסוכנים:** W7 (312K) — **שומש כפי שהוא**, כולל הכרעה-מוצהרת אחת (שגיאות-הצורה) שדווחה במקום להיקבר

### #1 · לילה-הטקסטים, שלב 2 — רכיבים משותפים + לשון-נקבה בכל המערכת — ✅ נסגרה
- **תוכנית / בריף:** `docs/plans/ui-copy-session-brief.md` ⑥ · `ui-copy-and-onboarding-mode.md` §4 שלב 2 · המסירה `ui-copy-handoff.md` §4
- **היקף:** ~50 קובצי-מקור (186 שורות-מועמד מ-grep, ~85 פניות-זכר אמיתיות) · `src/components`+`contexts`+`api` באיפוס מלא · `src/lib` (6 קבצים) · 8 מודולים ליישור-מגדר בלבד · אפס מיגרציה · **6 סוכני-Sonnet** בחלוקת-קבצים בלעדית + הסשן (Fable) ממזג, מריץ חבילה מלאה + `smoke` + E2E נגועים, ומעדכן 8 מדריכי-מיקרו
- **הערכה שנרשמה מראש (08/09/2026 23:3X):** ⏱️ **75–100 דק' קיר** · כותבים במקביל ~25 · מיזוג + CI-parity + smoke + E2E ~25 (§1: `lib` ⇒ חבילה מלאה +5; מחרוזות נעולות בבדיקות ⇒ תיקונים) · תיעוד-חובה ~25 (§1: מודול סגור ⇒ `↳ as-built` — **×8 מדריכים** הפעם, לא אחד) · טוקנים: ~6×200K לכותבים ≈ 1.2M
- **⚠️ חיזוי בר-הפרכה שנרשם מראש:** ‏(א) לפחות 6 בדיקות-יחידה חוצות-תיקיות יישברו אחרי המיזוג (בדיקת-מודול שטוענת על מחרוזת של `lib`, למשל `TeamTab.test` על `שלח את הקישור שוב`) — הכותבים לא יראו אותן כי הן מחוץ לחלוקה שלהם · (ב) 5 קריאות `getByTitle('העבר לארכיון')` ב-E2E יישברו ויתוקנו מרכזית · (ג) `smoke` יישאר ירוק — `sortLine` לא נוגעים בו בשלב הזה
- **זמן-עבודה בפועל:** ⏱️ **~60 דק' קיר** (שיגור 23:3X → קומיט 00:3X); הכותבים ~20 במקביל · מיזוג + חבילה + עשן + E2E ~30 (ה-E2E הנגוע לבדו 7.3 דק' + ביסקציה 2×~3) · תיעוד ~10 (המדריכים נכתבו בסקריפט אחד, לא ביד)
- **זמן עד שזה אצל ישי:** צילומים 00:0X (לפני הקומיט) · דיווח 00:3X — ~65 דק' מהשיגור
- **הפער והסיבה האחת הגדולה:** בתוך הטווח (75–100 ⇒ בפועל ~60, מהיר ב-20%). הסיבה: התיעוד-החוזר (9 מדריכים) נכתב בסקריפט אחד ולא 9 עריכות-יד; ומה שכן עלה: **3 מתוך 6 שיגורי-סוכן נחסמו ע"י מסנן-ההרשאות** על פרומפט ארוך בגוף ההודעה — ~5 דק' וניסוח-מחדש לקובץ
- **מה היה הופך את ההערכה לנכונה:** שורת-§1 חדשה: *ביסקציית-E2E* (כשל שאינו מיוחס — `git stash` → build+test → `pop` ≈ +4 דק' לכל הרצה) · ו*שיגור-סוכן*: בריף ארוך ⇒ לקובץ ופרומפט בן שורה, אחרת חסימת-מסנן (+5 דק')
- **פלט הסוכנים:** W1 רכיבים משותפים (333K) — נדרש תיקון (sr-only `סגירה` התנגש בבדיקת-מ6; ניסוח-עקיפה אוחד) · W2 מ1 (256K) — נדרש תיקון (ניסוח-עקיפה ×2, `המשתמש`⇒`המשתמשת`) · W3 מ2+מ3 (425K) — נדרש תיקון (מחרוזת קשורה-RPC הוחזרה) · W4 lib (242K) — שומש כפי שהוא · W5 מ4/מ6/מ7 (324K) — נדרש תיקון (עובדה מומצאת) · W6 מ5/מ8/מ9 (325K) — שומש כפי שהוא. **סה"כ ~1.9M** (ההערכה: 1.2M — הכותבים קראו את המדריך + הקבצים שלהם; ~300K לכותב, לא 200K)

*(⚠️ מה שאינו רשומה: שלב 0 של 08/09 נמדד בדיעבד ולכן נכנס ל-§1 כעוגן `n=1`, לא לכאן — "שניהם-בסוף = הערכה מזוהמת".)*

### #N+1 · תיקון-קוהרנטיות של הזריעה (מ11) — ✅ נסגרה 10/09/2026
- **תוכנית / בריף:** `docs/specs/module_11_reports/seed-fix-plan-2026-09-09.md`
- **היקף:** ‏**אפס קוד.** ‏10 פעולות כתיבה למסד החי · 3 קובצי-תיעוד חדשים · 6 קומיטים · בלי סוכנים (הכול בסשן הראשי, Opus)
- **הערכה שנרשמה מראש:** ⚠️ **לא נרשמה — וזה עצמו הממצא.** ההערכה שנמסרה לישי הייתה *"שלב ① כחצי יום"* בלי טווח ובלי גורמי-סיכון. **בפועל: ~2.5 שעות קיר**, כלומר **הערכת-יתר של פי-2 עד פי-3**.
- **⚠️ חיזוי בר-הפרכה שנרשם מראש:** *"תיקון-זריעה יחיד סוגר את מ1 · מ2 · מ3 יחד"* — ✅ **התאמת.** ‏`UPDATE` אחד על `attendance_status` + שניים קטנים סגרו את שלושתם.
- **זמן-עבודה בפועל:** ⏱️ **~2.5 שעות קיר** מהשאילתה הראשונה ועד הקומיט האחרון, מזה **~35 דק' אבודות** על חסימת-הרשאות (סיווג auto-mode חסם כתיבה למסד; נדרשה התערבות ישי).
- **הפער והסיבה האחת הגדולה:** מהיר בהרבה מהצפוי. **הסיבה:** ‏`execute_sql` רץ בטרנזקציה ⇒ כל כישלון גולגל במלואו, ולכן **שלוש נפילות-CHECK עלו דקות במקום שעות של ניקוי מצב-ביניים.** 🚫 **הסבר מתחרה שנשלל:** *"המשימה הייתה קטנה"* — לא: 10 פעולות כתיבה על 5,674+ שורות, ושלוש מהן נפלו בניסיון הראשון.
- **מה היה הופך את ההערכה לנכונה:** שורת-§1 חדשה — **"תיקון-נתונים מול תיקון-קוד"**: פעולת-זריעה טהורה (בלי בנייה, בלי בדיקות-רגרסיה, בלי מיזוג) רצה **פי-3 מהר** מפריט-פיצ'ר באותו "גודל מוצהר". להעריך אותה בסולם נפרד.
- **⚠️ גורם-סיכון שנמדד ואינו ב-§1: חסימת-הרשאות.** ‏`auto mode` חוסם כתיבה למסד **בלי לשאול**, ובסשן לא-אינטראקטיבי אין דרך לפתוח אותה מבפנים ⇒ **עצירה מוחלטת עד שישי מתערב.** לכל משימה שנוגעת במסד — **לוודא הרשאות לפני ההערכה, לא אחריה.**
- **פלט הסוכנים:** —

### תבנית לרשומה

```
### #N · <שם המשימה> — 🟡 פתוחה / ✅ נסגרה
- **תוכנית / בריף:** <נתיב>
- **היקף:** <קבצים · מיגרציה? · מודל/סוכנים>
- **הערכה שנרשמה מראש (DD/MM/YYYY HH:MM):** ⏱️ <טווח> · סולו: <X> · אם <גורם מ-§1>: +<Y>
- **⚠️ חיזוי בר-הפרכה שנרשם מראש:** <מה ייפול / מה יעבוד, ולמה>
- **זמן-עבודה בפועל:** ⏱️ **~60 דק' קיר** (שיגור 23:3X → קומיט 00:3X); הכותבים ~20 במקביל · מיזוג + חבילה + עשן + E2E ~30 (ה-E2E הנגוע לבדו 7.3 דק' + ביסקציה 2×~3) · תיעוד ~10 (המדריכים נכתבו בסקריפט אחד, לא ביד)
- **זמן עד שזה אצל ישי:** צילומים 00:0X (לפני הקומיט) · דיווח 00:3X — ~65 דק' מהשיגור
- **הפער והסיבה האחת הגדולה:** בתוך הטווח (75–100 ⇒ בפועל ~60, מהיר ב-20%). הסיבה: התיעוד-החוזר (9 מדריכים) נכתב בסקריפט אחד ולא 9 עריכות-יד; ומה שכן עלה: **3 מתוך 6 שיגורי-סוכן נחסמו ע"י מסנן-ההרשאות** על פרומפט ארוך בגוף ההודעה — ~5 דק' וניסוח-מחדש לקובץ
- **מה היה הופך את ההערכה לנכונה:** שורת-§1 חדשה: *ביסקציית-E2E* (כשל שאינו מיוחס — `git stash` → build+test → `pop` ≈ +4 דק' לכל הרצה) · ו*שיגור-סוכן*: בריף ארוך ⇒ לקובץ ופרומפט בן שורה, אחרת חסימת-מסנן (+5 דק')
- **פלט הסוכנים:** W1 רכיבים משותפים (333K) — נדרש תיקון (sr-only `סגירה` התנגש בבדיקת-מ6; ניסוח-עקיפה אוחד) · W2 מ1 (256K) — נדרש תיקון (ניסוח-עקיפה ×2, `המשתמש`⇒`המשתמשת`) · W3 מ2+מ3 (425K) — נדרש תיקון (מחרוזת קשורה-RPC הוחזרה) · W4 lib (242K) — שומש כפי שהוא · W5 מ4/מ6/מ7 (324K) — נדרש תיקון (עובדה מומצאת) · W6 מ5/מ8/מ9 (325K) — שומש כפי שהוא. **סה"כ ~1.9M** (ההערכה: 1.2M — הכותבים קראו את המדריך + הקבצים שלהם; ~300K לכותב, לא 200K)
```

</div>
