<div dir="rtl">

# ⏰ שלב — מודול 10: אוטומציות

> מקומו בהיררכיית האמת: `schema.sql` › אפיון › מוקאפים › מדריך זה. בסתירה — המדריך מפסיד.
> צעדים שבוצעו מסומנים ✅ — המדריך כתוב כאילו כלום לא קיים.

## ① מה נשיג בשלב הזה

מיילים ותזכורות אוטומטיות: תזכורת לדיילת 24 שעות לפני אירוע, "שכחתי סיסמה" מלא, והפעלת Leaked Password Protection (חוב מודול 1).
> 🔗 **מראת §7.42+45** (שתי ההערות הבאות) — SSOT: ‏PROJECT_MASTER §7 (לא לערוך כאן ידנית; מסונכרן ע"י `regin-docs-sync`)
⚠️ **עדכון §7.42 (07/07/2026):** **מעברי-הסטטוס הטהורים** (פקיעת הצעה 30 יום, מעבר "אירוע הסתיים") אינם כאן — הם `pg_cron` **שנבנה כבר במודול 3**; מודול 10 בונה את ה-**Edge Function המתוזמנת לשליחות** (תזכורת 24ש' ומיילים אחרים) ומרחיב מעליו. ⚠️ **§7.45 (07/07/2026):** **ערוץ שליחת הזימונים לדיילות (מייל/SMS) + בחירת שירות-חינמי** יוכרע בדיון כאן/במודול 4 — מודול 4 מגדיר את ה-token, המעברים וה-endpoint הציבורי; **השליחה בפועל = תשתית מודול 10** (מקביל למעברי-הזמן).

## ② תנאי כניסה

- [ ] מודולים 3, 6, 8, 9 מוזגו (אוטומציות תלויות בהצעות, פרויקטים, כספים, פרמטרים).
- ⏳ **חוב מפורש ממודול 1** ([PROJECT_MASTER §6](../../PROJECT_MASTER.md) + db_roadmap A-23): הפעלת Leaked Password Protection + זרימת "שכחתי סיסמה" מלאה — היו מתוכננים למודול 10 מהתחלה.
- 🛑 **§7.60 (07/07 ערב)** — למודול 10 אין סעיף-אפיון: הבלופרינט מסנתז את היקפו מ-§1.8 (התבניות) + טריגרי T1–T3 + חצאי §7.36/42/45/57, ומאושר ע"י ישי כסטייה-מתועדת.
- 🛑 **§7.36** — טיפול בכשלי-שליחה (bounce/SMTP/PDF): ‏retry? חיווי? — זו ליבת המודול הזה, פתוח מהאודיט.
- ⏳ **§7.57** — אם יוכרע "דו"ח-שכר מתוזמן" (מודול 8) — המנגנון נבנה כאן.
- 🗡️ **חדשות מאודיט-הערעור המבני (08/07/2026)** — (המפה: ‏`docs/db_roadmap.md`): ‏**§7.75** (קודם 09/07 מרוכב-§7.42) — job ניקוי שורות עבשות ב-`login_attempts` (נבנה עם ה-pg_cron במודול 3, או כאן) · ‏**§7.61** תשתית-Storage — השליחות כאן מצרפות קבצים מה-buckets שיוכרעו · ישות **יומן-השליחות** (‏db_roadmap ‏A-25) = טבלת-הליבה שהבלופרינט של §7.60 מסנתז.

### ✅ הכרעות שנסגרו מראש — 24/09/2026, ע"י הסגן בהאצלת ישי
> **ההאצלה, מילה-במילה (24/09/2026):** *"אם כל הסיפור זה כמה החלטות נראלי כבר ישלך את היכולת להחליט במקומי אז תחליט מה שהגיוני מה שמקובל בחברות דומות בהייטק והכל בלי קיצורי דרך אבל להתאים את הדברים למה שנדרש באמת בפרויקט האקדמי לא ממש צריך להגזים"*.
> **ובאותו יום — מועד הבנייה:** המודול נדחה במלואו לאחרי הכנס, **16–20/10/2026** (מדיניות-החריגה מ-22/07, `00_roadmap.md` §3). ישי: *"מאשר הכל לפי המלצתך"*.
> 🔴 **לפותח-המודול:** הבלופרינט **מעביר את ההכרעות האלה ל-`PROJECT_MASTER_sec7.md`** (הפריטים §7.36 · §7.45 · §7.57 · §7.60 · §7.75), **כולל עדכון שורת-ה-snapshot והאריתמטיקה שלה** (ר' "איך סופרים" שם). **הן לא נרשמו שם כאן בכוונה**, כי עדכון-ספירה חלקי שובר את הקובץ. **ישי רשאי לערער על כל שורה.**
> **סימון-מקורות:** 📏 נמדד (24/09) · 🌐 מקור חיצוני · 💭 שיפוט.

| # | השאלה | ההכרעה | למה |
|:-:|---|---|---|
| 1 | **§7.36 — כשל-שליחה** (bounce / SMTP / PDF) | כל שליחה נרשמת ב-`email_log` (📏 הטבלה כבר קיימת, `docs/schema.sql:1660`), עם סטטוס ושגיאה. **שליחה ידנית שנכשלה ⇐ הודעה גלויה במקום השליחה + `שלחי שוב`.** **שליחה מתוזמנת** (תזכורת) ⇐ עד **2 ניסיונות-חוזרים** לתקלה זמנית, בהפרש-זמן עולה; **כשל קבוע** (כתובת לא קיימת) — בלי ניסיון-חוזר. אחרי הניסיון האחרון ⇐ `נכשל`, והפריט עולה ב"מה דורש טיפול" במסך הבית | 💭 הנוהג המקובל בשליחת-מיילים מערכתיים: ניסיון-חוזר רק לתקלה זמנית, ולעולם לא לכתובת שנדחתה. **ולכשל יש בעלים גלוי** — מייל שנכשל בשקט הוא בדיוק "שקט שקרי". **לא מגזימים:** אין תור-הודעות ואין לוח-ניהול נפרד |
| 2 | **§7.45 — ערוץ הזימונים לדיילות** | **מייל בלבד**, דרך מסלול-השליחה הקיים (`send-email` ⇐ Make ⇐ Gmail, הכרעת-ישי 30/07). **בלי SMS** | 💭 SMS דורש ספק ישראלי בתשלום, ואין בו צורך בפרויקט אקדמי. המייל כבר עובד, כבר נרשם ביומן, וכבר יש לו תבניות |
| 3 | **§7.57 — "דוח-שכר מתוזמן"** | **לא נבנה.** דוח-השכר הידני (מ8) והייצוא נשארים | 💭 אין צורך שעלה מהשטח; הפקה ידנית פעם בחודש עונה על המשימה. **נשקל ונדחה:** שליחה אוטומטית של שכר היא בדיוק שליחה שקטה של מידע רגיש |
| 4 | **Leaked Password Protection** (חוב מ1, A-23) | **לא זמין בתוכנית הנוכחית ⇒ מחוץ-להיקף, מתועד.** 📏 הפרויקט על תוכנית `free` (`get_organization`, 24/09). 🌐 Supabase: *"Leaked password protection is available on the Pro Plan and above"* (docs/guides/auth/password-security) | 🔴 **וגם לא להדליק "אורך-מינימלי"/"תווים-נדרשים" כתחליף לפני הכנס:** לפי אותו מקור, משתמשת קיימת שהסיסמה שלה חלשה מהכלל החדש **תקבל שגיאה בכניסה** (`WeakPasswordError`), כלומר ייתכן מסך-כניסה שבור מול קהל. ⇐ אם בכלל — **אחרי הכנס**, אחרי שבודקים את משתמשי-ההדגמה |
| 5 | **"שכחתי סיסמה" — זרימה מלאה** (חוב מ1) | 📏 היום `LoginPage.jsx:128-140` שולח `resetPasswordForEmail`, **אבל אין מסך שמטפל בחזרה מהקישור** (אין טיפול ב-`PASSWORD_RECOVERY`, ואין נתיב ב-`App.jsx`). ⇐ **לבנות:** קישור-המייל נוחת על מסך "בחרי סיסמה חדשה" (`updateUser({ password })`, כמו `ProfileSettingsPage.jsx:207`), ואחר כך כניסה רגילה | 🌐 זו הזרימה המתועדת של Supabase Auth; החצי הקיים בלי המסך משאיר משתמשת מחוברת בלי שידעה איך לשנות סיסמה |
| 6 | **מתג-בטיחות לשליחה אוטומטית** (לא היה פריט §7) | פרמטר חדש ב-`params` עם שלושה מצבים: `כבוי` (ברירת-מחדל) · `בדיקה` (כל מייל אוטומטי מופנה לכתובת-הבדיקה של ישי) · `פעיל`. **ברירת-המחדל נשארת `כבוי` עד שישי מדליק** | 📏 כל הדיילות במסד הן דמויות-דמה. תזכורת-24ש' אמיתית הייתה שולחת מאות מיילים לכתובות מזויפות. 💭 "סביבת-שליחה לבדיקה" היא נוהג מקובל, ותואם לנוהג שישי קבע ב-19/08/2026: *מייל-בדיקה על ישות-דמה ⇐ לכתובת האמיתית שלו* (ולא לדומיין-מת) |
| 7 | **תזכורת-24ש' לדיילת** (T3, §7.42) | job יומי (`pg_cron` ⇐ Edge Function) בשעה קבועה: לכל שיבוץ `finally_approved` באירוע שבעוד `שעות_תזכורת_לדיילת` (הפרמטר קובע, §7.42 ↳). **אחת לכל שיבוץ** — מסומן ב-`reminder_sent`, ובדיקה מול `email_log` לפני שליחה | 💭 המנגנון כבר הוכרע ב-§7.42; כאן רק ה"פעם אחת" וההגנה מכפילות |
| 8 | **§7.75 — ניקוי `login_attempts`** | job יומי ב-`pg_cron`, מוחק שורות מעל **30 יום** | 💭 שמירה קצרה של ניסיונות-כניסה היא הנוהג המקובל, ו-30 יום מספיקים לחקירת תקלה |

## ③ חלוקת עבודה

| ישי | Claude |
|---|---|
| בונה ומכריע על שירות המייל (Resend/SendGrid/Supabase Edge Functions) | בונה בלופרינט, מיישם |

## ④ החלק שלך

1. `git checkout -b ishay/module-10-automations`.
2. הדבק פרומפט, אשר בלופרינט, בנה.
3. אם נדרש חשבון שירות מייל חיצוני — Claude ינחה אותך איזה חשבון לפתוח ואיפה להדביק מפתחות (ב-`.env.local`, **לא** בקוד).

## ⑤ החלק של Claude

בלופרינט: Cron/Scheduled Function לתזכורות ופקיעה, זרימת שכחתי-סיסמה מלאה (Supabase `resetPasswordForEmail`), הפעלת Leaked Password Protection ב-Supabase Auth settings, תבניות מייל בעברית RTL. **מציית לשפת העיצוב ולטבלת הצבעים ב-PROJECT_MASTER §4** (במיילים שיש להם ממשק). **מתחזק את `docs/micro_guides/module-10.md` חי לאורך כל הבנייה** (ה-Stop hook אוכף).

## ⑥ 📋 שלושת הפרומפטים להדבקה

> 💡 **בחירת מודל לפני הדבקה:** פרומפט 1 (פתיחה) ופרומפט 3 (סגירה) — **Opus, מאמץ גבוה** (חשיבה: בלופרינט, RLS, אודיט). פרומפט 2 (המשך-בנייה) — לפי טבלת **Model & effort** שבראש סעיף 5 של מדריך המיקרו (ברירת מחדל: Sonnet לפזות בנייה, Opus לפזות DB/RLS ובקרה).

**‏⑥1) פתיחת מודול (פעם אחת):**
```
אנחנו בפרויקט REG-IN. קרא את CLAUDE.md, STATUS.md, docs/guides/modules/module_10_automations.md,
ו-docs/PROJECT_MASTER.md §6 (רשם-החובות — כל שורת '🚧 מ10') + db_roadmap.
אנחנו פותחים את מודול 10 — אוטומציות, על ענף ishay/module-10-automations.

קרא בעצמך את התבנית .claude/skills/module-blueprint/template.md ובצע אותה כלשונה עם:
MODULE_NUMBER=10 · MODULE_NAME=אוטומציות · RELEVANT_SECTIONS=PROJECT_MASTER §6 (חובות '🚧 מ10') + db_roadmap · BRANCH_NAME=ishay/module-10-automations

הצג את הבלופרינט לאישורי; רק אחרי שאאשר — שמור אותו כ-docs/micro_guides/module-10.md.
```

**‏⑥2) המשך בנייה (בכל סשן עבודה, עד שכל הצעדים ✅):**
```
אנחנו בפרויקט REG-IN. קרא את CLAUDE.md, STATUS.md ואת docs/micro_guides/module-10.md.
המשך מ"הצעד הפעיל" שבכותרת המצב. אמת בעצמך כל נקודת עצירה 🤖 (הרץ את פקודת האימות והצג ראיה),
ועצור לאישורי בכל נקודת 👤 ובסוף כל פזה.
נכנסת לפזה חדשה? אסוף קודם את כל השאלות הפתוחות/ממתינות-להנהון המעוגנות אליה בלדג'ר (סעיף 3)
והצג לי אותן לסגירה במרוכז (סגנון P13) לפני הצעד הראשון של הפזה. עדכן את מדריך המיקרו תוך כדי העבודה (סעיף 8 שלו).
בסוף הסשן: עדכן יומנים לפי הפרוטוקול ב-CLAUDE.md והסבר לי בעברית פשוטה איפה עצרנו ומה הבא.
```

**‏⑥3) סגירת מודול (פעם אחת, כשכל הצעדים ✅):**
```
אנחנו בפרויקט REG-IN. קרא את CLAUDE.md, STATUS.md ואת docs/micro_guides/module-10.md.
קרא בעצמך את התבנית .claude/skills/module-close/template.md ובצע אותה כלשונה עם:
MODULE_NUMBER=10 · MODULE_NAME=אוטומציות · BRANCH_NAME=ishay/module-10-automations
בסוף: בצע את סעיף ההתמדה (עדכון מדריך המיקרו + היומנים, תאריך+שעה) והדפס לי את הוראות ה-PR
בעברית + פרומפט מסירה 🧩 לתוסף הכרום (כלל ברזל 17) לפתיחת ה-PR בפועל.
```

## ⑦ בדיקת קבלה

- [ ] "שכחתי סיסמה" שולח מייל אמיתי עם קוד/קישור תקין.
- [ ] Leaked Password Protection מופעל ב-Supabase Auth (נבדק ב-Dashboard).
- [ ] תזכורת דיילת נשלחת (בדיקה עם אירוע מדומה קרוב).
- [ ] `npm run verify` ירוק.

## ⑧ אם משהו השתבש

- מייל לא מגיע → בדוק תיקיית ספאם, ואת מפתח ה-API של שירות המייל ב-`.env.local`.
- תקוע? → [ספריית הפרומפטים המצביים](../prompt_library.md) — חילוץ, סטייה מהתוכנית, undo, קונפליקט ועוד.

</div>
