<div dir="rtl">

# 🔗 שלב — מודול 6: פרויקטים (המחבר)

> מקומו בהיררכיית האמת: `schema.sql` › אפיון › מוקאפים › מדריך זה. בסתירה — המדריך מפסיד.
> צעדים שבוצעו מסומנים ✅ — המדריך כתוב כאילו כלום לא קיים.
> **נקודת האינטגרציה הגדולה באמצע הפרויקט** — כאן נפגשים הצעות המחיר, שיבוץ הדיילות והלוגיסטיקה בישות אחת.
>
> ✅ **‏Discovery נערך ונסגר 13/08/2026** — התוצר ב-[`docs/specs/module_06_projects/`](../../specs/module_06_projects/),
> ו-**`spec.md` הוא מקור-האמת המוצרי של המודול** *(גובר על C5 ועל המוקאפים בכל מה שהוא מכסה
> בפועל, פר-פריט — כמו במודול 4)*. **נקודת-הכניסה: פרומפט ⑥1 למטה.**
> 🔴 **ושתי הכרעות-ה-§7 שהמדריך הזה סימן כ"חובה לפני הבנייה" — נסגרו שתיהן שם:** ‏`§7.17`
> *(שינוי-תכולה = כמויות בלבד · טבלת `project_changes`)* ו-`§7.7` *(אין stepper כלל)*.
> **אל תפתח אותן מחדש.**
> **פזה משולבת עם מודול 5** — ‏`00_roadmap §3`: 6-ליבה → 5 → 6-אינטגרציה. **תלות-בנייה הדדית,
> אבל אין תלות-Discovery** ⇒ מודול 6 רץ ראשון, וכך היה בפועל.

## ① מה נשיג בשלב הזה

הישות המרכזית שמחברת הצעות מחיר, שיבוץ דיילות ולוגיסטיקה: מכונת מצבים ל-8 סטטוסים (`טרם החל → בתהליך → מוכן לביצוע → אירוע הסתיים → ממתין לחשבונית → ממתין לתשלום → הסתיים / בוטל`), חישוב אחוזי שיבוץ/לוגיסטיקה, וטיפול בשינויי-תכולה. מסך 5.6-5.7 באפיון.
> 🔗 **מראת §7.12+43+44+16+31** (שתי ההערות הבאות) — SSOT: ‏PROJECT_MASTER §7 (לא לערוך כאן ידנית; מסונכרן ע"י `regin-docs-sync`)
> ℹ️ **דרישת-אימות (§7.12, 07/07/2026):** יצירת פרויקט מהצעה מאושרת **קוראת את השורות הקפואות** דרך `projects.quote_id`→`quote_services` (מחיר+עלות מוקפאים, §7.28) — **לא מחשבת מחדש**. נקודת-בדיקה מפורשת בבלופרינט.
> ℹ️ **מכונת-הסטטוס הוכרעה (07/07/2026, ישי — §7.43/44):** `project_status` נכתב ב-**טריגר DB אטומי** על שינויי `assignments`/`logistics` (לא חישוב-בקריאה); `ready` = מצב **נגזר והפיך** (איוש 100% **וגם** לוגיסטיקה 100%) — דיילת שבוטלה + דריסה ידנית של המנהלת → הטריגר מחזיר אוטומטית ל-`in_progress`. שינויים אפשריים עד ~36ש' לפני האירוע. **קסקדת-ביטול (§7.16↔§7.31):** צד השכר/שחרור נסגר (§7.16/§7.33); כאן במודול 6 — סגירת לוגיסטיקה + מיילי-ביטול לדיילות.

## ② תנאי כניסה

- [x] **‏Discovery הושלם** *(13/08/2026)* — ‏`docs/specs/module_06_projects/` קיים ומאושר.
- [x] מודולים **3** (הצעות מחיר) ו-**4** (דיילות) מוזגו ל-`dev`.
- ⚠️ **ומודול 5 (לוגיסטיקה) *אינו* תנאי-כניסה** — ‏`00_roadmap §3` קובע **6-ליבה → 5 → 6-אינטגרציה**.
  *(תוקן 13/08/2026: כאן נדרש *"מודול 5 מוזג"*, וזה הפך את המודול הזה לבלתי-פתיח. **נמדד: למ5 אין
  מדריך-מיקרו ואין ענף** — הוא טרם נבנה.)*
  🔴 **אבל התלות עצמה אמיתית ויש לה שם:** מדד-המוכנות השני של מ6 קורא מ-`logistics`, **שהיא
  `deny-all` היום** — ר' §⑧.

**מצב פריטי-ה-§7 של המודול — נמדד 13/08/2026, לא מועתק:**

| | |
|---|---|
| **סגורים ✅** *(אל תפתח מחדש)* | ‏7.7 · 7.9 · 7.12 · 7.16 · 7.17 · 7.31 · 7.32 · 7.39 · 7.43 · 7.44 · 7.45 · 7.53 · 7.61 · 7.63 · 7.72 · 7.77 |
| **הוכרעו — הביצוע כאן** ⚪ | ‏**7.49** *(RPC אטומי — הצד-היוצר)* · **7.52** *(שני מספרי-רווח: צפוי נגזר-חי · סופי נשמר וקופא)* · **7.76** *(snapshot-זהות ב-RPC ההמרה)* |
| **הונהן וממתין לביצוע** ⚪ | ‏**7.62** — ‏`SET NOT NULL` ל-`projects.quote_id` + `owner_email`, במיגרציית מ6 *(סוגר את מחלקת "פרויקט-בלי-שם")*. ⚠️ **לפני ההידוק — ספור NULLים חי במסד.** אם יש שורות ריקות — **עצור ודווח**, אל תמחק |
| **פתוחים למחצה** 🟡 | ‏**7.19** *(הבונוס בוטל; נשארה גזירת-השעות — **מודול 8**)* · **7.36** *(האטומיות נענתה ב-7.61; כשל-השליחה — **מודול 10**)* |

## ③ חלוקת עבודה

| ישי | Claude |
|---|---|
| מנהן על `§7.62` · מאשר בלופרינט · מאשר פזות ונקודות 👤 | קורא את `spec.md`, בונה בלופרינט, מיישם, ומאמת כל 🤖 בראיה |

*(עד 13/08 עמד כאן *"מכריע §7.17/§7.7"* — **שתיהן נסגרו ב-Discovery.**)*

## ④ החלק שלך

1. **הדבק את ⑥1** *(פתיחה)*. **הענף ייחתך שם מ-`dev` טרי** — הוא טרם קיים.
2. **אשר את הבלופרינט** — ורק אז הוא נשמר כ-`docs/micro_guides/module-6.md`.
3. **בנה עם ⑥2**, צעד-צעד לפי הסדר שהבלופרינט יציע *(DB/לוגיקה קודם, UI אחר-כך)*.
4. ✅ **`§7.62` הונהן 13/08/2026** — **אין פריט-§7 שממתין לך במודול הזה.**

## ⑤ החלק של Claude

**קורא קודם את `spec.md` במלואו** — ‏13 שורות רשימת-הקריאה, אוצר-המילים הנעול, ופרק "מה אסור לנחש".
**ואז בלופרינט:** מכונת-המצבים כ-**טריגר-מסד אטומי** *(לא חישוב-בקריאה ולא מטמון-לקוח)* · שני
מדדי-המוכנות **בנפרד** · `project_changes` לשינויי-תכולה · והמעבר ל-`ready` כ**מצב הפיך**.
**מציית לשפת-העיצוב ולטבלת-הצבעים ב-`PROJECT_MASTER §4`**, ולשמונת המוקאפים המאושרים כדרגה 2.
**ומתחזק את `docs/micro_guides/module-6.md` חי לאורך כל הבנייה** *(ה-Stop hook אוכף)* — קריטי
כאן במיוחד: המודול נשען על שלושה מודולים קודמים, והמדריך הוא נקודת-הסנכרון ביניהם.

## ⑥ 📋 שלושת הפרומפטים להדבקה

> 💡 **בחירת מודל לפני הדבקה:** ⑥‏1 (פתיחה) ו-⑥‏3 (סגירה) —
> **Opus, מאמץ גבוה** (חשיבה: אפיון-מוצר · בלופרינט · RLS · אודיט). פרומפט 2 (המשך-בנייה) — לפי
> טבלת **Model & effort** שבראש סעיף 5 של מדריך המיקרו (ברירת מחדל: Sonnet לפזות בנייה, Opus
> לפזות DB/RLS ובקרה).

**0) Discovery — ✅ הושלם 13/08/2026. אין מה להדביק כאן יותר.**
> ‏**כל שלושת השלבים רצו בסשן אחד** *(הסקיל: `.claude/skills/module-discovery/`)*, והתוצר יושב
> ב-`docs/specs/module_06_projects/` — ‏**‏38 הכרעות מצוטטות · ‏8 משטחים מאושרים (M=8) ·
> ‏8 מוקאפים · ‏5 חוזים מול מודולים 2/3/4/5/8+11 · ‏17 דרישות-מסד ב-`db_roadmap`** *(14 בבלוק
> `A-M6` + 3 בנתיב-ה-Storage; **נספר מחדש 13/08 — "18" היה שגוי, הרביעית ב-§5 הייתה שורה קיימת**)*.
> **‏10 פריטי-§7 נסגרו:** ‏`§7.7` · `§7.9` · `§7.17` · `§7.31` · `§7.32` · `§7.39` · `§7.61` ·
> `§7.63` · `§7.72` · `§7.77`; ‏**`§7.36` חצי-נסגר ונשאר 🟡** *(חצי-האטומיות נענה, כשל-השליחה
> למודול 10)*. **המספרים נמדדו 13/08/2026 — לא מועתקים.**
> 🔴 **פרומפט 1 למטה הוא נקודת-הכניסה מעכשיו**, והוא כבר מצביע ל-`spec.md`.

**‏⑥1) פתיחת מודול (פעם אחת):**
```
אנחנו בפרויקט REG-IN. קרא את CLAUDE.md ואת STATUS.md.
אנחנו פותחים את מודול 6 — פרויקטים, על ענף ishay/module-6-projects
(הענף טרם קיים — צור אותו מ-dev טרי; אמת קודם `git fetch origin` ואז חתוך).

🔴 ולפני הכל — קרא את docs/specs/module_06_projects/spec.md במלואו.
זהו האפיון המאושר של המודול (שיחת Discovery, 13/08/2026). מעמדו בהיררכיית-האמת כתוב
ב-CLAUDE.md שאתה קורא ממילא — לא משוכפל כאן.
בסעיף ① שלו יושבת רשימת-הקריאה המלאה שלך, בסדר מחייב — 13 שורות. ספור אותה שם ואל תמציא
רשימה משלך, ואל תסמוך על מספר שמישהו נקב בו: הרשימה הזאת גדלה תוך יום.
⚠️ ושלוש השורות הראשונות בה אינן אופציה, והן רוב האפיון — נקובות כאן כדי שלא תחפש:
· docs/specs/module_06_projects/processes-approved.md — 38 ההכרעות · 6 כרטיסי-התהליך · 5 החוזים חוצי-המודולים
· docs/specs/module_06_projects/screens-approved.md — כרטיס לכל אחד מ-8 המשטחים: מפת-לחיצות · מקור לכל מספר · הרשאות · מצבים · וולידציות
· docs/mockups/project-management-screen/approved/*.html — המראה המדויק. HTML — קרא אותו, אל תנחש ממנו
🔴 המסכים נבנים משלושת אלה ביחד. בלעדי אחד מהם אתה מנחש — והניחוש נכנס לקוד בשקט.

טען את הסקיל module-blueprint ובצע אותו — הוא זה ששולח אותך לתבנית, ומוסיף עליה דברים
שאינם בה (פרה-פלייט, ‏_shared/discipline.md, דגם-הפורמט, חיווט ה-council).
אל תדלג ממנו ישר ל-template.md. הפרמטרים:
MODULE_NUMBER=6 · MODULE_NAME=פרויקטים · BRANCH_NAME=ishay/module-6-projects
RELEVANT_SECTIONS=PROJECT_MASTER §5.6 · §5.7 (וכל אחד מהם מצביע ל-C5 §5.6.6 · §5.6.7)
+ מ-§7: 7 · 9 · 12 · 17 · 19 · 31 · 32 · 36 · 39 · 43 · 44 · 52 · 61 · 62 · 63 · 72 · 77
🔴 והסעיפים האלה אינם מקור-מוצר. האפיון המאושר הוא.
ב-PROJECT_MASTER §5.6 עדיין כתובות ארבע לשוניות-סטטוס ומצביע למוקאפים הישנים (01.png/02.png)
— שניהם הוחלפו ב-Discovery. קרא שם רק מה ש-spec.md אינו עונה עליו, וכל סתירה — האפיון מנצח.
🚫 ושלושה נדחו במפורש — אל תשחזר: צביעה לפי קרבת-תאריך · stepper · שאלון-משוב כפול.
(הפרמטר חייב לפתוח בסעיפי PROJECT_MASTER — הטמפלט מציב אותו במשפט "PROJECT_MASTER
(sections ...)", וסעיפי C5 לבדם היו שולחים לסעיפים שאינם קיימים שם.)
⚠️ עשרה מפריטי-§7 האלה נסגרו ב-Discovery ושניים חצי-נסגרו — קרא את הסטטוס ואת גוף הפריט,
לא רק את הכותרת, ואל תפתח מחדש מה שסגור.

חובה: מה שאסור לך לנחש מרוכז ב-spec.md, בפרק "🚫 מה אסור לבלופרינט לנחש".
🔴 ספור אותם שם בעצמך. מה שעדיין פתוח נכנס לטריאז' 🛑 שלך ומגיע אליי מרוכז, עם המלצה
מנומקת לכל אחד — אל תכריע בהם לבד.
⚠️ ושים לב במיוחד למוקש שנשבר בשקט: `logistics` היא deny-all במסד — RLS פעיל, אפס policies
(src/CLAUDE.md). מדד-המוכנות השני של מ6 ("לוגיסטיקה N/M") נשען עליה, ולכן בלי policy-קריאה
המסך יחזיר רשימה ריקה **בלי שגיאה** — בדיוק המלכודת {data:null, error:null}.

הצג את הבלופרינט לאישורי; רק אחרי שאאשר — שמור אותו כ-docs/micro_guides/module-6.md.

לפני כל פעולה ענה על החמש האלה — כל תשובה עם הקובץ או הפקודה שהוכיחה אותה.
אל תנחש, ואל תענה מהמסמכים בלבד אם אפשר לוודא חי:
1. מה המצב הנוכחי של המשימה, לפי מה שקראת?
2. מה בדיוק המשימה שלך, ומה במפורש לא?
3. מה אסור לך להחליט לבד כאן?
4. מה כבר הוכרע ואסור לפתוח מחדש?
5. מה המוקש שאם תפספס אותו — זה יישבר בשקט?

ואז שתי שאלות שלך אליי: מה לא ברור לך? מה חסר בהוראות שנתתי?
```

> 🔑 **למה חמש השאלות, ולמה #4 קריטית כאן במיוחד:** ‏**‏38 הכרעות נסגרו ב-Discovery של 13/08**,
> ועשרה פריטי-§7 איתן. **בלי #4 הן יובאו אליך שוב** — וזה בדיוק מה שהאפיון קיים כדי למנוע.
> **‏#5 היא המשתלמת:** היא מכריחה להצהיר בקול על מה שעלול להישבר **בלי שאיש ישים לב** — וכאן
> יש מועמד מדוד ומוכר בשם: ‏`logistics` ב-deny-all.

**‏⑥2) המשך בנייה (בכל סשן עבודה, עד שכל הצעדים ✅):**
```
אנחנו בפרויקט REG-IN. קרא את CLAUDE.md, STATUS.md ואת docs/micro_guides/module-6.md.

🔴 וקרא גם את האפיון המאושר של המודול — docs/specs/module_06_projects/spec.md.
בסעיף ① שלו יושבת רשימת-הקריאה המלאה: מה עוד לפתוח, ומה במפורש לא לקרוא.
הכתובות הייחודיות למודול הזה, כדי שלא תחפש:
· המוקאפים המאושרים: docs/mockups/project-management-screen/approved/ (HTML — קרא, אל תנחש)
  🎨 הם דרגה 2 ולא דרגה 4: ישי אישר את שמונתם. אל תצייר אותם מחדש ואל תבקש אישור עליהם.
· התהליכים והכרטיסים: docs/specs/module_06_projects/processes-approved.md + screens-approved.md
· אוצר-המילים הנעול ותוויות-המסך מילה-במילה: spec.md, פריט-חוזה 1 (כולל שמונת הסטטוסים —
  סגורים, אסור להוסיף תשיעי)
· המספר בר-הבדיקה שחושב ביד: פרויקט #8 — "הכנסה מתוכננת" = 5,355.00 ₪
  (6,300 פחות 15% הנחה בחיבור ולא בשרשור). בפריט-חוזה 3 שבאפיון — משם, ולא בחישוב מחדש.
· דרישות-המסד: docs/db_roadmap.md, בלוק A-M6 (14 שורות) + נתיב-ה-Storage (3) — זה הרשם,
  לא האפיון.

אני ישי. המשך מ"הצעד הפעיל" שבכותרת המצב.
אמת בעצמך כל נקודת עצירה 🤖 (הרץ את פקודת האימות והצג ראיה), ועצור לאישורי בכל נקודת 👤
ובסוף כל פזה.
נכנסת לפזה חדשה? אסוף קודם את כל השאלות הפתוחות/ממתינות-להנהון המעוגנות אליה בלדג'ר
(סעיף 3) והצג לי אותן לסגירה במרוכז (סגנון P13) לפני הצעד הראשון של הפזה.
עדכן את מדריך המיקרו תוך כדי העבודה (סעיף 8 שלו).
בסוף הסשן: עדכן יומנים לפי הפרוטוקול ב-CLAUDE.md והסבר לי בעברית פשוטה איפה עצרנו ומה הבא.
```

> 🔑 **למה הבלוק העליון קיים** *(מועתק בכוונה מדגם מודול 4, ‏08/08/2026)*: ‏`module-build` מעגן
> את עצמו ב-⑥2 ובמדריך-המיקרו, **ואף אחד מהשניים אינו שולח את הבנאי ל-`docs/specs/`**.
> ⇒ בלי השורות האלה **אוצר-המילים הנעול, התוויות מילה-במילה, והמספר `5,355.00`** לא היו מגיעים
> אליו כלל. ➕ **ושורת-ה-🎨 היא הנתיב השני**: כלל-המוקאפים בסקיל נכתב ל-`docs/mockups/**`
> כדרגה 4 *("וחלק מפרטיהם שגויים")* — מיושם כלשונו, הוא **מצייר מחדש ומבקש אישור על שמונה
> מסכים שכבר אישרת.**

**‏⑥3) סגירת מודול (פעם אחת, כשכל הצעדים ✅):**
```
אנחנו בפרויקט REG-IN. קרא את CLAUDE.md, STATUS.md ואת docs/micro_guides/module-6.md.
קרא בעצמך את התבנית .claude/skills/module-close/template.md ובצע אותה כלשונה עם:
MODULE_NUMBER=6 · MODULE_NAME=פרויקטים · BRANCH_NAME=ishay/module-6-projects
אימות-הקבלה נעשה מול "✅ מה ייחשב עובד" שב-docs/specs/module_06_projects/spec.md —
תשעת הקריטריונים, לא רשימה שתמציא.
בסוף: בצע את סעיף ההתמדה (עדכון מדריך המיקרו + היומנים, תאריך+שעה) והדפס לי את הוראות ה-PR
בעברית + פרומפט מסירה 🧩 לתוסף הכרום (כלל ברזל 17) לפתיחת ה-PR בפועל.
```

## ⑦ בדיקת קבלה

🔴 **הרשימה המחייבת היא "✅ מה ייחשב עובד" ב-[`spec.md`](../../specs/module_06_projects/spec.md) —
תשעה קריטריונים, והיא הקובעת.** ארבעת אלה מובאים כאן כדי שתוכל לסמן בעצמך:

- [ ] **דנה פותחת את מבט-העל ויודעת מיד לאיזה אירוע להיכנס** — הסדר והעמודה "מה חסר" עונים
      על זה, **לא רשימה ולא ציון-דחיפות** *(ההכרעה: אין ציון מורכב)*.
- [ ] **שני מדדי-המוכנות נשארים נפרדים** — `דיילות N/M` ו-`לוגיסטיקה N/M`, **לא ממוזגים לאחוז
      אחד**, כדי שתדע *למי* לפנות בלי להיכנס לפרויקט.
- [ ] **שינוי תכולה = כמויות בלבד**, נרשם ב-`project_changes` עם מחיר ועלות מוקפאים וסיבה
      חובה — **וההצעה נשארת קפואה.**
- [ ] 🔴 **מסך שלא הצליח לטעון אומר זאת. לעולם לא רשימה ריקה בשקט** *(‏`logistics` ב-deny-all —
      המלכודת `{data:null, error:null}`)*.
- [ ] 🎯 **המספר שחושב ביד תואם למה שהקוד מחשב — פרויקט `#8`, "הכנסה מתוכננת" `5,355.00 ₪`.**
- [ ] `npm run verify` ירוק; PR נפתח.

## ⑧ אם משהו השתבש

- 🔴 **מסך ריק במדד-הלוגיסטיקה / בלשונית הלוגיסטיקה** ⇐ **החשוד הראשון: `logistics` היא
  `deny-all`** *(‏RLS פעיל, אפס policies — `src/CLAUDE.md`)*. **מ6 חייב policy-קריאה עליה**;
  בלעדיה המסך חוזר ריק **לכולם, כולל המנכ"ל, בלי שגיאה.** *(אותה מחלקה בדיוק שמ4 נכווה בה
  עם `projects`.)*
- **סטטוס לא מתקדם אוטומטית** ⇐ הטריגר הוא **ברמת-מסד ואטומי**, לא חישוב-בקריאה ולא מטמון-לקוח.
  ⚠️ **ולטריגר שלושה מקורות ולא שניים** — גם `required_hostess_count` עצמה: שינוי-תכולה מזיז את
  **המכנה**.
- 🔴 **`טרם החל` בלתי-נגיש** ⇐ שורות-הלוגיסטיקה נולדות **אוטומטית** ב-RPC של אישור-ההצעה.
  המדד הלוגיסטי נספר רק כשפריט **יוצא** מ-`not_started` — לא כשנוצר.
- **"מוכן לביצוע" נתקע או שולח מייל פעמיים** ⇐ הוא **מצב הפיך ורגעי**, ולכן **אסור לתלות בו
  פעולה חד-פעמית.**
- **שינוי-תכולה לא מוצא איפה להישמר** ⇐ הוא **אינו** עורך את ההצעה. `project_changes` היא
  הבית, וההצעה נשארת קפואה. ⚠️ ובלי `logistics.line_id` אין ליניאז' לשורה המקורית.
- **`verify` אדום** ⇐ ‏`regin-pr-gate` תאבחן *(רוטינה — **אתה** לוחץ "Run now", לא Claude)*.
- **תקוע?** ⇐ [ספריית הפרומפטים המצביים](../prompt_library.md) — חילוץ, סטייה מהתוכנית, undo, קונפליקט.

</div>
