<div dir="rtl">

# 👥 שלב — מודול 4: דיילות + Smart Match

> **‏🔴 מקור-האמת המוצרי של המודול הזה הוא `docs/specs/module_04_hostesses/spec.md`** — האפיון
> המאושר שנולד בשיחת ה-Discovery (‏05–08/08/2026). **הוא גובר על C5/C6 בכל מה שהוא מכסה בפועל,
> פר-פריט.** המדריך הזה הוא **סדר-העבודה בלבד** — הוא לא מחזיק תוכן מוצרי משלו, ובסתירה הוא מפסיד.
> **היררכיית האמת המלאה:** `docs/schema.sql` › **האפיון המאושר** › ‏C5/C6 הקפואים › מוקאפים ›
> המדריכים *(`CLAUDE.md`, "היררכיית מקורות אמת")*.
>
> **‏🕓 שוכתב במלואו 08/08/2026 02:0X** — אחרי שנמדדו בו **15 טענות שהתיישנו** מאז שנכתב
> *(מראות §7 שהוחלפו · "מחיקה רכה" שאינה קיימת · "דירוג" כרכיב-ציון שהוסר · תנאי-כניסה של
> מודול 1 בעוד 1–3 מוזגו)*. **הגרסה הקודמת ב-`git log` של הקובץ.**
> 🔑 **והשינוי המבני, כדי שזה לא יחזור: המדריך הפסיק לשכפל תוכן §7.** ‏שבע מתוך 15 הטענות
> השגויות היו **מראות §7 שהמקור שלהן התעדכן והעותק כאן לא.** ⇒ **מכאן והלאה — מצביע בלבד.**

## ① מה נשיג בשלב הזה

**מאגר דיילות** (הוספה · עריכה · הפעלה/השבתה — 🚫 **אין מחיקה בשום מקום**, תג-סטטוס הפיך משני
הכיוונים) **+ שמונה משטחי-מסך** (מבט-על · Smart Match · מאגר · הוספה/עריכה/צפייה · תפריט-שורה ·
**דף ציבורי ללא התחברות**) **+ אלגוריתם השיבוץ**.

**מה שהאלגוריתם באמת עושה — בשורה אחת, כי הניסוח הישן כאן היה שגוי:**
**ארבע שכבות בסדר קבוע** — ‏**שער** שפוסל · **נעיצה** *(ריקה עד מ6)* · **ציון** משלושה מרכיבים ·
**מיון + מנוף-הוגנות**. שלושת מרכיבי-הציון: **סיכוי-היענות `0.40`** · **אמינות-הגעה `0.35`
(כבוי עד מ6, והמסך אומר זאת)** · **קרבה `0.25`**.
🔴 **ו-`התרשמות` (‏`hostesses.rating`) אינה רכיב בציון** — היא דעת המנהלת, לא פסק-דין של המערכת.
*(עד 08/08 עמד כאן *"מדרג לפי **דירוג**, קרבה ומהימנות"* — נוסח הנוסחה הישנה מ-`PROJECT_MASTER:311`,
שהוחלפה במלואה במחקר §11. ‏`spec.md` §1.4 · §שכבה-3.)*

**מסכים באפיון:** ‏`PROJECT_MASTER §5.10–5.12` ⇒ `C5 §5.6.10/11/12`. ⚠️ **‏C5 מגדיר שלושה מסכים,
והאפיון המאושר מגדיר שמונה משטחים** — החמישה שאין להם מסך ב-C5 כוללים את **הדף הציבורי**,
המשטח היחיד במערכת שכותב ל-DB בלי התחברות ובלי תפקיד.

## ② מצב הכניסה — מה כבר סגור, ומה לא

### ✅ מה שכבר נחת (אומת 08/08/2026)

- **מודולים 1, 2 ו-3 מוזגו ל-`dev`** — מ1 (PR #2) · מ2 (PR #6) · **מ3 05/08/2026, PR #10, merge
  `792456f`** *(‏`STATUS.md`, טבלת-המצב)*. **אין תנאי-כניסה פתוח.**
- **הענף `ishay/module-4-hostesses` קיים ומסונכרן מול `origin`** *(‏`3311cb2`)* — **אין ליצור אותו מחדש.**
- **שלב ה-Discovery נסגר** — ‏`docs/specs/module_04_hostesses/` מכיל אפיון מאושר, שני מסמכי-תהליכים
  ומסכים, **שמונה מוקאפים מאושרים**, ומסמך-האלגוריתם.
- **‏`module-blueprint` הותאם** (‏08/08, 7 עריכות) — הוא כבר יודע לקרוא מ-`docs/specs/`,
  **וראיון-ה-PM אינו רץ כשיש אפיון מאושר.**

### 📚 מה שנקרא לפני הבלופרינט — ואיפה זה כתוב

🔴 **אין רשימת-קריאה כאן, בכוונה.** היא חיה **במקום אחד** — ‏`spec.md` **§①**, טבלה ממוספרת
בת 13 פריטים, **בסדר קריאה מחייב**. *(רשימה שנייה כאן הייתה מתפצלת מהראשונה תוך יומיים — בדיוק
מה שקרה למראות-§7 שהוסרו מהקובץ הזה היום.)*
⚠️ **המוקש היחיד ששווה לחזור עליו:** ‏**ממסמך-האלגוריתם קוראים `§11` בלבד** — הוא 900+ שורות,
ו-§11 נכתב במפורש עבור סשן-בנייה.

### 🎯 מה כבר הוכרע — ואסור לפתוח מחדש

| האשכול | איפה ההכרעה חיה |
|---|---|
| ארכיטקטורת ארבע-השכבות · שלושת המרכיבים ומשקליהם · ריסון `m=3` · שער `80` · גולפוסט `40` · מנוף-הוגנות `0.02`/תקרה `8` | `spec.md` §🧮 + `module4_smart_match_research.md §11` *(**‏SSOT למספרים** — לא לשכפל)* |
| ששת סטטוסי-השיבוץ · "פג תוקף" כמצב **נגזר** · כל תווית-מסך מילה-במילה · שמות הכפתורים | `spec.md` **פריט-חוזה 1** *(אוצר-המילים הנעול)* |
| שני התהליכים מקצה-לקצה · אי-זמינות מוצהרת · אחראית משמרת · מצב T-24 · עודף-אישורים כאזהרה | `spec.md` **פריט-חוזה 2** + `processes-approved.md` |
| ‏**ת"ז יורדת ממפתח** ⇒ `hostess_id bigint identity` · ‏`rating` הופך ל-`null` · מייל בלי `UNIQUE` · "אירוע אחד ביום" כאילוץ-מסד | `PROJECT_MASTER §7.64 · §7.29 · §7.65` — **לצטט מספר, לא תוכן** |
| שמונת המשטחים ומוקאפיהם | `screens-approved.md` + `docs/mockups/hostesses-screen/approved/` |

🔴 **ולמה זה טבלת-מצביעים ולא טבלת-תוכן:** עד 08/08 ישבו כאן שבע מראות-§7 מנוסחות במלואן,
**וכולן התיישנו בשקט** — הבולטת: §7.29 (הגדרת ה-double-booking) **הוחלפה 30/07/2026** בעוד
העותק כאן המשיך לומר *"שני קצרים עם ≥שעה = מותר"*. **מספר-פריט לא מתיישן; ציטוט מתיישן.**

### ⏳ מה פתוח **בכוונה** — ואינו חוסם

ארבעה פריטים, **וכולם מפורטים ב-`spec.md` §"פתוח בכוונה"**: זהות שירות-הגאוקוד *(נבדק מול
ה-ToS בזמן הבנייה, לא מהזיכרון)* · סכום-הנסיעות *(המנגנון הוכרע — הסכום מאומת מול רו"ח לפני
שמ10 שולח מייל אמיתי)* · לוג-החשיפה *(לא-חוסם)* · **§7.63 — הגנת בנק/ת"ז ברמת-המסד, נדחתה
למ6/מ8; במ4 ההגנה בממשק בלבד, ‏🔴 מגבלה מוצהרת ולא חור לסתום.**

### 🛑 מה שהבלופרינט **חייב** להביא להכרעתך — ואל תיתן לו לדלג

✅ **ארבעתם הוכרעו על-ידך ב-08/08/2026 — והכותרת הזאת נשארת כדי שהבלופרינט לא יפתח אותם מחדש.**
🔴 **ההכרעה, הנימוק ותשובת-הכנס חיים ב-`PROJECT_MASTER §7` בלבד** *(הכרעת-ישי 08/08: **פריטים
פתוחים חיים רק במאסטר; בכל מקום אחר — קישור בלבד** — זה מה שמונע את מה שקרה לקובץ הזה היום)*:

| ‏§7 | השאלה | **ההכרעה** |
|---|---|---|
| **[§7.88](../../PROJECT_MASTER.md)** | איך מממשים "אירוע אחד ביום" כאילוץ-מסד | **‏`assignments.event_date` מדונרמלת + טריגר** *(כולל על שינוי תאריך-אירוע)* |
| **[§7.89](../../PROJECT_MASTER.md)** | תבנית-המייל הרביעית + placeholders בלי מקור | **נזרעת באותה מיגרציה** *(בנוסח הקיים)*; כל placeholder לגופו |
| **[§7.90](../../PROJECT_MASTER.md)** | איפה יושב הדגל "מרכיב-האמינות כבוי" | **‏`params`** — 🚫 לא גזירה-אוטומטית |
| **[§7.91](../../PROJECT_MASTER.md)** | מי מדליק את התראת ה-T-24 | **נגזרת בזמן-קריאה**, כמו "פג תוקף" |

🔑 **ובכל הארבעה — האפיון *אינו* שותק, וזו ההבחנה שקובעת מה מותר לבלופרינט גם עכשיו:**
**ההתנהגות והנוסח הוכרעו וכתובים** *(עד כדי **הנוסח המלא של תבנית-המייל הרביעית**,
‏`processes-approved.md:265`)*; **מה שפתוח הוא ה*מנגנון* — עמודה, טריגר, פרמטר.**
🚫 ⇒ **אסור לבלופרינט להמציא תווית או נוסח "כי זה פתוח"** — פתוח = **איך בונים, לא מה רואים.**

**‏🔵 ושני פריטים שאינם חוסמים ואינם שאלות:**
**‏(1) היקף ישות-המשמרת (§7.67)** — `🟢 סגור·נבחן-בבלופרינט`: ההכרעה (`project_shifts`) עומדת,
אבל הנימוק המרכזי שלה התאייד *("אירוע דו-תפקידי — לא קורה")*, והבלופרינט מתבקש **לבחון מחדש
את היקפה** מולך. **דיון, לא עצירה.**
**‏(2) `assignments.responded_at`** — **הוכרע, ואינו סתירה.** מ4 **מוסיף את העמודה** *(הפונקציה
הציבורית שלו כותבת אליה בכל מענה — `screens-approved.md` משטח 5 §①)*, **וזווית "תענה הכי מהר"
מוצגת מכובה-ומנומקת כל עוד אין מספיק דאטת-זמן-תגובה** *(`screens-approved.md` §②, אושר 07/08 —
‏`opacity:.5` + תווית "— כבוי" + שורת-הסבר)*. 🚫 **לא נעלמת ולא ממיינת שגוי.**

## ③ חלוקת עבודה

| ישי | Claude |
|---|---|
| מאשר את הבלופרינט · מאשר כל 🗣️ תקציר-חוויה לפני שנכתב קוד · מקליד את שם-המיגרציה בשער ה-typed-echo · פותח PR וממזג | בונה בלופרינט (`docs/micro_guides/module-4.md`) דרך התבנית · מיישם · מאמת בעצמו כל 🔻🤖 בראיה · מתחזק את מדריך-המיקרו חי |

## ④ החלק שלך

1. **הדבק את פרומפט ⑥1** *(פתיחת מודול)*. ‏🚫 **"פתח מודול 4" לבדו אינו מספיק** — הטריגר טוען את
   הסקיל אבל אינו נושא את `MODULE_NUMBER` ואת הענף, **וניחוש שלהם הוא תקלה שקטה שכבר נתפסה.**
2. **ענה על סבב-ההכרעות** שיגיע (⑥1 מחייב אותו להביא אותן מרוכזות, עם המלצה לכל אחת).
3. **אשר את הבלופרינט.** ‏🔒 עד שתאשר, הסשן ב-Plan Mode ואינו נוגע בקבצים.
4. **"תמשיך לבנות"** *(או הדבק את ⑥2)* בכל סשן, עד שכל הצעדים ✅.
5. **בסוף — הדבק את ⑥3** *(סגירת מודול)*, ואז פתח PR לפי ההוראות שהוא ידפיס.

🚫 **מה שכבר לא צריך לעשות:** ליצור ענף *(קיים)* · לוודא שמודול 1 מוזג *(מוזג, וגם 2 ו-3)* ·
להכריע בשאלות ה-🛑 הישנות של §7 *(הוכרעו ב-Discovery — הרשימה החיה היא זו שב-② למעלה)*.

## ⑤ החלק של Claude

בונה בלופרינט מלא לפי `.claude/skills/module-blueprint/template.md` — **פזות: DB/RLS ⇒ לוגיקה
עסקית (`src/lib` + `api.js`) ⇒ UI ⇒ בקרה ⇒ QA וסגירה.**
**מציית לשפת-העיצוב ולטבלת-הצבעים ב-`PROJECT_MASTER §4`.**
**נשען על RLS/Auth ממודול 1 ומצהיר במפורש איך** — אילו תפקידים עורכים/צופים ב-`hostesses`,
לפי המודל שהוכרע ב-§7.21 ולפי מטריצת-ההרשאות ב-`PROJECT_MASTER §3`.
**מתחזק את `docs/micro_guides/module-4.md` חי לאורך כל הבנייה** — סטטוס-צעדים, סטיות `↳ as-built`
וחוב טכני מתעדכנים **באותו סשן שבו קרו** (ה-Stop hook אוכף).

⚠️ **ושלוש נקודות שהתבנית מוסיפה במודול הזה ולא היו קודם:**
**‏(1)** ‏**ראיון-ה-PM אינו רץ** — יש אפיון מאושר; שאלה נקודתית במקום ראיון.
**‏(2)** ‏**בקרת-הכיסוי היא "סשן-בנייה מסומלץ"** — סוכן טרי שמורשה לקרוא **רק** את רשימת-הקריאה
של `spec.md` וצועד את הבנייה, ומדווח כל נקודה שבה היה **מנחש**.
**‏(3)** ‏**נקודת-רכיבים-משותפים אחת בראש פזת-ה-UI** — לפני המסך הראשון: מה חוזר בשמונת
הכרטיסים, ומה כבר קיים ב-`src/components/`.

## ⑥ 📋 שלושת הפרומפטים להדבקה

> 💡 **בחירת מודל לפני הדבקה:** ⑥1 (פתיחה) ו-⑥3 (סגירה) — **Opus, מאמץ גבוה** *(בלופרינט · RLS ·
> אודיט)*. ⑥2 (המשך-בנייה) — לפי טבלת **Model & effort** שבראש §5 של מדריך-המיקרו
> *(ברירת-מחדל: Sonnet לפזות בנייה, Opus לפזות DB/RLS ובקרה)*.

**1) פתיחת מודול (פעם אחת):**
```
אנחנו בפרויקט REG-IN. קרא את CLAUDE.md ואת STATUS.md.
אנחנו פותחים את מודול 4 — דיילות + Smart Match, על ענף ishay/module-4-hostesses
(הענף כבר קיים ומסונכרן — אל תיצור אותו מחדש).

🔴 ולפני הכל — קרא את docs/specs/module_04_hostesses/spec.md במלואו.
זהו האפיון המאושר של המודול (שיחת Discovery עם ישי, 05–08/08/2026), והוא גובר על
C5/C6 בכל מה שהוא מכסה בפועל — פר-פריט, לא פר-תיקייה.
בסעיף ① שלו יושבת רשימת-הקריאה המלאה שלך: 13 פריטים בסדר מחייב. אל תמציא רשימה משלך.
⚠️ ובפרט: ממסמך-האלגוריתם קרא §11 בלבד — הוא נכתב במפורש בשביל סשן-בנייה.
⚠️ ומ-PROJECT_MASTER §7 קרא לפחות: 6 · 14 · 15 · 16 · 29 · 33 · 41 · 43 · 44 · 45 · 47 ·
54 · 63 · 64 · 66 · 67 · 69. חלקם הוחלפו או נסגרו — קרא את הסטטוס, לא רק את הכותרת.

קרא בעצמך את התבנית .claude/skills/module-blueprint/template.md ובצע אותה כלשונה עם:
MODULE_NUMBER=4 · MODULE_NAME=דיילות + Smart Match · BRANCH_NAME=ishay/module-4-hostesses
RELEVANT_SECTIONS=§5.10-5.12 + רשימת §7 שלמעלה

חובה: החוסמים הפתוחים מרוכזים ב-spec.md, בפרק "מה אסור לבלופרינט לנחש" (⑱ ⑲ ⑬ ⑭ ⑯).
הם נכנסים לטריאז' 🛑 שלך ומגיעים אליי מרוכזים, עם המלצה מנומקת לכל אחד — אל תכריע בהם לבד.
הצג את הבלופרינט לאישורי; רק אחרי שאאשר — שמור אותו כ-docs/micro_guides/module-4.md.

לפני כל פעולה ענה על החמש האלה — כל תשובה עם הקובץ או הפקודה שהוכיחה אותה.
אל תנחש, ואל תענה מהמסמכים בלבד אם אפשר לוודא חי:
1. מה המצב הנוכחי של המשימה, לפי מה שקראת?
2. מה בדיוק המשימה שלך, ומה במפורש לא?
3. מה אסור לך להחליט לבד כאן?
4. מה כבר הוכרע ואסור לפתוח מחדש?
5. מה המוקש שאם תפספס אותו — זה יישבר בשקט?

ואז שתי שאלות שלך אליי: מה לא ברור לך? מה חסר בהוראות שנתתי?
```

> 🔑 **למה חמש השאלות כאן (‏08/08/2026), ומה כל אחת תפסה בפועל:** הן רצו שלוש פעמים ביום אחד
> והחזירו בכל פעם. **‏#5 היא המשתלמת** — היא מכריחה להצהיר בקול על מה שעלול להישבר **בלי שאיש
> ישים לב**, וזה בדיוק סוג התקלה ששתי הבדיקות של אותו יום מצאו *(שם-תיקייה עם אפס חסר ·
> תווית-סטטוס שגויה — שניהם היו עוברים כל קריאה אנושית)*.
> ⚠️ **ו-#4 קריטית לבלופרינט הזה במיוחד:** ‏**ישי הכריע חמש הכרעות ב-08/08** *(מנוף-ההוגנות ·
> מסך מבט-העל · בעלות "מוכן לביצוע" · `rating` · אפס-לוגיסטיקה)*. **בלי #4 הן יובאו אליו שוב.**
> 🚫 **ולא הוכנס ל-11 המדריכים האחרים בכוונה** — אותה הוראה ב-12 עותקים סוטה זה מזה, ומודולים
> 5–12 עוד לא עברו Discovery כך שהבלוק שלהם ישתנה ממילא. **התבנית לשכפול חיה במקום אחד:**
> [`docs/guides/module_playbook.md`](../module_playbook.md), בבלוק-ההדבקה של שלב 1.

**2) המשך בנייה (בכל סשן עבודה, עד שכל הצעדים ✅):**
```
אנחנו בפרויקט REG-IN. קרא את CLAUDE.md, STATUS.md ואת docs/micro_guides/module-4.md.

🔴 וקרא גם את docs/specs/module_04_hostesses/spec.md — הוא האפיון המאושר של המודול,
ומדריך-המיקרו אינו מחליף אותו. שלושה דברים חיים שם ורק שם:
· אוצר-המילים הנעול — כל סטטוס וכל תווית-מסך מילה-במילה (פריט-חוזה 1);
· "מה ייחשב עובד" — שבעה קריטריונים, כולל המספר בר-הבדיקה נועה 0.67 · מיכל 0.66 · דנה 0.64;
· "מה אסור לבלופרינט לנחש" — המלכודות שכבר נמדדו.
🚫 אל תמציא תווית, ואל תכתוב "מה ייחשב עובד" משלך למה שכבר כתוב שם — צטט משם.
⚠️ ופער שנראה לך טכני-בלבד: בדוק קודם אם האפיון כבר עונה עליו, לפני שאתה מכריע לבד.
🎨 ושמונת המוקאפים ב-docs/mockups/hostesses-screen/approved/ מאושרים (HTML — קרא, אל תנחש):
בונים אותם כפי שהם. לא מציירים מחדש ולא מבקשים עליהם אישור שוב — כבר אישרתי אותם.
המצבים והוולידציות של כל מסך יושבים בכרטיס שלו ב-screens-approved.md — משם, לא מהמצאה.

המשך מ"הצעד הפעיל" שבכותרת המצב. אמת בעצמך כל נקודת עצירה 🤖 (הרץ את פקודת האימות והצג ראיה),
ועצור לאישורי בכל נקודת 👤 ובסוף כל פזה.
נכנסת לפזה חדשה? אסוף קודם את כל השאלות הפתוחות/ממתינות-להנהון המעוגנות אליה בלדג'ר (סעיף 3)
והצג לי אותן לסגירה במרוכז (סגנון P13) לפני הצעד הראשון של הפזה.
עדכן את מדריך המיקרו תוך כדי העבודה (סעיף 8 שלו).
בסוף הסשן: עדכן יומנים לפי הפרוטוקול ב-CLAUDE.md והסבר לי בעברית פשוטה איפה עצרנו ומה הבא.
```

> 🔑 **הפסקה החדשה בראש ⑥2 (‏08/08/2026) סוגרת פער שנמדד באותו ערב:** ‏`module-build` מעגן את
> עצמו ב-⑥2 ובמדריך-המיקרו, **ואף אחד מהשניים לא שלח את הבנאי ל-`docs/specs/`** (‏`grep` ⇒ **0**).
> ⇒ **אוצר-המילים הנעול, התוויות מילה-במילה, והמספר `0.67/0.66/0.64` לא היו מגיעים אליו** אלא
> אם מדריך-המיקרו היה מעתיק אותם במקרה. **ושלושה ממצאים נוספים היו *תוצאה* של אותו פער** —
> ולכן שלוש השורות האלה סוגרות את כולם.
> ➕ **ושורת-ה-🎨 נוספה 08/08/2026 02:02** *(בלוק 6.0, יחד עם ארבע עריכות מקבילות ב-`module-build/SKILL.md`)*:
> כלל-המוקאפים בסקיל נכתב ל-`docs/mockups/**` ב**דרגה 4** (*"וחלק מפרטיהם שגויים"*) ⇒ מיושם כלשונו,
> הוא **מצייר מחדש ומבקש אישור על שמונה מסכים שכבר אישרת**. הסקיל תוקן במבחן גנרי *(מוקאפ שהאפיון
> מונה כמאושר = דרגה 2)*, **והשורה כאן היא הנתיב השני** — ⑥2 מגיע לבנאי גם אם הסקיל לא נטען.

**3) סגירת מודול (פעם אחת, כשכל הצעדים ✅):**
```
אנחנו בפרויקט REG-IN. קרא את CLAUDE.md, STATUS.md ואת docs/micro_guides/module-4.md.
קרא בעצמך את התבנית .claude/skills/module-close/template.md ובצע אותה כלשונה עם:
MODULE_NUMBER=4 · MODULE_NAME=דיילות + Smart Match · BRANCH_NAME=ishay/module-4-hostesses
אימות-הקבלה נעשה מול "מה ייחשב עובד" שב-docs/specs/module_04_hostesses/spec.md — שבעת
הקריטריונים, לא רשימה שתמציא.
בסוף: בצע את סעיף ההתמדה (עדכון מדריך המיקרו + היומנים, תאריך+שעה) והדפס לי את הוראות ה-PR
בעברית + פרומפט מסירה 🧩 לתוסף הכרום (כלל ברזל 17) לפתיחת ה-PR בפועל.
```

## ⑦ בדיקת קבלה

🔴 **הרשימה המחייבת היא "מה ייחשב עובד" ב-`spec.md` — שבעה קריטריונים.** מובאים כאן כדי שתוכל
לסמן בעצמך, **ו-`spec.md` הוא הקובע** *(עד 08/08 עמדו כאן ארבעה פריטים בלבד, וזה היה חצי מהמבחן)*:

- [ ] **המנהלת פותחת את המבט-על ויודעת מיד לאיזה אירוע להיכנס** — הסדר עונה על זה, לא רשימה.
- [ ] **‏Smart Match מחזיר רשימה מדורגת שהיא יכולה להסביר לעצמה** — כל שורה נושאת **למה** היא שם
      *(צ'יפי-הנמקה; **הציון עצמו נשאר סמוי**)*.
- [ ] **הדיילות עונות דרך הקישור בלי להתחבר**, והמסך מראה מה קרה עם כל זימון.
- [ ] 🔴 **מסך שלא הצליח לטעון אומר זאת. לעולם לא רשימה ריקה בשקט** *(מלכודת
      `{data:null, error:null}` — ‏RLS בלי policy)*.
- [ ] **אי-אפשר לשבץ דיילת לשני אירועים באותו יום — והמסד חוסם, לא הקוד.**
- [ ] **חור באיוש נראה לעין עד הרגע האחרון**, כולל ב-T-24 שבו המסך מחליף מצב.
- [ ] 🎯 **הציון שחושב ביד באפיון תואם למה שהקוד מחשב — `נועה 0.67 · מיכל 0.66 · דנה 0.64`,
      ובסדר הזה.**
- [ ] `npm run verify` ירוק; PR נפתח.

## ⑧ אם משהו השתבש

- **מסך ריק בטבלת `hostesses` / ברשימת האירועים** → ‏🔴 **החשוד הראשון: `projects` היא `deny-all`
  ומ4 חייב policy-קריאה עליה** — בלעדיה המסך הראשון חוזר ריק לכולם, **כולל המנכ"ל, בלי שגיאה.**
- **דיילת שהצהירה אי-זמינות עדיין מוצעת** → כנראה טבלת-אי-הזמינות נשלחה `deny-all` כמו אחיותיה;
  **תנאי-השער החמישי מת בשקט.**
- **`verify` אדום** → ‏`regin-pr-gate` תאבחן *(רוטינה — **אתה** לוחץ "Run now", לא Claude)*.
- **תקוע?** → [ספריית הפרומפטים המצביים](../prompt_library.md) — חילוץ, סטייה מהתוכנית, undo, קונפליקט.

</div>
