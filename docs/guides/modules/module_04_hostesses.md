<div dir="rtl">

# 👥 שלב — מודול 4: דיילות + Smart Match

> מקומו בהיררכיית האמת: `schema.sql` › אפיון › מוקאפים › מדריך זה. בסתירה — המדריך מפסיד.
> צעדים שבוצעו מסומנים ✅ — המדריך כתוב כאילו כלום לא קיים.

## ① מה נשיג בשלב הזה

מאגר דיילות מלא (הוספה, עריכה, מחיקה רכה) + אלגוריתם שיבוץ חכם (Smart Match) שמדרג דיילות פנויות לאירוע לפי דירוג, קרבה גיאוגרפית ומהימנות. מסך 5.10–5.12 באפיון.

## ② תנאי כניסה

- [ ] מודול 1 מוזג ל-`dev` ([מדריך סגירת מודול 1](../modules/module_01_close.md) הושלם).
- ✅ **אשכול השיבוץ הוכרע (07/07/2026, ישי — אודיט הפריטים הפתוחים)** — כבר לא לשאול, רק ליישם לפי [PROJECT_MASTER §7](../../PROJECT_MASTER.md):
  > 🔗 **מראת §7.6+14+15+16+29+33+43+44+45+47** — SSOT: ‏PROJECT_MASTER §7 (לא לערוך כאן ידנית; מסונכרן ע"י `regin-docs-sync`)
  - **§7.6** כתובת: להוסיף `hostesses.address` + `lat`/`lng` (קואורדינטות מגאוקוד).
  - **§7.14** Smart Match: **מרחק אווירי** (haversine/`earthdistance` ב-Postgres/Backend) — **בלי Maps API בתשלום**; נירמול לינארי-הפוך עם תקרה.
  - **§7.29** double-booking: התנגשות = אותה דיילת+תאריך + לפחות אחד "ארוך", או פער <שעה. שני "קצרים" עם ≥שעה = מותר. בדיקה ברמת API.
  - **§7.33** עודף זימונים: משרה התמלאה → "זמניות" עודפות → `released` אוטומטי; ניסיון-אישור מציג "המשרה כבר אוישה".
  - **§7.45** קישור-זימון: endpoint ציבורי + `assignments.invite_token`, פקיעה = מוקדם מבין {אוישה / 48ש' משליחה / 24ש' לפני האירוע}. **ערוץ (מייל/SMS) + שירות = דיון בבנייה, לא עכשיו** (M4 מגדיר token+endpoint, שליחה=M10).
  - **§7.16** היעדרות/איחור + מדיניות ביטול: המנהלת מסמנת בסגירה איחור (קצת/בינוני/הרבה) או אי-הגעה (חולה/אישור-מראש/הבריזה); בלי הגעה=בלי תשלום. **ביטול פרויקט:** `cancelled_at`+`cancellation_reason` → שכר 0/50/100% לפי >72/24–72/<24ש', force-majeure=0%.
  - **§7.43** `ready` = מצב נגזר והפיך (טריגר מחזיר אחורה אם דיילת בוטלה); **§7.44** `project_status` נכתב בטריגר DB אטומי.
  - **§7.15** — W3="מהימנות" (לא "עומס") — **↳ הורחב (07/07 ערב):** לא רק שם — הנוסחה עצמה מוגדרת בשלוש דרכים שונות באפיון (ספירת-משמרות / מהימנות / שיעור-השתתפות) + חלון ה"רבעון" לא מוגדר; להכריע כאן.
  - **↳§7.45** — מי כותב `confirmed_available`: הדיילת דרך הקישור (C6) או המנהלת (C5 "אושרה זמנית")? להכריע בבנייה.
- 🛑 **חדשות מביקורת-האפיון (07/07 ערב):** ‏**§7.55** קואורדינטות צד-האירוע + בחירת שירות-גאוקוד חינמי + דין-NULL (בלעדיהן אין W2) · להנהון: **§7.54** מניעת שיבוץ-כפול-פעיל באותו פרויקט (unique חלקי/בדיקת-API — כפל-שכר).
  - 📋 **תוספות-סכמה מרוכזות ב-§7.47** (address/lat/lng · זמני-אירוע+קצר/ארוך · cancelled_at/reason · invite_token · שדות-נוכחות) — כל אחת מיגרציה בעלת-שם + עדכון schema.sql + שורת-Done ב-db_roadmap §10.
- 🗡️ **חדשות מאודיט-הערעור המבני (08/07/2026)** — (רשימה: ‏[PROJECT_MASTER §7](../../PROJECT_MASTER.md); המפה: ‏`docs/db_roadmap.md`): ‏**§7.67** 🔴 **שיוך שיבוץ לשורת-שירות/משמרת** — בלעדיו אין שעות-פר-דיילת (§7.19), אין חלון-זמן ל-double-booking (§7.29) ואין רב-יומי (§7.30); לבלופרינט · ‏**§7.63** הגנת-עמודות `bank_*`+ת"ז (‏RLS שורתי בלבד — כל תפקיד עם קריאת-שורות רואה הכול) · ‏**§7.64** ✅✅ **אושר-לביצוע (ישי, 31/07/2026) — זהו הצעד הראשון של המודול: ת"ז → surrogate** (כמו ח"פ ב-customers). ‏`id_number` יורד לעמודה עסקית `unique not null`, וה-PK הופך `hostess_id bigint generated always as identity`; ‏`assignments` עובר ל-PK מורכב `(project_id, hostess_id, assignment_number)`, וכן ה-FK, ‏`partial UNIQUE(hostess_id, event_date)` (§7.54) והאינדקס C-1. **נמדד חי 31/07: `hostesses` ו-`assignments` = 0 שורות ⟹ אין מיגרציית-נתונים**, ו-`id_number` אינו מופיע כלל ב-`src/`. **תבנית להעתקה: `supabase/migrations/20260710160735_module2_customers_surrogate_key_rls_and_marketing.sql`** (עשתה בדיוק זאת ל-`customers`, כולל RLS). מבוצע בתיאום עם מודל assignments (§7.67/54) + סטיית-C6 §2.4.x (שמצייר ת"ז כ-PK). ⚠️ **המיגרציה דורשת אישור-הקלדה מישי** · ‏**§7.65** ✅ **הוכרע 31/07/2026: `hostesses.email` בלי UNIQUE** — הת"ז כבר מונעת כפילות-אדם, וכתובת משפחתית משותפת היא מקרה לגיטימי באוכלוסיית עובדי-שטח. **מה כן לבנות: אזהרה רכה בטופס** ("כתובת זו כבר רשומה אצל *שם* — להמשיך?"), מזהירה ולא חוסמת. ‏🔗 מראת §7.65 — SSOT: `PROJECT_MASTER §7` (לא לערוך כאן ידנית) · ‏**§7.66** מנגנון אכיפת שכר-מינימום · ‏**§7.69** "+ נסיעות" בתבנית-הזימון מול מודל-השכר (להנהון) · ‏**§7.54** ‏unique-חלקי לשיבוץ-פעיל (להנהון — ר' db_roadmap ‏A-15).

## ③ חלוקת עבודה

| ישי | Claude |
|---|---|
| מכריע בשאלות הפתוחות; בודק תסריטי שיבוץ בעצמו | בונה בלופרינט (`docs/micro_guides/module-4.md`) דרך התבנית, מיישם, בודק |

## ④ החלק שלך

1. הכרע בשאלות ה-🛑 מסעיף ②, או תגיד ל-Claude "תציע ברירת מחדל סבירה ואני אאשר".
2. צור ענף: `git checkout dev && git pull && git checkout -b ishay/module-4-hostesses`.
3. הדבק את הפרומפט בסעיף ⑥. אשר את הבלופרינט. אמור "בוא נעשה צעד 1" וכן הלאה עד הסוף.
4. בסוף — הדבק את פרומפט הסגירה (גם בסעיף ⑥).

## ⑤ החלק של Claude

בונה בלופרינט מלא לפי `.claude/skills/module-blueprint/template.md` (פזות: DB/RLS → לוגיקה עסקית `src/lib`+`api.js` → UI → בקרה → QA), עם טריאז' 🛑/⏳ לשאלות שנותרו פתוחות. **מציית לשפת העיצוב וטבלת הצבעים ב-PROJECT_MASTER §4.** מודול זה נשען על RLS/Auth ממודול 1 — מצהיר במפורש איך (איזה תפקידים עורכים/צופים ב-`hostesses`, לפי המודל שהוכרע ב-[§7.21](../../PROJECT_MASTER.md)). **מתחזק את `docs/micro_guides/module-4.md` חי לאורך כל הבנייה** — סטטוס צעדים, סטיות ("↳ as-built") וחוב טכני מתעדכנים באותו סשן שבו קרו (ה-Stop hook אוכף זאת).

## ⑥ 📋 שלושת הפרומפטים להדבקה

> 💡 **בחירת מודל לפני הדבקה:** פרומפט 1 (פתיחה) ופרומפט 3 (סגירה) — **Opus, מאמץ גבוה** (חשיבה: בלופרינט, RLS, אודיט). פרומפט 2 (המשך-בנייה) — לפי טבלת **Model & effort** שבראש סעיף 5 של מדריך המיקרו (ברירת מחדל: Sonnet לפזות בנייה, Opus לפזות DB/RLS ובקרה).

**1) פתיחת מודול (פעם אחת):**
```
אנחנו בפרויקט REG-IN. קרא את CLAUDE.md, STATUS.md, docs/guides/modules/module_04_hostesses.md
ואת docs/PROJECT_MASTER.md (סעיפים 5.10–5.12, §7 פריטים 6, 14, 15, 16).
אנחנו פותחים את מודול 4 — דיילות + Smart Match, על ענף ishay/module-4-hostesses.

🔴 ולפני הכל — קרא את docs/specs/module_04_hostesses/spec.md.
זהו האפיון המאושר של המודול (הופק בשיחת Discovery עם ישי, 05–08/08/2026), והוא
גובר על C5/C6 בכל מה שהוא מכסה בפועל — פר-פריט, לא פר-תיקייה.
בתוכו, בסעיף ①, יושבת רשימת-הקריאה המלאה שלך: מה עוד לפתוח ובאיזה סדר.
⚠️ ובפרט: ממסמך-האלגוריתם קרא §11 בלבד — הוא נכתב במפורש בשביל סשן-בנייה.

קרא בעצמך את התבנית .claude/skills/module-blueprint/template.md ובצע אותה כלשונה עם:
MODULE_NUMBER=4 · MODULE_NAME=דיילות + Smart Match · RELEVANT_SECTIONS=§5.10-5.12, §7.6/14/15/16/29/33/43/44/45/47 · BRANCH_NAME=ishay/module-4-hostesses

חובה: "שאלות פתוחות" מסעיף ② במדריך שלי נכנסות לטריאז' 🛑/⏳ שלך — אל תכריע בהן לבד.
הצג את הבלופרינט לאישורי; רק אחרי שאאשר — שמור אותו כ-docs/micro_guides/module-4.md.
```

**2) המשך בנייה (בכל סשן עבודה, עד שכל הצעדים ✅):**
```
אנחנו בפרויקט REG-IN. קרא את CLAUDE.md, STATUS.md ואת docs/micro_guides/module-4.md.
המשך מ"הצעד הפעיל" שבכותרת המצב. אמת בעצמך כל נקודת עצירה 🤖 (הרץ את פקודת האימות והצג ראיה),
ועצור לאישורי בכל נקודת 👤 ובסוף כל פזה.
נכנסת לפזה חדשה? אסוף קודם את כל השאלות הפתוחות/ממתינות-להנהון המעוגנות אליה בלדג'ר (סעיף 3)
והצג לי אותן לסגירה במרוכז (סגנון P13) לפני הצעד הראשון של הפזה. עדכן את מדריך המיקרו תוך כדי העבודה (סעיף 8 שלו).
בסוף הסשן: עדכן יומנים לפי הפרוטוקול ב-CLAUDE.md והסבר לי בעברית פשוטה איפה עצרנו ומה הבא.
```

**3) סגירת מודול (פעם אחת, כשכל הצעדים ✅):**
```
אנחנו בפרויקט REG-IN. קרא את CLAUDE.md, STATUS.md ואת docs/micro_guides/module-4.md.
קרא בעצמך את התבנית .claude/skills/module-close/template.md ובצע אותה כלשונה עם:
MODULE_NUMBER=4 · MODULE_NAME=דיילות + Smart Match · BRANCH_NAME=ishay/module-4-hostesses
בסוף: בצע את סעיף ההתמדה (עדכון מדריך המיקרו + היומנים, תאריך+שעה) והדפס לי את הוראות ה-PR
בעברית + פרומפט מסירה 🧩 לתוסף הכרום (כלל ברזל 17) לפתיחת ה-PR בפועל.
```

## ⑦ בדיקת קבלה

- [ ] הוספת דיילת חדשה עובדת (טופס + ולידציה).
- [ ] Smart Match לאירוע מדומה מחזיר רשימה מדורגת של דיילות פנויות — הניקוח לא מוצג למשתמשת, רק הסדר.
- [ ] שיבוץ דיילת לאירוע נשמר; דיילת לא ניתנת לשיבוץ כפול בתאריך זהה.
- [ ] `npm run verify` ירוק; PR נפתח.

## ⑧ אם משהו השתבש

- מסך ריק בטבלת `hostesses` → כנראה חסרות RLS policies — ראה פרוטוקול ב-`CLAUDE.md`.
- verify אדום → `regin-pr-gate` תאבחן.
- תקוע? → [ספריית הפרומפטים המצביים](../prompt_library.md) — חילוץ, סטייה מהתוכנית, undo, קונפליקט ועוד.

</div>
