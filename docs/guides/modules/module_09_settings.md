<div dir="rtl">

# ⚙️ שלב — מודול 9: הגדרות מערכת

> מקומו בהיררכיית האמת: `schema.sql` › אפיון › מוקאפים › מדריך זה. בסתירה — המדריך מפסיד.
> צעדים שבוצעו מסומנים ✅ — המדריך כתוב כאילו כלום לא קיים.
>
> 📘 **02/09/2026 20:5X — המודול נפתח: הבלופרינט אושר ע"י ישי. המדריך המחייב מכאן: `docs/micro_guides/module-9.md`.**
> **אין `docs/specs/module_09_*/` — הכרעת-ישי (R-1): מיקרו-גייד עם ראיון-מוצר קצר, בלי Discovery מלא.**
> **מה נבדק בפתיחה (02/09):** המסד החי (39 שורות · 5 סוגים · 2 policies · הרשאות · 4 ה-FKs ל-`users.email`) · הקוד (כל קוראי `params`, ששת הקבועים וכל אתרי-הקריאה שלהם) · המוקאפ (`settings-screen/01_…`) · §7.21/§7.35/§7.64/§7.66/§7.70/§7.80/§7.83/§7.84/§7.90 · ארבע שורות `🚧 מ9` ב-§6 · C5 §5.6.16/§5.6.17/§5.8 · C6 §2.4.9. **מה לא נבדק:** רשימת-הקריאה של ⑥1 מעולם לא תוקנה כולה — רק המצביע `§5.16` (למטה); ההנחות על מצב-הכניסה בסעיף ② נשארו כפי שנכתבו ביולי.
> ⚠️ **תיקון-מצביע:** בסעיף ⑥1 עמד `RELEVANT_SECTIONS=§5.16` — **סעיף כזה אינו קיים ב-C5** (C5 §5 משתרע על 5.2–5.8; מסך-ההגדרות הוא **C5 §5.6.16**, ו-`§5.16` הוא כותרת ב-`PROJECT_MASTER`). תוקן 02/09/2026.

## ① מה נשיג בשלב הזה

מסך "הגדרות מערכת" (מנכ"ל בלבד) לעריכת טבלת `params` בלי SQL: מע"מ, יחס אורחים-לדיילת, משקולות Smart Match, תבניות מייל. מסך 5.16 באפיון.

## ①א 🔎 מצב-הקרקע — נמדד `28/08/2026`, אחרי סגירת מ8

🔴 **‏§① למעלה מדבר על "מסך" — וזה כבר לא מדויק, וזו החדשה הטובה.** המסך קיים ועובד.

**‏`SystemManagementPage` חי עם ארבע לשוניות:** ‏`ניהול משתמשים` ✅ · `הרשאות` ✅ ·
`מחירים` ✅ · **`פרמטרים` 🔴 `<UnderConstruction />`** (‏`App.jsx`, הראוט `params`).
⇒ **מ9 אינו בונה מסך — הוא ממלא לשונית רביעית שכבר חצובה**, בתוך מסך עם ניווט והרשאות שכבר נבנו.

**🔢 והמדידה שקובעת את הגודל — ‏39 פרמטרים במסד, מהם 2 ניתנים לעריכה:**

| `param_type` | כמה | הערה |
|---|:--:|---|
| `templates` | **13** | גופי כל 13 מיילי-המערכת |
| `smart_match` | **13** | משקולות · שערי-מרחק · חלונות-חישוב |
| `control_alerts` | **6** | סולם דמי-ביטול · סף לקוח-רדום · תזכורות |
| `pricing_timing` | **6** | מע"מ · יחס-אורחים · תוקף-הצעה · שכר-מינימום · תנאי-תשלום |
| `integration_tech` | **1** | מייל רואה-החשבון |

**הנגיש היום:** ‏`PricingParamsCard` — **והערת-הקוד שלו אומרת זאת במפורש**: *"פרמטרי-תמחור —
**2 בלבד, לא כל `params`**"*. השניים: `אחוז_מעמ` · `יחס_אורחים_לדיילת`.
⇒ **‏37 מתוך 39 חסרי מסך-עריכה.** ‏**וזה גם התקדים:** אותו מסך כבר מוכיח את הדפוס.

🔑 **וההכרעה של ישי שקבעה את המסגור הזה (28/08/2026):** *"נראה לי זה להוסיף פרמטרים שפשוט לא
קיימים שם עדיין, ויש הרבה"* — **ומדידה אישרה אותו.** ⇒ **מ9 = השלמת-לשונית + הוספה, לא מודול-מסך.**

⚠️ **ושני דברים שהמסגור הזה מסתיר, ושניהם משפיעים על ההקצאה "~יומיים":**
**‏(א) ‏13 התבניות אינן שדה-טופס.** גוף-מייל הוא טקסט ארוך עם **משתנים בתוכו** (שם-לקוח,
מספר-הצעה); עריכה חופשית שמוחקת משתנה **שוברת מייל בשקט**. זהו עורך-תוכן, לא `input`.
**‏(ב) העדפות-ההתראות אינן `params` כלל** — טבלה **חדשה** + הדלקת הטוגלים שנמסרו מנוטרלים
ב-`ProfileSettingsPage` (הכרעת-ישי 07/07/2026, ‏`PROJECT_MASTER §6`). **זה החלק היחיד במ9
שנבנה מאפס.**

## ①ב 🎨 איך לסדר את זה על המסך — מה מקובל, ומה מתאים כאן

**נבדק בפועל 28/08/2026** *(מקורות בתחתית)*. **המוסכמה, וכל אחת עם בדיקת-התאמה:**

**‏① קיבוץ לקטגוריות לוגיות — ✅ מאמצים, והוא בחינם.** המוסכמה: *"קיבוץ חכם מאפשר למשתמש
לאתר מהר; היררכיה ויזואלית ברורה שומרת עליו מכוון"*. 🔑 **ואצלנו הקיבוץ כבר קיים בדאטה** —
‏`param_type` נותן חמש קבוצות. ⇒ **לא ממציאים טקסונומיה; קוראים אותה מהמסד.**

**‏② חיפוש בתוך ההגדרות — ✅ מאמצים.** המוסכמה מציינת חיפוש כ*"הדרך המהירה ביותר"* ברשימות
ארוכות. **‏39 שורות זה מעבר לסריקה-בעין.** ⇒ שדה-חיפוש אחד שמסנן חוצה-קבוצות.

**‏③ עמוד נפרד לכל קטגוריה עם URL משלה — 🚫 לא כאן.** המוסכמה מייעדת זאת ל*"אפליקציות מורכבות
עם הרבה הגדרות שנהנות מקישור-עמוק"*. **‏39 פריטים בחמש קבוצות אינם מצדיקים רמת-ניווט שנייה
בתוך לשונית שכבר יושבת בתוך טאב-בר.** ⇒ **קטע לכל קבוצה בעמוד אחד** מנצח.

**‏④ חשיפה-הדרגתית (progressive disclosure) — ✅ אבל רק על שני מקומות, לא כעיקרון גורף:**
· **‏13 התבניות** — לא שורות ברשימה; **עורך משלהן**, עם רשימת המשתנים המותרים גלויה.
· **‏13 משקולות Smart Match** — **שינוי משקולת משנה התאמות בפועל.** אזור נפרד עם אזהרה,
ולא לצד "אחוז מע"מ" באותו טופס. *(מתחבר לחוב `מרכיב_אמינות_פעיל` שכבר רשום ב-§6.)*

**‏⑤ סרגל-צד לניווט מקונן — 🚫 נדחה.** מקובל ל*"רשימות ארוכות והיררכיות מקוננות בפאנלי-אדמין"*,
**ואצלנו כבר יש טאב-בר**; רמת-ניווט שלישית בתוך לשונית היא בדיוק המקום שהמקורות מזהירים בו
*"להבחין ויזואלית בין כל רמה"* — עלות שאין לה כאן תמורה.

> **‏🔴 והמבחן שצריך לעבור לפני שכותבים שורת-קוד:** ‏`params` הוא **טבלת-תשתית שכל המערכת
> קוראת** — מע"מ, סולם-ביטולים, משקולות-שיבוץ. **ערך שגוי כאן אינו באג במסך אחד; הוא באג
> שקט בכל המודולים.** ⇒ **ולידציה פר-טיפוס היא הפיצ'ר, לא קישוט** — ומה שנדרש לכל טיפוס
> הוא הכרעה של ישי, לא של הבונה.

### 🖼️ והמוקאפ עצמו — `docs/mockups/settings-screen/01_params_tab_proposal.html`

**ארבע תצוגות, כל הערכים אמיתיים** (נשלפו מהמסד `28/08/2026`): סוג פשוט נבחר · עורך-התבניות ·
משתנה שנמחק והמסך אומר מה נשבר · והסוג המסוכן עם אזהרה ואילוץ-סכימה.
✅ **הכיוון אושר ע"י ישי `28/08/2026`** *(«אהבתי ממש… נראה ידידותי למשתמש»)* — **הכיוון, לא הפרטים:**
המוקאפ יושב מחוץ ל-`approved/` בכוונה, והתצוגות עצמן ייבחנו שוב בשער-ה-🎨 של הבנייה.
🔴 **ובתחתית הקובץ יש בלוק `מה הכרעתי לבד` עם שישה פריטים** — ביניהם **אילוץ-הסכימה `1.00`,
שנגזר מהדאטה החיה ולא מהאפיון, וטעון אישור:** האם זה אילוץ קשיח או צירוף-מקרים בערכי-הזריעה?

**מקורות:** [SaaSUI — Settings patterns](https://www.saasui.design/pattern/settings) ·
[Bricx — 8 Settings Page UI Examples](https://bricxlabs.com/blogs/settings-page-ui-examples) ·
[Eleken — UX navigation patterns](https://www.eleken.co/blog-posts/ux-navigation-design)

## ①ג 🔢 סריקת-פרמטרים — נמדדה `02/09/2026`, אחרי סגירת מ8

**‏① הטבלה עצמה במצב טוב.** הצלבת כל 39 השמות מול **כל** קוד-הייצור (`src/`, `supabase/functions/`,
`scripts/`, בלי קובצי-בדיקה) ⇒ **‏37 נקראים.** שניים לא נמצאו בשום מקום, **ושניהם מוסברים ולא מתים**:
`שעות_תזכורת_לדיילת` (מנוע-ההתראות הוא מ10) · `תבנית_איפוס_סיסמה` (‏Supabase Auth שולח את המייל הזה
בעצמו — **מועמד אמיתי למחיקה, טעון בדיקה**).

**‏② והשאלה ההפוכה היא המעניינת: מה מקודד קשיח ואמור להיות פרמטר.** סריקת `src/lib/` (לוגיקה עסקית
בלבד) החזירה **13 קבועים מספריים**. הסיווג — **שבעה מועמדים, שישה טכניים:**

| מועמד לפרמטר | הקובץ | הערך | למה |
|---|---|:--:|---|
| `SATISFACTION_ATTENTION_MAX` | `customers.js` | 3 | 🔴 **ר' הממצא למטה** |
| `INVITE_CUTOFF_HOURS_BEFORE_EVENT` | `hostesses.js` | 24 | מתי מפסיקים לשלוח זימונים |
| `INVITE_VALIDITY_HOURS` | `hostesses.js` | 48 | כמה זמן לדיילת לענות |
| `URGENT_EVENT_HOURS` | `hostesses.js` | 72 | מה נחשב אירוע דחוף |
| `QUARTER_WINDOW_DAYS` | `hostesses.js` | 90 | חלון-הרבעון לסטטיסטיקה |
| `AMBER_BUSINESS_DAYS` | `projectLogistics.js` | 10 | סף-אזהרה לוגיסטי |
| `EXPIRING_SOON_DAYS` | `quotes.js` | 7 | ⚠️ **תוקף-ההצעה כבר פרמטר (`ימי_תוקף_הצעה`=30), והאזהרה עליו לא** |

**נשארים בקוד ולא הופכים לפרמטר** — `EARTH_RADIUS_KM` (פיזיקה) · `MAX_GEOCODE_ATTEMPTS` ·
`MIN_LOCALITY_PREFIX` · `MAILTO_MAX_CHARS` (מגבלת-דפדפן) · `NEUTRAL_PROXIMITY_SCORE` (פנים-אלגוריתמי) ·
`MIN_PASSWORD_LENGTH` (**מדיניות-אבטחה — מסך-הגדרות שמתיר להוריד אותה הוא אנטי-דפוס**).

> 🔴 **הממצא שמצדיק את הסריקה כולה: סף-שביעות-הרצון `3` חי בארבעה מקומות, ואיננו פרמטר.**
> ‏`src/lib/customers.js` (`SATISFACTION_ATTENTION_MAX`) · ‏`record_feedback` במסד
> (`case when p_score < 3 then p_reason else null end`) · שער-הארכוב ב-`archive_project` ·
> והשער במסך-הסגירה. **אותו כלל עסקי, ארבעה עותקים.**
> 🩸 **וזו בדיוק משפחת-הכשל של `H2`/`H3`** — כלל שחי בשני מקומות, **ושניהם היו שגויים**; היא עלתה
> שתי מיגרציות. **כאן יש ארבעה.** ⇒ **מועמד ראשון להפוך לפרמטר, ולא בגלל נוחות-עריכה אלא כדי
> שיהיה לו מקור אחד.**

**‏③ מה שהסריקה עדיין לא כיסתה, ונאמר כדי שלא ייקרא כ"נסרק":** ערכים קשיחים **בתוך פונקציות-המסד**
(נבדק רק סף-שביעות-הרצון) · ערכים בקומפוננטות (`src/modules/`) ולא רק ב-`src/lib/` · והפרמטרים שטבלת
**העדפות-ההתראות** תזדקק להם.

## ①ה 📐 והאם הפריסה סופגת את מה שהסריקה מצאה — נבדק `02/09/2026`

**שאלת-ישי:** *"מבחינת הממשק, איך שבנינו את זה — נוכל להכניס הכל בצורה נעימה וידידותית?"*

**התשובה מתפצלת, וזה החלק המעניין: הפריסה כן, הטקסונומיה לא.**

**‏① הפריסה סופגת בלי מאמץ — וזה בדיוק מה שהיא נבחרה בשבילו.** ‏7 המועמדים מעלים את הטבלה מ-39
ל-**46**, ורשימת-הסוגים גדלה **בשורה אחת.** ⚠️ **והשוואה שכדאי לזכור:** בגרסה שנפסלה (חמישה קטעים
בעמוד אחד) אותה תוספת הייתה מייצרת **קטע שישי** ודוחפת את כל מה שמתחתיו מטה. **זו התוצאה הראשונה
שמאמתת את בחירת-הפריסה על נתונים ולא על טיעון.**

**‏② אבל שלושה מהשבעה אינם נכנסים לאף אחד מחמשת הסוגים הקיימים.**
`INVITE_CUTOFF_HOURS_BEFORE_EVENT` (24) · `INVITE_VALIDITY_HOURS` (48) · `URGENT_EVENT_HOURS` (72)
הם **מדיניות-זימונים** — לא משקולות-התאמה, לא תמחור, ולא התראות-בקרה. דחיפתם ל-`smart_match`
הייתה **הרחבת משמעות של סוג קיים** כדי להימנע מיצירת סוג חדש, וזה בדיוק איך טקסונומיה מתקלקלת.
⇒ **מומלץ סוג שישי — `shift_invites` / "שיבוץ וזימונים".** *(הכרעת-ישי נדרשת: זהו `param_type` חדש.)*

**והארבעה שכן נכנסים נקי:** סף-שביעות-רצון ⇐ `control_alerts` · סף-לוגיסטיקה ⇐ `control_alerts` ·
חלון-הרבעון ⇐ `smart_match` *(כבר יושבים שם `חלון_חישוב_חודשים`/`_מורחב`)* · "הצעה פגה בקרוב" ⇐
`pricing_timing` *(שם כבר יושב אחיו `ימי_תוקף_הצעה`)*.

**‏③ והעדפות-ההתראות אינן שייכות למסך הזה בכלל.** נקראו בקוד (`ProfileSettingsPage`,
`NotificationsSection`): **שני טוגלים, מנוטרלים בכוונה**, וההערה בקוד אומרת למה — *"לא מחובר ל-DB
(אין עדיין טבלת העדפות)… מנוטרל בכוונה כדי לא ליצור רושם מטעה"*:
· **מייל על פרויקטים חדשים** · **‏SMS על שינויי שיבוץ ברגע האחרון**
🔑 **ואלה העדפות פר-משתמש, לא הגדרות-מערכת** ⇒ **מקומן נשאר במסך-הפרופיל**, ומה שמ9 בונה הוא
**הטבלה שמאחוריהן + הדלקתן** — לא לשונית במסך-ההגדרות. **שני מסכים, לא אחד.**

## ①ז 🔬 בדיקת-יישימות — נמדדה `02/09/2026`, בבקשת-ישי *("בוא נראה רק אם יישים באמת")*

**‏🟢 יישים. שלוש בדיקות, ושתיים מהן חזרו טוב יותר מהצפוי.**

**‏① ההרשאות — זול יותר ממה שנאמר קודם, ואני חוזר בי מ"עבודת-מסד ששוברת את ההקצאה".**
‏`params_write_ceo_only` **אינו מקודד "מנכ"ל"** — הוא בודק `edit` על מודול `'הגדרות מערכת'`, כלומר
**כבר מבוסס-הרשאות**; השם מטעה. ⇒ הנדרש הוא **עמודת-בעלים + שכתוב policy אחד = מיגרציה אחת.**
🔑 **וה-override של המנכ"ל נופל בחינם:** נמדד — `מנכ"ל` נושא `edit` על **כל 9 המודולים** ו-`blocked`
על **אפס**, ולכן policy מבוסס-בעלים נותן לו הכול **בלי מקרה-קצה ובלי קוד מיוחד**.
📌 **ולמה זה משנה את אופי-ההחלטה כולה:** ברגע שהבעלות יושבת בעמודה היא **נתון, לא החלטת-עיצוב** —
מפת-23/07 מתעדכנת ב-`UPDATE`, לא במיגרציה ולא בקוד. *(הכרעת-ישי בשיחה: "אי אפשר לסדר את זה
במיגרציה וזהו?" — כן, וזו הצורה.)*

**‏② רשת-הביטחון לתבניות כבר קיימת, ומעוצבת נכון.** ‏`findUnknownPlaceholders` +
`fillEmailTemplate` (`src/lib/email.js`): תבנית שיש בה משתנה לא-מוכר ⇒ הפונקציה מחזירה `''` ⇒
‏`buildEmailPayload` מחזיר `null` ⇒ **השליחה אינה יוצאת.** ההערה בקוד מנמקת ומצטטת את §7.70:
*"מייל שיוצא ללקוח עם `[שם_חברה]` גולמי — נזק בלתי-הפיך מול לקוח משלם, בעוד שכפתור חסום עם הודעה
ברורה נפתר בחצי דקה."* ⇒ **המצב הגרוע ביותר הוא מייל שלא יצא, לא מייל מביך.**

**‏③ ⚠️ וחתיכת-העבודה האמיתית היחידה: רשימת-המשתנים-המותרים פר-תבנית אינה קיימת כנתון.**
המוקאפ מצייר אותה כתגיות-ללחיצה. בפועל היא **מרומזת** — חיה בתוך **5 קבצים** שכל אחד בונה את מפת-
ההחלפות שלו **ברגע השליחה**: `src/lib/email.js` · `quotes.js` · `shiftEmails.js` ·
`03_quotes/QuoteDocumentDialog.jsx` · `08_finance/api.js`.
🔴 **ובלי המיפוי הזה הכשל זז בזמן ובמקום:** המנהלת שומרת תבנית עם שגיאת-כתיב, **המסך נראה תקין**,
והכשל מתגלה **ברגע השליחה, רחוק מהמסך שבו טעתה.** ⇒ **נדרש מיפוי מוצהר: תבנית ⇐ משתנים מותרים.
‏13 תבניות, 5 קבצים — חסום ומדיד.**

**⇒ סיכום היישימות: מיגרציה אחת + מיפוי אחד. אף אחד מהם אינו נוגע בפריסה שאושרה.**
⏸️ **והמוקאפ טעון עדכון לפני אישור סופי** — כל תפקיד רואה רק את שלו *(בקשת-ישי 02/09: "צריך לעדכן
ולהראות לי שוב לאישור סופי")*.

## ①ו ✅ הכרעות-ישי `02/09/2026` — סוג שישי, ושישה מתוך שבעת המועמדים

**‏"מאשר לפי המלצתך".** מה שנסגר:

**‏① סוג שישי — `shift_invites` / "שיבוץ וזימונים". ✅ מאושר.** שלושת ספי-הזימונים עוברים אליו:
‏`INVITE_CUTOFF_HOURS_BEFORE_EVENT` (24) · `INVITE_VALIDITY_HOURS` (48) · `URGENT_EVENT_HOURS` (72).

**‏② שישה מהשבעה הופכים לפרמטרים.** שלושת הזימונים שלמעלה, ובנוסף:
· **סף שביעות-הרצון (3)** ⇐ `control_alerts`. 🔑 **והנימוק אינו נוחות-עריכה אלא מקור-אחד** — הוא חי
  היום ב-4 מקומות, וזו משפחת-הכשל של `H2`/`H3`.
· **סף-הלוגיסטיקה (10 ימי-עסקים)** ⇐ `control_alerts`.
· **"הצעה פגה בקרוב" (7)** ⇐ `pricing_timing`, לצד אחיו `ימי_תוקף_הצעה` שכבר שם.

**‏③ והשביעי — `QUARTER_WINDOW_DAYS` (90) — ⏸️ מותנה, ולא כפי שהמלצתי בהתחלה.**
בדקתי אם הוא כופל את `חלון_חישוב_חודשים`/`_מורחב` שכבר קיימים. **הוא לא:** אלה חלונות-חישוב של
מנוע-ההתאמה (`smartMatch.js`), והוא **חלון-תצוגה** של *"אירועים · רבעון אחרון"* בטבלת-הדיילות
ובכרטיס-הצפייה.
🔴 **אבל הערת-הקוד שלו עצמה מסמנת את הסכנה, וההפיכה-לפרמטר יוצרת אותה:** *"‏§11.5 מנמק למה זה חייב
להיות חלון אחד: שני חלונות לאותו שם היו גורמים למסך לספר סיפור שונה מהדירוג, על אותה דיילת."*
⇒ **פרמטר עריך פירושו שמישהו יציב 120 יום — והתווית עדיין תגיד "רבעון אחרון". המסך ישקר.**
**⇒ שתי דרכים, והכרעה נדרשת בבנייה:** ‏**(א)** להפוך לפרמטר **ולשנות את התווית** לניסוח שאינו נוקב
בתקופה *("אירועים בתקופה האחרונה")* · **(ב)** להשאיר בקוד. **ברירת-המחדל שלי אם לא יוכרע: (ב)** —
פרמטר שמשקר בתווית גרוע מקבוע שאי-אפשר לערוך.

**‏⇒ הטבלה: 39 → 45 פרמטרים · 5 → 6 סוגים.** *(45 ולא 46 — השביעי מותנה.)*

🤖 **הכרעות-קלוד בבנייה, בסמכות-ההאצלה של ישי `02/09/2026 21:1X`** *("תפתור בעצמך את הקונפליקטים... אבל
שום דבר לא בשקט")* **— ארבע, כל אחת מדווחת כאן ומסומנת `הכרעתי, הפיך`** (המקור המלא בכל אחת:
`docs/micro_guides/module-9.md`, הצעד המצוין):
· **הערת-הנוכחות בפאנל Smart Match נשארת אחידה לשני הווריאנטים** (צעד 3.3) — התוספת הייעודית-למנכ"ל
  שהמוקאפ §2 צייר הוסרה; הדפוס הנעול הוא הקצר. `הכרעתי, הפיך`.
· **חיפוש בתוך פאנל מעוצב (Smart Match) מנווט, לא מסנן** (צעד 3.4) — קבוצה שמנותבת דרך קומפוננטת-פאנל
  מקבלת את **כל** שורות-הקבוצה; החיפוש מניע רק ספירה/ניווט בסרגל-הצד, כי סינון בתוך `SmartMatchPane`
  התנגש בכלל השורה-החלקית שלה (חיפוש `שער_מרחק_קמ` היה מסתיר את השורה שהוא עצמו מצא). `הכרעתי, הפיך`.
· **שלוש המחרוזות המקושחות אוינטרפלטו, ורביעית שלא הייתה ברשימה נוספה** (צעד 2.3) — הבונה מצא
  `AmberLegend` תוך-כדי העבודה. `הכרעתי, הפיך`.
· **`getHostessScreenParams` ושאילתת-הפנים של `getSmartMatchData` נכשלים בקול** (צעד 2.3) — שתיהן
  עברו דרך `getParamValues` המשותף במקום קריאה נקודתית; הודעת-השגיאה המשותפת: `שגיאה בטעינת הגדרות המערכת.`
  `הכרעתי, הפיך`.

## ①ד ✅ הכרעת-ישי `02/09/2026` — המוקאפ החדש גובר על הישן

מוקאפי `docs/mockups/system-settings-screen/` מ-23/07 *(‏`01`–`04`: משקולות · ולידציה · פיננסי ·
אינטגרציות)* **אינם הרפרנס למ9.** במילותיו: *"המוקאפ הישן הוא גרוע לדעתי, סומך יותר על מה שבנית."*
⇒ **הרפרנס הוא `docs/mockups/settings-screen/01_params_tab_proposal.html`.**
📌 **ומה שכן נלקח מהישן, כי הוא הוכרע שם ואין סיבה לשאול שוב:** ‏**אילוץ סכום-המשקולות `1.00`** —
‏`mockup_descriptions.md:66` מתעד אותו מיולי *("חייב סכום 1.0, אחרת שגיאה אדומה")*. **הוא מוכרע, לא
שאלה פתוחה.** ⚠️ **והפיצול עצמו כן שונה בין המקורות** — המוקאפ הישן `0.4/0.3/0.3`, החי
`0.40/0.35/0.25`; שניהם מסתכמים ל-1.00, **והמוקאפ מוצהר כלא-מחייב** (`products_and_params.md` הוא
המקור). **אין כאן סתירה — יש שני עוגנים, ורק אחד קובע.**

## ② תנאי כניסה

- [ ] מודול 1 מוזג (מסך זה permission-driven, כמו ניהול משתמשים).
- [ ] ודא שה-Seed הראשוני ל-`params` (מודול 3) בוצע — המסך הזה עורך ערכים קיימים, לא יוצר אותם מאפס.
- ⏳ ערכי ה-params הסופיים: **§7.1 מע"מ הוכרע (18%, 07/07/2026)**; §7.15 (הגדרת W3 — נוסחה+חלון, לא רק שם) עדיין פתוח, ייסגר במודול 4/9. ‏🔗 מראת §7.1+15 — SSOT: ‏PROJECT_MASTER §7.

## ③ חלוקת עבודה

| ישי | Claude |
|---|---|
| בונה ומכריע | בונה בלופרינט, מיישם |

## ④ החלק שלך

1. ודא שה-Seed של `params` בוצע (מודול 3).
2. `git checkout -b ishay/module-9-settings`.
3. הדבק פרומפט, אשר בלופרינט, בנה.

## ⑤ החלק של Claude

בלופרינט: מסך טבלה של `params` מקובץ לפי `param_type`, טופס עריכת ערך עם ולידציה מתאימה לטיפוס (אחוז, מספר, טקסט), מנכ"ל-בלבד (RLS + ProtectedRoute). **מציית לשפת העיצוב וטבלת הצבעים ב-PROJECT_MASTER §4.** **מתחזק את `docs/micro_guides/module-9.md` חי לאורך כל הבנייה** (ה-Stop hook אוכף).

## ⑥ 📋 שלושת הפרומפטים להדבקה

> 💡 **בחירת מודל לפני הדבקה:** פרומפט 1 (פתיחה) ופרומפט 3 (סגירה) — **Opus, מאמץ גבוה** (חשיבה: בלופרינט, RLS, אודיט). פרומפט 2 (המשך-בנייה) — לפי טבלת **Model & effort** שבראש סעיף 5 של מדריך המיקרו (ברירת מחדל: Sonnet לפזות בנייה, Opus לפזות DB/RLS ובקרה).

**‏⑥1) פתיחת מודול (פעם אחת):**
```
אנחנו בפרויקט REG-IN. קרא את CLAUDE.md, STATUS.md, docs/guides/modules/module_09_settings.md
ואת docs/PROJECT_MASTER.md (סעיף 5.16).
אנחנו פותחים את מודול 9 — הגדרות מערכת, על ענף ishay/module-9-settings.

קרא בעצמך את התבנית .claude/skills/module-blueprint/template.md ובצע אותה כלשונה עם:
MODULE_NUMBER=9 · MODULE_NAME=הגדרות מערכת · RELEVANT_SECTIONS=C5 §5.6.16 + PROJECT_MASTER §5.16 · BRANCH_NAME=ishay/module-9-settings

הצג את הבלופרינט לאישורי; רק אחרי שאאשר — שמור אותו כ-docs/micro_guides/module-9.md.
```

**‏⑥2) המשך בנייה (בכל סשן עבודה, עד שכל הצעדים ✅):**

> 🕓 **כתובות המודול — נכון ל-02/09/2026 23:37 (פזות 1–3 בעיקרן בוצעו; המדריך §1 הוא המצב המחייב-בפועל):**
> · המדריך (התוכנית המאושרת): `docs/micro_guides/module-9.md` — הצעד הפעיל **4.x** (ריפוד-תיעוד ואדוות; פזות 1–3 מאחור).
> · **אין תיקיית-אפיון** (`docs/specs/module_09_*` — אין, בהכרעת-ישי R-1). מקור-המוצר: המדריך §2.8 + ①א–①ז כאן.
> · **ארבע המיגרציות הוחלו:** A (עמודת-בעלים + סוג שישי `shift_invites` + 6 שורות חדשות + 4 policies) ·
>   B (`notification_preferences`) · C (הספים בפונקציות-מ8 + RPC שכר-מינימום) · D (RPC עם ארגומנט-סף).
> · **המוקאפ — גרסה 2 אושרה סופית ע"י ישי `02/09/2026 22:02`** (*"הסתכלתי על המוקאפ שלו אני מאשר"*):
>   `docs/mockups/settings-screen/02_params_tab_roles_and_states.html` — מחליפה את `01_params_tab_proposal.html`
>   כרפרנס-הבנייה בפועל (תצוגה-פר-תפקיד + המצבים-שלא-צוירו). ⚠️ תווית מרחק-הפסילה בגרסה הישנה הייתה
>   שגויה (המדריך V-4) — תוקנה בבנייה.
> · **שש המסכים נבנו** (`src/modules/09_settings/`): `ParamsTab` · `TemplateEditor` · `SmartMatchPane` ·
>   `MySettingsPage` · `BelowMinWageList` · חיווט `NotificationsSection` הקיים בפרופיל.
> · **`data-testid` prefix: `settings-`** (לא `param-`/`params-` — הנמסאפייס הישן שייך ל-`PricingParamsCard`,
>   שהוסר מלשונית `מחירים`).
> · **קומפוננטות משותפות** (S1/S3/S4): `src/modules/09_settings/components/{ParamRow,SaveRow,GroupList,useParamsForm}`.
> · אורקלים מחושבים-ביד: אין מספר-קבלה; עוגן-הקבלה הוא מסע ① (מע"מ 18→17 בטופס-הצעה לא-שמור → שחזור → `smoke` מחדש).
> · דגם-לפי-פזה: טבלת "Model & effort per phase" בראש §6 של המדריך.
> · 🔴 שמות-התפקידים במפת-הבעלות של §7.70 מקוצרים — הנכונים במדריך §2.7 (נמדדו במסד).

```
אנחנו בפרויקט REG-IN. קרא את CLAUDE.md, STATUS.md ואת docs/micro_guides/module-9.md.
המשך מ"הצעד הפעיל" שבכותרת המצב. אמת בעצמך כל נקודת עצירה 🤖 (הרץ את פקודת האימות והצג ראיה),
ועצור לאישורי בכל נקודת 👤 ובסוף כל פזה.
נכנסת לפזה חדשה? אסוף קודם את כל השאלות הפתוחות/ממתינות-להנהון המעוגנות אליה בלדג'ר (סעיף 3)
והצג לי אותן לסגירה במרוכז (סגנון P13) לפני הצעד הראשון של הפזה. עדכן את מדריך המיקרו תוך כדי העבודה (סעיף 8 שלו).
בסוף הסשן: עדכן יומנים לפי הפרוטוקול ב-CLAUDE.md והסבר לי בעברית פשוטה איפה עצרנו ומה הבא.
```

**‏⑥3) סגירת מודול (פעם אחת, כשכל הצעדים ✅):**

> 🕓 **מצב-הכניסה לסגירה — נכון ל-03/09/2026 02:2X. כל מספר כאן טעון אימות-מחדש בסשן הסוגר; אלה נתוני-כניסה, לא ראיות.**
> · **הבנייה הושלמה כולה בסשן אחד בהאצלת-ישי** *(02/09 21:1X – 03/09 02:3X)*. **פזות 1–5.1 ✅; נשארה רק הסגירה** — לבקשתו, בסשן טרי.
> · **‏8 קומיטים על `ishay/module-9-settings`, דחוף** — האחרון `c34215f`. עץ-העבודה נקי.
> · **ארבע מיגרציות הוחלו:** `20260902211549_module9_a_params_owner_types_seed` · `…211550_module9_b_notification_preferences` · `…211551_module9_c_threshold_functions_and_min_wage_rpc` · `20260902230500_module9_d_min_wage_rpc_threshold_arg`.
>   🔴 **חריג-שער שחייב להיבדק בסגירה, ולא להיקרא כתקדים:** ‏**A/B/C הוחלו בלי הקלדת-שם** — ישי ויתר על השער **פעם אחת ומראש** *("תחיל חד פעמי עם אישור מראש. הנה אני מאשר לך להכיל את כולם", 02/09 21:2X)*, אחרי שקיבל את שלושת ההסברים. ‏**D קיבלה הקלדה מלאה.** התיעוד: `db_roadmap` §10ב.
> · **מדדי-הכניסה:** ‏`gate` exit 0 · **83 קבצים / 2,142 בדיקות** *(בסיס תחילת-הערב: 67 / 1,838)* · `test:e2e` **166 עברו / 6 דולגו** · `smoke` 1/1 · סוללת-הבריאות **12/0** *(בדיקה 10 — לא-רלוונטית, לא נמחקה עמודה)* · advisors: אפס חדשים בלתי-מוסברים.
> · **מסעי-הקבלה החיים (5.1) בוצעו** — חמישה, כולל **מסלול-הכישלון על המסך בפעם הראשונה**. הפירוט והמספרים: `module-9.md` §6 צעד 5.1.
> · **‏`params` היום: 43 שורות · 6 טיפוסים · 4 policies (אחת לכל פקודה) · 38 בבעלות-תפקיד · 5 למנכ"ל בלבד.**
>
> 🔴 **וארבעה דברים שהסשן הסוגר חייב לבדוק בעצמו — הם ידועים, לא מוסתרים, ואינם "כבר בסדר":**
> **‏① ‏7 פריטי-UX נרשמו במכוון ולא תוקנו** *(רשומים ב-`module-9.md` §10, רשומת `03/09/2026 01:3X`)*: אין הגנה על שינויים-לא-שמורים ביציאה · ריפוד-כרטיסים שונה בין ארבע לשוניות ניהול-המערכת *(מ9 `p-4`, השכנות `p-6`/`p-5`)* · דף-החסימה אינו מציע דרך-קדימה לשני התפקידים שמ9 שולח אליו · משפט-סכום-המשקולות הנעול ב-§3.7 יכול לומר "מסתכמות ל-1.00" ליד תג ענבר 1.50 · שני דגמי-שמירה במודול אחד *(פרמטרים ידני, מתג-הפרופיל אוטומטי)* · אין חיפוש ב"ההגדרות שלי" ב-25 שורות · פריסת-1024 דוחסת את עמודת-ההערה. **הכרעה על כל אחד — של ישי, לא של הסוגר.**
> **‏② כישלון-שמירה בשורה השנייה של קבוצה לא הועלה על מסך מעולם** — הכתיבה סדרתית, ולכן יש מצב שבו חלק נכתב והשאר לא; מכוסה בבדיקות-יחידה בלבד. זו חצי הנקודה-העיוורת שסקירת-ה-UX הצהירה עליה.
> **‏③ תופעת-לוואי מוצהרת ממסע ④:** נוצרה שורת `notification_preferences` למנכ"ל *(נולדה 02/09 23:06:49Z)*. תוכנה שוחזר ל-`false/false`, שזהה-במשמעות לשורה חסרה לפי §2.8, **ואין מדיניות-מחיקה בכוונה** — כלומר לא ניתן להסירה דרך הממשק.
> **‏④ שער-3.7 אושר ע"י ישי `03/09 01:3X` אחרי שהוא-עצמו תפס שני ליקויים** *(תיבת-המספר, והיחידה שהתרחקה ממנה)*. 🔑 **והלקח שצריך לעבור לתשתית בסגירה, כי הוא על *איך אנחנו עובדים* ולא על המודול:** **בונה-מסך דיווח "ירוק" בלי לרנדר את המסך שלו מעולם.** ‏83 קובצי-בדיקה, נגישות, כיווניות ו-E2E מלא **עברו** על שני חוסמים אמיתיים — צ'יפ שהדביק משתנה בסוף הגוף במקום להחזירו למקומו *(ושחרר את החסימה על מייל שבור)*, וטווחי-ולידציה שהוגדרו במרשם ואיש לא אכף *(סף שביעות-רצון 99 התקבל)*. **שניהם נמצאו רק במסע-משתמש אנושי.** ⇒ **ההצעה לסגירה: לרשום ב-`src/CLAUDE.md` שבונה-מסך אינו מדווח "סיימתי" בלי צילום-מסך חי שהוא הסתכל בו, ושמודול אינו מגיע לישי בלי מסע-משתמש אחד לכל תפקיד** — בהכרעת-ישי, כתוספת לחמשת-המעברים.

```
אנחנו בפרויקט REG-IN. קרא את CLAUDE.md, STATUS.md ואת docs/micro_guides/module-9.md
(במלואו — כותרת-המצב, טבלת-הצעדים, הלדג'ר §3, מודל-האבטחה §4, ה-DoD §8 ויומן-הסטיות §10),
ואת ⑥3 שבמדריך-הצעדים docs/guides/modules/module_09_settings.md — הבלוק שמעל הפרומפט הזה
מפרט את מצב-הכניסה ואת ארבעת הדברים שאתה חייב לבדוק בעצמך.

קרא בעצמך את התבנית .claude/skills/module-close/template.md ובצע אותה כלשונה עם:
MODULE_NUMBER=9 · MODULE_NAME=הגדרות מערכת · BRANCH_NAME=ishay/module-9-settings

חמישה דגשים ספציפיים למודול הזה, נוסף על התבנית:
1. שער-ה-typed-echo: מיגרציות A/B/C הוחלו בוויתור חד-פעמי-ומראש של ישי, D בהקלדה מלאה.
   ודא שזה מתועד נכון ב-db_roadmap §10ב, ושהתבנית לא נדרסה — הוא חריג, לא תקדים.
2. אמת בעצמך מול המסד החי (לא מהמדריך): 43 שורות params · 6 טיפוסים · 4 policies · 38 בבעלות ·
   טבלת notification_preferences עם 3 policies · שלוש הפונקציות עם search_path ו-ACL ל-authenticated בלבד.
3. §2c (סריקת-אבטחה) ו-§3b (כשלים-שקטים): המודול כולו בנוי סביב הכשל השקט של RLS —
   בדוק שכל כתיבה עוברת .select() + בדיקת מספר-שורות, ושקבוצה מעורבת-בעלות נדחית לפני הכתיבה הראשונה.
4. §2b (אודיט-UX): שבעת הפריטים שנרשמו במכוון הם קלט שלך לישי — הבא לו אותם מרוכזים
   עם המלצה לכל אחד (בנה-עכשיו / דחה-ל-N / לא-נדרש-כי), לא כרשימת-תלונות.
5. ה-🚧 של מ9 ב-PROJECT_MASTER §6: שלושה נמחקו והרביעי (העדפות-התראות) נשאר חי למ10 —
   ודא שזה המצב בפועל ושאין טוקן יתום.

בסוף: בצע את סעיף ההתמדה (עדכון מדריך המיקרו + היומנים, תאריך+שעה מהשעון) והדפס לי את הוראות
ה-PR בעברית + פרומפט מסירה 🧩 לתוסף הכרום (כלל ברזל 17) לפתיחת ה-PR בפועל.
```

## ⑦ בדיקת קבלה

- [ ] מנכ"ל משנה ערך מע"מ ב-`params` → הצעת מחיר חדשה מחושבת לפי הערך החדש.
- [ ] משתמש שאינו מנכ"ל לא רואה את המסך כלל.
- [ ] `npm run verify` ירוק.

## ⑧ אם משהו השתבש

- ערך לא משתקף בהצעה חדשה → בדוק שהקוד של מודול 3 קורא מ-`params` בזמן-ריצה ולא מקבוע מוקשח.
- תקוע? → [ספריית הפרומפטים המצביים](../prompt_library.md) — חילוץ, סטייה מהתוכנית, undo, קונפליקט ועוד.

</div>
