<div dir="rtl">

# 💰 שלב — מודול 3: הצעות מחיר

> מקומו בהיררכיית האמת: `schema.sql` › אפיון › מוקאפים › מדריך זה. בסתירה — המדריך מפסיד.
> צעדים שבוצעו מסומנים ✅ — המדריך כתוב כאילו כלום לא קיים.

## ① מה נשיג בשלב הזה

מנוע התמחור: המלצת מספר דיילות (`אורחים/50`), תמחור שירותים + מחירון מדורג לתגים/שרוכים, הנחות (לקוח קבועה + ידנית), חישוב מע"מ, PDF, וסטטוס הצעה (`בתהליך → מאושרת/נדחתה`). מסך 5.4-5.5 באפיון. **מודול הכסף** — יש כאן דוגמת בדיקה מדויקת מהאפיון.

## ② תנאי כניסה

- [ ] מודול 2 (לקוחות) מוזג ל-`dev`.
- ✅ **חבילת הכסף הוכרעה (07/07/2026, ישי — אודיט הפריטים הפתוחים)** — כבר לא צריך לשאול, רק ליישם לפי [PROJECT_MASTER §7](../../PROJECT_MASTER.md):
  > 🔗 **מראת §7.1+25+26+27+28+42+12+13** — SSOT: ‏PROJECT_MASTER §7 (לא לערוך כאן ידנית; מסונכרן ע"י `regin-docs-sync`)
  - **§7.1** — מע"מ = **18%** (נכנס ל-`params`, `param_name='אחוז_מעמ'` — השם הקנוני מה-Seed הנעול; **לא** `'מע"מ'` — אי-התאמת-מחרוזת = מע"מ-0% שקט).
  - **§7.25** — עיגול: **אגורות (2 ספרות) בחישוב ובאחסון, שקל שלם בתצוגה בלבד** (לוודא שסכום-הביניים 5,355 משוחזר מפירוט-השורות).
  - **§7.26** — הנחות: **חיבור** (לא כפל), תקרה משולבת ≤ 100%, אי-שליליות — **CHECK ב-DB + אכיפה ב-`pricing.js`**.
  - **§7.27** — מדרגות מחיר: ה-tie-break בקוד בלבד ("`min_qty` הגבוה ביותר ש-≤ הכמות מנצח"; `max_qty`=תצוגה בלבד) — **בלי אילוץ-DB**.
  - **§7.28** — הקפאת עלות-רכש: הוסף עמודה **`closing_unit_cost` ל-`quote_services`** (מיגרציה במודול זה) שמוקפאת באישור, סימטרית ל-`closing_unit_price`.
  - **§7.42** — פקיעת הצעה (30 יום) = **job ב-`pg_cron`** (מעבר-סטטוס טהור, בלי מייל); הבחן בין `expired` אוטומטי לדחייה ידנית (בחירת ערך-סטטוס/דגל — שלך בבנייה).
  - **§7.12** — אחסון PDF: **הפקה-בזמן-אמת מהנתונים, בלי אחסון קבוע** (07/07/2026). ‏`quotes.pdf_url` = deprecated. ההצעה המוקפאת היא ה-SSOT, ה-PDF נגזר. **דרישת-אימות:** יצירת פרויקט מהצעה קוראת שורות קפואות דרך `projects.quote_id`, לא מחשבת מחדש. רפרנס ויזואלי: טמפלט-הטיוטה של ישי ל-PDF ב-`docs/mockups/quote-screen/quote_template_draft.docx` — טיוטה תקפה (המפל: ביניים→הנחת-לקוח%→הנחה-נוספת%→לפני-מע"מ→מע"מ 18%→לתשלום, תואם `applied_customer_discount`+`manual_discount`). מבנה מלא ב-PROJECT_MASTER §7.12; מודול 3 מתרגם ל-renderer, לא בונה מחדש.
  - **§7.13** — ביצוע ה-Seed בפועל: ההחלטות נעולות ב-`docs/reference_spec/products_and_params.md`, וחסם המע"מ הוסר — הריצה עצמה עדיין לא בוצעה.
- ✅ **הוכרעו בסבב-הכרעות-קדם (ישי, 11/07/2026)** — הרישום המלא ב-[PROJECT_MASTER §7](../../PROJECT_MASTER.md); הבלופרינט **מיישם, לא שואל מחדש**:
  - **§7.49** המרת הצעה→פרויקט = **RPC אטומי** (הכל-או-כלום; הפרויקט נולד-שלם מההצעה; האישור נשאר אנושי — במייל אין כפתור-אישור) · **§7.50** נעילת-DB להצעה מאושרת (טריגר-חסימה) · **§7.51** `vat_rate_snapshot` מוקפא באישור · **§7.53** ✅ **נסגר** — "אין דבר כזה אירוע בלי דיילות" (ישי), CHECK‏>0 נשאר · **§7.76** snapshot-זהות בתוך ה-RPC.
  - **תוספות מאושרות לבלופרינט (§6, `grep '🚧 מ3'`):** היסטוריית-הצעות בכרטיס-הלקוח (לחיצה→תצוגת-הצעה+הפקת-PDF) · מנוע-PDF כפונקציה עצמאית (התמורה במ10).
- ✅ **סבב-טריאז'-2 הוכרע (ישי, 12/07/2026) — הרישום המרוכז ב-[§7.82](../../PROJECT_MASTER.md)** (+ §7.32/34/41/57/70 בפריטיהם): מאשר-הצעה = **מנהלת-פרויקטים** (§7.49, כספים=צפייה) · **7 סיבות-דחייה** + `rejection_notes` · תוקף נספר מ-`updated_at`+פרמטר · נעילת-DB לכל-סטטוס-שאינו-`in_progress` · CHECKs-הנחה 0-100 · שם-פרמטר-מע"מ = **`אחוז_מעמ`** · **מסנני-הצעות** (⭐ מבט "פג-בקרוב" + פילוח-סיבות). **§7.57 🟢 נסגר** (דו"ח-שכר ידני — לא-לזרוע).
- ✅ **הוכרעו 14/07/2026 (בדיקת-מוכנות-לפני-בלופרינט + זיקוק `AI_rec`) — הרישום ב-[PROJECT_MASTER §7](../../PROJECT_MASTER.md):**
  - **§7.83** — RLS ל-3 טבלאות-בסיס: `select` **פתוח לכל `authenticated`** + כתיבה **CEO-בלבד** (מודול 'הגדרות מערכת') על `params`/`products`/`price_tiers`. הרציונל: מנהלת-הפרויקטים חייבת לקרוא קטלוג/מע"מ/יחס-אורחים בזמן בניית-הצעה, ואין להן מודול-בית משלהן. **זה מחליף את תבנית-§7.21 הרגילה עבור שלוש הטבלאות האלה.**
  - **§7.84** — ✅ **תוספת-מסך חדשה בהיקף מודול 3 (סטייה מאושרת מהאפיון): טאב "מחירים" בניהול-מערכת** (`/system`, בנוסף לטאב "פרמטרים" הקיים). מכסה: קטלוג-מוצרים (`products`) + מדרגות-מחיר (`price_tiers`) + **2 פרמטרי-תמחור בלבד** (`אחוז_מעמ`, `יחס_אורחים_לדיילת`). כתיבה CEO-בלבד. **תפקיד: תחזוקה-שוטפת** — ה-Seed הנעול עדיין רץ ראשון דרך מיגרציה. **עיצוב-רקע מלא (מבנה-קבצים · api · RLS · ולידטורים · 9 סיכונים): `docs/module3_prices_tab_design_notes.md`** — קלט ל-DB Design Challenge/PM Interview של הבלופרינט, **לא מוכתב-סופי** (הבלופרינט שוקל מחדש).
  - **§7.85** — ✅ **הוכרע (ישי, 14/07) — מבנה-מפתח `quote_services` = `line_id bigint generated always as identity` (PK סינתטי)**; `quote_id`/`sku`/`color`/`line_number` עמודות רגילות. עקבי עם §7.64 (כמו `customer_id`); הפניות מ4/מ6 בעמודה-אחת. הבלופרינט מיישם, ה-DB Design Challenge מאמת-שמשתלב (לא מכריע מאפס). תרחיש-צבע נפתר בכל אפשרות (הצבע=עמודה).
  - **§7.72** — קיבל כיוון-מועמד ל-`project_changes` (שינויי-תכולה, מ6) — לידיעה, לא לביצוע כאן.
  - **Seed הורחב ל-20 שורות-`params`** (4 תבניות-מייל חדשות #17–20 עם תוכן-אמיתי; ר' `products_and_params.md`). **✅ URL טופס-הסקר (5.8.8) נסגר** — נזרע כהחלטת-Seed #14 (`https://forms.gle/YFJobqmgpBCqf1x87`).
- ✅ **סבב-סגירת-קרקע + ראיון-PM + מועצת-תמחור (ישי, 14/07/2026 ערב) — הרישום ב-[§7.82](../../PROJECT_MASTER.md) ריידרים F16–F26:**
  - **F16–F22** (ציד-פערים): אכיפת-סיבת-דחייה ב-**CHECK** (`rejected`⇔סיבה) · שמירת-**עריכה אטומית** (מחק+צור שורות בטרנזקציה) · סה"כ **מחושב מהאגורות המדויקות** (שורות מוצגות מעוגלות) · **אין הפרדת-חובות** (edit על 'הצעות מחיר' = יצירה+עריכה+אישור) · **יחס-אורחים** = ברירת-מחדל מהפרמטר, נדרסת פר-הצעה + **הזנת-כמות-דיילות ידנית** ישירה · **שעות↔מק"ט בלי אכיפה** (רמז-UI רך מותר) · `projects.required_hostess_count` = **סכום ה-qty של שורות-הדיילות** בהצעה המאושרת.
  - **F23** — הוסף ל-`quotes` עמודות **`estimated_start_time`+`estimated_end_time`** (time); `estimated_hours` **מחושב מההפרש**, לא מוקלד (שקלל ב-DB Design Challenge). **F24** — מסך ניהול-ההצעות בטאבים-לפי-סטטוס (בתהליך/מאושרות/נדחו + מבט-⭐פג-בקרוב), דפוס-הטאבים מ-`docs/mockups/project-management-screen/01_overview_reworked.html`; מיונים/סינונים לשיקול-דעתך, "לא מעמיס בעין". **F25** — כפתור "+ לקוח חדש" בבורר-הלקוח פותח את `CustomerFormDialog` של מ2 כדיאלוג (שימוש-חוזר, כלל 14), נבחר אוטומטית בהצעה.
  - **F26 — ✅ מודל-תמחור = חבילות (llm-council).** אינווריאנט-בנייה מחייב: **`src/lib/pricing.js` לעולם לא מקודד סמנטיקה של "משמרת" — רק `פריט × כמות × מחיר`.** המנוע כבר גנרי (`products.base_price` חופשי, בלי ENUM-שעות), ולכן חבילות/שעתי = אותה שורת-מוצר ומעבר-עתידי = הוספת-שורה במסך-המחירים. המחירים (500/800) סבירים-להתחיל, מכווננים דרך מסך-המחירים לא עכשיו. קלט-מ8 (לא כאן): דו"ח רווח-מתוכנן-מול-בפועל.
- ⏳ **לדיון-קל בבנייה (לא חוסם):** ‏**§7.56** אזור-זמן/DST — ה-pg_cron הראשון (פקיעת-הצעות) נבנה כאן, תזמון Asia/Jerusalem (הנהון-קליל).
- 🗡️ **חדשות מאודיט-הערעור המבני (08/07/2026)** — (רשימה: ‏[PROJECT_MASTER §7](../../PROJECT_MASTER.md); המפה: ‏`docs/db_roadmap.md`): ‏**§7.64** ✅ **הוכרע (ישי, 10/07/2026) — כיוון מדיניות-מפתחות.** אצלך: `products.sku` **נשאר PK טבעי** + `ON UPDATE CASCADE` על ה-FKs (מבוצע בבניית מודול 3). **למה בכלל שינינו את זה (הרקע):** מפתח-ראשי צריך להיות *ייחודי* **וגם** *קבוע-לנצח + פנימי*; ח"פ/ת"ז/מייל ייחודיים אך **חיצוניים** ולכן זזים — (1) תיקון-הקלדה בח"פ נחסם ברגע שהצעות-מחיר מצביעות עליו; (2) שתי יחידות של אותו גוף ממשלתי חולקות ח"פ אחד → PK-ייחודי בלתי-ניתן-לייצוג. הפתרון: מספר-רץ פנימי (`customer_id bigint`) כ-PK, וח"פ נשאר עמודת `company_number` ייחודית-חובה שמחפשים לפיה — שום דבר לא אבד. **החריג שלך:** `sku` הוא **קוד שהעסק שולט בו** (לא חיצוני/PII) → נשאר PK טבעי, רק `ON UPDATE CASCADE` כדי שתיקון-SKU עתידי לא ייחסם. אותו היגיון על ת"ז (מודול 4) · ‏**§7.62** ‏NOT NULL ל-`quotes.customer_id` (להנהון) · ‏**§7.70** ‏params מטופס+היסטוריה (מוכרע עם ה-Seed) · רוכבים (קודמו לפריטים מלאים 09/07/2026): ‏**§7.74** קיבוע `numeric(12,2)` במיגרציות-התמחור · ‏**§7.75** job ניקוי `login_attempts` אחרי התקנת pg_cron · ‏**§7.76** ‏RPC-ההמרה מעתיק snapshot-זהות (שם-אירוע) לפרויקט — ✅ הוכרע (11/07/2026) יחד עם §7.49 · ‏**§7.78** את 51+28+52 מכריעים כיחידת-הקפאה אחת — ✅ התכנס (11/07/2026): צד-מחיר/עלות/מע"מ קופא באישור-ההצעה, צד-בפועל קופא בסגירת-האירוע.

## ③ חלוקת עבודה

> **מודול 3 = ישי בלבד** (בעל-המודול — מחליט **וגם** בונה). מודול חד-בעלים.

| ישי | Claude |
|---|---|
| כל ההכרעות סגורות (§7 + §7.82); מדביק פרומפטים, מאשר בלופרינט, בונה יחד עם Claude | בונה בלופרינט, מריץ Seed, מיישם, מתחזק את מדריך-המיקרו חי |

## ④ החלק שלך

1. **כל הכרעות-מ3 סגורות** (§7 + §7.82 — סבבי-הכרעה-קדם 11–12/07/2026, ר' ②); הבלופרינט **מיישם, לא שואל מחדש**. אם בכל-זאת תצוץ שאלה עסקית *חדשה* בבנייה — היא של **ישי** (§7=ישי, כלל 1), נרשמת ב-§7 ולא מוכרעת ע"י Claude לבד.
2. `git checkout ishay/module-3-quotes-build` (הענף **כבר קיים** — נפתח 22/07 מ-dev טרי אחרי מיזוג PR #9; **לא** `-b`, שייכשל. הענף הישן `ishay/module-3-quotes` מוזג ומת — כלל 10).
3. הדבק פרומפט, אשר בלופרינט, בנה.

## ⑤ החלק של Claude

**Phase 1** — מריץ Seed ל-`products`/`price_tiers`/`params` לפי `docs/reference_spec/products_and_params.md` (ההחלטות נעולות; ערך מע"מ בפועל לפי הכרעת ישי) + RLS. **Phase 2** — מנוע התמחור כ-SSOT ב-`src/lib/pricing.js` + `api.js` עם בדיקות יחידה (כלל ברזל 14 — ה-UI רק מייבא, לא משכפל נוסחה). **אינווריאנט מחייב (§7.82/F26): `pricing.js` יודע רק `פריט × כמות × מחיר` — לעולם לא מקודד "משמרת"/"4 שעות" בנוסחה** (המנוע גנרי; חבילה=שורת-מוצר; זה מה שהופך מעבר-עתידי-לשעתי להוספת-שורה במסך-המחירים ולא לבנייה-מחדש). **Phase 3** — מסך יצירת הצעה (בחירת לקוח, פרטי אירוע, בחירת שירותים, חישוב חי), הפקת PDF. **Phase 4-5** — סטטוס הצעה, פקיעה אוטומטית ל-30 יום, בדיקת תרחיש הדוגמה. **מציית לשפת העיצוב ולטבלת הצבעים ב-PROJECT_MASTER §4.** **מתחזק את `docs/micro_guides/module-3.md` חי לאורך כל הבנייה** (ה-Stop hook אוכף).

**➕ מסך "מחירים" (§7.84) — חלק מהיקף המודול, לא בנפרד:** הבלופרינט משלב את בניית טאב "מחירים" בניהול-מערכת (קטלוג-מוצרים/מדרגות-מחיר/2-פרמטרי-תמחור, כתיבה CEO-בלבד) כפזה/צעדים משלו, לפי העיצוב-הרקע ב-`docs/module3_prices_tab_design_notes.md` — **אחרי** שה-DB Design Challenge אימת את §7.85 (מבנה-מפתח quote_services = `line_id` סינתטי, הוכרע 14/07) ואת מדיניות-ה-RLS של §7.83. ה-policies של §7.83 (`select`-פתוח + כתיבה-CEO על 3 הטבלאות) נכנסות ממילא ב-Phase 1, בין אם המסך נבנה מוקדם או מאוחר.

**בדיקת אמת מהאפיון (M2 milestone) — לצטט ל-Claude, זו הבדיקה הקובעת:** לקוח קיים עם 5% הנחה קבועה, 300 אורחים, 4 שעות, 300 תגים רגילים + 300 שרוכי בד, 10% הנחה ידנית → **סה"כ לתשלום: 6,319 ₪** בדיוק.

## ⑥ 📋 שלושת הפרומפטים להדבקה

> 💡 **בחירת מודל לפני הדבקה:** פרומפט 1 (פתיחה) ופרומפט 3 (סגירה) — **Opus, מאמץ גבוה** (חשיבה: בלופרינט, RLS, אודיט). פרומפט 2 (המשך-בנייה) — לפי טבלת **Model & effort** שבראש סעיף 5 של מדריך המיקרו (ברירת מחדל: Sonnet לפזות בנייה, Opus לפזות DB/RLS ובקרה).

**1) פתיחת מודול (פעם אחת):**
```
אנחנו בפרויקט REG-IN. קרא את CLAUDE.md, STATUS.md, docs/guides/modules/module_03_quotes.md,
docs/reference_spec/products_and_params.md, docs/module3_prices_tab_design_notes.md ו-docs/PROJECT_MASTER.md — **כל פריטי-§7 המתויגים ·מ3·** (`grep '·מ3' docs/PROJECT_MASTER.md`), ובמיוחד §7.1/12/13/25/26/27/28/42/49/50/51/52/53/70/76 + §7.32/34/41/57/62/74/75 + **§7.82 (חבילת-הכרעות-מ3, סבב-טריאז' 12/07)** + **§7.83/84/85 (הכרעות 14/07: RLS-3-טבלאות · טאב-מחירים-חדש · מבנה-מפתח-quote_services)**.
אנחנו פותחים את מודול 3 — הצעות מחיר, על ענף ishay/module-3-quotes-build (מ-dev טרי).

הכרעות ישי (07/07/2026): מע"מ=18% · עיגול לאגורות בחישוב/שקל בתצוגה · הנחות בחיבור עם CHECK ≤100% ·
מדרגות tie-break בקוד · הקפאת עלות-רכש (closing_unit_cost) · פקיעת-הצעה ב-pg_cron. פרטים ב-§7.1/25/26/27/28/42.
אחסון PDF (§7.12): **הוכרע** — הפקה בזמן-אמת מהנתונים, בלי אחסון קבוע (`quotes.pdf_url` deprecated); ההצעה המוקפאת = SSOT.

היקף נוסף שהוכרע (14/07): (א) §7.83 — ה-RLS ל-params/products/price_tiers = select-פתוח-לכל-authenticated + כתיבה-CEO-בלבד (מחליף את תבנית-§7.21 עבורן). (ב) §7.84 — המודול בונה **גם** טאב "מחירים" חדש בניהול-מערכת (קטלוג/מדרגות/2-פרמטרים, כתיבה CEO); עיצוב-רקע ב-docs/module3_prices_tab_design_notes.md — שקלל אותו ב-DB Design Challenge, אל תבנה עיוור לפיו. (ג) §7.85 — ✅ **הוכרע (14/07): מבנה-מפתח quote_services = `line_id bigint` סינתטי כ-PK** (עקבי §7.64; `quote_id`/`sku`/`color`/`line_number` עמודות רגילות). הבלופרינט מיישם, ה-DB Design Challenge מאמת-שמשתלב.
(ד) סבב-סגירת-קרקע 14/07-ערב — כל הריידרים F16–F26 חיים בתוך §7.82 (קרא אותו במלואו). F16–F22: אכיפת-דחייה ב-CHECK · עריכה-אטומית (מחק+צור בטרנזקציה) · סה"כ מחושב-מהאגורות-המדויקות · אין-הפרדת-חובות · יחס-אורחים=ברירת-מחדל-נדרסת + הזנת-כמות-דיילות-ידנית · שעות↔מק"ט בלי-אכיפה · required_hostess_count=סכום-qty-שורות-הדיילות. F23: עמודות `estimated_start_time`+`estimated_end_time` ל-quotes, `estimated_hours` נגזר-מההפרש (שקלל ב-DB Design Challenge). F24: מסך-ניהול-הצעות בטאבים-לפי-סטטוס (דפוס מ-docs/mockups/project-management-screen/01_overview_reworked.html). F25: "+לקוח-חדש" בבורר פותח את CustomerFormDialog של מ2 כדיאלוג (שימוש-חוזר).
(ה) §7.82/F26 — **מודל-תמחור = חבילות (הוכרע ב-llm-council). אינווריאנט מחייב ל-Phase 2: `src/lib/pricing.js` לעולם לא מקודד "משמרת" — רק פריט×כמות×מחיר** (המנוע כבר גנרי; חבילות/שעתי = אותה שורת-מוצר). המחירים מכווננים דרך מסך-המחירים, לא בקוד.
(ו) Seed: בלוק "הבהרות-Seed משלימות" (החלטות 10–14) ב-products_and_params.md — רפאים-#4 לא-נזרע · הרכב-20-שורות · אי-זריעת-מדרגות-שירות/מקפים · מק"טי-תגים-קנוניים · URL-סקר #14. + אינדקס db_roadmap C-6 (quote_status,updated_at).

קרא בעצמך את התבנית .claude/skills/module-blueprint/template.md ובצע אותה כלשונה עם:
MODULE_NUMBER=3 · MODULE_NAME=הצעות מחיר · RELEVANT_SECTIONS=§5.2-5.6 (תהליך+מסכי-הצעה+תמחור), §7 כל ·מ3· + §7.82/83/84/85 · BRANCH_NAME=ishay/module-3-quotes-build

חובה: "שאלות פתוחות" מסעיף ② במדריך שלי = פריטי §7, מנותבות לישי להכרעה (§7 = ישי, כלל 1) — אל תכריע בהן לבד.
הצג את הבלופרינט לאישורי; רק אחרי שאאשר — שמור אותו כ-docs/micro_guides/module-3.md.
```

**2) המשך בנייה (בכל סשן עבודה, עד שכל הצעדים ✅):**
```
אנחנו בפרויקט REG-IN. קרא את CLAUDE.md, STATUS.md ואת docs/micro_guides/module-3.md.
המשך מ"הצעד הפעיל" שבכותרת המצב. אמת בעצמך כל נקודת עצירה 🤖 (הרץ את פקודת האימות והצג ראיה),
ועצור לאישורי בכל נקודת 👤 ובסוף כל פזה.
נכנסת לפזה חדשה? אסוף קודם את כל השאלות הפתוחות/ממתינות-להנהון המעוגנות אליה בלדג'ר (סעיף 3)
והצג לי אותן לסגירה במרוכז (סגנון P13) לפני הצעד הראשון של הפזה. עדכן את מדריך המיקרו תוך כדי העבודה (סעיף 8 שלו).
בסוף הסשן: עדכן יומנים לפי הפרוטוקול ב-CLAUDE.md והסבר לי בעברית פשוטה איפה עצרנו ומה הבא.
```

**3) סגירת מודול (פעם אחת, כשכל הצעדים ✅):**
```
אנחנו בפרויקט REG-IN. קרא את CLAUDE.md, STATUS.md ואת docs/micro_guides/module-3.md.
קרא בעצמך את התבנית .claude/skills/module-close/template.md ובצע אותה כלשונה עם:
MODULE_NUMBER=3 · MODULE_NAME=הצעות מחיר · BRANCH_NAME=ishay/module-3-quotes-build
חובה באודיט: הרצה מפורשת של תרחיש הבדיקה מהאפיון — התוצאה חייבת להיות 6,319 ₪ בדיוק.
בסוף: בצע את סעיף ההתמדה (עדכון מדריך המיקרו + היומנים, תאריך+שעה) והדפס לי את הוראות ה-PR
בעברית + פרומפט מסירה 🧩 לתוסף הכרום (כלל ברזל 17) לפתיחת ה-PR בפועל.
```

## ⑦ בדיקת קבלה

- [ ] תרחיש הבדיקה מהאפיון מחזיר **6,319 ₪** בדיוק.
- [ ] PDF נפתח ומציג את פרטי ההצעה.
- [ ] דחיית הצעה דורשת בחירת סיבה מרשימה.
- [ ] אישור הצעה יוצר פרויקט חדש בסטטוס "טרם החל".
- [ ] `npm run verify` ירוק.

## ⑧ אם משהו השתבש

- הסכום לא תואם 6,319 ₪ → בדוק סדר פעולות: הנחות לפני מע"מ, לא אחרי.
- Seed נכשל → ודא שערכי ה-enum באנגלית תואמים בדיוק למה שב-`products_and_params.md`.
- תקוע? → [ספריית הפרומפטים המצביים](../prompt_library.md) — חילוץ, סטייה מהתוכנית, undo, קונפליקט ועוד.

</div>
