<div dir="rtl">

# 📦 שלב — מודול 5: לוגיסטיקה

> מקומו בהיררכיית האמת: `schema.sql` › אפיון › מוקאפים › מדריך זה. בסתירה — המדריך מפסיד.
> צעדים שבוצעו מסומנים ✅ — המדריך כתוב כאילו כלום לא קיים.
>
> 🔴 **‏Discovery טרם נערך למודול הזה** *(עודכן 12/08/2026 — פריט ⏸️ שנפתח 08/08/2026 ונדחה
> במכוון כי "מודול 5 רחוק שבועות"; זה כבר לא נכון, המודול הבא בתור אחרי מ4).* **אל תדלג ישר
> ל-`module-blueprint`** — הדבק קודם את פרומפט ⑥0 למטה. כשה-Discovery ייסגר, `docs/specs/module_05_logistics/spec.md`
> יהפוך למקור-האמת המוצרי (גובר על C5/מוקאפים בכל מה שהוא מכסה, פר-פריט — בדיוק כמו במודול 4),
> והמדריך הזה יעודכן בהתאם *(הדגם: `docs/guides/modules/module_04_hostesses.md`)*.
> **פזה משולבת עם מודול 6** — ‏`00_roadmap §3`: 6-ליבה → 5 → 6-אינטגרציה (תלות הדדית בבנייה,
> אבל אין תלות-Discovery: מותר ומומלץ להריץ את שני הדיסקברים בנפרד).

## ① מה נשיג בשלב הזה

Checklist לוגיסטי לכל פרויקט: כל פריט (`site`/`product`) שנכלל בהצעת מחיר מאושרת מקבל שורת מעקב (`טרם החל → הוזמן → מוכן`), עם התקדמות אוטומטית של סטטוס הפרויקט כשהכל מוכן. מסך 5.8–5.9 באפיון.

## ② תנאי כניסה

- [ ] מודול 3 (הצעות מחיר) מוזג ל-`dev` — הלוגיסטיקה שולפת פריטים מהצעה מאושרת.
- [ ] טבלת `projects` קיימת בסכמה — הלוגיסטיקה עושה FK ל-`project_id`. הטבלה כבר קיימת (מ-16 הטבלאות שנוצרו בתשתית, PROJECT_MASTER §2.2), אז **אין תלות-בנייה במודול 6**: הסדר הוא 5 לפני 6 (ר' [מפת הדרכים](../00_roadmap.md)), ומודול 6 הוא שדורש את מודול 5 מוזג — לא להפך.
- ⏳ **§7.22** — משמעות `actual_qty` < `planned_qty` (חוסר? פחת? חוסם "מוכן"?) — פתוח, מוכרע בבנייה כאן (מצוטט גם בפרומפט-הפתיחה ⑥).
- ⏳ **↳§7.44 (07/07 ערב) — שומר-הטריגר:** הטריגר האטומי שכותב `project_status` (🔴 **נבנה במ6 — הבעלות עודכנה 08/08/2026 בהכרעת-ישי; היה "מ4/5/6"**) חייב לחשב-מחדש **רק** בסטטוסי-קדם-אירוע (`not_started`/`in_progress`/`ready`) — אחרת עדכון "כמות בפועל" בסגירה יחזיר פרויקט שהסתיים ל-`in_progress`. רלוונטי ישירות ללוגיקת ה-auto-advance של המודול הזה.

## ③ חלוקת עבודה

| ישי | Claude |
|---|---|
| בונה ומכריע; מוודא שמודול 3 מוזג ל-`dev` | בונה בלופרינט, מיישם, בודק |

## ④ החלק שלך

1. ודא שמודול 3 מוזג ל-`dev`. `git checkout dev && git pull`.
2. `git checkout -b ishay/module-5-logistics`.
3. הדבק את הפרומפט בסעיף ⑥, אשר בלופרינט, בנה צעד-צעד.

## ⑤ החלק של Claude

בלופרינט לפי התבנית: יצירת שורות `logistics` אוטומטית באישור הצעה (מסונן ל-`category ∈ {site,product}` — לא שירותי דיילות), מסך checklist עם עדכון סטטוס, לוגיקת auto-advance לפרויקט כש-100% מוכן, טיפול בשינוי תכולה (הוספה/הסרה משפיעה על ה-checklist). **מציית לשפת העיצוב וטבלת הצבעים ב-PROJECT_MASTER §4.** **מתחזק את `docs/micro_guides/module-5.md` חי לאורך כל הבנייה** — סטטוס צעדים, סטיות וחוב טכני מתעדכנים באותו סשן (ה-Stop hook אוכף).

## ⑥ 📋 ארבעת הפרומפטים להדבקה

> 💡 **בחירת מודל לפני הדבקה:** פרומפט 0 (Discovery) · פרומפט 1 (פתיחה) · פרומפט 3 (סגירה) —
> **Opus, מאמץ גבוה** (חשיבה: אפיון-מוצר · בלופרינט · RLS · אודיט). פרומפט 2 (המשך-בנייה) — לפי
> טבלת **Model & effort** שבראש סעיף 5 של מדריך המיקרו (ברירת מחדל: Sonnet לפזות בנייה, Opus
> לפזות DB/RLS ובקרה).

**0) Discovery (פעם אחת, לפני הכל — ‏3–4 ימים, לא סשן אחד):**
```
אנחנו עושים Discovery מלא למודול 5 — לוגיסטיקה.
קרא את docs/guides/prompt_module_discovery.md ובצע אותו כלשונו, משלב 1.
```

**1) פתיחת מודול — רק אחרי שה-Discovery אושר (פעם אחת):**
```
אנחנו בפרויקט REG-IN. קרא את CLAUDE.md ואת STATUS.md.
אנחנו פותחים את מודול 5 — לוגיסטיקה, על ענף ishay/module-5-logistics.

🔴 ולפני הכל — קרא את docs/specs/module_05_logistics/spec.md במלואו.
זהו האפיון המאושר של המודול (שיחת Discovery). בסעיף ① שלו יושבת רשימת-הקריאה המלאה שלך,
בסדר מחייב — ספור אותה שם ואל תמציא רשימה משלך.

קרא בעצמך את התבנית .claude/skills/module-blueprint/template.md ובצע אותה כלשונה עם:
MODULE_NUMBER=5 · MODULE_NAME=לוגיסטיקה · BRANCH_NAME=ishay/module-5-logistics
RELEVANT_SECTIONS=PROJECT_MASTER §5.8 · §5.9 (⇒ C5 §5.6.8 · §5.6.9) + §7.22 · §7.44
(אמת כל אחד מהם מול docs/specs/module_05_logistics/spec.md — האפיון המאושר גובר בכל מה שהוא מכסה בפועל)

הצג את הבלופרינט לאישורי; רק אחרי שאאשר — שמור אותו כ-docs/micro_guides/module-5.md.
```

**2) המשך בנייה (בכל סשן עבודה, עד שכל הצעדים ✅):**
```
אנחנו בפרויקט REG-IN. קרא את CLAUDE.md, STATUS.md ואת docs/micro_guides/module-5.md.
המשך מ"הצעד הפעיל" שבכותרת המצב. אמת בעצמך כל נקודת עצירה 🤖 (הרץ את פקודת האימות והצג ראיה),
ועצור לאישורי בכל נקודת 👤 ובסוף כל פזה.
נכנסת לפזה חדשה? אסוף קודם את כל השאלות הפתוחות/ממתינות-להנהון המעוגנות אליה בלדג'ר (סעיף 3)
והצג לי אותן לסגירה במרוכז (סגנון P13) לפני הצעד הראשון של הפזה. עדכן את מדריך המיקרו תוך כדי העבודה (סעיף 8 שלו).
בסוף הסשן: עדכן יומנים לפי הפרוטוקול ב-CLAUDE.md והסבר לי בעברית פשוטה איפה עצרנו ומה הבא.
```

**4) סגירת מודול (פעם אחת, כשכל הצעדים ✅):**
```
אנחנו בפרויקט REG-IN. קרא את CLAUDE.md, STATUS.md ואת docs/micro_guides/module-5.md.
קרא בעצמך את התבנית .claude/skills/module-close/template.md ובצע אותה כלשונה עם:
MODULE_NUMBER=5 · MODULE_NAME=לוגיסטיקה · BRANCH_NAME=ishay/module-5-logistics
בסוף: בצע את סעיף ההתמדה (עדכון מדריך המיקרו + היומנים, תאריך+שעה) והדפס לי את הוראות ה-PR
בעברית + פרומפט מסירה 🧩 לתוסף הכרום (כלל ברזל 17) לפתיחת ה-PR בפועל.
```

## ⑦ בדיקת קבלה

- [ ] אישור הצעת מחיר יוצר שורות checklist אוטומטית (רק פריטי site/product).
- [ ] סימון כל הפריטים "מוכן" מקדם את סטטוס הפרויקט.
- [ ] הוספת/הסרת פריט אחרי שהפרויקט "מוכן" מחזיר אותו ל"בתהליך".
- [ ] `npm run verify` ירוק.

## ⑧ אם משהו השתבש

- checklist ריק אחרי אישור הצעה → בדוק RLS על `logistics`, ואת הסינון לפי `category`.
- תקוע? → [ספריית הפרומפטים המצביים](../prompt_library.md) — חילוץ, סטייה מהתוכנית, undo, קונפליקט ועוד.

</div>
