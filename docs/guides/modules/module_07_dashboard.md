<div dir="rtl">

# 📊 שלב — מודול 7: מסך הבית / Dashboard

> מקומו בהיררכיית האמת: `schema.sql` › אפיון › מוקאפים › מדריך זה. בסתירה — המדריך מפסיד.
> צעדים שבוצעו מסומנים ✅ — המדריך כתוב כאילו כלום לא קיים.

## ① מה נשיג בשלב הזה

מסך הבית: 4 כרטיסי KPI (רווח חודשי משוער, פרויקטים פעילים, שביעות רצון ממוצעת, הצעות ממתינות) + לוח שנה חודשי צבוע (אדום/צהוב/ירוק) לפי מוכנות אירועים. מסך 5.2 באפיון. **נגיש לכולם — לא שורה במטריצת ההרשאות** (הוכרע ב-[§7.10](../../PROJECT_MASTER.md)).

## ② תנאי כניסה

- [ ] מודול 6 (פרויקטים) מוזג — ה-Dashboard קורא מהפרויקטים.
- ✅ **§7.18 — סגור 03/09/2026, `PROJECT_MASTER_sec7.md` פריט 94.** חוקי אדום/צהוב/ירוק: "חוסר" = שיבוץ-לא-מלא **או** לוגיסטיקה-לא-מלאה (לא "וגם"); 🔴=חוסר+בתוך 14 יום · 🟡=חוסר רחוק-יותר · 🟢=אין חוסר.
- ✅ **§7.37 — סגור 03/09/2026, פריט 95.** חצי-השקלול סגור מ-26/08 (אין נוסחה, ציון אחד ידני); **החלון: 90-יום נגלל, לא רבעון קלנדרי.**
- ✅ **§7.9 — סגור, לא פתוח. 🔴 גובר: הוכרע 13/08/2026 · המקור: `PROJECT_MASTER_sec7.md` פריט 9 (🟢 `סגור·לוגיקה·מ6`).** הנוסחה **בוטלה ואינה נבנית**; במקומה הכרעת-ישי, מילה-במילה: *"רשימה אחת ממוינת, וכל שורה אומרת במילים למה היא שם"*. שלושת נימוקיו מדודים, ואחד מהם נוגע ישירות ללוח-השנה של מ7: **צביעה או מיון לפי קרבת-התאריך מקודדת את המשתנה שהמנהלת כבר יודעת.** ⚠️ **התיקון נכתב 03/09/2026 באודיט-סגירת מ9** — השורה הזו סימנה ⏳ ושלחה סשן טרי *לפתוח מחדש הכרעה סגורה*, וזה בדיוק המקרה שכלל-ברזל 1 (סתירה, מקרה ②) אוסר. *(הנוסח הישן, כהיסטוריה:)* ⏳ **§7.9** — נוסחת "דחיפות תפעולית" מהמוקאפ (50%/30%/20%) — לא אותרה במפורש באפיון; להכריע אם זו דרישה אמיתית או תוספת מוקאפ.
- ✅ **§7.59 — סגור 03/09/2026, פריט 96.** אוכלוסייה = פרויקטים עם `final_event_date` בחודש-היעד; `final_profit` אם סגור, אחרת רווח-צפוי; **מבוטל עם דמי-ביטול-שנפתרו נכלל כמו כל פרויקט אחר** (תקדים: §7.79).
- ✅ **§7.97 (מיסוך-הרשאות, 03/09/2026, פריט 97).** כרטיסי "רווח חודשי" ו"הצעות ממתינות" ממוסכים למנכ"ל+כספים בלבד — לא ירדו מהמסך, לא נגישים לשלוש התפקידים האחרות. ⚠️ **תוקן בבלופרינט עצמו:** `project_finance` **לא** צריכה policy חדשה — למודול 8 כבר יש policy זהה בדיוק (אותם שני תפקידים), וה-RPC המרכז (security definer) עוקף RLS ועושה את המיסוך בעצמו מבפנים. פרטים: `docs/micro_guides/module-7.md §4`.

## ③ חלוקת עבודה

**כל ארבעת פריטי-§7 סגורים (94/95/96/97) — אין יותר הכרעה חסומה על המדריך הזה.** 🚫 **§7.9 גם הוא סגור מ-13/08/2026, לא נפתח מחדש.**

## ④ החלק שלך

1. ✅ **הבלופרינט כבר נכתב ואושר-לקריאה** — `docs/micro_guides/module-7.md` (קומיט `b9ba153`), על הענף המשותף `ishay/m7-blueprint-and-seed` **(לא `ishay/module-7-dashboard` — סטייה מתועדת, נשארים על הענף הקיים, לא משנים שם באמצע)**. ✅ **המוקאפ המאושר גם הוא כבר בגיט** — `docs/specs/module_07_dashboard/dashboard-mockup-approved.html` (קומיט `ac98ae7`), אחרי ארבעה סבבי-תיקון אמיתיים מולך.
2. **פרומפט ⑥1 (פתיחה) כבר בוצע ולא רץ שוב.** נשאר רק ⑥2 — הדבק אותו בסשן חדש כדי להתחיל בפועל בפזה 1.

## ⑤ החלק של Claude

**המנגנון שהוכרע בפועל (לא "4 שאילתות/Views" כפי שנוסח כאן לפני הבלופרינט):** RPC-מרכז אחד (`get_dashboard_summary`) שמחזיר את כל ארבעת ה-KPI + מערך-ימי-הלוח בקריאה אחת, עם מיסוך-הכספים מובנה בגוף הפונקציה (לא policy נפרדת — ר' §4 בבלופרינט). **מציית לשפת העיצוב ולטבלת הצבעים ב-PROJECT_MASTER §4**, ולמוקאפ המאושר במדויק — כולל שני תיקוני-CSS ספציפיים שנתפסו בסבב-האישור (`grid-auto-rows` לגובה-שורות אחיד, `minmax(0,1fr)` לרוחב-עמודות אחיד) — **אל תאבד אותם בבנייה מחדש.** **מתחזק את `docs/micro_guides/module-7.md` חי לאורך כל הבנייה** (ה-Stop hook אוכף).

## ⑥ 📋 הפרומפטים להדבקה

> 💡 **בחירת מודל:** לפי טבלת **Model & effort** שבראש סעיף 5 של `docs/micro_guides/module-7.md` (בקצרה: Opus/גבוה לפזות 1+5, Sonnet לפזות 2–4).

**‏⑥1) פתיחת מודול — ✅ כבר בוצע, לא להריץ שוב.** *(הפרומפט המקורי נשמר כאן כהיסטוריה בלבד — הבלופרינט שהוא היה אמור להפיק כבר קיים ומאושר-לקריאה.)*

**‏⑥2) המשך בנייה — זה מה שמדביקים עכשיו, בסשן חדש:**
```
אנחנו בפרויקט REG-IN. קרא את CLAUDE.md, STATUS.md ואת docs/micro_guides/module-7.md.
הבלופרינט כבר מאושר-לקריאה על-ידי ישי; המוקאפ המאושר ב-
docs/specs/module_07_dashboard/dashboard-mockup-approved.html.
המשך מ"הצעד הפעיל" שבכותרת המצב (פזה 1, צעד 1.1 — אין עוד צעד קודם). אמת בעצמך כל נקודת עצירה 🤖
(הרץ את פקודת האימות והצג ראיה), ועצור לאישורי בכל נקודת 👤 ובסוף כל פזה — כולל אישורי-הקלדה
למיגרציות (פרוטוקול-ה-DB ב-supabase/migrations/CLAUDE.md).
נכנסת לפזה חדשה? אסוף קודם את כל השאלות הפתוחות/ממתינות-להנהון המעוגנות אליה בלדג'ר (סעיף 3
של מדריך-המיקרו) והצג לי אותן לסגירה במרוכז לפני הצעד הראשון של הפזה — אבל שים לב: כרגע **אין**
פריט פתוח, כל §7 הרלוונטי כבר סגור. עדכן את מדריך המיקרו תוך כדי העבודה (סעיף 8 שלו).
בסוף הסשן: עדכן יומנים לפי הפרוטוקול ב-CLAUDE.md והסבר לי בעברית פשוטה איפה עצרנו ומה הבא.
```

**‏⑥3) סגירת מודול — מוכן ומעודכן, אבל 🚫 לא להפעיל עד שכל צעדי-הבנייה ✅ בפועל:**
```
אנחנו בפרויקט REG-IN. קרא את CLAUDE.md, STATUS.md ואת docs/micro_guides/module-7.md.
קרא בעצמך את התבנית .claude/skills/module-close/template.md ובצע אותה כלשונה עם:
MODULE_NUMBER=7 · MODULE_NAME=מסך הבית / Dashboard · BRANCH_NAME=ishay/m7-blueprint-and-seed
בסוף: בצע את סעיף ההתמדה (עדכון מדריך המיקרו + היומנים, תאריך+שעה) והדפס לי את הוראות ה-PR
בעברית + פרומפט מסירה 🧩 לתוסף הכרום (כלל ברזל 17) לפתיחת ה-PR בפועל.
```

## ⑦ בדיקת קבלה

- [ ] 4 ה-KPI מציגים נתונים נכונים מול פרויקט seed ידוע (אפשר לחשב ידנית ולהשוות — לא לצטט את הנוסחה שחישבה אותם).
- [ ] לוח השנה צובע נכון לפי §7.94, וגם נראה כמו המוקאפ המאושר (שם-פרויקט בכל תא, אייקוני איוש/לוגיסטיקה, לא רק צבע).
- [ ] כרטיסי-הכסף ממוסכים ("לא זמין בתפקידך") לשלוש התפקידים הלא-כספיות — לא `0 ₪`.
- [ ] כל 5 התפקידים רואים את המסך (בלי `<ProtectedRoute>`).
- [ ] `npm run gate` ירוק, `test:e2e` + `smoke` נקובים בנפרד (אף אחד מהם לא רץ ב-`gate`).

## ⑧ אם משהו השתבש

- KPI מציג מספר שגוי → בדוק אם החישוב כולל/לא-כולל פרויקטים מבוטלים כנדרש.
- תקוע? → [ספריית הפרומפטים המצביים](../prompt_library.md) — חילוץ, סטייה מהתוכנית, undo, קונפליקט ועוד.

</div>
