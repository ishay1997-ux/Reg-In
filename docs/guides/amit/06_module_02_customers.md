<div dir="rtl">

# 👔 שלב — מודול 2: לקוחות

> מקומו בהיררכיית האמת: `schema.sql` › אפיון › מוקאפים › מדריך זה. בסתירה — המדריך מפסיד.
> צעדים שבוצעו מסומנים ✅ — המדריך כתוב כאילו כלום לא קיים.

## ① מה נשיג בשלב הזה

מאגר לקוחות: הוספה/עריכה/ארכוב (סטטוס `active`/`inactive`, בלי מחיקה), סינון, הפצת שיווק (`marketing_consent`). מסך 5.3 באפיון. **זה המודול הראשון שכותב RLS policies על טבלה עסקית** — קדימות אמיתית, לא רק תרגיל.

## ② תנאי כניסה

- [ ] מודול 1 מוזג ל-`dev` ([ishay/01](../ishay/01_close_module_1.md) הושלם).
- 🛑 **שאלות פתוחות שחייבות הכרעה עם ישי לפני הבנייה** (מ-[PROJECT_MASTER §7](../../PROJECT_MASTER.md)):
  - **§7.21** — מודל בעלות-דאטה ל-RLS (אמור להיות מוכרע כבר ב-ishay/01 — **אמת שהוא סגור** לפני שאתה כותב policy ראשון).
  - **§7.3** — מיפוי `customer_type` (enum) לתוויות המוקאפ.
  - **§7.11** — מדיניות מיזוג/כפילות לקוחות.

## ③ חלוקת עבודה

| ישי | אתה | Claude |
|---|---|---|
| מכריע בשאלות ה-🛑 (בטלפון/וואטסאפ אם צריך) | בונה עם Claude, פותח PR | בונה בלופרינט, מיישם |

## ④ החלק שלך

1. אמת עם ישי ש-§7.21 סגור (מודל ה-RLS). בלי זה — אל תתחיל לכתוב policies.
2. `git checkout dev && git pull && git checkout -b amit/module-2-customers`.
3. הדבק את הפרומפט בסעיף ⑥. אשר את הבלופרינט. אמור "בוא נעשה צעד 1" וכן הלאה.
4. בסוף — פרומפט הסגירה, ואז PR (ר' [04_git_and_module_workflow.md](04_git_and_module_workflow.md)).

## ⑤ החלק של Claude

בלופרינט מלא לפי `docs/templates/create_micro_guide_template.md`: **Phase 1** — RLS policies על `customers` (הראשונות מסוג הזה בפרויקט; לפי המודל שהוכרע ב-§7.21), constraints, מיפוי enum. **Phase 2** — מסך רשימה+סינון, טופס הוספה/עריכה, ארכוב דו-כיווני (בלי מסגור "מחיקה" — לפי המוסכמה שנקבעה במודול 1). **Phase 3-4** — בקרה ו-QA. **מציית לשפת העיצוב ולטבלת הצבעים ב-PROJECT_MASTER §4.**

## ⑥ 📋 הפרומפט להדבקה

**פתיחה:**
```
אנחנו בפרויקט REG-IN. קרא את CLAUDE.md, STATUS.md, docs/guides/amit/06_module_02_customers.md
ואת docs/PROJECT_MASTER.md (סעיף 5.3, §7 פריטים 3, 11, 21).
אנחנו פותחים את מודול 2 — לקוחות, על ענף amit/module-2-customers.

זו ה-RLS policy הראשונה על טבלה עסקית בפרויקט — היא תשמש תבנית לכל המודולים הבאים.
תזכיר לי במפורש את ההכרעה ב-§7.21 לפני שאתה כותב אותה.

בצע את התבנית הבאה (docs/templates/create_micro_guide_template.md) עם:
MODULE_NUMBER=2 · MODULE_NAME=לקוחות · RELEVANT_SECTIONS=§5.3, §7.3/11/21 · BRANCH_NAME=amit/module-2-customers
קרא בעצמך את קובץ התבנית docs/templates/create_micro_guide_template.md ובצע אותו כלשונו עם הפרמטרים שלמעלה (אין צורך שאדביק את תוכנו).

חובה: "שאלות פתוחות" מסעיף ② במדריך שלי נכנסות לטריאז' 🛑/⏳ שלך — אל תכריע בהן לבד.
```

**סגירה:** `create_module_final_test_template.md` עם `MODULE_NUMBER=2`.

## ⑦ בדיקת קבלה

- [ ] מנהלת כספים ולקוחות יוצרת לקוח חדש ורואה אותו ברשימה.
- [ ] מנהלת לוגיסטיקה **לא** רואה את מודול הלקוחות (חסום לפי המטריצה).
- [ ] סינון/חיפוש עובד.
- [ ] ארכוב לקוח משנה סטטוס בלי למחוק, והופך הפיך.
- [ ] `npm run verify` ירוק; PR נפתח.

## ⑧ אם משהו השתבש

- מסך ריק אחרי שהוספת RLS → בדוק שה-policy אכן מפנה ל-`current_user_role_id()` בצורה תקינה (ר' `CLAUDE.md` — פרוטוקול DB).
- verify אדום → `regin-pr-gate` תאבחן.
- תקוע? → פרומפט חילוץ ב-[README](../../../README.md).

</div>
