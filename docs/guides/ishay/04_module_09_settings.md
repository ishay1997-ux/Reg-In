<div dir="rtl">

# ⚙️ שלב — מודול 9: הגדרות מערכת

> מקומו בהיררכיית האמת: `schema.sql` › אפיון › מוקאפים › מדריך זה. בסתירה — המדריך מפסיד.
> צעדים שבוצעו מסומנים ✅ — המדריך כתוב כאילו כלום לא קיים.

## ① מה נשיג בשלב הזה

מסך "הגדרות מערכת" (מנכ"ל בלבד) לעריכת טבלת `params` בלי SQL: מע"מ, יחס אורחים-לדיילת, משקולות Smart Match, תבניות מייל. מסך 5.16 באפיון.

## ② תנאי כניסה

- [ ] מודול 1 מוזג (מסך זה permission-driven, כמו ניהול משתמשים).
- [ ] מודול 3 (עמית) ביצע את ה-Seed הראשוני ל-`params` — המסך הזה עורך ערכים קיימים, לא יוצר אותם מאפס.
- ⏳ ערכי ה-params הסופיים תלויים בהכרעות §7.1 (מע"מ) ו-§7.15 (שם W3) שהוכרעו/יוכרעו במודול 3.

## ③ חלוקת עבודה

| ישי | עמית | Claude |
|---|---|---|
| בונה ומכריע | מודיע כש-Seed של params בוצע | בונה בלופרינט, מיישם |

## ④ החלק שלך

1. ודא עם עמית ש-Seed של `params` בוצע (מודול 3).
2. `git checkout -b ishay/module-9-settings`.
3. הדבק פרומפט, אשר בלופרינט, בנה.

## ⑤ החלק של Claude

בלופרינט: מסך טבלה של `params` מקובץ לפי `param_type`, טופס עריכת ערך עם ולידציה מתאימה לטיפוס (אחוז, מספר, טקסט), מנכ"ל-בלבד (RLS + ProtectedRoute). **מציית לשפת העיצוב וטבלת הצבעים ב-PROJECT_MASTER §4.**

## ⑥ 📋 הפרומפט להדבקה

**פתיחה:**
```
אנחנו בפרויקט REG-IN. קרא את CLAUDE.md, STATUS.md, docs/guides/ishay/04_module_09_settings.md
ואת docs/PROJECT_MASTER.md (סעיף 5.16).
אנחנו פותחים את מודול 9 — הגדרות מערכת, על ענף ishay/module-9-settings.

בצע את התבנית הבאה (docs/templates/create_micro_guide_template.md) עם:
MODULE_NUMBER=9 · MODULE_NAME=הגדרות מערכת · RELEVANT_SECTIONS=§5.16 · BRANCH_NAME=ishay/module-9-settings
[הדבק כאן את תוכן התבנית המלא מהקובץ]
```

**סגירה:** `create_module_final_test_template.md` עם `MODULE_NUMBER=9`.

## ⑦ בדיקת קבלה

- [ ] מנכ"ל משנה ערך מע"מ ב-`params` → הצעת מחיר חדשה מחושבת לפי הערך החדש.
- [ ] משתמש שאינו מנכ"ל לא רואה את המסך כלל.
- [ ] `npm run verify` ירוק.

## ⑧ אם משהו השתבש

- ערך לא משתקף בהצעה חדשה → בדוק שהקוד של מודול 3 קורא מ-`params` בזמן-ריצה ולא מקבוע מוקשח.
- תקוע? → פרומפט חילוץ ב-[README](../../../README.md).

</div>
