<div dir="rtl">

# ‏`module-discovery-v2` — ארכיון, ‏13/08/2026

> **קריאה בלבד.** ‏`docs/archive/` אינו נערך ואינו נטען. **הסקיל החי הוא
> `.claude/skills/module-discovery/`, והוא היחיד.**

## מה זה היה

**מועמד-שכתוב לסקיל ה-Discovery**, שנכתב **בסשן מקביל** ב-13/08/2026 מתוך **צילום** של הגרסה
שהייתה חיה באותו יום. הוא ישב ב-`.claude/skills/module-discovery-v2/` והוגדר
`disable-model-invocation: true` — כלומר **מעולם לא נדלק מעצמו**, ולכן שתי הגרסאות לא התנגשו בפועל.

## למה הוא כאן ולא בסקילים

**כי הוא ניצח, ותוכנו כבר בפנים.** ‏13/08/2026 רץ **מבחן עיוור בן 6 סוכנים** — שתי הגרסאות נמסרו
כ-**A** *(החיה)* ו-**B** *(זו)*, בלי שמות ובלי רמז מי כתב מה:

| המדד | ‏A *(החיה אז)* | **B *(זו)*** |
|---|:-:|:-:|
| חוסמים בבדיקת-עובדות | 3 | **1** |
| הכרעות-ישי שנשמרו *(מתוך 131)* | 123 | **128** |
| כיסוי-מכניקה | 76% | **90%** |
| ניחושים-שמשנים-אפיון *(ריצה חיה, שלב 0 של מודול 5)* | 6 | 6 — **תיקו** |
| **ציון משוקלל** | ‏~70 | **‏~82** |

**בהכרעת-ישי מוזג: הקובץ הזה נעשה הבסיס**, ונוספו לו **ארבעה פריטים שחסרו בשתי הגרסאות** —
בדיקת `docs/plans/` *(שגם מפילה את ה-Stop hook)* · *"קרא §7·§8·§9 לפני שלב 1"* · תיקון פקודה
שנפלה על עצמה *(`grep … docs/specs/module_NN_*/` ⇒ `exit 2`)* · והסרת הפניית `§1.8` שאינה נפתרת.

⇒ **הקובץ החי גדול מזה שכאן.** ‏**אין סיבה לקרוא כאן במקום שם.** *(קומיט המיזוג: `fea4ef8`.)*

## מתי כן לפתוח את זה

**רק כדי לענות על *"מה היה כתוב כאן לפני המיזוג"*** — למשל אם מתגלה שמשהו נשמט. **ההשוואה המלאה
והמספרים חיים ב-`docs/guides/discovery_lessons.md` וביומן**, לא כאן.

</div>
