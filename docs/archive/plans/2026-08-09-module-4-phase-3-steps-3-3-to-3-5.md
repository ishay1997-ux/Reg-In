<div dir="rtl">

# מודול 4 · פזה 3 — צעדים 3.3 · 3.4 · 3.5 (מבט-על · Smart Match · תפריט-השורה)

> 🗣️ **תקציר-החוויה של יחידת-הבנייה. אני במצב-תכנון — לא ייכתב קוד עד שתאשר.**
>
> **מה קראתי בתור הזה** *(לפי `spec.md §①`, רשימת-הקריאה של המודול)*: ‏`spec.md` §①/§1.1/§"מה
> ייחשב עובד" · **שלושת כרטיסי-המסך במלואם** — מסך 1 (מבט-על), מסך 2 (Smart Match), מסך 4
> (תפריט-שורה) ב-`screens-approved.md` · `processes-approved.md` §ב (תהליך-הזימון, 48 השעות,
> T-24) · **המוקאפ המאושר `01_overview_approved.html` במלואו** + סריקת-מבנה על `02` ו-`04` ·
> `micro_guides/module-4.md` §1/§2/§3/§6-פזה-3/§10 · `src/CLAUDE.md` · `04_hostesses/CLAUDE.md` ·
> ‏`api.js` · `lib/hostesses.js` · `lib/smartMatch.js` · `lib/email.js` · `api/email.js` ·
> **והמסד החי** (ספירות, סכמת `assignments`, הטריגרים, ארבע תבניות-המייל).

---

## 🧭 למה שלושת הצעדים יחד, ולא רק 3.3

**אישרת לי לבחור** *(09/08)*, וזו הבחירה **ולמה**:

🔴 **‏3.3 לבדו אינו ניתן לאימות — מדדתי: יש במסד `0` שורות-שיבוץ.**
מבט-העל הוא מסך שכל תוכנו הוא **מה קרה עם הזימונים**: חמישה מונים, הפיצול `ממתינות · מתוכן
פג תוקפן`, וכפתור `שלח שוב`. עם אפס זימונים המסך מציג שלוש שורות של `0/6`, כל המונים אפס,
וכל כפתורי-השליחה כבויים. **אפשר לבנות אותו נכון — אי-אפשר להוכיח שהוא נכון.**

**ומי יוצר את השורות האלה? בדיוק 3.4 ו-3.5.** ‏3.4 שולח את הזימונים, ו-3.5 רושם את התשובות
שהגיעו בטלפון. ⇒ שלושתם **מעגל אחד**: רואה חור ← מדרגת מועמדות ← שולחת ← רושמת תשובה ←
מאשרת סופית. גם שכבת-הכתיבה ומנוע-המייל משותפים לשלושתם — פיצול היה בונה אותם פעמיים.

📌 **תקדים:** צעדים 3.0+3.1+3.2 נבנו כיחידה אחת מאושרת ונחתו נקי. זו יחידה **גדולה יותר**,
ולכן היא מחולקת למטה לתת-צעדים שכל אחד נגמר בראיה — **אפשר לעצור בנקי בין תת-צעדים.**

🚫 **מה **לא** נכנס:** ‏3.6 (הדף הציבורי) ו-3.7 (שער-הפזה). ‏⚠️ **והמשמעות שאני אומר בקול:**
הזימונים שיישלחו כאן נושאים `[לינק_אישור_משמרת]`, **והדף שהלינק מוביל אליו ייבנה רק ב-3.6** ⇒
עד אז הלחיצה על הלינק תיתן דף-שגיאה. מקובל, כי המיילים יוצאים לכתובות-דמו ולתיבה שלך בלבד.

---

## 🔴 מה מצאתי לפני שהתחלתי — ארבעה דברים, ושניים מהם שינו את התוכנית

### ① 🚨 המיילים של 20 הדיילות מגיעים לאנשים אמיתיים — **הכרעת: 19 לדומיין-דמו + אחת שלך**
‏`noa.sagi@gmail.com` · `dana.barak@gmail.com` · `michal.avraham@gmail.com` — כתובות
‏`@gmail.com` תקפות לחלוטין. **לחיצה אחת על "שלח מייל תיאום" הייתה שולחת מייל אמיתי לאדם זר.**

**מה ייעשה, לפני שנכתבת ולו שורת-שליחה אחת:** ‏19 הדיילות יעברו ל-`<שם>@regin-demo.co.il`
*(הקונבנציה שכבר קיימת בריפו — `tal@hitechgroup-demo.co.il`)*, ו**נועה שגיא תקבל את
`ishay1997@gmail.com`** — כדי שבכנס תלחץ "שלח" ותראה מייל אמיתי נוחת. 🔴 **אשאל אותך לאישור
הכתובת לפני העדכון** — לא אכתוב את הכתובת שלך למסד על סמך מה שמופיע בהקשר-הסשן.
**השמות, הערים, התעריפים והקואורדינטות — לא זזים.** העדכון דרך `updateHostess` האמיתי.

### ② אירוע שהתאריך שלו עבר — **ההשלכות, ואז ההכרעה שביקשת שאעזור לך בה**

**מה שיש במסד:** ‏`תרחיש-קבלה 5.1` · 01/08/2026 · חסרות 6 · סטטוס `טרם החל`.

**למה זה לא ייעלם לבד:** פרויקט יוצא מהרשימה כשהוא עובר ל-`מוכן לביצוע` — **ומודול 4 לעולם
אינו כותב את הסטטוס הזה** *(זה `🚧 מ6 ← מ4`)*. ⇒ בלי כלל-תאריך הוא יישב בראש המסך **לתמיד**,
וכל אירוע עתידי שיעבור יצטרף אליו. בעוד חצי שנה זה בית-קברות בראש מסך-הטריאז'.

**‏3 ההשלכות של "להסתיר":**
· **מה נעלם:** שורה שאי-אפשר לפעול עליה — **אי-אפשר לאייש את אתמול.** הקישור הציבורי ממילא
מת 24 שעות לפני האירוע, אז גם "שלח שוב" לא היה עוזר שם.
· **מה **לא** נעלם:** כלום מהמסד. הפרויקט שלם, השיבוצים שלו שלמים, ו**מ6 (סגירה תפעולית) ומ8
(שכר) רואים אותו במלואו** — הם הבעלים של "מה קרה אחרי האירוע".
· **מה נפתח:** מצב "ריק אמיתי" הופך **בר-הגעה** — *"אין כרגע אירועים הממתינים לאיוש"*,
שהאפיון מגדיר כ**בשורה טובה** ולא כשגיאה (כרטיס מסך 1 §⑥).

⇒ **ההכרעה שלי: מסתירים אירוע שתאריכו לפני היום.** העוגן: §② של הכרטיס — *"על איזה אירוע אני
נכנסת לטפל **עכשיו**"*, ו*"כל רכיב שאינו עוזר לדרג בין אירוע לאירוע — יוצא"*.
🔴 **והגבול המדויק, כי הוא חשוב: `final_event_date < היום` מסתיר. אירוע של היום נשאר** — ביום
האירוע היא עדיין סוגרת חורים בטלפון, וזה בדיוק מה ש-`אושרה סופית — סוכם בטלפון` קיים בשבילו.
📌 נרשם כהנחה (`הנחתי`) ב-§10 של מדריך-המיקרו, כי האפיון שותק. **תופעת-לוואי:** השם הטכני
`תרחיש-קבלה 5.1 (מדריך-מיקרו §Step 5.1)` נעלם מהמסך שתראה בכנס, בלי למחוק כלום.

### ③ באג הכיווניות — **מתקנים במנוע** (הכרעתך, 09/08)
`plainTextToEmailHtml` ב-`src/lib/email.js` יבודד כל רצף לטיני/סימן בתוך משפט עברי (LRI…PDI,
בדיוק כמו התיקון שכבר עשינו ל-`minWageError`). ⇒ כל 9 התבניות מרוויחות, וגם של מ8/מ11.
**מחיר:** קובץ משותף ⇒ בדיקות-יחידה + **הרצה חוזרת של `quote-email.spec.js`+`quote-document.spec.js`**
כדי להוכיח שמסלול הצעות-המחיר לא נשבר. **ואימות אמיתי:** לפתוח מייל שנשלח ולראות `REG-IN!`.

### ④ שני פערים קטנים בין המוקאפ המאושר לבין עצמו — הכרעתי, בלי לשאול
· **מסנן ברירת-המחדל:** המוקאפ מצייר את `הצג חסרים בלבד` **דלוק** — **ומציג באותה נשימה שורה
מאויישת** (`5/5`). §④ של הכרטיס קובע *"חסרים תחילה… **והמאוישים אחריהם**"* ⇒ **ברירת-המחדל
היא `הכול`**, והמיון עושה את העבודה. *(התוכן של הרשימה הוא התנהגות ⇒ האפיון גובר.)*
· **אריחי ה-KPI:** המוקאפ מצייר ערך-מעל-תווית; ‏`src/CLAUDE.md` מחייב את `StatTile` שהוא
תווית-מעל-ערך, **וכך כבר בנוי כרטיס-הדיילת של מודול 4 עצמו.** ⇒ **`StatTile`.** העוגן שמתיר
לי להכריע: הכרטיס עצמו רושם שאישרת את מסך 1 ב**האצלה** — *"נותן לך להחליט על זה"*.
· ➕ **ו-`🔗 פתיחה בלשונית חדשה` שכתוב ב-§⑧ — אינו במוקאפ המאושר בפועל.** לא נבנה.

---

## 🏗️ מה ייבנה, בסדר הזה

### ‏🅰️ הכנה (לפני כל שליחה) — כתובות-הדמו + תיקון-המנוע
`updateHostess` על 19+1 · תיקון `plainTextToEmailHtml` + בדיקות · רגרסיית מיילי-ההצעות (8/8).

### ‏🅱️ צעד 3.3 · מבט-על — `OverviewTab.jsx`
**קריאות בלבד.** ‏`listStaffingOverview` כבר קיים ב-`api.js` וקורא את שם-הלקוח מ-`projects.customer_name`
*(לא בצירוף ל-`customers`, שמנהלת-הגיוס חסומה עליו)*.
**חדש ב-`src/lib/hostesses.js`, בדיקות ראשונות ואדומות לפני הקוד:** ‏`overviewRowMetrics`
(מאויישות/חסרות מתוך `countAssignmentStates` הקיים) · `sortOverviewRows` (חסרים תחילה ← קרבת-אירוע)
· `isPastEvent`. **המונים עצמם מ-`countAssignmentStates` הקיים — לא נכתבים מחדש.**
**המסך:** שני KPI · שלושת המסננים · טבלה בת שש עמודות · חמשת המונים + `N פג תוקפן` · ⚠ T-24
*(נגזר בזמן-קריאה, §7.91)* · **ארבעת המצבים** (טעינה · ריק-אמיתי · ריק-אחרי-סינון · שגיאה+"נסה שוב")
· **הרשאות: למנהלת-פרויקטים אין עמודת-פעולה כלל.**
‏`HostessesPage` חוזר לפתוח על לשונית מבט-העל, והמציין-הזמני יורד.

### ‏🅲 צעד 3.4 · Smart Match — `SmartMatchPage.jsx` **(התת-צעד הגדול)**
· **‏C1 · הרכבת-המועמדות:** מ-`getSmartMatchData` אל הקלט ש-`rankCandidates` דורש (היענות,
מרחק דרך `candidateDistanceKm`, שבועות-מאז, העדפה, אי-זמינות, כפילות-תאריך). **בדיקות ראשונות.**
🎯 **ואז מריץ מחדש את העוגן `נועה 0.67 · מיכל 0.66 · דנה 0.64` — הוא לא זז.**
· **‏C2 · ארבע הזוויות** — מסדרות בלבד, לעולם לא מסננות. `תענה הכי מהר` **כבויה-ומנומקת**
(`responded_at` ריק).
· **‏C3 · כתיבות** ב-`api.js`: יצירת זימונים (`assignment_number = max+1` עם **ניסיון-חוזר אחד**
על `unique_violation` — §7.41↳; טוקן; `hourly_rate_snapshot` מוקפא) · אישור-סופי · **והשחרור
האוטומטי שנוסע איתו באותה פעולה** (`local-13`). ‏`event_date` נכתב ע"י טריגר — נמדד.
· **‏C4 · המסך:** שני טורים · ארבעה מונים · כרטיסי-מועמדת עם **שתי משפחות-צ'יפים נפרדות**
(`score` מול `ctx`) · ★ אחראית-משמרת · **המסך אומר בקול ש"אמינות ההגעה כבוי"** ולא מציג אפס שקט.
· **עודף-אישורים = אזהרה ולא חסימה · כפל-תאריך = חסימה מהמסד**, והשורה חוזרת למצבה הקודם.

### ‏🅳 צעד 3.5 · תפריט-השורה — `AssignmentRowMenu.jsx`
תוכן נגזר-סטטוס · `שלח את הקישור שוב` **מכובה-ומנומק בשלושה מצבים** (T-24 · האירוע אויש ·
השורה אינה `ממתינה`) · `פתח זימון חדש` יוצר **שורה שנייה** · **שתי פעולות-שחרור נפרדות** ·
סימונים ידניים · חיוג · **ולמי שאין עריכה — `⋯` לא קיים כלל.**
➕ **וכאן מתחברים כפתורי `שלח שוב` של מבט-העל** — אותה פונקציה בדיוק, לא עותק.

### ‏🅴 אימות מקצה-לקצה עם דאטה אמיתית
שולח **2–3 זימונים אמיתיים** דרך המסך (לכתובות-דמו + לתיבה שלך), רושם תשובות דרך תפריט-השורה,
מאשר סופית — **ואז חוזר למבט-על ומצלם אותו עם מונים אמיתיים.** בדיוק המסלול שבו נוצרו
20 הדיילות: דרך האפליקציה, לא ב-SQL.

---

## ✅ מה ייחשב עובד — מצוטט מהאפיון, ונכתב גם למדריך-המיקרו

**מבט-על (3.3)** — `screens-approved` מסך 1 §②/③ + `spec.md §"מה ייחשב עובד"` #1/#4
1. *"המנהלת פותחת את המבט-על ויודעת מיד לאיזה אירוע להיכנס"* — **הסדר עונה, לא רשימה.**
2. אירוע שבו אישרו זמינות ואיש לא אושר סופית **אינו נראה זהה** לאירוע שאיש לא ענה בו.
3. *"'3 ממתינות' אומר תני להן זמן; '3 פג תוקפן' אומר שלחי לעוד שלוש, עכשיו."*
4. 🔴 *"מסך שלא הצליח לטעון אומר זאת. לעולם לא רשימה ריקה בשקט"* — נפילת-טעינה מציגה שגיאה
   ו"נסה שוב", **ולא** *"אין כרגע אירועים"*. *(זה החצי שהמסד אינו יכול לאותת.)*
5. `יחידה-ספציפית` — **מנהלת פרויקטים רואה את הרשימה ואין לה אף כפתור-שליחה.** שני הכיוונים.

**‏Smart Match (3.4)** — `spec.md` #2 + `screens-approved` מסך 2 §④/⑥
1. *"רשימה מדורגת שהיא יכולה להסביר לעצמה — כל שורה נושאת למה היא שם."*
2. *"'אמינות ההגעה כבוי' = המרכיב מוסר מהנוסחה והמשקלים מתחלקים מחדש"* — ולא אפס שקט.
3. *"ארבע הזוויות מסדרות בלבד — אינן מסננות."*
4. **העוגן המחושב-ביד משתחזר: `0.67 · 0.66 · 0.64`, בסדר הזה, אחרי הבנייה.**

**תפריט-השורה (3.5)** — `screens-approved` מסך 4 §②/③
1. *"מה קרה עם הזימון הזה — ואיך אני רושמת מידע שהגיע בטלפון ולא דרך הקישור."*
2. *"אילו שתיהן היו נקראות 'שלח שוב', המנהלת הייתה מוחקת היסטוריית-מענה בלי לדעת."*
3. *"פריט-תפריט אחד לשתיהן היה מזייף את הציון של דיילת חפה-מפשע."*

---

## 🔬 איך יאומת

**בדיקות-יחידה — נכתבות ראשונות ונצפות אדומות**, ו🔴 **כל בדיקה חדשה נבדקת מול קוד ששבור
בכוונה לפני שסומכים עליה** *(זו הפעם הרביעית שהמלכודת הזאת חוזרת במודול הזה)*.
**בדפדפן חי, מחובר, עם צילום לכל אחד:** ארבעת מצבי מבט-העל · תפריט-השורה **בכל שישה הסטטוסים** ·
שני כיווני-ההרשאה · מייל אמיתי שנפתח בתיבה ומראה `REG-IN!` תקין.
**שערים:** `npm run gate` · `npm run test:run` · `npm run smoke` · **‏+חבילת מיילי-ההצעות** (בגלל
הנגיעה במנוע המשותף). ⚠️ **‏`test:e2e` אינו רץ ב-CI ואינו כולל את העשן** — אדווח כל אחד בשמו.

---

## ⚠️ מה אני אומר מראש שלא ייסגר כאן
· **הלינק בזימון יוביל לדף-שגיאה עד 3.6.** מוצהר, לא מוסתר.
· **מרכיב האמינות ונעיצת-"מצוינת" נשארים כבויים** — הם של מ6, והמסך אומר זאת בקול.
· **שער 1.5 עדיין פתוח אליך** — הרצת `regin-docs-sync`.

## 🌊 אדוות-סיום (חמשת היעדים, ייכתבו באותו סשן)
`↳ as-built` על 3.3/3.4/3.5 · §10 (כולל שתי ההנחות: אירוע-שעבר, וכתובות-הדמו) · תיבות-ה-DoD
שהמספרים שלהן זזו · שורות-Ledger חדשות · ו`src/CLAUDE.md` + `04_hostesses/CLAUDE.md` על
תיקון-המנוע. ואז `CLAUDE_CODE_LOG` ← `STATUS`.

</div>
