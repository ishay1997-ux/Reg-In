<div dir="rtl">

# מודול 4 · פזה 3 (מסכים) — צעדים 3.0 · 3.1 · 3.2

> 🗣️ **תקציר-החוויה של יחידת-הבנייה, ואני במצב-תכנון — לא ייכתב קוד עד שתאשר.**
> מקורות שנקראו בתור הזה: `spec.md §①` (רשימת-הקריאה) · כרטיסי מסך 3 / 3ב / 3ג / 3ד
> ב-`screens-approved.md` · `processes-approved.md §א1–§א4` · **ארבעת המוקאפים המאושרים
> `03` · `06` · `07` · `08`** · `schema.sql` (טבלת `hostesses`) · `src/modules/04_hostesses/api.js` ·
> `src/lib/hostesses.js` · `src/CLAUDE.md`.

---

## 🧭 למה זה עכשיו, ואיפה אנחנו באמת

פזה 2 (הלוגיקה) סגורה: הציון, השער, המרחק והגאוקוד בנויים ובדוקים — **אבל אין להם עדיין מסך.**
פזה 3 היא המסכים, והשלושה הראשונים הם **עולם המאגר**: הטבלה, וההוספה/עריכה/צפייה שנפתחות ממנה.
*(מסכי השיבוץ עצמם — מבט-על, Smart Match, תפריט-השורה, הדף הציבורי — הם 3.3–3.6, לא בתור הזה.)*

---

## 🔴 מה מצאתי לפני שהתחלתי

### ① העבודה היתומה כבר נקמטה — לא על-ידי
כשהתחלתי, `candidateDistanceKm` ישב על הדיסק בלי קומיט. **בזמן שהמתנתי לך, סשן מקביל קימט אותו**
(`a6aa6be`) **ומיד אחריו את הכרעת-המרחק שלך** (`840d9d9`). ⇐ **הפריט הזה ירד ממני.**
🔬 **מדדתי אחרי שני הקומיטים: `575 passed / 17 files`, `exit=0`.**

⚠️ **וכלל 16 — שיחה כותבת אחת: ל-`module-4.md` יש כרגע עריכות שטרם קומטו** *(שורות-Ledger
`local-11`/`local-12` והערת-הזרע בצעד 4.2)*. ⇐ **אגע במדריך אחרון, אריץ `git status` לפניו,
ואקמט בפאת'ספק מפורש עם `-m` לפני `--`** — בדיוק הצורה שנולדה מהתקרית של 08/08.

### ② שער פזה 2 (2.5) — כבר נסגר, **וכותרת-המדריך לא יודעת את זה**
‏`CLAUDE_CODE_LOG` *(רשומת 17:3X)* אומר במפורש: **"PHASES 0, 1, 2 CLOSED… Active step is 3.0"**,
עם `575 unit`. 🔴 **ו-§1 של מדריך-המיקרו עדיין אומר `Active step 2.5` ו-`571 unit`.**
⇒ **הכותרת מיושנת מול היומן** — האדווה נעצרה באמצע. **אתקן אותה כחלק מהעדכונים.**
🚫 **מה שעדיין שלך:** שער 1.5 — **הרצת `regin-docs-sync`** (כלל 13ז). **אינו חוסם את פזה 3.**

### 🆕 ⑥ ומה שקריאת-היומן תפסה, ולא היה בתוכנית שלי — דחיסת §9(i)
היומן רשם, במילים שלו: *"‏§9(i) compaction — NOT done… the guide is **1,464 lines**, read in full on
every build turn… **the natural moment is the opening of the Phase-3 session**"*. **הסשן הזה הוא בדיוק
הרגע ההוא.** *(אימתתי: `1,464` שורות, ופזות 0/1/2 עדיין נושאות את טקסט-הצעדים המלא.)*
⇒ **אדחוס את פזות 0 · 1 · 2 לטבלת "מה נחת + הראיה" עם הערת carry-forward**, ואשמור עותק
מלא ב-`docs/archive/` — **בדיוק התקדים של מודול 3** *(`docs/archive/module-3_full_2026-07-2X.md`)*.
🚫 **ולא אדחוס: את פזה 3 הפעילה · את §10 · ואת ה-Ledger** — שלושת אלה מוחרגים במפורש.
🔑 **למה זה משתלם דווקא עכשיו:** המדריך נקרא **במלואו בכל תור-בנייה**, ופזה 3 היא שש יחידות-בנייה
⇒ זה המקום שבו האורך גובה מס בכל תור, שוב ושוב.

### ③ סריקת-דלת לפזה 3 — בוצעה, וזו התשובה המדויקת
| פריט פתוח | חוסם את… | חוסם 3.0–3.2? |
|---|---|:-:|
| **§7.33** — מנגנון-הכתיבה של השחרור | 3.4 · 3.5 | **לא** |
| **§7.41** — מרוץ `max+1` על `assignment_number` | 3.4 · 3.5 | **לא** |
| 🆕 **`local-12`** — סימון "מיקום מקורב" | 3.4 | **לא** |

⇒ **אין ולו פריט אחד שחוסם את שלושת הצעדים האלה.** שלושתם יגיעו אליך כסבב-הכרעות אחד לפני 3.4.

🔴 **ו-`local-12` דורש מילה עכשיו, כי המדריך עצמו כותב "לסרוק בדלת-פזה-3, לא לבנות בשקט":**
**המלצתי — לא לבנות את הסימון.** ‏**הכרעת-המרחק שלך מהערב הוציאה לו את הקרקע:** הסימון נועד
להתריע ש-`18 ק"מ` מתחזה לדיוק שאין לנו — **ועכשיו לא מוצג מספר בכלל, אלא `קרובה`/`בינונית`/`רחוקה`.**
המחיר שלו, לעומת זאת, לא ירד: הוא מחייב לדעת **איזו** מועמדת-כתובת נפתרה ⇒ עמודת `precision`
(מיגרציה) או גזירה-בקריאה. **אינו חוסם דבר בתור הזה** — ההכרעה בסבב של 3.4.

### ④ הכרעת-המרחק שלך — בדקתי את טווח-הפגיעה שלה על מה שאני בונה
‏`local-10` (מרחק כתווית) נוגע ל**משטח 2 בלבד** *(`screens-approved.md:290,298` — נבדק, לא הונח)*.
**אינו נוגע בטבלת-המאגר ואינו נוגע בכרטיס-הדיילת** — אין בהם מרחק כלל. ⇐ שלושת הצעדים שלי
נבנים בדיוק כפי שתוכננו. ‏`distanceLabel()` כבר קיים ב-`src/lib/smartMatch.js`, ויצרוך אותו 3.4.

### ⑤ 🔴 והממצא שמשנה את סדר-העבודה: המסכים שאבנה **לא ניתנים לצפייה** לפי התוכנית
‏`src/App.jsx:126-129` עדיין מרנדר `<UnderConstruction moduleName="דיילות" />`, **וההחלפה מתוכננת
לצעד 4.1** — בעוד שפזה 3 דורשת מכל צעד *"אימות תפקודי **וויזואלי**, עם צילומי-מסך"*.
**כלומר התוכנית מבקשת צילום-מסך של מסך שאי-אפשר להגיע אליו.**
✅ **ההמלצה שלי: להקדים את שורת-החיווט הזו לצעד 3.1** — שורה אחת ב-`App.jsx`. צעד 4.1 נשאר
בעליו של **המסלול הציבורי** ושל רשימת-ההיתר ב-`App.routes.test.jsx`, שהם החלק האמיתי שלו.
נרשם כסטייה מודעת (`↳ as-built` + §10), ו-`App.routes.test.jsx` ירוץ כדי להוכיח שלא נפרץ שומר.

---

## 🔧 צעד 3.0 · מה משותף ומה מקומי — הכרעה, לא רשימה

סרקתי את **שמונת** המוקאפים המאושרים (אוצר-המחלקות של כל אחד) והצלבתי מול `src/components/`:

| הרכיב | מופיע ב… | ההכרעה |
|---|:-:|---|
| **תג-סטטוס** (`.st`) — פעילה · מושבתת · לא זמינה · ששת סטטוסי-השיבוץ | 6 משטחים | 🆕 **משותף** — `src/components/StatusTag.jsx` |
| **כוכבי-התרשמות** (מלא/ריק/`—`) | 4 משטחים | 🆕 **משותף** — `src/components/RatingStars.jsx`, קריאה **וגם** עריכה |
| **מתג הפעלה/השבתה** | 3 משטחים | ♻️ **קיים** — `src/components/ui/switch.jsx` (כבר מותאם RTL) |
| **צ'יפ-שפה** (`.lchip`) | 3 משטחים | 🆕 **משותף** — `src/components/ChipToggle.jsx` |
| **אריח-מדד** (`.htile`/`.kpi`) | 3 משטחים | ♻️ **קיים** — `StatTile` |
| סכום כסף | הכול | ♻️ **קיים** — `Money` |
| חלון-וידוא | 3 משטחים | ♻️ **קיים** — `useConfirm` |
| טעינה/שגיאה/ריק | הכול | ♻️ **קיים** — `LoadingOrError` |
| צ'יפי-נימוק של Smart Match | 1 משטח | ⏸️ **מקומי ל-3.4** — ‏`spec.md §1.5` דורש **שתי משפחות נפרדות**; איחוד מוקדם עם צ'יפ-השפה היה מוחק את ההפרדה |

🔑 **הכלל שהפעלתי הוא של המדריך עצמו:** מופיע ב-3 משטחים ומעלה ⇒ משותף. **שלושה חדשים בלבד,
וארבעה קיימים שרק נצרכים** — לא בניתי מה שכבר יש.

---

## 🧱 צעד 3.1 · מסך 3 — טבלת מאגר הדיילות

**קבצים:** `04_hostesses/HostessesPage.jsx` (מעטפת + שתי לשוניות) · `04_hostesses/RepositoryTab.jsx`
**מוקאפ מאושר:** `03_repository_approved.html` — **נבנה כפי שצויר, ואינו מובא לאישור מחדש.**

### (א) כך הבנתי את התהליך — תקן אותי
המנהלת מגיעה למסך הזה **רק אחרי שיחת טלפון**, באחת משלוש סיטואציות: דיילת חדשה נקלטה · דיילת
עדכנה פרטים או הודיעה שלא תהיה זמינה · דיילת עוזבת. **היא מחפשת אדם אחד ופועלת עליו** — ההשוואה
בין דיילות קורית ב-Smart Match, לא כאן.

### (ב) מה נבנה
שמונה עמודות כמצויר: **שם · עיר · טלפון · התרשמות · שכר שעתי · מצב · אירועים (רבעון אחרון) · פעולה**.
ארבעה מסננים: חיפוש חופשי (שם+טלפון בלבד) · "כל הערים" · "פעילות בלבד" (**דלוק כברירת-מחדל**) ·
`לא ענתה ל-N האחרונים (מספר)`. בלי עימוד (עד 50 שורות). **בלי מחיקה, בשום מקום.**
- **דיילת שלא דורגה מציגה `—`, לעולם לא `3 ★`.**
- **עמודת "מצב" היא טווח** — `לא זמינה 15/08–20/08` + ההערה מתחתיה, לעולם לא תאריך-סיום יחיד.
- **המתג תלוי-מצב:** אין שיבוץ עתידי פעיל ⇒ מתהפך מיד. **יש** ⇒ נפתח קודם חלון §א4 שמונה אותם
  **בשם ובתאריך**, עם שתי אפשרויות מנוסחות-לפי-כוונה: *"היא לא תגיע — שחרר אותה"* (מומלץ) /
  *"היא תשלים את מה שהתחייבה"*.
  ⚠️ **ומה שהחלון עושה בפועל בתור הזה: כותב את הסטטוס בלבד.** מסלול-השחרור עצמו (מייל-הביטול +
  שחרור השיבוצים) תלוי ב-**§7.33 שפתוח** ⇒ נבנה ב-3.5. **בחירת "שחרר אותה" תיחסם עם הסבר** ולא
  תעמיד פנים שהיא עבדה. *(העדפתי הודעה כנה על פעולה שקרית.)*
- **שם הדיילת ⇐ פותח 3ד · עיפרון ⇐ פותח 3ג · "+ הוספת דיילת" ⇐ פותח 3ב.**

### (ג) לוגיקה חדשה — נכתבת ב-`src/lib/hostesses.js`, **הבדיקה לפני הקוד**
אף אחת מהארבע אינה קיימת היום (בדקתי): `hostessDisplayState` (עמודת מצב) · `unansweredStreakTag`
(שני התגים — `לא ענתה ל-N האחרונים` ו-`לא ענתה מעולם (N זימונים)`) · `eventsInLastQuarter` ·
`futureActiveAssignments` (מזין את חלון §א4). **בדיקת-יחידה נכתבת ראשונה ונצפית אדומה.**

### (ד) מה אני מסמן לך — מהמוקאפ / אפיון-שותק
| # | הפריט | מה אני עושה, ולמה |
|:-:|---|---|
| 1 | **תפקיד-צפייה (מנהלת פרויקטים) לא רואה גם את עמודת "שכר שעתי"** | **מהמוקאפ** — פאנל "פעולה חסומה" מצייר זאת ומנמק *("נתון עלות, לא רלוונטי לתפקיד-צפייה")*; **כרטיס-המסך §⑥ שותק על העמודה הזו**. בונה כמצויר. **בדקתי:** כרטיס מסך 3 §⑥ · §⑤ |
| 2 | **‏`45 ₪` ולא `₪45`** | המוקאפ מצייר גליף-ראשון 12×; **הכרעה 14 בבלופרינט שאישרת** קובעת `<Money>`. הקוד גובר |
| 3 | **"אירועים · רבעון אחרון" = 90 יום מתגלגלים**, ונספר באותו כלל כמו "עבדה אצל" *(שיבוץ שהגיע ל-`אושרה סופית` ותאריך-האירוע כבר עבר)* | הנחות 10+11 שאישרת קובעות את החלון ואת כלל-הספירה בנפרד; **שהם אותו כלל — זו הנחה שלי** ⇐ מתויגת **`הנחתי`** ונרשמת ב-§10 |
| 4 | **שדות-תאריך באי-זמינות = `type="date"`** | **ביצוע טכני, תקדים בקוד** — שלושה מופעים ב-`03_quotes`. התצוגה של טווח שנשמר נשארת `DD/MM/YYYY` כמצויר |

### (ה) 🎯 מה ייחשב עובד
1. *"איזו דיילת אני פותחת עכשיו — ומה מצבה, לפני שאני נוגעת בה."* — `screens-approved` מסך 3 §①
2. *"חור במוקאפ: יש מסנן 'פעילות בלבד' ואין עמודה שמראה מצב"* — עמודת **מצב** סוגרת אותו · §③
3. *"שתי ההודעות חייבות להיות שונות, אחרת המנהלת חושבת שהמאגר נמחק"* — *"עדיין אין דיילות במאגר"*
   מול *"לא נמצאו דיילות התואמות לסינון"* + "נקה סינון" · §⑤
4. **שגיאת-טעינה מציגה *"לא הצלחנו לטעון את המאגר"* + "נסה שוב" — ולעולם לא טבלה ריקה בשקט** · §⑤
   🔴 *(זה החצי שה-DB לא יכול לאותת, ו-DoD רשם אותו כחוב של פזה 3 — `{data:null,error:null}`)*
5. `יחידה-ספציפית` — **מנהלת פרויקטים רואה את השורות בלי "+ הוספת דיילת", בלי עמודת פעולה ובלי
   שכר-שעתי**, ומנהלת גיוס רואה את שלושתם. **נבדק בהתחזות בשני הכיוונים, לא רק בזה שרציתי לראות.**

---

## 🧱 צעד 3.2 · משטחים 3ב · 3ג · 3ד — הוספה · עריכה · צפייה

**קבצים:** `04_hostesses/HostessFormDialog.jsx` (משרת גם הוספה וגם עריכה) · `04_hostesses/HostessViewCard.jsx`
**מוקאפים מאושרים:** `06` · `07` · `08` — **נבנים כפי שצוירו.**

### (א) כך הבנתי — תקן אותי
**הטופס אינו נפתח "ריק וממתין".** אין מסך לדיילת, אין הזמנה ואין קישור — המנהלת **כבר דיברה
איתה בטלפון**, והיא רק מתעדת מה שסוכם. בשמירה הדיילת **פעילה** ונכנסת **מיידית** למאגר-המועמדות.

### (ב) ולידציות — בדיוק שלוש התנהגויות שונות, וזה העיקר
| הקלט | ההתנהגות |
|---|---|
| **ת"ז שגוי** | 🔴 **חוסם**, ונבדק **תוך כדי ההקלדה** — היחיד שאינו ממתין ליציאה מהשדה |
| **שכר מתחת למינימום** | 🔴 **חוסם** — *"השכר השעתי חייב להיות לפחות 35 ₪ (שכר מינימום)"*, **והמספר מגיע מ-`params`, לא מוקלד** |
| **אימייל שכבר קיים** | 🟡 **מזהיר ולא חוסם** — *"כתובת זו כבר רשומה אצל \<שם\> — להמשיך?"* |
| **כתובת שלא ניתן לגאוקד** | 🟢 **לא חוסם כלל** — הדיילת נשמרת, ומסומנת "אין קואורדינטות" |
| **הכתיבה נחסמה (RLS)** | 🔴 **הודעת-שגיאה מפורשת. לעולם לא "נשמר" בשקט** |

**ועוד, מהכרטיסים:**
- 🔴 **כוכבי-ההתרשמות נפתחים ריקים בהוספה — לא על 3.** המוקאפ מצייר `3 מתוך 5`; בלי זה כל דיילת
  נולדת מדורגת, והעמודה ש-`NULL` נפתחה בשבילה מתה. **סטייה מודעת ומאושרת בבלופרינט.**
- 🔴 **חמישה שדות שהיו "רשות" במוקאפ נושאים `*` וחוסמים שמירה** — אימייל · עיר · שלושת שדות-הבנק.
  **זו ההכרעה שלך מ-08/08** *(הם `not null` במסד — אימתתי ב-`schema.sql:143-155`)*. סה"כ 9 שדות מסומנים.
- **בעריכה: ת"ז נעולה** (כמו ח"פ-לקוח), ואזהרת הקפאת-התעריף מוצגת על המסך —
  *"שינוי כאן לא ישנה תעריף של שיבוץ עתידי שכבר קיים"*.
- **טווחי אי-זמינות נצברים בזיכרון-הטופס** ונכתבים רק עם "שמור שינויים" — ‏`replaceHostessUnavailability`
  כבר בנוי ב-`api.js` בסדר **insert-first**, שנולד מאובדן-דאטה אמיתי ב-30/07.
- **3ד הוא overlay מעל הטבלה, לא עמוד.** לתפקיד-צפייה: **בלי עיפרון ובלי סעיף "פרטים עסקיים"**
  (בנק + ת"ז). פאנל "העדפת-לקוחות" מוצג כ**ריק-מוצהר** עם ההסבר שמודול 6 ימלא אותו.

### (ג) 🎯 מה ייחשב עובד
1. *"כרטיס נפתח רק אחרי שהמנהלת כבר דיברה עם הדיילת. אין 'מועמדת ממתינה לאישור'."* — `processes §א1`
2. *"בשמירה: `פעילה`, ונכנסת מיידית למאגר-המועמדות."* — `processes §א1`
3. *"נכשל ⇒ נשמרת בכל מקרה ומסומנת"* — כישלון-גאוקוד **לעולם אינו חוסם שמירה** — `spec.md §2.1(1)`
4. *"התרשמות" ולא "דירוג"* — **ודיילת חדשה נשמרת בלי דירוג כלל**, ומופיעה בטבלה כ-`—` · `screens 3ב §⑦`
5. `יחידה-ספציפית` — **שמירה בלי אימייל נחסמת בהודעה מפורשת; אימייל כפול מזהיר ו"שמור" נשאר פעיל.**
   שתי התנהגויות שונות על אותו שדה — וזה בדיוק מה שקל לפספס.

---

## 🔬 איך אני מאמת — ומה זה דורש ממך להכריע

הטבלה תיראה **ריקה** אחרי הבנייה, כי **‏`hostesses` מכילה 0 שורות** *(נמדד)*. ⇒ האימות מתחיל
דווקא מ-3.2: **אקים דיילת אמיתית דרך הטופס האמיתי**, ואז הטבלה של 3.1 תציג אותה.

✅ **הכרעתך: 20 דיילות במסד, ריאליסטיות, בלי המילה "דמו" ובלי שום סימון-הדגמה.** מיושמת כך:

- **20 דיילות עם נתונים שנראים אמיתיים** — שמות ישראליים, טלפונים תקינים, **ערים שונות**
  *(ת"א · חיפה · ירושלים · באר שבע · נתניה · רמת גן · ראשל"צ · פ"ת · אשדוד · רחובות)*,
  **כתובות ברמת-רחוב**, תעריפים מגוונים מעל שכר-המינימום, בנקים אמיתיים, חלקן עם רכב וחלקן בלי,
  חלקן מדורגות וחלקן **בלי דירוג כלל** *(כדי שעמודת ה-`—` תיראה על מסך אמיתי)*, ושתיים-שלוש
  עם טווח אי-זמינות. 🚫 **אפס מחרוזות "דמו"/"בדיקה"/"טסט" בשום שדה.**
- 🔴 **ת"ז — כל 20 יעברו ספרת-ביקורת אמיתית** *(אחרת הטריגר/הטופס יפסלו אותן, וגם זה היה "נתון
  שנראה אמיתי ואינו")*. מחושבות בקוד, לא מומצאות.
- 🔴 **כתובות ברמת-רחוב בערים שונות — לא במקרה:** המדריך מזהיר בצעד 4.2 שדיילת עם **עיר בלבד**
  שזהה לעיר-האירוע מקבלת `null` **בכוונת-התכנון** ותוצג `אין קואורדינטות`. **אשאיר אחת כזו
  במכוון**, כדי שהמצב הזה יהיה נראה במסך אמיתי ולא רק בבדיקה.
- **המסלול: דרך הקוד האמיתי של האפליקציה בדפדפן**, מחוברת כמנהלת הגיוס — בדיוק התקדים של
  צעד 2.4. ⚠️ **ולא מ-node ולא ב-SQL ישיר:** ‏Nominatim מחזיר `Access denied` ל-node ⇒ זרע
  לא יכול לגאוקד, ו-SQL ישיר היה עוקף גם את הגאוקוד וגם את שער-ההרשאות.
- ➕ **ותוצר-לוואי שסוגר חוב פתוח:** הריצה הזו תייצר **20 זוגות `lat`/`lng` אמיתיים** — בדיוק
  הערכים הליטרליים שצעד 4.2 חייב לקודד בזרע, והמדריך רשם זאת כבעיה פתוחה שעה קודם. אשמור אותם.

⚠️ **ואומר בקול מה זה עולה, כי אין דרך חזרה:** אין סביבת-בדיקה נפרדת — **מסד חי אחד** —
**ואין מחיקה בשום מקום במודול** *(רק השבתה)*. **20 השורות יישארו.** זו הכרעתך, ואני מבצע אותה;
רשמתי אותה כאן כדי שתהיה מתועדת ולא רק נאמרת.
🔴 **ואכתוב לצעד 4.2 שהזרע חייב לדלג על ת"ז שכבר קיימת** — אחרת הוא ייפול על השורות האלה.

**ראיות שאביא, לכל צעד:** צילומי-מסך של **טעינה · ריק-אמיתי · ריק-אחרי-סינון · שגיאה** ·
חלון §א4 עם שם ותאריך אמיתיים · שלושת מצבי-הוולידציה בטופס · הכרטיס בשתי ההרשאות ·
`npm run test:run` · `npm run gate` · `npm run smoke` · `App.routes.test.jsx` ירוק.
🔴 **ואת שער-ההרשאות אבדוק בשני הכיוונים** — קודם שמנהלת הגיוס **כן** רואה, ורק אחר-כך שהאחרות לא.
*(בקרה חיובית קודם: אם ההתחזות שבורה — הכול מחזיר אפס, וזה נראה כמו "RLS מושלם".)*

---

## 📋 סדר-הביצוע

1. **3.0** — שלושת הרכיבים המשותפים (`StatusTag` · `RatingStars` · `ChipToggle`) + בדיקות.
2. **לוגיקה** — ארבע פונקציות ב-`src/lib/hostesses.js`, **הבדיקה נכתבת ראשונה ונצפית אדומה**.
3. **3.1** — `HostessesPage` + `RepositoryTab` + חיווט השורה ב-`App.jsx`.
4. **3.2** — `HostessFormDialog` *(הוספה+עריכה)* + `HostessViewCard`.
5. **‏20 הדיילות** — דרך הקוד האמיתי בדפדפן, מחוברת כמנהלת הגיוס; שמירת 20 זוגות הקואורדינטות.
6. **אימות חי** — צילומי-מסך של כל המצבים + שער-הרשאות **בשני הכיוונים** + `test:run` · `gate` · `smoke`
   · `App.routes.test.jsx`.
7. **דחיסת §9(i)** — פזות 0/1/2 ⇐ טבלת-"מה נחת + הראיה", עותק מלא ל-`docs/archive/`.
8. **עדכון קבצים** — מדריך-המיקרו (**תיקון הכותרת המיושנת** · טבלת-צעדים · `↳ as-built` · §10 ·
   **סריקת-אדוות בת-חמשת-היעדים, בשורה כתובה**) → `CLAUDE_CODE_LOG.md` → `STATUS.md` ·
   ‏`04_hostesses/CLAUDE.md` למוקשים שייתפסו · הסרת פטור-ה-knip ל-`api.js` *(הטריגר שלו הגיע
   מוקדם, עם החיווט)* · **הערה בצעד 4.2: הזרע מדלג על ת"ז קיימת, ואלה הקואורדינטות שלו.**
   ⚠️ **המדריך אחרון ואחרי `git status`** — כלל 16, יש בו עריכות של סשן אחר שטרם קומטו.

⚠️ **מה שאני לא עושה בתור הזה:** מסכי 3.3–3.6 · כתיבות מחזור-חיי-השיבוץ (§7.33/§7.41 פתוחות) ·
המסלול הציבורי · זרע-ההדגמה · ‏E2E. **וגם לא מיזוג, לא PR ולא דחיפה.**

</div>
