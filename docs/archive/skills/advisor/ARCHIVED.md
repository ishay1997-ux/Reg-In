# advisor — archived 2026-09-18 (Ishay's ruling, restated twice)

**Replaced by the global skill `deputy`** (`~/.claude/skills/deputy/`). Its role — counsel to Ishay,
critique before things run, holding what is open for him — now works in every project instead of
only this one, and with the authority to decide in his place on classes he has already ruled on,
which this skill deliberately did not have.

**Extracted into `deputy` before this was archived, so nothing global was lost:**
the three checker shapes and each one's characteristic failure · the dosing rule *"if I am wrong,
who tells me?"* · **the third lens** — *walk this as Ishay would experience it, and find where the
CONVERSATION fails him* (the one measured at nine findings that five other reviewers missed) · the
announce-before-dispatch rule with its 120K anchor and his ruling *"אני לא מגביל, אני רוצה בקרה"* ·
part ④ of his report shape, the deliberation out loud · his six approved ordering rules · the
commit-bucket split of the day's pace.

**What stayed behind on purpose:** everything specific to REG-IN — `PROJECT_MASTER §6/§7`, the
STATUS board and its ⏸️ list, the iron rules, the merge routing, the journal.

⚠️ **Trigger conflict was the operative reason to archive now, not tidiness:** while both existed,
`advisor` and `deputy` could both fire in this repository on the same message.

Full history is in git. Restore with `git mv` if `deputy` fails its own test
(status requests to 0, turns-to-decisions under 5:1) — that test is written into it.
