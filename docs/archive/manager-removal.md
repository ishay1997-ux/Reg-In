<div dir="rtl">

# תוכנית הוצאת `work-manager` מהמסלול הפעיל

> **נכתב 05/08/2026.** ‏זהו **הפרומפט המלא** לסשן שיבצע — מיועד להדבקה כמות שהוא, אחרי
> תנאי-הפתיחה שבראשו. ‏**נשמר בריפו ולא בתיקייה זמנית** כי הוא מחזיק 18 ממצאי-ביקורת שקיימים
> רק כאן.
>
> **מקור:** טיוטה שנכתבה 04/08 ותוקנה שש פעמים, ואז עברה **ביקורת בהקשר-טרי** (כלל
> `~/.claude/CLAUDE.md`) שמצאה בה **5 טענות עובדתיות שגויות ו-13 משטחים חסרים** — **שתיים
> מהשגיאות הרסניות.** כל התיקונים משולבים כאן. הטיוטה המקורית מתה.
>
> ⏸️ **שאלה פתוחה אחת שאינה מכנית וממתינה לישי:** האם ההוצאה היא **ניקוי** או **שלב 1 בהגירה**
> לצינור *Discovery → אפיון → Blueprint → Build → בדיקה → Close*. ‏13 מהתיקונים אינם תלויים
> בתשובה; **העיצוב של מה שמחליף את חוזה-⚡ כן.**

---

## תנאי פתיחה — לפני כל דבר אחר

`git status --short --branch`, ושלושה תנאים:

1. **הענף: `ishay/post-m3-followups`** — **הכרעת-ישי 05/08/2026, והיא מחליפה את "ענף חדש מ-`dev` טרי" שעמד כאן קודם.**
   **הנימוק, מדוד:** הפרומפט הזה, וההוק `block-shell-dialect-mixup.sh`, **אינם קיימים על `dev`** —
   הם קומטו רק על הענף הזה. ענף טרי מ-`dev` פשוט לא יכיל את המסמך שאתה קורא עכשיו.
   ‏🔴 **הכלל המקורי עדיין בתוקף במלואו: אל תרוץ על ענף של מודול.** ‏`ishay/post-m3-followups`
   הוא **ענף-תשתית** (אפס קוד מוצר, אפס מיגרציות), וההוצאה היא אותה מחלקת-עבודה בדיוק.
   **ודא לפני שאתה מתחיל:** `git branch --show-current` מחזיר `ishay/post-m3-followups`,
   ו-`git merge-base --is-ancestor HEAD origin/dev` **נכשל** (כלומר הענף חי, לא מוזג — כלל ברזל 10).
2. **‏`.claude/skills/work-manager/**` ו-`module-blueprint/template.md` נקיים בעץ.** מתוקנים ⇒
   עצור ודווח: יש עליהם עבודה לא-מוכרעת.
3. **אין סשן אחר שכותב.**

**קמט תמיד בפאת'ספק מפורש** — `git commit -m "..." -- <נתיבים>`. **לעולם לא `git add -A`:**
האינדקס משותף, וקימוט של קובץ משותף מקמט גם שורות תלויות של אחרים *(קרה בפועל: קומיט של קובץ
אחד סחף 9 קבצים זרים כולל מיגרציה)*.

---

## מה לחפש — 🔴 סרוק עיוור קודם

**יד חופשית לחפש היכן שנראה לך**, מגודרת בתחום שלמטה ובחובת-דיווח על כל מקום שנגעת בו.

🔴 **הסדר קריטי:** בהמשך יש "הכרעות מראש" הנוקבות במקומות מדויקים. **אלה ממצאים של מישהו אחר,
כלומר תשובה צפויה.** מי שקורא אותם קודם בודק חמישה מקומות ועוצר — **וכל מה שהתגלה בסבב האחרון
היה מחוץ לרשימה הראשונה.**

> **① בנה רשימה משלך · ② רק אז קרא את ההכרעות · ③ הצהר: מה מצאת שאין שם ⇒ פער אמיתי, דווח בקול.**

**ארבעה מעברים, לא אחד:**

**א׳ — מנה משטחים לפני שאתה מחפש.** טענת-היעדר רחבה רק כרוחב המקומות שנבדקו. **שורה לכל
משטח, כולל "נקי".**

**ב׳ — בשתי השפות ובניסוח של המקור.** ‏grep בעברית על קורפוס אנגלי מחזיר אפס **ונראה כמו נקי**.

**ג׳ — 🔴 התנהגות, לא מילה.** ‏**תלות במנהל יכולה לא להכיל את המילה "מנהל" אף פעם.** לכל כלל:
*האם הוא מנתב שאלה · אישור · דוח · המתנה — **למישהו**? למי, והאם הוא עדיין קיים?*
*(כך התגלתה שורת "פיצול עצירות-👤" — מנתבת אישורים ולא נראתה בחיפוש מילולי.)*

**ד׳ — מיון.** ‏**התאמה אינה ממצא.** "מנהל המוצר" = ישי · "מנהל פרויקטים" = תפקיד-משתמש.

**מונחי פתיחה (נקודת-מוצא, לא גבול):** `work-manager` · `current_manager` · `manager` · `מנהל` ·
routing · session manager · הוראה לשאול/לשלוח/לקרוא-קובץ-מנהל.

**תחום השינוי:** `.claude/` · `docs/` · `scripts/` · `.md` בשורש.
🚫 **`src/` · `e2e/` · `supabase/` מחוץ לתחום לחלוטין** — "מנהל" שם הוא תפקיד-משתמש.

---

## הכרעה מראש #1 — סקילי מחזור-החיים

**החלפת-יעד, לא עיצוב-מחדש.** בכל מקום שבו **יעד הניתוב** הוא "המנהל" — **החלף ב"ישי". אל תמחק
את הכלל.** הכללים טובים; רק הכתובת מתה.

### 🔴 `module-build/SKILL.md` — התיקון הקריטי לטיוטה הקודמת

**הטיוטה אמרה "7 מקומות **מחוץ** לסעיף ה-⚡" ו"סעיף ה-⚡ (שורות 10–32) מוסר במלואו".
שתיהן שגויות, והצירוף הרסני:**

- **סעיף ה-⚡ הוא שורות 10–**52**, לא 10–32.** (`## Pre-flight` הוא שורה 54.)
- **ארבעה מהשבעה — ~36 · ~42 · ~46 · ~48–49 — נמצאים **בתוך** הסעיף.** רק שלושה (55 · 69 · 91)
  מחוצה לו.
- ⇒ **"החלף בהם יעד" ו"מחק את הסעיף" חלים על אותן שורות.** מוחק 10–32 ⇒ בולטים יתומים תחת
  כותרת מחוקה. מוחק 10–52 ⇒ **משמיד ארבעה כללים שהפרומפט עצמו ביקש לשמר**, בהם פרוטוקול-הפער.

**ההוראה הנכונה:** ‏**עבור שורה-שורה, לא בלוק-בלוק.** ‏הסר את **כותרת** ה-⚡ ואת הבולטים
שכל-כולם חוזה-מנהל (‏~12–35 — התקשורת, שלוש נקודות-הבקרה, "אין מענה"); **שמר** את הבולטים
שהם כללי-איכות עם יעד שגוי (‏פרוטוקול-הפער · "הוראה ברורה אינה מאושרת" · תיוג-הטענות · שתי
שאלות-החקירה) **והחלף בהם את היעד לישי.**
**מספרי-השורה נמדדו 04–05/08 — אמת מחדש. אם מצאת מקום שמיני — עצור ודווח.**

### `module-blueprint` — אפס ניתובי-תפקיד *(הנוסח כאן תוקן 05/08/2026)*

**נמדד בשלוש שיטות בלתי-תלויות: 0 בשני הקבצים.** ‏`module-blueprint/SKILL.md` (29 שורות) —
0 התאמות · `template.md` (**81 שורות**) — **שתי** התאמות בלבד, שורות 46 ו-63, **ושתיהן
"מנהל-מוצר" = ישי.** ‏**אל תייצר כאן ממצא.**

⚠️ **הנוסח הקודם כאן היה שגוי לחלוטין — והוא ההדגמה החיה של המחלקה שהמסמך הזה נועד למנוע.**
הוא טען ל-10 התאמות, לסעיף `### ⚡ When a manager session exists` **שלם** בשורות ~12–34,
ולציטוט-נתיב קשיח ל-`work-manager/references/ishay.md` situation 10 בשורה ~48 — **ונשא
`⏸️ ממתין להכרעת ישי`, כלומר ישי היה מתבקש להכריע על תלות שאינה קיימת.**
**נמדד:** ‏`⚡` · `When a manager session exists` · `situation 10` · `work-manager` ·
`references/ishay` · `current_manager` — **אף אחת מהמחרוזות אינה בקובץ.** שורות 12–34 הן
`### ⚠️ Workflow & Plan Rules` (על Plan Mode), **ושורה 48 ריקה.**

🔴 **הלקח שאתה, הסשן המבצע, צריך לקחת מזה: אל תסמוך על סעיף במסמך הזה רק כי הוא מסומן 🔴.**
הסימון הזה נשא טענה שגויה שעברה ביקורת אחת ושרדה אותה. **מדוד לפני שאתה מוחק.**

### `module-close` — אפס ניתובי-תפקיד

**נמדד: 0.** ‏אזכורי "Manager" בו הם **שם-התפקיד** ("Release Manager"). ‏**אל תייצר שם ממצא.**

---

## הכרעה מראש #2 — `scripts/check-context.mjs`

**היוצא-מן-הכלל היחיד לכלל "אל תשנה Hook/מסמך/Skill"** — הוא **סקריפט**, ולכן אינו מכוסה, אבל
הוא הקובץ היחיד שיכול לשבור את `npm run gate`.

**מספרים מתוקנים (הטיוטה שגתה):**
- הבלוק: **202–221** · `problems.push` על קובץ חסר: **שורה 220** *(218 היא `}`)* ·
  `process.exit(1)`: **שורה 242 — והיא השורה האחרונה בקובץ** (הקובץ = 242 שורות).
  *(‏"243" שנכתב כאן קודם **אינו קיים**; ‏`process.exit(0)` הוא ב-235. תוקן 05/08/2026 —
  שלושה מבקרים בלתי-תלויים מדדו 242.)*
- ⚠️ **יש `problems.push` שני בשורה ~206** (חסרה חותמת "עודכן") שהטיוטה לא הזכירה.
- ⚠️ **ובדיקת-מנהל שנייה, שורות ~185–197** — סורקת `docs/handoff_manager_*.md`. דוחפת `note`
  בלבד ⇒ לא מפילה את השער, **אבל שורדת כבדיקה חיה לתפקיד שחוסל.**

⇒ **מסיר את `docs/current_manager.md`? חובה להסיר את שתי הבדיקות באותו שינוי.**
**‏`check:context` הוא שלב ב-`gate` (`package.json:25`).**

---

## הכרעה מראש #3 — שני קבצים ניצלים לפני הארכוב

### ① `references/concurrency.md` → `.claude/skills/_shared/parallel-sessions.md`

בטיחות ריבוי-סשנים. **אינו תלוי בתפקיד** — ישי מריץ סשנים במקביל בכל מקרה.

🔴 **תיקון קריטי לטיוטה — שלושת יעדי-הגיזום תוארו שגוי, ו"מחק את הסעיף" היה הורס:**
- *"שורות הפתיחה על מצב 8"* — **אין "מצב 8" בקובץ.** הפתיחה היא שורות 3–4.
- *"הסעיף תקצירי-חילופין"* — **אינו סעיף אלא בולט, שורות ~51–54.**
- *"הסעיף מענה במקומו של ישי"* — **בולט, שורות ~58–61.**
- **שניהם בתוך `## Direct session-to-session communication` (~36–62).** מחיקת הסעיף הייתה
  לוקחת גם את כלל העובדות-הנדיפות ואת ***"You are a single point of failure — write the
  fallback into every prompt"***, שהפרומפט ביקש לשמר.

⇒ **גזום שלושה בולטים, לא סעיף.** **שמר את כל השאר**, ובמיוחד `git commit -- <paths>`.
⚠️ **ובנוסף — שורות ~63–69 (`Documentation boundary`) הן תלות התנהגותית:** הן מנתבות
**סמכות-כתיבה** (*"builders REPORT and **you** filter and write"*). ‏החלפה גורפת הופכת אותן
ל"ישי מסנן וכותב" — **טענה שקרית.** ‏**הכרע עליהן במפורש.**

### ② `references/prompts.md` → `.claude/skills/_shared/writing-prompts.md`

כתיבת פרומפט לסשן אחר — ממשיך להיות רלוונטי.
**גזום:** כלל 0 (באנר-מנהל) · כלל 8 (חוזה-ניתוב) · כללים 9 ו-10 · המצביעים ב-14/16/17.
**שמר במיוחד:** כללים 1ב · 4 · 4ב · 5 · 12 · 15.
⚠️ **שניים בין הכיסאות שהטיוטה לא הזכירה:** **כלל 18** (*"Ishay reads the manager, not the
builder's session"*) — ניתוב טהור, **הופך ל"ישי קורא את ישי"**; הסר. · **כלל 9ב**
(המלצת-דגם-ומאמץ) — **כללי ושימושי, אל תיקח אותו בטעות עם 9 ו-10.**

**בשניהם:** החלף "המנהל" ב"ישי". **הוסף שורת-מצביע ב-`_shared/discipline.md`.**

---

## הכרעה מראש #4 — ארבע תוספות ל-`_shared/discipline.md`

**הקובץ באנגלית.** שלוש כבולטים מתחת לטבלה:
- **An absence-claim is verified the way the SOURCE writes it, not the way the reporter searched.**
- **Never hand a checker an expected number — only a measurement method.**
- **A fix is not done until you have checked where else the same defect lives.**

**והרביעית נכנסת לטבלה עצמה כשורה חמישית, לא כבולט:**

```markdown
| "it works" / "the tests are green" / "this is what the screen looks like" | run it through the **production path** — `npm run test:e2e`, the real browser, the served asset — and **say which path produced the artifact you are judging**. *(Anchor `ba2d41e`: the quote PDF was defective in its **production**, not its code — rendered under vitest instead of fetched from the browser blob, so the verification method lied while the code was fine.)* |
```

🚫 **אל תוסיף דבר ל-`CLAUDE.md` בשורש** — נגזם בכוונה 28/07.
⚠️ **הכותרת של `discipline.md` כבר תוקנה 05/08** ואינה נוקבת במספר — אל תחזיר מספר.

---

## הכרעה מראש #5 — `_shared/failure-modes.md` כבר קיים

**נוצר וקומט 04/08.** ‏**אל תיצור מחדש** — פתח, ודא ששתי החטיבות שלמות (חמשת מצבי-הכשל + שש
שאלות הבדיקה-העצמית). **תואם ⇒ ציין בדוח. חסר/שונה ⇒ עצור ודווח.**
⚠️ **‏`skill-scan` כבר מיישם את מפרט-הסריקה שב-miss-ledger** — אל תבנה ממנו סקיל, הוא קיים.

---

## הכרעה מראש #6 — קובצי הזיכרון

🔴 **חריג מוגדר ויחיד ל-"`~/.claude/**` לא נכנס לסבב REG-IN":** התיקייה
`~/.claude/projects/C--Users-ishay-Reg-In/memory/` שייכת ל-REG-IN בלבד. **שום דבר אחר תחת
`~/.claude/` לא נפתח.**

**נסרקו כל 23 הקבצים. שבעה מזכירים "מנהל" — ארבעה קשורים לתפקיד:**

**① `manager_evidence_regin.md` (28,544 בתים)** → `docs/archive/work-manager/`. **אל תמחק.**
**② `MEMORY.md` שורה ~24** — המצביע אליו. **הסר. אל תיגע בשאר.**
**③ `feedback_verbatim_before_writing.md`** — **הכלל נשאר, אל תיגע.** ‏רק הקישור
`[[manager-evidence-regin]]` (שורה ~29) יהפוך למת — הפנה לארכיון או הסר את הקישור.
**④ 🔴 `ishay_response_playbook.md`** — שלוש שורות ינתבו שגוי, והקריטית:
*"פיצול עצירות-👤 — **המנהל עונה במקומו של ישי** על אישורי תוכנית-צעדים"*. **החזר את היעד לישי
בשלושתן. אל תמחק שורות.**
⚠️ **ושארית ידועה מחוץ לתחום:** `~/.claude/references/ishay-response-playbook.md` מחזיק **14
אזכורי-מנהל** ומשפיע גם על 710. **לא נפתח. ציין בדוח כשארית מוצהרת** — תיקון-חצי שנאמר עדיף
על שתיקה.

🚫 **שלושה שייראו כמו התאמות ואינם — אל תיגע:** `user_profile.md` · `working_agreement.md` ·
`feedback_question_style.md`. שם "מנהל" = **"מנהל המוצר"** או **"מנהל ריבוי-סשנים"**.

---

## 🔴 שבעה משטחים שהטיוטה פספסה לגמרי

1. **`STATUS.md` — 58 התאמות, ולא הוזכר אף פעם.** נקרא בפתיחת כל סשן. רובו היסטוריה לגיטימית,
   **אבל ארבעה מצביעים חיים ימותו:** `work-manager/references/prompts.md` כלל 7 ·
   `docs/manager_triggers.md` · `.claude/skills/work-manager/` · ו`docs/work_manager_situations_draft.md`
   **בנתיב שכבר שגוי היום** (הקובץ ב-`docs/delete/`).
2. **`docs/delete/work_manager_situations_draft.md` — 58,455 בתים, מנוהל-גיט.** טיוטת 21
   המצבים, ובה §ז' כללים חסרי-בית **הממתינים להכרעת ישי.** ‏+ `docs/delete/README.md`.
3. **`docs/CLAUDE.md` שורות ~19 · 24 · 25** — נטען בכל נגיעה ב-`docs/`, ומפרט את **שלושת**
   הקבצים שיארכבו כמקורות-אמת. **ארכוב בלי עריכת הטבלה מותיר את פרוטוקול-התיעוד מצביע לשלושה
   קבצים שנעלמו.**
4. **`docs/toolbox.md`** — מונה את `work-manager` בין "בריפו, תמיד זמינים", ו-`CLAUDE.md` מורה
   *"לקרוא לפני שמציעים כלי"*. ✅ *(אומת: `check-context` מצליב רק פלאגינים — עריכתו לא תשבור
   את השער.)*
5. **`quality-audit/SKILL.md`** — *"NOT part of the shared-kernel set of **the other five**"*.
   הספירה תשתנה. **אל תחזיר מספר — נסח בלי מספר.**
6. **🔴 ה-Stop hook יחסום אותך.** ‏`check-docs-updated.sh` חוסם סיום עד עדכון
   `CLAUDE_CODE_LOG.md` **ו-**`STATUS.md`. **כלול את פרוטוקול-סוף-הסשן בעבודה, לא כהפתעה.**
   *(ה-hooks נקיים מהמילה "manager" — הצימוד כאן התנהגותי.)*
7. **`docs/micro_guides/module-3.md` (66) · `module-2.md` (10)** — רשומות-§9 היסטוריות. **סיווג
   נכון: אזכור היסטורי.** אבל המדריך נקרא במלואו בכל תור-בנייה ⇒ **ראוי לשורת "נבדק — היסטורי
   בלבד"**, לא להשמטה.

**ומשטחים שנבדקו ונמצאו נקיים (05/08 — אמת מחדש):** `.claude/hooks/**` · `.claude/settings*.json` ·
`.github/**` · `package.json` · `CLAUDE.md` בשורש.

---

## אימות לפני ארכוב

1. **חיפוש חוזר** באותו תחום, ארבעת המעברים.
2. **אין Skill פעיל שמורה** לפנות/לחפש/לקרוא/לשלוח/לנתב דרך מנהל.
3. **שני הקבצים הניצלים במקומם, והמצביע ב-`discipline.md` עובד.** קובץ שהועבר ואיש אינו מצביע
   אליו — **ארכיון, לא הצלה.**
4. **`npm run gate` → exit 0.** ‏🔴 **אל תריץ `cmd | tail`** — צינור מחזיר את קוד-היציאה של
   `tail`. **grep מוכיח שלא נשאר טקסט; רק השער מוכיח ששום דבר לא נשבר.**
   נכשל ⇒ **החזר, דווח, אל תארכב.**
5. **רק אז:** `.claude/skills/work-manager/` → `docs/archive/work-manager/`. **לא מחיקה.**
   **ואיתו:** `docs/work_plan.md` · `docs/manager_triggers.md` · `docs/current_manager.md`
   *(האחרון רק אחרי שהבדיקות ב-`check-context.mjs` הוסרו)*.

---

## הדוח

בעברית. כל טענה מתויגת **`[מדדתי בעצמי]`** או **`[קראתי במסמך]`**.

1. **החיפוש העיוור — לפני שקראת את ההכרעות:** המשטחים שמנית ומה נמצא בכל אחד (כולל "נקי").
   **ואז ההשוואה: מה מצאת שאינו בהכרעות — זה החלק החשוב בדוח.**
2. אילו קבצים השתנו · 3. אילו הפניות נמצאו, מה עשית, **ובאיזה מעבר נתפסה כל אחת** (מילה/התנהגות).
4. אזכורים היסטוריים שנשארו בכוונה · 5. תוצאת החיפוש החוזר · 6. **`npm run gate` — קוד יציאה.**
7. **החלפות-היעד:** כמה מקומות בפועל, באילו שורות, לפני ואחרי. **שונה מהצפוי ⇒ אמור זאת.**
8. ההצלה: מה הועבר, מה נגזם, האם המצביע נבדק · 9. מה לארכיון ובאיזה נתיב · 10. מה ללא שינוי בכוונה.
11. **ידע שימושי שמצאת ולא הועברת** — ציטוט + קובץ, להכרעת ישי.
12. **מיפוי `section7-rulings`** *(קריאה בלבד, אל תשנה)*: מה בו שייך להחלטות-מודול ועשוי להשתלב
    ב-Discovery · מה כלל-כללי · מה יהפוך לכפילות כשה-Discovery ייבנה.
13. חסמים והתאמות עתידיות.

**וסיים בשתיים, גם אם "אין":** מה עוד לא בדקת? · מה הבליינד-ספוט שלך?

**כל עובדה כאן ניתנת לערעור — מדדת אחרת, תקן אותי עם המדידה.**

**עצור אחרי הדוח.**

---

## כללי-שינוי כלליים

- הסר/עדכן **רק הפניות פעילות**. אל תיצור Agent/Manager/routing/תפקיד חלופי.
- אל תיצור סקילים חדשים · אל תעצב מחדש Blueprint/Build/Close **מעבר להחלפת-היעד**.
- **אל תשנה את `section7-rulings`** (מיפוי בלבד).
- **ידע שימושי שאינו תלוי-תפקיד: צטט בדוח, אל תעביר לבד.** החריגים היחידים: הכרעות #3–#6.
- **אל תארכב לפני האימות החוזר.**

</div>
