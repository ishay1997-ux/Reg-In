// תצורה נפרדת ל-`npm run test:e2e` (חבילת ה-78 בדיקות) — playwright.config.js הבסיסי
// נשאר בלי שינוי ומשרת את `npm run smoke` בדיוק כפי שהוא (שרת-dev חי על 5173, בכוונה —
// smoke.mjs בודק את המערכת כפי שהיא רצה עכשיו, לא בנייה נפרדת). קובץ זה קיים כי לשתי
// המטרות יש דרישות-שרת שונות; מיזוגן לקובץ אחד היה משנה בשקט את מה ש-smoke בודק.
//
// 🔬 09/08/2026 — הוחלף מ-`npm run dev` (יציאה 5173) ל-build+preview (יציאה 4173),
// מדוד ולא ניחוש: שש עקבות-הכשל של הריצה שנפלה (14:05, playwright-report/data/*.zip)
// הראו 2-3 רענוני-דף מלאים בתוך כל אחת משלוש הבדיקות ה"תקועות-על-login" — WS של Vite
// מתחבר-מחדש + main.jsx נטען עם חותמת-זמן חדשה + net::ERR_ABORTED על בקשת-Supabase
// שהייתה באוויר באותו רגע. אותה חותמת-זמן חוזרת בשני עקבות שונים (למשל t=1786272857661
// גם ב-4c9f7f43 וגם ב-b5380118) — כלומר זה שרת-dev אחד שנרענן שוב ושוב לכל מי שנטען
// עליו באותו רגע, לא באג בקוד ההתחברות: AuthContext.loadUser הצליח בפועל בכל שלוש
// (permissions נטענו במלואן) — הדף פשוט נמחק ע"י הרענון לפני שהניווט הספיק לקרות.
// שרת-פיתוח עם HMR עלול לרענן-מלא (גילוי-תלות חדש / שמירת-קובץ חיצונית שרואה אותה —
// כלל 16: סשן אחר עלול לערוך קבצים באותו רגע) — שרת-preview על בנייה קפואה לא יכול,
// כי אין בו צופה-קבצים בכלל. יציאה נפרדת (4173, לא 5173) כדי שריצת-החבילה המלאה לעולם
// לא תתחבר בטעות לשרת-dev אינטראקטיבי שרץ כבר במקביל. `reuseExistingServer:false`
// בכוונה — preview ישן עלול לשרת קוד לא-עדכני בלי אזהרה; מחיר הבנייה נספג ב-timeout
// המורחב (120s, מול 30s שהספיקו לעליית dev בלבד).
import { defineConfig } from '@playwright/test'
import baseConfig from './playwright.config.js'

export default defineConfig(baseConfig, {
  use: {
    baseURL: 'http://localhost:4173',
  },
  webServer: {
    command: 'npm run build && npm run preview -- --port 4173 --strictPort',
    url: 'http://localhost:4173',
    reuseExistingServer: false,
    timeout: 120_000,
  },
})
