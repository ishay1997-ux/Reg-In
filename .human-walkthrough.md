# human-walkthrough — הגדרות REG-IN
עודכן: 13/08/2026

## סביבה
כתובת: https://reg-in-umber.vercel.app/ (Vercel, ענף `main`)
ייצור/עותק: **ייצור בלבד — אין סביבת-בדיקה נפרדת.** (פרויקט אקדמי, אין לקוח אמיתי בקצה)
כלים זמינים: דפדפן (Claude in Chrome, ההתחברות מבוצעת ע"י ישי בלבד — לעולם לא מקליד סיסמה) ·
מסד-נתונים (Supabase MCP, פרויקט `yfeovxppnfoafmfbdfvh`, קריאה+כתיבה) · GitHub CLI (`ishay1997-ux`) ·
לוגים: Supabase (Postgres/Edge) בלבד — **אין גישה ללוגי Vercel.**

## זהות
משתמש: ישי (מתחבר בעצמו בטאב שקלוד פותח)
תפקידים לבדיקה: 5 — מנכ"ל · מנהלת פרויקטים · מנהלת כספים ולקוחות · מנהלת גיוס ושיבוץ · מנהלת לוגיסטיקה
(כל תפקיד דורש כניסה נפרדת של ישי — לא ניתן להחליף תפקיד בלי חשבון-בדיקה נפרד)
מסכים שנפתחים על ידי משתמש לא-מיומן: **`/shift/:token`** — הדף הציבורי לאישור משמרת, נפתח ע"י דיילת
מהטלפון בלי חשבון בכלל. מקבל מעבר-נגישות/RTL/מובייל נפרד תמיד.

## הרשאות שניתנו
מיילים: מותר — עם `ishay1997@gmail.com` כנמען-בדיקה
כתיבה: מותר — זה בדיוק העניין
מחיקות: מותר, **רק על שורות שקלוד יצר בעצמו** — אסור לגעת בנתונים קיימים, ללא יוצא מהכלל
כסף: לא זוהה מסלול תשלום/סליקה אמיתי במערכת; לאשר מחדש אם יתגלה
תיעוד נתונים אישיים: מטשטש כברירת מחדל

## פעולות חד-כיווניות ידועות
- אישור הצעת-מחיר → יוצר פרויקט (RPC ייעודי, `approve_quote_and_create_project`)
- אישור/סירוב משמרת דרך `/shift/:token` — קישור נצרב לתוך המייל שנשלח, לא ניתן לשינוי-נתיב
- טריגר-נעילה על `quotes`/`quote_services`: UPDATE/DELETE על שורה שאינה `in_progress` נכשל בקול (`P0001`)

## מפות-תוצאות מאושרות
טרם נבנו — ריצה ראשונה הייתה עשן בלבד.

## תקרת פעולות
40 (ברירת מחדל)
