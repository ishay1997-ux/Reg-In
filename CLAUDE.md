<div dir="rtl">

# CLAUDE.md — REG-IN

> **מה הקובץ הזה מכיל (שונה מ-28/07/2026):** רק **מוקשים** — מה שלא ניתן להסיק מקריאת הקוד או עץ
> הקבצים — ו**שערים** — מה שאסור לעשות לבד. כל השאר עבר לקובץ שנטען בדיוק כשהוא נחוץ. הגרסה
> המלאה הקודמת: `docs/archive/CLAUDE_full_2026-07-28.md`. **הסגנון והתקשורת מול ישי:
> `~/.claude/CLAUDE.md`** (גלובלי, נטען בכל סשן) — לא משוכפל כאן.

## מה זה REG-IN

מערכת ניהול עסקית מודולרית לחברת אירועים/כנסים — 13 מודולים (0–12): לקוחות · הצעות מחיר · דיילות
+ Smart Match · לוגיסטיקה · פרויקטים · כספים · דו"חות.
**מפתח יחיד: ישי** (מ-22/07/2026; עמית פרש — אזכוריו ביומנים מתוארכים = היסטוריה קפואה. "עמית
מילר" שבדאטת-הבדיקה הוא לקוח, לא המפתח). ⏱️ **לוח-הדדליינים שונה 12/08/2026 — ‏`19/09` מבוטל.** ‏**28/08** הצגת-ביניים *(10 דק' · תהליך אחד · ~50%)* · **01/10** כנס-הסיום *(היעד: **100%**)* · **20/10** סיום. הלוח המלא + כלל-החיץ: `docs/guides/00_roadmap.md` §3.
**מוגש: האפליקציה בלבד** — התיעוד הוא כלי-עבודה פנימי, לא תוצר.

🎓 **התוצר: הצגה בת שעה בכנס + מענה לשאלות — פרויקט אקדמי.** החברה אמיתית, **אך אין לקוח בקצה
ואין משתמשים אמיתיים**; המבחן הוא **קוהרנטיות, לא עומס** — מסך שלא מתחבר לסיפור שובר את השעה,
ואף בדיקה אוטומטית לא תתפוס זאת. שלושת מסנני-ההצעות **וארבעת קריטריוני-השיפוט בכנס**: `docs/PROJECT_MASTER.md §1`.
🎯 **והקריטריונים במילותיו (28/08/2026): ידידותית · נעימה למשתמש · פונקציונלית · בקיאים בחומר.**
🔴 **הרביעי אינו על המערכת אלא על ישי** — הוא נמדד בחדר כשישאלו *"למה בחרת ככה?"*, **וזהו
הקריטריון היחיד שאפשר להכשיל בלי לכתוב שורת-קוד:** החלטה נכונה בלי נימוק-במשפט-אחד שישי יכול
לומר בקול היא כישלון בו. ⚠️ **ו"יפה" אינו ברשימה** — ליטוש מעבר לשפה שהוכרעה אינו קונה נקודות. *(נוסף
05/08/2026 בהאצלת-ישי — נמדד שאף קובץ שנטען-תמיד לא נשא זאת, וסשן חופשי היה מסיק "מוצר
ללקוח אמיתי" ומתכנן Enterprise.)*

**Stack:** Vite 8 + React 19 · **JavaScript (לא TypeScript)** · Tailwind 4 + shadcn/ui + Radix ·
Supabase (Auth + Postgres 17) · react-router-dom 7 · עברית RTL מלאה.

## 🧨 עובדות סביבה (מוקשים — ניחוש כאן שובר דברים בשקט)

- **ייבוא לקוח Supabase: `@/supabaseClient`** — הקובץ יושב ב-`src/supabaseClient.js`.
  ⚠️ **לא `@/lib/supabaseClient`** (הנתיב שכל אחד מנחש — ואינו קיים).
- alias `@/` → `src/` · שרת פיתוח `npm run dev` על פורט **5173**.
- **חותמת-זמן — לא לערבב בין הכלים:** ב-PowerShell `Get-Date`; ב-Bash `date +"%d/%m/%Y %H:%M"`.
  ‏`Get-Date` **אינו קיים** בכלי Bash ויחזיר `command not found`. פורמט: **`DD/MM/YYYY HH:MM`**,
  מהשעון — לא מנוחש. ⚠️ **ומוקש-משנה: גם *חיפוש* אחרי שמות ה-cmdlets האלה ייחסם** — ההוק
  קורא `|` שלפני השם כמיקום-פועל *(`block-shell-dialect-mixup.sh:71`; קרה 07/08/2026)*.
  לחפש דרך הכלים `Read`/`Grep`, לא דרך `grep` בכלי Bash.
- **ספירת שורות — `(Get-Content -LiteralPath X).Count`. ⚠️ לא `Measure-Object -Line`:** הוא
  **מדלג בשקט על שורות ריקות** ומחזיר מספר קטן יותר בלי להתלונן. *(נמדד 05/08/2026 בבקרה: קובץ
  עם 3 שורות טקסט ו-3 ריקות → `.Count`=6 · `Measure-Object -Line`=3. **שני סשנים נפרדים דיווחו
  1,387 ו-1,838 על קובץ בן 1,839.**)* ‏`git show HEAD:file | Measure-Object` הוא אימות-צולב טוב.
  ⚠️ ו-`wc -l` מחזיר **אחד פחות** כשהקובץ אינו מסתיים בשורה חדשה — זה מקור לפער-של-1, לא באג.
- 🔴 **כתיבת קובץ מפייתון על Windows — חובה `newline` מפורש, אחרת השער נשבר בשקט.**
  ‏`io.open(p,'w',encoding='utf-8')` **מתרגם כל סוף-שורה ל-CRLF**, והריפו כולו LF
  (`.gitattributes`: `* text=auto eol=lf`). ⇒ ‏`npm run format:check` נופל על כל קובץ שנכתב כך
  ו-Prettier בודק — **וזה מפיל את `gate` כולו**. ⚠️ **ולמה זה מוקש ולא באג רגיל: `git status`
  נקי ו-`git diff` ריק** (‏`core.autocrlf=true` מנרמל לפני ההשוואה) — **אין שום סימן** עד
  שהשער נופל, וגם אז ההודעה מדברת על "בעיית-סגנון" ולא על סופי-שורה.
  ‏**המדידה הכנה:** לספור בתי-CR בקובץ (בכלי Bash: `tr -cd` עם התו CR לתוך `wc -c`) ⇒ חייב 0.
  **התיקון:** ‏`sed -i` שמוחק CR בסוף כל שורה. 🚫 **ולא `prettier --write .`** — הוא סורק את כל
  הריפו ועלול לגעת בקבצים של סשן אחר.
  *(נמדד 27/08/2026 בשער-פזה-1 של מ8: **11 קבצים נדבקו** — `schema.sql`, `STATUS.md`,
  ‏`db_roadmap`, מדריך-המיקרו, שלוש מיגרציות, ‏`ci.yml` ו-`send-email/index.ts`. הבלובים שב-Git
  היו תקינים כל הזמן — הזיהום היה **רק בעץ-העבודה**, ולכן **CI היה נשאר ירוק** והפער היה מתגלה
  רק אצל מי שמריץ את השער מקומית.)*
- 🔄 **סיבוב סשן — לפני 60% מהחלון:** סיים את הפריט הנוכחי · קמט · **עצור.** ‏🚫 **ולא קומפקט** —
  הוא נורה ב-~83% ‏**וזה מאוחר מדי בהגדרה**; מעל 60% הוא "אזור אדום" שבו סשנים מייצרים עבודה
  שתצטרך לעשות מחדש. **סשן טרי + מבחן-קבלה** *(הסשן הנכנס עונה על שאלות שהתשובות על הדיסק,
  לפני כל פעולה)*. **המדריך המלא, ואל תגזור אותו מחדש — המספרים מדודים:**
  `~/.claude/references/session-handoff-guide.md`. *(נכתב כאן 06/08/2026 כי נמדד שהוא לא היה
  מוזכר באף קובץ שסשן-בנייה טוען — ובנייה היא בדיוק המקום שקומפקט-באמצע עולה בו הכי יקר.)*
- 🔴 **`npm run gate` אינו מה ש-CI מריץ.** ‏`.github/workflows/ci.yml` מריץ את השלבים בנפרד
  ואינו קורא ל-`gate` כלל ⇒ **`check:docs-structure` אינו רץ בשום מקום אוטומטי** (מוזכר רק
  ב-`package.json`), ו-`check:context` רץ רק כהוק-פתיחת-סשן **פתוח-לכישלון** — הקשר, לא שער.
  ההרכב המדויק: `package.json` §scripts מול `ci.yml`. *(נמדד 07/08/2026.)*
- ⚠️ **`npm run test:e2e` מדלג בשתיקה על בדיקות-העשן** — הסקריפט הוא
  `playwright test --grep-invert בדיקת-עשן`. ‏**ו-E2E אינו רץ ב-CI כלל.** בדיקות-העשן הן
  `npm run smoke` (`e2e/smoke.spec.js`) — גם היא אינה ב-`gate` ואינה ב-CI. *(נמדד 07/08/2026.)*
- **קבצים חדשים — שמות באנגלית בלבד** (עברית בשמות קבצים נשברת ב-Git/Windows). תוכן עברי בתוך
  `<div dir="rtl">`.
- **חלוקת שפות:** `docs/guides/` + `STATUS.md` בעברית (לבני אדם) · `docs/micro_guides/`,
  `.claude/skills/**`, `docs/CLAUDE_CODE_LOG.md` באנגלית (ל-Claude; עברית רק כדאטה — שמות מודולים,
  מחרוזות UI, ציטוטי §7). **דיווחים בצ'אט לישי — תמיד בעברית.**

## 📂 הפרוטוקולים המלאים — נטענים לפי מה שנוגעים בו

| נוגע ב… | הקובץ שנטען אוטומטית ומכיל את הפרוטוקול המלא |
|---|---|
| **מיגרציה / DB** | **`supabase/migrations/CLAUDE.md`** — שער ה-typed-echo, `apply_migration`, ‏`schema.sql`, ‏`db_roadmap` |
| קוד (`src/`) | `src/CLAUDE.md` — מודל-אבטחה, SSOT ללוגיקה, מוקשי-קוד |
| תיעוד (`docs/`) | `docs/CLAUDE.md` — מקורות-אמת, §7, מראות 🔗, מקרא-אימוג'י |

## היררכיית מקורות אמת (בסתירה — הגבוה קובע)

1. **`docs/schema.sql`** — סכמת ה-DB (snapshot; מקור-אמת לשינויים = `supabase/migrations/`).
2. 🆕 **האפיון המאושר של המודול** — `docs/specs/module_NN_*/` *(תוצרי שיחת ה-Discovery: תהליכים · מסכים · מוקאפים מאושרים)*.
   **גובר על C5/C6 בכל מה שהוא מכסה בפועל — פר-פריט, לא פר-תיקייה.** מה שאינו מכוסה שם — ‏C5/C6 קובעים.
   *(נוסף 06/08/2026 בהכרעת-ישי. **מה זה מחליף:** את ההנחה השקטה ש-C5 הוא תמיד הסמכות המוצרית —
   הנחה שנשברה כשנמדד ש-C5 נכתב לפני שישי הכיר את התחום. **ומה זה אינו:** אינו מבטל את C5, אינו
   מתיר לערוך אותו, ואינו מסיר את בדיקת-הכיסוי מולו — ‏C5 נשאר **עוגן-ההצלבה הבלתי-תלוי היחיד**,
   כי את `docs/specs/` קלוד עצמו כתב.)*
3. **האפיון הקפוא** — `docs/reference_spec/C5_clean_transcript.md` (תהליכים/מסכים) +
   `C6_clean_transcript.md` (טבלאות/ERD). 🧊 **קפואים — לא עורכים לעולם** (ה-hook חוסם); סטייה
   נרשמת בתיעוד החי עם הערת "סטייה מ-5.x".
4. **המוקאפים** (`docs/mockups/`) — רפרנס ויזואלי בלבד, **וחלק מפרטיהם שגויים**.
5. **המדריכים** (`docs/guides/`, `docs/micro_guides/`) — מתכון לסדר העבודה. הכי נמוך.

## כללי הברזל — אינדקס

> **המספור קפוא ולא ממוחזר לעולם** (עשרות ציטוטים בריפו תלויים בו).
> **כלל שאינו מנוסח כאן עדיין בתוקף במלואו** — הוא פשוט חי במקום שנטען בזמן הנכון, או שמנגנון
> אוכף אותו טוב יותר מטקסט. *(נגזם 28/07/2026: 17 כללים מנוסחים → **5 מנוסחים + 4 מצביעים**
> (נספר מחדש 05/08; קודם נכתב כאן "7", ואין 7). הנוסח המלא הקודם:
> `docs/archive/CLAUDE_full_2026-07-28.md`. **למה:** הוראות חופפות עולות זמן-חשיבה בכל תור — לא בגלל
> אורכן, אלא כי צריך ליישב ביניהן. הכרעת-ישי 28/07.)*

**1. שאל כשלא חד-משמעי — ההכרעה תמיד של ישי.**
🔴 **ורעיון שלו אינו הכרעה שלו — וזו ההבחנה שמונעת ממך להפוך ל-yes-man** *(הוכרע 06/08/2026; היה
⏸️ פתוח מ-05/08)*. ‏`_shared/writing-prompts.md` כלל 2 מורה לצטט אותו **מילה-במילה** — **וזה נכון
להכרעה ומסוכן לרעיון.** כשהוא זורק *"אולי כדאי ש…"* / *"חשבתי ש…"* / *"מעלה כרעיון"* — **רשום אותו
כרעיון ובדוק אותו כמו כל אפשרות אחרת, כולל אם הוא מתנגש בהכרעה קודמת שלו.** ‏**הכרעה מצוטטת ומחייבת;
רעיון עובר בדיקה, וחוזר אליו עם המלצה.** ⚠️ **וב-write-back זה נכשל בפועל:** *"אחראית משמרת"* נרשם
ב-`db_roadmap.md` *(grep `אחראית משמרת`)* כ-**"הכרעת-ישי"** בעוד מילותיו היו *"מעלה **כרעיון** אם לאמץ או לא, **מתייעץ
איתך** על זה"*. **ההבחנה תקפה בכל שיחה — לא רק בסשן אפיון**, ושלושה סוכנים בלתי-תלויים הגיעו אליה בנפרד.
שאלות פתוחות מוכרעות **רק איתו** (הרשימה: `PROJECT_MASTER §7` — **הקובץ בפועל `docs/PROJECT_MASTER_sec7.md` מ-08/08/2026**; ‏§6 נשאר ב-`PROJECT_MASTER.md`), לעולם לא לבד. אותו דבר לכל החלטת
מוצר או עיצוב. **טריאז' בקשת-שינוי:** לכל בקשה לא-טריוויאלית — **ורדיקט גלוי** `בנה-עכשיו` /
`דחה-ל-X` / `לא-נדרש-כי-Y` עם נימוק, כדי שישי יוכל לתפוס דילוג; מודול בפיגור מול הדדליין → ברירת-המחדל
`דחה-ל-אחרי-הדדליין`, ו**דוחים מודולים שלמים, לא מקצצים תוכן**.

🔑 **ולפני שאתה מביא לו ממצא, שאלה או הצעה — עוגן, אחרת אל תביא** *(הכרעת-ישי 08/08/2026)*.
עוגן = תקדים במערכת · מקום בקובץ *(עוגן-גריפ, לא מספר-שורה)* · מדידה שרצה · מסך שהוא ראה · הכרעה שלו.
**יש עוגן ⇒ הכרע לבד, הצג את העוגן, והוא יכול לעקוף.**
➕ **וברירת-מחדל שאינה דורשת עוגן כלל: ביצוע-טכני בלי משמעות מוצרית** — שם משתנה · מבנה שאילתה ·
ניסוח קוד · תיקון לוג. **אלה שלך להכריע ללא שאלה; עוגן נדרש למה שמישהו רואה או מרגיש.**
🔴 **אין עוגן פנימי ⇒ לך והבא עוגן חיצוני לפני שאתה חוזר אליו** — **מה מקובל בעולם** *(מקור
מצוטט)* **ומה הגיוני עסקית אצלנו.** **זה מה שהופך "אין תשובה אצלנו" ל"המלצה עם בסיס", וזה
החיסכון האמיתי בכאב שלו.** 🚫 **אך אינו מחליף אותו — ההכרעה נשארת שלו, ומגיעה *עם* ההמלצה,
לא במקומה.** ➕ **ומוסכמת-עולם אינה מאומצת בלי בדיקת-התאמה ומחיר-מימוש** — ארבע שאלות-ההתאמה
ודרישת-המחיר ב-**`_shared/discipline.md`**.
**ורק כשגם זה ריק** ⇒ אמור **"אין עוגן"**, מה היה נדרש כדי שיהיה, ומה בכל זאת ידוע.
🚫 **לעולם לא להמציא, ולא להכריע לפי "מה שנשמע לי הגיוני".** ⚠️ **אבל "לא להכריע לפי תחושה"
אינו "לא להביע דעה":** גם בלי עוגן — **אמור מה דעתך ומדוע, מסומן כדעה** *(תג `הנחתי`,
`_shared/discipline.md`)*. **האיסור הוא להכריע לפיה, לא לומר אותה.**
⚠️ **ושאלה שההכרעה בה שלו — עדיין נשאלת** *(המקרה השלישי בפרוטוקול-הסתירה למטה)*; **מה שאסור
הוא לשאול אותה בידיים ריקות.**
➕ **וגם הצעה טובה שלא ביקש היא כשל** — במילותיו *(08/08/2026)*: *"אין סיבה להוסיף עבודה גם אם
זה שיפור נחמד"*. **לפני שאתה מציע שיפור: הוא פותר בעיה שנצפתה, או רק נראה נכון?**
⚠️ **וזה אינו מרחיב את מה שאתה רשאי להכריע — הוא משנה את *צורת* מה שמגיע אליו.** שאלה פתוחה
ב-§7, הכרעת-מוצר והכרעת-עיצוב **נשארות שלו** כפי שנכתב למעלה.
🔴 **אבל "ההכרעה שלו" אינה "אין לי דעה":** הפריט מגיע **מעוכל, עם ראיה, ועם המלצה מנומקת** —
וסמן במפורש מה **עוגן** ומה **המלצה**. **"אני לא יודע, תחליט אתה" הוא בדיוק היד-הריקה שנאסרה
למעלה.**
🔴 **ועוגן שהוא אינו יכול לבדוק בעצמו אינו עוגן — הוא טענה בתחפושת.** ישי אינו קורא קוד ⇒ תרגם
למשהו שהוא יכול לשים עליו אצבע: מסך · מוקאפ · הכרעה שלו · תרחיש עם שם ומספר. **המבחן: אם יאשר
בלי להבין — סיפקת ניסוח משכנע, לא עוגן.**
⚠️ **ושתי הרשימות אינן סותרות ואינן כפולות:** ההגדרה למעלה היא **מה שמספיק לך כדי להכריע**;
זו — **מה שמספיק כדי להציג לו.** ‏`מקום בקובץ` ו`מדידה שרצה` הם עוגן מצוין עבורך **וחסרי-ערך
עבורו** עד שתתרגם אותם.
**וארבעת המבחנים, במילותיו — וחלים על כל דבר שאתה מביא, כולל הכרעת-מוצר:** *"מה שהכי הגיוני
וישים במערכת ומתחבר היטב עם כל התהליכים הלוגיקה ושאר המסכים… שמהנדס יסתכל על הקוד ויתרשם
לטובה, ושיהיה לי מה הסברים טובים אם ישאלו אותי בכנס למה בחרתי ככה."*
**וכשאלות-ביצוע — עונים עליהן לפני שמביאים, לא אחרי:**
**‏① הגיוני וישים** ⇒ *על מה זה רץ?* — נקוב בדבר הקונקרטי שקיים היום או רשום ב-`db_roadmap`;
אין ⇒ זו משאלה, לא הצעה. *(ומוסכמת-עולם — ‏`_shared/discipline.md`, כמו למעלה.)*
**‏② מתחבר, לא מבודד** ⇒ *מי כבר הכריע משהו דומה?* — חפש תקדים. רכיב דומה שמתנהג אחרת הוא
**סתירה לפתור, לא בחירה חופשית.**
**‏③ מהנדס יתרשם** ⇒ *דפוס קיים או חריג חדש?* — מה שמרשים מהנדס זו **מערכת אחת עקבית**, לא ערימת
מקרים-פרטיים; חריג חדש חייב להצדיק את עצמו.
**‏④ הסבר בכנס** ⇒ *מה הסיבה במשפט אחד, בלי לצטט מסמך?* — אם כל התשובה היא *"ככה כתוב
ב-C5/במוקאפ"*, **זו העתקה ולא הכרעה.**
🔴 **וסתירה אינה שאלה — היא סימן שלא קראת מספיק. שלושה מקרים, ורק השלישי הוא שאלה:**
**‏① הוכרע במקום אחר** ⇒ לך תמצא. קרא את המסמך הרלוונטי **במלואו**, לא חצי ולא grep ממוקד.
**‏② הוא הכריע פעמיים, אחרת** — קורה, **כי הוא לא זוכר מה הכריע** ⇒ הבא את **שתי** ההכרעות
מצוטטות ומתוארכות, ושאל איזו עומדת. 🚫 **לעולם לא לציית לזו שמצאת ראשונה** *(עוגן: §7.14 נשא
משקולות שהוחלפו 22 יום אחר-כך, ואיש לא עדכן)*. **‏③ מעולם לא הוכרע** ⇒ שאלה, עם המלצה.
🔴 **ולפני שאתה שולח — ודא שאינך עושה אחד מארבעת אלה** *(נמדדו עד כה, 08/08/2026; **לא** טקסונומיה סגורה)*:
**ממצא-בלי-עוגן** · **שאלה-שיש-לי-עליה-תשובה** · **הצעה-שלא-ביקש** · **עוגן-שאתה-לא-יכול-לבדוק**.
*(ואלה גם המילים שישי תופס בהן — אותם שמות, שני כיוונים.)*

*(מנגנון תקציר-החוויה 🗣️ וההמתנה-לאישור-לפני-קוד: `.claude/skills/module-build` — הוא רלוונטי רק בתור-בנייה.)*

**8. עיצוב — לא ממציאים.** שפת-העיצוב ב-`PROJECT_MASTER §4`: פרימרי teal `#0D9488` · רקע `#F8FAFC` ·
סרגל-צד ימני. **אין לשנות צבעים או פריסה בלי אישור ישי.**

**10. Git — שער קשיח.** לעולם לא לדחוף ל-`main`/`dev` ישירות ולא `--force`. עבודה רק על ענף אישי
`ishay/module-N-…`. 🔴 **וקומיט תמיד בפאת'ספק מפורש — `git commit -- <נתיבים>`, לעולם לא `git add -A`:**
האינדקס משותף, וקימוט של קובץ אחד סוחף את השורות התלויות של סשן אחר *(קרה: קומיט של קובץ אחד סחף
9 קבצים זרים כולל מיגרציה)*. 🚫 **ולעולם לא לארכב או לסגור סשן — זו פעולת-ישי בלבד.** *(01/08:
הוראה שלו נקראה כהרשאה, שלושה סשנים אורכבו, **והוא שחזר את שלושתם ביד.** הכלי `archive_session`
נגיש מכל סשן ואף הוק אינו חוסם אותו.)*
*(שני אלה נכתבו כאן 05/08 כי הם חיו רק ב-`_shared/parallel-sessions.md` — קובץ שנטען רק דרך סקיל,
ולכן סשן חופשי לעולם לא ראה אותם. נמדד: `git add -A` לא הופיע באף קובץ שנטען תמיד.)* **מיזוג-באמצע-סשן** (אחרי תקרית אמיתית): אחרי כל push, לפני כל commit נוסף —
`git fetch origin` + `git merge-base --is-ancestor HEAD origin/dev`. אם כבר מוזג — **לא לערום על
ענף-מת**; להודיע ולהציע ענף חדש מ-`dev` טרי.
🔴 **וסייג שחסר כאן מאז שהכלל נכתב, ונמצא 12/08/2026:** הבדיקה **מצליחה על ענף טרי בלי קומיטים
בדיוק כמו על ענף שמוזג** — בשני המקרים `HEAD` הוא אב-קדמון של `origin/dev`. ⇒ **ענף שזה עתה נחתך
מ-`dev` נקרא "מת"**, והסשן מציע לחתוך ענף חדש במקום לעבוד. **המבחן שמפריד:** `git log
origin/dev..HEAD --oneline` — **ריק ⇒ ענף טרי** *(אין מה למזג, פשוט תתחיל לעבוד)* · **לא-ריק ⇒
באמת מוזג/מפגר.** *(הבדיקה מוזמנת בכמה קובצי-הוראות ואף אחד מהם לא נשא את הסייג; שוחזר מדחיסת-יומן. **מספר מדויק לא נרשם כאן בכוונה — הוא מתיישן.**)*

**12. DB/RLS קודם, UI אחר-כך.** סדר-הבנייה בתוך מודול. *(נקודות-העצירה 🔻 עצמן חיות במדריך-המיקרו.)*

**17. פרומפט מסירה 🧩.** כל צעד שקלוד לא יכול לבצע (PR ב-GitHub, Supabase Studio, OAuth, סודות) מקבל
**שני** דברים: הוראות בעברית צעד-צעד **+** בלוק "🧩 פרומפט לקלוד בדפדפן" — עצמאי, בלי להסתמך על השיחה,
בלי סודות. *(ישי לא מריץ פקודות — זה לא נימוס, זו הדרך היחידה שהצעד יתבצע.)*

🔴 **כותב פרומפט לסשן אחר? קרא `.claude/skills/_shared/writing-prompts.md` לפני.** מעבר-כוונה ·
אימות-עובדות באותו יום · מוקש-על אחד עם דרישת-ספירה · המלצת-מודל · ‏2–3 שורות עברית לישי לפני הבלוק.
*(נכתב כאן 05/08 כי הקובץ נטען **רק** דרך סקיל, ואין סקיל שנדלק על "תכתוב פרומפט" — כלומר
בזרימת-העבודה הנפוצה ביותר כאן הוא לא נקרא מעולם. נמדד: 04–05/08 שלושה פרומפטים נשאו כל אחד פגם,
ואפס נתפסו בקריאה-חוזרת.)*

**14. SSOT ללוגיקה עסקית** → `src/CLAUDE.md` · **9. מודל אבטחה** → `module-blueprint/template.md` (הצהרת-ההישענות) + `module-close/template.md` (רגרסיית-אבטחה); ‏`src/CLAUDE.md` נושא את **מוקשי**-ההרשאות, לא את הכלל ·
**5. שינויי DB** → `supabase/migrations/CLAUDE.md` · **13. פרוטוקול אדוות** → `docs/CLAUDE.md`.

### הכללים שנגרעו מכאן — ואיפה הם חיים עכשיו

| # | הכלל | למה לא כאן | היכן הוא בתוקף |
|---|---|---|---|
| 2 | Plan Mode בפתיחה/סגירה/§7 | התנהגות-סביבה + נוהל-תוכניות | `docs/CLAUDE.md` · הסקילים |
| 3 | הערות "why-first" בעברית | ברירת-המחדל היא ממילא "כתוב כמו הקוד שסביבך", והקוד כאן מוער בעברית בעקביות | הקוד עצמו · `src/CLAUDE.md` |
| 4 | לצטט קובץ+שורה, לא מהזיכרון | **כתוב ב-`~/.claude/CLAUDE.md`** שנטען בכל סשן | הגלובלי · `_shared/discipline.md` (יעדי-האימות פר-סוג-טענה) |
| 6 | לא לקמט סודות | 🔴 **וחצי הכלל שאינו על קימוט כלל: לעולם לא להדפיס סוד בצ'אט או במדריך.** הנוסח המלא ב-`README.md` (אינו נטען אוטומטית) ⇒ **הוא נכתב כאן כי לא היה לו בית שקלוד קורא** *(נמדד 05/08: החצי הזה חי אצל קלוד רק ב-`micro_guides/module-3.md`, שיימחק בדחיסת המדריך)*. ⚠️ **וההמשך — חלקי: `gitleaks` חוסם *מיזוג*, לא *קימוט*.** הוא רץ ב-CI רק על PR/push ל-`dev`/`main`; ב-pre-commit רצים eslint+prettier בלבד, ו-`gitleaks` **אינו מותקן מקומית** (נמדד 05/08). ⇒ סוד שנדחף לענף אישי כבר ב-GitHub ונשאר בהיסטוריה. **הכלל המקורי עדיין בתוקף כמשמעת** | `.github/workflows/ci.yml` · `🚧 מ4` |
| 7 | לוודא בפועל | **כתוב ב-`~/.claude/CLAUDE.md`**: *"Correctness is entirely yours — actually run the thing"* | הגלובלי |
| 11 | שמות קבצים באנגלית | היה **כפול באותו קובץ** | "עובדות סביבה" למעלה |
| 15 | מדריך-מיקרו חי + חוב 🚧 | **ה-Stop hook חוסם ומסביר בדיוק מה חסר** | `.claude/hooks/check-docs-updated.sh` · `module-build` |
| 16 | **שיחה כותבת אחת בכל רגע** — 🔴 **והכלל עצמו נשאר כאן, לא רק המצביע** *(תוקן 12/08/2026)*: **שני סשנים שכותבים לאותו עץ מתנגשים** — האינדקס משותף, וקימוט של אחד סוחף את השורות של השני. **לא בטוח אם מישהו כותב עכשיו? שאל את ישי לפני שאתה נוגע בקובץ משותף.** ⚠️ **ולמה זה הוחזר לכאן:** ההוק שמתריע יורה **רק אם סשן אחר נגע בקבצים ב-120 הדקות האחרונות**, והוא פתוח-לכישלון ⇒ **סשן שעובד לבד לעולם לא נתקל בכלל**, ושני הקבצים שנשאו אותו נטענים רק דרך סקיל. *(נמדד באודיט-הגיזום 12/08/2026: אפס קבצים נטענים-תמיד נשאו את הנוסח.)* | ה-hook **מזהה בראיה ומתריע**, ואינו תחליף לכלל | `.claude/hooks/session-start-context.sh` · `_shared/parallel-sessions.md` |

## 🧰 ארגז הכלים — מדיניות שיקול-דעת

`module-blueprint` · `module-build` · `module-close` הם **זרימת-העבודה הרגילה** — נטענים לפי הטריגר
בלי לשאול. **כל סקיל אחר: הצע בשורה אחת ("זה נראה מקרה ל-X — להריץ?") והמתן.** משימה קטנה לא מצדיקה
סקיל; אם התשובה ברורה — פשוט לענות.
**מה קיים, מה כבוי ומתי להדליק: [`docs/toolbox.md`](docs/toolbox.md)** — לקרוא לפני שמציעים כלי.
‏`llm-council` (התייעצות בהחלטות עם טרייד-אוף אמיתי) — **מציעים וממתינים לאישור**, לעולם לא מריצים ביוזמה.

## זרימת מודול

לכל מודול שלושה פרומפטים ב-`docs/guides/modules/module_NN_*.md` §⑥, ולכל אחד סקיל תואם שנטען
לפי ניסוח-הטריגר: **"בוא נאפיין את מודול N" / "פתח דיסקברי למודול N"** → `module-discovery` *(**קודם לבלופרינט** — מפיק את `docs/specs/module_NN_*/`; נוסף 13/08/2026 והשורה הזו המשיכה למנות שלושה סקילי-מודול — תוקן ב-`regin-docs-sync` 26/08/2026)* · **"פתח מודול N"** → `module-blueprint` · **"תמשיך לבנות"** → `module-build` ·
**"סגור את המודול"** → `module-close`. ‏+`section7-rulings` ("בוא נסגור שאלות פתוחות") ·
`post-merge` ("מיזגתי") · `quality-audit` ("עשה סקירת קוד") · `skill-scan` ("תסרוק לי את X") ·
`advisor` ("אתה היועץ שלי" · "על מה לעבוד" · "תבקר את זה" — ייעוץ לישי בלבד, לא בונה).
**הרשימה המלאה והמעודכנת — `docs/toolbox.md`.** *(שם, ולא כאן: רשימה קשיחה בשני מקומות נסחפת,
וזה כבר קרה — `skill-scan` נוצר 04/08 ולא הופיע באף אחד מהם עד 05/08.)*

## סוף סשן

מעדכנים — לפי הסדר — **מדריך-המיקרו הפעיל** → `docs/CLAUDE_CODE_LOG.md` → `STATUS.md`.
**ה-Stop hook אוכף את זה מכנית** ויסביר בדיוק מה חסר; אין צורך לזכור את הפרטים.
**אורך רשומת-היומן נקבע במדיניות שבראש `CLAUDE_CODE_LOG.md` עצמו** — היא המפורטת והמעודכנת,
והיא זו שנקראת ברגע הכתיבה. *(עד 05/08 עמדה כאן "3–8 שורות" — סתירה לנוסח שביומן, ו"שורות" מעולם
לא הוגדר. הוסר; הבעלות ליומן.)* העיקרון שנשאר: **מה השתנה · למה · מה נשבר** — הנרטיב המלא ב-`git log`.

**שאלה-חוזרת ⇒ מתקנים את מסמך-המקור** (המדריך/הטמפלט/הקובץ הזה), לא פותחים FAQ נפרד.

</div>
