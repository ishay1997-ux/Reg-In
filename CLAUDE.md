<div dir="rtl">

# CLAUDE.md — REG-IN

> **מה הקובץ הזה מכיל:** רק **מוקשים** — מה שלא ניתן להסיק מקריאת הקוד או עץ הקבצים — ו**שערים** —
> מה שאסור לעשות לבד. כל השאר חי בקובץ שנטען בדיוק כשהוא נחוץ. **הסגנון והתקשורת מול ישי:
> `~/.claude/CLAUDE.md`** (גלובלי, נטען בכל סשן) — לא משוכפל כאן.
> **כלל שהוסר מכאן עדיין בתוקף** — הנוסח המלא האחרון לפני כל גיזום: `docs/archive/CLAUDE_full_<תאריך>.md`
> (האחרון: `2026-09-10`). 🔴 **לפני שמוסיפים פסקה לקובץ הזה: תקרית ראשונה = הערה מתוארכת במקום
> שנטען בזמן הנכון; כלל נולד בתקרית השנייה** (`_shared/failure-modes.md` 🅳). *(נגזם ב-28/07/2026 ל-10 אלף
> תווים; ב-10/09/2026 עמד על 35 אלף — פי שלושה בשישה שבועות. למדוד: `git show <commit>:CLAUDE.md | wc -m`.)*

## מה זה REG-IN

מערכת ניהול עסקית מודולרית לחברת אירועים/כנסים — 13 מודולים (0–12): לקוחות · הצעות מחיר · דיילות
+ Smart Match · לוגיסטיקה · פרויקטים · כספים · דו"חות.
**מפתח יחיד: ישי** (מ-22/07/2026; עמית פרש — אזכוריו ביומנים מתוארכים = היסטוריה קפואה. "עמית
מילר" שבדאטת-הבדיקה הוא לקוח, לא המפתח).
⏱️ **דדליינים: 15/10 כנס-הסיום** *(היעד: 100%)* · **20/10** יעד-עצמי · **24/10** מסירת-הדוח למנחה.
**מ11 (דו"חות) הוא מודול-הראווה** — המנחה: זה החלק שהשופטים הכי אוהבים. הלוח המלא + כלל-החיץ: `docs/guides/00_roadmap.md` §3.
**מוגש: האפליקציה בלבד** — התיעוד הוא כלי-עבודה פנימי, לא תוצר.

🎓 **התוצר: הצגה בת שעה בכנס + מענה לשאלות — פרויקט אקדמי.** החברה אמיתית, **אך אין לקוח בקצה
ואין משתמשים אמיתיים**; המבחן הוא **קוהרנטיות, לא עומס** — מסך שלא מתחבר לסיפור שובר את השעה,
ואף בדיקה אוטומטית לא תתפוס זאת. שלושת מסנני-ההצעות **וארבעת קריטריוני-השיפוט בכנס**: `docs/PROJECT_MASTER.md §1`.
🎯 **והקריטריונים במילותיו (28/08/2026): ידידותית · נעימה למשתמש · פונקציונלית · בקיאים בחומר.**
🔴 **הרביעי אינו על המערכת אלא על ישי** — הוא נמדד בחדר כשישאלו *"למה בחרת ככה?"*, **וזהו
הקריטריון היחיד שאפשר להכשיל בלי לכתוב שורת-קוד:** החלטה נכונה בלי נימוק-במשפט-אחד שישי יכול
לומר בקול היא כישלון בו. ⚠️ **ו"יפה" אינו ברשימה** — ליטוש מעבר לשפה שהוכרעה אינו קונה נקודות.
*(למה זה כאן: סשן שלא קרא זאת הסיק "מוצר ללקוח אמיתי" ותכנן Enterprise.)*

**Stack:** Vite 8 + React 19 · **JavaScript (לא TypeScript)** · Tailwind 4 + shadcn/ui + Radix ·
Supabase (Auth + Postgres 17) · react-router-dom 7 · עברית RTL מלאה.

## 🧨 עובדות סביבה (מוקשים — ניחוש כאן שובר דברים בשקט)

- **ייבוא לקוח Supabase: `@/supabaseClient`** — הקובץ יושב ב-`src/supabaseClient.js`.
  ⚠️ **לא `@/lib/supabaseClient`** (הנתיב שכל אחד מנחש — ואינו קיים).
- alias `@/` → `src/` · שרת פיתוח `npm run dev` על פורט **5173**.
- **חותמת-זמן — לא לערבב בין הכלים:** ב-PowerShell `Get-Date`; ב-Bash `date +"%d/%m/%Y %H:%M"`.
  פורמט: **`DD/MM/YYYY HH:MM`**, מהשעון — לא מנוחש. ההוק `block-shell-dialect-mixup.sh` חוסם cmdlet של
  PowerShell בכלי Bash — ⚠️ **וגם *חיפוש* אחרי שם ה-cmdlet ייחסם** (ההוק קורא `|` שלפניו כמיקום-פועל).
  לחפש דרך הכלים `Read`/`Grep`, לא דרך `grep` בכלי Bash.
- **ספירת שורות — `(Get-Content -LiteralPath X).Count`. ⚠️ לא `Measure-Object -Line`:** הוא
  **מדלג בשקט על שורות ריקות** ומחזיר מספר קטן יותר בלי להתלונן. ‏`git show HEAD:file | Measure-Object`
  הוא אימות-צולב טוב. ⚠️ ו-`wc -l` מחזיר **אחד פחות** כשהקובץ אינו מסתיים בשורה חדשה — פער-של-1, לא באג.
- 🔴 **כתיבת קובץ מפייתון על Windows — חובה `newline` מפורש, אחרת השער נשבר בשקט.**
  ‏`io.open(p,'w',encoding='utf-8')` **מתרגם כל סוף-שורה ל-CRLF**, והריפו כולו LF
  (`.gitattributes`: `* text=auto eol=lf`). ⇒ ‏`npm run format:check` נופל **ומפיל את `gate` כולו**.
  ⚠️ **ולמה זה מוקש: `git status` נקי ו-`git diff` ריק** (‏`core.autocrlf=true` מנרמל לפני ההשוואה) —
  **אין שום סימן** עד שהשער נופל, וגם אז ההודעה מדברת על "בעיית-סגנון". הזיהום הוא **רק בעץ-העבודה**,
  ולכן CI נשאר ירוק. **המדידה:** לספור בתי-CR בקובץ (בכלי Bash: `tr -cd` עם התו CR לתוך `wc -c`) ⇒ חייב 0.
  **התיקון:** ‏`sed -i` שמוחק CR בסוף כל שורה. 🚫 **ולא `prettier --write .`** — סורק את כל הריפו
  ועלול לגעת בקבצים של סשן אחר. *(27/08/2026: 11 קבצים נדבקו כך בשער-פזה-1 של מ8.)*
- 🔄 **סיבוב סשן — לפני 60% מהחלון:** סיים את הפריט הנוכחי · קמט · **עצור.** ‏🚫 **ולא קומפקט** —
  הוא נורה מאוחר מדי בהגדרה, באזור שבו סשנים מייצרים עבודה שתיעשה מחדש. **סשן טרי + מבחן-קבלה**
  *(הסשן הנכנס עונה על שאלות שהתשובות על הדיסק, לפני כל פעולה)*. המדריך, ואל תגזור אותו מחדש:
  `~/.claude/references/session-handoff-guide.md`. ⚠️ *סשן אינו רואה את אחוז החלון שלו — זו הנחיה,
  לא שער; נמדד 10/09/2026: 12 מ-183 שיחות הגיעו לקומפקט בכל זאת.*
- 🔴 **`npm run gate` אינו מה ש-CI מריץ.** ‏`.github/workflows/ci.yml` מריץ את השלבים בנפרד
  ואינו קורא ל-`gate` כלל ⇒ **`check:docs-structure` אינו רץ בשום מקום אוטומטי**, ו-`check:context` רץ
  רק כהוק-פתיחת-סשן **פתוח-לכישלון** — הקשר, לא שער. ההרכב המדויק: `package.json` §scripts מול `ci.yml`.
- 🔴 **ומעבר לכך — `npm run gate` עיוור לקובץ-בדיקות שנוגע בלקוח Supabase האמיתי בזמן-הייבוא.**
  ‏`.env.local` המקומי מספק `VITE_SUPABASE_URL`, ולרץ של GitHub אין קובץ כזה ⇒ הקובץ קורס שם
  ב-`Error: supabaseUrl is required` **לפני שבדיקה אחת רצה**, והשער המקומי אינו רואה זאת כלל —
  שני סשנים יקראו "ירוק" על עץ ש-CI מפיל. **הסימן:** מספר הבדיקות המקומי גדול ממספר ה-CI.
  **השחזור המקומי:** `VITE_SUPABASE_URL= VITE_SUPABASE_ANON_KEY= npx vitest run`. **התיקון:**
  `vi.mock('@/supabaseClient')` כמו בשאר קובצי-הבדיקות.
  🚫 **והשער עצמו לא שונה בכוונה** — שינוי כלי-המדידה הוא הכרעת-ישי, לא תיקון-לילה.
- 🔴 **`PROJECT_MASTER_sec7.md` גדל ראש-ואז-זנב: המשפט הפותח של כל פריט הוא המצב *לפני* ההכרעה**,
  וההכרעה מתווספת **אחריו** כ-`✅`. ⇒ **ציטוט של פתיחת סעיף הוא ציטוט של השאלה, לא של התשובה** —
  חובה לקרוא עד סוף הפריט. *(03/09/2026: §7.37 צוטט כ"פתוח" לפי משפטו הראשון; ה-`✅` ישב 14 מילים אחריו.)*
- ⚠️ **`npm run test:e2e` מדלג בשתיקה על בדיקות-העשן** — הסקריפט הוא
  `playwright test --grep-invert בדיקת-עשן`. ‏**ו-E2E אינו רץ ב-CI כלל.** בדיקות-העשן הן
  `npm run smoke` (מריץ את `e2e/smoke.spec.js`) — גם היא אינה ב-`gate` ואינה ב-CI.
- 🔴 **סודות — לעולם לא לקמט, ולעולם לא להדפיס בצ'אט או במדריך.** ⚠️ **ההגנה האוטומטית חלקית:**
  `gitleaks` רץ ב-CI רק על PR/push ל-`dev`/`main`; ב-pre-commit רצים eslint+prettier בלבד, ו-`gitleaks`
  **אינו מותקן מקומית** ⇒ סוד שנדחף לענף אישי כבר ב-GitHub ונשאר בהיסטוריה. הכלל הוא משמעת, לא הוק.
- 🔴 **שיחה כותבת אחת בכל רגע — ובפועל רצות כמה.** שני סשנים שכותבים לאותו checkout מתנגשים: האינדקס
  משותף, `checkout`/`stash` של אחד מחליף את העץ מתחת לשני באמצע עריכה, וקימוט של אחד סוחף את השורות של
  השני. **עבודה שאינה על הענף הפעיל ⇒ worktree נפרד** (`git worktree add ../Reg-In.worktrees/<שם> <ענף>`),
  לא `checkout` בעץ המשותף. **לא בטוח אם מישהו כותב עכשיו? שאל את ישי לפני שאתה נוגע בקובץ משותף.**
  ההוק `session-start-context.sh` מתריע **רק אם סשן אחר נגע בקבצים ב-120 הדקות האחרונות**, והוא
  פתוח-לכישלון — מזהה בראיה ואינו תחליף לכלל. הפרוטוקול המלא: `_shared/parallel-sessions.md`.
  *(10/09/2026 02:48–03:00: שלושה סשנים על checkout אחד; הענף הוחלף שלוש פעמים בעשר דקות — `git reflog`.)*
- **קבצים חדשים — שמות באנגלית בלבד** (עברית בשמות קבצים נשברת ב-Git/Windows). תוכן עברי בתוך
  `<div dir="rtl">`.
- **חלוקת שפות:** `docs/guides/` + `STATUS.md` בעברית (לבני אדם) · `docs/micro_guides/`,
  `.claude/skills/**`, `docs/CLAUDE_CODE_LOG.md` באנגלית (ל-Claude; עברית רק כדאטה — שמות מודולים,
  מחרוזות UI, ציטוטי §7). **דיווחים בצ'אט לישי — תמיד בעברית.**

## 📂 הפרוטוקולים המלאים — נטענים לפי מה שנוגעים בו

| נוגע ב… | הקובץ שנטען אוטומטית ומכיל את הפרוטוקול המלא |
|---|---|
| **מיגרציה / DB** | **`supabase/migrations/CLAUDE.md`** — שער ההסבר-והמילה, `apply_migration`, ‏`schema.sql`, ‏`db_roadmap` |
| קוד (`src/`) | `src/CLAUDE.md` — מודל-אבטחה, SSOT ללוגיקה, מוקשי-קוד |
| תיעוד (`docs/`) | `docs/CLAUDE.md` — מקורות-אמת, §7, מראות 🔗, מקרא-אימוג'י |

## היררכיית מקורות אמת (בסתירה — הגבוה קובע)

> ⚠️ **המספור לא שונה** *(ציטוטים בריפו תלויים בו)* — **אבל דרגה 3 יצאה מ"קובע" ב-03/09/2026.**

1. **`docs/schema.sql`** — סכמת ה-DB (snapshot; מקור-אמת לשינויים = `supabase/migrations/`).
2. **האפיון המאושר של המודול** — `docs/specs/module_NN_*/` *(תוצרי שיחת ה-Discovery: תהליכים · מסכים · מוקאפים מאושרים)*.
   **גובר על C5/C6 בכל מה שהוא מכסה בפועל — פר-פריט, לא פר-תיקייה.**
   🔴 **ומה שאינו מכוסה שם — אינו מוכרע: הוא שאלה לישי, לא ברירת-מחדל מ-C5.**
   **למה C5 בכל זאת נשאר ברשימה:** את `docs/specs/` קלוד עצמו כתב ⇒ C5 הוא **עוגן-ההצלבה הבלתי-תלוי
   היחיד** — אסור לערוך אותו, ובדיקת-הכיסוי מולו נשארת.
3. **האפיון הקפוא — 🔴 רשימת-כיסוי, לא סמכות מוצרית. אינו קובע דבר.**
   `docs/reference_spec/C5_clean_transcript.md` (תהליכים/מסכים) + `C6_clean_transcript.md` (טבלאות/ERD).
   **במילותיו של ישי (03/09/2026): *"הוא רק השראה. לא קובע כלום, הוא מיושן."*** ⇒ **אסור להסיק ממנו
   מה יהיה על מסך; מותר — ונדרש — לשאול בעזרתו "מה שכחנו".** המשפט הישן *"מה שאינו מכוסה ב-`specs` —
   C5/C6 קובעים"* **בוטל**: אין מצב שבו C5 מכריע במקום ישי רק כי לא הספיקו לשאול אותו.
   *(העוגן: ארבעת מדדי מסך-הבית של מ7 הועתקו מ-`C5:210` מילה-במילה ואיש לא שאל אם הם הנכונים. ישי שאל.)*
   🧊 **קפואים — לא עורכים לעולם** (ה-hook חוסם); סטייה נרשמת בתיעוד החי עם הערת "סטייה מ-5.x".
4. **המוקאפים** (`docs/mockups/`) — רפרנס ויזואלי בלבד, **וחלק מפרטיהם שגויים**.
5. **המדריכים** (`docs/guides/`, `docs/micro_guides/`) — מתכון לסדר העבודה. הכי נמוך.

## כללי הברזל — אינדקס

> **המספור קפוא ולא ממוחזר לעולם** (עשרות ציטוטים בריפו תלויים בו).
> **כלל שאינו מנוסח כאן עדיין בתוקף במלואו** — הוא חי במקום שנטען בזמן הנכון, או שמנגנון אוכף
> אותו טוב יותר מטקסט. **למה:** הוראות חופפות עולות זמן-חשיבה בכל תור — צריך ליישב ביניהן. הכרעת-ישי 28/07.

**1. שאל כשלא חד-משמעי — ההכרעה תמיד של ישי.**
שאלות פתוחות מוכרעות **רק איתו** (הרשימה: `PROJECT_MASTER §7` = הקובץ `docs/PROJECT_MASTER_sec7.md`;
‏§6 נשאר ב-`PROJECT_MASTER.md`). אותו דבר לכל החלטת מוצר או עיצוב.
🔴 **רעיון שלו אינו הכרעה שלו.** הכרעה מצוטטת **מילה-במילה** ומחייבת (`_shared/writing-prompts.md` כלל 2);
*"אולי כדאי ש…"* / *"חשבתי ש…"* / *"מעלה כרעיון"* — **נרשם כרעיון, נבדק כמו כל אפשרות** (כולל מול
הכרעה קודמת שלו), וחוזר אליו עם המלצה. תקף בכל שיחה, לא רק באפיון. *(נכשל ב-write-back: "אחראית
משמרת" נרשם ב-`db_roadmap.md` כ"הכרעת-ישי" כשמילותיו היו "מעלה כרעיון… מתייעץ איתך".)*
**טריאז' בקשת-שינוי — ורדיקט גלוי** לכל בקשה לא-טריוויאלית: `בנה-עכשיו` / `דחה-ל-X` / `לא-נדרש-כי-Y`
עם נימוק, כדי שישי יוכל לתפוס דילוג. מודול בפיגור מול הדדליין ⇒ ברירת-המחדל `דחה-ל-אחרי-הדדליין`,
ו**דוחים מודולים שלמים, לא מקצצים תוכן**.

🔑 **עוגן לפני שמביאים** *(הכרעת-ישי 08/08/2026)* — ממצא, שאלה או הצעה מגיעים אליו רק עם עוגן:
תקדים במערכת · מקום בקובץ *(עוגן-גריפ, לא מספר-שורה)* · מדידה שרצה · מסך שהוא ראה · הכרעה שלו.
- **יש עוגן ⇒ הכרע לבד, הצג את העוגן, והוא יכול לעקוף.**
- **ביצוע-טכני בלי משמעות מוצרית** (שם משתנה · מבנה שאילתה · ניסוח קוד · תיקון לוג) **— שלך, בלי שאלה
  ובלי עוגן.** עוגן נדרש למה שמישהו רואה או מרגיש.
- **אין עוגן פנימי ⇒ הבא עוגן חיצוני לפני שאתה חוזר אליו:** מה מקובל בעולם *(מקור מצוטט)* ומה הגיוני
  עסקית אצלנו — **עם בדיקת-ההתאמה (4 שאלות) ומחיר-המימוש, `_shared/discipline.md`.** ההכרעה נשארת שלו
  ומגיעה *עם* ההמלצה, לא במקומה.
- **ורק כשגם זה ריק ⇒ אמור "אין עוגן"**, מה היה נדרש כדי שיהיה, ומה בכל זאת ידוע. 🚫 **לעולם לא להמציא
  ולא להכריע לפי "מה שנשמע לי הגיוני"** — אבל **דעה מותרת ונדרשת, מסומנת `הנחתי`** (`_shared/discipline.md`).
  האיסור הוא להכריע לפיה, לא לומר אותה.
- **שאלה שההכרעה בה שלו — עדיין נשאלת**, אבל **לא בידיים ריקות:** מעוכלת, עם ראיה, עם המלצה מנומקת,
  ומסומן מה **עוגן** ומה **המלצה**. *"אני לא יודע, תחליט אתה"* אסור.
- 🔴 **עוגן שהוא אינו יכול לבדוק בעצמו אינו עוגן.** ישי אינו קורא קוד ⇒ תרגם למסך · מוקאפ · הכרעה שלו ·
  תרחיש עם שם ומספר. `מקום בקובץ` ו`מדידה שרצה` מספיקים **לך** כדי להכריע; **לו** הם חסרי-ערך עד שתתרגם.
  **המבחן: אם יאשר בלי להבין — סיפקת ניסוח משכנע, לא עוגן.**
- ➕ **הצעה טובה שלא ביקש היא כשל** — במילותיו: *"אין סיבה להוסיף עבודה גם אם זה שיפור נחמד"*. לפני
  שאתה מציע: **הוא פותר בעיה שנצפתה, או רק נראה נכון?**

**ארבעת המבחנים, במילותיו — לכל דבר שאתה מביא, כולל הכרעת-מוצר:** *"מה שהכי הגיוני וישים במערכת
ומתחבר היטב עם כל התהליכים הלוגיקה ושאר המסכים… שמהנדס יסתכל על הקוד ויתרשם לטובה, ושיהיה לי מה
הסברים טובים אם ישאלו אותי בכנס למה בחרתי ככה."*
🔴 **שורת-הווידוא — נכתבת בהודעה, לפני ההצעה, ארבע מילים:** `רץ-על: <הדבר הקיים/ב-db_roadmap>` ·
`תקדים: <מי הכריע דומה>` · `דפוס: קיים / חריג-מוצדק` · `בכנס: <הסיבה במשפט>`. חסר אחד ⇒ זו משאלה /
סתירה לפתור / חריג שלא הצדיק עצמו / **העתקה ולא הכרעה** (*"ככה כתוב ב-C5/במוקאפ"* אינו נימוק).
*(הוספה 10/09/2026: הכלל עמד כאן חודש ונמצא בפלט של 5 מ-183 שיחות — שורה נדרשת היא הדרך היחידה
שישי יראה דילוג.)*

🔴 **סתירה אינה שאלה — היא סימן שלא קראת מספיק. שלושה מקרים, ורק השלישי הוא שאלה:**
‏① **הוכרע במקום אחר** ⇒ קרא את המסמך **במלואו**, לא חצי ולא grep ממוקד.
‏② **הוא הכריע פעמיים, אחרת** — קורה, כי הוא לא זוכר ⇒ הבא את **שתי** ההכרעות מצוטטות ומתוארכות ושאל
איזו עומדת. 🚫 **לעולם לא לציית לזו שמצאת ראשונה** *(§7.14 נשא משקולות שהוחלפו 22 יום אחר-כך, ואיש לא עדכן)*.
‏③ **מעולם לא הוכרע** ⇒ שאלה, עם המלצה.
🔴 **ולפני שאתה שולח — אינך עושה אחד מארבעת אלה** *(לא טקסונומיה סגורה)*:
**ממצא-בלי-עוגן** · **שאלה-שיש-לי-עליה-תשובה** · **הצעה-שלא-ביקש** · **עוגן-שאתה-לא-יכול-לבדוק**.
*(אלה גם המילים שישי תופס בהן.)*
*(תקציר-החוויה 🗣️ וההמתנה-לאישור-לפני-קוד: `.claude/skills/module-build` — רלוונטי רק בתור-בנייה.)*

**8. עיצוב — לא ממציאים.** שפת-העיצוב ב-`PROJECT_MASTER §4`: פרימרי teal `#0D9488` · רקע `#F8FAFC` ·
סרגל-צד ימני. **אין לשנות צבעים או פריסה בלי אישור ישי.**

**10. Git — שער קשיח.** לעולם לא לדחוף ל-`main`/`dev` ישירות ולא `--force`. עבודה רק על ענף אישי
`ishay/<נושא>` (מודול: `ishay/module-N-…`; עבודה שאינה מודול: `ishay/<שם-קצר-באנגלית>`).
✏️ **מיזוג ל-`dev` — הכרעת-ישי 10/09/2026, במילותיו "מזג." ואז "מחק את הכלל הזה מיד":** מותר
כשיש הרשאה **מפורשת, בזמן-אמת, בגוף-ראשון, בצ'אט של אותה שיחה** ("מזג" / "אני מאשר למזג") — לא רמז
ולא הנחיה כללית מראש על עבודה עתידית. ⚠️ **חל רק על מיזוג ל-`dev`.** ‏`--force`, דחיפה ישירה ל-`main`
בלי PR, **וארכוב/סגירת-סשן נשארים אסורים במלואם גם בהוראה מפורשת** — `archive_session` נגיש מכל
סשן ואף הוק אינו חוסם אותו, ושלושה סשנים אורכבו בטעות ב-01/08/2026 כשהוראה נקראה כהרשאה.
🔴 **קומיט תמיד בפאת'ספק מפורש — `git commit -- <נתיבים>`, לעולם לא `git add -A`:** האינדקס משותף,
וקימוט של קובץ אחד סוחף את השורות התלויות של סשן אחר *(קרה: 9 קבצים זרים כולל מיגרציה)*.
**מיזוג-באמצע-סשן:** אחרי כל push, לפני כל commit נוסף — `git fetch origin` +
`git merge-base --is-ancestor HEAD origin/dev`. אם כבר מוזג — **לא לערום על ענף-מת**; להודיע ולהציע
ענף חדש מ-`dev` טרי. 🔴 **והסייג:** הבדיקה **מצליחה גם על ענף טרי בלי קומיטים**. **המבחן שמפריד:**
`git log origin/dev..HEAD --oneline` — **ריק ⇒ ענף טרי, פשוט תתחיל לעבוד** · **לא-ריק ⇒ באמת מוזג/מפגר.**

**12. DB/RLS קודם, UI אחר-כך.** סדר-הבנייה בתוך מודול. *(נקודות-העצירה 🔻 עצמן חיות במדריך-המיקרו.)*

**17. פרומפט מסירה 🧩.** כל צעד שקלוד לא יכול לבצע (PR ב-GitHub, Supabase Studio, OAuth, סודות) מקבל
**שני** דברים: הוראות בעברית צעד-צעד **+** בלוק "🧩 פרומפט לקלוד בדפדפן" — עצמאי, בלי להסתמך על השיחה,
בלי סודות. *(ישי לא מריץ פקודות — זה לא נימוס, זו הדרך היחידה שהצעד יתבצע.)*

🔴 **כותב פרומפט לסשן אחר? קרא `.claude/skills/_shared/writing-prompts.md` לפני.** מעבר-כוונה ·
אימות-עובדות באותו יום · מוקש-על אחד עם דרישת-ספירה · המלצת-מודל · ‏2–3 שורות עברית לישי לפני הבלוק.
*(כאן ולא רק שם: הקובץ נטען רק דרך סקיל, ואין סקיל שנדלק על "תכתוב פרומפט".)*

**14. SSOT ללוגיקה עסקית** → `src/CLAUDE.md` · **9. מודל אבטחה** → `module-blueprint/template.md`
(הצהרת-ההישענות) + `module-close/template.md` (רגרסיית-אבטחה); ‏`src/CLAUDE.md` נושא את **מוקשי**-ההרשאות,
לא את הכלל · **5. שינויי DB** → `supabase/migrations/CLAUDE.md` · **13. פרוטוקול אדוות** → `docs/CLAUDE.md`.

### הכללים שנגרעו מכאן — ואיפה הם חיים עכשיו

| # | הכלל | היכן הוא בתוקף |
|---|---|---|
| 2 | Plan Mode בפתיחה/סגירה/§7 | `docs/CLAUDE.md` · הסקילים |
| 3 | הערות "why-first" בעברית | הקוד עצמו (מוער בעברית בעקביות) · `src/CLAUDE.md` |
| 4 | לצטט קובץ+שורה, לא מהזיכרון | `~/.claude/CLAUDE.md` · `_shared/discipline.md` (יעדי-האימות פר-סוג-טענה) |
| 6 | לא לקמט סודות | **"עובדות סביבה" למעלה** (הכלל המלא, כולל חצי-ההדפסה וגבול `gitleaks`) |
| 7 | לוודא בפועל | `~/.claude/CLAUDE.md`: *"actually run the thing"* |
| 11 | שמות קבצים באנגלית | "עובדות סביבה" למעלה |
| 15 | מדריך-מיקרו חי + חוב 🚧 | **ה-Stop hook חוסם ומסביר בדיוק מה חסר**: `.claude/hooks/check-docs-updated.sh` · `module-build` |
| 16 | שיחה כותבת אחת בכל רגע | **"עובדות סביבה" למעלה** · `_shared/parallel-sessions.md` |

## 🧰 ארגז הכלים וזרימת המודול

`module-discovery` · `module-blueprint` · `module-build` · `module-close` הם **זרימת-העבודה הרגילה** —
נטענים לפי ניסוח-הטריגר בלי לשאול: **"בוא נאפיין את מודול N" / "פתח דיסקברי למודול N"** → `module-discovery`
*(קודם לבלופרינט — מפיק את `docs/specs/module_NN_*/`)* · **"פתח מודול N"** → `module-blueprint` ·
**"תמשיך לבנות"** → `module-build` · **"סגור את המודול"** → `module-close`. לכל מודול שלושה פרומפטים
ב-`docs/guides/modules/module_NN_*.md` §⑥.
**כל סקיל אחר: הצע בשורה אחת ("זה נראה מקרה ל-X — להריץ?") והמתן.** `llm-council` — לעולם לא ביוזמה.
משימה קטנה לא מצדיקה סקיל; אם התשובה ברורה — פשוט לענות.
**מה קיים, מה כבוי ומתי להדליק — [`docs/toolbox.md`](docs/toolbox.md), לקרוא לפני שמציעים כלי.**
ביטוי-הטריגר של כל סקיל אחר — ב-`description` של ה-`SKILL.md` שלו. *(רשימה קשיחה כאן נסחפה כבר פעם.)*

## סוף סשן

מעדכנים — לפי הסדר — **מדריך-המיקרו הפעיל** → `docs/CLAUDE_CODE_LOG.md` → `STATUS.md`.
**ה-Stop hook אוכף את זה מכנית** ויסביר בדיוק מה חסר; אין צורך לזכור את הפרטים.
**אורך רשומת-היומן נקבע במדיניות שבראש `CLAUDE_CODE_LOG.md` עצמו.** העיקרון: **מה השתנה · למה · מה
נשבר** — הנרטיב המלא ב-`git log`.

**שאלה שכבר נענתה — ישי אומר "כבר עניתי על זה", או שמצאת את התשובה ביומן/§7 אחרי ששאלת ⇒ מתקנים את
מסמך-המקור** (המדריך/הטמפלט/הקובץ הזה), לא פותחים FAQ נפרד.

</div>
