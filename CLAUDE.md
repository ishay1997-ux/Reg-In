<div dir="rtl">

# CLAUDE.md — כללי הברזל של REG-IN

> קובץ זה נטען אוטומטית בכל סשן Claude Code בריפו הזה. **קרא את `STATUS.md` מיד אחריו** — הוא לוח המצב היחיד (איפה עומדים, מה הצעד הבא).

## מה זה REG-IN
מערכת ניהול עסקית מודולרית לחברת אירועים/כנסים (לקוחות, הצעות מחיר, דיילות + Smart Match, לוגיסטיקה, פרויקטים, כספים, דו"חות). **מפתח יחיד: ישי** (בעל הריפו; כל 13 המודולים 0–12). *(עד 07/2026 היו שני מפתחים — עמית פרש 22/07/2026 ומודוליו 2,3,7,8,11 עברו לישי; אזכוריו ביומנים המתוארכים = היסטוריה קפואה, לא מצב-נוכחי. "עמית מילר" שמופיע בדאטת-הבדיקה הוא לקוח/משתמש, לא המפתח.)*
**דדליין-הגשה: 19/09/2026** — לוח-הדדליינים המלא לכל מודול ב-`docs/guides/00_roadmap.md` §3.

**Stack:** Vite 8 + React 19 · JavaScript (לא TypeScript) · Tailwind 4 + shadcn/ui + Radix · Supabase (Auth + Postgres 17) · עברית RTL מלאה.
**עובדות סביבה:** alias `@/` → `src/` · לקוח Supabase: `src/supabaseClient.js` → מיובא כ-`@/supabaseClient` (⚠️ לא `@/lib/`) · פרויקט Supabase: `Reg-In` (ref `yfeovxppnfoafmfbdfvh`, אזור `eu-west-3`) · שרת פיתוח: `npm run dev` (פורט 5173) · Windows + PowerShell (⚠️ לחותמת-זמן: ב-PowerShell `Get-Date`, ב-Bash `date +"%d/%m/%Y %H:%M"` — לא לערבב; `Get-Date` אינו קיים בכלי Bash ויחזיר `command not found`) · **Supabase MCP** מוגדר ב-`.mcp.json` (HTTP+OAuth) — לאימות מצב DB (ספירות, RLS, advisors) בקריאה, **וגם להחלת מיגרציות** דרך `apply_migration` (אחרי שער ה-typed-echo — ר' פרוטוקול-DB). ⚠️ **בלי `execute_sql` לשינויי-סכמה אד-הוק** — שינויי סכמה **תמיד** דרך קובץ-מיגרציה בעל-שם. אם ה-MCP לא זמין בסשן — לציין זאת במפורש בדיווח, לא לדלג בשקט.

## היררכיית מקורות אמת (בסתירה — הגבוה קובע)
1. **`docs/schema.sql`** — סכמת ה-DB (snapshot; מקור-אמת לשינויים = `supabase/migrations/`).
2. **האפיון המאושר, פרקים 5–6** (`docs/reference_spec/C5_clean_transcript.md` + `C6_clean_transcript.md`) — הלוגיקה העסקית והמסכים. אלה **תעתיקים מנוקים-מכנית** (שחזור-מספור C5 · הזזת-טבלה ב-C6 · עוגני-ניווט · הערות-§9) שהחליפו את יצואי-ה-Word המקוריים (`C5_Processes_And_Screens.md`/`C6_DB_Tables.md`, נמחקו 09/07/2026 אחרי סוללת-אימות מלאה + PASS-ישי). **קבצים קפואים — לא עורכים לעולם**; סטייה מהאפיון נרשמת בתיעוד החי בלבד (PROJECT_MASTER, מדריך המודול, CLAUDE_CODE_LOG) עם הערת "סטייה מ-5.x". **ציטוטי `C5:NNN`/`C6:NNN`** נפתרים דרך הערות `<!-- מקור C5/C6 L… -->` שבראש כל סעיף בתעתיק (הסעיף שערך-ה-L שלו הגדול-ביותר ≤ NNN).
3. **המוקאפים** (`docs/mockups/`) — רפרנס ויזואלי בלבד.
4. **המדריכים** (`docs/guides/`, `docs/micro_guides/`) — מתכון לסדר העבודה. הכי נמוך.

## מפת המסמכים — מה לקרוא ומתי
| קובץ | תפקיד |
|---|---|
| `STATUS.md` | **לוח מצב יחיד** — סטטוס מודולים + הצעד הנוכחי. לקרוא בכל סשן |
| `README.md` | דף הבית: הפרומפטים, מפת התיעוד |
| `docs/guides/00_roadmap.md` | מפת הדרכים: המודולים, התלויות, אבני הדרך, **לוח-הדדליינים ל-19/09/2026**, הלולאה לכל מודול |
| `docs/guides/modules/` | מדריך שלב לכל מודול (`module_NN_name.md`) |
| `docs/guides/reference/` | מדריכי-רפרנס קבועים: ‏`git_workflow` · `working_with_claude` · `claude_code_setup` · `claude_routines_setup` · `install_tools` |
| `docs/PROJECT_MASTER.md` | האפיון המסונתז + **§7 = רישום השאלות הפתוחות הממוספר (יחיד!)** |
| `docs/reference_spec/C5_clean_transcript.md` · `C6_clean_transcript.md` | 🧊 **האפיון המאושר (פרקים 5–6) — דרגה 2, קפוא** (תעתיקים מנוקים; החליפו את יצואי-ה-Word המקוריים). כל אחד פותח במפת-מודולים ניווטית. C5 = תהליכים/מסכים · C6 = טבלאות-DB/DFD/ERD |
| `docs/db_roadmap.md` | 🗺️ **מפת שינויי-ה-DB העתידיים** (אגרגטור-ביצוע; ההחלטות רק ב-§7) — לקרוא לפני כל תכנון מיגרציה ובכל בלופרינט |
| `docs/CLAUDE_CODE_LOG.md` | יומן הסשנים של Claude (נרטיב, "למה", tech-debt) |
| `docs/CHANGELOG.md` | ⛔ **הוקפא 23/07/2026 — ארכיון בלבד.** ציר-הזמן החי: `git log` + CLAUDE_CODE_LOG · שינויי-DB: `db_roadmap`+migrations · חובות: `PROJECT_MASTER §6` |
| `docs/claude_routines.md` | ההגדרות הקנוניות של 4 הרוטינות + פרוטוקול עדכון |
| `docs/architecture_and_qa_roadmap.md` | סטנדרט הנדסי + Definition of Done |

## כללי ברזל (Rules of Engagement)

> 🗂️ **מפת-הכללים (המספור קבוע — עשרות ציטוטים תלויים בו):** תהליך-עבודה 1·2·12·13·15·16·17 · איכות-קוד 3·4·11·14 · אבטחה 6·9 · ‏git+DB ‏5·10 (+פרוטוקול DB למטה) · עיצוב 8 · אימות 7.
>
> **מקרא-אימוג'י (F7 — הסט קפוא; אין להוסיף חדש בלי אישור-ישי + מחיקת-אחד):** 🔻 עצירה · 🤖 Claude-מאמת-לבד · 👤 עצירת-אדם (אישור/הכרעה/פעולה-ידנית) · 🚧 חוב חוצה-מודולים (§6) · ⏳ החלטה-נדחית · 🕓 חותמת-טריות (רפרנס) · 🔗 מראה מתויגת (§7) · 🧩 פרומפט-מסירה · 🧊 הקפאת-תשתית · 🔮 checkpoint-עתידי · 🗡️ ‏DB-Design-Challenge (טמפלטים). *(‏📣 "יידוע-המפתח-השני" **נגרע** 22/07/2026 בהכרעת-ישי עם פרישת עמית — גריעה, לא הוספה; מופעיו ביומנים המתוארכים נשארים כהיסטוריה.)*

1. **שאל שאלות הבהרה לפני עבודה** כשמשהו לא חד-משמעי — אל תניח. שאלות פתוחות מוכרעות **רק עם ישי** — לעולם לא לבד (הרשימה: PROJECT_MASTER §7). כל הכרעת-§7 מוצגת לישי בסגנון-ההכרעה (רקע · משמעות · אפשרויות · המלצה מנומקת) ונרשמת בשמו (כלל 13).
    **מוקאפ בערבון-מוגבל + תקציר-חוויה לפני בנייה (הידוק 10/07/2026; הודק 11/07/2026 — הכרעות-ישי):** המוקאפים הם רפרנס-ויזואלי בלבד (היררכיית-האמת דרגה 3) ו**חלק מפרטיהם שגויים / אינם מה שהתכוונו**. לכן בזמן הבנייה — בכל מודול — לפני כל יחידת-בנייה קלוד מציג **"תקציר-חוויה" חובה בעברית פשוטה** (מנגנון 🗣️ בטמפלט-הפתיחה): ‏(א) הצהרת-הבנה של הזרימה העסקית כפי שהבין — **מוזמנת-תיקון** (רק ישי יודע איך התהליך באמת עובד) ‏(ב) הוולידציות ‏(ג) תיאור-מסך או מוקאפ ‏(ד) פרט שמקורו **מוקאפ-בלבד או אפיון-שותק** מסומן *"מהמוקאפ / אפיון-שותק — לאישורך"* — **וממתין לאישור מנהל-המוצר לפני שנכתב קוד** (החליף את נוסח ההמשך-בלי-המתנה). אימות נשאר 🤖: קלוד מוודא בעצמו פונקציונלית+ויזואלית עם סקרינשוטים כראיה; שער-👤 מלא רק בסוף-פאזה ובהחלטות-עיצוב. זהו הידוק של כלל-זה (שאל-כשלא-חד-משמעי) ושל היררכיית-המוקאפ, לא מנגנון חדש.
    **טריאז' בקשת-שינוי (הכרעת-ישי 11/07/2026):** על **כל בקשת-שינוי לא-טריוויאלית** (עיצוב · פיצ'ר · התנהגות), **לפני שבונים** קלוד עונה בקול על 3 שאלות ונותן ורדיקט **גלוי**: ‏(1) **אפשרי עכשיו?** הנתונים/הטבלאות/המודולים/התלויות קיימים (למשל דאטת-פרויקטים חסומה עד מ6). ‏(2) **שייך למקום אחר?** צעד עתידי במדריך · מודול עתידי · §6/§7 · משטח-של-מודול-אחר. ‏(3) **נחוץ עכשיו, או מוקדם-מדי?** F1/gold-plating, או מנגנון עתידי שיבנה-מחדש בכל מקרה (למשל שליחת-שרת מ10). ורדיקט לכל בקשה: `בנה-עכשיו` / `דחה-ל-X` / `לא-נדרש-כי-Y` — **המלצה מנומקת; ההכרעה של ישי** (כלל-זה, לא לבד). הפלט-הגלוי הוא האכיפה: אין hook שאוכף חשיבה-ביקורתית, אז הטריאז' **מוצג** כדי שישי יוכל לתפוס דילוג. הרחבה של כלל-זה (שאל-כשלא-חד-משמעי) לממדי-תזמון/נחיצות, לא מנגנון חדש.
    **⏱️ ממד-דדליין (נוסף 22/07/2026, עם קביעת 19/09):** בשאלה (3) לשקול גם את לוח-הזמנים — בקשה שאינה חוסמת ואינה באפיון, כשהמודול הנוכחי בפיגור מול לוח-הדדליינים, מקבלת ורדיקט `דחה-ל-אחרי-19/09` כברירת-מחדל. **מדיניות-החריגה היא דחייה, לא קיצוץ:** מודול שנבנה — נבנה מלא לפי האפיון; אם הזמן נגמר, מודולים שלמים זזים (סדר-הדחייה ומה שלא-נדחה: `00_roadmap.md` §3).
2. **Plan Mode:** לתכנן ולקבל אישור לפני ביצוע; במצב תכנון לא נוגעים בקבצים. **מתי נדרש:** פתיחת מודול (בלופרינט), סגירת מודול, הכרעת §7, וכל סטייה מהותית מהתוכנית. **מתי לא:** בנייה שוטפת לפי מדריך מיקרו מאושר — מדריך המיקרו *הוא* התוכנית המאושרת, ונקודות העצירה 🤖/👤 הן הבקרה; מבצעים ישירות בלי סבב תכנון נוסף. **נוהל תוכניות (הלקח מתיקון-שאבד-פעמיים):** ‏(א) **write-back:** כל הערה/דיוק על תוכנית פעילה (מישי או מסשן אחר) נכתבת **לתוך קובץ-התוכנית** כצעד לפני שממשיכים — הערת-צ'אט שלא הומרה לצעד = לא קיימת (המקבילה של כלל-ההתמדה במדריכי-המיקרו). ‏(ב) **בקר-תוכנית:** תוכנית רב-קבצים עוברת סוכן-ביקורת פנימי בהקשר-טרי לפני ההגשה — צ'קליסט: כללי-הברזל · ‏STATUS · קובצי-תוכנית פעילים של סשנים אחרים (`~/.claude/plans/`) · "כל הנחיית-צ'אט הומרה לצעד". ‏(ג) **רישום תוכנית-פעילה:** עם האישור נרשמת שורת "🔧 תוכנית פעילה: <נתיב>" בתזכורות של STATUS, ונמחקת בסיום — סשן מקביל קורא אותה לבד במקום שישי ישמש שליח בין הסשנים.
3. **הערות בעברית, "why-first":** להסביר כוונה אדריכלית ומקרי-קצה, לא תחביר. כל לוגיקה עסקית בקוד (נוסחה, כלל סטטוס, אכיפת הרשאה) מקבלת הערה שמסבירה אותה — בלי לחכות שיבקשו.
4. **לצטט קובץ+שורה**, לא לסמוך על הזיכרון.
5. **לא לשכתב/למחוק טבלאות קיימות.** שינויי DB רק דרך migration בעל שם (ראו פרוטוקול DB למטה).
6. **לעולם לא לקמט סודות** (`.env.local`, סיסמאות) ל-Git ולא להדפיס אותם במדריכים/צ'אט.
7. **לוודא בפועל (verify):** להריץ את האפליקציה/בדיקות ולראות שהשינוי עובד.
8. **עיצוב — לא ממציאים:** שפת העיצוב וטבלת הצבעים ב-`docs/PROJECT_MASTER.md` §4 (פרימרי teal `#0D9488` — עודכן בהכרעת-ישי 11/07/2026, סבב-UX מ2, מ-`#14B8A6` המקורי; רקע `#F8FAFC`, סרגל צד ימני וכו') **אושרו ע"י ישי (06/07/2026) ומחייבות את כל המסכים**. אין לשנות צבעים/פריסה בלי אישור ישי.
9. **מודל אבטחה:** כל מודול חדש מצהיר במפורש איך הוא נשען על תשתית ה-Auth של מודול 1 (RLS, שער תפקיד/הרשאה, session/OAuth). בסגירת מודול בודקים רגרסיה גם על מודל האבטחה.
10. **Git:** לעולם לא לדחוף ל-`main`/`dev` ישירות ולא `--force`. עבודה רק על ענף אישי `ishay/module-N-…`. **כל שינוי סכמה עובר דרך קובץ-מיגרציה בעל-שם** — גם בלי שותף לתאם איתו, זו הדרך היחידה לשמור על `docs/schema.sql` נאמן ועל היסטוריה הפיכה. **מיזוג באמצע-סשן (אחרי תקרית אמיתית):** אחרי כל push — אם ממשיכים לעבוד באותו סשן, אל תניחו שהענף עדיין לא-ממוזג; ישי עשוי למזג ב-GitHub באמצע בלי שהסשן ידע. לפני כל commit נוסף על אותו ענף: `git fetch origin` ואימות שהענף עדיין לא ancestor של `origin/dev` (`git merge-base --is-ancestor HEAD origin/dev`). אם כבר מוזג — לא לערום commits נוספים על ענף-מת; להודיע ולהציע ענף חדש מ-`dev` טרי.
11. **קבצים חדשים:** שמות באנגלית בלבד (עברית בשמות קבצים נשברת ב-Git/Windows); תוכן עברי בתוך `<div dir="rtl">`.
12. **בנייה בעצירות:** כל צעד מסתיים ב-🔻 נקודת עצירה שנבדקת לפני שממשיכים. DB/RLS קודם, UI אחר-כך.
13. **פרוטוקול שינוי מהותי (אדוות):** התקבלה החלטה שמשנה סכמה / פריט §7 / תהליך עבודה? מעדכנים **באותו סשן, לא "אחר כך"**, לפי הצ'קליסט הדו-כיווני המלא (‏`regin-docs-sync` אוכפת אותו בדיעבד):
    ‏(א) **§7 קודם:** עדכון הפריט ב-`PROJECT_MASTER.md` (סימון מוכרע/ניסוח) + יישור §6 ו"מצב נוכחי" אם הם מזכירים את אותה עובדה + עדכון הערות-האצווה של §7 אם הפריט אחרון-באשכול. **סדר-כתיבה — "§7 קודם" מילולית (הלקח מדריפט-ה-nod, 10/07/2026):** ה-write-back ל-§7 נכתב **ראשון**, לפני ה-Ledger/הציטוט שמשקף אותו — כך ה-SSOT לעולם לא מפגר אחרי החלטה שנרשמה במקום-מטה. **אסור לדחות את עדכון-§7 ל"שלב הביצוע"** (שם נולד הדריפט: ה-Ledger אמר RULED בעוד §7 עדיין אמר `🔵 להנהון`). **ובשער-הכרעה מהותי (הכרעת §7 / חבילת-nod) — הרץ `regin-docs-sync` לפני סוף-הסשן:** ‏STEP 2 FORWARD שלה תופס "מוכרע-שנשאר-פתוח" — רשת-הביטחון ל-§7-קודם (F1: משמעת + רוטינה קיימת, לא מנגנון חדש).
    ‏(ב) **הקוד/DB שבו הערך חי** (כלל 14 SSOT): ‏`src/lib/**` / מיגרציה / Seed / `params` — אם ההחלטה מקודדת שם.
    ‏(ג) **כל מקום שמצטט או משקף:** חיפוש `§7.N` **וגם** `מראת §7.N` על `docs/guides/` **ו-`docs/micro_guides/`**; ציטוט-מספר מתעדכן אם הקשרו השתנה, **מראה מתויגת (🔗) מסתנכרנת מילולית מ-§7**.
    ‏(ד) `docs/architecture_and_qa_roadmap.md` — אם ההחלטה נוגעת ל-QA/CI/DoD.
    ‏(ה) `STATUS.md` (+ `CLAUDE_CODE_LOG.md`) לפי פרוטוקול סוף-סשן.
    ‏(ו) **כיוון הפוך:** יישמת בקוד/DB משהו שסוגר בפועל פריט §7 פתוח? סוגרים את הפריט באותו סשן — בהכרעת ישי (כלל 1), לא לבד.
    ‏(ז) בסיום — להריץ את רוטינת `regin-docs-sync` (אודיט-הסנכרון העמוק) לאימות שאין סתירות שנשארו.
    **קונבנציית מראה (🔗):** כשמדריך צריך את *תוכן* ההחלטה ולא רק את מספרה — מותר לשכפל, אך ורק עם תג צמוד: ‏`🔗 מראת §7.N — SSOT: PROJECT_MASTER §7 (לא לערוך כאן ידנית)`. ‏§7 נשאר מקור-האמת היחיד; מראה מתויגת שסטתה מסונכרנת אוטומטית ע"י הרוטינה, ושכפול-החלטה **בלי** תג הוא ממצא לתיקון (לתייג או להמיר לציטוט-מספר).
14. **SSOT ללוגיקה עסקית:** כל חישוב עסקי (תמחור, מע"מ, הנחות, Smart Match, שכר, רווח גולמי) נכתב **פעם אחת** במודול ב-`src/lib/` (למשל `src/lib/pricing.js`) עם בדיקות יחידה לצידו; קומפוננטות UI רק מייבאות — לעולם לא משכפלות נוסחה. ממודול 2 והלאה: שאילתות ה-Supabase של מודול מרוכזות בקובץ `api.js` בתיקיית המודול (`src/modules/NN_name/api.js`), לא מפוזרות בתוך קומפוננטות.
15. **מדריך מיקרו חי:** `docs/micro_guides/module-N.md` (אנגלית, כתוב ל-Claude) הוא מפת הבנייה של המודול הפעיל — **מעדכנים אותו תוך כדי העבודה, לא בדיעבד**: מעבר צעד → עדכון כותרת המצב וטבלת הצעדים באותו סשן; סטייה מהתוכנית → הערת "↳ as-built" בצעד + שורה ביומן הסטיות שלו. נקודות עצירה 🔻 מתויגות **🤖** (Claude מאמת לבד בפקודה+ראיה וממשיך) או **👤** (עצירת ישי — סופי פזות, הכרעות §7, החלת מיגרציות, סודות/OAuth, חתימת DoD). ה-Stop hook חוסם סיום סשן אם השתנה קוד תחת `src/modules/NN_*/` בלי שמדריך המיקרו של אותו מודול עודכן אחריו. **חוב חוצה-מודולים (🚧):** יכולת שנבנית חלקית ומושלמת במודול אחר מסומנת במדריך-המיקרו `🚧 מN` (מודול-היעד), ו**חייבת** שורה תואמת `🚧 מN` ב-`PROJECT_MASTER` §6 **באותו סשן** — זה הרשם היחיד שפתיחת מודול N קוראת (`grep '🚧 מN'`) כדי לחזור ולהשלים את מה שהושאר. שורת-🚧 במדריך בלי תאום ב-§6 = חוב שקט (ה-Stop hook אוכף, אכיפה-0ג; זהו ripple של כלל 13). לא לבלבל 🚧 (חוב-יכולת חוצה-מודולים) עם ⏳ (החלטה-נדחית בטריאז') או 🕓 (חותמת-טריות לסעיפי-רפרנס).
16. **עבודה במקביל (כמה שיחות Claude שלך בו-זמנית):** בכל רגע נתון **שיחה כותבת אחת בלבד** (קוד או תיעוד) על עותק העבודה. שיחות נוספות במקביל מותרות רק לקריאה: מחקר, שאלות על הקוד, תכנון (Plan Mode), סקירת מסמכים. **למה:** שתי שיחות שכותבות תיעוד באותו זמן מתנגשות על הקבצים המרכזיים (`STATUS.md`/`CLAUDE_CODE_LOG.md`); גם פורט 5173 של שרת הפיתוח יחיד. *(הכלל נשאר בתוקף מלא גם עם מפתח יחיד — הוא על סשנים מקבילים, לא על אנשים.)* **הבהרה — ה-Stop hook מודע-לסשן:** ה-Stop hook כבר **מודע-לסשן** — סשן קריאה-בלבד שלא ערך קבצים בעצמו פטור מבדיקת התיעוד ולא נחסם גם ליד סשן כותב פעיל, והאכיפה מושווית מול העריכה האחרונה של אותו סשן בלבד (לא מול עץ-העבודה המשותף). לכן הלולאה-שנחסמת-בטעות כבר לא קורית לשיחת-קריאה; אבל הכלל "כותבת אחת בכל רגע" **עדיין עומד** בגלל התנגשויות git על התיעוד המשותף ופורט הפיתוח היחיד. כללי עזר: ‏(א) לא סיימת שיחה כותבת? אל תפתח שיחה כותבת חדשה — חכה או פתח שיחת קריאה ‏(ב) עבודת קוד מקבילה אמיתית — רק ב-worktree נפרד על ענף נפרד, ועדיין שיחה אחת מעדכנת יומנים בכל רגע ‏(ג) רוטינה שכותבת ליומן (כל 4 הרוטינות) לא רצה במקביל לשיחה כותבת ‏(ד) `npm run dev` רץ בשיחה אחת בלבד. טבלה מהירה לפי סוג בקשה: מחקר/שאלה/תכנון/סקירה = ✅ במקביל · בנייה/תיקון/עדכון תיעוד/רוטינה = ❌ אחד בתור. **המדריך המעשי המלא** (מצב-פתיחה, המילים המדויקות, העברת-תפקיד-כותב בין הסשנים שלך): ‏`docs/guides/reference/working_with_claude.md` ⑥.
17. **פרומפט מסירה (Handoff Prompt):** כשמגיעים לצעד ש-Claude לא יכול לבצע (👤 — פתיחת PR ב-GitHub, פעולה ב-Supabase Studio/Dashboard, OAuth, סודות), מדפיסים **תמיד** שני דברים: ‏(א) הוראות ביצוע בעברית פשוטה, צעד-צעד ‏(ב) בלוק **"🧩 פרומפט לקלוד בדפדפן"** — פרומפט עצמאי מוכן להדבקה בתוסף הכרום של Claude, שמכיל את כל ההקשר הדרוש (שמות ריפו/ענף/טבלה, מה בדיוק לעשות, איך לוודא הצלחה) **בלי** להסתמך על השיחה הנוכחית ו**בלי** סודות. הכלל חל גם על המדריכים: כל צעד 👤 במדריך מלווה בבלוק 🧩 צמוד.
## פרוטוקול DB (Supabase)
- **לפני תכנון כל מיגרציה — לקרוא `docs/db_roadmap.md`**: צ'קליסט-העיצוב (§1 שם) + השורות הממתינות לטבלאות שנוגעים בהן (§6 שם). אחרי החלה — לעדכן שם את השורות **באותו סשן** (ה-Stop hook אוכף; פרוטוקול המפה: db_roadmap §10).
- מתעדים רק DDL / פונקציות / טריגרים / RLS. לא INSERT/UPDATE של בדיקה (Seed של roles/modules/params הוא היוצא-מן-הכלל המותר).
- **כל שינוי DB = migration בעל שם** ב-`supabase/migrations/` עם כותרת "why" בעברית.
- **אישור-הקלדה לפני החלת מיגרציה (typed-echo):** החלת מיגרציה על פרויקט-Supabase היא פעולה **בלתי-הפיכה** — לפניה, שער-👤 של **וידוא-כפול**: ‏(א) **Claude מסביר בעברית מה המיגרציה עושה ועל מה היא משפיעה** (טבלאות/עמודות/RLS/תלויות/הפיכוּת); ‏(ב) ישי **מקליד את שם-המיגרציה** (לא "כן"/"אישור") כאישור מפורש שהבין את מה שמוחל. *(השער נשאר בתוקף מלא גם עם מפתח יחיד — ערכו הוא וידוא-הבנה לפני פעולה בלתי-הפיכה, לא תיאום בין שותפים.)* **⚠️ אחרי אישור-ההקלדה — Claude מחיל את המיגרציה בעצמו דרך ה-MCP (`apply_migration`); המשתמש אינו מריץ SQL ידנית.** מטרת השער = **וידוא-הבנה, לא ביצוע-ידני** (הובהר ע"י ישי 11/07/2026, אחרי שסשן הגזים בזהירות והפנה את ההרצה לדפדפן — טעות). ‏fallback להרצה-בדפדפן/CLI **רק** אם ה-MCP לא זמין בסשן, ואז מדווחים במפורש + מוסרים גרסת-SQL **נקייה מהערות-עברית** (הערות-RTL משתבשות בהעתקה לעורך). זהו אחד משני השערים הבלתי-הפיכים היחידים עם typed-echo (השני = חתימת-DoD בטמפלט-הסגירה); בשאר השערים 👤 מספיק אישור רגיל.
- **אחרי כל migration:** ‏(1) **Claude מחיל אותה דרך MCP `apply_migration`** (אחרי שער ה-typed-echo; fallback CLI/Studio/דפדפן רק אם ה-MCP לא זמין) ‏(2) לוודא שהוחלה (דרך ה-MCP בקריאה) ‏(3) לעדכן את `docs/schema.sql` — הדרך הקלה: Supabase SQL Editor → **SQL → Snapshots → "Generate schema SQL"** → להעתיק ל-`docs/schema.sql` ‏(4) commit של ה-migration + `docs/schema.sql` **ביחד**:
  ```bash
  git add supabase/migrations/…_name.sql docs/schema.sql
  git commit -m "db: [שם המיגרציה] + עדכון schema snapshot"
  ```
- ⚠️ migration שמגיעה ל-PR בלי `docs/schema.sql` מעודכן — לא approved.
- 🧩 **פרומפט מסירה ל-snapshot** (כלל ברזל 17 — להדביק בתוסף הכרום כשמגיעים לצעד 3):
  ```
  אני בפרויקט Supabase בשם Reg-In (ref: yfeovxppnfoafmfbdfvh). פתח את ה-SQL Editor,
  עבור ל-SQL → Snapshots → "Generate schema SQL", והעתק את כל הפלט.
  אחזיר אותו לקובץ docs/schema.sql בריפו. ודא שהפלט כולל את כל הטבלאות, הפונקציות וה-policies.
  ```
- **טבלה חדשה בשימוש ראשון:** לכל 11 הטבלאות העסקיות RLS מופעל עם אפס policies (deny-all מכוון). מסך ריק מול טבלה חדשה = כנראה חסרות policies — כל מודול מוסיף את ה-policies שלו במיגרציה, לפי מודל הבעלות שהוכרע ב-§7.

## פרוטוקול סוף סשן (הסדר מחייב; ה-hook חוסם סיום בלעדיו)
0. **מדריך המיקרו הפעיל** — אם עבדת לפי `docs/micro_guides/module-N.md`: עדכן בו את כותרת המצב, טבלת הצעדים והסטיות **לפני** היומנים (כלל ברזל 15; ה-hook אוכף גם את זה).
1. **`docs/CLAUDE_CODE_LOG.md`** (אנגלית מ-12/07/2026 — קובץ Claude-facing) — סעיף "Current State" נכתב-מחדש (snapshot, לא append) + רשומת סשן מתוארכת בראש ה-"Session Log". **שינוי שנוגע לטבלאות/משטח של מודול עתידי** — נרשם עם מספרי-המודולים המושפעים, כדי שסשן-פתיחת-המודול ההוא ימצא אותו (שינוי-DB → גם שורת-Done ב-`db_roadmap §10`; חוב-למודול-עתידי → גם `PROJECT_MASTER §6`).
2. **`STATUS.md`** — עדכון שורת המודול/השלב אם השתנה סטטוס; אם לא — לוודא שהלוח עדיין נכון ולעדכן את שורת "עודכן לאחרונה".

*(⛔ **`docs/CHANGELOG.md` הוקפא 23/07/2026** — לא כותבים אליו יותר. ציר-הזמן החי = `git log` + CLAUDE_CODE_LOG; שינויי-DB = migrations + `db_roadmap`; חובות = `PROJECT_MASTER §6`. קונבנציית 📣 של יידוע-המפתח-השני בוטלה 22/07/2026.)*

**פורמט תאריך (מ-07/07/2026 והלאה): `DD/MM/YYYY HH:MM`** — בכותרות רשומות סשן ב-CLAUDE_CODE_LOG ובשורת "עודכן לאחרונה" ב-STATUS.md. **למה:** כמה סשנים ביום זה כבר המצב הרגיל — בלי שעה אין סדר ביניהם. את השעה לוקחים מהמערכת (`Get-Date`), לא מנחשים. רשומות עבר בפורמט הישן (תאריך בלבד) לא משכתבים — הן קפואות.

**חלוקת אחריות (בלי כפילות):** `STATUS.md` = מצב-עכשיו בלבד (לוח, בלי היסטוריה) · `CLAUDE_CODE_LOG` = נרטיב/למה/tech-debt + ציר-הזמן של הסשנים (עם `git log` = הציר האמיתי) · `db_roadmap` + migrations = ציר שינויי-ה-DB · `PROJECT_MASTER §6` = רשם-החובות · `PROJECT_MASTER §7` = רישום השאלות הפתוחות היחיד (מדריכים מצטטים מספרי פריטים; שכפול-תוכן מותר **רק** כמראה מתויגת 🔗 — ר' כלל 13). *(`CHANGELOG` הוקפא 23/07/2026 — ארכיון בלבד.)* **רשומות יומן מתוארכות לא משכתבים לעולם** — גם אם הן מזכירות קבצים שכבר לא קיימים.

**משמעת-ראיות לכתיבת-נרטיב (הלקח מתקרית ה-PR שנכתבה שגוי):** כל טענה ביומן על **מצב חיצוני** (מוזג/נסגר/הוחל/מה קרה ב-GitHub או ב-Supabase) חייבת **לצטט את שורת-הראיה מילולית** — פלט `git fetch`/`merge-base`, הדבקת-אדם בצ'אט, או תוצאת-MCP — לא ניסוח-מהזיכרון (ציטוט כופה שליפה; תג-שם לבד = יכול להיות תיאטרון). **עדות-אדם שהודבקה בצ'אט על מערכת חיצונית גוברת על הסקה מ-git מקומי**; בסתירה — `git fetch origin` **חובה** לפני שכותבים את הטענה. **הרחבת ה-write-back של כלל 2 לדיווחי-אירוע:** דיווח שישי מדביק (סיכום מתוסף-הכרום, "מיזגתי PR", "הרצתי X") הוא **אירוע ממדרגה-ראשונה** — נרשם ליומן ברגע-הבליעה, מצוטט, **לפני** שממשיכים לפרש; צ'אט שלא הומר לרישום = לא קיים (המקבילה של כלל 2(א) לתוכניות). **צ'קליסט-PR (אחרי שישי תפס בלבול-PR שנשאר ב-STATUS):** כל עובדת-PR/מיזוג/ענף שנכתבת ל-`STATUS`/`CLAUDE_CODE_LOG` חייבת **לצטט פלט-git מהסשן הזה** (‏`git fetch` + `git log/show origin/dev`) — לא מהזיכרון ולא מ-git-מקומי-לא-מרוענן. עובדת-PR בלי ציטוט-git-טרי = לא נכתבת. *(הרקע: PR #2=קוד-המודול · PR #3=דוקס-3-קבצים; בלבול ביניהם שרד ב-STATUS עד שישי תפס — git-מקומי מיושן מהתקרית מבלבל בשקט.)*

**חותמת-סריקה לסעיפי-רפרנס (רעיון-ישי; אכיפה עודכנה — F9):** סעיפי-הרפרנס של `CLAUDE_CODE_LOG` (מוקשים תפעוליים / Tech-debt / יומן-DB / טמפלטים-ו-hooks) הם **ידע-חי שמתיישן בשקט** — פריט מיושן נראה זהה לעדכני. לכן כל סעיף נושא חותמת `🕓 נסקר DD/MM/YYYY HH:MM` בכותרתו; חותמת ישנה-בהרבה מ"היום" = **דגל-דריפט**. **האחריות לרענון החותמת = `regin-docs-sync` (STEP 3, בכל ריצה)** — לא נטל-טקסי על כל נגיעה. סשן שעורך סעיף-רפרנס **עדיין קורא את כל הסעיף ומוודא שהתוכן טרי** (זה חלק מהעריכה), ורשאי לרענן את החותמת אם בדק — אבל אינו **חייב** לגעת בחותמת בכל עריכה; הרוטינה מיישרת. **המטרה (רפרנס-טרי) נשמרת; האכיפה עברה לאוטומציה.**

## עבודה לפי מדריך
כשישי מדביק פרומפט של שלב — קרא קודם: `CLAUDE.md` (זה), `STATUS.md`, ואת מדריך השלב שצוין (‏`docs/guides/modules/module_NN_name.md`). לכל מודול **שלושה פרומפטים** במדריך השלב (סעיף ⑥): **פתיחה** (מפעיל את טמפלט-הבלופרינט `.claude/skills/module-blueprint/template.md` → בלופרינט machine-first שנשמר, אחרי אישור, כ-`docs/micro_guides/module-N.md`) · **המשך-בנייה** (קורא את מדריך המיקרו וממשיך מ"הצעד הפעיל") · **סגירה** (מפעיל את טמפלט-הסגירה `.claude/skills/module-close/template.md` → אודיט + התמדה לתיעוד + הוראות PR). **סקילי-זרימת-מודול (מ-23/07/2026):** לשלושת הפרומפטים יש סקילים תואמים בריפו — `module-blueprint` · `module-build` · `module-close` (‏`.claude/skills/`) — שנטענים אוטומטית לפי ניסוח-הטריגר ("פתח מודול N", "תמשיך לבנות", "סגור את המודול"), מנתבים לטמפלט/מדריך-המיקרו ואוכפים את משמעת-הראיות. הפרומפטים ⑥ במדריכי-השלב נשארים **חבילת-ההקשר הפר-מודולית** של כל מודול (רשימות-§7, הכרעות) וה-fallback אם הסקיל לא נטען. **+3 סקילי-עזר לתהליכים חוזרים (הכרעת-ישי 23/07/2026):** ‏`section7-rulings` ("בוא נסגור שאלות פתוחות" — סבב-הכרעות ממוזער-מאמץ: הכנה+סינון-מציאות לבד, לישי מגיעות רק הכרעות אמיתיות במנות 3–4, המלצה-תחילה) · `post-merge` ("מיזגתי" — אימות-מיזוג בראיות-git טריות + היפוך-STATUS; מיכון צ'קליסט-ה-PR) — שני אלה **רפו-מקומיים**. `feature-acceptance` ("בוא נעבור על מה שבנית" — סיור-קבלה מודרך מול תקצירי-החוויה שאושרו; ישי רק מאשר/מסמן-פער) **עבר לתיקיית-הסקילים הגלובלית של ישי `~/.claude/skills/` (23/07 לילה)** — פרויקט-אגנוסטי במהותו (סגנון-העבודה מול ישי, לא מבנה-הקבצים של REG-IN), כך שישרת כל פרויקט שלו, לא רק את זה. **חלוקת שפות:** מדריכי `docs/guides/` בעברית — לבני אדם; `docs/micro_guides/`, הטמפלטים (`.claude/skills/*/template.md`), גוף-הסקילים (`.claude/skills/*/SKILL.md`), **ו-`docs/CLAUDE_CODE_LOG.md`** (מ-12/07/2026) באנגלית — ל-Claude (עברית רק כדאטה: שמות מודולים/תפקידים, מחרוזות UI, ציטוטי §7, שמות-מיגרציה, ניסוחי-טריגר של סקיל); ‏`STATUS.md` נשאר עברית (אנושי; ‏`CHANGELOG.md` הוקפא 23/07 — ארכיון-עברית); דיווחים בצ'אט לישי — תמיד בעברית.

**שאלה-חוזרת ⇒ תיקון מסמך-המקור:** אם אותה שאלה חוזרת ועולה (סימן שמדריך/טמפלט לא ברורים) — **מתקנים את מסמך-המקור** (המדריך, הטמפלט, או `CLAUDE.md`) כך שהתשובה תשב שם לתמיד — **לא** פותחים קובץ-FAQ נפרד (שמתיישן ומתפצל מהמקור). התיקון הוא ripple של כלל 13 ונרשם ביומן.

**כלל מודלים (המלצת-בחירה — לא מנגנון; המודל בפועל הוא מה שנבחר ב-`/model`):** ‏Opus/Fable כדי לחשוב (בלופרינט, RLS, אבטחה, סגירה, אודיטים — מאמץ גבוה; ‏Fable = השכבה שמעל Opus) · Sonnet כדי לייצר (UI, טפסים, ניתוב — מאמץ בינוני).

## כלי-עזר להחלטות: llm-council
מותקן פר-משתמש (`~/.claude/skills/llm-council/`) — זה לא סקיל של הפרויקט ולא נכנס ל-Git. כשעולה החלטה עם טרייד-אוף אמיתי (הכרעת §7, עיצוב DB שנוי-במחלוקת, אסטרטגיית מיגרציה, נוסחת תמחור/Smart Match), וגם כשמבקשים לכתוב מסמך-דיון/ניתוח-אופציות, או סתם מתייעצים על כיוון בלי לנסח שאלה פורמלית — Claude **מציע** לישי להריץ `council this` (5 יועצים עם זוויות מנוגדות + ביקורת-עמיתים בעילום-שם + סינתזת יו"ר) ו**ממתין לאישורו** לפני שמפעיל אותו בפועל — אף פעם לא מריץ את המועצה ביוזמתו בלי לשאול קודם. הפלט הוא כלי-חשיבה שמקדים את ההכרעה — **לא תחליף לה**: הכרעת §7 עצמה תמיד לפי כלל 1 (ישי, לא הסוכן).

</div>
