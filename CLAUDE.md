<div dir="rtl">

# CLAUDE.md — כללי הברזל של REG-IN

> קובץ זה נטען אוטומטית בכל סשן Claude Code בריפו הזה. **קרא את `STATUS.md` מיד אחריו** — הוא לוח המצב היחיד (איפה עומדים, מה השלב הבא של כל מפתח).

## מה זה REG-IN
מערכת ניהול עסקית מודולרית לחברת אירועים/כנסים (לקוחות, הצעות מחיר, דיילות + Smart Match, לוגיסטיקה, פרויקטים, כספים, דו"חות). שני מפתחים: **ישי** (בעל הריפו; מודולים 0,1,4,5,9,10) ו**עמית** (מודולים 2,3,7,8,11); מודולים 6,12 משותפים.

**Stack:** Vite 8 + React 19 · JavaScript (לא TypeScript) · Tailwind 4 + shadcn/ui + Radix · Supabase (Auth + Postgres 17) · עברית RTL מלאה.
**עובדות סביבה:** alias `@/` → `src/` · לקוח Supabase: `src/supabaseClient.js` → מיובא כ-`@/supabaseClient` (⚠️ לא `@/lib/`) · פרויקט Supabase: `Reg-In` (ref `yfeovxppnfoafmfbdfvh`, אזור `eu-west-3`) · שרת פיתוח: `npm run dev` (פורט 5173) · Windows + PowerShell (⚠️ לחותמת-זמן: ב-PowerShell `Get-Date`, ב-Bash `date +"%d/%m/%Y %H:%M"` — לא לערבב; `Get-Date` אינו קיים בכלי Bash ויחזיר `command not found`) · **Supabase MCP** מוגדר ב-`.mcp.json` (HTTP+OAuth) — להשתמש בו לכל אימות מצב DB (ספירות, RLS, advisors) **בקריאה בלבד**; שינויי סכמה לעולם רק דרך מיגרציות. אם ה-MCP לא זמין בסשן — לציין זאת במפורש בדיווח, לא לדלג בשקט.

## היררכיית מקורות אמת (בסתירה — הגבוה קובע)
1. **`docs/schema.sql`** — סכמת ה-DB (snapshot; מקור-אמת לשינויים = `supabase/migrations/`).
2. **האפיון המאושר, פרקים 5–6** (`docs/reference_spec/C5_clean_transcript.md` + `C6_clean_transcript.md`) — הלוגיקה העסקית והמסכים. אלה **תעתיקים מנוקים-מכנית** (שחזור-מספור C5 · הזזת-טבלה ב-C6 · עוגני-ניווט · הערות-§9) שהחליפו את יצואי-ה-Word המקוריים (`C5_Processes_And_Screens.md`/`C6_DB_Tables.md`, נמחקו 09/07/2026 אחרי סוללת-אימות מלאה + PASS-ישי). **קבצים קפואים — לא עורכים לעולם**; סטייה מהאפיון נרשמת בתיעוד החי בלבד (PROJECT_MASTER, מדריך המודול, CHANGELOG) עם הערת "סטייה מ-5.x". **ציטוטי `C5:NNN`/`C6:NNN`** נפתרים דרך הערות `<!-- מקור C5/C6 L… -->` שבראש כל סעיף בתעתיק (הסעיף שערך-ה-L שלו הגדול-ביותר ≤ NNN).
3. **המוקאפים** (`docs/mockups/`) — רפרנס ויזואלי בלבד.
4. **המדריכים** (`docs/guides/`, `docs/micro_guides/`) — מתכון לסדר העבודה. הכי נמוך.

## מפת המסמכים — מה לקרוא ומתי
| קובץ | תפקיד |
|---|---|
| `STATUS.md` | **לוח מצב יחיד** — סטטוס מודולים + השלב הנוכחי של כל מסלול. לקרוא בכל סשן |
| `README.md` | דף הבית: מי זה מי, הפרומפטים, מפת התיעוד |
| `docs/guides/00_roadmap.md` | מפת הדרכים: המודולים, התלויות, אבני הדרך, הלולאה לכל מודול |
| `docs/guides/ishay/` · `docs/guides/amit/` · `docs/guides/shared/` | מדריכי השלבים לכל מסלול |
| `docs/PROJECT_MASTER.md` | האפיון המסונתז + **§7 = רישום השאלות הפתוחות הממוספר (יחיד!)** |
| `docs/reference_spec/C5_clean_transcript.md` · `C6_clean_transcript.md` | 🧊 **האפיון המאושר (פרקים 5–6) — דרגה 2, קפוא** (תעתיקים מנוקים; החליפו את יצואי-ה-Word המקוריים). כל אחד פותח במפת-מודולים ניווטית. C5 = תהליכים/מסכים · C6 = טבלאות-DB/DFD/ERD |
| `docs/db_roadmap.md` | 🗺️ **מפת שינויי-ה-DB העתידיים** (אגרגטור-ביצוע; ההחלטות רק ב-§7) — לקרוא לפני כל תכנון מיגרציה ובכל בלופרינט |
| `docs/CLAUDE_CODE_LOG.md` | יומן הסשנים של Claude (נרטיב, "למה", tech-debt) |
| `docs/CHANGELOG.md` | יומן שינויים מתוארך (DB+קוד) לתיאום בין שני המפתחים |
| `docs/claude_routines.md` | ההגדרות הקנוניות של 4 הרוטינות + פרוטוקול עדכון |
| `docs/architecture_and_qa_roadmap.md` | סטנדרט הנדסי + Definition of Done |

## כללי ברזל (Rules of Engagement)

> 🗂️ **מפת-הכללים (המספור קבוע — עשרות ציטוטים תלויים בו):** תהליך-עבודה 1·2·12·13·15·16·17 · איכות-קוד 3·4·11·14 · אבטחה 6·9 · ‏git+DB ‏5·10 (+פרוטוקול DB למטה) · עיצוב 8 · אימות 7.
>
> **מקרא-אימוג'י (F7 — הסט קפוא; אין להוסיף חדש בלי אישור-ישי + מחיקת-אחד):** 🔻 עצירה · 🤖 Claude-מאמת-לבד · 👤 עצירת-אדם (אישור/הכרעה/פעולה-ידנית) · 🚧 חוב חוצה-מודולים (§6) · ⏳ החלטה-נדחית · 🕓 חותמת-טריות (רפרנס) · 🔗 מראה מתויגת (§7) · 🧩 פרומפט-מסירה · 📣 יידוע-המפתח-השני · 🧊 הקפאת-תשתית · 🔮 checkpoint-עתידי · 🗡️ ‏DB-Design-Challenge (טמפלטים).

1. **שאל שאלות הבהרה לפני עבודה** כשמשהו לא חד-משמעי — אל תניח. שאלות פתוחות מוכרעות **רק עם ישי/עמית** — לעולם לא לבד (הרשימה: PROJECT_MASTER §7). **זהות הדובר:** אם זהות המדביק (ישי/עמית) משנה להכרעה ואינה ברורה מההקשר — שאל לפני שממשיכים.
    **ניתוב §7 = ישי-בלבד:** כל הכרעת-§7 היא של **ישי**, גם בפריטי-§7 של מודולים של עמית. סשן של עמית שנתקל בשאלת-§7 **מנתב אותה לישי** — מציג לו את השאלה בסגנון-ההכרעה (רקע · משמעות · אפשרויות · המלצה), לא מכריע לבד ולא נותן לעמית להכריע פריט-§7; ההכרעה נרשמת בשם ישי (כלל 13).
2. **Plan Mode:** לתכנן ולקבל אישור לפני ביצוע; במצב תכנון לא נוגעים בקבצים. **מתי נדרש:** פתיחת מודול (בלופרינט), סגירת מודול, הכרעת §7, וכל סטייה מהותית מהתוכנית. **מתי לא:** בנייה שוטפת לפי מדריך מיקרו מאושר — מדריך המיקרו *הוא* התוכנית המאושרת, ונקודות העצירה 🤖/👤 הן הבקרה; מבצעים ישירות בלי סבב תכנון נוסף. **נוהל תוכניות (הלקח מתיקון-שאבד-פעמיים):** ‏(א) **write-back:** כל הערה/דיוק על תוכנית פעילה (מישי/עמית או מסשן אחר) נכתבת **לתוך קובץ-התוכנית** כצעד לפני שממשיכים — הערת-צ'אט שלא הומרה לצעד = לא קיימת (המקבילה של כלל-ההתמדה במדריכי-המיקרו). ‏(ב) **בקר-תוכנית:** תוכנית רב-קבצים עוברת סוכן-ביקורת פנימי בהקשר-טרי לפני ההגשה — צ'קליסט: כללי-הברזל · ‏STATUS · קובצי-תוכנית פעילים של סשנים אחרים (`~/.claude/plans/`) · "כל הנחיית-צ'אט הומרה לצעד". ‏(ג) **רישום תוכנית-פעילה:** עם האישור נרשמת שורת "🔧 תוכנית פעילה: <נתיב> (<מי כותב>)" בתזכורות של STATUS, ונמחקת בסיום — סשן מקביל קורא אותה לבד במקום שישי ישמש שליח.
3. **הערות בעברית, "why-first":** להסביר כוונה אדריכלית ומקרי-קצה, לא תחביר. כל לוגיקה עסקית בקוד (נוסחה, כלל סטטוס, אכיפת הרשאה) מקבלת הערה שמסבירה אותה — בלי לחכות שיבקשו.
4. **לצטט קובץ+שורה**, לא לסמוך על הזיכרון.
5. **לא לשכתב/למחוק טבלאות קיימות.** שינויי DB רק דרך migration בעל שם (ראו פרוטוקול DB למטה).
6. **לעולם לא לקמט סודות** (`.env.local`, סיסמאות) ל-Git ולא להדפיס אותם במדריכים/צ'אט.
7. **לוודא בפועל (verify):** להריץ את האפליקציה/בדיקות ולראות שהשינוי עובד.
8. **עיצוב — לא ממציאים:** שפת העיצוב וטבלת הצבעים ב-`docs/PROJECT_MASTER.md` §4 (פרימרי teal `#14B8A6`, רקע `#F8FAFC`, סרגל צד ימני וכו') **אושרו ע"י ישי (06/07/2026) ומחייבות את כל המסכים**. אין לשנות צבעים/פריסה בלי אישור ישי.
9. **מודל אבטחה:** כל מודול חדש מצהיר במפורש איך הוא נשען על תשתית ה-Auth של מודול 1 (RLS, שער תפקיד/הרשאה, session/OAuth). בסגירת מודול בודקים רגרסיה גם על מודל האבטחה.
10. **Git:** לעולם לא לדחוף ל-`main`/`dev` ישירות ולא `--force`. עבודה רק על ענף אישי `שם/module-N-…`. שני המפתחים חולקים פרויקט Supabase אחד — **כל שינוי סכמה מתואם מראש**. **מיזוג באמצע-סשן (אחרי תקרית אמיתית):** אחרי כל push — אם ממשיכים לעבוד באותו סשן, אל תניחו שהענף עדיין לא-ממוזג; ישי/עמית עשויים למזג ב-GitHub באמצע בלי שהסשן ידע. לפני כל commit נוסף על אותו ענף: `git fetch origin` ואימות שהענף עדיין לא ancestor של `origin/dev` (`git merge-base --is-ancestor HEAD origin/dev`). אם כבר מוזג — לא לערום commits נוספים על ענף-מת; להודיע ולהציע ענף חדש מ-`dev` טרי.
11. **קבצים חדשים:** שמות באנגלית בלבד (עברית בשמות קבצים נשברת ב-Git/Windows); תוכן עברי בתוך `<div dir="rtl">`.
12. **בנייה בעצירות:** כל צעד מסתיים ב-🔻 נקודת עצירה שנבדקת לפני שממשיכים. DB/RLS קודם, UI אחר-כך.
13. **פרוטוקול שינוי מהותי (אדוות):** התקבלה החלטה שמשנה סכמה / פריט §7 / תהליך עבודה? מעדכנים **באותו סשן, לא "אחר כך"**, לפי הצ'קליסט הדו-כיווני המלא (‏`regin-docs-sync` אוכפת אותו בדיעבד):
    ‏(א) **§7 קודם:** עדכון הפריט ב-`PROJECT_MASTER.md` (סימון מוכרע/ניסוח) + יישור §6 ו"מצב נוכחי" אם הם מזכירים את אותה עובדה + עדכון הערות-האצווה של §7 אם הפריט אחרון-באשכול.
    ‏(ב) **הקוד/DB שבו הערך חי** (כלל 14 SSOT): ‏`src/lib/**` / מיגרציה / Seed / `params` — אם ההחלטה מקודדת שם.
    ‏(ג) **כל מקום שמצטט או משקף:** חיפוש `§7.N` **וגם** `מראת §7.N` על `docs/guides/` **ו-`docs/micro_guides/`**; ציטוט-מספר מתעדכן אם הקשרו השתנה, **מראה מתויגת (🔗) מסתנכרנת מילולית מ-§7**.
    ‏(ד) `docs/architecture_and_qa_roadmap.md` — אם ההחלטה נוגעת ל-QA/CI/DoD.
    ‏(ה) `STATUS.md`+`CHANGELOG.md` לפי פרוטוקול סוף-סשן.
    ‏(ו) **כיוון הפוך:** יישמת בקוד/DB משהו שסוגר בפועל פריט §7 פתוח? סוגרים את הפריט באותו סשן — בהכרעת ישי (כלל 1), לא לבד.
    ‏(ז) בסיום — להריץ את רוטינת `regin-docs-sync` (אודיט-הסנכרון העמוק) לאימות שאין סתירות שנשארו.
    **קונבנציית מראה (🔗):** כשמדריך צריך את *תוכן* ההחלטה ולא רק את מספרה — מותר לשכפל, אך ורק עם תג צמוד: ‏`🔗 מראת §7.N — SSOT: PROJECT_MASTER §7 (לא לערוך כאן ידנית)`. ‏§7 נשאר מקור-האמת היחיד; מראה מתויגת שסטתה מסונכרנת אוטומטית ע"י הרוטינה, ושכפול-החלטה **בלי** תג הוא ממצא לתיקון (לתייג או להמיר לציטוט-מספר).
14. **SSOT ללוגיקה עסקית:** כל חישוב עסקי (תמחור, מע"מ, הנחות, Smart Match, שכר, רווח גולמי) נכתב **פעם אחת** במודול ב-`src/lib/` (למשל `src/lib/pricing.js`) עם בדיקות יחידה לצידו; קומפוננטות UI רק מייבאות — לעולם לא משכפלות נוסחה. ממודול 2 והלאה: שאילתות ה-Supabase של מודול מרוכזות בקובץ `api.js` בתיקיית המודול (`src/modules/NN_name/api.js`), לא מפוזרות בתוך קומפוננטות.
15. **מדריך מיקרו חי:** `docs/micro_guides/module-N.md` (אנגלית, כתוב ל-Claude) הוא מפת הבנייה של המודול הפעיל — **מעדכנים אותו תוך כדי העבודה, לא בדיעבד**: מעבר צעד → עדכון כותרת המצב וטבלת הצעדים באותו סשן; סטייה מהתוכנית → הערת "↳ as-built" בצעד + שורה ביומן הסטיות שלו. נקודות עצירה 🔻 מתויגות **🤖** (Claude מאמת לבד בפקודה+ראיה וממשיך) או **👤** (עצירת ישי/עמית — סופי פזות, הכרעות §7, החלת מיגרציות, סודות/OAuth, חתימת DoD). ה-Stop hook חוסם סיום סשן אם השתנה קוד תחת `src/modules/NN_*/` בלי שמדריך המיקרו של אותו מודול עודכן אחריו. **חוב חוצה-מודולים (🚧):** יכולת שנבנית חלקית ומושלמת במודול אחר מסומנת במדריך-המיקרו `🚧 מN` (מודול-היעד), ו**חייבת** שורה תואמת `🚧 מN` ב-`PROJECT_MASTER` §6 **באותו סשן** — זה הרשם היחיד שפתיחת מודול N קוראת (`grep '🚧 מN'`) כדי לחזור ולהשלים את מה שהושאר. שורת-🚧 במדריך בלי תאום ב-§6 = חוב שקט (ה-Stop hook אוכף, אכיפה-0ג; זהו ripple של כלל 13). לא לבלבל 🚧 (חוב-יכולת חוצה-מודולים) עם ⏳ (החלטה-נדחית בטריאז') או 🕓 (חותמת-טריות לסעיפי-רפרנס).
16. **עבודה במקביל (כמה שיחות Claude בו-זמנית):** בכל רגע נתון **שיחה כותבת אחת בלבד** (קוד או תיעוד) על עותק העבודה. שיחות נוספות במקביל מותרות רק לקריאה: מחקר, שאלות על הקוד, תכנון (Plan Mode), סקירת מסמכים. **למה:** שתי שיחות שכותבות תיעוד באותו זמן מתנגשות על הקבצים המשותפים (`STATUS.md`/`CLAUDE_CODE_LOG.md`); גם פורט 5173 של שרת הפיתוח יחיד. **הבהרה — ה-Stop hook מודע-לסשן:** ה-Stop hook כבר **מודע-לסשן** — סשן קריאה-בלבד שלא ערך קבצים בעצמו פטור מבדיקת התיעוד ולא נחסם גם ליד סשן כותב פעיל, והאכיפה מושווית מול העריכה האחרונה של אותו סשן בלבד (לא מול עץ-העבודה המשותף). לכן הלולאה-שנחסמת-בטעות כבר לא קורית לשיחת-קריאה; אבל הכלל "כותבת אחת בכל רגע" **עדיין עומד** בגלל התנגשויות git על התיעוד המשותף ופורט הפיתוח היחיד. כללי עזר: ‏(א) לא סיימת שיחה כותבת? אל תפתח שיחה כותבת חדשה — חכה או פתח שיחת קריאה ‏(ב) עבודת קוד מקבילה אמיתית — רק ב-worktree נפרד על ענף נפרד, ועדיין שיחה אחת מעדכנת יומנים בכל רגע ‏(ג) רוטינה שכותבת ליומן (כל 4 הרוטינות) לא רצה במקביל לשיחה כותבת ‏(ד) `npm run dev` רץ בשיחה אחת בלבד. טבלה מהירה לפי סוג בקשה: מחקר/שאלה/תכנון/סקירה = ✅ במקביל · בנייה/תיקון/עדכון תיעוד/רוטינה = ❌ אחד בתור. **המדריך המעשי המלא** (מצב-פתיחה, המילים המדויקות, העברת-תפקיד-כותב): ‏`docs/guides/amit/04c_working_with_claude.md` ⑥.
17. **פרומפט מסירה (Handoff Prompt):** כשמגיעים לצעד ש-Claude לא יכול לבצע (👤 — פתיחת PR ב-GitHub, פעולה ב-Supabase Studio/Dashboard, OAuth, סודות), מדפיסים **תמיד** שני דברים: ‏(א) הוראות ביצוע בעברית פשוטה, צעד-צעד ‏(ב) בלוק **"🧩 פרומפט לקלוד בדפדפן"** — פרומפט עצמאי מוכן להדבקה בתוסף הכרום של Claude, שמכיל את כל ההקשר הדרוש (שמות ריפו/ענף/טבלה, מה בדיוק לעשות, איך לוודא הצלחה) **בלי** להסתמך על השיחה הנוכחית ו**בלי** סודות. הכלל חל גם על המדריכים: כל צעד 👤 במדריך מלווה בבלוק 🧩 צמוד.
## פרוטוקול DB (Supabase)
- **לפני תכנון כל מיגרציה — לקרוא `docs/db_roadmap.md`**: צ'קליסט-העיצוב (§1 שם) + השורות הממתינות לטבלאות שנוגעים בהן (§6 שם). אחרי החלה — לעדכן שם את השורות **באותו סשן** (ה-Stop hook אוכף; פרוטוקול המפה: db_roadmap §10).
- מתעדים רק DDL / פונקציות / טריגרים / RLS. לא INSERT/UPDATE של בדיקה (Seed של roles/modules/params הוא היוצא-מן-הכלל המותר).
- **כל שינוי DB = migration בעל שם** ב-`supabase/migrations/` עם כותרת "why" בעברית.
- **אישור-הקלדה לפני החלת מיגרציה (typed-echo, רשת-עמית):** החלת מיגרציה על פרויקט-Supabase המשותף היא פעולה **בלתי-הפיכה** — לפניה, שער-👤 שבו המשתמש (ישי/עמית) **מקליד את שם-המיגרציה** (לא "כן"/"אישור") כאישור מפורש שהבין את מה שהוא מחיל. זהו אחד משני השערים הבלתי-הפיכים היחידים עם typed-echo (השני = חתימת-DoD בטמפלט-הסגירה); בשאר השערים 👤 מספיק אישור רגיל.
- **אחרי כל migration:** ‏(1) להריץ אותה (CLI או Supabase Studio) ‏(2) לוודא שהוחלה ‏(3) לעדכן את `docs/schema.sql` — הדרך הקלה: Supabase SQL Editor → **SQL → Snapshots → "Generate schema SQL"** → להעתיק ל-`docs/schema.sql` ‏(4) commit של ה-migration + `docs/schema.sql` **ביחד**:
  ```bash
  git add supabase/migrations/…_name.sql docs/schema.sql
  git commit -m "db: [שם המיגרציה] + עדכון schema snapshot"
  ```
- ⚠️ migration שמגיעה ל-PR בלי `docs/schema.sql` מעודכן — לא approved.
- 🧩 **פרומפט מסירה ל-snapshot** (כלל ברזל 17 — להדביק בתוסף הכרום כשמגיעים לצעד 3):
  ```
  אני בפרויקט Supabase בשם Reg-In (ref: yfeovxppnfoafmfbdfvh). פתח את ה-SQL Editor,
  עבור ל-SQL → Snapshots → "Generate schema SQL", והעתק את כל הפלט.
  אחזיר אותו לקובץ docs/schema.sql בריפו. ודא שהפלט כולל את כל הטבלאות, הפונקציות וה-policies.
  ```
- **טבלה חדשה בשימוש ראשון:** לכל 11 הטבלאות העסקיות RLS מופעל עם אפס policies (deny-all מכוון). מסך ריק מול טבלה חדשה = כנראה חסרות policies — כל מודול מוסיף את ה-policies שלו במיגרציה, לפי מודל הבעלות שהוכרע ב-§7.

## פרוטוקול סוף סשן (הסדר מחייב; ה-hook חוסם סיום בלעדיו)
0. **מדריך המיקרו הפעיל** — אם עבדת לפי `docs/micro_guides/module-N.md`: עדכן בו את כותרת המצב, טבלת הצעדים והסטיות **לפני** היומנים (כלל ברזל 15; ה-hook אוכף גם את זה).
1. **`docs/CHANGELOG.md`** — אם השתנה DB או קוד: שורה מתוארכת (`סוג DB/קוד · מי · מה · מודולים מושפעים`). **קונבנציית 📣:** שינוי שנוגע למודול/לטבלאות של המפתח השני מסומן 📣 עם שמו, והדיווח הסופי של הסשן מדפיס לו הודעה עברית מוכנה-להדבקה (מה השתנה, איפה זה פוגש אותו, מה לקרוא).
2. **`docs/CLAUDE_CODE_LOG.md`** — "מצב נוכחי" נכתב-מחדש (snapshot, לא append) + רשומת סשן מתוארכת בראש היומן.
3. **`STATUS.md`** — עדכון שורת המודול/השלב אם השתנה סטטוס; אם לא — לוודא שהלוח עדיין נכון ולעדכן את שורת "עודכן לאחרונה".

**פורמט תאריך (מ-07/07/2026 והלאה): `DD/MM/YYYY HH:MM`** — בכותרות רשומות סשן ב-CLAUDE_CODE_LOG, בעמודת התאריך ב-CHANGELOG, ובשורת "עודכן לאחרונה" ב-STATUS.md. **למה:** כמה סשנים ביום זה כבר המצב הרגיל — בלי שעה אין סדר ביניהם. את השעה לוקחים מהמערכת (`Get-Date`), לא מנחשים. רשומות עבר בפורמט הישן (תאריך בלבד) לא משכתבים — הן קפואות.

**חלוקת אחריות (בלי כפילות):** `STATUS.md` = מצב-עכשיו בלבד (לוח, בלי היסטוריה) · `CHANGELOG` = שורות מתוארכות לתיאום · `CLAUDE_CODE_LOG` = נרטיב/למה/tech-debt · `PROJECT_MASTER §7` = רישום השאלות הפתוחות היחיד (מדריכים מצטטים מספרי פריטים; שכפול-תוכן מותר **רק** כמראה מתויגת 🔗 — ר' כלל 13). **רשומות יומן מתוארכות לא משכתבים לעולם** — גם אם הן מזכירות קבצים שכבר לא קיימים.

**משמעת-ראיות לכתיבת-נרטיב (הלקח מתקרית ה-PR שנכתבה שגוי):** כל טענה ביומן על **מצב חיצוני** (מוזג/נסגר/הוחל/מה המפתח השני עשה) חייבת **לצטט את שורת-הראיה מילולית** — פלט `git fetch`/`merge-base`, הדבקת-אדם בצ'אט, או תוצאת-MCP — לא ניסוח-מהזיכרון (ציטוט כופה שליפה; תג-שם לבד = יכול להיות תיאטרון). **עדות-אדם שהודבקה בצ'אט על מערכת חיצונית גוברת על הסקה מ-git מקומי**; בסתירה — `git fetch origin` **חובה** לפני שכותבים את הטענה. **הרחבת ה-write-back של כלל 2 לדיווחי-אירוע:** דיווח שישי/עמית מדביקים (סיכום מתוסף-הכרום, "מיזגתי PR", "הרצתי X") הוא **אירוע ממדרגה-ראשונה** — נרשם ליומן ברגע-הבליעה, מצוטט, **לפני** שממשיכים לפרש; צ'אט שלא הומר לרישום = לא קיים (המקבילה של כלל 2(א) לתוכניות). **צ'קליסט-PR (אחרי שישי תפס בלבול-PR שנשאר ב-STATUS):** כל עובדת-PR/מיזוג/ענף שנכתבת ל-`STATUS`/`CLAUDE_CODE_LOG` חייבת **לצטט פלט-git מהסשן הזה** (‏`git fetch` + `git log/show origin/dev`) — לא מהזיכרון ולא מ-git-מקומי-לא-מרוענן. עובדת-PR בלי ציטוט-git-טרי = לא נכתבת. *(הרקע: PR #2=קוד-המודול · PR #3=דוקס-3-קבצים; בלבול ביניהם שרד ב-STATUS עד שישי תפס — git-מקומי מיושן מהתקרית מבלבל בשקט.)*

**חותמת-סריקה לסעיפי-רפרנס (רעיון-ישי; אכיפה עודכנה — F9):** סעיפי-הרפרנס של `CLAUDE_CODE_LOG` (מוקשים תפעוליים / Tech-debt / יומן-DB / טמפלטים-ו-hooks) הם **ידע-חי שמתיישן בשקט** — פריט מיושן נראה זהה לעדכני. לכן כל סעיף נושא חותמת `🕓 נסקר DD/MM/YYYY HH:MM` בכותרתו; חותמת ישנה-בהרבה מ"היום" = **דגל-דריפט**. **האחריות לרענון החותמת = `regin-docs-sync` (STEP 3, בכל ריצה)** — לא נטל-טקסי על כל נגיעה. סשן שעורך סעיף-רפרנס **עדיין קורא את כל הסעיף ומוודא שהתוכן טרי** (זה חלק מהעריכה), ורשאי לרענן את החותמת אם בדק — אבל אינו **חייב** לגעת בחותמת בכל עריכה; הרוטינה מיישרת. **המטרה (רפרנס-טרי) נשמרת; האכיפה עברה לאוטומציה.**

## עבודה לפי מדריך
כשישי או עמית מדביקים פרומפט של שלב — קרא קודם: `CLAUDE.md` (זה), `STATUS.md`, ואת מדריך השלב שצוין. לכל מודול **שלושה פרומפטים** במדריך השלב (סעיף ⑥): **פתיחה** (מפעיל את `docs/templates/create_micro_guide_template.md` → בלופרינט machine-first שנשמר, אחרי אישור, כ-`docs/micro_guides/module-N.md`) · **המשך-בנייה** (קורא את מדריך המיקרו וממשיך מ"הצעד הפעיל") · **סגירה** (מפעיל את `docs/templates/create_module_final_test_template.md` → אודיט + התמדה לתיעוד + הוראות PR). **חלוקת שפות:** מדריכי `docs/guides/` בעברית — לבני אדם; `docs/micro_guides/` והטמפלטים באנגלית — ל-Claude (עברית רק כדאטה: שמות מודולים/תפקידים, מחרוזות UI); דיווחים בצ'אט לישי/עמית — תמיד בעברית.

**שאלה-חוזרת ⇒ תיקון מסמך-המקור (רשת-עמית):** אם אותה שאלה חוזרת ועולה (סימן שמדריך/טמפלט לא ברורים) — **מתקנים את מסמך-המקור** (המדריך, הטמפלט, או `CLAUDE.md`) כך שהתשובה תשב שם לתמיד — **לא** פותחים קובץ-FAQ נפרד (שמתיישן ומתפצל מהמקור). התיקון הוא ripple של כלל 13 ונרשם ביומן.

**כלל מודלים (המלצת-בחירה לישי/עמית — לא מנגנון; המודל בפועל הוא מה שנבחר ב-`/model`):** ‏Opus/Fable כדי לחשוב (בלופרינט, RLS, אבטחה, סגירה, אודיטים — מאמץ גבוה; ‏Fable = השכבה שמעל Opus) · Sonnet כדי לייצר (UI, טפסים, ניתוב — מאמץ בינוני).

## כלי-עזר להחלטות: llm-council (אישי אצל ישי בלבד)
מותקן פר-משתמש אצל ישי (`~/.claude/skills/llm-council/`) — **לא זמין בסשן של עמית** אלא אם יתקין אותו אצלו בנפרד; זה לא סקיל של הפרויקט ולא נכנס ל-Git. כשעולה החלטה עם טרייד-אוף אמיתי (הכרעת §7, עיצוב DB שנוי-במחלוקת, אסטרטגיית מיגרציה, נוסחת תמחור/Smart Match), וגם כשמבקשים לכתוב מסמך-דיון/ניתוח-אופציות, או סתם מתייעצים על כיוון בלי לנסח שאלה פורמלית — Claude **מציע** לישי להריץ `council this` (5 יועצים עם זוויות מנוגדות + ביקורת-עמיתים בעילום-שם + סינתזת יו"ר) ו**ממתין לאישורו** לפני שמפעיל אותו בפועל — אף פעם לא מריץ את המועצה ביוזמתו בלי לשאול קודם. הפלט הוא כלי-חשיבה שמקדים את ההכרעה — **לא תחליף לה**: הכרעת §7 עצמה תמיד לפי כלל 1 (ישי/עמית, לא הסוכן).

</div>
