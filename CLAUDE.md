<div dir="rtl">

# CLAUDE.md — כללי הברזל של REG-IN

> קובץ זה נטען אוטומטית בכל סשן Claude Code בריפו הזה. **קרא את `STATUS.md` מיד אחריו** — הוא לוח המצב היחיד (איפה עומדים, מה השלב הבא של כל מפתח).

## מה זה REG-IN
מערכת ניהול עסקית מודולרית לחברת אירועים/כנסים (לקוחות, הצעות מחיר, דיילות + Smart Match, לוגיסטיקה, פרויקטים, כספים, דו"חות). שני מפתחים: **ישי** (בעל הריפו; מודולים 0,1,4,5,9,10) ו**עמית** (מודולים 2,3,7,8,11); מודולים 6,12 משותפים.

**Stack:** Vite 8 + React 19 · JavaScript (לא TypeScript) · Tailwind 4 + shadcn/ui + Radix · Supabase (Auth + Postgres 17) · עברית RTL מלאה.
**עובדות סביבה:** alias `@/` → `src/` · לקוח Supabase: `src/supabaseClient.js` → מיובא כ-`@/supabaseClient` (⚠️ לא `@/lib/`) · פרויקט Supabase: `Reg-In` (ref `yfeovxppnfoafmfbdfvh`, אזור `eu-west-3`) · שרת פיתוח: `npm run dev` (פורט 5173) · Windows + PowerShell.

## היררכיית מקורות אמת (בסתירה — הגבוה קובע)
1. **`docs/schema.sql`** — סכמת ה-DB (snapshot; מקור-אמת לשינויים = `supabase/migrations/`).
2. **האפיון המאושר, פרקים 5–6** (`docs/reference_spec/C5_…` + `C6_…`) — הלוגיקה העסקית והמסכים. **קבצים קפואים — לא עורכים לעולם**; סטייה מהאפיון נרשמת בתיעוד החי בלבד (PROJECT_MASTER, מדריך המודול, CHANGELOG) עם הערת "סטייה מ-5.x".
3. **המוקאפים** (`docs/mockups/`) — רפרנס ויזואלי בלבד.
4. **המדריכים** (`docs/guides/`, `docs/micro_guides/`) — מתכון לסדר העבודה. הכי נמוך.

## מפת המסמכים — מה לקרוא ומתי
| קובץ | תפקיד |
|---|---|
| `STATUS.md` | **לוח מצב יחיד** — סטטוס מודולים + השלב הנוכחי של כל מסלול. לקרוא בכל סשן |
| `README.md` | דף הבית: מי זה מי, הפרומפטים, מפת התיעוד |
| `docs/guides/00_roadmap.md` | מפת הדרכים: המודולים, התלויות, אבני הדרך, הלולאה לכל מודול |
| `docs/guides/ishay/` · `docs/guides/amit/` · `docs/guides/shared/` | מדריכי השלבים לכל מסלול |
| `docs/PROJECT_MASTER.md` | האפיון המסונתז + **§7 = רישום השאלות הפתוחות הממוספר (יחיד!)** |
| `docs/CLAUDE_CODE_LOG.md` | יומן הסשנים של Claude (נרטיב, "למה", tech-debt) |
| `docs/CHANGELOG.md` | יומן שינויים מתוארך (DB+קוד) לתיאום בין שני המפתחים |
| `docs/claude_routines.md` | ההגדרות הקנוניות של 4 הרוטינות + פרוטוקול עדכון |
| `docs/architecture_and_qa_roadmap.md` | סטנדרט הנדסי + Definition of Done |

## כללי ברזל (Rules of Engagement)
1. **שאל שאלות הבהרה לפני עבודה** כשמשהו לא חד-משמעי — אל תניח. שאלות פתוחות מוכרעות **רק עם ישי/עמית** — לעולם לא לבד (הרשימה: PROJECT_MASTER §7).
2. **Plan Mode:** לתכנן ולקבל אישור לפני ביצוע; במצב תכנון לא נוגעים בקבצים.
3. **הערות בעברית, "why-first":** להסביר כוונה אדריכלית ומקרי-קצה, לא תחביר. כל לוגיקה עסקית בקוד (נוסחה, כלל סטטוס, אכיפת הרשאה) מקבלת הערה שמסבירה אותה — בלי לחכות שיבקשו.
4. **לצטט קובץ+שורה**, לא לסמוך על הזיכרון.
5. **לא לשכתב/למחוק טבלאות קיימות.** שינויי DB רק דרך migration בעל שם (ראו פרוטוקול DB למטה).
6. **לעולם לא לקמט סודות** (`.env.local`, סיסמאות) ל-Git ולא להדפיס אותם במדריכים/צ'אט.
7. **לוודא בפועל (verify):** להריץ את האפליקציה/בדיקות ולראות שהשינוי עובד.
8. **עיצוב — לא ממציאים:** שפת העיצוב וטבלת הצבעים ב-`docs/PROJECT_MASTER.md` §4 (פרימרי teal `#14B8A6`, רקע `#F8FAFC`, סרגל צד ימני וכו') **אושרו ע"י ישי (06/07/2026) ומחייבות את כל המסכים**. אין לשנות צבעים/פריסה בלי אישור ישי.
9. **מודל אבטחה:** כל מודול חדש מצהיר במפורש איך הוא נשען על תשתית ה-Auth של מודול 1 (RLS, שער תפקיד/הרשאה, session/OAuth). בסגירת מודול בודקים רגרסיה גם על מודל האבטחה.
10. **Git:** לעולם לא לדחוף ל-`main`/`dev` ישירות ולא `--force`. עבודה רק על ענף אישי `שם/module-N-…`. שני המפתחים חולקים פרויקט Supabase אחד — **כל שינוי סכמה מתואם מראש**.
11. **קבצים חדשים:** שמות באנגלית בלבד (עברית בשמות קבצים נשברת ב-Git/Windows); תוכן עברי בתוך `<div dir="rtl">`.
12. **בנייה בעצירות:** כל צעד מסתיים ב-🔻 נקודת עצירה שנבדקת לפני שממשיכים. DB/RLS קודם, UI אחר-כך.
13. **פרוטוקול שינוי מהותי (אדוות):** התקבלה החלטה שמשנה סכמה / פריט §7 / תהליך עבודה? מעדכנים **באותו סשן, לא "אחר כך"**: ‏(א) `PROJECT_MASTER.md` — הסעיף הרלוונטי + סימון פריט ה-§7 כמוכרע ‏(ב) כל מדריך שמצטט את הפריט (חיפוש `§7.N` על `docs/guides/`) ‏(ג) `STATUS.md`+`CHANGELOG.md` לפי פרוטוקול סוף-סשן ‏(ד) בסיום — להריץ את רוטינת `regin-docs-sync` לאימות שאין סתירות שנשארו.
14. **SSOT ללוגיקה עסקית:** כל חישוב עסקי (תמחור, מע"מ, הנחות, Smart Match, שכר, רווח גולמי) נכתב **פעם אחת** במודול ב-`src/lib/` (למשל `src/lib/pricing.js`) עם בדיקות יחידה לצידו; קומפוננטות UI רק מייבאות — לעולם לא משכפלות נוסחה. ממודול 2 והלאה: שאילתות ה-Supabase של מודול מרוכזות בקובץ `api.js` בתיקיית המודול (`src/modules/NN_name/api.js`), לא מפוזרות בתוך קומפוננטות.
15. **מדריך מיקרו חי:** `docs/micro_guides/module-N.md` (אנגלית, כתוב ל-Claude) הוא מפת הבנייה של המודול הפעיל — **מעדכנים אותו תוך כדי העבודה, לא בדיעבד**: מעבר צעד → עדכון כותרת המצב וטבלת הצעדים באותו סשן; סטייה מהתוכנית → הערת "↳ as-built" בצעד + שורה ביומן הסטיות שלו. נקודות עצירה 🔻 מתויגות **🤖** (Claude מאמת לבד בפקודה+ראיה וממשיך) או **👤** (עצירת ישי/עמית — סופי פזות, הכרעות §7, החלת מיגרציות, סודות/OAuth, חתימת DoD). ה-Stop hook חוסם סיום סשן אם השתנה קוד תחת `src/modules/NN_*/` בלי שמדריך המיקרו של אותו מודול עודכן אחריו.

## פרוטוקול DB (חובה — חלק מ-Definition of Done)
- מתעדים רק DDL / פונקציות / טריגרים / RLS. לא INSERT/UPDATE של בדיקה (Seed של roles/modules/params הוא היוצא-מן-הכלל המותר).
- **כל שינוי DB = migration בעל שם** ב-`supabase/migrations/` עם כותרת "why" בעברית.
- **אחרי כל migration:** ‏(1) להריץ אותה (CLI או Supabase Studio) ‏(2) לוודא שהוחלה ‏(3) לעדכן את `docs/schema.sql` — הדרך הקלה: Supabase SQL Editor → **SQL → Snapshots → "Generate schema SQL"** → להעתיק ל-`docs/schema.sql` ‏(4) commit של ה-migration + `docs/schema.sql` **ביחד**:
  ```bash
  git add supabase/migrations/…_name.sql docs/schema.sql
  git commit -m "db: [שם המיגרציה] + עדכון schema snapshot"
  ```
- ⚠️ migration שמגיעה ל-PR בלי `docs/schema.sql` מעודכן — לא approved.
- **טבלה חדשה בשימוש ראשון:** לכל 11 הטבלאות העסקיות RLS מופעל עם אפס policies (deny-all מכוון). מסך ריק מול טבלה חדשה = כנראה חסרות policies — כל מודול מוסיף את ה-policies שלו במיגרציה, לפי מודל הבעלות שהוכרע ב-§7.

## פרוטוקול סוף סשן (הסדר מחייב; ה-hook חוסם סיום בלעדיו)
0. **מדריך המיקרו הפעיל** — אם עבדת לפי `docs/micro_guides/module-N.md`: עדכן בו את כותרת המצב, טבלת הצעדים והסטיות **לפני** היומנים (כלל ברזל 15; ה-hook אוכף גם את זה).
1. **`docs/CHANGELOG.md`** — אם השתנה DB או קוד: שורה מתוארכת (`סוג DB/קוד · מי · מה · מודולים מושפעים`).
2. **`docs/CLAUDE_CODE_LOG.md`** — "מצב נוכחי" נכתב-מחדש (snapshot, לא append) + רשומת סשן מתוארכת בראש היומן.
3. **`STATUS.md`** — עדכון שורת המודול/השלב אם השתנה סטטוס; אם לא — לוודא שהלוח עדיין נכון ולעדכן את שורת "עודכן לאחרונה".

**חלוקת אחריות (בלי כפילות):** `STATUS.md` = מצב-עכשיו בלבד (לוח, בלי היסטוריה) · `CHANGELOG` = שורות מתוארכות לתיאום · `CLAUDE_CODE_LOG` = נרטיב/למה/tech-debt · `PROJECT_MASTER §7` = רישום השאלות הפתוחות היחיד (מדריכים מצטטים מספרי פריטים, לא משכפלים טקסט). **רשומות יומן מתוארכות לא משכתבים לעולם** — גם אם הן מזכירות קבצים שכבר לא קיימים.

## עבודה לפי מדריך
כשישי או עמית מדביקים פרומפט של שלב — קרא קודם: `CLAUDE.md` (זה), `STATUS.md`, ואת מדריך השלב שצוין. לכל מודול **שלושה פרומפטים** במדריך השלב (סעיף ⑥): **פתיחה** (מפעיל את `docs/templates/create_micro_guide_template.md` → בלופרינט machine-first שנשמר, אחרי אישור, כ-`docs/micro_guides/module-N.md`) · **המשך-בנייה** (קורא את מדריך המיקרו וממשיך מ"הצעד הפעיל") · **סגירה** (מפעיל את `docs/templates/create_module_final_test_template.md` → אודיט + התמדה לתיעוד + הוראות PR). **חלוקת שפות:** מדריכי `docs/guides/` בעברית — לבני אדם; `docs/micro_guides/` והטמפלטים באנגלית — ל-Claude (עברית רק כדאטה: שמות מודולים/תפקידים, מחרוזות UI); דיווחים בצ'אט לישי/עמית — תמיד בעברית.

**כלל מודלים:** Opus כדי לחשוב (בלופרינט, RLS, אבטחה, סגירה — מאמץ גבוה) · Sonnet כדי לייצר (UI, טפסים, ניתוב — מאמץ בינוני).

</div>
