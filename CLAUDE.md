<div dir="rtl">

# REG-IN — מדריך הפעלה הנדסי ופנקס אינווריאנטים (Operational Runbook)

> **מערכת ניהול עסקית מודולרית לחברת אירועים וכנסים**  
> **מפתח יחיד:** ישי | **סטאק:** Vite 8 + React 19, JavaScript (ללא TypeScript), Tailwind CSS v4, shadcn/ui + Radix, Supabase (Auth + PostgreSQL 17), react-router-dom 7, עברית RTL מלאה.  
> **מפת קוד מלאה וארכיטקטורה:** [docs/CODE_MAP.md](docs/CODE_MAP.md)

---

## 1. שערי איכות ופקודות הרצה מקומיות (Quality Gates & Commands)

| פקודה | תפקיד ופירוט | סף מעבר מחייב |
|---|---|:---:|
| `npm run verify` | אימות בסיסי: Lint + Prettier + בדיקות יחידה + Build | 100% ירוק |
| `npm run gate` | שער איכות מלא: `verify` + שכפול קוד (`jscpd`) + קוד מת (`knip`) + פטורי אבטחה (`audit`) + תווי Bidi + בדיקת ארכיטקטורת הקשר (`check:context`) + בדיקת מבנה מסמכים (`check:docs-structure`) | 0 שגיאות |
| `npm run test:run` | הרצת סוויטת בדיקות יחידה מלאה (Vitest, 2,400+ בדיקות) | 100% ירוק |
| `npm run lint` | בדיקת תקני קוד (ESLint 10 + SonarJS, רף מורכבות קוגניטיבית 20) | 0 אזהרות/שגיאות חוסמות |
| `npm run format:check` | בדיקת עיצוב קוד וסופי שורה (Prettier, הזנת LF בלבד) | 0 קבצים לא מעוצבים |
| `npm run build` | קומפילציית בנדל ייצור של Vite | מעבר נקי |
| `npm run check:context` | אימות שלמות עץ ה-`CLAUDE.md` והפלאגינים הפעילים | 0 בעיות |
| `npm run check:docs-structure` | בדיקת רצף כותרות וספירות משטחים ב-`docs/guides/` ו-`docs/specs/` | 0 ממצאים |
| `npm run smoke` | בדיקת עשן חיה במסע לקוח מול שרת dev מקומי (פורט 5173) | קוד יציאה 0 |

---

## 2. חוקי ברזל ארכיטקטוניים ואינווריאנטים (Architectural Invariants)

1. **נתיב לקוח Supabase:** ייבוא אך ורק מ-`@/supabaseClient` (הקובץ יושב פיזית ב-`src/supabaseClient.js`). לעולם לא `@/lib/supabaseClient`.
2. **מסד נתונים יחיד (Single DB Environment):** מסד הנתונים של Supabase (`yfeovxppnfoafmfbdfvh`) משרת הן את הפיתוח והן את האתר החי. **המסד תמיד מקדים את הקוד שנפרס או שווה לו.**
3. **פרוטוקול שינויי DB (Expand-Contract):**
   - הוספת טבלאות/עמודות/פונקציות/אינדקסים וריכוך אילוצים: **מותר תמיד**.
   - מחיקת עמודות/טבלאות או הידוק אילוצים (`set not null` / שינוי חתימה): **רק לאחר שהקוד הישן נפרס לייצור ואינו קורא עוד את השדה**.
4. **שער המיגרציות הבלתי-הפיך:** החלת מיגרציה דרך MCP (`apply_migration`) מתבצעת אך ורק לאחר הסבר מלא בעברית ואישור מפורש של ישי בצ'אט (פירוט מלא: [supabase/migrations/CLAUDE.md](supabase/migrations/CLAUDE.md)).
5. **ספירת שורות אמינה ב-Windows:** יש להשתמש ב-`(Get-Content -LiteralPath X).Count`. לעולם לא `Measure-Object -Line` (המדלג על שורות ריקות ומזייף ספירות).
6. **סופי שורה LF בלבד:** הריפו כולו מוגדר כ-LF (`.gitattributes`). כתיבת קובץ מפייתון או כלים חיצוניים ב-Windows חייבת להגדיר `newline='\n'` במפורש כדי למנוע הזרקת CRLF שמפילה את שער Prettier.
7. **שמות קבצים באנגלית בלבד:** שמות קבצים חדשים נכתבים באותיות לטיניות בלבד. תוכן עברי ייכתב תמיד תחת עטיפת `<div dir="rtl">`.
8. **שפת עיצוב קבועה:** צבע ראשי Teal `#0F766E`, רקע דפים `#F8FAFC`, סרגל צד ימני קבוע. אין להמציא צבעים (סגול/כתום אינם בשימוש).
9. **היררכיית מקורות אמת:**
   - רמה 1: `docs/schema.sql` (סכמת מסד נתונים פעילה).
   - רמה 2: האפיון המאושר של המודול (`docs/specs/module_NN_*/`).
   - רמה 3: האפיון הקפוא (`docs/reference_spec/C5_clean_transcript.md`) — **רשימת כיסוי ובקרה בלבד** ("מה שכחנו?"), אינו סמכות מוצרית מחייבת.
   - רמה 4: מוקאפים (`docs/mockups/`) — רפרנס חזותי בלבד.
   - רמה 5: מדריכים (`docs/guides/`).

---

## 3. פנקס מלכודות שקטות (Silent Traps Register)

| מלכודת שקטה | מדוע הבאג שורד קומפילציה ובדיקות | התוצאה והפתרון ההנדסי המחייב |
|---|---|---|
| **זיהום CRLF בעריכת קבצים** | `git status` ו-`git diff` נקיים לחלוטין (עקב נרמול מקומי), אך `npm run format:check` נופל ב-CI | כתיבה ב-LF בלבד. אימות כמות בתי CR בקובץ: חייב להיות 0 |
| **Utilities לוגיות ב-Tailwind RTL** | כתיבת `ms-60` או `ps-4` נראית תקינה ב-LTR, אך מכיוון ש-`<html dir="rtl">` מוגדר, ה-offset נופל על ימין ומחביא תוכן מתחת לסרגל | שימוש אך ורק ב-utilities פיזיות הפוכות: `mr-60`, `pr-4`, `right-0` |
| **דריסת התאמות RTL ב-shadcn** | `components.json` מוגדר `"rtl": false`. הרצה מקרית של `npx shadcn add` דורסת רכיבים ומחזירה אותם ל-LTR ללא שגיאה | כל רכיבי `src/components/ui/` ערוכים ידנית ל-RTL. אין להריץ `shadcn add` ללא ביקורת ידנית |
| **טעינת משתנה סביבה מקומי מול CI** | קובץ `.env.local` מקומי מספק `VITE_SUPABASE_URL`. ב-CI אין קובץ כזה | בדיקות יחידה הנוגעות ברכיבים חייבות לבצע `vi.mock('@/supabaseClient')` כדי לא לקרוס ב-CI |
| **משטח צף שנסגר ב-`blur`** | לחיצה ב-Playwright עם `click()` רגיל מעבירה פוקוס וסוגרת את הרשימה בטיימר לפני שהאירוע נתפס | בדיקות Playwright למשטח צף משתמשות ב-`mouse.down()` ➔ המתנה ➔ `mouse.up()` |
| **כותרות לא עוקבות במסמכים** | עריכת מסמך ב-`docs/guides` או `docs/specs` עם כותרת `## 3` פעמיים או בסדר הפוך עוברת עריכה ידנית אך מפילה את `check:docs-structure` | כותרות `## (\d+)` חייבות להיות עוקבות, ייחודיות ומוגדרות היטב |

---

## 4. מוסכמות קוד ודפוסי שכבה (Conventions & Layer Patterns)

1. **הפרדת רשויות:**
   - קומפוננטות UI (`src/modules/` ו-`src/components/`) אינן מבצעות קריאות ישירות ל-`supabase.from` (למעט מודול 1 ההיסטורי). כל הגישה לרשת עוברת דרך `api.js` המודולרי.
   - לוגיקה עסקית מורכבת (תמחור, שיבוץ, שכר) יושבת ב-`src/lib/` כפונקציות טהורות הנבדקות ב-Vitest.
2. **ניהול מצב ב-URL (URL State):**
   - סינונים, חיפוש טקסטואלי, טאבים פתוחים ועמודי פאגינציה במסכי הרשימה נשמרים ב-URL Search Params (ולא ב-`useState`) דרך רכיב `ListWindow` או פונקציות עזר.
   - עדכון מצב ב-URL מחייב תמיכה ב-Functional Setter (`set(prev => ...)`).
3. **מצבי טעינה תלת-ערכיים (Tri-State Data Loading):**
   - `null` = טרם ידוע / בטעינה.
   - `[]` או `{}` = נטען בהצלחה, ריק מנתונים.
   - מערך/אובייקט עם ערכים = נתונים קיימים.
   - **איסור מוחלט:** לעולם אין להמיר שגיאת רשת ל-`{}` או `[]` ריק — זה מציג למשתמש מצג שווא כאילו "אין נתונים" במקום להציג באנר שגיאה עם "נסי שוב".

---

## 5. עוגני קבצים קריטיים (Critical Layer Anchors)

- **מפת קוד וארכיטקטורה:** [docs/CODE_MAP.md](docs/CODE_MAP.md)
- **מוקשי קוד, React ו-RTL:** [src/CLAUDE.md](src/CLAUDE.md)
- **פרוטוקול תיעוד ואדוות:** [docs/CLAUDE.md](docs/CLAUDE.md)
- **פרוטוקול בסיס נתונים ומיגרציות:** [supabase/migrations/CLAUDE.md](supabase/migrations/CLAUDE.md)
- **מוקשי בדיקות ו-E2E:** [e2e/CLAUDE.md](e2e/CLAUDE.md)
- **לוח מצב פעיל יחיד:** [STATUS.md](STATUS.md)

---

## 6. כללי הברזל — אינדקס וכתובות (Iron Rules Index)

> **‏17 כללי-הברזל אינם כתובים כאן במלואם — הם מנותבים.** ‏🔴 **והטבלה הזו אינה נוחוּת:** ‏**400+ מקומות בריפו ו-11 סקילים מצטטים אותם לפי מספר** *(`iron rule 13` ×41 · `iron rule 1` ×32 · `iron rule 10` ×31 · `iron rule 15` ×28)*, למשל `module-build/SKILL.md`: *"Full wording: iron rule 10 in root `CLAUDE.md`."* **מספר בלי כתובת הוא מצביע מת, והוא נכשל בשקט** — הסשן פשוט ממציא.

| # | הכלל | היכן הוא בתוקף |
|:-:|---|---|
| **1** | שאל כשלא חד-משמעי — **ההכרעה תמיד של ישי** | `~/.claude/CLAUDE.md` *("He is the product manager")* |
| **2** | Plan Mode בפתיחה · סגירה · §7 | `docs/CLAUDE.md` · הסקילים |
| **3** | הערות "why-first" בעברית | `src/CLAUDE.md` |
| **4** | לצטט קובץ+שורה, לא מהזיכרון | `~/.claude/CLAUDE.md` · `_shared/discipline.md` |
| **5** | שינויי DB | `supabase/migrations/CLAUDE.md` |
| **6** | לא לקמט סודות | §2 כאן · `gitleaks` בשער |
| **7** | לוודא בפועל | `~/.claude/CLAUDE.md` *("actually run the thing")* |
| **8** | עיצוב — לא ממציאים | §2.8 כאן · `PROJECT_MASTER §4` |
| **9** | מודל אבטחה | `module-blueprint/template.md` · `module-close/template.md` |
| **10** | **Git — שער קשיח** | **כאן, במלואו, למטה** |
| **11** | שמות קבצים באנגלית | §2.7 כאן |
| **12** | DB/RLS קודם, UI אחר-כך | מדריך-המיקרו של המודול *(נקודות 🔻)* |
| **13** | פרוטוקול אדוות | `docs/CLAUDE.md §4` |
| **14** | SSOT ללוגיקה עסקית | `src/CLAUDE.md` |
| **15** | מדריך-מיקרו חי + חוב 🚧 | ה-Stop hook חוסם ומסביר · `module-build` |
| **16** | שיחה כותבת אחת בכל רגע | `_shared/parallel-sessions.md` |
| **17** | פרומפט מסירה 🧩 | `_shared/writing-prompts.md` |

### כלל 10 — Git, שער קשיח *(היחיד שכתוב כאן במלואו: הוא גדר, לא הפניה)*

לעולם לא לדחוף ל-`main`/`dev` ישירות ולא `--force`. עבודה רק על ענף אישי `ishay/<נושא>`.
✏️ **מיזוג ל-`dev` — הכרעת-ישי 10/09/2026:** מותר בהרשאה **מפורשת, בזמן-אמת, בגוף-ראשון, בצ'אט של
אותה שיחה** — לא רמז ולא הנחיה כללית מראש. ⚠️ **החריג חל על `dev` בלבד.** ‏`--force` **וארכוב/סגירת-סשן
נשארים אסורים במלואם גם בהוראה מפורשת** *(01/08/2026: הוראה נקראה כהרשאה ושלושה סשנים אורכבו)*.
🔴 **קומיט תמיד בפאת'ספק מפורש — `git commit -- <נתיבים>`, לעולם לא `git add -A`:** האינדקס משותף,
וקימוט קובץ אחד סוחף שורות של סשן אחר *(קרה: 9 קבצים זרים כולל מיגרציה)*.
**לפני כל קומיט נוסף אחרי push:** ‏`git fetch origin` + `git merge-base --is-ancestor HEAD origin/dev`.
🔴 **הסייג:** הבדיקה מצליחה **גם על ענף טרי בלי קומיטים.** המבחן שמפריד: `git log origin/dev..HEAD`
— **ריק ⇒ ענף טרי · לא-ריק ⇒ באמת מוזג**, ואז לא לערום על ענף מת.

</div>
