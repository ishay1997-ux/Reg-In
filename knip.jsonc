{
  "$schema": "https://unpkg.com/knip@6/schema.json",
  "project": ["src/**/*.{js,jsx}"],
  "ignore": [
    "src/components/ui/**",
    // ⏸️ מודול 3, פזה 2 (29/07/2026) — שני קבצי-API נבנו לפי כלל-הברזל 12 (DB/RLS קודם,
    // ‏UI אחר-כך): גוף-כתוב-ובדוק, אבל בלי מסך שמייבא אותם עדיין. זו לא מתה — knip פשוט
    // לא יכול להבחין בין "ייבוא עתידי ידוע" ל"נשכח". הכרעת-ישי 29/07 (הוצג לו הכשל +
    // 2 אפשרויות, ביקש שיקול-דעת ארכיטקטוני): פטור נקודתי ומתועד, לא השתקת-השער כולו.
    // 🔁 הסרה: ברגע שפזה 3 מייבאת בפועל — step 2.3 (getPricingCatalog וכו') ב-3.2/3.3,
    // ‏pricesApi.js ב-3.6 (docs/micro_guides/module-3.md). knip יחזור לבדוק אותם כרגיל.
    // ✅ 29/07/2026 — `src/modules/03_quotes/api.js` הוסר מהרשימה: צעד 3.2 מייבא ממנו בפועל
    //    (createQuote/saveQuoteEdit/getQuote/getPricingCatalog). ‏pricesApi.js נשאר עד צעד 3.6.
    "src/modules/01_auth/pricesApi.js",
  ],
}
