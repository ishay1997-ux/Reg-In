<div dir="rtl">

# מצביע-תיקייה (נטען אוטומטית כשעובדים כאן) — ה-SSOT: כללי הברזל ופרוטוקול ה-DB ב-`CLAUDE.md` שבשורש

- **append-only:** מיגרציה שכבר קומטה = היסטוריה — לעולם לא עורכים/מוחקים אותה (ה-hook חוסם). תיקון = מיגרציית-תיקון חדשה קדימה (פרומפט P15).
- כל קובץ חדש: שם-מתוארך + **כותרת "why" בעברית** בראשו.
- אחרי החלה: לעדכן את `docs/schema.sql` (snapshot) ולקמט **ביחד** עם המיגרציה — פרוטוקול ה-DB בשורש.
- פרויקט Supabase **משותף** לשני המפתחים: כל שינוי סכמה מתואם מראש (כלל 10) + שער-👤 לפני החלה.
- ‏policies חדשות: לפי תבנית §7.21 (‏PROJECT_MASTER) עם עטיפת `(select …)`.
- **לפני תכנון מיגרציה חדשה:** לקרוא `docs/db_roadmap.md` — צ'קליסט-העיצוב (§1) + השורות הממתינות לטבלה שנוגעים בה (§6). אחרי החלה: לעדכן שם את השורה **באותו סשן** (ה-Stop hook אוכף מיגרציה-בלי-עדכון-מפה).

</div>
