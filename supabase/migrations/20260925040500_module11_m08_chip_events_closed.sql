-- why: בודק-ניסוח טרי, 25/09/2026 (tonight-copy-review.md, ממצא 0-2); הכרעת הסגן: תיקון-אמת במסד הלילה.
--      השבב של מ8 אומר *"‹244› פרויקטים שנסגרו"* (נכתב ב-20260925010100), ובשורת-האוכלוסייה שמתחתיו הרשימה כוללת
--      "ממתין לסגירה" — כלומר **פרויקטים** שעוד לא נסגרו. מה שנסגר הוא ה**אירוע**: ההגדרה באותו דף אומרת
--      *"אירוע שנסגר = התקיים לפני יותר משבוע ולא בוטל"*, ושורת-האוכלוסייה פותחת ב"אירועים שנסגרו". כמו במ2 ובמ12.
-- what: החלפה אחת, "בדיוק פעם אחת", אותה חתימה ⇒ ה-ACL נשמר: השבב ⇐ "‹N› אירועים שנסגרו" / "אירוע אחד שנסגר".
--       🚫 לא נוגע: שורת-האוכלוסייה (בה "ממתין לסגירה" לצד "שנסגרו" — הכרעת ישי, לפי הבודק) · הספירות · הערות-השוליים.
-- 📏 הגוף החי לפני: md5 `aa86b9d7d0b6bbf0a55f58eda7982c25`, ‏22,243 תווים.
-- 📏 הגוף הצפוי אחרי: md5 `2d443b5cd84ca8b10e4026553acf4f2c`, ‏22,360 תווים — בלוק-ניסיון משורשר (25/09): שלושת הקבצים (040300 · 040400 · 040500) ב-EXECUTE בתוך DO אחד שמסתיים ב-raise ⇒ גלגול;
--    md5 כל בלוק נבדק במסד מול הקובץ; ‏ACL של שלוש הפונקציות זהה לפני/אחרי; קריאה כמנכ"ל לפני/אחרי: השבב "⁦244⁩ פרויקטים
--    שנסגרו" ⇐ "⁦244⁩ אירועים שנסגרו"; כל שאר הפלט זהה.
-- reversible: כן — ההחלפה ההפוכה. אין שינוי-סכמה ואין נתונים.
-- ⏳ נכתב, לא הוחל — הסגן מחיל.

do $m08e$
declare
  v_oid oid;
  v_def text;
  v_old text := $o$      'summary', case when v_n = 1 then 'פרויקט אחד שנסגר'
                      else v_lri || v_n || v_pdi || ' פרויקטים שנסגרו' end,$o$;
  v_new text := $n$      -- ✏️ 25/09/2026 (בודק-ניסוח 0-2): "אירועים" ולא "פרויקטים" — ברשימה יש "ממתין לסגירה"; האירוע נסגר, לא הפרויקט.
      'summary', case when v_n = 1 then 'אירוע אחד שנסגר'
                      else v_lri || v_n || v_pdi || ' אירועים שנסגרו' end,$n$;
begin
  select p.oid into strict v_oid from pg_proc p join pg_namespace n on n.oid = p.pronamespace
   where n.nspname = 'public' and p.proname = 'report_m08_profitability';
  v_def := pg_get_functiondef(v_oid);
  if (length(v_def) - length(replace(v_def, v_old, ''))) <> length(v_old) then
    raise exception 'm08e: summary chip segment not exactly once'; end if;
  v_def := replace(v_def, v_old, v_new);
  execute v_def;
end $m08e$;
