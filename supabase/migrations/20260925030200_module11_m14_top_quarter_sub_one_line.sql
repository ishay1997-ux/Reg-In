-- why: הכרעת הסגן, 25/09/2026 ~03:2X, בעקבות כשל בדיקת-הצפיפות (`e2e/report-density.spec.js`, מצב 0, ענף-האיחוד):
--      *"מ14 · רבע הדיילות העמוסות · שורה אחת · מהמשמרות · בחלוקה שווה 25%"* — שורת-המשנה של האריח נשברת לשתי שורות.
--      הכלל של ישי (02:2X): הסבר = רמז של מצב ההטמעה, לא טקסט במסך הרגיל.
-- what: החלפה אחת, "בדיוק פעם אחת", אותה חתימה ⇒ ה-ACL נשמר. שורת-המשנה: "מהמשמרות" בלבד.
--       ‏"בחלוקה שווה 25%" עובר לרמז `reports.hostessOverview.topQuarter` (מצב 2, `src/lib/onboardingCopy.m11.hostesses.js`),
--       שכבר מסביר "בחלוקה שווה כל רבע מהדיילות מקבל רבע מהמשמרות" — נוסף לו המספר 25%.
-- 📏 סדר ובסיס: תיקון-קדימה אחרי 20260925010800 (בונה מ12, f7ae26a6). לפי סדר-השמות הקובץ רץ אחרי 20260925010350,
--    ולכן הבסיס שלו בפועל הוא 010300 ⇐ 010800 ⇐ 010350 (md5 `af0ecb180488e715e4a65aaf88a47174`, כפי שכתוב ב-010800).
--    הקטע שמוחלף כאן לא נגוע באף אחד מהשלושה, ולכן הקובץ תקין גם על `eae6970dd391098550ac4240bbcf0272` (בלי 010350).
--    📏 בלוק-ניסיון משורשר (25/09 ~03:3X, DO אחד שמריץ את הקבצים ב-EXECUTE ומסתיים ב-raise; אחריו נבדק שהגופים החיים לא זזו): חי `489f97eb` ⇐ 010300 = `4fbd074d` ⇐ 010800 = `eae6970d` ⇐ 010350 = `af0ecb18` (שלושתם כפי שכתוב
--    בכותרות הבונה) ⇐ הקובץ הזה = md5 `7e9dc99bd2522bad8dff159e335a0316`, ‏32,273 תווים (הצפוי, לפי סדר-השמות).
--    על בסיס `eae6970d` (בלי 010350) הצפוי הוא `092b22fb62ad4071649704ffddf3c689` (חושב על הטקסט; הקטע נמצא בדיוק פעם אחת).
-- reversible: כן — ההחלפה ההפוכה. אין שינוי-סכמה ואין נתונים.
-- ⏳ נכתב, לא הוחל — הסגן מחיל, אחרי 010300 · 010800 · 010350.

do $m14q$
declare
  v_oid oid;
  v_def text;
  v_old text := $o$        'sub', 'מהמשמרות · בחלוקה שווה ' || v_lri || '25%' || v_pdi),$o$;
  v_new text := $n$        -- ✏️ 25/09/2026 (הכרעת הסגן): שורה אחת. "בחלוקה שווה 25%" — ברמז reports.hostessOverview.topQuarter.
        'sub', 'מהמשמרות'),$n$;
begin
  v_oid := 'public.report_m14_hostess_overview(date,date,integer,jsonb)'::regprocedure;
  v_def := pg_get_functiondef(v_oid);
  if (length(v_def) - length(replace(v_def, v_old, ''))) <> length(v_old) then
    raise exception 'm14q: top-quarter sub segment not exactly once'; end if;
  v_def := replace(v_def, v_old, v_new);
  execute v_def;
end $m14q$;
