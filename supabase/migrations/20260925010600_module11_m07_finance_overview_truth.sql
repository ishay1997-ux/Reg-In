-- why: הכרעת הסגן, 25/09/2026 ~01:3X (סבב תיקוני-אמת), מילה-במילה: *"יחיד/רבים: … מ7. כל מספר שיכול להיות 1."* ·
--      *"כל שורה היא דוגמה לכלל, לא תקרה. מצאת עוד משפט באותו דוח שאומר דבר לא נכון? הוא בפנים."*
--      📏 נמדד 25/09 בגוף החי (`report_m07_finance_overview`):
--         · שורות 340–341, 346: ‏"מתוך ‹N› אירועים" — גם כש-N = 1 (אריח "פרויקטים שחרגו מהתקציב החודש" והערת ההשוואה).
--         · שורות 306–307 (שורת-האוכלוסייה): "שני האריחים התחתונים נשענים על אוכלוסיית דוחות-הכסף: אירועים שהתקיימו
--           ויצאה להם חשבונית — …". אותה טענה שנמדדה שגויה במ8: הסטטוסים כוללים "ממתין לסגירה" ו"ממתין לחשבונית",
--           ול-31 מתוך 248 אין חשבונית. ובמ8 האוכלוסייה עוברת עכשיו ל"שנסגרו" (20260925010100) — ולכן גם "אוכלוסיית
--           דוחות-הכסף" כבר לא שם של אותו דבר.
-- what: שלוש החלפות "בדיוק פעם אחת" על הגוף החי, אותה חתימה ⇒ ה-ACL נשמר.
--       (1) שורת-האוכלוסייה: "…נשענים על אירועים שהתקיימו: … (חלקם טרם חויבו)."
--       (2) שורת-המשנה של "פרויקטים שחרגו מהתקציב החודש": "מתוך אירוע אחד" ביחיד.
--       (3) הערת-ההשוואה של אותו אריח: אותו דבר.
--       🚫 לא נוגע: מי נספר באריח. ⚠️ **ומדווח לסגן כשאלה, לא מתוקן:** האריח (שהדלת שלו מובילה למ8) סופר גם אירועים
--          מהשבוע האחרון, ואחרי 010100 מ8 כבר לא. אירוע בלי שעות אינו "חורג" (הסטייה שלו שלילית), אבל הוא נכנס למכנה.
-- 📏 הגוף החי לפני: md5 `8627ddc1de2f308867f1bb487709328d`, ‏24,588 תווים (25/09).
-- 📏 הגוף הצפוי אחרי: md5 `3d403c08a6b981944b46f310b2aa157a`, ‏24,848 תווים — בלוק-הניסיון (25/09, בין 01:40 ל-01:56, כמנכ"ל,
--    pg_temp, מתגלגל; md5 הבלוק נבדק במסד מול הקובץ). ערכי האריחים, הטבלה ושורת-"אז מה" — זהים לחי. שורת-האוכלוסייה
--    מסתיימת "…נשענים על אירועים שהתקיימו: … (חלקם טרם חויבו)." · האריח: "מתוך 25 אירועים", הערה "4 מתוך 18 אירועים"
--    (היום אין 1, ולכן הענף ביחיד לא נורה בנתונים — נבדק בקריאת הקוד בלבד).
-- reversible: כן — ההחלפות ההפוכות, או הגוף הקודם (md5 למעלה). אין שינוי-סכמה ואין נתונים.
-- ⏳ נכתב, לא הוחל — הסגן מחיל.

do $m07b$
declare
  v_oid oid;
  v_def text;
  -- (1) שורת-האוכלוסייה.
  v_old_pop text := $o$               || 'שני האריחים התחתונים נשענים על אוכלוסיית דוחות-הכסף: אירועים שהתקיימו '
               || 'ויצאה להם חשבונית — פרויקט הסתיים · ממתין לסגירה · ממתין לחשבונית · ממתין לתשלום.',$o$;
  v_new_pop text := $n$               -- ✏️ 25/09/2026: "ויצאה להם חשבונית" לא היה נכון (כמו במ8: 31/248 בלי חשבונית).
               || 'שני האריחים התחתונים נשענים על אירועים שהתקיימו: '
               || 'פרויקט הסתיים · ממתין לסגירה · ממתין לחשבונית · ממתין לתשלום (חלקם טרם חויבו).',$n$;
  -- (2) שורת-המשנה.
  v_old_sub text := $o$                    else 'מתוך ' || v_lri || v_dev_den || v_pdi || ' אירועים' end,$o$;
  v_new_sub text := $n$                    -- ✏️ 25/09/2026: יחיד כשהמספר 1.
                    when v_dev_den = 1 then 'מתוך אירוע אחד'
                    else 'מתוך ' || v_lri || v_dev_den || v_pdi || ' אירועים' end,$n$;
  -- (3) הערת-ההשוואה.
  v_old_note text := $o$          'note', v_lri || v_dev_prev_n || v_pdi || ' מתוך ' || v_lri || v_dev_prev_den || v_pdi || ' אירועים',$o$;
  v_new_note text := $n$          'note', v_lri || v_dev_prev_n || v_pdi || ' מתוך '
                  || case when v_dev_prev_den = 1 then 'אירוע אחד' else v_lri || v_dev_prev_den || v_pdi || ' אירועים' end,$n$;
begin
  select p.oid into strict v_oid from pg_proc p join pg_namespace n on n.oid = p.pronamespace
   where n.nspname = 'public' and p.proname = 'report_m07_finance_overview';
  v_def := pg_get_functiondef(v_oid);

  if (length(v_def) - length(replace(v_def, v_old_pop, ''))) <> length(v_old_pop) then
    raise exception 'm07b: population segment not exactly once'; end if;
  if (length(v_def) - length(replace(v_def, v_old_sub, ''))) <> length(v_old_sub) then
    raise exception 'm07b: over-threshold sub segment not exactly once'; end if;
  if (length(v_def) - length(replace(v_def, v_old_note, ''))) <> length(v_old_note) then
    raise exception 'm07b: compare-note segment not exactly once'; end if;

  v_def := replace(v_def, v_old_pop, v_new_pop);
  v_def := replace(v_def, v_old_sub, v_new_sub);
  v_def := replace(v_def, v_old_note, v_new_note);
  execute v_def;
end $m07b$;
