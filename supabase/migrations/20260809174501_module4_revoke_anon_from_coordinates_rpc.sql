-- =====================================================================================
-- מודול 4 · מיגרציה F — תיקון-קדימה: שלילת EXECUTE מ-`anon` על `set_project_coordinates`
-- =====================================================================================
--
-- why: מיגרציה E (`20260809172638`) כתבה `revoke execute … from public` והניחה שזה מספיק.
--   **זה לא הספיק, וזה נמדד מיד אחרי ההחלה:**
--   ‏`proacl` של הפונקציה יצא `{postgres=X, anon=X, authenticated=X, service_role=X}` —
--   כלומר ל-`anon` יש **הענקה מפורשת בשמו**, ולא דרך `PUBLIC`.
--
-- 🧨 **המוקש, והוא כללי לכל הפרויקט הזה ולא לפונקציה הזאת:** ‏Supabase מגדיר
--   `alter default privileges` שמעניק EXECUTE ל-`anon`/`authenticated`/`service_role` על כל
--   פונקציה חדשה ב-`public`. ⇒ **`revoke … from public` אינו נוגע בהן.** יש לשלול **בשם**.
--   📌 והתקדים היה בריפו כל הזמן: מיגרציה D כתבה על `enforce_hostess_min_wage`
--   `revoke execute … from public, anon, authenticated` — ושם `proacl` אכן יצא נקי
--   (`{postgres=X, service_role=X}`). מיגרציה E פשוט לא העתיקה את החלק הזה.
--
-- ⚠️ **מה זה *לא* היה — ונמדד, לא הונח:** זו **לא הייתה פרצה נצלת.** בדיקה בהתחזות ל-`anon`
--   (בלי claims, `set local role anon`, עם קואורדינטה פסולה בכוונה כדי שדבר לא ייכתב)
--   החזירה **`42501` — "אין הרשאה לעדכן קואורדינטות של אירוע"**, כלומר שער-ההרשאה שבתוך
--   הפונקציה חסם. הקוד השני (`22023`, בדיקת-הטווח) היה מעיד על פריצה, והוא לא הופיע.
--   ⇒ הפגם הוא **הגנה-לעומק שנשברה + אזהרת-יועץ שלא הוזמנה**, לא דלת פתוחה.
--
-- 🔴 ולמה מיגרציה חדשה ולא תיקון של E: הקבצים כאן **append-only** (כלל ברזל 5) — מיגרציה
--   שהוחלה היא היסטוריה, ותיקון הוא תמיד קדימה.
--
-- אימות-יועצים: הבסיס אחרי E נמדד **16** (הצפי היה 15 — הפער *הוא* התקלה הזאת).
--   אחרי F הצפי **15**: האזהרה `anon_security_definer_function_executable` על הפונקציה
--   הזאת נעלמת, וזו של `authenticated` **נשארת ובמכוון** — מנהלת הגיוס חייבת להריץ אותה.
--
-- reversibility: הפיכה — `grant execute … to anon` מחזיר את המצב הקודם.

revoke execute on function public.set_project_coordinates(integer, numeric, numeric) from anon;
