-- =============================================================================
-- N2ד — מחיקת שלוש עמודות-האב. **הצעד הבלתי-הפיך שסוגר את N2.**
-- =============================================================================
-- 🔴🔴🔴 **טרם הוחלה. נכתבה 02/09/2026 17:33 וממתינה לשער-ההקלדה של ישי.**
-- =============================================================================
--
-- 🩸 **בלתי-הפיכה, וזה נאמר ראשון כי זה מה שחשוב:** ‏`drop column` מוחק את הנתונים יחד עם
--    ההגדרה. **אין `undo`.** שחזור = מיגרציה שמוסיפה שלוש עמודות **ריקות**; הערכים עצמם
--    יאבדו. ⇒ **מה שמצדיק את זה אינו שהעמודות מיותרות אלא ש-`customer_contacts` מחזיקה את
--    אותם נתונים בדיוק** — נמדד רגע לפני הכתיבה: ‏9 לקוחות · 9 שורות-בת · 9 ראשיים ·
--    **0 לקוחות בלי ראשי · 0 אי-התאמות ערך-בערך מול האב.** אין מה לאבד, וזה נבדק ולא הונח.
--
-- 🔻 **ארבעת התנאים שכלל-הפריסה דורש — כולם נבדקו היום, ואף אחד מהם לא הונח:**
--   **‏① הקוד שנפרס אינו קורא מהן.** ‏`origin/main` נסרק: כל קריאה עוברת דרך
--      `primaryContact()` שב-`src/lib/customers.js`. ‏**החבילה החיה נמשכה מ-
--      `reg-in-umber.vercel.app` ואומתה** — ‏`customer_contacts(` · `is_primary` ·
--      `replace_customer_contacts` בפנים.
--   **‏② הקוד שנפרס אינו כותב אליהן.** ‏`payload.contact_name/phone/email` — **אינם בחבילה.**
--      🔑 **וזה התנאי שכמעט פספסנו:** ‏`N2ג` נולדה בדיוק מהגילוי ש"לא קורא" ≠ "לא כותב",
--      והשניים נפרדו **דווקא מפני שהחיווט נעשה היטב**. מחיקה לפני `N2ג` הייתה מפילה כל
--      שמירת-לקוח ב-`42703`.
--   **‏③ אף אובייקט במסד אינו תלוי בהן.** אומת ב-`pg_policy`/`pg_indexes`/`pg_proc`:
--      **אפס policies · אפס אינדקסים · אפס views.** ⚠️ **וסריקת-הפונקציות החזירה תחילה
--      התרעת-שווא** — ביטוי רחב מדי תפס את `auth.email()` ואת `u.email` של טבלת-המשתמשים
--      בתוך `approve_quote_and_create_project`. **בדיקה מדויקת: `contact_name` אינו מופיע
--      בגופה כלל, והיא אינה קוראת את `customers` בכלל.** *(נרשם כאן כי הפונקציה הזו כבר
--      שברה אישור-הצעה בשקט ב-09/08, ומי שיראה את שמה בהקשר של מחיקה יבהל בצדק.)*
--   **‏④ וגם חבילת-הבדיקות אינה תלויה בהן** — וזה המימד שכמעט הפיל את המחיקה:
--      ‏`e2e/load-failure-guards` הזריק את שלוש העמודות ב-`insert` ישיר, ו-`e2e/quote-email`
--      בחר מועמדים לפי `customers(email)`. **‏`grep` על `src/` בלבד היה מכריז "אפס שרידים"
--      והמחיקה הייתה מפילה את חבילת-הבדיקות.** שניהם תוקנו ונפרסו לפני המיגרציה הזו.
--
-- 🧭 **N2 נגמר בארבע מיגרציות, וזה סדר שאין לו קיצור:**
--      ‏`N2א` הוסיפה · `N2ב` ה-RPC · `N2ג` ריככה והפסיקה לכתוב **⇒ פריסה ⇒** `N2ד` מוחקת.
--    **המסד משותף לייצור ולפיתוח והקוד החי רץ מ-`main`** ⇒ כל דילוג שובר את המסך החי
--    **ברגע ההחלה, לא במיזוג.**
--
-- 🚫 **מה שהמיגרציה הזו אינה צריכה לעשות: העתקה-חוזרת.** כלל-הפריסה דורש אותה כשהייצור
--    המשיך לכתוב למקום הישן בחלון שבין החלקים — **וכאן הוא הפסיק לכתוב ב-`N2ג`**, וזה אומת
--    בחבילה החיה. ‏`child_parent_mismatch = 0` הוא הראיה שאין דלתא לסנכרן.
--
-- 🗑️ **ומה שמת יחד עם המיגרציה הזו, בכוונה:** בדיקה §10 ב-`docs/db_health_checks.md`
--    (‏`child_parent_mismatch`) **מאבדת את האב להשוות אליו.** היא מוחלפת בבדיקת-שריד
--    בשלושה מימדים — קריאות · כתיבות · בדיקות/E2E — לא בשניים.
--
-- 🔻 אימות אחרי ההחלה:
--   select count(*) from information_schema.columns
--    where table_schema='public' and table_name='customers'
--      and column_name in ('contact_name','phone','email');            -- ⇒ 0
--   select count(*) from public.customer_contacts where is_primary;    -- ⇒ 9 (לא נגענו)
--   select count(*) from public.customers;                             -- ⇒ 9 (לא נגענו)
-- =============================================================================

alter table public.customers drop column if exists contact_name;
alter table public.customers drop column if exists phone;
alter table public.customers drop column if exists email;

comment on table public.customers is
  'לקוחות (מודול 2). אנשי-הקשר חיים ב-customer_contacts, והראשי מסומן ב-is_primary — בדיוק אחד לכל לקוח (אינדקס חלקי + אכיפה ב-replace_customer_contacts). N2ד, 02/09/2026.';
